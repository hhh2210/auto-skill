{"schema_version": "example-pack/v1", "builder_version": "code-pack-builder/0.1", "pack_id": "codebench_vllm-project_vllm_models", "split_id": "codebench::vllm::models", "source": "CodeBench", "learning_problem": "few_shot_skill_induction", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "input_boundary": {"auto_skill_module_can_use": ["train_examples.task_input", "train_examples.materials", "train_examples.desired_output.text"], "must_not_use_for_induction": ["private rubrics", "private judge prompts", "heldout tasks", "heldout scores", "CI logs", "maintainer-only notes"]}, "train_examples": [{"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#14526/review_1986735257", "source": "CodeBench", "source_task_id": "vllm-project/vllm#14526", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/models/roberta.py\nReview Comment:\nUpdate this docstring?\n\nOriginal Code Context:\n@@ -160,6 +163,105 @@ def forward(self, features, **kwargs):\n         return x\n \n \n+def filter_secondary_weights(\n+    all_weights: Iterable[Tuple[str, torch.Tensor]],\n+    secondary_weights: list[str],\n+) -> Tuple[Iterable[Tuple[str, torch.Tensor]], Iterable[Tuple[str,\n+                                                              torch.Tensor]]]:\n+    all_weights1, all_weights2 = itertools.tee(all_weights)\n+\n+    def filtered(n):\n+        return any(n.startswith(f) for f in secondary_weights)\n+\n+    return ((n, w) for n, w in all_weights1 if filtered(n)), \\\n+           ((n, w) for n, w in all_weights2 if not filtered(n))\n+\n+\n+class M3SparsePooler(AllPool):\n+    \"\"\"A layer that pools specific information from hidden states.\n+\n+    This layer does the following:\n+    1. Extracts specific tokens or aggregates data based on pooling method.\n+    2. Normalizes output if specified.\n+    3. Returns structured results as `PoolerOutput`.\n+\n+    Attributes:\n+        pooling_type: The type of pooling to use.\n+        normalize: Whether to normalize the pooled data.\n+    \"\"\"\n", "materials": ["Update this docstring?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,15 +1,30 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n \n+import itertools\n from collections.abc import Iterable\n \n import torch\n from torch import nn\n from transformers import RobertaConfig\n \n-from vllm.config import ModelConfig, VllmConfig\n-from vllm.model_executor.layers.pooler import DispatchPooler\n+from vllm.config import ModelConfig, PoolerConfig, VllmConfig\n+from vllm.model_executor.layers.pooler import (\n+    BOSEOSFilter,\n+    DispatchPooler,\n+    Pooler,\n+)\n+from vllm.model_executor.layers.pooler.seqwise import (\n+    pooler_for_embed,\n+)\n+from vllm.model_executor.layers.pooler.tokwise import (\n+    AllPool,\n+    pooler_for_token_classify,\n+    pooler_for_token_embed,\n+)\n from vllm.model_executor.layers.vocab_parallel_embedding import VocabParallelEmbedding\n+from vllm.model_executor.model_loader.default_loader import DefaultModelLoader\n+from vllm.model_executor.model_loader.weight_utils import default_weight_loader\n from vllm.model_executor.models.bert import (\n     TOKEN_TYPE_SHIFT,\n     BertEmbeddingModel,\n@@ -149,6 +164,98 @@ def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]):\n         return loader.load_weights(weights_list, mapper=mapper)\n \n \n+def filter_secondary_weights(\n+    all_weights: Iterable[tuple[str, torch.Tensor]],\n+    secondary_weights: list[str],\n+) -> tuple[Iterable[tuple[str, torch.Tensor]], Iterable[tuple[str, torch.Tensor]]]:\n+    all_weights1, all_weights2 = itertools.tee(all_weights)\n+\n+    def filtered(n):\n+        return any(n.startswith(f) for f in secondary_weights)\n+\n+    return ((n, w) for n, w in all_weights1 if filtered(n)), (\n+        (n, w) for n, w in all_weights2 if not filtered(n)\n+    )\n+\n+\n+class BgeM3EmbeddingModel(RobertaEmbeddingModel):\n+    \"\"\"A model that extends RobertaEmbeddingModel with sparse embeddings.\n+\n+    This class supports loading an additional sparse_linear.pt file\n+    to create sparse embeddings as described in https://arxiv.org/abs/2402.03216\n+    \"\"\"\n+\n+    def __init__(self, *, vllm_config: VllmConfig, prefix: str = \"\"):\n+        self.hidden_size = vllm_config.model_config.hf_config.hidden_size\n+\n+        model_config = vllm_config.model_config\n+        self.head_dtype = model_config.head_dtype\n+        self.bos_token_id = model_config.hf_config.bos_token_id\n+        self.eos_token_id = model_config.hf_config.eos_token_id\n+\n+        super().__init__(vllm_config=vllm_config, prefix=prefix)\n+        self.secondary_weight_prefixes = [\"sparse_linear.\", \"colbert_linear.\"]\n+        self.secondary_weight_files = [\n+            prefix + \"pt\" for prefix in self.secondary_weight_prefixes\n+        ]\n+\n+        self.secondary_weights = [\n+            DefaultModelLoader.Source(\n+                model_or_path=vllm_config.model_config.model,\n+                revision=None,\n+                prefix=prefix,\n+                allow_patterns_overrides=[filename],\n+            )\n+            for filename, prefix in zip(\n+                self.secondary_weight_files, self.secondary_weight_prefixes\n+            )\n+        ]\n+\n+    def _build_pooler(self, pooler_config: PoolerConfig) -> Pooler:\n+        self.sparse_linear = nn.Linear(self.hidden_size, 1, dtype=self.head_dtype)\n+        self.colbert_linear = nn.Linear(\n+            self.hidden_size, self.hidden_size, dtype=self.head_dtype\n+        )\n+\n+        return DispatchPooler(\n+            {\n+                \"embed\": pooler_for_embed(pooler_config),\n+                \"token_embed\": BOSEOSFilter(\n+                    pooler_for_token_embed(pooler_config, self.colbert_linear),\n+                    self.bos_token_id,\n+                    # for some reason m3 only filters the bos for colbert vectors\n+                ),\n+                \"token_classify\": BOSEOSFilter(\n+                    pooler_for_token_classify(\n+                        pooler_config,\n+                        pooling=AllPool(),\n+                        classifier=self.sparse_linear,\n+                        act_fn=torch.relu,\n+                    ),\n+                    self.bos_token_id,\n+                    self.eos_token_id,\n+                ),\n+            }\n+        )\n+\n+    def load_weights(self, all_weights: Iterable[tuple[str, torch.Tensor]]):\n+        secondary, weights = filter_secondary_weights(\n+            all_weights, self.secondary_weight_prefixes\n+        )\n+\n+        super().load_weights(weights)\n+\n+        params_dict = dict(self.named_parameters())\n+\n+        for name, loaded_weight in secondary:\n+            if any(\n+                name.startswith(prefix) for prefix in self.secondary_weight_prefixes\n+            ):\n+                param = params_dict[name]\n+                weight_loader = getattr(param, \"weight_loader\", default_weight_loader)\n+                weight_loader(param, loaded_weight)\n+\n+\n @default_pooling_type(seq_pooling_type=\"CLS\")\n class RobertaForSequenceClassification(nn.Module, SupportsCrossEncoding):\n     \"\"\"A model that uses Roberta to provide embedding functionalities.", "patch_sha256": "5aef338cf27137ca"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#14526/review_1986743846", "source": "CodeBench", "source_task_id": "vllm-project/vllm#14526", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/models/roberta.py\nReview Comment:\nI think it would be cleaner if we define a separate architecture (like `RobertaSparseEmbeddingModel`) and require the user to pass `--hf-overrides` explicitly to enable this.\n\nOriginal Code Context:\n@@ -171,14 +273,53 @@ class RobertaEmbeddingModel(BertEmbeddingModel):\n        _pooler: An instance of Pooler used for pooling operations.\n    \"\"\"\n \n+    def __init__(self, *, vllm_config: VllmConfig, prefix: str = \"\"):\n+\n+        self.is_m3 = vllm_config.model_config.model == \"BAAI/bge-m3\"\n+        self.hidden_size = vllm_config.model_config.hf_config.hidden_size\n+\n+        self.bos_token_id = vllm_config.model_config.hf_config.bos_token_id\n+        self.eos_token_id = vllm_config.model_config.hf_config.eos_token_id\n+\n+        super().__init__(vllm_config=vllm_config, prefix=prefix)\n+\n+        self.secondary_weights = []\n+        self.secondary_weight_names = []\n+\n+        if self.is_m3:\n", "materials": ["I think it would be cleaner if we define a separate architecture (like `RobertaSparseEmbeddingModel`) and require the user to pass `--hf-overrides` explicitly to enable this."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,15 +1,30 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n \n+import itertools\n from collections.abc import Iterable\n \n import torch\n from torch import nn\n from transformers import RobertaConfig\n \n-from vllm.config import ModelConfig, VllmConfig\n-from vllm.model_executor.layers.pooler import DispatchPooler\n+from vllm.config import ModelConfig, PoolerConfig, VllmConfig\n+from vllm.model_executor.layers.pooler import (\n+    BOSEOSFilter,\n+    DispatchPooler,\n+    Pooler,\n+)\n+from vllm.model_executor.layers.pooler.seqwise import (\n+    pooler_for_embed,\n+)\n+from vllm.model_executor.layers.pooler.tokwise import (\n+    AllPool,\n+    pooler_for_token_classify,\n+    pooler_for_token_embed,\n+)\n from vllm.model_executor.layers.vocab_parallel_embedding import VocabParallelEmbedding\n+from vllm.model_executor.model_loader.default_loader import DefaultModelLoader\n+from vllm.model_executor.model_loader.weight_utils import default_weight_loader\n from vllm.model_executor.models.bert import (\n     TOKEN_TYPE_SHIFT,\n     BertEmbeddingModel,\n@@ -149,6 +164,98 @@ def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]):\n         return loader.load_weights(weights_list, mapper=mapper)\n \n \n+def filter_secondary_weights(\n+    all_weights: Iterable[tuple[str, torch.Tensor]],\n+    secondary_weights: list[str],\n+) -> tuple[Iterable[tuple[str, torch.Tensor]], Iterable[tuple[str, torch.Tensor]]]:\n+    all_weights1, all_weights2 = itertools.tee(all_weights)\n+\n+    def filtered(n):\n+        return any(n.startswith(f) for f in secondary_weights)\n+\n+    return ((n, w) for n, w in all_weights1 if filtered(n)), (\n+        (n, w) for n, w in all_weights2 if not filtered(n)\n+    )\n+\n+\n+class BgeM3EmbeddingModel(RobertaEmbeddingModel):\n+    \"\"\"A model that extends RobertaEmbeddingModel with sparse embeddings.\n+\n+    This class supports loading an additional sparse_linear.pt file\n+    to create sparse embeddings as described in https://arxiv.org/abs/2402.03216\n+    \"\"\"\n+\n+    def __init__(self, *, vllm_config: VllmConfig, prefix: str = \"\"):\n+        self.hidden_size = vllm_config.model_config.hf_config.hidden_size\n+\n+        model_config = vllm_config.model_config\n+        self.head_dtype = model_config.head_dtype\n+        self.bos_token_id = model_config.hf_config.bos_token_id\n+        self.eos_token_id = model_config.hf_config.eos_token_id\n+\n+        super().__init__(vllm_config=vllm_config, prefix=prefix)\n+        self.secondary_weight_prefixes = [\"sparse_linear.\", \"colbert_linear.\"]\n+        self.secondary_weight_files = [\n+            prefix + \"pt\" for prefix in self.secondary_weight_prefixes\n+        ]\n+\n+        self.secondary_weights = [\n+            DefaultModelLoader.Source(\n+                model_or_path=vllm_config.model_config.model,\n+                revision=None,\n+                prefix=prefix,\n+                allow_patterns_overrides=[filename],\n+            )\n+            for filename, prefix in zip(\n+                self.secondary_weight_files, self.secondary_weight_prefixes\n+            )\n+        ]\n+\n+    def _build_pooler(self, pooler_config: PoolerConfig) -> Pooler:\n+        self.sparse_linear = nn.Linear(self.hidden_size, 1, dtype=self.head_dtype)\n+        self.colbert_linear = nn.Linear(\n+            self.hidden_size, self.hidden_size, dtype=self.head_dtype\n+        )\n+\n+        return DispatchPooler(\n+            {\n+                \"embed\": pooler_for_embed(pooler_config),\n+                \"token_embed\": BOSEOSFilter(\n+                    pooler_for_token_embed(pooler_config, self.colbert_linear),\n+                    self.bos_token_id,\n+                    # for some reason m3 only filters the bos for colbert vectors\n+                ),\n+                \"token_classify\": BOSEOSFilter(\n+                    pooler_for_token_classify(\n+                        pooler_config,\n+                        pooling=AllPool(),\n+                        classifier=self.sparse_linear,\n+                        act_fn=torch.relu,\n+                    ),\n+                    self.bos_token_id,\n+                    self.eos_token_id,\n+                ),\n+            }\n+        )\n+\n+    def load_weights(self, all_weights: Iterable[tuple[str, torch.Tensor]]):\n+        secondary, weights = filter_secondary_weights(\n+            all_weights, self.secondary_weight_prefixes\n+        )\n+\n+        super().load_weights(weights)\n+\n+        params_dict = dict(self.named_parameters())\n+\n+        for name, loaded_weight in secondary:\n+            if any(\n+                name.startswith(prefix) for prefix in self.secondary_weight_prefixes\n+            ):\n+                param = params_dict[name]\n+                weight_loader = getattr(param, \"weight_loader\", default_weight_loader)\n+                weight_loader(param, loaded_weight)\n+\n+\n @default_pooling_type(seq_pooling_type=\"CLS\")\n class RobertaForSequenceClassification(nn.Module, SupportsCrossEncoding):\n     \"\"\"A model that uses Roberta to provide embedding functionalities.", "patch_sha256": "5aef338cf27137ca"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#14526/review_2437929671", "source": "CodeBench", "source_task_id": "vllm-project/vllm#14526", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/models/roberta.py\nReview Comment:\nCool !\r\n\r\nBGE-M3 Multi-Functionality:\r\n- embed for dense retrieval\r\n- token_embed for multi-vector retrieval\r\n- token_classify for sparse retrieval\r\n\r\nNothing stops us from using a plugin task to output everything at once. (after #26973 landing)\r\n\r\nThis way, BGE-M3 will be the best demonstration of the flexibility of our new pooler API.\r\n\r\n@DarkLight1337 You must come and see this\r\n\r\n***\r\n\r\nPlease add examples to demonstrate how users can use it. As well as adding tests to guard this feature\r\n\n\nOriginal Code Context:\n@@ -160,6 +164,83 @@ def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]):\n         return loader.load_weights(weights_list, mapper=mapper)\n \n \n+def filter_secondary_weights(\n+    all_weights: Iterable[tuple[str, torch.Tensor]],\n+    secondary_weights: list[str],\n+) -> tuple[Iterable[tuple[str, torch.Tensor]], Iterable[tuple[str, torch.Tensor]]]:\n+    all_weights1, all_weights2 = itertools.tee(all_weights)\n+\n+    def filtered(n):\n+        return any(n.startswith(f) for f in secondary_weights)\n+\n+    return ((n, w) for n, w in all_weights1 if filtered(n)), (\n+        (n, w) for n, w in all_weights2 if not filtered(n)\n+    )\n+\n+\n+class BgeM3EmbeddingModel(RobertaEmbeddingModel):\n+    \"\"\"A model that extends RobertaEmbeddingModel with sparse embeddings.\n+\n+    This class supports loading an additional sparse_linear.pt file\n+    to create sparse embeddings as described in https://arxiv.org/abs/2402.03216\n+    \"\"\"\n+\n+    def __init__(self, *, vllm_config: VllmConfig, prefix: str = \"\"):\n+        self.hidden_size = vllm_config.model_config.hf_config.hidden_size\n+\n+        model_config = vllm_config.model_config\n+        self.head_dtype = model_config.head_dtype\n+        self.bos_token_id = model_config.hf_config.bos_token_id\n+        self.eos_token_id = model_config.hf_config.eos_token_id\n+\n+        super().__init__(vllm_config=vllm_config, prefix=prefix)\n+        self.secondary_weight_prefix = \"sparse_linear.\"\n+\n+        self.secondary_weights = [\n+            DefaultModelLoader.Source(\n+                model_or_path=vllm_config.model_config.model,\n+                revision=None,\n+                prefix=self.secondary_weight_prefix,\n+                allow_patterns_overrides=[\"sparse_linear.pt\"],\n+            )\n+        ]\n+\n+    def _build_pooler(self, pooler_config: PoolerConfig) -> Pooler:\n+        self.sparse_linear = nn.Linear(self.hidden_size, 1, dtype=self.head_dtype)\n+        return DispatchPooler(\n+            {\n+                \"embed\": Pooler.for_embed(pooler_config),\n+                \"token_embed\": BOSEOSFilter(\n+                    Pooler.for_token_embed(pooler_config),\n+                    self.bos_token_id,\n+                    self.eos_token_id,\n+                ),\n+                \"token_classify\": BOSEOSFilter(\n+                    Pooler.for_token_classify(\n+                        pooler_config, classifier=self.sparse_linear, act_fn=torch.relu\n+                    ),\n+                    self.bos_token_id,\n+                    self.eos_token_id,\n+                ),\n+            }\n+        )\n", "materials": ["Cool !\r\n\r\nBGE-M3 Multi-Functionality:\r\n- embed for dense retrieval\r\n- token_embed for multi-vector retrieval\r\n- token_classify for sparse retrieval\r\n\r\nNothing stops us from using a plugin task to output everything at once. (after #26973 landing)\r\n\r\nThis way, BGE-M3 will be the best demonstration of the flexibility of our new pooler API.\r\n\r\n@DarkLight1337 You must come and see this\r\n\r\n***\r\n\r\nPlease add examples to demonstrate how users can use it. As well as adding tests to guard this feature\r\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,15 +1,30 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n \n+import itertools\n from collections.abc import Iterable\n \n import torch\n from torch import nn\n from transformers import RobertaConfig\n \n-from vllm.config import ModelConfig, VllmConfig\n-from vllm.model_executor.layers.pooler import DispatchPooler\n+from vllm.config import ModelConfig, PoolerConfig, VllmConfig\n+from vllm.model_executor.layers.pooler import (\n+    BOSEOSFilter,\n+    DispatchPooler,\n+    Pooler,\n+)\n+from vllm.model_executor.layers.pooler.seqwise import (\n+    pooler_for_embed,\n+)\n+from vllm.model_executor.layers.pooler.tokwise import (\n+    AllPool,\n+    pooler_for_token_classify,\n+    pooler_for_token_embed,\n+)\n from vllm.model_executor.layers.vocab_parallel_embedding import VocabParallelEmbedding\n+from vllm.model_executor.model_loader.default_loader import DefaultModelLoader\n+from vllm.model_executor.model_loader.weight_utils import default_weight_loader\n from vllm.model_executor.models.bert import (\n     TOKEN_TYPE_SHIFT,\n     BertEmbeddingModel,\n@@ -149,6 +164,98 @@ def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]):\n         return loader.load_weights(weights_list, mapper=mapper)\n \n \n+def filter_secondary_weights(\n+    all_weights: Iterable[tuple[str, torch.Tensor]],\n+    secondary_weights: list[str],\n+) -> tuple[Iterable[tuple[str, torch.Tensor]], Iterable[tuple[str, torch.Tensor]]]:\n+    all_weights1, all_weights2 = itertools.tee(all_weights)\n+\n+    def filtered(n):\n+        return any(n.startswith(f) for f in secondary_weights)\n+\n+    return ((n, w) for n, w in all_weights1 if filtered(n)), (\n+        (n, w) for n, w in all_weights2 if not filtered(n)\n+    )\n+\n+\n+class BgeM3EmbeddingModel(RobertaEmbeddingModel):\n+    \"\"\"A model that extends RobertaEmbeddingModel with sparse embeddings.\n+\n+    This class supports loading an additional sparse_linear.pt file\n+    to create sparse embeddings as described in https://arxiv.org/abs/2402.03216\n+    \"\"\"\n+\n+    def __init__(self, *, vllm_config: VllmConfig, prefix: str = \"\"):\n+        self.hidden_size = vllm_config.model_config.hf_config.hidden_size\n+\n+        model_config = vllm_config.model_config\n+        self.head_dtype = model_config.head_dtype\n+        self.bos_token_id = model_config.hf_config.bos_token_id\n+        self.eos_token_id = model_config.hf_config.eos_token_id\n+\n+        super().__init__(vllm_config=vllm_config, prefix=prefix)\n+        self.secondary_weight_prefixes = [\"sparse_linear.\", \"colbert_linear.\"]\n+        self.secondary_weight_files = [\n+            prefix + \"pt\" for prefix in self.secondary_weight_prefixes\n+        ]\n+\n+        self.secondary_weights = [\n+            DefaultModelLoader.Source(\n+                model_or_path=vllm_config.model_config.model,\n+                revision=None,\n+                prefix=prefix,\n+                allow_patterns_overrides=[filename],\n+            )\n+            for filename, prefix in zip(\n+                self.secondary_weight_files, self.secondary_weight_prefixes\n+            )\n+        ]\n+\n+    def _build_pooler(self, pooler_config: PoolerConfig) -> Pooler:\n+        self.sparse_linear = nn.Linear(self.hidden_size, 1, dtype=self.head_dtype)\n+        self.colbert_linear = nn.Linear(\n+            self.hidden_size, self.hidden_size, dtype=self.head_dtype\n+        )\n+\n+        return DispatchPooler(\n+            {\n+                \"embed\": pooler_for_embed(pooler_config),\n+                \"token_embed\": BOSEOSFilter(\n+                    pooler_for_token_embed(pooler_config, self.colbert_linear),\n+                    self.bos_token_id,\n+                    # for some reason m3 only filters the bos for colbert vectors\n+                ),\n+                \"token_classify\": BOSEOSFilter(\n+                    pooler_for_token_classify(\n+                        pooler_config,\n+                        pooling=AllPool(),\n+                        classifier=self.sparse_linear,\n+                        act_fn=torch.relu,\n+                    ),\n+                    self.bos_token_id,\n+                    self.eos_token_id,\n+                ),\n+            }\n+        )\n+\n+    def load_weights(self, all_weights: Iterable[tuple[str, torch.Tensor]]):\n+        secondary, weights = filter_secondary_weights(\n+            all_weights, self.secondary_weight_prefixes\n+        )\n+\n+        super().load_weights(weights)\n+\n+        params_dict = dict(self.named_parameters())\n+\n+        for name, loaded_weight in secondary:\n+            if any(\n+                name.startswith(prefix) for prefix in self.secondary_weight_prefixes\n+            ):\n+                param = params_dict[name]\n+                weight_loader = getattr(param, \"weight_loader\", default_weight_loader)\n+                weight_loader(param, loaded_weight)\n+\n+\n @default_pooling_type(seq_pooling_type=\"CLS\")\n class RobertaForSequenceClassification(nn.Module, SupportsCrossEncoding):\n     \"\"\"A model that uses Roberta to provide embedding functionalities.", "patch_sha256": "5aef338cf27137ca"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#14526/review_2440511863", "source": "CodeBench", "source_task_id": "vllm-project/vllm#14526", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/models/roberta.py\nReview Comment:\nI think the best is to use a plugin task to output everything all at once. This is more efficient.\r\n\n\nOriginal Code Context:\n@@ -160,6 +164,83 @@ def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]):\n         return loader.load_weights(weights_list, mapper=mapper)\n \n \n+def filter_secondary_weights(\n+    all_weights: Iterable[tuple[str, torch.Tensor]],\n+    secondary_weights: list[str],\n+) -> tuple[Iterable[tuple[str, torch.Tensor]], Iterable[tuple[str, torch.Tensor]]]:\n+    all_weights1, all_weights2 = itertools.tee(all_weights)\n+\n+    def filtered(n):\n+        return any(n.startswith(f) for f in secondary_weights)\n+\n+    return ((n, w) for n, w in all_weights1 if filtered(n)), (\n+        (n, w) for n, w in all_weights2 if not filtered(n)\n+    )\n+\n+\n+class BgeM3EmbeddingModel(RobertaEmbeddingModel):\n+    \"\"\"A model that extends RobertaEmbeddingModel with sparse embeddings.\n+\n+    This class supports loading an additional sparse_linear.pt file\n+    to create sparse embeddings as described in https://arxiv.org/abs/2402.03216\n+    \"\"\"\n+\n+    def __init__(self, *, vllm_config: VllmConfig, prefix: str = \"\"):\n+        self.hidden_size = vllm_config.model_config.hf_config.hidden_size\n+\n+        model_config = vllm_config.model_config\n+        self.head_dtype = model_config.head_dtype\n+        self.bos_token_id = model_config.hf_config.bos_token_id\n+        self.eos_token_id = model_config.hf_config.eos_token_id\n+\n+        super().__init__(vllm_config=vllm_config, prefix=prefix)\n+        self.secondary_weight_prefix = \"sparse_linear.\"\n+\n+        self.secondary_weights = [\n+            DefaultModelLoader.Source(\n+                model_or_path=vllm_config.model_config.model,\n+                revision=None,\n+                prefix=self.secondary_weight_prefix,\n+                allow_patterns_overrides=[\"sparse_linear.pt\"],\n+            )\n+        ]\n+\n+    def _build_pooler(self, pooler_config: PoolerConfig) -> Pooler:\n+        self.sparse_linear = nn.Linear(self.hidden_size, 1, dtype=self.head_dtype)\n+        return DispatchPooler(\n+            {\n+                \"embed\": Pooler.for_embed(pooler_config),\n+                \"token_embed\": BOSEOSFilter(\n+                    Pooler.for_token_embed(pooler_config),\n+                    self.bos_token_id,\n+                    self.eos_token_id,\n+                ),\n+                \"token_classify\": BOSEOSFilter(\n+                    Pooler.for_token_classify(\n+                        pooler_config, classifier=self.sparse_linear, act_fn=torch.relu\n+                    ),\n+                    self.bos_token_id,\n+                    self.eos_token_id,\n+                ),\n+            }\n+        )\n", "materials": ["I think the best is to use a plugin task to output everything all at once. This is more efficient.\r\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,15 +1,30 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n \n+import itertools\n from collections.abc import Iterable\n \n import torch\n from torch import nn\n from transformers import RobertaConfig\n \n-from vllm.config import ModelConfig, VllmConfig\n-from vllm.model_executor.layers.pooler import DispatchPooler\n+from vllm.config import ModelConfig, PoolerConfig, VllmConfig\n+from vllm.model_executor.layers.pooler import (\n+    BOSEOSFilter,\n+    DispatchPooler,\n+    Pooler,\n+)\n+from vllm.model_executor.layers.pooler.seqwise import (\n+    pooler_for_embed,\n+)\n+from vllm.model_executor.layers.pooler.tokwise import (\n+    AllPool,\n+    pooler_for_token_classify,\n+    pooler_for_token_embed,\n+)\n from vllm.model_executor.layers.vocab_parallel_embedding import VocabParallelEmbedding\n+from vllm.model_executor.model_loader.default_loader import DefaultModelLoader\n+from vllm.model_executor.model_loader.weight_utils import default_weight_loader\n from vllm.model_executor.models.bert import (\n     TOKEN_TYPE_SHIFT,\n     BertEmbeddingModel,\n@@ -149,6 +164,98 @@ def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]):\n         return loader.load_weights(weights_list, mapper=mapper)\n \n \n+def filter_secondary_weights(\n+    all_weights: Iterable[tuple[str, torch.Tensor]],\n+    secondary_weights: list[str],\n+) -> tuple[Iterable[tuple[str, torch.Tensor]], Iterable[tuple[str, torch.Tensor]]]:\n+    all_weights1, all_weights2 = itertools.tee(all_weights)\n+\n+    def filtered(n):\n+        return any(n.startswith(f) for f in secondary_weights)\n+\n+    return ((n, w) for n, w in all_weights1 if filtered(n)), (\n+        (n, w) for n, w in all_weights2 if not filtered(n)\n+    )\n+\n+\n+class BgeM3EmbeddingModel(RobertaEmbeddingModel):\n+    \"\"\"A model that extends RobertaEmbeddingModel with sparse embeddings.\n+\n+    This class supports loading an additional sparse_linear.pt file\n+    to create sparse embeddings as described in https://arxiv.org/abs/2402.03216\n+    \"\"\"\n+\n+    def __init__(self, *, vllm_config: VllmConfig, prefix: str = \"\"):\n+        self.hidden_size = vllm_config.model_config.hf_config.hidden_size\n+\n+        model_config = vllm_config.model_config\n+        self.head_dtype = model_config.head_dtype\n+        self.bos_token_id = model_config.hf_config.bos_token_id\n+        self.eos_token_id = model_config.hf_config.eos_token_id\n+\n+        super().__init__(vllm_config=vllm_config, prefix=prefix)\n+        self.secondary_weight_prefixes = [\"sparse_linear.\", \"colbert_linear.\"]\n+        self.secondary_weight_files = [\n+            prefix + \"pt\" for prefix in self.secondary_weight_prefixes\n+        ]\n+\n+        self.secondary_weights = [\n+            DefaultModelLoader.Source(\n+                model_or_path=vllm_config.model_config.model,\n+                revision=None,\n+                prefix=prefix,\n+                allow_patterns_overrides=[filename],\n+            )\n+            for filename, prefix in zip(\n+                self.secondary_weight_files, self.secondary_weight_prefixes\n+            )\n+        ]\n+\n+    def _build_pooler(self, pooler_config: PoolerConfig) -> Pooler:\n+        self.sparse_linear = nn.Linear(self.hidden_size, 1, dtype=self.head_dtype)\n+        self.colbert_linear = nn.Linear(\n+            self.hidden_size, self.hidden_size, dtype=self.head_dtype\n+        )\n+\n+        return DispatchPooler(\n+            {\n+                \"embed\": pooler_for_embed(pooler_config),\n+                \"token_embed\": BOSEOSFilter(\n+                    pooler_for_token_embed(pooler_config, self.colbert_linear),\n+                    self.bos_token_id,\n+                    # for some reason m3 only filters the bos for colbert vectors\n+                ),\n+                \"token_classify\": BOSEOSFilter(\n+                    pooler_for_token_classify(\n+                        pooler_config,\n+                        pooling=AllPool(),\n+                        classifier=self.sparse_linear,\n+                        act_fn=torch.relu,\n+                    ),\n+                    self.bos_token_id,\n+                    self.eos_token_id,\n+                ),\n+            }\n+        )\n+\n+    def load_weights(self, all_weights: Iterable[tuple[str, torch.Tensor]]):\n+        secondary, weights = filter_secondary_weights(\n+            all_weights, self.secondary_weight_prefixes\n+        )\n+\n+        super().load_weights(weights)\n+\n+        params_dict = dict(self.named_parameters())\n+\n+        for name, loaded_weight in secondary:\n+            if any(\n+                name.startswith(prefix) for prefix in self.secondary_weight_prefixes\n+            ):\n+                param = params_dict[name]\n+                weight_loader = getattr(param, \"weight_loader\", default_weight_loader)\n+                weight_loader(param, loaded_weight)\n+\n+\n @default_pooling_type(seq_pooling_type=\"CLS\")\n class RobertaForSequenceClassification(nn.Module, SupportsCrossEncoding):\n     \"\"\"A model that uses Roberta to provide embedding functionalities.", "patch_sha256": "5aef338cf27137ca"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2399998867", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu_model_runner.py\nReview Comment:\nI'm seeing the draft model loaded on all ranks when the target is running in tensor-parallel mode, even when the draft model config is set to TP=1. That doesn't seem like the intended behaviour to me. \n\nOriginal Code Context:\n@@ -281,6 +282,12 @@ def __init__(\n         if self.speculative_config and get_pp_group().is_last_rank:\n             if self.speculative_config.method == \"ngram\":\n                 self.drafter = NgramProposer(self.vllm_config)\n+            elif self.speculative_config.uses_draft_model():\n+                self.drafter = DraftModelProposer(\n+                    vllm_config=self.vllm_config,\n+                    device=self.device,\n+                    runner=self,\n+                )  # type: ignore\n", "materials": ["I'm seeing the draft model loaded on all ranks when the target is running in tensor-parallel mode, even when the draft model config is set to TP=1. That doesn't seem like the intended behaviour to me. "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -145,6 +145,7 @@\n from vllm.v1.sample.metadata import SamplingMetadata\n from vllm.v1.sample.rejection_sampler import RejectionSampler\n from vllm.v1.sample.sampler import Sampler\n+from vllm.v1.spec_decode.draft_model import DraftModelProposer\n from vllm.v1.spec_decode.eagle import EagleProposer\n from vllm.v1.spec_decode.medusa import MedusaProposer\n from vllm.v1.spec_decode.metadata import SpecDecodeMetadata\n@@ -432,10 +433,20 @@ def __init__(\n         # layers in the draft model.\n         if self.speculative_config and get_pp_group().is_last_rank:\n             self.drafter: (\n-                NgramProposer | SuffixDecodingProposer | EagleProposer | MedusaProposer\n+                NgramProposer\n+                | SuffixDecodingProposer\n+                | EagleProposer\n+                | DraftModelProposer\n+                | MedusaProposer\n             )\n             if self.speculative_config.method == \"ngram\":\n                 self.drafter = NgramProposer(self.vllm_config)\n+            elif self.speculative_config.uses_draft_model():\n+                self.drafter = DraftModelProposer(\n+                    vllm_config=self.vllm_config,\n+                    device=self.device,\n+                    runner=self,\n+                )\n             elif self.speculative_config.method == \"suffix\":\n                 self.drafter = SuffixDecodingProposer(self.vllm_config)\n             elif self.speculative_config.use_eagle():\n@@ -3443,10 +3454,13 @@ def propose_draft_token_ids(sampled_token_ids):\n                 spec_decode_common_attn_metadata.max_seq_len + self.num_spec_tokens\n                 <= self.effective_drafter_max_model_len\n             )\n-            if spec_config.use_eagle() and not spec_config.disable_padded_drafter_batch:\n-                # EAGLE speculative decoding can use the GPU sampled tokens\n+            use_gpu_toks = (\n+                spec_config.use_eagle() or spec_config.uses_draft_model()\n+            ) and not spec_config.disable_padded_drafter_batch\n+            if use_gpu_toks:\n+                # EAGLE/DraftModel speculative decoding can use the GPU sampled tokens\n                 # as inputs, and does not need to wait for bookkeeping to finish.\n-                assert isinstance(self.drafter, EagleProposer)\n+                assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n                 sampled_token_ids = sampler_output.sampled_token_ids\n                 if input_fits_in_drafter:\n                     propose_draft_token_ids(sampled_token_ids)\n@@ -3679,8 +3693,8 @@ def propose_draft_token_ids(\n                 target_hidden_states=hidden_states,\n                 sampling_metadata=sampling_metadata,\n             )\n-        elif spec_config.use_eagle():\n-            assert isinstance(self.drafter, EagleProposer)\n+        elif spec_config.use_eagle() or spec_config.uses_draft_model():\n+            assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n \n             if spec_config.disable_padded_drafter_batch:\n                 # When padded-batch is disabled, the sampled_token_ids should be\n@@ -4475,8 +4489,12 @@ def _dummy_run(\n             else:\n                 hidden_states = outputs\n \n-            if self.speculative_config and self.speculative_config.use_eagle():\n-                assert isinstance(self.drafter, EagleProposer)\n+            if self.speculative_config and (\n+                self.speculative_config.use_eagle()\n+                or self.speculative_config.uses_draft_model()\n+            ):\n+                assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n+                assert self.speculative_config is not None\n                 # Eagle currently only supports PIECEWISE cudagraphs.\n                 # Therefore only use cudagraphs if the main model uses PIECEWISE\n                 # NOTE(lucas): this is a hack, need to clean up.\n@@ -5652,8 +5670,11 @@ def initialize_kv_cache(self, kv_cache_config: KVCacheConfig) -> None:\n             kv_cache_config, kernel_block_sizes\n         )\n \n-        if self.speculative_config and self.speculative_config.use_eagle():\n-            assert isinstance(self.drafter, EagleProposer)\n+        if self.speculative_config and (\n+            self.speculative_config.use_eagle()\n+            or self.speculative_config.uses_draft_model()\n+        ):\n+            assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n             # validate all draft model layers belong to the same kv cache\n             # group\n             self.drafter.validate_same_kv_cache_group(kv_cache_config)", "patch_sha256": "edac1e210171a53d"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2400017330", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu_model_runner.py\nReview Comment:\nI haven't addressed this use case yet. Would you mind sharing the vllm command you are using? @smsegal \n\nOriginal Code Context:\n@@ -281,6 +282,12 @@ def __init__(\n         if self.speculative_config and get_pp_group().is_last_rank:\n             if self.speculative_config.method == \"ngram\":\n                 self.drafter = NgramProposer(self.vllm_config)\n+            elif self.speculative_config.uses_draft_model():\n+                self.drafter = DraftModelProposer(\n+                    vllm_config=self.vllm_config,\n+                    device=self.device,\n+                    runner=self,\n+                )  # type: ignore\n", "materials": ["I haven't addressed this use case yet. Would you mind sharing the vllm command you are using? @smsegal "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -145,6 +145,7 @@\n from vllm.v1.sample.metadata import SamplingMetadata\n from vllm.v1.sample.rejection_sampler import RejectionSampler\n from vllm.v1.sample.sampler import Sampler\n+from vllm.v1.spec_decode.draft_model import DraftModelProposer\n from vllm.v1.spec_decode.eagle import EagleProposer\n from vllm.v1.spec_decode.medusa import MedusaProposer\n from vllm.v1.spec_decode.metadata import SpecDecodeMetadata\n@@ -432,10 +433,20 @@ def __init__(\n         # layers in the draft model.\n         if self.speculative_config and get_pp_group().is_last_rank:\n             self.drafter: (\n-                NgramProposer | SuffixDecodingProposer | EagleProposer | MedusaProposer\n+                NgramProposer\n+                | SuffixDecodingProposer\n+                | EagleProposer\n+                | DraftModelProposer\n+                | MedusaProposer\n             )\n             if self.speculative_config.method == \"ngram\":\n                 self.drafter = NgramProposer(self.vllm_config)\n+            elif self.speculative_config.uses_draft_model():\n+                self.drafter = DraftModelProposer(\n+                    vllm_config=self.vllm_config,\n+                    device=self.device,\n+                    runner=self,\n+                )\n             elif self.speculative_config.method == \"suffix\":\n                 self.drafter = SuffixDecodingProposer(self.vllm_config)\n             elif self.speculative_config.use_eagle():\n@@ -3443,10 +3454,13 @@ def propose_draft_token_ids(sampled_token_ids):\n                 spec_decode_common_attn_metadata.max_seq_len + self.num_spec_tokens\n                 <= self.effective_drafter_max_model_len\n             )\n-            if spec_config.use_eagle() and not spec_config.disable_padded_drafter_batch:\n-                # EAGLE speculative decoding can use the GPU sampled tokens\n+            use_gpu_toks = (\n+                spec_config.use_eagle() or spec_config.uses_draft_model()\n+            ) and not spec_config.disable_padded_drafter_batch\n+            if use_gpu_toks:\n+                # EAGLE/DraftModel speculative decoding can use the GPU sampled tokens\n                 # as inputs, and does not need to wait for bookkeeping to finish.\n-                assert isinstance(self.drafter, EagleProposer)\n+                assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n                 sampled_token_ids = sampler_output.sampled_token_ids\n                 if input_fits_in_drafter:\n                     propose_draft_token_ids(sampled_token_ids)\n@@ -3679,8 +3693,8 @@ def propose_draft_token_ids(\n                 target_hidden_states=hidden_states,\n                 sampling_metadata=sampling_metadata,\n             )\n-        elif spec_config.use_eagle():\n-            assert isinstance(self.drafter, EagleProposer)\n+        elif spec_config.use_eagle() or spec_config.uses_draft_model():\n+            assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n \n             if spec_config.disable_padded_drafter_batch:\n                 # When padded-batch is disabled, the sampled_token_ids should be\n@@ -4475,8 +4489,12 @@ def _dummy_run(\n             else:\n                 hidden_states = outputs\n \n-            if self.speculative_config and self.speculative_config.use_eagle():\n-                assert isinstance(self.drafter, EagleProposer)\n+            if self.speculative_config and (\n+                self.speculative_config.use_eagle()\n+                or self.speculative_config.uses_draft_model()\n+            ):\n+                assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n+                assert self.speculative_config is not None\n                 # Eagle currently only supports PIECEWISE cudagraphs.\n                 # Therefore only use cudagraphs if the main model uses PIECEWISE\n                 # NOTE(lucas): this is a hack, need to clean up.\n@@ -5652,8 +5670,11 @@ def initialize_kv_cache(self, kv_cache_config: KVCacheConfig) -> None:\n             kv_cache_config, kernel_block_sizes\n         )\n \n-        if self.speculative_config and self.speculative_config.use_eagle():\n-            assert isinstance(self.drafter, EagleProposer)\n+        if self.speculative_config and (\n+            self.speculative_config.use_eagle()\n+            or self.speculative_config.uses_draft_model()\n+        ):\n+            assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n             # validate all draft model layers belong to the same kv cache\n             # group\n             self.drafter.validate_same_kv_cache_group(kv_cache_config)", "patch_sha256": "edac1e210171a53d"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2400085685", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu_model_runner.py\nReview Comment:\nSure, this can be observed via running Llama 3.1 8B instruct as the target model, and 3.2 1B as the drafter.\r\n\r\n```\r\nVLLM_SERVER_DEV_MODE=1 \\\r\nVLLM_USE_V1=1 \\\r\nCUDA_LAUNCH_BLOCKING=1 \\\r\nVLLM_ENABLE_V1_MULTIPROCESSING=0 \\\r\nCUDA_VISIBLE_DEVICES=0,1,2,3 \\\r\npython -m vllm.entrypoints.openai.api_server \\\r\n  --model meta-llama/Llama-3.1-8B-Instruct \\\r\n  --max-num-seqs 64 \\\r\n  --tensor_parallel_size 4 \\\r\n  --speculative_config '{\"method\": \"draft_model\", \"model\": \"meta-llama/Llama-3.2-1B-Instruct\", \"num_speculative_tokens\": 6}'\r\n```\n\nOriginal Code Context:\n@@ -281,6 +282,12 @@ def __init__(\n         if self.speculative_config and get_pp_group().is_last_rank:\n             if self.speculative_config.method == \"ngram\":\n                 self.drafter = NgramProposer(self.vllm_config)\n+            elif self.speculative_config.uses_draft_model():\n+                self.drafter = DraftModelProposer(\n+                    vllm_config=self.vllm_config,\n+                    device=self.device,\n+                    runner=self,\n+                )  # type: ignore\n", "materials": ["Sure, this can be observed via running Llama 3.1 8B instruct as the target model, and 3.2 1B as the drafter.\r\n\r\n```\r\nVLLM_SERVER_DEV_MODE=1 \\\r\nVLLM_USE_V1=1 \\\r\nCUDA_LAUNCH_BLOCKING=1 \\\r\nVLLM_ENABLE_V1_MULTIPROCESSING=0 \\\r\nCUDA_VISIBLE_DEVICES=0,1,2,3 \\\r\npython -m vllm.entrypoints.openai.api_server \\\r\n  --model meta-llama/Llama-3.1-8B-Instruct \\\r\n  --max-num-seqs 64 \\\r\n  --tensor_parallel_size 4 \\\r\n  --speculative_config '{\"method\": \"draft_model\", \"model\": \"meta-llama/Llama-3.2-1B-Instruct\", \"num_speculative_tokens\": 6}'\r\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -145,6 +145,7 @@\n from vllm.v1.sample.metadata import SamplingMetadata\n from vllm.v1.sample.rejection_sampler import RejectionSampler\n from vllm.v1.sample.sampler import Sampler\n+from vllm.v1.spec_decode.draft_model import DraftModelProposer\n from vllm.v1.spec_decode.eagle import EagleProposer\n from vllm.v1.spec_decode.medusa import MedusaProposer\n from vllm.v1.spec_decode.metadata import SpecDecodeMetadata\n@@ -432,10 +433,20 @@ def __init__(\n         # layers in the draft model.\n         if self.speculative_config and get_pp_group().is_last_rank:\n             self.drafter: (\n-                NgramProposer | SuffixDecodingProposer | EagleProposer | MedusaProposer\n+                NgramProposer\n+                | SuffixDecodingProposer\n+                | EagleProposer\n+                | DraftModelProposer\n+                | MedusaProposer\n             )\n             if self.speculative_config.method == \"ngram\":\n                 self.drafter = NgramProposer(self.vllm_config)\n+            elif self.speculative_config.uses_draft_model():\n+                self.drafter = DraftModelProposer(\n+                    vllm_config=self.vllm_config,\n+                    device=self.device,\n+                    runner=self,\n+                )\n             elif self.speculative_config.method == \"suffix\":\n                 self.drafter = SuffixDecodingProposer(self.vllm_config)\n             elif self.speculative_config.use_eagle():\n@@ -3443,10 +3454,13 @@ def propose_draft_token_ids(sampled_token_ids):\n                 spec_decode_common_attn_metadata.max_seq_len + self.num_spec_tokens\n                 <= self.effective_drafter_max_model_len\n             )\n-            if spec_config.use_eagle() and not spec_config.disable_padded_drafter_batch:\n-                # EAGLE speculative decoding can use the GPU sampled tokens\n+            use_gpu_toks = (\n+                spec_config.use_eagle() or spec_config.uses_draft_model()\n+            ) and not spec_config.disable_padded_drafter_batch\n+            if use_gpu_toks:\n+                # EAGLE/DraftModel speculative decoding can use the GPU sampled tokens\n                 # as inputs, and does not need to wait for bookkeeping to finish.\n-                assert isinstance(self.drafter, EagleProposer)\n+                assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n                 sampled_token_ids = sampler_output.sampled_token_ids\n                 if input_fits_in_drafter:\n                     propose_draft_token_ids(sampled_token_ids)\n@@ -3679,8 +3693,8 @@ def propose_draft_token_ids(\n                 target_hidden_states=hidden_states,\n                 sampling_metadata=sampling_metadata,\n             )\n-        elif spec_config.use_eagle():\n-            assert isinstance(self.drafter, EagleProposer)\n+        elif spec_config.use_eagle() or spec_config.uses_draft_model():\n+            assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n \n             if spec_config.disable_padded_drafter_batch:\n                 # When padded-batch is disabled, the sampled_token_ids should be\n@@ -4475,8 +4489,12 @@ def _dummy_run(\n             else:\n                 hidden_states = outputs\n \n-            if self.speculative_config and self.speculative_config.use_eagle():\n-                assert isinstance(self.drafter, EagleProposer)\n+            if self.speculative_config and (\n+                self.speculative_config.use_eagle()\n+                or self.speculative_config.uses_draft_model()\n+            ):\n+                assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n+                assert self.speculative_config is not None\n                 # Eagle currently only supports PIECEWISE cudagraphs.\n                 # Therefore only use cudagraphs if the main model uses PIECEWISE\n                 # NOTE(lucas): this is a hack, need to clean up.\n@@ -5652,8 +5670,11 @@ def initialize_kv_cache(self, kv_cache_config: KVCacheConfig) -> None:\n             kv_cache_config, kernel_block_sizes\n         )\n \n-        if self.speculative_config and self.speculative_config.use_eagle():\n-            assert isinstance(self.drafter, EagleProposer)\n+        if self.speculative_config and (\n+            self.speculative_config.use_eagle()\n+            or self.speculative_config.uses_draft_model()\n+        ):\n+            assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n             # validate all draft model layers belong to the same kv cache\n             # group\n             self.drafter.validate_same_kv_cache_group(kv_cache_config)", "patch_sha256": "edac1e210171a53d"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2688857041", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/model_loader/tensorizer_loader.py\nReview Comment:\nWhy is this needed?\n\nOriginal Code Context:\n@@ -134,7 +135,7 @@ def load_model(\n                     )\n             self.load_weights(model, model_config)\n             return model\n-        return self._load_model_serialized_cpu(vllm_config=vllm_config)\n+        return self._load_model_serialized_cpu(vllm_config=vllm_config, prefix=prefix)\n", "materials": ["Why is this needed?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -68,6 +68,7 @@ def _get_weights_iterator(\n     def _load_model_serialized_cpu(\n         self,\n         vllm_config: VllmConfig,\n+        prefix: str = \"\",\n     ) -> nn.Module:\n         \"\"\"Load a serialized model with tensorizer to the CPU.\n \n@@ -80,7 +81,7 @@ def _load_model_serialized_cpu(\n         model_config = vllm_config.model_config\n         with set_default_torch_dtype(model_config.dtype):\n             with torch.device(device_config.device):\n-                model = initialize_model(vllm_config=vllm_config)\n+                model = initialize_model(vllm_config=vllm_config, prefix=prefix)\n \n             model.load_weights(self._get_weights_iterator())\n         return model.eval()\n@@ -112,7 +113,7 @@ def load_weights(self, model: nn.Module, model_config: ModelConfig) -> None:\n             model.load_weights(self._get_weights_iterator())\n \n     def load_model(\n-        self, vllm_config: VllmConfig, model_config: ModelConfig\n+        self, vllm_config: VllmConfig, model_config: ModelConfig, prefix: str = \"\"\n     ) -> nn.Module:\n         parallel_config = vllm_config.parallel_config\n         self._verify_config(model_config, parallel_config)\n@@ -134,7 +135,7 @@ def load_model(\n                     )\n             self.load_weights(model, model_config)\n             return model\n-        return self._load_model_serialized_cpu(vllm_config=vllm_config)\n+        return self._load_model_serialized_cpu(vllm_config=vllm_config, prefix=prefix)\n \n     @staticmethod\n     def save_model(", "patch_sha256": "67aac4d45c5c1546"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2690492538", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/model_loader/tensorizer_loader.py\nReview Comment:\nWe load the draft-model with `initialize_model(..., prefix=\"draft_model\")` to prevent a collision of layer names with the target model (it raises an error). The code changes to the loader classes are all intended to pass this `prefix: str` argument down to `initialize_model()`.\n\nOriginal Code Context:\n@@ -134,7 +135,7 @@ def load_model(\n                     )\n             self.load_weights(model, model_config)\n             return model\n-        return self._load_model_serialized_cpu(vllm_config=vllm_config)\n+        return self._load_model_serialized_cpu(vllm_config=vllm_config, prefix=prefix)\n", "materials": ["We load the draft-model with `initialize_model(..., prefix=\"draft_model\")` to prevent a collision of layer names with the target model (it raises an error). The code changes to the loader classes are all intended to pass this `prefix: str` argument down to `initialize_model()`."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -68,6 +68,7 @@ def _get_weights_iterator(\n     def _load_model_serialized_cpu(\n         self,\n         vllm_config: VllmConfig,\n+        prefix: str = \"\",\n     ) -> nn.Module:\n         \"\"\"Load a serialized model with tensorizer to the CPU.\n \n@@ -80,7 +81,7 @@ def _load_model_serialized_cpu(\n         model_config = vllm_config.model_config\n         with set_default_torch_dtype(model_config.dtype):\n             with torch.device(device_config.device):\n-                model = initialize_model(vllm_config=vllm_config)\n+                model = initialize_model(vllm_config=vllm_config, prefix=prefix)\n \n             model.load_weights(self._get_weights_iterator())\n         return model.eval()\n@@ -112,7 +113,7 @@ def load_weights(self, model: nn.Module, model_config: ModelConfig) -> None:\n             model.load_weights(self._get_weights_iterator())\n \n     def load_model(\n-        self, vllm_config: VllmConfig, model_config: ModelConfig\n+        self, vllm_config: VllmConfig, model_config: ModelConfig, prefix: str = \"\"\n     ) -> nn.Module:\n         parallel_config = vllm_config.parallel_config\n         self._verify_config(model_config, parallel_config)\n@@ -134,7 +135,7 @@ def load_model(\n                     )\n             self.load_weights(model, model_config)\n             return model\n-        return self._load_model_serialized_cpu(vllm_config=vllm_config)\n+        return self._load_model_serialized_cpu(vllm_config=vllm_config, prefix=prefix)\n \n     @staticmethod\n     def save_model(", "patch_sha256": "67aac4d45c5c1546"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2678008342", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nThis is a great catch! I discussed this with @zou3519 and we should make `unified_kv_cache_update` fully functional (`mutated_args=[]`) and return a dummy tensor, which we then pass to attention. That will signal both the side-effect of kv_cache_update as well as the dependency chain between the kv update and attention.\n\nOriginal Code Context:\n@@ -792,6 +789,42 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    attn_metadata, self, kv_cache = get_attention_context(layer_name)\n+    assert hasattr(self.impl, \"do_kv_cache_update\"), (\n+        f\"{self.impl.__class__.__name__} does not support kv cache update\"\n+    )\n+    self.impl.do_kv_cache_update(\n+        self,\n+        key,\n+        value,\n+        kv_cache,\n+        attn_metadata,\n+    )\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    return\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[\"output\"],  # fake dependency to make sure op is not optimized out\n", "materials": ["This is a great catch! I discussed this with @zou3519 and we should make `unified_kv_cache_update` fully functional (`mutated_args=[]`) and return a dummy tensor, which we then pass to attention. That will signal both the side-effect of kv_cache_update as well as the dependency chain between the kv update and attention."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688569781", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu/model_runner.py\nReview Comment:\nwouldn't we need a dummy slot mapping if `skip_attn` is true?\n\nOriginal Code Context:\n@@ -284,6 +285,15 @@ def _dummy_run(\n             positions = input_batch.positions\n         else:\n             positions = input_batch.mrope_positions\n+\n+        if skip_attn:\n", "materials": ["wouldn't we need a dummy slot mapping if `skip_attn` is true?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -27,6 +27,7 @@\n from vllm.v1.worker.gpu.async_utils import AsyncOutput\n from vllm.v1.worker.gpu.attn_utils import (\n     build_attn_metadata,\n+    build_slot_mappings_by_layer,\n     get_kv_cache_spec,\n     init_attn_backend,\n     init_kv_cache,\n@@ -869,13 +870,22 @@ def execute_model(\n             if self.uses_mrope:\n                 assert input_batch.mrope_positions is not None\n                 positions = input_batch.mrope_positions\n+            slot_mappings = self.block_tables.compute_slot_mappings(\n+                input_batch.idx_mapping,\n+                input_batch.query_start_loc,\n+                input_batch.positions[: input_batch.num_tokens],\n+            )\n+            slot_mappings_by_layer = build_slot_mappings_by_layer(\n+                slot_mappings, self.kv_cache_config\n+            )\n             with set_forward_context(\n                 input_batch.attn_metadata,\n                 self.vllm_config,\n                 num_tokens=input_batch.num_tokens_after_padding,\n                 # TODO(woosuk): Support piecewise CUDA graph.\n                 cudagraph_runtime_mode=CUDAGraphMode.NONE,\n                 num_tokens_across_dp=num_tokens_across_dp,\n+                slot_mapping=slot_mappings_by_layer,\n             ):\n                 hidden_states = self.model(\n                     input_ids=input_batch.input_ids,", "patch_sha256": "f08cf5aed621bed5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688596904", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu/model_runner.py\nReview Comment:\nWhy can we leave this empty - I assume we always need to build this?\n\nOriginal Code Context:\n@@ -284,6 +285,15 @@ def _dummy_run(\n             positions = input_batch.positions\n         else:\n             positions = input_batch.mrope_positions\n+\n+        if skip_attn:\n+            slot_mappings_by_layer: dict[str, torch.Tensor] = {}\n", "materials": ["Why can we leave this empty - I assume we always need to build this?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -27,6 +27,7 @@\n from vllm.v1.worker.gpu.async_utils import AsyncOutput\n from vllm.v1.worker.gpu.attn_utils import (\n     build_attn_metadata,\n+    build_slot_mappings_by_layer,\n     get_kv_cache_spec,\n     init_attn_backend,\n     init_kv_cache,\n@@ -869,13 +870,22 @@ def execute_model(\n             if self.uses_mrope:\n                 assert input_batch.mrope_positions is not None\n                 positions = input_batch.mrope_positions\n+            slot_mappings = self.block_tables.compute_slot_mappings(\n+                input_batch.idx_mapping,\n+                input_batch.query_start_loc,\n+                input_batch.positions[: input_batch.num_tokens],\n+            )\n+            slot_mappings_by_layer = build_slot_mappings_by_layer(\n+                slot_mappings, self.kv_cache_config\n+            )\n             with set_forward_context(\n                 input_batch.attn_metadata,\n                 self.vllm_config,\n                 num_tokens=input_batch.num_tokens_after_padding,\n                 # TODO(woosuk): Support piecewise CUDA graph.\n                 cudagraph_runtime_mode=CUDAGraphMode.NONE,\n                 num_tokens_across_dp=num_tokens_across_dp,\n+                slot_mapping=slot_mappings_by_layer,\n             ):\n                 hidden_states = self.model(\n                     input_ids=input_batch.input_ids,", "patch_sha256": "f08cf5aed621bed5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688740579", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu/model_runner.py\nReview Comment:\nFixed in [d9e5493](https://github.com/vllm-project/vllm/pull/25954/commits/d9e54931ce4fc8952792504f92a61100986f34cb)\n\nOriginal Code Context:\n@@ -284,6 +285,15 @@ def _dummy_run(\n             positions = input_batch.positions\n         else:\n             positions = input_batch.mrope_positions\n+\n+        if skip_attn:\n+            slot_mappings_by_layer: dict[str, torch.Tensor] = {}\n", "materials": ["Fixed in [d9e5493](https://github.com/vllm-project/vllm/pull/25954/commits/d9e54931ce4fc8952792504f92a61100986f34cb)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -27,6 +27,7 @@\n from vllm.v1.worker.gpu.async_utils import AsyncOutput\n from vllm.v1.worker.gpu.attn_utils import (\n     build_attn_metadata,\n+    build_slot_mappings_by_layer,\n     get_kv_cache_spec,\n     init_attn_backend,\n     init_kv_cache,\n@@ -869,13 +870,22 @@ def execute_model(\n             if self.uses_mrope:\n                 assert input_batch.mrope_positions is not None\n                 positions = input_batch.mrope_positions\n+            slot_mappings = self.block_tables.compute_slot_mappings(\n+                input_batch.idx_mapping,\n+                input_batch.query_start_loc,\n+                input_batch.positions[: input_batch.num_tokens],\n+            )\n+            slot_mappings_by_layer = build_slot_mappings_by_layer(\n+                slot_mappings, self.kv_cache_config\n+            )\n             with set_forward_context(\n                 input_batch.attn_metadata,\n                 self.vllm_config,\n                 num_tokens=input_batch.num_tokens_after_padding,\n                 # TODO(woosuk): Support piecewise CUDA graph.\n                 cudagraph_runtime_mode=CUDAGraphMode.NONE,\n                 num_tokens_across_dp=num_tokens_across_dp,\n+                slot_mapping=slot_mappings_by_layer,\n             ):\n                 hidden_states = self.model(\n                     input_ids=input_batch.input_ids,", "patch_sha256": "f08cf5aed621bed5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688740819", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu/model_runner.py\nReview Comment:\nFixed in [d9e5493](https://github.com/vllm-project/vllm/pull/25954/commits/d9e54931ce4fc8952792504f92a61100986f34cb)\n\nOriginal Code Context:\n@@ -284,6 +285,15 @@ def _dummy_run(\n             positions = input_batch.positions\n         else:\n             positions = input_batch.mrope_positions\n+\n+        if skip_attn:\n", "materials": ["Fixed in [d9e5493](https://github.com/vllm-project/vllm/pull/25954/commits/d9e54931ce4fc8952792504f92a61100986f34cb)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -27,6 +27,7 @@\n from vllm.v1.worker.gpu.async_utils import AsyncOutput\n from vllm.v1.worker.gpu.attn_utils import (\n     build_attn_metadata,\n+    build_slot_mappings_by_layer,\n     get_kv_cache_spec,\n     init_attn_backend,\n     init_kv_cache,\n@@ -869,13 +870,22 @@ def execute_model(\n             if self.uses_mrope:\n                 assert input_batch.mrope_positions is not None\n                 positions = input_batch.mrope_positions\n+            slot_mappings = self.block_tables.compute_slot_mappings(\n+                input_batch.idx_mapping,\n+                input_batch.query_start_loc,\n+                input_batch.positions[: input_batch.num_tokens],\n+            )\n+            slot_mappings_by_layer = build_slot_mappings_by_layer(\n+                slot_mappings, self.kv_cache_config\n+            )\n             with set_forward_context(\n                 input_batch.attn_metadata,\n                 self.vllm_config,\n                 num_tokens=input_batch.num_tokens_after_padding,\n                 # TODO(woosuk): Support piecewise CUDA graph.\n                 cudagraph_runtime_mode=CUDAGraphMode.NONE,\n                 num_tokens_across_dp=num_tokens_across_dp,\n+                slot_mapping=slot_mappings_by_layer,\n             ):\n                 hidden_states = self.model(\n                     input_ids=input_batch.input_ids,", "patch_sha256": "f08cf5aed621bed5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2696199204", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/attention/cross_attention.py\nReview Comment:\n```suggestion\n```\n\nOriginal Code Context:\n@@ -106,18 +107,59 @@ def build(\n             )\n \n             # NOTE (NickLucche) use `new_metadata` instead of `common_*` (initial) here\n-            new_metadata.slot_mapping = _get_cross_slot_mapping(\n+            slot_mapping = _get_cross_slot_mapping(\n                 new_metadata.encoder_seq_lens_cpu,\n                 new_metadata.block_table_tensor,\n                 self.kv_cache_spec,\n                 self.device,\n             )\n-            return super().build(common_prefix_len, new_metadata, fast_build)\n+            print(\"build slot_mapping\", slot_mapping)\n", "materials": ["```suggestion\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -15,7 +15,7 @@\n     AttentionMetadata,\n     AttentionType,\n     CommonAttentionMetadata,\n-    subclass_attention_backend,\n+    subclass_attention_backend_with_overrides,\n )\n from vllm.v1.attention.selector import get_attn_backend\n from vllm.v1.kv_cache_interface import CrossAttentionSpec, KVCacheSpec\n@@ -72,6 +72,7 @@ def create_cross_attention_backend(\n ) -> type[AttentionBackend]:\n     prefix = \"CrossAttention_\"\n     underlying_builder = underlying_attn_backend.get_builder_cls()\n+    underlying_impl = underlying_attn_backend.get_impl_cls()\n \n     class CrossAttentionBuilder(underlying_builder):  # type: ignore\n         def build(\n@@ -106,18 +107,60 @@ def build(\n             )\n \n             # NOTE (NickLucche) use `new_metadata` instead of `common_*` (initial) here\n-            new_metadata.slot_mapping = _get_cross_slot_mapping(\n+            slot_mapping = _get_cross_slot_mapping(\n                 new_metadata.encoder_seq_lens_cpu,\n                 new_metadata.block_table_tensor,\n                 self.kv_cache_spec,\n                 self.device,\n             )\n-            return super().build(common_prefix_len, new_metadata, fast_build)\n+            attn_metadata = super().build(common_prefix_len, new_metadata, fast_build)\n+            attn_metadata.slot_mapping = slot_mapping\n+            return attn_metadata\n+\n+    # NOTE(Lucas): we need a custom impl so we can use the slot-mapping computed by\n+    # `CrossAttentionBuilder` instead of the one computed by `BlockTable`\n+    # (gpu_model_runner)\n+    class CrossAttentionImpl(underlying_impl):  # type: ignore[valid-type,misc]\n+        def forward(\n+            self,\n+            layer: torch.nn.Module,\n+            query: torch.Tensor,\n+            key: torch.Tensor,\n+            value: torch.Tensor,\n+            kv_cache: torch.Tensor,\n+            attn_metadata: AttentionMetadata,\n+            output: torch.Tensor | None = None,\n+            output_scale: torch.Tensor | None = None,\n+            output_block_scale: torch.Tensor | None = None,\n+        ) -> torch.Tensor:\n+            if (\n+                not underlying_attn_backend.forward_includes_kv_cache_update\n+                and attn_metadata is not None\n+            ):\n+                self.do_kv_cache_update(\n+                    layer, key, value, kv_cache, attn_metadata.slot_mapping\n+                )\n+\n+            return super().forward(\n+                layer,\n+                query,\n+                key,\n+                value,\n+                kv_cache,\n+                attn_metadata,\n+                output,\n+                output_scale,\n+                output_block_scale,\n+            )\n \n-    attn_backend = subclass_attention_backend(\n+    attn_backend = subclass_attention_backend_with_overrides(\n         name_prefix=prefix,\n         attention_backend_cls=underlying_attn_backend,\n-        builder_cls=CrossAttentionBuilder,\n+        overrides={\n+            \"get_builder_cls\": lambda: CrossAttentionBuilder,\n+            \"get_impl_cls\": lambda: CrossAttentionImpl,\n+            \"forward_includes_kv_cache_update\": True,\n+        },\n     )\n \n     return attn_backend", "patch_sha256": "d5e100e7830e6d38"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2696199733", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/attention/cross_attention.py\nReview Comment:\n@LucasWilkinson what did you mean here?\n\nOriginal Code Context:\n@@ -106,18 +107,59 @@ def build(\n             )\n \n             # NOTE (NickLucche) use `new_metadata` instead of `common_*` (initial) here\n-            new_metadata.slot_mapping = _get_cross_slot_mapping(\n+            slot_mapping = _get_cross_slot_mapping(\n                 new_metadata.encoder_seq_lens_cpu,\n                 new_metadata.block_table_tensor,\n                 self.kv_cache_spec,\n                 self.device,\n             )\n-            return super().build(common_prefix_len, new_metadata, fast_build)\n+            print(\"build slot_mapping\", slot_mapping)\n+            attn_metadata = super().build(common_prefix_len, new_metadata, fast_build)\n+            attn_metadata.slot_mapping = slot_mapping\n+            return attn_metadata\n+\n+    # NOTE(Lucas): we need\n", "materials": ["@LucasWilkinson what did you mean here?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -15,7 +15,7 @@\n     AttentionMetadata,\n     AttentionType,\n     CommonAttentionMetadata,\n-    subclass_attention_backend,\n+    subclass_attention_backend_with_overrides,\n )\n from vllm.v1.attention.selector import get_attn_backend\n from vllm.v1.kv_cache_interface import CrossAttentionSpec, KVCacheSpec\n@@ -72,6 +72,7 @@ def create_cross_attention_backend(\n ) -> type[AttentionBackend]:\n     prefix = \"CrossAttention_\"\n     underlying_builder = underlying_attn_backend.get_builder_cls()\n+    underlying_impl = underlying_attn_backend.get_impl_cls()\n \n     class CrossAttentionBuilder(underlying_builder):  # type: ignore\n         def build(\n@@ -106,18 +107,60 @@ def build(\n             )\n \n             # NOTE (NickLucche) use `new_metadata` instead of `common_*` (initial) here\n-            new_metadata.slot_mapping = _get_cross_slot_mapping(\n+            slot_mapping = _get_cross_slot_mapping(\n                 new_metadata.encoder_seq_lens_cpu,\n                 new_metadata.block_table_tensor,\n                 self.kv_cache_spec,\n                 self.device,\n             )\n-            return super().build(common_prefix_len, new_metadata, fast_build)\n+            attn_metadata = super().build(common_prefix_len, new_metadata, fast_build)\n+            attn_metadata.slot_mapping = slot_mapping\n+            return attn_metadata\n+\n+    # NOTE(Lucas): we need a custom impl so we can use the slot-mapping computed by\n+    # `CrossAttentionBuilder` instead of the one computed by `BlockTable`\n+    # (gpu_model_runner)\n+    class CrossAttentionImpl(underlying_impl):  # type: ignore[valid-type,misc]\n+        def forward(\n+            self,\n+            layer: torch.nn.Module,\n+            query: torch.Tensor,\n+            key: torch.Tensor,\n+            value: torch.Tensor,\n+            kv_cache: torch.Tensor,\n+            attn_metadata: AttentionMetadata,\n+            output: torch.Tensor | None = None,\n+            output_scale: torch.Tensor | None = None,\n+            output_block_scale: torch.Tensor | None = None,\n+        ) -> torch.Tensor:\n+            if (\n+                not underlying_attn_backend.forward_includes_kv_cache_update\n+                and attn_metadata is not None\n+            ):\n+                self.do_kv_cache_update(\n+                    layer, key, value, kv_cache, attn_metadata.slot_mapping\n+                )\n+\n+            return super().forward(\n+                layer,\n+                query,\n+                key,\n+                value,\n+                kv_cache,\n+                attn_metadata,\n+                output,\n+                output_scale,\n+                output_block_scale,\n+            )\n \n-    attn_backend = subclass_attention_backend(\n+    attn_backend = subclass_attention_backend_with_overrides(\n         name_prefix=prefix,\n         attention_backend_cls=underlying_attn_backend,\n-        builder_cls=CrossAttentionBuilder,\n+        overrides={\n+            \"get_builder_cls\": lambda: CrossAttentionBuilder,\n+            \"get_impl_cls\": lambda: CrossAttentionImpl,\n+            \"forward_includes_kv_cache_update\": True,\n+        },\n     )\n \n     return attn_backend", "patch_sha256": "d5e100e7830e6d38"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2696224975", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/attention/cross_attention.py\nReview Comment:\n```suggestion\r\n    # NOTE(Lucas): we need a custom impl so we can use the slot-mapping computed by\r\n    # `CrossAttentionBuilder` instead of the one computed by `BlockTable` \r\n    # (gpu_model_runner)\r\n```\n\nOriginal Code Context:\n@@ -106,18 +107,59 @@ def build(\n             )\n \n             # NOTE (NickLucche) use `new_metadata` instead of `common_*` (initial) here\n-            new_metadata.slot_mapping = _get_cross_slot_mapping(\n+            slot_mapping = _get_cross_slot_mapping(\n                 new_metadata.encoder_seq_lens_cpu,\n                 new_metadata.block_table_tensor,\n                 self.kv_cache_spec,\n                 self.device,\n             )\n-            return super().build(common_prefix_len, new_metadata, fast_build)\n+            print(\"build slot_mapping\", slot_mapping)\n+            attn_metadata = super().build(common_prefix_len, new_metadata, fast_build)\n+            attn_metadata.slot_mapping = slot_mapping\n+            return attn_metadata\n+\n+    # NOTE(Lucas): we need\n", "materials": ["```suggestion\r\n    # NOTE(Lucas): we need a custom impl so we can use the slot-mapping computed by\r\n    # `CrossAttentionBuilder` instead of the one computed by `BlockTable` \r\n    # (gpu_model_runner)\r\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -15,7 +15,7 @@\n     AttentionMetadata,\n     AttentionType,\n     CommonAttentionMetadata,\n-    subclass_attention_backend,\n+    subclass_attention_backend_with_overrides,\n )\n from vllm.v1.attention.selector import get_attn_backend\n from vllm.v1.kv_cache_interface import CrossAttentionSpec, KVCacheSpec\n@@ -72,6 +72,7 @@ def create_cross_attention_backend(\n ) -> type[AttentionBackend]:\n     prefix = \"CrossAttention_\"\n     underlying_builder = underlying_attn_backend.get_builder_cls()\n+    underlying_impl = underlying_attn_backend.get_impl_cls()\n \n     class CrossAttentionBuilder(underlying_builder):  # type: ignore\n         def build(\n@@ -106,18 +107,60 @@ def build(\n             )\n \n             # NOTE (NickLucche) use `new_metadata` instead of `common_*` (initial) here\n-            new_metadata.slot_mapping = _get_cross_slot_mapping(\n+            slot_mapping = _get_cross_slot_mapping(\n                 new_metadata.encoder_seq_lens_cpu,\n                 new_metadata.block_table_tensor,\n                 self.kv_cache_spec,\n                 self.device,\n             )\n-            return super().build(common_prefix_len, new_metadata, fast_build)\n+            attn_metadata = super().build(common_prefix_len, new_metadata, fast_build)\n+            attn_metadata.slot_mapping = slot_mapping\n+            return attn_metadata\n+\n+    # NOTE(Lucas): we need a custom impl so we can use the slot-mapping computed by\n+    # `CrossAttentionBuilder` instead of the one computed by `BlockTable`\n+    # (gpu_model_runner)\n+    class CrossAttentionImpl(underlying_impl):  # type: ignore[valid-type,misc]\n+        def forward(\n+            self,\n+            layer: torch.nn.Module,\n+            query: torch.Tensor,\n+            key: torch.Tensor,\n+            value: torch.Tensor,\n+            kv_cache: torch.Tensor,\n+            attn_metadata: AttentionMetadata,\n+            output: torch.Tensor | None = None,\n+            output_scale: torch.Tensor | None = None,\n+            output_block_scale: torch.Tensor | None = None,\n+        ) -> torch.Tensor:\n+            if (\n+                not underlying_attn_backend.forward_includes_kv_cache_update\n+                and attn_metadata is not None\n+            ):\n+                self.do_kv_cache_update(\n+                    layer, key, value, kv_cache, attn_metadata.slot_mapping\n+                )\n+\n+            return super().forward(\n+                layer,\n+                query,\n+                key,\n+                value,\n+                kv_cache,\n+                attn_metadata,\n+                output,\n+                output_scale,\n+                output_block_scale,\n+            )\n \n-    attn_backend = subclass_attention_backend(\n+    attn_backend = subclass_attention_backend_with_overrides(\n         name_prefix=prefix,\n         attention_backend_cls=underlying_attn_backend,\n-        builder_cls=CrossAttentionBuilder,\n+        overrides={\n+            \"get_builder_cls\": lambda: CrossAttentionBuilder,\n+            \"get_impl_cls\": lambda: CrossAttentionImpl,\n+            \"forward_includes_kv_cache_update\": True,\n+        },\n     )\n \n     return attn_backend", "patch_sha256": "d5e100e7830e6d38"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2696285471", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/attention/cross_attention.py\nReview Comment:\n```suggestion\n    # NOTE(Lucas): we need a custom impl so we can use the slot-mapping computed by\n    # `CrossAttentionBuilder` instead of the one computed by `BlockTable`\n    # (gpu_model_runner)\n```\n\nOriginal Code Context:\n@@ -106,18 +107,60 @@ def build(\n             )\n \n             # NOTE (NickLucche) use `new_metadata` instead of `common_*` (initial) here\n-            new_metadata.slot_mapping = _get_cross_slot_mapping(\n+            slot_mapping = _get_cross_slot_mapping(\n                 new_metadata.encoder_seq_lens_cpu,\n                 new_metadata.block_table_tensor,\n                 self.kv_cache_spec,\n                 self.device,\n             )\n-            return super().build(common_prefix_len, new_metadata, fast_build)\n+            attn_metadata = super().build(common_prefix_len, new_metadata, fast_build)\n+            attn_metadata.slot_mapping = slot_mapping\n+            return attn_metadata\n+\n+    # NOTE(Lucas): we need a custom impl so we can use the slot-mapping computed by\n+    # `CrossAttentionBuilder` instead of the one computed by `BlockTable` \n+    # (gpu_model_runner)\n", "materials": ["```suggestion\n    # NOTE(Lucas): we need a custom impl so we can use the slot-mapping computed by\n    # `CrossAttentionBuilder` instead of the one computed by `BlockTable`\n    # (gpu_model_runner)\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -15,7 +15,7 @@\n     AttentionMetadata,\n     AttentionType,\n     CommonAttentionMetadata,\n-    subclass_attention_backend,\n+    subclass_attention_backend_with_overrides,\n )\n from vllm.v1.attention.selector import get_attn_backend\n from vllm.v1.kv_cache_interface import CrossAttentionSpec, KVCacheSpec\n@@ -72,6 +72,7 @@ def create_cross_attention_backend(\n ) -> type[AttentionBackend]:\n     prefix = \"CrossAttention_\"\n     underlying_builder = underlying_attn_backend.get_builder_cls()\n+    underlying_impl = underlying_attn_backend.get_impl_cls()\n \n     class CrossAttentionBuilder(underlying_builder):  # type: ignore\n         def build(\n@@ -106,18 +107,60 @@ def build(\n             )\n \n             # NOTE (NickLucche) use `new_metadata` instead of `common_*` (initial) here\n-            new_metadata.slot_mapping = _get_cross_slot_mapping(\n+            slot_mapping = _get_cross_slot_mapping(\n                 new_metadata.encoder_seq_lens_cpu,\n                 new_metadata.block_table_tensor,\n                 self.kv_cache_spec,\n                 self.device,\n             )\n-            return super().build(common_prefix_len, new_metadata, fast_build)\n+            attn_metadata = super().build(common_prefix_len, new_metadata, fast_build)\n+            attn_metadata.slot_mapping = slot_mapping\n+            return attn_metadata\n+\n+    # NOTE(Lucas): we need a custom impl so we can use the slot-mapping computed by\n+    # `CrossAttentionBuilder` instead of the one computed by `BlockTable`\n+    # (gpu_model_runner)\n+    class CrossAttentionImpl(underlying_impl):  # type: ignore[valid-type,misc]\n+        def forward(\n+            self,\n+            layer: torch.nn.Module,\n+            query: torch.Tensor,\n+            key: torch.Tensor,\n+            value: torch.Tensor,\n+            kv_cache: torch.Tensor,\n+            attn_metadata: AttentionMetadata,\n+            output: torch.Tensor | None = None,\n+            output_scale: torch.Tensor | None = None,\n+            output_block_scale: torch.Tensor | None = None,\n+        ) -> torch.Tensor:\n+            if (\n+                not underlying_attn_backend.forward_includes_kv_cache_update\n+                and attn_metadata is not None\n+            ):\n+                self.do_kv_cache_update(\n+                    layer, key, value, kv_cache, attn_metadata.slot_mapping\n+                )\n+\n+            return super().forward(\n+                layer,\n+                query,\n+                key,\n+                value,\n+                kv_cache,\n+                attn_metadata,\n+                output,\n+                output_scale,\n+                output_block_scale,\n+            )\n \n-    attn_backend = subclass_attention_backend(\n+    attn_backend = subclass_attention_backend_with_overrides(\n         name_prefix=prefix,\n         attention_backend_cls=underlying_attn_backend,\n-        builder_cls=CrossAttentionBuilder,\n+        overrides={\n+            \"get_builder_cls\": lambda: CrossAttentionBuilder,\n+            \"get_impl_cls\": lambda: CrossAttentionImpl,\n+            \"forward_includes_kv_cache_update\": True,\n+        },\n     )\n \n     return attn_backend", "patch_sha256": "d5e100e7830e6d38"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2487944840", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/fp8.py\nReview Comment:\nWhy is this necessary? I think `register_parameter` is only needed for tensors that get loaded from model checkpoint\n\nOriginal Code Context:\n@@ -442,6 +451,7 @@ def create_weights(\n                 weight_loader=weight_loader,\n             )\n         layer.register_parameter(\"weight\", weight)\n+        layer.register_parameter(\"input_scale_ub\", None)\n", "materials": ["Why is this necessary? I think `register_parameter` is only needed for tensors that get loaded from model checkpoint"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -48,6 +48,9 @@\n     QuantizationConfig,\n     QuantizeMethodBase,\n )\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n from vllm.model_executor.layers.quantization.kv_cache import BaseKVCacheMethod\n from vllm.model_executor.layers.quantization.utils.flashinfer_utils import (\n     apply_fi_trtllm_fp8_per_tensor_moe,\n@@ -76,12 +79,13 @@\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     GroupShape,\n     is_layer_skipped,\n+    kFp8DynamicTensorSym,\n+    kFp8DynamicTokenSym,\n+    kFp8StaticTensorSym,\n )\n from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n-    Fp8LinearOp,\n     cutlass_block_fp8_supported,\n     cutlass_fp8_supported,\n-    maybe_create_device_identity,\n     normalize_e4m3fn_to_e4m3fnuz,\n )\n from vllm.model_executor.parameter import (\n@@ -328,28 +332,30 @@ def __init__(self, quant_config: Fp8Config):\n         self.weight_block_size = self.quant_config.weight_block_size\n         self.block_quant = self.weight_block_size is not None\n         self.act_q_static = self.quant_config.activation_scheme == \"static\"\n-        if self.weight_block_size:\n-            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n-        else:\n-            # Use per-token quantization for better perf if dynamic and cutlass\n-            if not self.act_q_static and cutlass_fp8_supported():\n-                self.act_q_group_shape = GroupShape.PER_TOKEN\n-            else:\n-                self.act_q_group_shape = GroupShape.PER_TENSOR\n \n         if self.block_quant:\n             assert not self.act_q_static\n             assert self.weight_block_size is not None\n             self.w8a8_block_fp8_linear = W8A8BlockFp8LinearOp(\n                 weight_group_shape=GroupShape(*self.weight_block_size),\n-                act_quant_group_shape=self.act_q_group_shape,\n+                act_quant_group_shape=GroupShape(1, self.weight_block_size[0]),\n                 cutlass_block_fp8_supported=self.cutlass_block_fp8_supported,\n                 use_aiter_and_is_supported=self.use_aiter_and_is_supported,\n             )\n         else:\n-            self.fp8_linear = Fp8LinearOp(\n-                act_quant_static=self.act_q_static,\n-                act_quant_group_shape=self.act_q_group_shape,\n+            # Use per-token quantization for better perf if dynamic and cutlass\n+            if self.act_q_static:\n+                activation_quant_key = kFp8StaticTensorSym\n+            elif cutlass_fp8_supported():\n+                activation_quant_key = kFp8DynamicTokenSym\n+            else:\n+                activation_quant_key = kFp8DynamicTensorSym\n+\n+            self.fp8_linear = init_fp8_linear_kernel(\n+                activation_quant_key=activation_quant_key,\n+                weight_quant_key=kFp8StaticTensorSym,\n+                out_dtype=torch.get_default_dtype(),\n+                module_name=self.__class__.__name__,\n             )\n \n     def create_weights(\n@@ -362,8 +368,6 @@ def create_weights(\n         params_dtype: torch.dtype,\n         **extra_weight_attrs,\n     ):\n-        maybe_create_device_identity()\n-\n         output_size_per_partition = sum(output_partition_sizes)\n         weight_loader = extra_weight_attrs.get(\"weight_loader\")\n         layer.logical_widths = output_partition_sizes\n@@ -462,8 +466,6 @@ def patched_weight_loader(param, loaded_weight, *args, **kwargs):\n                 scale = create_fp8_input_scale(output_partition_sizes, weight_loader)\n                 set_weight_attrs(scale, {\"scale_type\": \"input_scale\"})\n                 layer.register_parameter(\"input_scale\", scale)\n-            else:\n-                layer.register_parameter(\"input_scale\", None)\n \n     def process_weights_after_loading(self, layer: Module) -> None:\n         if getattr(layer, \"_already_called_process_weights_after_loading\", False):\n@@ -602,14 +604,7 @@ def apply(\n                 bias=bias,\n             )\n \n-        return self.fp8_linear.apply(\n-            input=x,\n-            weight=layer.weight,\n-            weight_scale=layer.weight_scale,\n-            out_dtype=self.out_dtype,\n-            input_scale=layer.input_scale,\n-            bias=bias,\n-        )\n+        return self.fp8_linear.apply_weights(layer, x, bias)\n \n \n class Fp8MoEMethod(FusedMoEMethodBase):", "patch_sha256": "642b41eb0eed9aa5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2487949894", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/fbgemm_fp8.py\nReview Comment:\nCan we make these accept `QuantKey` instances as the quantization descriptors?\n\nOriginal Code Context:\n@@ -91,11 +96,16 @@ def get_quant_method(\n class FBGEMMFp8LinearMethod(LinearMethodBase):\n     def __init__(self, quant_config: FBGEMMFp8Config):\n         self.quant_config = quant_config\n-        self.fp8_linear = Fp8LinearOp(\n-            act_quant_static=False, act_quant_group_shape=GroupShape.PER_TOKEN\n-        )\n         self.out_dtype = torch.get_default_dtype()\n \n+        self.fp8_linear_kernel = init_fp8_linear_kernel(\n+            act_q_static=False,\n+            act_q_group_shape=GroupShape.PER_TOKEN,\n+            weight_quant_strategy=ScaledMMLinearQuantStrategy.CHANNEL,\n", "materials": ["Can we make these accept `QuantKey` instances as the quantization descriptors?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -18,17 +18,19 @@\n     QuantizationConfig,\n     QuantizeMethodBase,\n )\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n from vllm.model_executor.layers.quantization.utils.marlin_utils_fp8 import (\n     apply_fp8_marlin_linear,\n     prepare_fp8_layer_for_marlin,\n )\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n-    GroupShape,\n     is_layer_skipped,\n+    kFp8DynamicTokenSym,\n+    kFp8StaticTokenSym,\n )\n from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n-    Fp8LinearOp,\n-    maybe_create_device_identity,\n     normalize_e4m3fn_to_e4m3fnuz,\n )\n from vllm.model_executor.parameter import (\n@@ -91,10 +93,13 @@ def get_quant_method(\n class FBGEMMFp8LinearMethod(LinearMethodBase):\n     def __init__(self, quant_config: FBGEMMFp8Config):\n         self.quant_config = quant_config\n-        self.fp8_linear = Fp8LinearOp(\n-            act_quant_static=False, act_quant_group_shape=GroupShape.PER_TOKEN\n-        )\n         self.out_dtype = torch.get_default_dtype()\n+        self.fp8_linear = init_fp8_linear_kernel(\n+            activation_quant_key=kFp8DynamicTokenSym,\n+            weight_quant_key=kFp8StaticTokenSym,\n+            out_dtype=torch.get_default_dtype(),\n+            module_name=self.__class__.__name__,\n+        )\n \n     def create_weights(\n         self,\n@@ -106,7 +111,6 @@ def create_weights(\n         params_dtype: torch.dtype,\n         **extra_weight_attrs,\n     ):\n-        maybe_create_device_identity()\n         weight_loader = extra_weight_attrs.get(\"weight_loader\")\n         del input_size, output_size\n         output_size_per_partition = sum(output_partition_sizes)\n@@ -184,12 +188,4 @@ def apply(\n                 bias=bias,\n             )\n \n-        return self.fp8_linear.apply(\n-            input=x,\n-            weight=layer.weight,\n-            weight_scale=layer.weight_scale,\n-            out_dtype=self.out_dtype,\n-            input_scale=None,\n-            input_scale_ub=layer.input_scale_ub,\n-            bias=bias,\n-        )\n+        return self.fp8_linear.apply_weights(layer, x, bias)", "patch_sha256": "77c89ca805047f5d"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2487953396", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/quark/schemes/quark_w8a8_int8.py\nReview Comment:\nWhy move this?\n\nOriginal Code Context:\n@@ -50,18 +47,13 @@ def create_weights(\n     ):\n         layer.logical_widths = output_partition_sizes\n \n-        scaled_mm_linear_kernel_config = ScaledMMLinearLayerConfig(\n+        self.kernel = init_int8_linear_kernel(\n", "materials": ["Why move this?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -7,8 +7,7 @@\n \n from vllm.logger import init_logger\n from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n-    ScaledMMLinearLayerConfig,\n-    choose_scaled_mm_linear_kernel,\n+    init_int8_linear_kernel,\n )\n from vllm.model_executor.layers.quantization.quark.schemes import QuarkScheme\n from vllm.model_executor.parameter import (\n@@ -22,8 +21,6 @@\n \n \n class QuarkW8A8Int8(QuarkScheme):\n-    _kernel_backends_being_used: set[str] = set()\n-\n     def __init__(\n         self,\n         qscheme: str,\n@@ -50,18 +47,13 @@ def create_weights(\n     ):\n         layer.logical_widths = output_partition_sizes\n \n-        scaled_mm_linear_kernel_config = ScaledMMLinearLayerConfig(\n+        self.kernel = init_int8_linear_kernel(\n             is_channelwise=(self.qscheme == \"per_channel\"),\n             is_static_input_scheme=(self.is_static_input_scheme is True),\n             input_symmetric=(self.input_symmetric is True),\n+            module_name=self.__class__.__name__,\n         )\n \n-        kernel_type = choose_scaled_mm_linear_kernel(scaled_mm_linear_kernel_config)\n-\n-        if kernel_type.__name__ not in self._kernel_backends_being_used:\n-            logger.info(\"Using %s for QuarkW8A8Int8\", kernel_type.__name__)\n-            self._kernel_backends_being_used.add(kernel_type.__name__)\n-\n         # WEIGHT\n         weight = ModelWeightParameter(\n             data=torch.empty(\n@@ -102,25 +94,21 @@ def create_weights(\n         layer.register_parameter(\"weight_zero_point\", weight_zero_point)\n \n         # INPUT SCALE\n+        input_zero_point = None\n+        input_scale = None\n         if self.is_static_input_scheme:\n             input_scale = BasevLLMParameter(\n                 data=torch.empty(1, dtype=torch.float32), weight_loader=weight_loader\n             )\n-            layer.register_parameter(\"input_scale\", input_scale)\n \n             input_zero_point = BasevLLMParameter(\n                 data=torch.empty(1, dtype=torch.int8), weight_loader=weight_loader\n             )\n-            layer.register_parameter(\"input_zero_point\", input_zero_point)\n-\n-        self.kernel = kernel_type(\n-            c=scaled_mm_linear_kernel_config,\n-            w_q_param_name=\"weight\",\n-            w_s_param_name=\"weight_scale\",\n-            i_s_param_name=\"input_scale\",\n-            i_zp_param_name=\"input_zero_point\",\n-            azp_adj_param_name=\"azp_adj\",\n-        )\n+\n+        layer.register_parameter(\"input_scale\", input_scale)\n+        layer.register_parameter(\"input_zero_point\", input_zero_point)\n+        if not hasattr(layer, \"azp_adj\"):\n+            layer.register_parameter(\"azp_adj\", None)\n \n     # Checkpoints are serialized in quark format, which is\n     # different from the format the kernel may want. Handle repacking here.", "patch_sha256": "ea6220c966ff0c83"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2487973467", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py\nReview Comment:\nPlease name this file `flashinfer.py`\n\nOriginal Code Context:\n@@ -0,0 +1,92 @@\n+# SPDX-License-Identifier: Apache-2.0\n", "materials": ["Please name this file `flashinfer.py`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,57 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )", "patch_sha256": "298db567412a54f3"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2490704552", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/quark/schemes/quark_w8a8_int8.py\nReview Comment:\nWe opted to use a helper function for both fp8 and int8 kernel initialisation and move the logging of the selected kernel to the helper function to reduce the boilerplate.\n\nOriginal Code Context:\n@@ -50,18 +47,13 @@ def create_weights(\n     ):\n         layer.logical_widths = output_partition_sizes\n \n-        scaled_mm_linear_kernel_config = ScaledMMLinearLayerConfig(\n+        self.kernel = init_int8_linear_kernel(\n", "materials": ["We opted to use a helper function for both fp8 and int8 kernel initialisation and move the logging of the selected kernel to the helper function to reduce the boilerplate."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -7,8 +7,7 @@\n \n from vllm.logger import init_logger\n from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n-    ScaledMMLinearLayerConfig,\n-    choose_scaled_mm_linear_kernel,\n+    init_int8_linear_kernel,\n )\n from vllm.model_executor.layers.quantization.quark.schemes import QuarkScheme\n from vllm.model_executor.parameter import (\n@@ -22,8 +21,6 @@\n \n \n class QuarkW8A8Int8(QuarkScheme):\n-    _kernel_backends_being_used: set[str] = set()\n-\n     def __init__(\n         self,\n         qscheme: str,\n@@ -50,18 +47,13 @@ def create_weights(\n     ):\n         layer.logical_widths = output_partition_sizes\n \n-        scaled_mm_linear_kernel_config = ScaledMMLinearLayerConfig(\n+        self.kernel = init_int8_linear_kernel(\n             is_channelwise=(self.qscheme == \"per_channel\"),\n             is_static_input_scheme=(self.is_static_input_scheme is True),\n             input_symmetric=(self.input_symmetric is True),\n+            module_name=self.__class__.__name__,\n         )\n \n-        kernel_type = choose_scaled_mm_linear_kernel(scaled_mm_linear_kernel_config)\n-\n-        if kernel_type.__name__ not in self._kernel_backends_being_used:\n-            logger.info(\"Using %s for QuarkW8A8Int8\", kernel_type.__name__)\n-            self._kernel_backends_being_used.add(kernel_type.__name__)\n-\n         # WEIGHT\n         weight = ModelWeightParameter(\n             data=torch.empty(\n@@ -102,25 +94,21 @@ def create_weights(\n         layer.register_parameter(\"weight_zero_point\", weight_zero_point)\n \n         # INPUT SCALE\n+        input_zero_point = None\n+        input_scale = None\n         if self.is_static_input_scheme:\n             input_scale = BasevLLMParameter(\n                 data=torch.empty(1, dtype=torch.float32), weight_loader=weight_loader\n             )\n-            layer.register_parameter(\"input_scale\", input_scale)\n \n             input_zero_point = BasevLLMParameter(\n                 data=torch.empty(1, dtype=torch.int8), weight_loader=weight_loader\n             )\n-            layer.register_parameter(\"input_zero_point\", input_zero_point)\n-\n-        self.kernel = kernel_type(\n-            c=scaled_mm_linear_kernel_config,\n-            w_q_param_name=\"weight\",\n-            w_s_param_name=\"weight_scale\",\n-            i_s_param_name=\"input_scale\",\n-            i_zp_param_name=\"input_zero_point\",\n-            azp_adj_param_name=\"azp_adj\",\n-        )\n+\n+        layer.register_parameter(\"input_scale\", input_scale)\n+        layer.register_parameter(\"input_zero_point\", input_zero_point)\n+        if not hasattr(layer, \"azp_adj\"):\n+            layer.register_parameter(\"azp_adj\", None)\n \n     # Checkpoints are serialized in quark format, which is\n     # different from the format the kernel may want. Handle repacking here.", "patch_sha256": "ea6220c966ff0c83"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2501085627", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/rocm.py\nReview Comment:\nRestore if?\n\nOriginal Code Context:\n@@ -72,18 +71,18 @@ def rocm_per_tensor_float_w8a8_scaled_mm(\n     bias: torch.Tensor,\n     output_shape: list[int],\n ) -> torch.Tensor:\n-    output = torch.ops.vllm.rocm_per_tensor_w8a8_scaled_mm_impl(\n+    output = torch.ops.vllm.rocm_per_tensor_float_w8a8_scaled_mm_impl(\n         A, B, out_dtype, As, Bs, bias\n     )\n     return torch.narrow(output, 0, 0, A.shape[0]).view(*output_shape)\n \n \n-if current_platform.is_rocm():\n-    direct_register_custom_op(\n-        op_name=\"rocm_per_tensor_float_w8a8_scaled_mm_impl\",\n-        op_func=rocm_per_tensor_float_w8a8_scaled_mm_impl,\n-        fake_impl=rocm_per_tensor_float_w8a8_scaled_mm_fake,\n-    )\n+# if current_platform.is_rocm():\n", "materials": ["Restore if?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,117 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+import vllm.envs as envs\n+from vllm import _custom_ops as ops\n+from vllm.platforms import current_platform\n+from vllm.utils.platform_utils import get_cu_count\n+from vllm.utils.torch_utils import direct_register_custom_op\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+def rocm_per_tensor_float_w8a8_scaled_mm_impl(\n+    A: torch.Tensor,\n+    B: torch.Tensor,\n+    out_dtype: torch.dtype,\n+    As: torch.Tensor,\n+    Bs: torch.Tensor,\n+    bias: torch.Tensor,\n+) -> torch.Tensor:\n+    if (\n+        A.shape[0] == 1\n+        and B.shape[1] % 16 == 0\n+        and ((bias is None) or (bias.dtype == out_dtype))\n+    ):\n+        output = ops.wvSplitKQ(\n+            B.t(),\n+            A,\n+            out_dtype,\n+            As,\n+            Bs,\n+            get_cu_count(),\n+            bias,\n+        )\n+    # Fallback\n+    else:\n+        output = torch._scaled_mm(\n+            A,\n+            B,\n+            out_dtype=out_dtype,\n+            scale_a=As,\n+            scale_b=Bs,\n+            bias=bias,\n+        )\n+    return output\n+\n+\n+def rocm_per_tensor_float_w8a8_scaled_mm_fake(\n+    A: torch.Tensor,\n+    B: torch.Tensor,\n+    out_dtype: torch.dtype,\n+    As: torch.Tensor,\n+    Bs: torch.Tensor,\n+    bias: torch.Tensor,\n+) -> torch.Tensor:\n+    return A.new_empty((*A.shape[:-1], B.shape[1]), dtype=out_dtype)\n+\n+\n+if current_platform.is_rocm():\n+    direct_register_custom_op(\n+        op_name=\"rocm_per_tensor_float_w8a8_scaled_mm_impl\",\n+        op_func=rocm_per_tensor_float_w8a8_scaled_mm_impl,\n+        fake_impl=rocm_per_tensor_float_w8a8_scaled_mm_fake,\n+    )\n+\n+\n+class ROCmFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_rocm():\n+            return False, \"requires ROCm.\"\n+\n+        from vllm.platforms.rocm import on_mi3xx\n+\n+        if not on_mi3xx():\n+            return False, \"requires MI3xx.\"\n+\n+        if not envs.VLLM_ROCM_USE_SKINNY_GEMM:\n+            return False, \"requires VLLM_ROCM_USE_SKINNY_GEMM to be enabled.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        output = torch.ops.vllm.rocm_per_tensor_float_w8a8_scaled_mm_impl(\n+            A, B, out_dtype, As, Bs, bias\n+        )\n+        return torch.narrow(output, 0, 0, A.shape[0]).view(*output_shape)", "patch_sha256": "1da593bcfb5ea2b8"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2501096826", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py\nReview Comment:\nLet's move these into the `if`\n\nOriginal Code Context:\n@@ -57,35 +69,26 @@ def __init__(self, weight_quant: QuantizationArgs, is_static_input_scheme: bool)\n         self.strategy = weight_quant.strategy\n         self.out_dtype = torch.get_default_dtype()\n         self.is_static_input_scheme = is_static_input_scheme\n-\n         self.weight_block_size = self.weight_quant.block_structure\n-        if self.weight_block_size is not None:\n-            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n-        else:\n-            self.act_q_group_shape = (\n-                GroupShape.PER_TENSOR\n-                if is_static_input_scheme\n-                else GroupShape.PER_TOKEN\n-            )\n-\n         self.cutlass_block_fp8_supported = cutlass_block_fp8_supported()\n         self.use_aiter_and_is_supported = check_aiter_fp8_linear_support()\n", "materials": ["Let's move these into the `if`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -8,9 +8,13 @@\n from torch.nn import Parameter\n \n from vllm._aiter_ops import rocm_aiter_ops\n+from vllm.logger import init_logger\n from vllm.model_executor.layers.quantization.compressed_tensors.schemes import (\n     CompressedTensorsScheme,\n )\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n from vllm.model_executor.layers.quantization.utils.fp8_utils import (\n     W8A8BlockFp8LinearOp,\n     create_fp8_input_scale,\n@@ -22,11 +26,14 @@\n     process_fp8_weight_tensor_strategy,\n     validate_fp8_block_shape,\n )\n-from vllm.model_executor.layers.quantization.utils.quant_utils import GroupShape\n+from vllm.model_executor.layers.quantization.utils.quant_utils import (\n+    GroupShape,\n+    kFp8DynamicTokenSym,\n+    kFp8StaticTensorSym,\n+    kFp8StaticTokenSym,\n+)\n from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n-    Fp8LinearOp,\n     cutlass_block_fp8_supported,\n-    maybe_create_device_identity,\n )\n from vllm.model_executor.parameter import (\n     BlockQuantScaleParameter,\n@@ -42,39 +49,46 @@\n     QuantizationStrategy.TENSOR: PerTensorScaleParameter,\n }\n \n+STATIC_QUANT = True\n+DYNAMIC_QUANT = False\n+activation_quant_key_mapping = {\n+    STATIC_QUANT: kFp8StaticTensorSym,\n+    DYNAMIC_QUANT: kFp8DynamicTokenSym,\n+}\n+weight_quant_key_mapping = {\n+    QuantizationStrategy.CHANNEL: kFp8StaticTokenSym,\n+    QuantizationStrategy.TENSOR: kFp8StaticTensorSym,\n+}\n+logger = init_logger(__name__)\n+\n \n class CompressedTensorsW8A8Fp8(CompressedTensorsScheme):\n     def __init__(self, weight_quant: QuantizationArgs, is_static_input_scheme: bool):\n         self.weight_quant = weight_quant\n         self.strategy = weight_quant.strategy\n         self.out_dtype = torch.get_default_dtype()\n         self.is_static_input_scheme = is_static_input_scheme\n-\n         self.weight_block_size = self.weight_quant.block_structure\n-        if self.weight_block_size is not None:\n-            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n-        else:\n-            self.act_q_group_shape = (\n-                GroupShape.PER_TENSOR\n-                if is_static_input_scheme\n-                else GroupShape.PER_TOKEN\n-            )\n-\n-        self.cutlass_block_fp8_supported = cutlass_block_fp8_supported()\n-        self.use_aiter_and_is_supported = rocm_aiter_ops.is_linear_fp8_enabled()\n \n         if self.weight_block_size is not None:\n+            self.cutlass_block_fp8_supported = cutlass_block_fp8_supported()\n+            self.use_aiter_and_is_supported = rocm_aiter_ops.is_linear_fp8_enabled()\n             assert not self.is_static_input_scheme\n+            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n             self.w8a8_block_fp8_linear = W8A8BlockFp8LinearOp(\n                 weight_group_shape=GroupShape(*self.weight_block_size),\n                 act_quant_group_shape=self.act_q_group_shape,\n                 cutlass_block_fp8_supported=self.cutlass_block_fp8_supported,\n                 use_aiter_and_is_supported=self.use_aiter_and_is_supported,\n             )\n         else:\n-            self.fp8_linear = Fp8LinearOp(\n-                act_quant_static=self.is_static_input_scheme,\n-                act_quant_group_shape=self.act_q_group_shape,\n+            activation_quant_key = activation_quant_key_mapping[is_static_input_scheme]\n+            weight_quant_key = weight_quant_key_mapping[self.strategy]\n+            self.fp8_linear = init_fp8_linear_kernel(\n+                activation_quant_key=activation_quant_key,\n+                weight_quant_key=weight_quant_key,\n+                out_dtype=self.out_dtype,\n+                module_name=self.__class__.__name__,\n             )\n \n     @classmethod\n@@ -93,8 +107,6 @@ def create_weights(\n         weight_loader: Callable,\n         **kwargs,\n     ):\n-        maybe_create_device_identity()\n-\n         output_size_per_partition = sum(output_partition_sizes)\n         layer.logical_widths = output_partition_sizes\n         layer.weight_block_size = None\n@@ -143,7 +155,6 @@ def process_weights_after_loading(self, layer) -> None:\n                 getattr(layer, \"input_scale\", None),\n             )\n             weight = weight.t()\n-\n         elif self.strategy == QuantizationStrategy.CHANNEL:\n             weight, weight_scale, input_scale = process_fp8_weight_channel_strategy(\n                 layer.weight, layer.weight_scale, getattr(layer, \"input_scale\", None)\n@@ -174,7 +185,6 @@ def process_weights_after_loading(self, layer) -> None:\n             layer.input_scale = Parameter(layer.input_scale.max(), requires_grad=False)\n         else:\n             layer.input_scale = None\n-\n         if self.strategy == QuantizationStrategy.BLOCK:\n             maybe_post_process_fp8_weight_block(layer)\n \n@@ -193,11 +203,4 @@ def apply_weights(\n                 bias=bias,\n             )\n \n-        return self.fp8_linear.apply(\n-            input=x,\n-            weight=layer.weight,\n-            weight_scale=layer.weight_scale,\n-            out_dtype=self.out_dtype,\n-            input_scale=layer.input_scale,\n-            bias=bias,\n-        )\n+        return self.fp8_linear.apply_weights(layer, x, bias)", "patch_sha256": "9ffc94d3ca8687e4"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2501100726", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/fbgemm_fp8.py\nReview Comment:\nWeight quant is never dynamic token (I assume you meant static per-channel)?\n\nOriginal Code Context:\n@@ -97,12 +94,10 @@ class FBGEMMFp8LinearMethod(LinearMethodBase):\n     def __init__(self, quant_config: FBGEMMFp8Config):\n         self.quant_config = quant_config\n         self.out_dtype = torch.get_default_dtype()\n-\n-        self.fp8_linear_kernel = init_fp8_linear_kernel(\n-            act_q_static=False,\n-            act_q_group_shape=GroupShape.PER_TOKEN,\n-            weight_quant_strategy=ScaledMMLinearQuantStrategy.CHANNEL,\n-            out_dtype=self.out_dtype,\n+        self.fp8_linear = init_fp8_linear_kernel(\n+            activation_quant_key=kFp8DynamicTokenSym,\n+            weight_quant_key=kFp8DynamicTokenSym,\n", "materials": ["Weight quant is never dynamic token (I assume you meant static per-channel)?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -18,17 +18,19 @@\n     QuantizationConfig,\n     QuantizeMethodBase,\n )\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n from vllm.model_executor.layers.quantization.utils.marlin_utils_fp8 import (\n     apply_fp8_marlin_linear,\n     prepare_fp8_layer_for_marlin,\n )\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n-    GroupShape,\n     is_layer_skipped,\n+    kFp8DynamicTokenSym,\n+    kFp8StaticTokenSym,\n )\n from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n-    Fp8LinearOp,\n-    maybe_create_device_identity,\n     normalize_e4m3fn_to_e4m3fnuz,\n )\n from vllm.model_executor.parameter import (\n@@ -91,10 +93,13 @@ def get_quant_method(\n class FBGEMMFp8LinearMethod(LinearMethodBase):\n     def __init__(self, quant_config: FBGEMMFp8Config):\n         self.quant_config = quant_config\n-        self.fp8_linear = Fp8LinearOp(\n-            act_quant_static=False, act_quant_group_shape=GroupShape.PER_TOKEN\n-        )\n         self.out_dtype = torch.get_default_dtype()\n+        self.fp8_linear = init_fp8_linear_kernel(\n+            activation_quant_key=kFp8DynamicTokenSym,\n+            weight_quant_key=kFp8StaticTokenSym,\n+            out_dtype=torch.get_default_dtype(),\n+            module_name=self.__class__.__name__,\n+        )\n \n     def create_weights(\n         self,\n@@ -106,7 +111,6 @@ def create_weights(\n         params_dtype: torch.dtype,\n         **extra_weight_attrs,\n     ):\n-        maybe_create_device_identity()\n         weight_loader = extra_weight_attrs.get(\"weight_loader\")\n         del input_size, output_size\n         output_size_per_partition = sum(output_partition_sizes)\n@@ -184,12 +188,4 @@ def apply(\n                 bias=bias,\n             )\n \n-        return self.fp8_linear.apply(\n-            input=x,\n-            weight=layer.weight,\n-            weight_scale=layer.weight_scale,\n-            out_dtype=self.out_dtype,\n-            input_scale=None,\n-            input_scale_ub=layer.input_scale_ub,\n-            bias=bias,\n-        )\n+        return self.fp8_linear.apply_weights(layer, x, bias)", "patch_sha256": "77c89ca805047f5d"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2503207097", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/fbgemm_fp8.py\nReview Comment:\nMy bad, fixed\n\nOriginal Code Context:\n@@ -97,12 +94,10 @@ class FBGEMMFp8LinearMethod(LinearMethodBase):\n     def __init__(self, quant_config: FBGEMMFp8Config):\n         self.quant_config = quant_config\n         self.out_dtype = torch.get_default_dtype()\n-\n-        self.fp8_linear_kernel = init_fp8_linear_kernel(\n-            act_q_static=False,\n-            act_q_group_shape=GroupShape.PER_TOKEN,\n-            weight_quant_strategy=ScaledMMLinearQuantStrategy.CHANNEL,\n-            out_dtype=self.out_dtype,\n+        self.fp8_linear = init_fp8_linear_kernel(\n+            activation_quant_key=kFp8DynamicTokenSym,\n+            weight_quant_key=kFp8DynamicTokenSym,\n", "materials": ["My bad, fixed"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -18,17 +18,19 @@\n     QuantizationConfig,\n     QuantizeMethodBase,\n )\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n from vllm.model_executor.layers.quantization.utils.marlin_utils_fp8 import (\n     apply_fp8_marlin_linear,\n     prepare_fp8_layer_for_marlin,\n )\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n-    GroupShape,\n     is_layer_skipped,\n+    kFp8DynamicTokenSym,\n+    kFp8StaticTokenSym,\n )\n from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n-    Fp8LinearOp,\n-    maybe_create_device_identity,\n     normalize_e4m3fn_to_e4m3fnuz,\n )\n from vllm.model_executor.parameter import (\n@@ -91,10 +93,13 @@ def get_quant_method(\n class FBGEMMFp8LinearMethod(LinearMethodBase):\n     def __init__(self, quant_config: FBGEMMFp8Config):\n         self.quant_config = quant_config\n-        self.fp8_linear = Fp8LinearOp(\n-            act_quant_static=False, act_quant_group_shape=GroupShape.PER_TOKEN\n-        )\n         self.out_dtype = torch.get_default_dtype()\n+        self.fp8_linear = init_fp8_linear_kernel(\n+            activation_quant_key=kFp8DynamicTokenSym,\n+            weight_quant_key=kFp8StaticTokenSym,\n+            out_dtype=torch.get_default_dtype(),\n+            module_name=self.__class__.__name__,\n+        )\n \n     def create_weights(\n         self,\n@@ -106,7 +111,6 @@ def create_weights(\n         params_dtype: torch.dtype,\n         **extra_weight_attrs,\n     ):\n-        maybe_create_device_identity()\n         weight_loader = extra_weight_attrs.get(\"weight_loader\")\n         del input_size, output_size\n         output_size_per_partition = sum(output_partition_sizes)\n@@ -184,12 +188,4 @@ def apply(\n                 bias=bias,\n             )\n \n-        return self.fp8_linear.apply(\n-            input=x,\n-            weight=layer.weight,\n-            weight_scale=layer.weight_scale,\n-            out_dtype=self.out_dtype,\n-            input_scale=None,\n-            input_scale_ub=layer.input_scale_ub,\n-            bias=bias,\n-        )\n+        return self.fp8_linear.apply_weights(layer, x, bias)", "patch_sha256": "77c89ca805047f5d"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2524158287", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/fp8.py\nReview Comment:\n**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Fix activation quant key selection**\n\nIn `Fp8LinearMethod.__init__` the new code now sets `self.activation_quant_key` to `kFp8StaticTokenSym` whenever the model uses dynamic (non‑static) activation scales on a cutlass‑capable GPU and to `kFp8DynamicTensorSym` for all other cases (lines 382‑387). This reverses the semantics of the key: dynamic models now instantiate `QuantFP8` with `static=True` even though `layer.input_scale` is always `None`, and static models instantiate it with `static=False` even though an input scale tensor is provided. The `QuantFP8` operator enforces `assert (scale is not None) == self.static` (`input_quant_fp8.py`, lines 42‑48), so any non‑block FP8 layer now triggers an assertion error during the first forward call. Please derive the quant key from `self.act_q_static` (static activations should use `kFp8StaticTensorSym`, dynamic per‑token should use `kFp8DynamicTokenSym`, etc.) so that the quantizer receives matching settings.\n\nUseful? React with 👍 / 👎.\n\nOriginal Code Context:\n@@ -377,8 +381,10 @@ def __init__(self, quant_config: Fp8Config):\n             # Use per-token quantization for better perf if dynamic and cutlass\n             if not self.act_q_static and cutlass_fp8_supported():\n                 self.act_q_group_shape = GroupShape.PER_TOKEN\n+                self.activation_quant_key = kFp8StaticTokenSym\n             else:\n                 self.act_q_group_shape = GroupShape.PER_TENSOR\n+                self.activation_quant_key = kFp8DynamicTensorSym\n", "materials": ["**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Fix activation quant key selection**\n\nIn `Fp8LinearMethod.__init__` the new code now sets `self.activation_quant_key` to `kFp8StaticTokenSym` whenever the model uses dynamic (non‑static) activation scales on a cutlass‑capable GPU and to `kFp8DynamicTensorSym` for all other cases (lines 382‑387). This reverses the semantics of the key: dynamic models now instantiate `QuantFP8` with `static=True` even though `layer.input_scale` is always `None`, and static models instantiate it with `static=False` even though an input scale tensor is provided. The `QuantFP8` operator enforces `assert (scale is not None) == self.static` (`input_quant_fp8.py`, lines 42‑48), so any non‑block FP8 layer now triggers an assertion error during the first forward call. Please derive the quant key from `self.act_q_static` (static activations should use `kFp8StaticTensorSym`, dynamic per‑token should use `kFp8DynamicTokenSym`, etc.) so that the quantizer receives matching settings.\n\nUseful? React with 👍 / 👎."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -48,6 +48,9 @@\n     QuantizationConfig,\n     QuantizeMethodBase,\n )\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n from vllm.model_executor.layers.quantization.kv_cache import BaseKVCacheMethod\n from vllm.model_executor.layers.quantization.utils.flashinfer_utils import (\n     apply_fi_trtllm_fp8_per_tensor_moe,\n@@ -76,12 +79,13 @@\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     GroupShape,\n     is_layer_skipped,\n+    kFp8DynamicTensorSym,\n+    kFp8DynamicTokenSym,\n+    kFp8StaticTensorSym,\n )\n from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n-    Fp8LinearOp,\n     cutlass_block_fp8_supported,\n     cutlass_fp8_supported,\n-    maybe_create_device_identity,\n     normalize_e4m3fn_to_e4m3fnuz,\n )\n from vllm.model_executor.parameter import (\n@@ -328,28 +332,30 @@ def __init__(self, quant_config: Fp8Config):\n         self.weight_block_size = self.quant_config.weight_block_size\n         self.block_quant = self.weight_block_size is not None\n         self.act_q_static = self.quant_config.activation_scheme == \"static\"\n-        if self.weight_block_size:\n-            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n-        else:\n-            # Use per-token quantization for better perf if dynamic and cutlass\n-            if not self.act_q_static and cutlass_fp8_supported():\n-                self.act_q_group_shape = GroupShape.PER_TOKEN\n-            else:\n-                self.act_q_group_shape = GroupShape.PER_TENSOR\n \n         if self.block_quant:\n             assert not self.act_q_static\n             assert self.weight_block_size is not None\n             self.w8a8_block_fp8_linear = W8A8BlockFp8LinearOp(\n                 weight_group_shape=GroupShape(*self.weight_block_size),\n-                act_quant_group_shape=self.act_q_group_shape,\n+                act_quant_group_shape=GroupShape(1, self.weight_block_size[0]),\n                 cutlass_block_fp8_supported=self.cutlass_block_fp8_supported,\n                 use_aiter_and_is_supported=self.use_aiter_and_is_supported,\n             )\n         else:\n-            self.fp8_linear = Fp8LinearOp(\n-                act_quant_static=self.act_q_static,\n-                act_quant_group_shape=self.act_q_group_shape,\n+            # Use per-token quantization for better perf if dynamic and cutlass\n+            if self.act_q_static:\n+                activation_quant_key = kFp8StaticTensorSym\n+            elif cutlass_fp8_supported():\n+                activation_quant_key = kFp8DynamicTokenSym\n+            else:\n+                activation_quant_key = kFp8DynamicTensorSym\n+\n+            self.fp8_linear = init_fp8_linear_kernel(\n+                activation_quant_key=activation_quant_key,\n+                weight_quant_key=kFp8StaticTensorSym,\n+                out_dtype=torch.get_default_dtype(),\n+                module_name=self.__class__.__name__,\n             )\n \n     def create_weights(\n@@ -362,8 +368,6 @@ def create_weights(\n         params_dtype: torch.dtype,\n         **extra_weight_attrs,\n     ):\n-        maybe_create_device_identity()\n-\n         output_size_per_partition = sum(output_partition_sizes)\n         weight_loader = extra_weight_attrs.get(\"weight_loader\")\n         layer.logical_widths = output_partition_sizes\n@@ -462,8 +466,6 @@ def patched_weight_loader(param, loaded_weight, *args, **kwargs):\n                 scale = create_fp8_input_scale(output_partition_sizes, weight_loader)\n                 set_weight_attrs(scale, {\"scale_type\": \"input_scale\"})\n                 layer.register_parameter(\"input_scale\", scale)\n-            else:\n-                layer.register_parameter(\"input_scale\", None)\n \n     def process_weights_after_loading(self, layer: Module) -> None:\n         if getattr(layer, \"_already_called_process_weights_after_loading\", False):\n@@ -602,14 +604,7 @@ def apply(\n                 bias=bias,\n             )\n \n-        return self.fp8_linear.apply(\n-            input=x,\n-            weight=layer.weight,\n-            weight_scale=layer.weight_scale,\n-            out_dtype=self.out_dtype,\n-            input_scale=layer.input_scale,\n-            bias=bias,\n-        )\n+        return self.fp8_linear.apply_weights(layer, x, bias)\n \n \n class Fp8MoEMethod(FusedMoEMethodBase):", "patch_sha256": "642b41eb0eed9aa5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2535731544", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/fp8.py\nReview Comment:\nWhy not `kFp8DynamicTensorSym`?\n\nOriginal Code Context:\n@@ -384,10 +380,10 @@ def __init__(self, quant_config: Fp8Config):\n             # Use per-token quantization for better perf if dynamic and cutlass\n             if not self.act_q_static and cutlass_fp8_supported():\n                 self.act_q_group_shape = GroupShape.PER_TOKEN\n-                self.activation_quant_key = kFp8StaticTokenSym\n+                self.activation_quant_key = kFp8DynamicTokenSym\n             else:\n                 self.act_q_group_shape = GroupShape.PER_TENSOR\n-                self.activation_quant_key = kFp8DynamicTensorSym\n+                self.activation_quant_key = kFp8StaticTensorSym\n", "materials": ["Why not `kFp8DynamicTensorSym`?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -48,6 +48,9 @@\n     QuantizationConfig,\n     QuantizeMethodBase,\n )\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n from vllm.model_executor.layers.quantization.kv_cache import BaseKVCacheMethod\n from vllm.model_executor.layers.quantization.utils.flashinfer_utils import (\n     apply_fi_trtllm_fp8_per_tensor_moe,\n@@ -76,12 +79,13 @@\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     GroupShape,\n     is_layer_skipped,\n+    kFp8DynamicTensorSym,\n+    kFp8DynamicTokenSym,\n+    kFp8StaticTensorSym,\n )\n from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n-    Fp8LinearOp,\n     cutlass_block_fp8_supported,\n     cutlass_fp8_supported,\n-    maybe_create_device_identity,\n     normalize_e4m3fn_to_e4m3fnuz,\n )\n from vllm.model_executor.parameter import (\n@@ -328,28 +332,30 @@ def __init__(self, quant_config: Fp8Config):\n         self.weight_block_size = self.quant_config.weight_block_size\n         self.block_quant = self.weight_block_size is not None\n         self.act_q_static = self.quant_config.activation_scheme == \"static\"\n-        if self.weight_block_size:\n-            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n-        else:\n-            # Use per-token quantization for better perf if dynamic and cutlass\n-            if not self.act_q_static and cutlass_fp8_supported():\n-                self.act_q_group_shape = GroupShape.PER_TOKEN\n-            else:\n-                self.act_q_group_shape = GroupShape.PER_TENSOR\n \n         if self.block_quant:\n             assert not self.act_q_static\n             assert self.weight_block_size is not None\n             self.w8a8_block_fp8_linear = W8A8BlockFp8LinearOp(\n                 weight_group_shape=GroupShape(*self.weight_block_size),\n-                act_quant_group_shape=self.act_q_group_shape,\n+                act_quant_group_shape=GroupShape(1, self.weight_block_size[0]),\n                 cutlass_block_fp8_supported=self.cutlass_block_fp8_supported,\n                 use_aiter_and_is_supported=self.use_aiter_and_is_supported,\n             )\n         else:\n-            self.fp8_linear = Fp8LinearOp(\n-                act_quant_static=self.act_q_static,\n-                act_quant_group_shape=self.act_q_group_shape,\n+            # Use per-token quantization for better perf if dynamic and cutlass\n+            if self.act_q_static:\n+                activation_quant_key = kFp8StaticTensorSym\n+            elif cutlass_fp8_supported():\n+                activation_quant_key = kFp8DynamicTokenSym\n+            else:\n+                activation_quant_key = kFp8DynamicTensorSym\n+\n+            self.fp8_linear = init_fp8_linear_kernel(\n+                activation_quant_key=activation_quant_key,\n+                weight_quant_key=kFp8StaticTensorSym,\n+                out_dtype=torch.get_default_dtype(),\n+                module_name=self.__class__.__name__,\n             )\n \n     def create_weights(\n@@ -362,8 +368,6 @@ def create_weights(\n         params_dtype: torch.dtype,\n         **extra_weight_attrs,\n     ):\n-        maybe_create_device_identity()\n-\n         output_size_per_partition = sum(output_partition_sizes)\n         weight_loader = extra_weight_attrs.get(\"weight_loader\")\n         layer.logical_widths = output_partition_sizes\n@@ -462,8 +466,6 @@ def patched_weight_loader(param, loaded_weight, *args, **kwargs):\n                 scale = create_fp8_input_scale(output_partition_sizes, weight_loader)\n                 set_weight_attrs(scale, {\"scale_type\": \"input_scale\"})\n                 layer.register_parameter(\"input_scale\", scale)\n-            else:\n-                layer.register_parameter(\"input_scale\", None)\n \n     def process_weights_after_loading(self, layer: Module) -> None:\n         if getattr(layer, \"_already_called_process_weights_after_loading\", False):\n@@ -602,14 +604,7 @@ def apply(\n                 bias=bias,\n             )\n \n-        return self.fp8_linear.apply(\n-            input=x,\n-            weight=layer.weight,\n-            weight_scale=layer.weight_scale,\n-            out_dtype=self.out_dtype,\n-            input_scale=layer.input_scale,\n-            bias=bias,\n-        )\n+        return self.fp8_linear.apply_weights(layer, x, bias)\n \n \n class Fp8MoEMethod(FusedMoEMethodBase):", "patch_sha256": "642b41eb0eed9aa5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2536749978", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/fp8.py\nReview Comment:\nadded a branch to use `kFp8DynamicTensorSym` if `not self.act_q_static`\n\nOriginal Code Context:\n@@ -384,10 +380,10 @@ def __init__(self, quant_config: Fp8Config):\n             # Use per-token quantization for better perf if dynamic and cutlass\n             if not self.act_q_static and cutlass_fp8_supported():\n                 self.act_q_group_shape = GroupShape.PER_TOKEN\n-                self.activation_quant_key = kFp8StaticTokenSym\n+                self.activation_quant_key = kFp8DynamicTokenSym\n             else:\n                 self.act_q_group_shape = GroupShape.PER_TENSOR\n-                self.activation_quant_key = kFp8DynamicTensorSym\n+                self.activation_quant_key = kFp8StaticTensorSym\n", "materials": ["added a branch to use `kFp8DynamicTensorSym` if `not self.act_q_static`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -48,6 +48,9 @@\n     QuantizationConfig,\n     QuantizeMethodBase,\n )\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n from vllm.model_executor.layers.quantization.kv_cache import BaseKVCacheMethod\n from vllm.model_executor.layers.quantization.utils.flashinfer_utils import (\n     apply_fi_trtllm_fp8_per_tensor_moe,\n@@ -76,12 +79,13 @@\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     GroupShape,\n     is_layer_skipped,\n+    kFp8DynamicTensorSym,\n+    kFp8DynamicTokenSym,\n+    kFp8StaticTensorSym,\n )\n from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n-    Fp8LinearOp,\n     cutlass_block_fp8_supported,\n     cutlass_fp8_supported,\n-    maybe_create_device_identity,\n     normalize_e4m3fn_to_e4m3fnuz,\n )\n from vllm.model_executor.parameter import (\n@@ -328,28 +332,30 @@ def __init__(self, quant_config: Fp8Config):\n         self.weight_block_size = self.quant_config.weight_block_size\n         self.block_quant = self.weight_block_size is not None\n         self.act_q_static = self.quant_config.activation_scheme == \"static\"\n-        if self.weight_block_size:\n-            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n-        else:\n-            # Use per-token quantization for better perf if dynamic and cutlass\n-            if not self.act_q_static and cutlass_fp8_supported():\n-                self.act_q_group_shape = GroupShape.PER_TOKEN\n-            else:\n-                self.act_q_group_shape = GroupShape.PER_TENSOR\n \n         if self.block_quant:\n             assert not self.act_q_static\n             assert self.weight_block_size is not None\n             self.w8a8_block_fp8_linear = W8A8BlockFp8LinearOp(\n                 weight_group_shape=GroupShape(*self.weight_block_size),\n-                act_quant_group_shape=self.act_q_group_shape,\n+                act_quant_group_shape=GroupShape(1, self.weight_block_size[0]),\n                 cutlass_block_fp8_supported=self.cutlass_block_fp8_supported,\n                 use_aiter_and_is_supported=self.use_aiter_and_is_supported,\n             )\n         else:\n-            self.fp8_linear = Fp8LinearOp(\n-                act_quant_static=self.act_q_static,\n-                act_quant_group_shape=self.act_q_group_shape,\n+            # Use per-token quantization for better perf if dynamic and cutlass\n+            if self.act_q_static:\n+                activation_quant_key = kFp8StaticTensorSym\n+            elif cutlass_fp8_supported():\n+                activation_quant_key = kFp8DynamicTokenSym\n+            else:\n+                activation_quant_key = kFp8DynamicTensorSym\n+\n+            self.fp8_linear = init_fp8_linear_kernel(\n+                activation_quant_key=activation_quant_key,\n+                weight_quant_key=kFp8StaticTensorSym,\n+                out_dtype=torch.get_default_dtype(),\n+                module_name=self.__class__.__name__,\n             )\n \n     def create_weights(\n@@ -362,8 +368,6 @@ def create_weights(\n         params_dtype: torch.dtype,\n         **extra_weight_attrs,\n     ):\n-        maybe_create_device_identity()\n-\n         output_size_per_partition = sum(output_partition_sizes)\n         weight_loader = extra_weight_attrs.get(\"weight_loader\")\n         layer.logical_widths = output_partition_sizes\n@@ -462,8 +466,6 @@ def patched_weight_loader(param, loaded_weight, *args, **kwargs):\n                 scale = create_fp8_input_scale(output_partition_sizes, weight_loader)\n                 set_weight_attrs(scale, {\"scale_type\": \"input_scale\"})\n                 layer.register_parameter(\"input_scale\", scale)\n-            else:\n-                layer.register_parameter(\"input_scale\", None)\n \n     def process_weights_after_loading(self, layer: Module) -> None:\n         if getattr(layer, \"_already_called_process_weights_after_loading\", False):\n@@ -602,14 +604,7 @@ def apply(\n                 bias=bias,\n             )\n \n-        return self.fp8_linear.apply(\n-            input=x,\n-            weight=layer.weight,\n-            weight_scale=layer.weight_scale,\n-            out_dtype=self.out_dtype,\n-            input_scale=layer.input_scale,\n-            bias=bias,\n-        )\n+        return self.fp8_linear.apply_weights(layer, x, bias)\n \n \n class Fp8MoEMethod(FusedMoEMethodBase):", "patch_sha256": "642b41eb0eed9aa5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2538330153", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py\nReview Comment:\n**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  FlashInfer FP8 linear output not reshaped**\n\nThe FlashInfer FP8 path returns the raw `flashinfer_scaled_fp8_mm` output without applying the `output_shape` provided by `FP8ScaledMMLinearKernel.apply_weights`, which flattens inputs to 2D. For any batched input (e.g., `[batch, seq, hidden]`), this leaves the result as a 2D `(M, N)` tensor instead of restoring the original batch/sequence dimensions, unlike the other scaled_mm kernels that narrow and `view` to `output_shape`. This will produce incorrectly shaped outputs whenever FlashInfer is selected for non-2D inputs.\n\nUseful? React with 👍 / 👎.\n\nOriginal Code Context:\n@@ -0,0 +1,76 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from collections.abc import Callable\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+def flashinfer_w8a8_scaled_mm(\n+    *,\n+    A: torch.Tensor,\n+    B: torch.Tensor,\n+    out_dtype: torch.dtype,\n+    As: torch.Tensor,\n+    Bs: torch.Tensor,\n+    bias: torch.Tensor,\n+    output_shape: list,\n+) -> torch.Tensor:\n+    return flashinfer_scaled_fp8_mm(\n+        A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+    )\n", "materials": ["**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  FlashInfer FP8 linear output not reshaped**\n\nThe FlashInfer FP8 path returns the raw `flashinfer_scaled_fp8_mm` output without applying the `output_shape` provided by `FP8ScaledMMLinearKernel.apply_weights`, which flattens inputs to 2D. For any batched input (e.g., `[batch, seq, hidden]`), this leaves the result as a 2D `(M, N)` tensor instead of restoring the original batch/sequence dimensions, unlike the other scaled_mm kernels that narrow and `view` to `output_shape`. This will produce incorrectly shaped outputs whenever FlashInfer is selected for non-2D inputs.\n\nUseful? React with 👍 / 👎."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,57 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )", "patch_sha256": "298db567412a54f3"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2662426613", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/aiter.py\nReview Comment:\nLet's get rid of `get_min_capability` and just implement `is_supported` here, that makes it more general as some devices don't have a concept of compute capability anyway\n\nOriginal Code Context:\n@@ -9,27 +9,22 @@\n from vllm.platforms import current_platform\n \n from .cutlass import CutlassScaledMMLinearKernel\n-from .ScaledMMLinearKernel import ScaledMMLinearLayerConfig\n+from .ScaledMMLinearKernel import Int8ScaledMMLinearLayerConfig\n \n \n class AiterScaledMMLinearKernel(CutlassScaledMMLinearKernel):\n     @classmethod\n-    def is_supported(\n-        cls, compute_capability: int | None = None\n-    ) -> tuple[bool, str | None]:\n+    def get_min_capability(cls) -> int:\n+        return 90\n+\n+    @classmethod\n+    def is_platform_supported(cls) -> tuple[bool, str | None]:\n         if not current_platform.is_rocm():\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel requires `aiter` which is not \"\n-                + \"currently supported on non-ROCm platform.\",\n-            )\n-        if compute_capability is None:\n-            _cc = current_platform.get_device_capability()\n-            if _cc is not None:\n-                compute_capability = _cc.major * 10 + _cc.minor\n-        if compute_capability is not None and compute_capability < 90:\n-            return False, f\"requires capability 90, got {compute_capability}\"\n+            return False, \"ROCm\"\n+        return True, None\n", "materials": ["Let's get rid of `get_min_capability` and just implement `is_supported` here, that makes it more general as some devices don't have a concept of compute capability anyway"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -8,89 +8,70 @@\n from vllm._aiter_ops import rocm_aiter_ops\n from vllm.platforms import current_platform\n \n-from .cutlass import CutlassScaledMMLinearKernel\n-from .ScaledMMLinearKernel import ScaledMMLinearLayerConfig\n+from .cutlass import CutlassInt8ScaledMMLinearKernel\n+from .ScaledMMLinearKernel import Int8ScaledMMLinearLayerConfig\n \n \n-class AiterScaledMMLinearKernel(CutlassScaledMMLinearKernel):\n+class AiterInt8ScaledMMLinearKernel(CutlassInt8ScaledMMLinearKernel):\n     @classmethod\n     def is_supported(\n         cls, compute_capability: int | None = None\n     ) -> tuple[bool, str | None]:\n         if not current_platform.is_rocm():\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel requires `aiter` which is not \"\n-                + \"currently supported on non-ROCm platform.\",\n-            )\n-        if compute_capability is None:\n-            _cc = current_platform.get_device_capability()\n-            if _cc is not None:\n-                compute_capability = _cc.major * 10 + _cc.minor\n+            return False, \"Requires ROCm.\"\n+\n         if compute_capability is not None and compute_capability < 90:\n-            return False, f\"requires capability 90, got {compute_capability}\"\n+            return False, \"requires compute capability 90 and above.\"\n \n         try:\n             import aiter  # noqa: F401 # deliberately attempt to import aiter\n         except Exception:\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel requires `aiter` which is not \"\n-                + \"installed on ROCm.\",\n-            )\n+            return False, \"requires `aiter` to be installed.\"\n \n         if not rocm_aiter_ops.is_linear_enabled():\n             return (\n                 False,\n-                \"AiterScaledMMLinearKernel is disabled. \"\n-                + \"Enable by setting `VLLM_ROCM_USE_AITER=1` \"\n+                \"requires setting `VLLM_ROCM_USE_AITER=1` \"\n                 + \"and `VLLM_ROCM_USE_AITER_LINEAR=1`. \"\n                 + \"`VLLM_ROCM_USE_AITER_LINEAR` default is True.\",\n             )\n-\n         return True, None\n \n     @classmethod\n-    def can_implement(cls, c: ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+    def can_implement(cls, c: Int8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n         if not c.input_symmetric:\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel only supports symmetric \" + \"quantization.\",\n-            )\n+            return False, \"supports symmetric quantization only.\"\n         return True, None\n \n-    def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n-        super().process_weights_after_loading(layer)\n-\n     def apply_weights(\n         self,\n         layer: torch.nn.Module,\n         x: torch.Tensor,\n         bias: torch.Tensor | None = None,\n     ) -> torch.Tensor:\n         \"\"\"\n-        `AiterScaledMMLinearKernel` implements a fused version of\n+        `AiterInt8ScaledMMLinearKernel` implements a fused version of\n             `output = torch.mm((scale_a * a), (scale_b * b)).to(out_dtype)`\n         where scale_a * a and scale_b * b are implemented using numpy-style\n         broadcasting.\n         Currently only support per-tensor-per-tensor GEMM\n         and per-token-per-channel GEMM through AITER\n-        w8a8 scaled gemm. `AiterScaledMMLinearKernel` also does not support\n+        w8a8 scaled gemm. `AiterInt8ScaledMMLinearKernel` also does not support\n         ATIER block scaled GEMM and mix-precision GEMM.\n         \"\"\"\n-        w_q, w_s, i_s, i_zp, azp_adj = self._get_weight_params(layer)\n+        w_q, w_s, i_s, i_zp, azp_adj = self._get_layer_params(layer)\n \n         # ops.scaled_int8_quant supports both dynamic and static quant:\n         # * dynamic, i_s is None and x_s computed from x.\n         # * static, i_s is scalar and x_s is i_s.\n         symmetric = azp_adj is None\n         assert symmetric, (\n-            \"AiterScaledMMLinearKernel only supports symmetric quantization.\"\n+            \"AiterInt8ScaledMMLinearKernel only supports symmetric quantization.\"\n         )\n         x_q, x_s, x_zp = ops.scaled_int8_quant(x, i_s, i_zp, symmetric=symmetric)\n \n         assert x_zp is None, (\n-            \"AiterScaledMMLinearKernel only supports symmetric quantization.\"\n+            \"AiterInt8ScaledMMLinearKernel only supports symmetric quantization.\"\n         )\n         out_dtype = x.dtype\n \n@@ -117,12 +98,12 @@ def apply_weights(\n         ), (\n             \"Currently only support per-tensor-per-tensor GEMM \"\n             + \" and per-token-per-channel GEMM through AITER\"\n-            \" w8a8 scaled gemm. `AiterScaledMMLinearKernel` \"\n+            \" w8a8 scaled gemm. `AiterInt8ScaledMMLinearKernel` \"\n             + \"does not support AITER block scaled GEMM.\"\n         )\n \n         # gemm_a8w8_CK(a, b, scale_a, scale_b, bias) expects\n         # a to be [M, K]\n         # b to be [N, K]\n-        # CutlassScaledMMLinearKernel prepare weight `w_q` in [K, N] format\n+        # CutlassInt8ScaledMMLinearKernel prepare weight `w_q` in [K, N] format\n         return rocm_aiter_ops.gemm_a8w8(x_q, w_q.t(), x_s, w_s, bias, out_dtype)", "patch_sha256": "d87b3125d747a815"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2662442607", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/cutlass.py\nReview Comment:\nWe can remove these once we just implement `CutlassScaledMMLinearKernel.apply_scaled_mm`\n\nOriginal Code Context:\n@@ -11,35 +13,51 @@\n )\n from vllm.platforms import current_platform\n \n-from .ScaledMMLinearKernel import ScaledMMLinearKernel, ScaledMMLinearLayerConfig\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+    Int8ScaledMMLinearKernel,\n+    Int8ScaledMMLinearLayerConfig,\n+)\n \n \n-class CutlassScaledMMLinearKernel(ScaledMMLinearKernel):\n+def cutlass_w8a8_scaled_mm_fp8(\n", "materials": ["We can remove these once we just implement `CutlassScaledMMLinearKernel.apply_scaled_mm`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -11,35 +11,36 @@\n )\n from vllm.platforms import current_platform\n \n-from .ScaledMMLinearKernel import ScaledMMLinearKernel, ScaledMMLinearLayerConfig\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+    Int8ScaledMMLinearKernel,\n+    Int8ScaledMMLinearLayerConfig,\n+)\n \n \n-class CutlassScaledMMLinearKernel(ScaledMMLinearKernel):\n+class CutlassInt8ScaledMMLinearKernel(Int8ScaledMMLinearKernel):\n     @classmethod\n     def is_supported(\n         cls, compute_capability: int | None = None\n     ) -> tuple[bool, str | None]:\n         if not current_platform.is_cuda():\n-            return False, \"Requires CUDA.\"\n-        if compute_capability is None:\n-            _cc = current_platform.get_device_capability()\n-            if _cc is not None:\n-                compute_capability = _cc.major * 10 + _cc.minor\n-        if compute_capability is not None and compute_capability < 75:\n-            return False, f\"requires capability 75, got {compute_capability}\"\n+            return False, \"requires CUDA.\"\n         return True, None\n \n     @classmethod\n-    def can_implement(cls, c: ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+    def can_implement(cls, c: Int8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n         return True, None\n \n     def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n+        w_q_name, w_s_name, i_s_name, i_zp_name, azp_adj_name = self.layer_param_names\n+        config = self.config\n         # WEIGHT\n         # Cutlass kernels need transposed weight.\n-        weight = getattr(layer, self.w_q_name)\n+        weight = getattr(layer, w_q_name)\n         replace_parameter(\n             layer,\n-            self.w_q_name,\n+            w_q_name,\n             torch.nn.Parameter(weight.t().data, requires_grad=False),\n         )\n \n@@ -48,28 +49,28 @@ def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n         # If we have a fused module (QKV, MLP) with per tensor scales (thus N\n         # scales being passed to the kernel), convert to the per-channel case.\n         is_fused_module = len(layer.logical_widths) > 1\n-        weight_scale = getattr(layer, self.w_s_name)\n-        if is_fused_module and not self.config.is_channelwise:\n+        weight_scale = getattr(layer, w_s_name)\n+        if is_fused_module and not config.is_channelwise:\n             weight_scale = convert_to_channelwise(weight_scale, layer.logical_widths)\n         replace_parameter(\n             layer,\n-            self.w_s_name,\n+            w_s_name,\n             torch.nn.Parameter(weight_scale.data, requires_grad=False),\n         )\n \n         # INPUT SCALE\n-        if self.config.is_static_input_scheme:\n-            input_scale = getattr(layer, self.i_s_name)\n+        if config.is_static_input_scheme:\n+            input_scale = getattr(layer, i_s_name)\n \n-            if self.config.input_symmetric:\n+            if config.input_symmetric:\n                 replace_parameter(\n                     layer,\n-                    self.i_s_name,\n+                    i_s_name,\n                     torch.nn.Parameter(input_scale.max(), requires_grad=False),\n                 )\n-                setattr(layer, self.i_zp_name, None)\n+                setattr(layer, i_zp_name, None)\n             else:\n-                input_zero_point = getattr(layer, self.i_zp_name)\n+                input_zero_point = getattr(layer, i_zp_name)\n \n                 # reconstruct the ranges\n                 int8_traits = torch.iinfo(torch.int8)\n@@ -79,46 +80,40 @@ def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n \n                 scale = (range_max - range_min) / (int8_traits.max - int8_traits.min)\n                 replace_parameter(\n-                    layer, self.i_s_name, torch.nn.Parameter(scale, requires_grad=False)\n+                    layer, i_s_name, torch.nn.Parameter(scale, requires_grad=False)\n                 )\n \n                 # AZP loaded as int8 but used as int32\n                 azp = (int8_traits.min - range_min / scale).to(dtype=torch.int32)\n                 replace_parameter(\n-                    layer, self.i_zp_name, torch.nn.Parameter(azp, requires_grad=False)\n+                    layer, i_zp_name, torch.nn.Parameter(azp, requires_grad=False)\n                 )\n \n-        else:\n-            setattr(layer, self.i_s_name, None)\n-            setattr(layer, self.i_zp_name, None)\n-\n         # azp_adj is the AZP adjustment term, used to account for weights.\n         # It does not depend on scales or azp, so it is the same for\n         # static and dynamic quantization.\n         # For more details, see csrc/quantization/w8a8/cutlass/Epilogues.md\n         # https://github.com/vllm-project/vllm/blob/main/csrc/quantization/w8a8/cutlass/Epilogues.md\n-        if not self.config.input_symmetric:\n-            weight = getattr(layer, self.w_q_name)\n+        if not config.input_symmetric:\n+            weight = getattr(layer, w_q_name)\n             azp_adj = weight.sum(dim=0, keepdim=True, dtype=torch.int32)\n-            if self.config.is_static_input_scheme:\n+            if config.is_static_input_scheme:\n                 # cutlass_w8a8 requires azp to be folded into azp_adj\n                 # in the per-tensor case\n-                azp_adj = getattr(layer, self.i_zp_name) * azp_adj\n+                azp_adj = getattr(layer, i_zp_name) * azp_adj\n             setattr(\n                 layer,\n-                self.azp_adj_name,\n+                azp_adj_name,\n                 torch.nn.Parameter(azp_adj, requires_grad=False),\n             )\n-        else:\n-            setattr(layer, self.azp_adj_name, None)\n \n     def apply_weights(\n         self,\n         layer: torch.nn.Module,\n         x: torch.Tensor,\n         bias: torch.Tensor | None = None,\n     ) -> torch.Tensor:\n-        w_q, w_s, i_s, i_zp, azp_adj = self._get_weight_params(layer)\n+        w_q, w_s, i_s, i_zp, azp_adj = self._get_layer_params(layer)\n \n         # ops.scaled_int8_quant supports both dynamic and static quant:\n         # * dynamic, i_s is None and x_s computed from x.\n@@ -145,3 +140,34 @@ def apply_weights(\n         return ops.cutlass_scaled_mm(\n             x_q, w_q, scale_a=x_s, scale_b=w_s, out_dtype=x.dtype, bias=bias\n         )\n+\n+\n+class CutlassFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        # Fused GEMM_DQ\n+        output = ops.cutlass_scaled_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )\n+        return output.view(*output_shape)", "patch_sha256": "46f5d7d2d8eaf50a"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2665740935", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/aiter.py\nReview Comment:\nAlso, let's do the import check inside `is_platform_supported` instead of `can_implement`\n\nOriginal Code Context:\n@@ -9,27 +9,22 @@\n from vllm.platforms import current_platform\n \n from .cutlass import CutlassScaledMMLinearKernel\n-from .ScaledMMLinearKernel import ScaledMMLinearLayerConfig\n+from .ScaledMMLinearKernel import Int8ScaledMMLinearLayerConfig\n \n \n class AiterScaledMMLinearKernel(CutlassScaledMMLinearKernel):\n     @classmethod\n-    def is_supported(\n-        cls, compute_capability: int | None = None\n-    ) -> tuple[bool, str | None]:\n+    def get_min_capability(cls) -> int:\n+        return 90\n+\n+    @classmethod\n+    def is_platform_supported(cls) -> tuple[bool, str | None]:\n         if not current_platform.is_rocm():\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel requires `aiter` which is not \"\n-                + \"currently supported on non-ROCm platform.\",\n-            )\n-        if compute_capability is None:\n-            _cc = current_platform.get_device_capability()\n-            if _cc is not None:\n-                compute_capability = _cc.major * 10 + _cc.minor\n-        if compute_capability is not None and compute_capability < 90:\n-            return False, f\"requires capability 90, got {compute_capability}\"\n+            return False, \"ROCm\"\n+        return True, None\n \n+    @classmethod\n+    def can_implement(cls, c: Int8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n", "materials": ["Also, let's do the import check inside `is_platform_supported` instead of `can_implement`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -8,89 +8,70 @@\n from vllm._aiter_ops import rocm_aiter_ops\n from vllm.platforms import current_platform\n \n-from .cutlass import CutlassScaledMMLinearKernel\n-from .ScaledMMLinearKernel import ScaledMMLinearLayerConfig\n+from .cutlass import CutlassInt8ScaledMMLinearKernel\n+from .ScaledMMLinearKernel import Int8ScaledMMLinearLayerConfig\n \n \n-class AiterScaledMMLinearKernel(CutlassScaledMMLinearKernel):\n+class AiterInt8ScaledMMLinearKernel(CutlassInt8ScaledMMLinearKernel):\n     @classmethod\n     def is_supported(\n         cls, compute_capability: int | None = None\n     ) -> tuple[bool, str | None]:\n         if not current_platform.is_rocm():\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel requires `aiter` which is not \"\n-                + \"currently supported on non-ROCm platform.\",\n-            )\n-        if compute_capability is None:\n-            _cc = current_platform.get_device_capability()\n-            if _cc is not None:\n-                compute_capability = _cc.major * 10 + _cc.minor\n+            return False, \"Requires ROCm.\"\n+\n         if compute_capability is not None and compute_capability < 90:\n-            return False, f\"requires capability 90, got {compute_capability}\"\n+            return False, \"requires compute capability 90 and above.\"\n \n         try:\n             import aiter  # noqa: F401 # deliberately attempt to import aiter\n         except Exception:\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel requires `aiter` which is not \"\n-                + \"installed on ROCm.\",\n-            )\n+            return False, \"requires `aiter` to be installed.\"\n \n         if not rocm_aiter_ops.is_linear_enabled():\n             return (\n                 False,\n-                \"AiterScaledMMLinearKernel is disabled. \"\n-                + \"Enable by setting `VLLM_ROCM_USE_AITER=1` \"\n+                \"requires setting `VLLM_ROCM_USE_AITER=1` \"\n                 + \"and `VLLM_ROCM_USE_AITER_LINEAR=1`. \"\n                 + \"`VLLM_ROCM_USE_AITER_LINEAR` default is True.\",\n             )\n-\n         return True, None\n \n     @classmethod\n-    def can_implement(cls, c: ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+    def can_implement(cls, c: Int8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n         if not c.input_symmetric:\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel only supports symmetric \" + \"quantization.\",\n-            )\n+            return False, \"supports symmetric quantization only.\"\n         return True, None\n \n-    def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n-        super().process_weights_after_loading(layer)\n-\n     def apply_weights(\n         self,\n         layer: torch.nn.Module,\n         x: torch.Tensor,\n         bias: torch.Tensor | None = None,\n     ) -> torch.Tensor:\n         \"\"\"\n-        `AiterScaledMMLinearKernel` implements a fused version of\n+        `AiterInt8ScaledMMLinearKernel` implements a fused version of\n             `output = torch.mm((scale_a * a), (scale_b * b)).to(out_dtype)`\n         where scale_a * a and scale_b * b are implemented using numpy-style\n         broadcasting.\n         Currently only support per-tensor-per-tensor GEMM\n         and per-token-per-channel GEMM through AITER\n-        w8a8 scaled gemm. `AiterScaledMMLinearKernel` also does not support\n+        w8a8 scaled gemm. `AiterInt8ScaledMMLinearKernel` also does not support\n         ATIER block scaled GEMM and mix-precision GEMM.\n         \"\"\"\n-        w_q, w_s, i_s, i_zp, azp_adj = self._get_weight_params(layer)\n+        w_q, w_s, i_s, i_zp, azp_adj = self._get_layer_params(layer)\n \n         # ops.scaled_int8_quant supports both dynamic and static quant:\n         # * dynamic, i_s is None and x_s computed from x.\n         # * static, i_s is scalar and x_s is i_s.\n         symmetric = azp_adj is None\n         assert symmetric, (\n-            \"AiterScaledMMLinearKernel only supports symmetric quantization.\"\n+            \"AiterInt8ScaledMMLinearKernel only supports symmetric quantization.\"\n         )\n         x_q, x_s, x_zp = ops.scaled_int8_quant(x, i_s, i_zp, symmetric=symmetric)\n \n         assert x_zp is None, (\n-            \"AiterScaledMMLinearKernel only supports symmetric quantization.\"\n+            \"AiterInt8ScaledMMLinearKernel only supports symmetric quantization.\"\n         )\n         out_dtype = x.dtype\n \n@@ -117,12 +98,12 @@ def apply_weights(\n         ), (\n             \"Currently only support per-tensor-per-tensor GEMM \"\n             + \" and per-token-per-channel GEMM through AITER\"\n-            \" w8a8 scaled gemm. `AiterScaledMMLinearKernel` \"\n+            \" w8a8 scaled gemm. `AiterInt8ScaledMMLinearKernel` \"\n             + \"does not support AITER block scaled GEMM.\"\n         )\n \n         # gemm_a8w8_CK(a, b, scale_a, scale_b, bias) expects\n         # a to be [M, K]\n         # b to be [N, K]\n-        # CutlassScaledMMLinearKernel prepare weight `w_q` in [K, N] format\n+        # CutlassInt8ScaledMMLinearKernel prepare weight `w_q` in [K, N] format\n         return rocm_aiter_ops.gemm_a8w8(x_q, w_q.t(), x_s, w_s, bias, out_dtype)", "patch_sha256": "d87b3125d747a815"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2667700645", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/aiter.py\nReview Comment:\nI reverted `is_platform_supported` to  `is_supported`. Also moved the import checks there.\n\nOriginal Code Context:\n@@ -9,27 +9,22 @@\n from vllm.platforms import current_platform\n \n from .cutlass import CutlassScaledMMLinearKernel\n-from .ScaledMMLinearKernel import ScaledMMLinearLayerConfig\n+from .ScaledMMLinearKernel import Int8ScaledMMLinearLayerConfig\n \n \n class AiterScaledMMLinearKernel(CutlassScaledMMLinearKernel):\n     @classmethod\n-    def is_supported(\n-        cls, compute_capability: int | None = None\n-    ) -> tuple[bool, str | None]:\n+    def get_min_capability(cls) -> int:\n+        return 90\n+\n+    @classmethod\n+    def is_platform_supported(cls) -> tuple[bool, str | None]:\n         if not current_platform.is_rocm():\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel requires `aiter` which is not \"\n-                + \"currently supported on non-ROCm platform.\",\n-            )\n-        if compute_capability is None:\n-            _cc = current_platform.get_device_capability()\n-            if _cc is not None:\n-                compute_capability = _cc.major * 10 + _cc.minor\n-        if compute_capability is not None and compute_capability < 90:\n-            return False, f\"requires capability 90, got {compute_capability}\"\n+            return False, \"ROCm\"\n+        return True, None\n \n+    @classmethod\n+    def can_implement(cls, c: Int8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n", "materials": ["I reverted `is_platform_supported` to  `is_supported`. Also moved the import checks there."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -8,89 +8,70 @@\n from vllm._aiter_ops import rocm_aiter_ops\n from vllm.platforms import current_platform\n \n-from .cutlass import CutlassScaledMMLinearKernel\n-from .ScaledMMLinearKernel import ScaledMMLinearLayerConfig\n+from .cutlass import CutlassInt8ScaledMMLinearKernel\n+from .ScaledMMLinearKernel import Int8ScaledMMLinearLayerConfig\n \n \n-class AiterScaledMMLinearKernel(CutlassScaledMMLinearKernel):\n+class AiterInt8ScaledMMLinearKernel(CutlassInt8ScaledMMLinearKernel):\n     @classmethod\n     def is_supported(\n         cls, compute_capability: int | None = None\n     ) -> tuple[bool, str | None]:\n         if not current_platform.is_rocm():\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel requires `aiter` which is not \"\n-                + \"currently supported on non-ROCm platform.\",\n-            )\n-        if compute_capability is None:\n-            _cc = current_platform.get_device_capability()\n-            if _cc is not None:\n-                compute_capability = _cc.major * 10 + _cc.minor\n+            return False, \"Requires ROCm.\"\n+\n         if compute_capability is not None and compute_capability < 90:\n-            return False, f\"requires capability 90, got {compute_capability}\"\n+            return False, \"requires compute capability 90 and above.\"\n \n         try:\n             import aiter  # noqa: F401 # deliberately attempt to import aiter\n         except Exception:\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel requires `aiter` which is not \"\n-                + \"installed on ROCm.\",\n-            )\n+            return False, \"requires `aiter` to be installed.\"\n \n         if not rocm_aiter_ops.is_linear_enabled():\n             return (\n                 False,\n-                \"AiterScaledMMLinearKernel is disabled. \"\n-                + \"Enable by setting `VLLM_ROCM_USE_AITER=1` \"\n+                \"requires setting `VLLM_ROCM_USE_AITER=1` \"\n                 + \"and `VLLM_ROCM_USE_AITER_LINEAR=1`. \"\n                 + \"`VLLM_ROCM_USE_AITER_LINEAR` default is True.\",\n             )\n-\n         return True, None\n \n     @classmethod\n-    def can_implement(cls, c: ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+    def can_implement(cls, c: Int8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n         if not c.input_symmetric:\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel only supports symmetric \" + \"quantization.\",\n-            )\n+            return False, \"supports symmetric quantization only.\"\n         return True, None\n \n-    def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n-        super().process_weights_after_loading(layer)\n-\n     def apply_weights(\n         self,\n         layer: torch.nn.Module,\n         x: torch.Tensor,\n         bias: torch.Tensor | None = None,\n     ) -> torch.Tensor:\n         \"\"\"\n-        `AiterScaledMMLinearKernel` implements a fused version of\n+        `AiterInt8ScaledMMLinearKernel` implements a fused version of\n             `output = torch.mm((scale_a * a), (scale_b * b)).to(out_dtype)`\n         where scale_a * a and scale_b * b are implemented using numpy-style\n         broadcasting.\n         Currently only support per-tensor-per-tensor GEMM\n         and per-token-per-channel GEMM through AITER\n-        w8a8 scaled gemm. `AiterScaledMMLinearKernel` also does not support\n+        w8a8 scaled gemm. `AiterInt8ScaledMMLinearKernel` also does not support\n         ATIER block scaled GEMM and mix-precision GEMM.\n         \"\"\"\n-        w_q, w_s, i_s, i_zp, azp_adj = self._get_weight_params(layer)\n+        w_q, w_s, i_s, i_zp, azp_adj = self._get_layer_params(layer)\n \n         # ops.scaled_int8_quant supports both dynamic and static quant:\n         # * dynamic, i_s is None and x_s computed from x.\n         # * static, i_s is scalar and x_s is i_s.\n         symmetric = azp_adj is None\n         assert symmetric, (\n-            \"AiterScaledMMLinearKernel only supports symmetric quantization.\"\n+            \"AiterInt8ScaledMMLinearKernel only supports symmetric quantization.\"\n         )\n         x_q, x_s, x_zp = ops.scaled_int8_quant(x, i_s, i_zp, symmetric=symmetric)\n \n         assert x_zp is None, (\n-            \"AiterScaledMMLinearKernel only supports symmetric quantization.\"\n+            \"AiterInt8ScaledMMLinearKernel only supports symmetric quantization.\"\n         )\n         out_dtype = x.dtype\n \n@@ -117,12 +98,12 @@ def apply_weights(\n         ), (\n             \"Currently only support per-tensor-per-tensor GEMM \"\n             + \" and per-token-per-channel GEMM through AITER\"\n-            \" w8a8 scaled gemm. `AiterScaledMMLinearKernel` \"\n+            \" w8a8 scaled gemm. `AiterInt8ScaledMMLinearKernel` \"\n             + \"does not support AITER block scaled GEMM.\"\n         )\n \n         # gemm_a8w8_CK(a, b, scale_a, scale_b, bias) expects\n         # a to be [M, K]\n         # b to be [N, K]\n-        # CutlassScaledMMLinearKernel prepare weight `w_q` in [K, N] format\n+        # CutlassInt8ScaledMMLinearKernel prepare weight `w_q` in [K, N] format\n         return rocm_aiter_ops.gemm_a8w8(x_q, w_q.t(), x_s, w_s, bias, out_dtype)", "patch_sha256": "d87b3125d747a815"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2674990827", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py\nReview Comment:\n### Typo in method name causes runtime error\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe method call `rocm_aiter_ops.is_linear_fp8_enaled()` contains a typo — it's missing the 'b' in \"enabled\". The correct method name is `is_linear_fp8_enabled()`. This will cause an `AttributeError` at runtime when block-wise FP8 quantization is used on ROCm.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py#L74-L75\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..eXeeOzgpTFPc-o559EpnMRuy3oxIQFg8jSDKqZpKhekenD4D7CWqLBKdeS6aPnpRUsNhRliQLDUPPKcBviTRoYfH_dqP3hDA0fkoUcmGzSUtcIfjs01pZ-xHyBu0-ybgrIBH7HqMARvV3Q3BMwkCxywECOo8wFhd8U0tyszZ1BzgNOUXm75z_T4SjVB5rIF8lFdWhaEC-X2kjox1op4lmP-zli74Zt6ULavXoEjXqZs5_HzR7DIHTwSLET5VR0rpzBeDacG_eXLzX07SHhnhCGTkNCtSLwQAHjowJ_2_-u71a1g8PWZmVdJyBt7Q3BtlNggoQkbRkArnAzKWlNLO0A\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..BVjzBG8Nk5V-DNXW4l0hHRHbYoM2OKyM1hncJOfb5C8X9mSoWl3bCi9l0JRz-siqPERD2QwYxHP_HOM9VFfPXSelt-CJHFygv_dVMTZSYNJ-3FX9WLYOpb_SmV_IEyY5R1sibQtZvlSEay42M_ygh5gS3Wn_DUKiGmPtIRnXYFQC4VpJqEkzciv_XOOOpCp2t_YBSlEZUN32zm3bkp_Xbsn_J84z_g8UGAol6reAkbRm_xsrsrYUz2YrBKBAc3gWwtiJs4YFS1nPBYyWISR6Adp6eVSpoAETNd1gILnBMWyVZU1MyEy80ak2qBfzhEvTloc64d06ZnmnSdQfNzZE8w\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -42,39 +49,46 @@\n     QuantizationStrategy.TENSOR: PerTensorScaleParameter,\n }\n \n+STATIC_QUANT = True\n+DYNAMIC_QUANT = False\n+activation_quant_key_mapping = {\n+    STATIC_QUANT: kFp8StaticTensorSym,\n+    DYNAMIC_QUANT: kFp8DynamicTokenSym,\n+}\n+weight_quant_key_mapping = {\n+    QuantizationStrategy.CHANNEL: kFp8StaticTokenSym,\n+    QuantizationStrategy.TENSOR: kFp8StaticTensorSym,\n+}\n+logger = init_logger(__name__)\n+\n \n class CompressedTensorsW8A8Fp8(CompressedTensorsScheme):\n     def __init__(self, weight_quant: QuantizationArgs, is_static_input_scheme: bool):\n         self.weight_quant = weight_quant\n         self.strategy = weight_quant.strategy\n         self.out_dtype = torch.get_default_dtype()\n         self.is_static_input_scheme = is_static_input_scheme\n-\n         self.weight_block_size = self.weight_quant.block_structure\n-        if self.weight_block_size is not None:\n-            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n-        else:\n-            self.act_q_group_shape = (\n-                GroupShape.PER_TENSOR\n-                if is_static_input_scheme\n-                else GroupShape.PER_TOKEN\n-            )\n-\n-        self.cutlass_block_fp8_supported = cutlass_block_fp8_supported()\n-        self.use_aiter_and_is_supported = rocm_aiter_ops.is_linear_fp8_enabled()\n \n         if self.weight_block_size is not None:\n+            self.cutlass_block_fp8_supported = cutlass_block_fp8_supported()\n+            self.use_aiter_and_is_supported = rocm_aiter_ops.is_linear_fp8_enaled()\n", "materials": ["### Typo in method name causes runtime error\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe method call `rocm_aiter_ops.is_linear_fp8_enaled()` contains a typo — it's missing the 'b' in \"enabled\". The correct method name is `is_linear_fp8_enabled()`. This will cause an `AttributeError` at runtime when block-wise FP8 quantization is used on ROCm.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py#L74-L75\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..eXeeOzgpTFPc-o559EpnMRuy3oxIQFg8jSDKqZpKhekenD4D7CWqLBKdeS6aPnpRUsNhRliQLDUPPKcBviTRoYfH_dqP3hDA0fkoUcmGzSUtcIfjs01pZ-xHyBu0-ybgrIBH7HqMARvV3Q3BMwkCxywECOo8wFhd8U0tyszZ1BzgNOUXm75z_T4SjVB5rIF8lFdWhaEC-X2kjox1op4lmP-zli74Zt6ULavXoEjXqZs5_HzR7DIHTwSLET5VR0rpzBeDacG_eXLzX07SHhnhCGTkNCtSLwQAHjowJ_2_-u71a1g8PWZmVdJyBt7Q3BtlNggoQkbRkArnAzKWlNLO0A\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..BVjzBG8Nk5V-DNXW4l0hHRHbYoM2OKyM1hncJOfb5C8X9mSoWl3bCi9l0JRz-siqPERD2QwYxHP_HOM9VFfPXSelt-CJHFygv_dVMTZSYNJ-3FX9WLYOpb_SmV_IEyY5R1sibQtZvlSEay42M_ygh5gS3Wn_DUKiGmPtIRnXYFQC4VpJqEkzciv_XOOOpCp2t_YBSlEZUN32zm3bkp_Xbsn_J84z_g8UGAol6reAkbRm_xsrsrYUz2YrBKBAc3gWwtiJs4YFS1nPBYyWISR6Adp6eVSpoAETNd1gILnBMWyVZU1MyEy80ak2qBfzhEvTloc64d06ZnmnSdQfNzZE8w\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -8,9 +8,13 @@\n from torch.nn import Parameter\n \n from vllm._aiter_ops import rocm_aiter_ops\n+from vllm.logger import init_logger\n from vllm.model_executor.layers.quantization.compressed_tensors.schemes import (\n     CompressedTensorsScheme,\n )\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n from vllm.model_executor.layers.quantization.utils.fp8_utils import (\n     W8A8BlockFp8LinearOp,\n     create_fp8_input_scale,\n@@ -22,11 +26,14 @@\n     process_fp8_weight_tensor_strategy,\n     validate_fp8_block_shape,\n )\n-from vllm.model_executor.layers.quantization.utils.quant_utils import GroupShape\n+from vllm.model_executor.layers.quantization.utils.quant_utils import (\n+    GroupShape,\n+    kFp8DynamicTokenSym,\n+    kFp8StaticTensorSym,\n+    kFp8StaticTokenSym,\n+)\n from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n-    Fp8LinearOp,\n     cutlass_block_fp8_supported,\n-    maybe_create_device_identity,\n )\n from vllm.model_executor.parameter import (\n     BlockQuantScaleParameter,\n@@ -42,39 +49,46 @@\n     QuantizationStrategy.TENSOR: PerTensorScaleParameter,\n }\n \n+STATIC_QUANT = True\n+DYNAMIC_QUANT = False\n+activation_quant_key_mapping = {\n+    STATIC_QUANT: kFp8StaticTensorSym,\n+    DYNAMIC_QUANT: kFp8DynamicTokenSym,\n+}\n+weight_quant_key_mapping = {\n+    QuantizationStrategy.CHANNEL: kFp8StaticTokenSym,\n+    QuantizationStrategy.TENSOR: kFp8StaticTensorSym,\n+}\n+logger = init_logger(__name__)\n+\n \n class CompressedTensorsW8A8Fp8(CompressedTensorsScheme):\n     def __init__(self, weight_quant: QuantizationArgs, is_static_input_scheme: bool):\n         self.weight_quant = weight_quant\n         self.strategy = weight_quant.strategy\n         self.out_dtype = torch.get_default_dtype()\n         self.is_static_input_scheme = is_static_input_scheme\n-\n         self.weight_block_size = self.weight_quant.block_structure\n-        if self.weight_block_size is not None:\n-            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n-        else:\n-            self.act_q_group_shape = (\n-                GroupShape.PER_TENSOR\n-                if is_static_input_scheme\n-                else GroupShape.PER_TOKEN\n-            )\n-\n-        self.cutlass_block_fp8_supported = cutlass_block_fp8_supported()\n-        self.use_aiter_and_is_supported = rocm_aiter_ops.is_linear_fp8_enabled()\n \n         if self.weight_block_size is not None:\n+            self.cutlass_block_fp8_supported = cutlass_block_fp8_supported()\n+            self.use_aiter_and_is_supported = rocm_aiter_ops.is_linear_fp8_enabled()\n             assert not self.is_static_input_scheme\n+            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n             self.w8a8_block_fp8_linear = W8A8BlockFp8LinearOp(\n                 weight_group_shape=GroupShape(*self.weight_block_size),\n                 act_quant_group_shape=self.act_q_group_shape,\n                 cutlass_block_fp8_supported=self.cutlass_block_fp8_supported,\n                 use_aiter_and_is_supported=self.use_aiter_and_is_supported,\n             )\n         else:\n-            self.fp8_linear = Fp8LinearOp(\n-                act_quant_static=self.is_static_input_scheme,\n-                act_quant_group_shape=self.act_q_group_shape,\n+            activation_quant_key = activation_quant_key_mapping[is_static_input_scheme]\n+            weight_quant_key = weight_quant_key_mapping[self.strategy]\n+            self.fp8_linear = init_fp8_linear_kernel(\n+                activation_quant_key=activation_quant_key,\n+                weight_quant_key=weight_quant_key,\n+                out_dtype=self.out_dtype,\n+                module_name=self.__class__.__name__,\n             )\n \n     @classmethod\n@@ -93,8 +107,6 @@ def create_weights(\n         weight_loader: Callable,\n         **kwargs,\n     ):\n-        maybe_create_device_identity()\n-\n         output_size_per_partition = sum(output_partition_sizes)\n         layer.logical_widths = output_partition_sizes\n         layer.weight_block_size = None\n@@ -143,7 +155,6 @@ def process_weights_after_loading(self, layer) -> None:\n                 getattr(layer, \"input_scale\", None),\n             )\n             weight = weight.t()\n-\n         elif self.strategy == QuantizationStrategy.CHANNEL:\n             weight, weight_scale, input_scale = process_fp8_weight_channel_strategy(\n                 layer.weight, layer.weight_scale, getattr(layer, \"input_scale\", None)\n@@ -174,7 +185,6 @@ def process_weights_after_loading(self, layer) -> None:\n             layer.input_scale = Parameter(layer.input_scale.max(), requires_grad=False)\n         else:\n             layer.input_scale = None\n-\n         if self.strategy == QuantizationStrategy.BLOCK:\n             maybe_post_process_fp8_weight_block(layer)\n \n@@ -193,11 +203,4 @@ def apply_weights(\n                 bias=bias,\n             )\n \n-        return self.fp8_linear.apply(\n-            input=x,\n-            weight=layer.weight,\n-            weight_scale=layer.weight_scale,\n-            out_dtype=self.out_dtype,\n-            input_scale=layer.input_scale,\n-            bias=bias,\n-        )\n+        return self.fp8_linear.apply_weights(layer, x, bias)", "patch_sha256": "9ffc94d3ca8687e4"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2674990831", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py\nReview Comment:\n### Compute capability check contradicts error message\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe check `compute_capability < 100` returns a failure with message \"requires compute capability 89 and above.\" This is inconsistent — compute capability 89 (Ada Lovelace) would fail this check since 89 < 100. The `TorchScaledMMLinearKernel` in `pytorch.py` correctly uses `< 89` for the same message. Either the check or the message is wrong.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py#L26-L28\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..FRmf1PE6mrfQFP9UfPx-EkauRQIxDm9OXtTQxkaPWf_RqOoxbYd7aTPih_tzUjfCHM9H_6PO4180V8jvRCrhwhLzpZlY24nZm_FrS8sd1Nz9nr-vd-LadGbblIRgx8UpLXjfPOqVDfhUb9UUlW2Zx0yZZ0Dod5_TnIg2UQ0JwXPysHPry6dyOFXSvomZHBum2y0D24v9Lg12FxWGCjBGJIZs-tEAhxz-9qOf4D2KPOG7iFud_6qv4U6Y9rmDi9g3VBZ5r_JV_0tAa_-KN6ZdM2XcA-Ro-m3nK01VMcJDoo-IH7GIhH5bRCQYzROXutiaTlhRvBFMXxYnNKkfrFGGoA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..zBXXgxhZXf_tIP-oYCQ0KwZanMZnfThTLUDKoIoU65yW2aTub0229U1UynIgT_jSc5zJhlmQjIXacKgGmBqDhFTy0c4nmua3bi0GERhgJ3F0sqLZ8B3eCL5MA6084522iO9mO14EdQWdIsgkj0waBzhTKS0FDCyEQK3GOoOLEdOsIESGn7VyT7IiawL5f1H4nE58JKA-WL-blo6qxWrWXNOPBmNas3FZVLWP7KcJtX6gVHCowN5hWQHC--gFbc5KmvjYzrlMJ8M0HC57QWOK3ploB-7D6XSOcwDIfUO4qVL7ZauuTcdHco4u1E0rrrR9d717VrxHoWWScYwqmdYLfg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -0,0 +1,60 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 89 and above.\"\n", "materials": ["### Compute capability check contradicts error message\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe check `compute_capability < 100` returns a failure with message \"requires compute capability 89 and above.\" This is inconsistent — compute capability 89 (Ada Lovelace) would fail this check since 89 < 100. The `TorchScaledMMLinearKernel` in `pytorch.py` correctly uses `< 89` for the same message. Either the check or the message is wrong.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py#L26-L28\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..FRmf1PE6mrfQFP9UfPx-EkauRQIxDm9OXtTQxkaPWf_RqOoxbYd7aTPih_tzUjfCHM9H_6PO4180V8jvRCrhwhLzpZlY24nZm_FrS8sd1Nz9nr-vd-LadGbblIRgx8UpLXjfPOqVDfhUb9UUlW2Zx0yZZ0Dod5_TnIg2UQ0JwXPysHPry6dyOFXSvomZHBum2y0D24v9Lg12FxWGCjBGJIZs-tEAhxz-9qOf4D2KPOG7iFud_6qv4U6Y9rmDi9g3VBZ5r_JV_0tAa_-KN6ZdM2XcA-Ro-m3nK01VMcJDoo-IH7GIhH5bRCQYzROXutiaTlhRvBFMXxYnNKkfrFGGoA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..zBXXgxhZXf_tIP-oYCQ0KwZanMZnfThTLUDKoIoU65yW2aTub0229U1UynIgT_jSc5zJhlmQjIXacKgGmBqDhFTy0c4nmua3bi0GERhgJ3F0sqLZ8B3eCL5MA6084522iO9mO14EdQWdIsgkj0waBzhTKS0FDCyEQK3GOoOLEdOsIESGn7VyT7IiawL5f1H4nE58JKA-WL-blo6qxWrWXNOPBmNas3FZVLWP7KcJtX6gVHCowN5hWQHC--gFbc5KmvjYzrlMJ8M0HC57QWOK3ploB-7D6XSOcwDIfUO4qVL7ZauuTcdHco4u1E0rrrR9d717VrxHoWWScYwqmdYLfg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,57 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )", "patch_sha256": "298db567412a54f3"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2674990832", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py\nReview Comment:\n### Undefined variable causes NameError in BLOCK strategy\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nIn `process_weights_after_loading`, the BLOCK strategy branch no longer assigns `input_scale = None` (this line was removed in the diff). The TENSOR and CHANNEL branches set `input_scale` via their return values, but the BLOCK branch doesn't set it. When line 180 executes `if input_scale is not None:`, it will raise a `NameError` because `input_scale` is undefined for BLOCK quantization.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py#L164-L169\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Qq7tEqbDnz0-cM1kGcol8lRxG9VN_e2MGgVU8KwCeqcmgc_ai5zId17sfCRwJ-vJt2dy1xi7nJi96V5JfdrqTN_Vkfvoj7n54t6m-Xsr-unJaFBFTB-bzTJUOj4uk0Cjb8cGzxQr1AN-d0SS-KGVoAcFdJ10YVRcnnFyr8scB4LENtlOWfYYr9c3FN09IOOBxvgkCmiJEaMGTBN0XtGircqEVxZ8yeJwXxnfebPlSybLy_vvu-5nWXBhIL1zDinAkI_pF-5IEsif1GDRL15ufT2wtKRoB2rXVq6oeo0ku85yNgY_X_orXGa5LFiNT2wYLGfUBczdDAqBwooYCxIMBA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Wb8Ejcl48u7RJe1u09Fv5yHPXfoMsoIpn4D1Kijlu0faj7AfCjPucRoj1jikVxWPydYFMWvKGZsGAeGAD8ThPGeclb03ywN3ykchgA_KoL_NPfJovOIMVO1xkEtXruxk7a4-ttEuqn3rwM5jdIjrCA41Xkg0JznDEVftn6HWAVAV_8liuMGYTZ18iTNQbQ80ucLbBcKKa-4mbsLCoIpTe0rxX28hRlJH6jkVV-i5oXQCS520mOcRy7gxZE9mRLJL4ZeOccvvfVLjdmyx9QIPEiDS9cDnAXqc6JWLhzHkK_afVHEcIO7x9HvBDwpEu_MkdqVsRMS-nil21hA1spbjQA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -155,7 +167,6 @@ def process_weights_after_loading(self, layer) -> None:\n             weight, weight_scale = process_fp8_weight_block_strategy(\n                 layer.weight, layer.weight_scale\n             )\n-            input_scale = None\n", "materials": ["### Undefined variable causes NameError in BLOCK strategy\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nIn `process_weights_after_loading`, the BLOCK strategy branch no longer assigns `input_scale = None` (this line was removed in the diff). The TENSOR and CHANNEL branches set `input_scale` via their return values, but the BLOCK branch doesn't set it. When line 180 executes `if input_scale is not None:`, it will raise a `NameError` because `input_scale` is undefined for BLOCK quantization.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py#L164-L169\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Qq7tEqbDnz0-cM1kGcol8lRxG9VN_e2MGgVU8KwCeqcmgc_ai5zId17sfCRwJ-vJt2dy1xi7nJi96V5JfdrqTN_Vkfvoj7n54t6m-Xsr-unJaFBFTB-bzTJUOj4uk0Cjb8cGzxQr1AN-d0SS-KGVoAcFdJ10YVRcnnFyr8scB4LENtlOWfYYr9c3FN09IOOBxvgkCmiJEaMGTBN0XtGircqEVxZ8yeJwXxnfebPlSybLy_vvu-5nWXBhIL1zDinAkI_pF-5IEsif1GDRL15ufT2wtKRoB2rXVq6oeo0ku85yNgY_X_orXGa5LFiNT2wYLGfUBczdDAqBwooYCxIMBA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Wb8Ejcl48u7RJe1u09Fv5yHPXfoMsoIpn4D1Kijlu0faj7AfCjPucRoj1jikVxWPydYFMWvKGZsGAeGAD8ThPGeclb03ywN3ykchgA_KoL_NPfJovOIMVO1xkEtXruxk7a4-ttEuqn3rwM5jdIjrCA41Xkg0JznDEVftn6HWAVAV_8liuMGYTZ18iTNQbQ80ucLbBcKKa-4mbsLCoIpTe0rxX28hRlJH6jkVV-i5oXQCS520mOcRy7gxZE9mRLJL4ZeOccvvfVLjdmyx9QIPEiDS9cDnAXqc6JWLhzHkK_afVHEcIO7x9HvBDwpEu_MkdqVsRMS-nil21hA1spbjQA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -8,9 +8,13 @@\n from torch.nn import Parameter\n \n from vllm._aiter_ops import rocm_aiter_ops\n+from vllm.logger import init_logger\n from vllm.model_executor.layers.quantization.compressed_tensors.schemes import (\n     CompressedTensorsScheme,\n )\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n from vllm.model_executor.layers.quantization.utils.fp8_utils import (\n     W8A8BlockFp8LinearOp,\n     create_fp8_input_scale,\n@@ -22,11 +26,14 @@\n     process_fp8_weight_tensor_strategy,\n     validate_fp8_block_shape,\n )\n-from vllm.model_executor.layers.quantization.utils.quant_utils import GroupShape\n+from vllm.model_executor.layers.quantization.utils.quant_utils import (\n+    GroupShape,\n+    kFp8DynamicTokenSym,\n+    kFp8StaticTensorSym,\n+    kFp8StaticTokenSym,\n+)\n from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n-    Fp8LinearOp,\n     cutlass_block_fp8_supported,\n-    maybe_create_device_identity,\n )\n from vllm.model_executor.parameter import (\n     BlockQuantScaleParameter,\n@@ -42,39 +49,46 @@\n     QuantizationStrategy.TENSOR: PerTensorScaleParameter,\n }\n \n+STATIC_QUANT = True\n+DYNAMIC_QUANT = False\n+activation_quant_key_mapping = {\n+    STATIC_QUANT: kFp8StaticTensorSym,\n+    DYNAMIC_QUANT: kFp8DynamicTokenSym,\n+}\n+weight_quant_key_mapping = {\n+    QuantizationStrategy.CHANNEL: kFp8StaticTokenSym,\n+    QuantizationStrategy.TENSOR: kFp8StaticTensorSym,\n+}\n+logger = init_logger(__name__)\n+\n \n class CompressedTensorsW8A8Fp8(CompressedTensorsScheme):\n     def __init__(self, weight_quant: QuantizationArgs, is_static_input_scheme: bool):\n         self.weight_quant = weight_quant\n         self.strategy = weight_quant.strategy\n         self.out_dtype = torch.get_default_dtype()\n         self.is_static_input_scheme = is_static_input_scheme\n-\n         self.weight_block_size = self.weight_quant.block_structure\n-        if self.weight_block_size is not None:\n-            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n-        else:\n-            self.act_q_group_shape = (\n-                GroupShape.PER_TENSOR\n-                if is_static_input_scheme\n-                else GroupShape.PER_TOKEN\n-            )\n-\n-        self.cutlass_block_fp8_supported = cutlass_block_fp8_supported()\n-        self.use_aiter_and_is_supported = rocm_aiter_ops.is_linear_fp8_enabled()\n \n         if self.weight_block_size is not None:\n+            self.cutlass_block_fp8_supported = cutlass_block_fp8_supported()\n+            self.use_aiter_and_is_supported = rocm_aiter_ops.is_linear_fp8_enabled()\n             assert not self.is_static_input_scheme\n+            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n             self.w8a8_block_fp8_linear = W8A8BlockFp8LinearOp(\n                 weight_group_shape=GroupShape(*self.weight_block_size),\n                 act_quant_group_shape=self.act_q_group_shape,\n                 cutlass_block_fp8_supported=self.cutlass_block_fp8_supported,\n                 use_aiter_and_is_supported=self.use_aiter_and_is_supported,\n             )\n         else:\n-            self.fp8_linear = Fp8LinearOp(\n-                act_quant_static=self.is_static_input_scheme,\n-                act_quant_group_shape=self.act_q_group_shape,\n+            activation_quant_key = activation_quant_key_mapping[is_static_input_scheme]\n+            weight_quant_key = weight_quant_key_mapping[self.strategy]\n+            self.fp8_linear = init_fp8_linear_kernel(\n+                activation_quant_key=activation_quant_key,\n+                weight_quant_key=weight_quant_key,\n+                out_dtype=self.out_dtype,\n+                module_name=self.__class__.__name__,\n             )\n \n     @classmethod\n@@ -93,8 +107,6 @@ def create_weights(\n         weight_loader: Callable,\n         **kwargs,\n     ):\n-        maybe_create_device_identity()\n-\n         output_size_per_partition = sum(output_partition_sizes)\n         layer.logical_widths = output_partition_sizes\n         layer.weight_block_size = None\n@@ -143,7 +155,6 @@ def process_weights_after_loading(self, layer) -> None:\n                 getattr(layer, \"input_scale\", None),\n             )\n             weight = weight.t()\n-\n         elif self.strategy == QuantizationStrategy.CHANNEL:\n             weight, weight_scale, input_scale = process_fp8_weight_channel_strategy(\n                 layer.weight, layer.weight_scale, getattr(layer, \"input_scale\", None)\n@@ -174,7 +185,6 @@ def process_weights_after_loading(self, layer) -> None:\n             layer.input_scale = Parameter(layer.input_scale.max(), requires_grad=False)\n         else:\n             layer.input_scale = None\n-\n         if self.strategy == QuantizationStrategy.BLOCK:\n             maybe_post_process_fp8_weight_block(layer)\n \n@@ -193,11 +203,4 @@ def apply_weights(\n                 bias=bias,\n             )\n \n-        return self.fp8_linear.apply(\n-            input=x,\n-            weight=layer.weight,\n-            weight_scale=layer.weight_scale,\n-            out_dtype=self.out_dtype,\n-            input_scale=layer.input_scale,\n-            bias=bias,\n-        )\n+        return self.fp8_linear.apply_weights(layer, x, bias)", "patch_sha256": "9ffc94d3ca8687e4"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2675656270", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py\nReview Comment:\n### FlashInfer kernel missing output tensor reshape\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe `FlashInferScaledMMLinearKernel.apply_scaled_mm` method accepts `output_shape` as a parameter but does not use it to reshape the output. The parent class `FP8ScaledMMLinearKernel.apply_weights` flattens the input to 2D (`x.view(-1, x.shape[-1])`) before calling `apply_scaled_mm`, and passes `output_shape` to restore the original batch dimensions. All other FP8 kernel implementations (Cutlass, ROCm, all PyTorch variants) correctly call `.view(*output_shape)` on their output, but FlashInfer returns the raw 2D GEMM result. This causes incorrect output tensor shapes when inputs have more than 2 dimensions (e.g., `[batch, seq_len, hidden]` would remain as `[batch*seq_len, out_features]` instead of being reshaped to `[batch, seq_len, out_features]`).\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py#L43-L57\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..FDNA-smuDMrFZDLsM5A2An87p33ZRjLrSllREDM8fMkmdouZnd1UGkdWHtnGNNv9kOwxr8YBEqny7X04nl7cyAZz73h4K4jhn7p1Tj1hp_3VoKc6NzdqmudUHLQ8onf3AZ5_T6_ZCVpILXEwpKDAHmJ1KqyK7cfUHcDNcSk1nzXD8lyYlq6m7M423UF9nGiC14tEaSx7Lo2_5Q_7GuLGyKVB9C-158gmjm46-Z0vn2QCxIJXBVBF3oyl0fQxsqu7eiyUYTUpJnrU30Jtjdt9ZZ83p5C2B_jPS2idaUDJlJXvewKNn-00EOD1pIxfnVkvSFRnFStBpmRWqrb7V1BSBg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..RwMwTqOFAXGiGaeC3-QKYqUOS0cxBnKS9h7Z54nSuTsRz38tn83V0u2DmYfgxNQTrIeYGbCW-1W_373RMkaoAR6ZpPloLxCdralwGZHxB3FZGQ2SsWYaRo8eVE_baOgf9BUq2sQ5kJMLxhhM_K2Jg7PfZ12wf5cDLOYrLQKvRrmBdOB-zhHhNJPfAF4V0sdkgN0gUWi2gw1iR5q6xyY79DP9OPdN2XVOXJioATAadahd1qgHLQ8X3_BoBtHWi3QbEgHIgH_tAODhm5sXdCFUNny9NRsR6KYhXW58mMSYE3xKgvCa1sFB-vgY9jYUO_ZPG1jF7jxFDvPxvW62rOdDTQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -0,0 +1,60 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )\n", "materials": ["### FlashInfer kernel missing output tensor reshape\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe `FlashInferScaledMMLinearKernel.apply_scaled_mm` method accepts `output_shape` as a parameter but does not use it to reshape the output. The parent class `FP8ScaledMMLinearKernel.apply_weights` flattens the input to 2D (`x.view(-1, x.shape[-1])`) before calling `apply_scaled_mm`, and passes `output_shape` to restore the original batch dimensions. All other FP8 kernel implementations (Cutlass, ROCm, all PyTorch variants) correctly call `.view(*output_shape)` on their output, but FlashInfer returns the raw 2D GEMM result. This causes incorrect output tensor shapes when inputs have more than 2 dimensions (e.g., `[batch, seq_len, hidden]` would remain as `[batch*seq_len, out_features]` instead of being reshaped to `[batch, seq_len, out_features]`).\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py#L43-L57\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..FDNA-smuDMrFZDLsM5A2An87p33ZRjLrSllREDM8fMkmdouZnd1UGkdWHtnGNNv9kOwxr8YBEqny7X04nl7cyAZz73h4K4jhn7p1Tj1hp_3VoKc6NzdqmudUHLQ8onf3AZ5_T6_ZCVpILXEwpKDAHmJ1KqyK7cfUHcDNcSk1nzXD8lyYlq6m7M423UF9nGiC14tEaSx7Lo2_5Q_7GuLGyKVB9C-158gmjm46-Z0vn2QCxIJXBVBF3oyl0fQxsqu7eiyUYTUpJnrU30Jtjdt9ZZ83p5C2B_jPS2idaUDJlJXvewKNn-00EOD1pIxfnVkvSFRnFStBpmRWqrb7V1BSBg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..RwMwTqOFAXGiGaeC3-QKYqUOS0cxBnKS9h7Z54nSuTsRz38tn83V0u2DmYfgxNQTrIeYGbCW-1W_373RMkaoAR6ZpPloLxCdralwGZHxB3FZGQ2SsWYaRo8eVE_baOgf9BUq2sQ5kJMLxhhM_K2Jg7PfZ12wf5cDLOYrLQKvRrmBdOB-zhHhNJPfAF4V0sdkgN0gUWi2gw1iR5q6xyY79DP9OPdN2XVOXJioATAadahd1qgHLQ8X3_BoBtHWi3QbEgHIgH_tAODhm5sXdCFUNny9NRsR6KYhXW58mMSYE3xKgvCa1sFB-vgY9jYUO_ZPG1jF7jxFDvPxvW62rOdDTQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,57 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )", "patch_sha256": "298db567412a54f3"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2675656272", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py\nReview Comment:\n### Missing `input_scale` attribute causes AttributeError for BLOCK strategy\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe refactor removed the `else: layer.input_scale = None` branch from `process_weights_after_loading`. For BLOCK quantization strategy, `is_static_input_scheme` is always `False` (asserted on line 76), so `input_scale` is never registered in `create_weights` and never set to `None` in `process_weights_after_loading`. However, `apply_weights` directly accesses `layer.input_scale` for the BLOCK path (line 201), which will raise an `AttributeError` since the attribute doesn't exist on the layer.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py#L182-L185\nvllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py#L195-L203\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py#L195-L203`](https://github.com/vllm-project/vllm/blob/c52cfae1bb582849cfb8fefaaa351fa91ce46f16/vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py#L195-L203)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..ayOo22KKN5p4JrTGO82cffN9vU5eh0-oHeYjVJPT1YIn-Wq9viGNpVAHmYs4q-3bG1qYSAVK50LEzsLtaO_iVA0hpRZfyR9oHCd4hbn_g5a6PsvOdT8zuJ7OniT7g4565YiCkesImKj6nVjjbEmUOOmIp54U1KMSW-f6C0FN_D5Ix3TqkTOutvpOe5cFdk6XhXzgClDxTeQO64u_g1dXu4M9F4drG3CeOh2_kWEsEzOBM1qbVqmSkIcGgJWKzKGk_a4FmvWs0lts82yzLSsauKgzzXhQ6D9zDRpp4LeA-uqePgGCpdj2HFNdju5RFdqe1QmNLhgSEM3d1q-sKhmKxg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..PquGfbzxWwmMgnAMTxYcFZ932XOFNy8h1mTt1cKELOqaJEyVxWJFRB1VYfsDSTrwtDNGyh3LmML3MV0aor3xH7DKHlHHTZxJrXEtnFjTtOVRhvgkMdTJv-l8Rji48KsJupgcRl9Q7t-8vy-C1t-fNJOIexmjKP9IrbAGzXSeNt1MwF3tT8qpoVYEbNLDIlNMJVL1Rz4cfm2CPxdvurbL3MBChe2vz2fjlV5f9Q_CugWD5Wanj3WV-39qATD7ADFPGJChgpEAdj3S6LpEqYrvkQHLuVrEK0RMmJoN-MbZIta9HbK226Y2d_ikNJxhsWoahObFPL3_e6T1Ne1Lsc9RCQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -172,8 +183,6 @@ def process_weights_after_loading(self, layer) -> None:\n         # INPUT SCALE\n         if self.is_static_input_scheme and hasattr(layer, \"input_scale\"):\n             layer.input_scale = Parameter(layer.input_scale.max(), requires_grad=False)\n-        else:\n-            layer.input_scale = None\n", "materials": ["### Missing `input_scale` attribute causes AttributeError for BLOCK strategy\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe refactor removed the `else: layer.input_scale = None` branch from `process_weights_after_loading`. For BLOCK quantization strategy, `is_static_input_scheme` is always `False` (asserted on line 76), so `input_scale` is never registered in `create_weights` and never set to `None` in `process_weights_after_loading`. However, `apply_weights` directly accesses `layer.input_scale` for the BLOCK path (line 201), which will raise an `AttributeError` since the attribute doesn't exist on the layer.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py#L182-L185\nvllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py#L195-L203\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py#L195-L203`](https://github.com/vllm-project/vllm/blob/c52cfae1bb582849cfb8fefaaa351fa91ce46f16/vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py#L195-L203)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..ayOo22KKN5p4JrTGO82cffN9vU5eh0-oHeYjVJPT1YIn-Wq9viGNpVAHmYs4q-3bG1qYSAVK50LEzsLtaO_iVA0hpRZfyR9oHCd4hbn_g5a6PsvOdT8zuJ7OniT7g4565YiCkesImKj6nVjjbEmUOOmIp54U1KMSW-f6C0FN_D5Ix3TqkTOutvpOe5cFdk6XhXzgClDxTeQO64u_g1dXu4M9F4drG3CeOh2_kWEsEzOBM1qbVqmSkIcGgJWKzKGk_a4FmvWs0lts82yzLSsauKgzzXhQ6D9zDRpp4LeA-uqePgGCpdj2HFNdju5RFdqe1QmNLhgSEM3d1q-sKhmKxg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..PquGfbzxWwmMgnAMTxYcFZ932XOFNy8h1mTt1cKELOqaJEyVxWJFRB1VYfsDSTrwtDNGyh3LmML3MV0aor3xH7DKHlHHTZxJrXEtnFjTtOVRhvgkMdTJv-l8Rji48KsJupgcRl9Q7t-8vy-C1t-fNJOIexmjKP9IrbAGzXSeNt1MwF3tT8qpoVYEbNLDIlNMJVL1Rz4cfm2CPxdvurbL3MBChe2vz2fjlV5f9Q_CugWD5Wanj3WV-39qATD7ADFPGJChgpEAdj3S6LpEqYrvkQHLuVrEK0RMmJoN-MbZIta9HbK226Y2d_ikNJxhsWoahObFPL3_e6T1Ne1Lsc9RCQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -8,9 +8,13 @@\n from torch.nn import Parameter\n \n from vllm._aiter_ops import rocm_aiter_ops\n+from vllm.logger import init_logger\n from vllm.model_executor.layers.quantization.compressed_tensors.schemes import (\n     CompressedTensorsScheme,\n )\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n from vllm.model_executor.layers.quantization.utils.fp8_utils import (\n     W8A8BlockFp8LinearOp,\n     create_fp8_input_scale,\n@@ -22,11 +26,14 @@\n     process_fp8_weight_tensor_strategy,\n     validate_fp8_block_shape,\n )\n-from vllm.model_executor.layers.quantization.utils.quant_utils import GroupShape\n+from vllm.model_executor.layers.quantization.utils.quant_utils import (\n+    GroupShape,\n+    kFp8DynamicTokenSym,\n+    kFp8StaticTensorSym,\n+    kFp8StaticTokenSym,\n+)\n from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n-    Fp8LinearOp,\n     cutlass_block_fp8_supported,\n-    maybe_create_device_identity,\n )\n from vllm.model_executor.parameter import (\n     BlockQuantScaleParameter,\n@@ -42,39 +49,46 @@\n     QuantizationStrategy.TENSOR: PerTensorScaleParameter,\n }\n \n+STATIC_QUANT = True\n+DYNAMIC_QUANT = False\n+activation_quant_key_mapping = {\n+    STATIC_QUANT: kFp8StaticTensorSym,\n+    DYNAMIC_QUANT: kFp8DynamicTokenSym,\n+}\n+weight_quant_key_mapping = {\n+    QuantizationStrategy.CHANNEL: kFp8StaticTokenSym,\n+    QuantizationStrategy.TENSOR: kFp8StaticTensorSym,\n+}\n+logger = init_logger(__name__)\n+\n \n class CompressedTensorsW8A8Fp8(CompressedTensorsScheme):\n     def __init__(self, weight_quant: QuantizationArgs, is_static_input_scheme: bool):\n         self.weight_quant = weight_quant\n         self.strategy = weight_quant.strategy\n         self.out_dtype = torch.get_default_dtype()\n         self.is_static_input_scheme = is_static_input_scheme\n-\n         self.weight_block_size = self.weight_quant.block_structure\n-        if self.weight_block_size is not None:\n-            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n-        else:\n-            self.act_q_group_shape = (\n-                GroupShape.PER_TENSOR\n-                if is_static_input_scheme\n-                else GroupShape.PER_TOKEN\n-            )\n-\n-        self.cutlass_block_fp8_supported = cutlass_block_fp8_supported()\n-        self.use_aiter_and_is_supported = rocm_aiter_ops.is_linear_fp8_enabled()\n \n         if self.weight_block_size is not None:\n+            self.cutlass_block_fp8_supported = cutlass_block_fp8_supported()\n+            self.use_aiter_and_is_supported = rocm_aiter_ops.is_linear_fp8_enabled()\n             assert not self.is_static_input_scheme\n+            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n             self.w8a8_block_fp8_linear = W8A8BlockFp8LinearOp(\n                 weight_group_shape=GroupShape(*self.weight_block_size),\n                 act_quant_group_shape=self.act_q_group_shape,\n                 cutlass_block_fp8_supported=self.cutlass_block_fp8_supported,\n                 use_aiter_and_is_supported=self.use_aiter_and_is_supported,\n             )\n         else:\n-            self.fp8_linear = Fp8LinearOp(\n-                act_quant_static=self.is_static_input_scheme,\n-                act_quant_group_shape=self.act_q_group_shape,\n+            activation_quant_key = activation_quant_key_mapping[is_static_input_scheme]\n+            weight_quant_key = weight_quant_key_mapping[self.strategy]\n+            self.fp8_linear = init_fp8_linear_kernel(\n+                activation_quant_key=activation_quant_key,\n+                weight_quant_key=weight_quant_key,\n+                out_dtype=self.out_dtype,\n+                module_name=self.__class__.__name__,\n             )\n \n     @classmethod\n@@ -93,8 +107,6 @@ def create_weights(\n         weight_loader: Callable,\n         **kwargs,\n     ):\n-        maybe_create_device_identity()\n-\n         output_size_per_partition = sum(output_partition_sizes)\n         layer.logical_widths = output_partition_sizes\n         layer.weight_block_size = None\n@@ -143,7 +155,6 @@ def process_weights_after_loading(self, layer) -> None:\n                 getattr(layer, \"input_scale\", None),\n             )\n             weight = weight.t()\n-\n         elif self.strategy == QuantizationStrategy.CHANNEL:\n             weight, weight_scale, input_scale = process_fp8_weight_channel_strategy(\n                 layer.weight, layer.weight_scale, getattr(layer, \"input_scale\", None)\n@@ -174,7 +185,6 @@ def process_weights_after_loading(self, layer) -> None:\n             layer.input_scale = Parameter(layer.input_scale.max(), requires_grad=False)\n         else:\n             layer.input_scale = None\n-\n         if self.strategy == QuantizationStrategy.BLOCK:\n             maybe_post_process_fp8_weight_block(layer)\n \n@@ -193,11 +203,4 @@ def apply_weights(\n                 bias=bias,\n             )\n \n-        return self.fp8_linear.apply(\n-            input=x,\n-            weight=layer.weight,\n-            weight_scale=layer.weight_scale,\n-            out_dtype=self.out_dtype,\n-            input_scale=layer.input_scale,\n-            bias=bias,\n-        )\n+        return self.fp8_linear.apply_weights(layer, x, bias)", "patch_sha256": "9ffc94d3ca8687e4"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2681711539", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py\nReview Comment:\nI feel that this refactor shouldn't attempt to change the underlying execution logic of existing kernels; if this indeed causes an issue for the flash infer kernel, another bug fix PR should be opened to resolve it.\n\nOriginal Code Context:\n@@ -0,0 +1,60 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )\n", "materials": ["I feel that this refactor shouldn't attempt to change the underlying execution logic of existing kernels; if this indeed causes an issue for the flash infer kernel, another bug fix PR should be opened to resolve it."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,57 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )", "patch_sha256": "298db567412a54f3"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2681933496", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/triton.py\nReview Comment:\n### Variable naming error causes runtime TypeError in Triton kernel\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nIn `TritonScaledMMLinearKernel.process_weights_after_loading`, the variable `w_s_name` is assigned from `_get_layer_params(layer)` which returns **tensors**, not string names. However, the code then uses `w_s_name` as if it were a string in `getattr(layer, w_s_name)` on line 53 and `replace_parameter(layer, w_s_name, ...)` on line 58. This will cause a `TypeError` at runtime because `getattr` and `replace_parameter` expect string attribute names, not tensors. The weight_scale name from `layer_param_names` is incorrectly discarded with `_` on line 40. Compare with `cutlass.py` and `cpu.py` which correctly unpack names from `layer_param_names` and tensors from `_get_layer_params` into separate variables.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/kernels/scaled_mm/triton.py#L38-L58\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..ZnNj5QRsaZyC1Th8nJdxYQ-eEG7m5Z_JTnLTRc7oVjbXrjefV3TdBOupvXOHcDLCc1KtDmWQGZBcdn5HvLcw6N-nJIywM8nFFtKbcQquqHnTB8prwJ6LRoPZnzNHSo-F5SqwQFkVrsHrguJoKKXdku4tF_1gasZtMzhEfDm8B26tfF26w9c8Ez38cxLNLuCy4WgJ3ztU49OC486I8w2VMz318c05B4iwDwSZfM_mDbohvvHGBU4t-86QIOXD1TwscSpQze7BKl_lQemzHSIvfisoFw8cuir_fhWGUMhpmJmlQmvWMFVo2IiaMbWMKDSqfDcwR-Y3LOoTYuzsoPs42A\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..0Eiabybgythz_uZ3nKG6d2DAaAvlPR1lLKLvZDWs9D57IGQxDYMyBZkSd3N2nVOLHI323ZgMIl-os9alypSQowRCaUdLYPPLAzvhQTf94-kt6XMDJiJQRAaahjWKsn9V_RbillAsYuK40jh_n1yaMNPqgBhbsMuB-J1fer265G4StbAq7kHp4hLCyKzoLBpeNDXcQUIUkgZR7cJCwbTllqpUXsnMeprg5VkBdzUgbg-PZvrmdAj-RbledMqu0kzyMt24Tb64HfxE39QJ8NrX4vwwCjCex6AqrCRxvpiyNUHD5bJ_tCAJsIOZi1mCmycY6pUSulDy1yCAkFb2bIqYqg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -14,68 +14,73 @@\n )\n from vllm.platforms import current_platform\n \n-from .ScaledMMLinearKernel import ScaledMMLinearKernel, ScaledMMLinearLayerConfig\n+from .cutlass import CutlassScaledMMLinearKernel\n+from .ScaledMMLinearKernel import (\n+    Int8ScaledMMLinearLayerConfig,\n+)\n \n \n-class TritonScaledMMLinearKernel(ScaledMMLinearKernel):\n+class TritonScaledMMLinearKernel(CutlassScaledMMLinearKernel):\n     @classmethod\n     def is_supported(\n         cls, compute_capability: int | None = None\n     ) -> tuple[bool, str | None]:\n         if current_platform.is_cuda_alike():\n             return True, None\n-        return False, \"Requires ROCm or CUDA.\"\n+        return False, \"requires ROCm or CUDA.\"\n \n     @classmethod\n-    def can_implement(cls, c: ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+    def can_implement(cls, c: Int8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n         if not c.input_symmetric:\n-            return False, \"Only symmetric input is supported.\"\n+            return False, \"supports symmetric input only.\"\n         return True, None\n \n     def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n-        weight = getattr(layer, self.w_q_name)\n+        w_q, w_s_name, i_s, _, _ = self._get_layer_params(layer)\n+        w_q_name, _, i_s_name, i_zp_name, azp_adj_name = self.layer_param_names\n+\n         replace_parameter(\n             layer,\n-            self.w_q_name,\n-            torch.nn.Parameter(weight.t().data, requires_grad=False),\n+            w_q_name,\n+            torch.nn.Parameter(w_q.t().data, requires_grad=False),\n         )\n \n         # WEIGHT SCALE\n         # Triton kernel supports only per-tensor and per-channel.\n         # If we have a fused module (QKV, MLP) with per tensor scales (thus N\n         # scales being passed to the kernel), convert to the per-channel case.\n         is_fused_module = len(layer.logical_widths) > 1\n-        weight_scale = getattr(layer, self.w_s_name)\n+        weight_scale = getattr(layer, w_s_name)\n         if is_fused_module and not self.config.is_channelwise:\n             weight_scale = convert_to_channelwise(weight_scale, layer.logical_widths)\n         replace_parameter(\n             layer,\n-            self.w_s_name,\n+            w_s_name,\n", "materials": ["### Variable naming error causes runtime TypeError in Triton kernel\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nIn `TritonScaledMMLinearKernel.process_weights_after_loading`, the variable `w_s_name` is assigned from `_get_layer_params(layer)` which returns **tensors**, not string names. However, the code then uses `w_s_name` as if it were a string in `getattr(layer, w_s_name)` on line 53 and `replace_parameter(layer, w_s_name, ...)` on line 58. This will cause a `TypeError` at runtime because `getattr` and `replace_parameter` expect string attribute names, not tensors. The weight_scale name from `layer_param_names` is incorrectly discarded with `_` on line 40. Compare with `cutlass.py` and `cpu.py` which correctly unpack names from `layer_param_names` and tensors from `_get_layer_params` into separate variables.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/kernels/scaled_mm/triton.py#L38-L58\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..ZnNj5QRsaZyC1Th8nJdxYQ-eEG7m5Z_JTnLTRc7oVjbXrjefV3TdBOupvXOHcDLCc1KtDmWQGZBcdn5HvLcw6N-nJIywM8nFFtKbcQquqHnTB8prwJ6LRoPZnzNHSo-F5SqwQFkVrsHrguJoKKXdku4tF_1gasZtMzhEfDm8B26tfF26w9c8Ez38cxLNLuCy4WgJ3ztU49OC486I8w2VMz318c05B4iwDwSZfM_mDbohvvHGBU4t-86QIOXD1TwscSpQze7BKl_lQemzHSIvfisoFw8cuir_fhWGUMhpmJmlQmvWMFVo2IiaMbWMKDSqfDcwR-Y3LOoTYuzsoPs42A\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..0Eiabybgythz_uZ3nKG6d2DAaAvlPR1lLKLvZDWs9D57IGQxDYMyBZkSd3N2nVOLHI323ZgMIl-os9alypSQowRCaUdLYPPLAzvhQTf94-kt6XMDJiJQRAaahjWKsn9V_RbillAsYuK40jh_n1yaMNPqgBhbsMuB-J1fer265G4StbAq7kHp4hLCyKzoLBpeNDXcQUIUkgZR7cJCwbTllqpUXsnMeprg5VkBdzUgbg-PZvrmdAj-RbledMqu0kzyMt24Tb64HfxE39QJ8NrX4vwwCjCex6AqrCRxvpiyNUHD5bJ_tCAJsIOZi1mCmycY6pUSulDy1yCAkFb2bIqYqg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -14,68 +14,73 @@\n )\n from vllm.platforms import current_platform\n \n-from .ScaledMMLinearKernel import ScaledMMLinearKernel, ScaledMMLinearLayerConfig\n+from .cutlass import CutlassInt8ScaledMMLinearKernel\n+from .ScaledMMLinearKernel import (\n+    Int8ScaledMMLinearLayerConfig,\n+)\n \n \n-class TritonScaledMMLinearKernel(ScaledMMLinearKernel):\n+class TritonInt8ScaledMMLinearKernel(CutlassInt8ScaledMMLinearKernel):\n     @classmethod\n     def is_supported(\n         cls, compute_capability: int | None = None\n     ) -> tuple[bool, str | None]:\n         if current_platform.is_cuda_alike():\n             return True, None\n-        return False, \"Requires ROCm or CUDA.\"\n+        return False, \"requires ROCm or CUDA.\"\n \n     @classmethod\n-    def can_implement(cls, c: ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+    def can_implement(cls, c: Int8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n         if not c.input_symmetric:\n-            return False, \"Only symmetric input is supported.\"\n+            return False, \"supports symmetric input only.\"\n         return True, None\n \n     def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n-        weight = getattr(layer, self.w_q_name)\n+        w_q, _, i_s, _, _ = self._get_layer_params(layer)\n+        w_q_name, w_s_name, i_s_name, i_zp_name, azp_adj_name = self.layer_param_names\n+\n         replace_parameter(\n             layer,\n-            self.w_q_name,\n-            torch.nn.Parameter(weight.t().data, requires_grad=False),\n+            w_q_name,\n+            torch.nn.Parameter(w_q.t().data, requires_grad=False),\n         )\n \n         # WEIGHT SCALE\n         # Triton kernel supports only per-tensor and per-channel.\n         # If we have a fused module (QKV, MLP) with per tensor scales (thus N\n         # scales being passed to the kernel), convert to the per-channel case.\n         is_fused_module = len(layer.logical_widths) > 1\n-        weight_scale = getattr(layer, self.w_s_name)\n+        weight_scale = getattr(layer, w_s_name)\n         if is_fused_module and not self.config.is_channelwise:\n             weight_scale = convert_to_channelwise(weight_scale, layer.logical_widths)\n         replace_parameter(\n             layer,\n-            self.w_s_name,\n+            w_s_name,\n             torch.nn.Parameter(weight_scale.data, requires_grad=False),\n         )\n \n         # INPUT SCALE\n         if self.config.is_static_input_scheme:\n-            input_scale = getattr(layer, self.i_s_name)\n+            assert i_s is not None\n             replace_parameter(\n                 layer,\n-                self.i_s_name,\n-                torch.nn.Parameter(input_scale.max(), requires_grad=False),\n+                i_s_name,\n+                torch.nn.Parameter(i_s.max(), requires_grad=False),\n             )\n-            setattr(layer, self.i_zp_name, None)\n+            setattr(layer, i_zp_name, None)\n         else:\n-            setattr(layer, self.i_s_name, None)\n-            setattr(layer, self.i_zp_name, None)\n+            setattr(layer, i_s_name, None)\n+            setattr(layer, i_zp_name, None)\n \n-        setattr(layer, self.azp_adj_name, None)\n+        setattr(layer, azp_adj_name, None)\n \n     def apply_weights(\n         self,\n         layer: torch.nn.Module,\n         x: torch.Tensor,\n         bias: torch.Tensor | None = None,\n     ) -> torch.Tensor:\n-        w_q, w_s, i_s, i_zp, azp_adj = self._get_weight_params(layer)\n+        w_q, w_s, i_s, i_zp, _ = self._get_layer_params(layer)\n \n         x_q, x_s, x_zp = ops.scaled_int8_quant(\n             x.contiguous(), i_s, i_zp, symmetric=True", "patch_sha256": "69fd7351243be92b"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2681933503", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/aiter.py\nReview Comment:\n### Compute capability check removed from Aiter kernel validation\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `AiterScaledMMLinearKernel.is_supported()` method removed the compute capability validation that previously checked for `compute_capability < 90`. The `get_min_capability()` method was added returning 90, but it's never called anywhere—making it dead code. The `compute_capability` parameter is accepted but ignored in `is_supported()`. This could cause the kernel to be incorrectly selected on older ROCm hardware that doesn't meet the minimum capability requirement. Other kernels like `FlashInferScaledMMLinearKernel` and `TorchScaledMMLinearKernel` properly validate compute capability, but this check was removed from Aiter.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/kernels/scaled_mm/aiter.py#L15-L39\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..oK4tHUlp0EQNKZd0u7sxEI-i1is_0BgvVdeVTgyjOGkmrx5xV5_0rTXNotYBTgk3h5_F_75aexV7MP-J8zyGSBulr8xZ8A1h1ufCjM1FXJsnK4yYmVUwx39ucO5dO1E8LNZk-2VYbu8HoRcTDLJnDEQtnLC53k1pEn7A3QyqYtKbQWGZxjJor2A8B-FTwoOw8nM8MwJ0dxEO5WBap0jZRNJAGkMKYjuRg2_-oPnccNRnw6bGgkARqOO2IaCMz9zDgdt_pbVCyB1wb1GdCANWwITe64RdXbrzdKAFDWDXRvX5IGkgt86oq6HdL9xkw2E58-UuxEm7M61E8evAMgy-cg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..HUO53g2y5_V6sM3WADDrQ565QI7N8KmOBis7ihfce9UOdrKCxhhfU368QLK5WGgtDvXp9LoojwDDWqAemRjv8bSWipHVYpE0O4XNi3WnQZ60TcBShkP2MLrIXQ-Jjap7cTmVyvrEgfdJsZjTekhqbZkdBw7pMX3tYEPBg8wcm4zm91qbgm0aG55SuLyVR_qyK8-MOLYLPpud6oDHfjxNZbw-GRldH_IwtaM6z1Pc44j_z4TPEpdDNwLBajbz5mACE8I4DW7vfP9zkUcHk11akzocb0IFw3J7jT5TSPqskTzD1m0vxtoXrqmJUlXQr2NndWympuyYMlKuopQz0pafUA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -9,59 +9,41 @@\n from vllm.platforms import current_platform\n \n from .cutlass import CutlassScaledMMLinearKernel\n-from .ScaledMMLinearKernel import ScaledMMLinearLayerConfig\n+from .ScaledMMLinearKernel import Int8ScaledMMLinearLayerConfig\n \n \n class AiterScaledMMLinearKernel(CutlassScaledMMLinearKernel):\n+    @classmethod\n+    def get_min_capability(cls) -> int:\n+        return 90\n+\n     @classmethod\n     def is_supported(\n         cls, compute_capability: int | None = None\n     ) -> tuple[bool, str | None]:\n         if not current_platform.is_rocm():\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel requires `aiter` which is not \"\n-                + \"currently supported on non-ROCm platform.\",\n-            )\n-        if compute_capability is None:\n-            _cc = current_platform.get_device_capability()\n-            if _cc is not None:\n-                compute_capability = _cc.major * 10 + _cc.minor\n-        if compute_capability is not None and compute_capability < 90:\n-            return False, f\"requires capability 90, got {compute_capability}\"\n+            return False, \"Requires ROCm.\"\n \n         try:\n             import aiter  # noqa: F401 # deliberately attempt to import aiter\n         except Exception:\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel requires `aiter` which is not \"\n-                + \"installed on ROCm.\",\n", "materials": ["### Compute capability check removed from Aiter kernel validation\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `AiterScaledMMLinearKernel.is_supported()` method removed the compute capability validation that previously checked for `compute_capability < 90`. The `get_min_capability()` method was added returning 90, but it's never called anywhere—making it dead code. The `compute_capability` parameter is accepted but ignored in `is_supported()`. This could cause the kernel to be incorrectly selected on older ROCm hardware that doesn't meet the minimum capability requirement. Other kernels like `FlashInferScaledMMLinearKernel` and `TorchScaledMMLinearKernel` properly validate compute capability, but this check was removed from Aiter.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/kernels/scaled_mm/aiter.py#L15-L39\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..oK4tHUlp0EQNKZd0u7sxEI-i1is_0BgvVdeVTgyjOGkmrx5xV5_0rTXNotYBTgk3h5_F_75aexV7MP-J8zyGSBulr8xZ8A1h1ufCjM1FXJsnK4yYmVUwx39ucO5dO1E8LNZk-2VYbu8HoRcTDLJnDEQtnLC53k1pEn7A3QyqYtKbQWGZxjJor2A8B-FTwoOw8nM8MwJ0dxEO5WBap0jZRNJAGkMKYjuRg2_-oPnccNRnw6bGgkARqOO2IaCMz9zDgdt_pbVCyB1wb1GdCANWwITe64RdXbrzdKAFDWDXRvX5IGkgt86oq6HdL9xkw2E58-UuxEm7M61E8evAMgy-cg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..HUO53g2y5_V6sM3WADDrQ565QI7N8KmOBis7ihfce9UOdrKCxhhfU368QLK5WGgtDvXp9LoojwDDWqAemRjv8bSWipHVYpE0O4XNi3WnQZ60TcBShkP2MLrIXQ-Jjap7cTmVyvrEgfdJsZjTekhqbZkdBw7pMX3tYEPBg8wcm4zm91qbgm0aG55SuLyVR_qyK8-MOLYLPpud6oDHfjxNZbw-GRldH_IwtaM6z1Pc44j_z4TPEpdDNwLBajbz5mACE8I4DW7vfP9zkUcHk11akzocb0IFw3J7jT5TSPqskTzD1m0vxtoXrqmJUlXQr2NndWympuyYMlKuopQz0pafUA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -8,89 +8,70 @@\n from vllm._aiter_ops import rocm_aiter_ops\n from vllm.platforms import current_platform\n \n-from .cutlass import CutlassScaledMMLinearKernel\n-from .ScaledMMLinearKernel import ScaledMMLinearLayerConfig\n+from .cutlass import CutlassInt8ScaledMMLinearKernel\n+from .ScaledMMLinearKernel import Int8ScaledMMLinearLayerConfig\n \n \n-class AiterScaledMMLinearKernel(CutlassScaledMMLinearKernel):\n+class AiterInt8ScaledMMLinearKernel(CutlassInt8ScaledMMLinearKernel):\n     @classmethod\n     def is_supported(\n         cls, compute_capability: int | None = None\n     ) -> tuple[bool, str | None]:\n         if not current_platform.is_rocm():\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel requires `aiter` which is not \"\n-                + \"currently supported on non-ROCm platform.\",\n-            )\n-        if compute_capability is None:\n-            _cc = current_platform.get_device_capability()\n-            if _cc is not None:\n-                compute_capability = _cc.major * 10 + _cc.minor\n+            return False, \"Requires ROCm.\"\n+\n         if compute_capability is not None and compute_capability < 90:\n-            return False, f\"requires capability 90, got {compute_capability}\"\n+            return False, \"requires compute capability 90 and above.\"\n \n         try:\n             import aiter  # noqa: F401 # deliberately attempt to import aiter\n         except Exception:\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel requires `aiter` which is not \"\n-                + \"installed on ROCm.\",\n-            )\n+            return False, \"requires `aiter` to be installed.\"\n \n         if not rocm_aiter_ops.is_linear_enabled():\n             return (\n                 False,\n-                \"AiterScaledMMLinearKernel is disabled. \"\n-                + \"Enable by setting `VLLM_ROCM_USE_AITER=1` \"\n+                \"requires setting `VLLM_ROCM_USE_AITER=1` \"\n                 + \"and `VLLM_ROCM_USE_AITER_LINEAR=1`. \"\n                 + \"`VLLM_ROCM_USE_AITER_LINEAR` default is True.\",\n             )\n-\n         return True, None\n \n     @classmethod\n-    def can_implement(cls, c: ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+    def can_implement(cls, c: Int8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n         if not c.input_symmetric:\n-            return (\n-                False,\n-                \"AiterScaledMMLinearKernel only supports symmetric \" + \"quantization.\",\n-            )\n+            return False, \"supports symmetric quantization only.\"\n         return True, None\n \n-    def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n-        super().process_weights_after_loading(layer)\n-\n     def apply_weights(\n         self,\n         layer: torch.nn.Module,\n         x: torch.Tensor,\n         bias: torch.Tensor | None = None,\n     ) -> torch.Tensor:\n         \"\"\"\n-        `AiterScaledMMLinearKernel` implements a fused version of\n+        `AiterInt8ScaledMMLinearKernel` implements a fused version of\n             `output = torch.mm((scale_a * a), (scale_b * b)).to(out_dtype)`\n         where scale_a * a and scale_b * b are implemented using numpy-style\n         broadcasting.\n         Currently only support per-tensor-per-tensor GEMM\n         and per-token-per-channel GEMM through AITER\n-        w8a8 scaled gemm. `AiterScaledMMLinearKernel` also does not support\n+        w8a8 scaled gemm. `AiterInt8ScaledMMLinearKernel` also does not support\n         ATIER block scaled GEMM and mix-precision GEMM.\n         \"\"\"\n-        w_q, w_s, i_s, i_zp, azp_adj = self._get_weight_params(layer)\n+        w_q, w_s, i_s, i_zp, azp_adj = self._get_layer_params(layer)\n \n         # ops.scaled_int8_quant supports both dynamic and static quant:\n         # * dynamic, i_s is None and x_s computed from x.\n         # * static, i_s is scalar and x_s is i_s.\n         symmetric = azp_adj is None\n         assert symmetric, (\n-            \"AiterScaledMMLinearKernel only supports symmetric quantization.\"\n+            \"AiterInt8ScaledMMLinearKernel only supports symmetric quantization.\"\n         )\n         x_q, x_s, x_zp = ops.scaled_int8_quant(x, i_s, i_zp, symmetric=symmetric)\n \n         assert x_zp is None, (\n-            \"AiterScaledMMLinearKernel only supports symmetric quantization.\"\n+            \"AiterInt8ScaledMMLinearKernel only supports symmetric quantization.\"\n         )\n         out_dtype = x.dtype\n \n@@ -117,12 +98,12 @@ def apply_weights(\n         ), (\n             \"Currently only support per-tensor-per-tensor GEMM \"\n             + \" and per-token-per-channel GEMM through AITER\"\n-            \" w8a8 scaled gemm. `AiterScaledMMLinearKernel` \"\n+            \" w8a8 scaled gemm. `AiterInt8ScaledMMLinearKernel` \"\n             + \"does not support AITER block scaled GEMM.\"\n         )\n \n         # gemm_a8w8_CK(a, b, scale_a, scale_b, bias) expects\n         # a to be [M, K]\n         # b to be [N, K]\n-        # CutlassScaledMMLinearKernel prepare weight `w_q` in [K, N] format\n+        # CutlassInt8ScaledMMLinearKernel prepare weight `w_q` in [K, N] format\n         return rocm_aiter_ops.gemm_a8w8(x_q, w_q.t(), x_s, w_s, bias, out_dtype)", "patch_sha256": "d87b3125d747a815"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2684421826", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py\nReview Comment:\nJust make this return None by default and only override in pytorch.py\n\nOriginal Code Context:\n@@ -0,0 +1,60 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )\n+\n+    def get_ouput_padding(self) -> int | None:\n+        return None\n", "materials": ["Just make this return None by default and only override in pytorch.py"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,57 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )", "patch_sha256": "298db567412a54f3"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2684435852", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/cutlass.py\nReview Comment:\nRename to `CutlassInt8ScaledMMLinearKernel`\n\nOriginal Code Context:\n@@ -11,35 +11,36 @@\n )\n from vllm.platforms import current_platform\n \n-from .ScaledMMLinearKernel import ScaledMMLinearKernel, ScaledMMLinearLayerConfig\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+    Int8ScaledMMLinearKernel,\n+    Int8ScaledMMLinearLayerConfig,\n+)\n \n \n-class CutlassScaledMMLinearKernel(ScaledMMLinearKernel):\n+class CutlassScaledMMLinearKernel(Int8ScaledMMLinearKernel):\n", "materials": ["Rename to `CutlassInt8ScaledMMLinearKernel`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -11,35 +11,36 @@\n )\n from vllm.platforms import current_platform\n \n-from .ScaledMMLinearKernel import ScaledMMLinearKernel, ScaledMMLinearLayerConfig\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+    Int8ScaledMMLinearKernel,\n+    Int8ScaledMMLinearLayerConfig,\n+)\n \n \n-class CutlassScaledMMLinearKernel(ScaledMMLinearKernel):\n+class CutlassInt8ScaledMMLinearKernel(Int8ScaledMMLinearKernel):\n     @classmethod\n     def is_supported(\n         cls, compute_capability: int | None = None\n     ) -> tuple[bool, str | None]:\n         if not current_platform.is_cuda():\n-            return False, \"Requires CUDA.\"\n-        if compute_capability is None:\n-            _cc = current_platform.get_device_capability()\n-            if _cc is not None:\n-                compute_capability = _cc.major * 10 + _cc.minor\n-        if compute_capability is not None and compute_capability < 75:\n-            return False, f\"requires capability 75, got {compute_capability}\"\n+            return False, \"requires CUDA.\"\n         return True, None\n \n     @classmethod\n-    def can_implement(cls, c: ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+    def can_implement(cls, c: Int8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n         return True, None\n \n     def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n+        w_q_name, w_s_name, i_s_name, i_zp_name, azp_adj_name = self.layer_param_names\n+        config = self.config\n         # WEIGHT\n         # Cutlass kernels need transposed weight.\n-        weight = getattr(layer, self.w_q_name)\n+        weight = getattr(layer, w_q_name)\n         replace_parameter(\n             layer,\n-            self.w_q_name,\n+            w_q_name,\n             torch.nn.Parameter(weight.t().data, requires_grad=False),\n         )\n \n@@ -48,28 +49,28 @@ def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n         # If we have a fused module (QKV, MLP) with per tensor scales (thus N\n         # scales being passed to the kernel), convert to the per-channel case.\n         is_fused_module = len(layer.logical_widths) > 1\n-        weight_scale = getattr(layer, self.w_s_name)\n-        if is_fused_module and not self.config.is_channelwise:\n+        weight_scale = getattr(layer, w_s_name)\n+        if is_fused_module and not config.is_channelwise:\n             weight_scale = convert_to_channelwise(weight_scale, layer.logical_widths)\n         replace_parameter(\n             layer,\n-            self.w_s_name,\n+            w_s_name,\n             torch.nn.Parameter(weight_scale.data, requires_grad=False),\n         )\n \n         # INPUT SCALE\n-        if self.config.is_static_input_scheme:\n-            input_scale = getattr(layer, self.i_s_name)\n+        if config.is_static_input_scheme:\n+            input_scale = getattr(layer, i_s_name)\n \n-            if self.config.input_symmetric:\n+            if config.input_symmetric:\n                 replace_parameter(\n                     layer,\n-                    self.i_s_name,\n+                    i_s_name,\n                     torch.nn.Parameter(input_scale.max(), requires_grad=False),\n                 )\n-                setattr(layer, self.i_zp_name, None)\n+                setattr(layer, i_zp_name, None)\n             else:\n-                input_zero_point = getattr(layer, self.i_zp_name)\n+                input_zero_point = getattr(layer, i_zp_name)\n \n                 # reconstruct the ranges\n                 int8_traits = torch.iinfo(torch.int8)\n@@ -79,46 +80,40 @@ def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n \n                 scale = (range_max - range_min) / (int8_traits.max - int8_traits.min)\n                 replace_parameter(\n-                    layer, self.i_s_name, torch.nn.Parameter(scale, requires_grad=False)\n+                    layer, i_s_name, torch.nn.Parameter(scale, requires_grad=False)\n                 )\n \n                 # AZP loaded as int8 but used as int32\n                 azp = (int8_traits.min - range_min / scale).to(dtype=torch.int32)\n                 replace_parameter(\n-                    layer, self.i_zp_name, torch.nn.Parameter(azp, requires_grad=False)\n+                    layer, i_zp_name, torch.nn.Parameter(azp, requires_grad=False)\n                 )\n \n-        else:\n-            setattr(layer, self.i_s_name, None)\n-            setattr(layer, self.i_zp_name, None)\n-\n         # azp_adj is the AZP adjustment term, used to account for weights.\n         # It does not depend on scales or azp, so it is the same for\n         # static and dynamic quantization.\n         # For more details, see csrc/quantization/w8a8/cutlass/Epilogues.md\n         # https://github.com/vllm-project/vllm/blob/main/csrc/quantization/w8a8/cutlass/Epilogues.md\n-        if not self.config.input_symmetric:\n-            weight = getattr(layer, self.w_q_name)\n+        if not config.input_symmetric:\n+            weight = getattr(layer, w_q_name)\n             azp_adj = weight.sum(dim=0, keepdim=True, dtype=torch.int32)\n-            if self.config.is_static_input_scheme:\n+            if config.is_static_input_scheme:\n                 # cutlass_w8a8 requires azp to be folded into azp_adj\n                 # in the per-tensor case\n-                azp_adj = getattr(layer, self.i_zp_name) * azp_adj\n+                azp_adj = getattr(layer, i_zp_name) * azp_adj\n             setattr(\n                 layer,\n-                self.azp_adj_name,\n+                azp_adj_name,\n                 torch.nn.Parameter(azp_adj, requires_grad=False),\n             )\n-        else:\n-            setattr(layer, self.azp_adj_name, None)\n \n     def apply_weights(\n         self,\n         layer: torch.nn.Module,\n         x: torch.Tensor,\n         bias: torch.Tensor | None = None,\n     ) -> torch.Tensor:\n-        w_q, w_s, i_s, i_zp, azp_adj = self._get_weight_params(layer)\n+        w_q, w_s, i_s, i_zp, azp_adj = self._get_layer_params(layer)\n \n         # ops.scaled_int8_quant supports both dynamic and static quant:\n         # * dynamic, i_s is None and x_s computed from x.\n@@ -145,3 +140,34 @@ def apply_weights(\n         return ops.cutlass_scaled_mm(\n             x_q, w_q, scale_a=x_s, scale_b=w_s, out_dtype=x.dtype, bias=bias\n         )\n+\n+\n+class CutlassFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        # Fused GEMM_DQ\n+        output = ops.cutlass_scaled_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )\n+        return output.view(*output_shape)", "patch_sha256": "46f5d7d2d8eaf50a"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2684436540", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/triton.py\nReview Comment:\nadd int8 to name?\n\nOriginal Code Context:\n@@ -14,68 +14,73 @@\n )\n from vllm.platforms import current_platform\n \n-from .ScaledMMLinearKernel import ScaledMMLinearKernel, ScaledMMLinearLayerConfig\n+from .cutlass import CutlassScaledMMLinearKernel\n+from .ScaledMMLinearKernel import (\n+    Int8ScaledMMLinearLayerConfig,\n+)\n \n \n-class TritonScaledMMLinearKernel(ScaledMMLinearKernel):\n+class TritonScaledMMLinearKernel(CutlassScaledMMLinearKernel):\n", "materials": ["add int8 to name?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -14,68 +14,73 @@\n )\n from vllm.platforms import current_platform\n \n-from .ScaledMMLinearKernel import ScaledMMLinearKernel, ScaledMMLinearLayerConfig\n+from .cutlass import CutlassInt8ScaledMMLinearKernel\n+from .ScaledMMLinearKernel import (\n+    Int8ScaledMMLinearLayerConfig,\n+)\n \n \n-class TritonScaledMMLinearKernel(ScaledMMLinearKernel):\n+class TritonInt8ScaledMMLinearKernel(CutlassInt8ScaledMMLinearKernel):\n     @classmethod\n     def is_supported(\n         cls, compute_capability: int | None = None\n     ) -> tuple[bool, str | None]:\n         if current_platform.is_cuda_alike():\n             return True, None\n-        return False, \"Requires ROCm or CUDA.\"\n+        return False, \"requires ROCm or CUDA.\"\n \n     @classmethod\n-    def can_implement(cls, c: ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+    def can_implement(cls, c: Int8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n         if not c.input_symmetric:\n-            return False, \"Only symmetric input is supported.\"\n+            return False, \"supports symmetric input only.\"\n         return True, None\n \n     def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n-        weight = getattr(layer, self.w_q_name)\n+        w_q, _, i_s, _, _ = self._get_layer_params(layer)\n+        w_q_name, w_s_name, i_s_name, i_zp_name, azp_adj_name = self.layer_param_names\n+\n         replace_parameter(\n             layer,\n-            self.w_q_name,\n-            torch.nn.Parameter(weight.t().data, requires_grad=False),\n+            w_q_name,\n+            torch.nn.Parameter(w_q.t().data, requires_grad=False),\n         )\n \n         # WEIGHT SCALE\n         # Triton kernel supports only per-tensor and per-channel.\n         # If we have a fused module (QKV, MLP) with per tensor scales (thus N\n         # scales being passed to the kernel), convert to the per-channel case.\n         is_fused_module = len(layer.logical_widths) > 1\n-        weight_scale = getattr(layer, self.w_s_name)\n+        weight_scale = getattr(layer, w_s_name)\n         if is_fused_module and not self.config.is_channelwise:\n             weight_scale = convert_to_channelwise(weight_scale, layer.logical_widths)\n         replace_parameter(\n             layer,\n-            self.w_s_name,\n+            w_s_name,\n             torch.nn.Parameter(weight_scale.data, requires_grad=False),\n         )\n \n         # INPUT SCALE\n         if self.config.is_static_input_scheme:\n-            input_scale = getattr(layer, self.i_s_name)\n+            assert i_s is not None\n             replace_parameter(\n                 layer,\n-                self.i_s_name,\n-                torch.nn.Parameter(input_scale.max(), requires_grad=False),\n+                i_s_name,\n+                torch.nn.Parameter(i_s.max(), requires_grad=False),\n             )\n-            setattr(layer, self.i_zp_name, None)\n+            setattr(layer, i_zp_name, None)\n         else:\n-            setattr(layer, self.i_s_name, None)\n-            setattr(layer, self.i_zp_name, None)\n+            setattr(layer, i_s_name, None)\n+            setattr(layer, i_zp_name, None)\n \n-        setattr(layer, self.azp_adj_name, None)\n+        setattr(layer, azp_adj_name, None)\n \n     def apply_weights(\n         self,\n         layer: torch.nn.Module,\n         x: torch.Tensor,\n         bias: torch.Tensor | None = None,\n     ) -> torch.Tensor:\n-        w_q, w_s, i_s, i_zp, azp_adj = self._get_weight_params(layer)\n+        w_q, w_s, i_s, i_zp, _ = self._get_layer_params(layer)\n \n         x_q, x_s, x_zp = ops.scaled_int8_quant(\n             x.contiguous(), i_s, i_zp, symmetric=True", "patch_sha256": "69fd7351243be92b"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2684448581", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/fp8.py\nReview Comment:\nLet's get rid of `self.act_q_group_shape`, should be inferable from `self.activation_quant_key.group_shape`.\n\nAlso, let's reverse the order of the branches, it looks a bit confusing to do:\n- dynamic cutlass\n- static\n- dynamic not cutlass\n\nCan we instead do:\n- static\n- dynamic cutlass\n- dynamic other\n\n```suggestion\n            if self.act_q_static:\n                self.activation_quant_key = kFp8StaticTensorSym\n            elif cutlass_fp8_supported():\n                # Use per-token quantization for better perf if dynamic and cutlass\n                self.activation_quant_key = kFp8DynamicTokenSym\n            else:\n                self.activation_quant_key = kFp8DynamicTensorSym\n```\n\nOriginal Code Context:\n@@ -334,8 +338,13 @@ def __init__(self, quant_config: Fp8Config):\n             # Use per-token quantization for better perf if dynamic and cutlass\n             if not self.act_q_static and cutlass_fp8_supported():\n                 self.act_q_group_shape = GroupShape.PER_TOKEN\n+                self.activation_quant_key = kFp8DynamicTokenSym\n+            elif self.act_q_static:\n+                self.act_q_group_shape = GroupShape.PER_TENSOR\n+                self.activation_quant_key = kFp8StaticTensorSym\n             else:\n                 self.act_q_group_shape = GroupShape.PER_TENSOR\n+                self.activation_quant_key = kFp8DynamicTensorSym\n", "materials": ["Let's get rid of `self.act_q_group_shape`, should be inferable from `self.activation_quant_key.group_shape`.\n\nAlso, let's reverse the order of the branches, it looks a bit confusing to do:\n- dynamic cutlass\n- static\n- dynamic not cutlass\n\nCan we instead do:\n- static\n- dynamic cutlass\n- dynamic other\n\n```suggestion\n            if self.act_q_static:\n                self.activation_quant_key = kFp8StaticTensorSym\n            elif cutlass_fp8_supported():\n                # Use per-token quantization for better perf if dynamic and cutlass\n                self.activation_quant_key = kFp8DynamicTokenSym\n            else:\n                self.activation_quant_key = kFp8DynamicTensorSym\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -48,6 +48,9 @@\n     QuantizationConfig,\n     QuantizeMethodBase,\n )\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n from vllm.model_executor.layers.quantization.kv_cache import BaseKVCacheMethod\n from vllm.model_executor.layers.quantization.utils.flashinfer_utils import (\n     apply_fi_trtllm_fp8_per_tensor_moe,\n@@ -76,12 +79,13 @@\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     GroupShape,\n     is_layer_skipped,\n+    kFp8DynamicTensorSym,\n+    kFp8DynamicTokenSym,\n+    kFp8StaticTensorSym,\n )\n from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n-    Fp8LinearOp,\n     cutlass_block_fp8_supported,\n     cutlass_fp8_supported,\n-    maybe_create_device_identity,\n     normalize_e4m3fn_to_e4m3fnuz,\n )\n from vllm.model_executor.parameter import (\n@@ -328,28 +332,30 @@ def __init__(self, quant_config: Fp8Config):\n         self.weight_block_size = self.quant_config.weight_block_size\n         self.block_quant = self.weight_block_size is not None\n         self.act_q_static = self.quant_config.activation_scheme == \"static\"\n-        if self.weight_block_size:\n-            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n-        else:\n-            # Use per-token quantization for better perf if dynamic and cutlass\n-            if not self.act_q_static and cutlass_fp8_supported():\n-                self.act_q_group_shape = GroupShape.PER_TOKEN\n-            else:\n-                self.act_q_group_shape = GroupShape.PER_TENSOR\n \n         if self.block_quant:\n             assert not self.act_q_static\n             assert self.weight_block_size is not None\n             self.w8a8_block_fp8_linear = W8A8BlockFp8LinearOp(\n                 weight_group_shape=GroupShape(*self.weight_block_size),\n-                act_quant_group_shape=self.act_q_group_shape,\n+                act_quant_group_shape=GroupShape(1, self.weight_block_size[0]),\n                 cutlass_block_fp8_supported=self.cutlass_block_fp8_supported,\n                 use_aiter_and_is_supported=self.use_aiter_and_is_supported,\n             )\n         else:\n-            self.fp8_linear = Fp8LinearOp(\n-                act_quant_static=self.act_q_static,\n-                act_quant_group_shape=self.act_q_group_shape,\n+            # Use per-token quantization for better perf if dynamic and cutlass\n+            if self.act_q_static:\n+                activation_quant_key = kFp8StaticTensorSym\n+            elif cutlass_fp8_supported():\n+                activation_quant_key = kFp8DynamicTokenSym\n+            else:\n+                activation_quant_key = kFp8DynamicTensorSym\n+\n+            self.fp8_linear = init_fp8_linear_kernel(\n+                activation_quant_key=activation_quant_key,\n+                weight_quant_key=kFp8StaticTensorSym,\n+                out_dtype=torch.get_default_dtype(),\n+                module_name=self.__class__.__name__,\n             )\n \n     def create_weights(\n@@ -362,8 +368,6 @@ def create_weights(\n         params_dtype: torch.dtype,\n         **extra_weight_attrs,\n     ):\n-        maybe_create_device_identity()\n-\n         output_size_per_partition = sum(output_partition_sizes)\n         weight_loader = extra_weight_attrs.get(\"weight_loader\")\n         layer.logical_widths = output_partition_sizes\n@@ -462,8 +466,6 @@ def patched_weight_loader(param, loaded_weight, *args, **kwargs):\n                 scale = create_fp8_input_scale(output_partition_sizes, weight_loader)\n                 set_weight_attrs(scale, {\"scale_type\": \"input_scale\"})\n                 layer.register_parameter(\"input_scale\", scale)\n-            else:\n-                layer.register_parameter(\"input_scale\", None)\n \n     def process_weights_after_loading(self, layer: Module) -> None:\n         if getattr(layer, \"_already_called_process_weights_after_loading\", False):\n@@ -602,14 +604,7 @@ def apply(\n                 bias=bias,\n             )\n \n-        return self.fp8_linear.apply(\n-            input=x,\n-            weight=layer.weight,\n-            weight_scale=layer.weight_scale,\n-            out_dtype=self.out_dtype,\n-            input_scale=layer.input_scale,\n-            bias=bias,\n-        )\n+        return self.fp8_linear.apply_weights(layer, x, bias)\n \n \n class Fp8MoEMethod(FusedMoEMethodBase):", "patch_sha256": "642b41eb0eed9aa5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2686326215", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py\nReview Comment:\n### FlashInfer kernel missing output shape transformation\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\n`FlashInferScaledMMLinearKernel.apply_scaled_mm` ignores the `output_shape` parameter and returns the raw GEMM output without reshaping it. All other kernel implementations (`CutlassFP8ScaledMMLinearKernel`, `PerTensorTorchScaledMMLinearKernel`, `ROCmScaledMMLinearKernel`, `RowWiseTorchScaledMMLinearKernel`, `ChannelWiseTorchScaledMMLinearKernel`) properly call `.view(*output_shape)` on their output. When the input tensor has more than 2 dimensions (e.g., `[batch, seq_len, hidden]`), the output will have incorrect shape `[batch*seq_len, output_features]` instead of the expected `[batch, seq_len, output_features]`.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py#L53-L57\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Na0nUNZxSgZUKSsDLW3C3V0I-12MJcULAk3uk6iydy7IqHXrg3fZQBEJTzB1bEmLSQW5vccR9a_o_It8SKZY8Wk9VvB6yBikgFpDruywzTQBmbVe1rMvo2ixtWLMlmI8hcr5KEy-1DhriHH0pgufhbUMgkL_3z92qM340_vd0a1qAdWxQBR2man-bXEcdoF5LDVKEpEYZzLbNusaaOYNuWIkyyxt7EF4X2rSgXnWomFAjytTzlQtdG27dNvJJbWT1stqBQwCNrTFHuhOHV80QtocEbvMslii75SR_kipqYbc1LlzO9sS5EkuYpntCNXIsHPVtIUGpup0Ul7-hncY1Q\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..VxDp7x1yPcUSMSf8aKtEl0QhH0TlY-ppKP8Sw3LU3cb8BsANvjzREjbePcUCF0PTdwBQop0lP736iNJPThiWnUaKB_714hR_r2BJMavea1jlFJ_PrTZGV5l5Vuhgd0o14GuGSdeRzCRoreHV-ej-OFZoU050qWZ8snqjJRj1QdsIAmRbpAbaKJMBf7p8Mb5x4n0T--lfNaDGhO8Q3WCvPs2UQ_iPjXGSeuWYNkNptjrzCmtDvQF4GVLYD4UYeNM5aKARcTd_BJEyjroGxwnEK62DIgqL0rta6DkDqPfyN0eF2enmaUse4yZacbqmELpWVs0NNQELU8fU_8FAKctb4A\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -0,0 +1,60 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )\n", "materials": ["### FlashInfer kernel missing output shape transformation\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\n`FlashInferScaledMMLinearKernel.apply_scaled_mm` ignores the `output_shape` parameter and returns the raw GEMM output without reshaping it. All other kernel implementations (`CutlassFP8ScaledMMLinearKernel`, `PerTensorTorchScaledMMLinearKernel`, `ROCmScaledMMLinearKernel`, `RowWiseTorchScaledMMLinearKernel`, `ChannelWiseTorchScaledMMLinearKernel`) properly call `.view(*output_shape)` on their output. When the input tensor has more than 2 dimensions (e.g., `[batch, seq_len, hidden]`), the output will have incorrect shape `[batch*seq_len, output_features]` instead of the expected `[batch, seq_len, output_features]`.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py#L53-L57\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Na0nUNZxSgZUKSsDLW3C3V0I-12MJcULAk3uk6iydy7IqHXrg3fZQBEJTzB1bEmLSQW5vccR9a_o_It8SKZY8Wk9VvB6yBikgFpDruywzTQBmbVe1rMvo2ixtWLMlmI8hcr5KEy-1DhriHH0pgufhbUMgkL_3z92qM340_vd0a1qAdWxQBR2man-bXEcdoF5LDVKEpEYZzLbNusaaOYNuWIkyyxt7EF4X2rSgXnWomFAjytTzlQtdG27dNvJJbWT1stqBQwCNrTFHuhOHV80QtocEbvMslii75SR_kipqYbc1LlzO9sS5EkuYpntCNXIsHPVtIUGpup0Ul7-hncY1Q\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..VxDp7x1yPcUSMSf8aKtEl0QhH0TlY-ppKP8Sw3LU3cb8BsANvjzREjbePcUCF0PTdwBQop0lP736iNJPThiWnUaKB_714hR_r2BJMavea1jlFJ_PrTZGV5l5Vuhgd0o14GuGSdeRzCRoreHV-ej-OFZoU050qWZ8snqjJRj1QdsIAmRbpAbaKJMBf7p8Mb5x4n0T--lfNaDGhO8Q3WCvPs2UQ_iPjXGSeuWYNkNptjrzCmtDvQF4GVLYD4UYeNM5aKARcTd_BJEyjroGxwnEK62DIgqL0rta6DkDqPfyN0eF2enmaUse4yZacbqmELpWVs0NNQELU8fU_8FAKctb4A\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,57 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )", "patch_sha256": "298db567412a54f3"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2686994845", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/cutlass.py\nReview Comment:\nI have made all the names consistent\n\nOriginal Code Context:\n@@ -11,35 +11,36 @@\n )\n from vllm.platforms import current_platform\n \n-from .ScaledMMLinearKernel import ScaledMMLinearKernel, ScaledMMLinearLayerConfig\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+    Int8ScaledMMLinearKernel,\n+    Int8ScaledMMLinearLayerConfig,\n+)\n \n \n-class CutlassScaledMMLinearKernel(ScaledMMLinearKernel):\n+class CutlassScaledMMLinearKernel(Int8ScaledMMLinearKernel):\n", "materials": ["I have made all the names consistent"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -11,35 +11,36 @@\n )\n from vllm.platforms import current_platform\n \n-from .ScaledMMLinearKernel import ScaledMMLinearKernel, ScaledMMLinearLayerConfig\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+    Int8ScaledMMLinearKernel,\n+    Int8ScaledMMLinearLayerConfig,\n+)\n \n \n-class CutlassScaledMMLinearKernel(ScaledMMLinearKernel):\n+class CutlassInt8ScaledMMLinearKernel(Int8ScaledMMLinearKernel):\n     @classmethod\n     def is_supported(\n         cls, compute_capability: int | None = None\n     ) -> tuple[bool, str | None]:\n         if not current_platform.is_cuda():\n-            return False, \"Requires CUDA.\"\n-        if compute_capability is None:\n-            _cc = current_platform.get_device_capability()\n-            if _cc is not None:\n-                compute_capability = _cc.major * 10 + _cc.minor\n-        if compute_capability is not None and compute_capability < 75:\n-            return False, f\"requires capability 75, got {compute_capability}\"\n+            return False, \"requires CUDA.\"\n         return True, None\n \n     @classmethod\n-    def can_implement(cls, c: ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+    def can_implement(cls, c: Int8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n         return True, None\n \n     def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n+        w_q_name, w_s_name, i_s_name, i_zp_name, azp_adj_name = self.layer_param_names\n+        config = self.config\n         # WEIGHT\n         # Cutlass kernels need transposed weight.\n-        weight = getattr(layer, self.w_q_name)\n+        weight = getattr(layer, w_q_name)\n         replace_parameter(\n             layer,\n-            self.w_q_name,\n+            w_q_name,\n             torch.nn.Parameter(weight.t().data, requires_grad=False),\n         )\n \n@@ -48,28 +49,28 @@ def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n         # If we have a fused module (QKV, MLP) with per tensor scales (thus N\n         # scales being passed to the kernel), convert to the per-channel case.\n         is_fused_module = len(layer.logical_widths) > 1\n-        weight_scale = getattr(layer, self.w_s_name)\n-        if is_fused_module and not self.config.is_channelwise:\n+        weight_scale = getattr(layer, w_s_name)\n+        if is_fused_module and not config.is_channelwise:\n             weight_scale = convert_to_channelwise(weight_scale, layer.logical_widths)\n         replace_parameter(\n             layer,\n-            self.w_s_name,\n+            w_s_name,\n             torch.nn.Parameter(weight_scale.data, requires_grad=False),\n         )\n \n         # INPUT SCALE\n-        if self.config.is_static_input_scheme:\n-            input_scale = getattr(layer, self.i_s_name)\n+        if config.is_static_input_scheme:\n+            input_scale = getattr(layer, i_s_name)\n \n-            if self.config.input_symmetric:\n+            if config.input_symmetric:\n                 replace_parameter(\n                     layer,\n-                    self.i_s_name,\n+                    i_s_name,\n                     torch.nn.Parameter(input_scale.max(), requires_grad=False),\n                 )\n-                setattr(layer, self.i_zp_name, None)\n+                setattr(layer, i_zp_name, None)\n             else:\n-                input_zero_point = getattr(layer, self.i_zp_name)\n+                input_zero_point = getattr(layer, i_zp_name)\n \n                 # reconstruct the ranges\n                 int8_traits = torch.iinfo(torch.int8)\n@@ -79,46 +80,40 @@ def process_weights_after_loading(self, layer: torch.nn.Module) -> None:\n \n                 scale = (range_max - range_min) / (int8_traits.max - int8_traits.min)\n                 replace_parameter(\n-                    layer, self.i_s_name, torch.nn.Parameter(scale, requires_grad=False)\n+                    layer, i_s_name, torch.nn.Parameter(scale, requires_grad=False)\n                 )\n \n                 # AZP loaded as int8 but used as int32\n                 azp = (int8_traits.min - range_min / scale).to(dtype=torch.int32)\n                 replace_parameter(\n-                    layer, self.i_zp_name, torch.nn.Parameter(azp, requires_grad=False)\n+                    layer, i_zp_name, torch.nn.Parameter(azp, requires_grad=False)\n                 )\n \n-        else:\n-            setattr(layer, self.i_s_name, None)\n-            setattr(layer, self.i_zp_name, None)\n-\n         # azp_adj is the AZP adjustment term, used to account for weights.\n         # It does not depend on scales or azp, so it is the same for\n         # static and dynamic quantization.\n         # For more details, see csrc/quantization/w8a8/cutlass/Epilogues.md\n         # https://github.com/vllm-project/vllm/blob/main/csrc/quantization/w8a8/cutlass/Epilogues.md\n-        if not self.config.input_symmetric:\n-            weight = getattr(layer, self.w_q_name)\n+        if not config.input_symmetric:\n+            weight = getattr(layer, w_q_name)\n             azp_adj = weight.sum(dim=0, keepdim=True, dtype=torch.int32)\n-            if self.config.is_static_input_scheme:\n+            if config.is_static_input_scheme:\n                 # cutlass_w8a8 requires azp to be folded into azp_adj\n                 # in the per-tensor case\n-                azp_adj = getattr(layer, self.i_zp_name) * azp_adj\n+                azp_adj = getattr(layer, i_zp_name) * azp_adj\n             setattr(\n                 layer,\n-                self.azp_adj_name,\n+                azp_adj_name,\n                 torch.nn.Parameter(azp_adj, requires_grad=False),\n             )\n-        else:\n-            setattr(layer, self.azp_adj_name, None)\n \n     def apply_weights(\n         self,\n         layer: torch.nn.Module,\n         x: torch.Tensor,\n         bias: torch.Tensor | None = None,\n     ) -> torch.Tensor:\n-        w_q, w_s, i_s, i_zp, azp_adj = self._get_weight_params(layer)\n+        w_q, w_s, i_s, i_zp, azp_adj = self._get_layer_params(layer)\n \n         # ops.scaled_int8_quant supports both dynamic and static quant:\n         # * dynamic, i_s is None and x_s computed from x.\n@@ -145,3 +140,34 @@ def apply_weights(\n         return ops.cutlass_scaled_mm(\n             x_q, w_q, scale_a=x_s, scale_b=w_s, out_dtype=x.dtype, bias=bias\n         )\n+\n+\n+class CutlassFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        # Fused GEMM_DQ\n+        output = ops.cutlass_scaled_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )\n+        return output.view(*output_shape)", "patch_sha256": "46f5d7d2d8eaf50a"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2686997064", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py\nReview Comment:\nThis comment is a duplicate and has been already addressed.\n\nOriginal Code Context:\n@@ -0,0 +1,60 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )\n", "materials": ["This comment is a duplicate and has been already addressed."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,57 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )", "patch_sha256": "298db567412a54f3"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2687007569", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py\nReview Comment:\n### FlashInfer kernel missing output tensor reshape\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe `FlashInferFP8ScaledMMLinearKernel.apply_scaled_mm` method returns the raw GEMM output without reshaping it using the `output_shape` parameter. The base class `FP8ScaledMMLinearKernel.apply_weights` flattens input to 2D and passes `output_shape = [*x.shape[:-1], w.shape[1]]` expecting subclasses to reshape the output back to the original batch dimensions. All other kernel implementations (Cutlass, PerTensorTorch, RowWiseTorch, ChannelWiseTorch, ROCm) correctly call `.view(*output_shape)` on the output, but FlashInfer does not. This will cause incorrect output tensor shapes when the input has more than 2 dimensions, leading to shape mismatches in downstream operations.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py#L53-L57\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..f6Sella8pO_hAaDoNMRBfqD4v5kH-7Bory-fM0tbKvIEgyKQfymewkk4X2zU1DYVmUtciAwmUIGWQCDv8MzffUZnm6-UuTRaj2fde9H5GTVYu07-tQZbKiRwSeth0IZU8QUajonilDLSECeF6X5Z5vQXZnok1cnwgAPEBDvajOV-kSLqwnmvaYhR2jYw14n_neiC8n3xCVnMXU9tWibtzOOi86kxu_AI1OzGGH23w76L9fwoGtLxZ9CVfsSOjnQkBvnUZES88tJA9vbrOIX8nn7guNliEMS6jzSikUnj2BemaMoSgjYcBd9sdJ-2-0SqHQ9g2wphWCkwazyek3KmTQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..CoQQYycPYEDYIPS3AtMJPwDMA3mPMuLQLT7xZv9MkjP8qi7qgwDvSD5U_BVd7zxVeLhkL4OuRc0nvdi3KST9mPVb9UoYoIs-65ykkbrRNQCRXxanVMwSTehwXL723f08khGEbY3k5-JlogxNQrq7JVwkS-jQ-IH7eL-vpdgQcUhnuM9VZYTLMtbWqvQ5WAYQ3tkESXwgUvhJTb-kdfbXBVZCED4tkX_SM2ucD84GiOf8Ca2sjZVaiDRRyDR1LV-OFpfvd64Blaq2Gzyjk62Fu-wSY5pDwcCfmLrHqFcmMIaEVMyqgHu0TsEa4tc3cA7qPCcea_FPjVANR5hBPyreUA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -0,0 +1,57 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )\n", "materials": ["### FlashInfer kernel missing output tensor reshape\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe `FlashInferFP8ScaledMMLinearKernel.apply_scaled_mm` method returns the raw GEMM output without reshaping it using the `output_shape` parameter. The base class `FP8ScaledMMLinearKernel.apply_weights` flattens input to 2D and passes `output_shape = [*x.shape[:-1], w.shape[1]]` expecting subclasses to reshape the output back to the original batch dimensions. All other kernel implementations (Cutlass, PerTensorTorch, RowWiseTorch, ChannelWiseTorch, ROCm) correctly call `.view(*output_shape)` on the output, but FlashInfer does not. This will cause incorrect output tensor shapes when the input has more than 2 dimensions, leading to shape mismatches in downstream operations.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py#L53-L57\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..f6Sella8pO_hAaDoNMRBfqD4v5kH-7Bory-fM0tbKvIEgyKQfymewkk4X2zU1DYVmUtciAwmUIGWQCDv8MzffUZnm6-UuTRaj2fde9H5GTVYu07-tQZbKiRwSeth0IZU8QUajonilDLSECeF6X5Z5vQXZnok1cnwgAPEBDvajOV-kSLqwnmvaYhR2jYw14n_neiC8n3xCVnMXU9tWibtzOOi86kxu_AI1OzGGH23w76L9fwoGtLxZ9CVfsSOjnQkBvnUZES88tJA9vbrOIX8nn7guNliEMS6jzSikUnj2BemaMoSgjYcBd9sdJ-2-0SqHQ9g2wphWCkwazyek3KmTQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..CoQQYycPYEDYIPS3AtMJPwDMA3mPMuLQLT7xZv9MkjP8qi7qgwDvSD5U_BVd7zxVeLhkL4OuRc0nvdi3KST9mPVb9UoYoIs-65ykkbrRNQCRXxanVMwSTehwXL723f08khGEbY3k5-JlogxNQrq7JVwkS-jQ-IH7eL-vpdgQcUhnuM9VZYTLMtbWqvQ5WAYQ3tkESXwgUvhJTb-kdfbXBVZCED4tkX_SM2ucD84GiOf8Ca2sjZVaiDRRyDR1LV-OFpfvd64Blaq2Gzyjk62Fu-wSY5pDwcCfmLrHqFcmMIaEVMyqgHu0TsEa4tc3cA7qPCcea_FPjVANR5hBPyreUA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,57 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )", "patch_sha256": "298db567412a54f3"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2687519726", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py\nReview Comment:\n### FlashInfer kernel missing output reshape to expected shape\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\n`FlashInferFP8ScaledMMLinearKernel.apply_scaled_mm` returns the raw output from `flashinfer_scaled_fp8_mm` without calling `.view(*output_shape)`. The base class `apply_weights` passes `output_shape` based on the original input dimensions (e.g., `[batch, seq, hidden]`), expecting the returned tensor to match that shape. All other kernel implementations (Cutlass, ROCm, and Torch variants) correctly include `.view(*output_shape)` to reshape the 2D GEMM output back to the expected multi-dimensional shape. This bug causes incorrect output tensor shapes on Blackwell GPUs (compute capability 100+) when FlashInfer is installed, as it's the first-choice kernel for CUDA.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py#L53-L57\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..J3e1sG40Qh7LznqkMNKjEbAxgBNpBwsUlID0413PXsVHI7jSUaSjxzsCI8ktXwGbVh_ptq1oXSMRga9RAlJgpelwp-uNnaJyFIeB9TWrjELJxINYVa8K4spgM6qjbNp_GYN5MDeNUNFqXOl4dhXiU6DAvQJ7aKnWguZU1PirRPw8c6_Xjus34VCAg3-gYIkHMAmyPuJo7VZiUfqBAVC6-VXH-CcpsorXTOGJ4oPzt-BUJ-rWSmgY3ucvt2hn0Kg9Xm9Z7NhJl40mt_Hfc8REM4FdWQolgh0ZdksHYRf1nDHpUFDlruoD4HFcps4lDTyrQnewrgThy6FWOTmkdGJXEA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..PrP6pVBuJhMAPJj-sNXOsOXV8I_X4CAtxlcEO2VeMRQr2nJ6IghqOY7lxUQ2DVJs4q1l6NrZCDWNDY5ZAlou_aSrwmhiRu7_Nkt3DFcA_ud2l4I3giY2E_jvMhyBHXG2k98K_yoQc2zhc49DQ-ABNDyGGYY-RZJ0EMB13W-T7QfsN6KZ03nxbd1frS6I8Dq-bctAeUUDe3K9PNh-RhVxnAFW8HpU7zXyXcYvD899u_HG6EKl2zI-g799Fh1w10NNnzJkMMfLtp8E3md_FBeubL07YzFPs9z6qqGGOLJpLJpPkV9b2IQYOQEc2Lm14W_vvTeRBBEAHL2R3_DZ4YGQbA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -0,0 +1,57 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )\n", "materials": ["### FlashInfer kernel missing output reshape to expected shape\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\n`FlashInferFP8ScaledMMLinearKernel.apply_scaled_mm` returns the raw output from `flashinfer_scaled_fp8_mm` without calling `.view(*output_shape)`. The base class `apply_weights` passes `output_shape` based on the original input dimensions (e.g., `[batch, seq, hidden]`), expecting the returned tensor to match that shape. All other kernel implementations (Cutlass, ROCm, and Torch variants) correctly include `.view(*output_shape)` to reshape the 2D GEMM output back to the expected multi-dimensional shape. This bug causes incorrect output tensor shapes on Blackwell GPUs (compute capability 100+) when FlashInfer is installed, as it's the first-choice kernel for CUDA.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/quantization/kernels/scaled_mm/flashinfer.py#L53-L57\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..J3e1sG40Qh7LznqkMNKjEbAxgBNpBwsUlID0413PXsVHI7jSUaSjxzsCI8ktXwGbVh_ptq1oXSMRga9RAlJgpelwp-uNnaJyFIeB9TWrjELJxINYVa8K4spgM6qjbNp_GYN5MDeNUNFqXOl4dhXiU6DAvQJ7aKnWguZU1PirRPw8c6_Xjus34VCAg3-gYIkHMAmyPuJo7VZiUfqBAVC6-VXH-CcpsorXTOGJ4oPzt-BUJ-rWSmgY3ucvt2hn0Kg9Xm9Z7NhJl40mt_Hfc8REM4FdWQolgh0ZdksHYRf1nDHpUFDlruoD4HFcps4lDTyrQnewrgThy6FWOTmkdGJXEA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..PrP6pVBuJhMAPJj-sNXOsOXV8I_X4CAtxlcEO2VeMRQr2nJ6IghqOY7lxUQ2DVJs4q1l6NrZCDWNDY5ZAlou_aSrwmhiRu7_Nkt3DFcA_ud2l4I3giY2E_jvMhyBHXG2k98K_yoQc2zhc49DQ-ABNDyGGYY-RZJ0EMB13W-T7QfsN6KZ03nxbd1frS6I8Dq-bctAeUUDe3K9PNh-RhVxnAFW8HpU7zXyXcYvD899u_HG6EKl2zI-g799Fh1w10NNnzJkMMfLtp8E3md_FBeubL07YzFPs9z6qqGGOLJpLJpPkV9b2IQYOQEc2Lm14W_vvTeRBBEAHL2R3_DZ4YGQbA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,57 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+import torch\n+\n+from vllm.platforms import current_platform\n+from vllm.utils.flashinfer import flashinfer_scaled_fp8_mm, has_flashinfer\n+\n+from .ScaledMMLinearKernel import (\n+    FP8ScaledMMLinearKernel,\n+    FP8ScaledMMLinearLayerConfig,\n+)\n+\n+\n+class FlashInferFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):\n+    @classmethod\n+    def is_supported(\n+        cls, compute_capability: int | None = None\n+    ) -> tuple[bool, str | None]:\n+        if not current_platform.is_cuda():\n+            return False, \"requires CUDA.\"\n+\n+        if not has_flashinfer():\n+            return False, \"requires FlashInfer to be installed.\"\n+\n+        if compute_capability is not None and compute_capability < 100:\n+            return False, \"requires compute capability 100 and above.\"\n+\n+        return True, None\n+\n+    @classmethod\n+    def can_implement(cls, c: FP8ScaledMMLinearLayerConfig) -> tuple[bool, str | None]:\n+        per_tensor_activation_scales = (\n+            c.activation_quant_key.scale.group_shape.is_per_tensor()\n+        )\n+        per_tensor_weight_scales = c.weight_quant_key.scale.group_shape.is_per_tensor()\n+\n+        if not (per_tensor_activation_scales and per_tensor_weight_scales):\n+            return False, \"requires per tensor activation and weight scales.\"\n+\n+        return True, None\n+\n+    def apply_scaled_mm(\n+        self,\n+        *,\n+        A: torch.Tensor,\n+        B: torch.Tensor,\n+        out_dtype: torch.dtype,\n+        As: torch.Tensor,\n+        Bs: torch.Tensor,\n+        bias: torch.Tensor | None,\n+        output_shape: list,\n+    ) -> torch.Tensor:\n+        return flashinfer_scaled_fp8_mm(\n+            A, B, out_dtype=out_dtype, scale_a=As, scale_b=Bs, bias=bias\n+        )", "patch_sha256": "298db567412a54f3"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2697326227", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/utils/quant_utils.py\nReview Comment:\nPER_TENSOR should be GroupShape(1, 1)? Another thing, _GroupShape seems for 2D tensors only. What about tensors that have more than 2 dimensions like weight of conv?\n\nOriginal Code Context:\n\n", "materials": ["PER_TENSOR should be GroupShape(1, 1)? Another thing, _GroupShape seems for 2D tensors only. What about tensors that have more than 2 dimensions like weight of conv?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -123,6 +123,9 @@ def __str__(self):\n kDynamicTensorScale = ScaleDesc(torch.float32, False, GroupShape.PER_TENSOR)\n kFp8DynamicTensorSym = QuantKey(FP8_DTYPE, kDynamicTensorScale, symmetric=True)\n \n+kStaticTokenScale = ScaleDesc(torch.float32, True, GroupShape.PER_TOKEN)\n+kFp8StaticTokenSym = QuantKey(FP8_DTYPE, kStaticTokenScale, symmetric=True)\n+\n kDynamicTokenScale = ScaleDesc(torch.float32, False, GroupShape.PER_TOKEN)\n kFp8DynamicTokenSym = QuantKey(FP8_DTYPE, kDynamicTokenScale, symmetric=True)\n ", "patch_sha256": "888b79e9363c64c5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2697371835", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/utils/quant_utils.py\nReview Comment:\nA little suggestion here. I don't think hiding the quant_scheme(per_tensor, per_channel, per_token) in `GroupShape` of `ScaleDesc` is a good idea. Users definitely know what quant_schemes they want. Why don't we specify quant_schem explicitly in `QuantKey` like this:\r\n```python\r\nclass QuantKey:\r\n    \"\"\"\r\n    Class for identifying the type of quantization.\r\n    dtype: quantized data type\r\n    scale: scale descriptor\r\n    scale2: second-level scale descriptor\r\n    symmetric: symmetric if True, asymmetric if False\r\n    \"\"\"\r\n\r\n    dtype: torch.dtype\r\n    scale: ScaleDesc\r\n    scale2: ScaleDesc | None = None\r\n    quant_scheme: QuantSchemeEnum\r\n    symmetric: bool = True\r\n```\r\nFurthermore, letting `QuantKey.quant_scheme` determine the scale_shape (or `GroupShape`) instead of the reverse would be more reasonable to me.\r\n\r\nThe instantiated `QuantKey`s in vllm/model_executor/layers/quantization/utils/quant_utils.py like `kFp8DynamicTensorSym` have already implied the quant_scheme. Inserting quant_scheme explicitly in `QuantKey` would be no harm I think.\n\nOriginal Code Context:\n\n", "materials": ["A little suggestion here. I don't think hiding the quant_scheme(per_tensor, per_channel, per_token) in `GroupShape` of `ScaleDesc` is a good idea. Users definitely know what quant_schemes they want. Why don't we specify quant_schem explicitly in `QuantKey` like this:\r\n```python\r\nclass QuantKey:\r\n    \"\"\"\r\n    Class for identifying the type of quantization.\r\n    dtype: quantized data type\r\n    scale: scale descriptor\r\n    scale2: second-level scale descriptor\r\n    symmetric: symmetric if True, asymmetric if False\r\n    \"\"\"\r\n\r\n    dtype: torch.dtype\r\n    scale: ScaleDesc\r\n    scale2: ScaleDesc | None = None\r\n    quant_scheme: QuantSchemeEnum\r\n    symmetric: bool = True\r\n```\r\nFurthermore, letting `QuantKey.quant_scheme` determine the scale_shape (or `GroupShape`) instead of the reverse would be more reasonable to me.\r\n\r\nThe instantiated `QuantKey`s in vllm/model_executor/layers/quantization/utils/quant_utils.py like `kFp8DynamicTensorSym` have already implied the quant_scheme. Inserting quant_scheme explicitly in `QuantKey` would be no harm I think."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -123,6 +123,9 @@ def __str__(self):\n kDynamicTensorScale = ScaleDesc(torch.float32, False, GroupShape.PER_TENSOR)\n kFp8DynamicTensorSym = QuantKey(FP8_DTYPE, kDynamicTensorScale, symmetric=True)\n \n+kStaticTokenScale = ScaleDesc(torch.float32, True, GroupShape.PER_TOKEN)\n+kFp8StaticTokenSym = QuantKey(FP8_DTYPE, kStaticTokenScale, symmetric=True)\n+\n kDynamicTokenScale = ScaleDesc(torch.float32, False, GroupShape.PER_TOKEN)\n kFp8DynamicTokenSym = QuantKey(FP8_DTYPE, kDynamicTokenScale, symmetric=True)\n ", "patch_sha256": "888b79e9363c64c5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2698248663", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/utils/quant_utils.py\nReview Comment:\nI highly agree with both of your remarks, can you open an issue?\n\nOriginal Code Context:\n\n", "materials": ["I highly agree with both of your remarks, can you open an issue?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -123,6 +123,9 @@ def __str__(self):\n kDynamicTensorScale = ScaleDesc(torch.float32, False, GroupShape.PER_TENSOR)\n kFp8DynamicTensorSym = QuantKey(FP8_DTYPE, kDynamicTensorScale, symmetric=True)\n \n+kStaticTokenScale = ScaleDesc(torch.float32, True, GroupShape.PER_TOKEN)\n+kFp8StaticTokenSym = QuantKey(FP8_DTYPE, kStaticTokenScale, symmetric=True)\n+\n kDynamicTokenScale = ScaleDesc(torch.float32, False, GroupShape.PER_TOKEN)\n kFp8DynamicTokenSym = QuantKey(FP8_DTYPE, kDynamicTokenScale, symmetric=True)\n ", "patch_sha256": "888b79e9363c64c5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2699477534", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/utils/quant_utils.py\nReview Comment:\nIf you open an issue we can discuss, definitely not something to be changed in this PR. Just FYI I like the group shape more because it's IMO the proper abstraction here to represent tensor/token/group quant all together.\r\n\r\nAlso per-tensor is not group shape (1,1) - that would be a single element per group, so size of scales would be equal to the size of the tensor\n\nOriginal Code Context:\n\n", "materials": ["If you open an issue we can discuss, definitely not something to be changed in this PR. Just FYI I like the group shape more because it's IMO the proper abstraction here to represent tensor/token/group quant all together.\r\n\r\nAlso per-tensor is not group shape (1,1) - that would be a single element per group, so size of scales would be equal to the size of the tensor"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -123,6 +123,9 @@ def __str__(self):\n kDynamicTensorScale = ScaleDesc(torch.float32, False, GroupShape.PER_TENSOR)\n kFp8DynamicTensorSym = QuantKey(FP8_DTYPE, kDynamicTensorScale, symmetric=True)\n \n+kStaticTokenScale = ScaleDesc(torch.float32, True, GroupShape.PER_TOKEN)\n+kFp8StaticTokenSym = QuantKey(FP8_DTYPE, kStaticTokenScale, symmetric=True)\n+\n kDynamicTokenScale = ScaleDesc(torch.float32, False, GroupShape.PER_TOKEN)\n kFp8DynamicTokenSym = QuantKey(FP8_DTYPE, kDynamicTokenScale, symmetric=True)\n ", "patch_sha256": "888b79e9363c64c5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2704175022", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/utils/quant_utils.py\nReview Comment:\n@vllmellm @ProExpertProg Hi, guys, we can continue discussing on this [RFC](https://github.com/vllm-project/vllm/issues/32589)\n\nOriginal Code Context:\n\n", "materials": ["@vllmellm @ProExpertProg Hi, guys, we can continue discussing on this [RFC](https://github.com/vllm-project/vllm/issues/32589)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -123,6 +123,9 @@ def __str__(self):\n kDynamicTensorScale = ScaleDesc(torch.float32, False, GroupShape.PER_TENSOR)\n kFp8DynamicTensorSym = QuantKey(FP8_DTYPE, kDynamicTensorScale, symmetric=True)\n \n+kStaticTokenScale = ScaleDesc(torch.float32, True, GroupShape.PER_TOKEN)\n+kFp8StaticTokenSym = QuantKey(FP8_DTYPE, kStaticTokenScale, symmetric=True)\n+\n kDynamicTokenScale = ScaleDesc(torch.float32, False, GroupShape.PER_TOKEN)\n kFp8DynamicTokenSym = QuantKey(FP8_DTYPE, kDynamicTokenScale, symmetric=True)\n ", "patch_sha256": "888b79e9363c64c5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28284/review_2502443268", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28284", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\n![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThe import `from vllm.model_executor.models.utils import extract_layer_index` is performed inside the `__init__` method. It is a best practice to place all imports at the top of the file for better readability, performance, and to avoid potential circular import issues. Please move this import to the top of the file.\n\nOriginal Code Context:\n@@ -1283,6 +1294,8 @@ def __init__(\n             raise ValueError(\"Duplicate layer name: {}\".format(prefix))\n         compilation_config.static_forward_context[prefix] = self\n         self.layer_name = prefix\n+        from vllm.model_executor.models.utils import extract_layer_index\n", "materials": ["![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThe import `from vllm.model_executor.models.utils import extract_layer_index` is performed inside the `__init__` method. It is a best practice to place all imports at the top of the file for better readability, performance, and to avoid potential circular import issues. Please move this import to the top of the file."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -35,6 +35,9 @@\n from vllm.model_executor.layers.fused_moe.rocm_aiter_fused_moe import (\n     init_aiter_topK_meta_data,\n )\n+from vllm.model_executor.layers.fused_moe.routed_experts_capturer import (\n+    RoutedExpertsCapturer,\n+)\n from vllm.model_executor.layers.fused_moe.routing_simulator import RoutingSimulator\n from vllm.model_executor.layers.quantization.base_config import (\n     QuantizationConfig,\n@@ -701,6 +704,13 @@ def maybe_init_modular_kernel(self) -> None:\n     def shared_experts(self) -> torch.nn.Module | None:\n         return None\n \n+    @property\n+    def layer_id(self):\n+        # Delayed import to avoid circular dependency\n+        from vllm.model_executor.models.utils import extract_layer_index\n+\n+        return extract_layer_index(self.layer_name)\n+\n     @property\n     def gate(self) -> torch.nn.Module | None:\n         return None\n@@ -1650,6 +1660,18 @@ def valid_grouping() -> bool:\n \n         assert topk_ids.dtype == indices_type or indices_type is None\n \n+        if (\n+            self.vllm_config.model_config is not None\n+            and self.vllm_config.model_config.enable_return_routed_experts\n+        ):\n+            # In dummy runs, the capturer is not initialized.\n+            capturer = RoutedExpertsCapturer.get_instance()\n+            if capturer is not None:  # in dummmy_run may be None\n+                capturer.capture(  # noqa\n+                    layer_id=self.layer_id,\n+                    topk_ids=topk_ids,\n+                )\n+\n         return topk_weights, topk_ids\n \n     def must_reduce_shared_expert_outputs(self) -> bool:", "patch_sha256": "151b0a5ec1769257"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28284/review_2652811661", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28284", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu_model_runner.py\nReview Comment:\nwhen world_size==1 (e.g. tp=1 with multiple independent servers)\r\nuse vllm_config.instance_id to ensure each server have a unique ID.\r\ncode like this:\r\nif world_size==1:\r\nself.instance_id=self.vllm_config.instance_id\r\nelse:\r\nself.instance_id = f\"rank_{rank // world_size}\"\n\nOriginal Code Context:\n@@ -5468,6 +5491,37 @@ def initialize_kv_cache(self, kv_cache_config: KVCacheConfig) -> None:\n                 kv_transfer_group.register_kv_caches(kv_caches)\n             kv_transfer_group.set_host_xfer_buffer_ops(copy_kv_blocks)\n \n+        self.init_routed_experts_capturer()\n+\n+    def init_routed_experts_capturer(self):\n+        logger.info(\n+            \"Initializing routed experts capturer, enable_return_routed_experts: %s\",\n+            self.model_config.enable_return_routed_experts,\n+        )\n+        routed_experts_capturer = RoutedExpertsCapturer.create(\n+            self.model_config.enable_return_routed_experts\n+        )\n+        block_size = self.cache_config.block_size\n+        self.max_num_kv_tokens = (\n+            self.kv_cache_config.num_blocks // len(self.kv_cache_config.kv_cache_groups)\n+            + 1\n+        ) * block_size\n+\n+        if \":\" in self.vllm_config.instance_id:  # for async mode in verl\n+            self.instance_id = self.vllm_config.instance_id.rsplit(\":\", 1)[-1]\n+        else:  # sync mode in verl\n+            rank = self.vllm_config.parallel_config.rank\n+            world_size = self.vllm_config.parallel_config.world_size\n+            self.instance_id = f\"rank_{rank // world_size}\"\n", "materials": ["when world_size==1 (e.g. tp=1 with multiple independent servers)\r\nuse vllm_config.instance_id to ensure each server have a unique ID.\r\ncode like this:\r\nif world_size==1:\r\nself.instance_id=self.vllm_config.instance_id\r\nelse:\r\nself.instance_id = f\"rank_{rank // world_size}\""], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -50,6 +50,9 @@\n from vllm.logger import init_logger\n from vllm.lora.layers import LoRAMapping, LoRAMappingType\n from vllm.model_executor.layers.attention_layer_base import AttentionLayerBase\n+from vllm.model_executor.layers.fused_moe.routed_experts_capturer import (\n+    RoutedExpertsCapturer,\n+)\n from vllm.model_executor.layers.rotary_embedding import (\n     MRotaryEmbedding,\n     XDRotaryEmbedding,\n@@ -1633,6 +1636,8 @@ def _get_block_table_and_slot_mapping(kv_cache_gid: int):\n             return blk_table_tensor, slot_mapping\n \n         block_table_gid_0, slot_mapping_gid_0 = _get_block_table_and_slot_mapping(0)\n+        if self.model_config.enable_return_routed_experts:\n+            self.slot_mapping = slot_mapping_gid_0[:num_tokens].cpu().numpy()\n         cm_base = CommonAttentionMetadata(\n             query_start_loc=self.query_start_loc.gpu[: num_reqs_padded + 1],\n             query_start_loc_cpu=self.query_start_loc.cpu[: num_reqs_padded + 1],\n@@ -3112,6 +3117,13 @@ def execute_model(\n                 \"after execute_model() returns None.\"\n             )\n \n+        if self.vllm_config.model_config.enable_return_routed_experts:\n+            capturer = RoutedExpertsCapturer.get_instance()\n+            if capturer is not None:\n+                capturer.clear_buffer()  # noqa\n+            else:\n+                logger.error(\"RoutedExpertsCapturer not initialized.\")\n+\n         if scheduler_output.preempted_req_ids and has_kv_transfer_group():\n             get_kv_transfer_group().handle_preemptions(\n                 scheduler_output.preempted_req_ids\n@@ -3485,6 +3497,13 @@ def propose_draft_token_ids(sampled_token_ids):\n             self.eplb_step()\n \n         with record_function_or_nullcontext(\"gpu_model_runner: ModelRunnerOutput\"):\n+            if self.model_config.enable_return_routed_experts:\n+                capturer = RoutedExpertsCapturer.get_instance()\n+                if capturer is not None:\n+                    capturer.save_captured_experts(indices=self.slot_mapping)  # noqa\n+                else:\n+                    logger.error(\"RoutedExpertsCapturer not initialized.\")\n+\n             output = ModelRunnerOutput(\n                 req_ids=req_ids_output_copy,\n                 req_id_to_index=req_id_to_index_output_copy,\n@@ -5646,6 +5665,28 @@ def initialize_kv_cache(self, kv_cache_config: KVCacheConfig) -> None:\n                 kv_transfer_group.register_kv_caches(kv_caches)\n             kv_transfer_group.set_host_xfer_buffer_ops(copy_kv_blocks)\n \n+        if self.model_config.enable_return_routed_experts:\n+            self.init_routed_experts_capturer()\n+\n+    def init_routed_experts_capturer(self):\n+        logger.info(\n+            \"Initializing routed experts capturer, enable_return_routed_experts: %s\",\n+            self.model_config.enable_return_routed_experts,\n+        )\n+        routed_experts_capturer = RoutedExpertsCapturer.create()\n+        block_size = self.cache_config.block_size\n+        self.max_num_kv_tokens = (\n+            self.kv_cache_config.num_blocks // len(self.kv_cache_config.kv_cache_groups)\n+            + 1\n+        ) * block_size\n+\n+        routed_experts_capturer.init_buffer(\n+            max_num_batched_tokens=self.scheduler_config.max_num_batched_tokens,\n+            max_num_kv_tokens=self.max_num_kv_tokens,\n+            model_config=self.model_config,\n+            instance_id=self.vllm_config.instance_id,\n+        )\n+\n     def may_add_encoder_only_layers_to_kv_cache_config(self) -> None:\n         \"\"\"\n         Add encoder-only layers to the KV cache config.", "patch_sha256": "0c2f4c812c68a30d"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28284/review_2672441216", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28284", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nsince it's has a property annotation:\n```suggestion\n    def layer_id(self):\n```\n\nOriginal Code Context:\n@@ -670,6 +678,10 @@ def maybe_init_modular_kernel(self) -> None:\n     def shared_experts(self) -> torch.nn.Module | None:\n         return None\n \n+    @property\n+    def get_layer_id(self):\n", "materials": ["since it's has a property annotation:\n```suggestion\n    def layer_id(self):\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -35,6 +35,9 @@\n from vllm.model_executor.layers.fused_moe.rocm_aiter_fused_moe import (\n     init_aiter_topK_meta_data,\n )\n+from vllm.model_executor.layers.fused_moe.routed_experts_capturer import (\n+    RoutedExpertsCapturer,\n+)\n from vllm.model_executor.layers.fused_moe.routing_simulator import RoutingSimulator\n from vllm.model_executor.layers.quantization.base_config import (\n     QuantizationConfig,\n@@ -701,6 +704,13 @@ def maybe_init_modular_kernel(self) -> None:\n     def shared_experts(self) -> torch.nn.Module | None:\n         return None\n \n+    @property\n+    def layer_id(self):\n+        # Delayed import to avoid circular dependency\n+        from vllm.model_executor.models.utils import extract_layer_index\n+\n+        return extract_layer_index(self.layer_name)\n+\n     @property\n     def gate(self) -> torch.nn.Module | None:\n         return None\n@@ -1650,6 +1660,18 @@ def valid_grouping() -> bool:\n \n         assert topk_ids.dtype == indices_type or indices_type is None\n \n+        if (\n+            self.vllm_config.model_config is not None\n+            and self.vllm_config.model_config.enable_return_routed_experts\n+        ):\n+            # In dummy runs, the capturer is not initialized.\n+            capturer = RoutedExpertsCapturer.get_instance()\n+            if capturer is not None:  # in dummmy_run may be None\n+                capturer.capture(  # noqa\n+                    layer_id=self.layer_id,\n+                    topk_ids=topk_ids,\n+                )\n+\n         return topk_weights, topk_ids\n \n     def must_reduce_shared_expert_outputs(self) -> bool:", "patch_sha256": "151b0a5ec1769257"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28284/review_2672508932", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28284", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/model.py\nReview Comment:\njust call it `return_routed_experts`?\n\nOriginal Code Context:\n@@ -196,6 +196,8 @@ class ModelConfig:\n     graph and always execute the model in eager mode. If False, we will use\n     CUDA graph and eager execution in hybrid for maximal performance and\n     flexibility.\"\"\"\n+    enable_return_routed_experts: bool = False\n", "materials": ["just call it `return_routed_experts`?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -198,6 +198,8 @@ class ModelConfig:\n     graph and always execute the model in eager mode. If False, we will use\n     CUDA graph and eager execution in hybrid for maximal performance and\n     flexibility.\"\"\"\n+    enable_return_routed_experts: bool = False\n+    \"\"\"Whether to return routed experts.\"\"\"\n     max_logprobs: int = 20\n     \"\"\"Maximum number of log probabilities to return when `logprobs` is\n     specified in `SamplingParams`. The default value comes the default for the", "patch_sha256": "816340a952dd4e8e"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28284/review_2674553873", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28284", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu_model_runner.py\nReview Comment:\n### Uninitialized `self.slot_mapping` Attribute May Cause AttributeError\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\n`self.slot_mapping` is only conditionally set in `_get_block_table_and_slot_mapping()` (line 1617-1618) when `enable_return_routed_experts` is True, `kv_cache_gid == 0`, AND the code path is NOT `EncoderOnlyAttentionSpec`. However, in `sample_tokens()` at line 3500, `self.slot_mapping` is accessed without verifying it was properly initialized:\n\n```python\ncapturer.save_captured_experts(indices=self.slot_mapping)\n```\n\nIf a model uses `EncoderOnlyAttentionSpec` for the first KV cache group while `enable_return_routed_experts` is True, `self.slot_mapping` will never be set, causing an `AttributeError`. The fix is to initialize `self.slot_mapping = None` in `__init__` and add a guard check before using it.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/worker/gpu_model_runner.py#L3499-L3500\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..l8vv1oftT480z9AgYN-TaVRKl7w8pZGoD-_6nD54nZDjnAFqibBH--x9h-rWpBDTcGSdAf7pKbr6sDWFNhbd0fZcfpsaN8MJ-YpHvgdtLVB3XTJh0Fz3fyEBTWkZ6YREWB0t1ARaQcO0ayhVj3Ayoab_Ovw1zoFUmq_Gzq6z6gPzc8MOk_dJKFPXGjmga6VC3ajY-Y90O-Jy224_uGXWNRtfdDaaU4H2O7PWq07x4OaOEbztAHMY_76a_K4CqpexIPF3QB7urMsY7QUUmqfFd1MAfuiudmaquQN6zjM3Y5D4yS_XO4factr_IYvZiA9UTRUPb_4zkQqxB_niVAY_wg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..bXdJEd2XRr2daU_TMS6IQgErmJ7ni3p3pvx-4gAOeMqIo-lee7oiK2qlMmkR5v7r88TMq0gqWATdTO7lKRUm1xjKoMLcJDuXuOuQ1FqD-_Jj0jhvy2eGrvHkaf5Pa4d7JBAYtNZC-XxFKCksOO1h0_aHQ8rGmyLWjrejck_oeiJ9CCkWJ3Brqkb9w2kss_1jV7Ro_RoZgTDnGyXnoJq5up5WDQTG1UiuEaWwA7QrdSgxndfKqwuqPHB6dB7Ut0Rq7qqIYqYlfdll51CS8C9Ng8KnrZ5dJPt0W58ZQro5pMkbZwhGJJGXyF54G7Ilu0wX9cb7sCI7u8ybxirZyyyufQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -3478,6 +3491,16 @@ def propose_draft_token_ids(sampled_token_ids):\n         with record_function_or_nullcontext(\"gpu_model_runner: eplb\"):\n             self.eplb_step()\n         with record_function_or_nullcontext(\"gpu_model_runner: ModelRunnerOutput\"):\n+            if (\n+                self.model_config.enable_return_routed_experts\n+                and get_tensor_model_parallel_rank() == 0\n+            ):\n+                capturer = RoutedExpertsCapturer.get_instance()\n+                if capturer is not None:\n+                    capturer.save_captured_experts(indices=self.slot_mapping)  # noqa\n", "materials": ["### Uninitialized `self.slot_mapping` Attribute May Cause AttributeError\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\n`self.slot_mapping` is only conditionally set in `_get_block_table_and_slot_mapping()` (line 1617-1618) when `enable_return_routed_experts` is True, `kv_cache_gid == 0`, AND the code path is NOT `EncoderOnlyAttentionSpec`. However, in `sample_tokens()` at line 3500, `self.slot_mapping` is accessed without verifying it was properly initialized:\n\n```python\ncapturer.save_captured_experts(indices=self.slot_mapping)\n```\n\nIf a model uses `EncoderOnlyAttentionSpec` for the first KV cache group while `enable_return_routed_experts` is True, `self.slot_mapping` will never be set, causing an `AttributeError`. The fix is to initialize `self.slot_mapping = None` in `__init__` and add a guard check before using it.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/worker/gpu_model_runner.py#L3499-L3500\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..l8vv1oftT480z9AgYN-TaVRKl7w8pZGoD-_6nD54nZDjnAFqibBH--x9h-rWpBDTcGSdAf7pKbr6sDWFNhbd0fZcfpsaN8MJ-YpHvgdtLVB3XTJh0Fz3fyEBTWkZ6YREWB0t1ARaQcO0ayhVj3Ayoab_Ovw1zoFUmq_Gzq6z6gPzc8MOk_dJKFPXGjmga6VC3ajY-Y90O-Jy224_uGXWNRtfdDaaU4H2O7PWq07x4OaOEbztAHMY_76a_K4CqpexIPF3QB7urMsY7QUUmqfFd1MAfuiudmaquQN6zjM3Y5D4yS_XO4factr_IYvZiA9UTRUPb_4zkQqxB_niVAY_wg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..bXdJEd2XRr2daU_TMS6IQgErmJ7ni3p3pvx-4gAOeMqIo-lee7oiK2qlMmkR5v7r88TMq0gqWATdTO7lKRUm1xjKoMLcJDuXuOuQ1FqD-_Jj0jhvy2eGrvHkaf5Pa4d7JBAYtNZC-XxFKCksOO1h0_aHQ8rGmyLWjrejck_oeiJ9CCkWJ3Brqkb9w2kss_1jV7Ro_RoZgTDnGyXnoJq5up5WDQTG1UiuEaWwA7QrdSgxndfKqwuqPHB6dB7Ut0Rq7qqIYqYlfdll51CS8C9Ng8KnrZ5dJPt0W58ZQro5pMkbZwhGJJGXyF54G7Ilu0wX9cb7sCI7u8ybxirZyyyufQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -50,6 +50,9 @@\n from vllm.logger import init_logger\n from vllm.lora.layers import LoRAMapping, LoRAMappingType\n from vllm.model_executor.layers.attention_layer_base import AttentionLayerBase\n+from vllm.model_executor.layers.fused_moe.routed_experts_capturer import (\n+    RoutedExpertsCapturer,\n+)\n from vllm.model_executor.layers.rotary_embedding import (\n     MRotaryEmbedding,\n     XDRotaryEmbedding,\n@@ -1633,6 +1636,8 @@ def _get_block_table_and_slot_mapping(kv_cache_gid: int):\n             return blk_table_tensor, slot_mapping\n \n         block_table_gid_0, slot_mapping_gid_0 = _get_block_table_and_slot_mapping(0)\n+        if self.model_config.enable_return_routed_experts:\n+            self.slot_mapping = slot_mapping_gid_0[:num_tokens].cpu().numpy()\n         cm_base = CommonAttentionMetadata(\n             query_start_loc=self.query_start_loc.gpu[: num_reqs_padded + 1],\n             query_start_loc_cpu=self.query_start_loc.cpu[: num_reqs_padded + 1],\n@@ -3112,6 +3117,13 @@ def execute_model(\n                 \"after execute_model() returns None.\"\n             )\n \n+        if self.vllm_config.model_config.enable_return_routed_experts:\n+            capturer = RoutedExpertsCapturer.get_instance()\n+            if capturer is not None:\n+                capturer.clear_buffer()  # noqa\n+            else:\n+                logger.error(\"RoutedExpertsCapturer not initialized.\")\n+\n         if scheduler_output.preempted_req_ids and has_kv_transfer_group():\n             get_kv_transfer_group().handle_preemptions(\n                 scheduler_output.preempted_req_ids\n@@ -3485,6 +3497,13 @@ def propose_draft_token_ids(sampled_token_ids):\n             self.eplb_step()\n \n         with record_function_or_nullcontext(\"gpu_model_runner: ModelRunnerOutput\"):\n+            if self.model_config.enable_return_routed_experts:\n+                capturer = RoutedExpertsCapturer.get_instance()\n+                if capturer is not None:\n+                    capturer.save_captured_experts(indices=self.slot_mapping)  # noqa\n+                else:\n+                    logger.error(\"RoutedExpertsCapturer not initialized.\")\n+\n             output = ModelRunnerOutput(\n                 req_ids=req_ids_output_copy,\n                 req_id_to_index=req_id_to_index_output_copy,\n@@ -5646,6 +5665,28 @@ def initialize_kv_cache(self, kv_cache_config: KVCacheConfig) -> None:\n                 kv_transfer_group.register_kv_caches(kv_caches)\n             kv_transfer_group.set_host_xfer_buffer_ops(copy_kv_blocks)\n \n+        if self.model_config.enable_return_routed_experts:\n+            self.init_routed_experts_capturer()\n+\n+    def init_routed_experts_capturer(self):\n+        logger.info(\n+            \"Initializing routed experts capturer, enable_return_routed_experts: %s\",\n+            self.model_config.enable_return_routed_experts,\n+        )\n+        routed_experts_capturer = RoutedExpertsCapturer.create()\n+        block_size = self.cache_config.block_size\n+        self.max_num_kv_tokens = (\n+            self.kv_cache_config.num_blocks // len(self.kv_cache_config.kv_cache_groups)\n+            + 1\n+        ) * block_size\n+\n+        routed_experts_capturer.init_buffer(\n+            max_num_batched_tokens=self.scheduler_config.max_num_batched_tokens,\n+            max_num_kv_tokens=self.max_num_kv_tokens,\n+            model_config=self.model_config,\n+            instance_id=self.vllm_config.instance_id,\n+        )\n+\n     def may_add_encoder_only_layers_to_kv_cache_config(self) -> None:\n         \"\"\"\n         Add encoder-only layers to the KV cache config.", "patch_sha256": "0c2f4c812c68a30d"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28284/review_2674553874", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28284", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu_model_runner.py\nReview Comment:\n### Unconditional Error Logging Regardless of Feature State\n\n**Low Severity**\n\n<!-- DESCRIPTION START -->\nThe code unconditionally logs an error if `RoutedExpertsCapturer.get_instance()` returns `None`, regardless of whether `enable_return_routed_experts` is enabled:\n\n```python\ncapturer = RoutedExpertsCapturer.get_instance()\nif capturer is not None:\n    capturer.clear_buffer()\nelse:\n    logger.error(\"RoutedExpertsCapturer not initialized.\")\n```\n\nWhile in normal operation a Noop capturer is created when the feature is disabled, this could log misleading errors in edge cases or during testing. The error logging should be conditional on the feature being enabled.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/worker/gpu_model_runner.py#L3106-L3108\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..piZbGOJdUNbt9YCit59C1HkIodk_sv0xdN2OZpU1oxWf4LxS2G2b5FK-Cc3vUKMtGiR6hTS7SVZES8LxIwI3zU6Lh1lr79J8-YE92PoIZR1NEAx04R2Efz3i7wt_pqqa_AprhNn6qZJhDecJLBfeDZl7irF7sW-rfcOmP8Ldwr2-EGDrixuRb_w9OOQ8OZcO0BUnToDgPssa1yw2K4FG6yumXeJKGU7XTErSNNYnUBDKRNsA6lwcAOm1gg_apPKdzPqi_fC5B_XbBXwXivkcgg9fbRj3vspzghgjZanvpIqR5z8L2thu36lS-hLAyrMRx56da9YAg_a37hHnnF6EQg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..lfpkYba_TviQBv86TjPidOY6Ns-WHhmMskrDSz0reAVETPdu0e88R9WSzd04KkZDeUZslYITBMx5OgRtwk6FyNwJqTgKZurwXqBFGSlA3z4CYA3vrCW-OMEb_2d0thu3gMs6rzhNgnZRlElbgOlI5R3HZuQB6C7jGsDAD7E15C4nUyjK7ylA7KXpdm_lQHwX3A14RvS9XC11qBjO3yxhahbqGcNbkEpvFRuycbkt7KIQw_X00MPrKWJyGRL87sHwCjFWYl3g98VwT3zEU4IvXOwWD1RYTcf7s4tQv0y5SBe3t_Oh9qthxBoXHKazUPtn1hh5lyr-JxOZZXaKWA9mlA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -3094,6 +3101,12 @@ def execute_model(\n                 \"after execute_model() returns None.\"\n             )\n \n+        capturer = RoutedExpertsCapturer.get_instance()\n+        if capturer is not None:\n+            capturer.clear_buffer()  # noqa\n+        else:\n+            logger.error(\"RoutedExpertsCapturer not initialized.\")\n", "materials": ["### Unconditional Error Logging Regardless of Feature State\n\n**Low Severity**\n\n<!-- DESCRIPTION START -->\nThe code unconditionally logs an error if `RoutedExpertsCapturer.get_instance()` returns `None`, regardless of whether `enable_return_routed_experts` is enabled:\n\n```python\ncapturer = RoutedExpertsCapturer.get_instance()\nif capturer is not None:\n    capturer.clear_buffer()\nelse:\n    logger.error(\"RoutedExpertsCapturer not initialized.\")\n```\n\nWhile in normal operation a Noop capturer is created when the feature is disabled, this could log misleading errors in edge cases or during testing. The error logging should be conditional on the feature being enabled.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/worker/gpu_model_runner.py#L3106-L3108\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..piZbGOJdUNbt9YCit59C1HkIodk_sv0xdN2OZpU1oxWf4LxS2G2b5FK-Cc3vUKMtGiR6hTS7SVZES8LxIwI3zU6Lh1lr79J8-YE92PoIZR1NEAx04R2Efz3i7wt_pqqa_AprhNn6qZJhDecJLBfeDZl7irF7sW-rfcOmP8Ldwr2-EGDrixuRb_w9OOQ8OZcO0BUnToDgPssa1yw2K4FG6yumXeJKGU7XTErSNNYnUBDKRNsA6lwcAOm1gg_apPKdzPqi_fC5B_XbBXwXivkcgg9fbRj3vspzghgjZanvpIqR5z8L2thu36lS-hLAyrMRx56da9YAg_a37hHnnF6EQg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..lfpkYba_TviQBv86TjPidOY6Ns-WHhmMskrDSz0reAVETPdu0e88R9WSzd04KkZDeUZslYITBMx5OgRtwk6FyNwJqTgKZurwXqBFGSlA3z4CYA3vrCW-OMEb_2d0thu3gMs6rzhNgnZRlElbgOlI5R3HZuQB6C7jGsDAD7E15C4nUyjK7ylA7KXpdm_lQHwX3A14RvS9XC11qBjO3yxhahbqGcNbkEpvFRuycbkt7KIQw_X00MPrKWJyGRL87sHwCjFWYl3g98VwT3zEU4IvXOwWD1RYTcf7s4tQv0y5SBe3t_Oh9qthxBoXHKazUPtn1hh5lyr-JxOZZXaKWA9mlA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -50,6 +50,9 @@\n from vllm.logger import init_logger\n from vllm.lora.layers import LoRAMapping, LoRAMappingType\n from vllm.model_executor.layers.attention_layer_base import AttentionLayerBase\n+from vllm.model_executor.layers.fused_moe.routed_experts_capturer import (\n+    RoutedExpertsCapturer,\n+)\n from vllm.model_executor.layers.rotary_embedding import (\n     MRotaryEmbedding,\n     XDRotaryEmbedding,\n@@ -1633,6 +1636,8 @@ def _get_block_table_and_slot_mapping(kv_cache_gid: int):\n             return blk_table_tensor, slot_mapping\n \n         block_table_gid_0, slot_mapping_gid_0 = _get_block_table_and_slot_mapping(0)\n+        if self.model_config.enable_return_routed_experts:\n+            self.slot_mapping = slot_mapping_gid_0[:num_tokens].cpu().numpy()\n         cm_base = CommonAttentionMetadata(\n             query_start_loc=self.query_start_loc.gpu[: num_reqs_padded + 1],\n             query_start_loc_cpu=self.query_start_loc.cpu[: num_reqs_padded + 1],\n@@ -3112,6 +3117,13 @@ def execute_model(\n                 \"after execute_model() returns None.\"\n             )\n \n+        if self.vllm_config.model_config.enable_return_routed_experts:\n+            capturer = RoutedExpertsCapturer.get_instance()\n+            if capturer is not None:\n+                capturer.clear_buffer()  # noqa\n+            else:\n+                logger.error(\"RoutedExpertsCapturer not initialized.\")\n+\n         if scheduler_output.preempted_req_ids and has_kv_transfer_group():\n             get_kv_transfer_group().handle_preemptions(\n                 scheduler_output.preempted_req_ids\n@@ -3485,6 +3497,13 @@ def propose_draft_token_ids(sampled_token_ids):\n             self.eplb_step()\n \n         with record_function_or_nullcontext(\"gpu_model_runner: ModelRunnerOutput\"):\n+            if self.model_config.enable_return_routed_experts:\n+                capturer = RoutedExpertsCapturer.get_instance()\n+                if capturer is not None:\n+                    capturer.save_captured_experts(indices=self.slot_mapping)  # noqa\n+                else:\n+                    logger.error(\"RoutedExpertsCapturer not initialized.\")\n+\n             output = ModelRunnerOutput(\n                 req_ids=req_ids_output_copy,\n                 req_id_to_index=req_id_to_index_output_copy,\n@@ -5646,6 +5665,28 @@ def initialize_kv_cache(self, kv_cache_config: KVCacheConfig) -> None:\n                 kv_transfer_group.register_kv_caches(kv_caches)\n             kv_transfer_group.set_host_xfer_buffer_ops(copy_kv_blocks)\n \n+        if self.model_config.enable_return_routed_experts:\n+            self.init_routed_experts_capturer()\n+\n+    def init_routed_experts_capturer(self):\n+        logger.info(\n+            \"Initializing routed experts capturer, enable_return_routed_experts: %s\",\n+            self.model_config.enable_return_routed_experts,\n+        )\n+        routed_experts_capturer = RoutedExpertsCapturer.create()\n+        block_size = self.cache_config.block_size\n+        self.max_num_kv_tokens = (\n+            self.kv_cache_config.num_blocks // len(self.kv_cache_config.kv_cache_groups)\n+            + 1\n+        ) * block_size\n+\n+        routed_experts_capturer.init_buffer(\n+            max_num_batched_tokens=self.scheduler_config.max_num_batched_tokens,\n+            max_num_kv_tokens=self.max_num_kv_tokens,\n+            model_config=self.model_config,\n+            instance_id=self.vllm_config.instance_id,\n+        )\n+\n     def may_add_encoder_only_layers_to_kv_cache_config(self) -> None:\n         \"\"\"\n         Add encoder-only layers to the KV cache config.", "patch_sha256": "0c2f4c812c68a30d"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28284/review_2678525141", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28284", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/model.py\nReview Comment:\nI think this is fine. However, the verl framework currently uses this parameter when integrating with vLLM, so changing it would require coordinated changes in verl as well.\n\nOriginal Code Context:\n@@ -196,6 +196,8 @@ class ModelConfig:\n     graph and always execute the model in eager mode. If False, we will use\n     CUDA graph and eager execution in hybrid for maximal performance and\n     flexibility.\"\"\"\n+    enable_return_routed_experts: bool = False\n", "materials": ["I think this is fine. However, the verl framework currently uses this parameter when integrating with vLLM, so changing it would require coordinated changes in verl as well."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -198,6 +198,8 @@ class ModelConfig:\n     graph and always execute the model in eager mode. If False, we will use\n     CUDA graph and eager execution in hybrid for maximal performance and\n     flexibility.\"\"\"\n+    enable_return_routed_experts: bool = False\n+    \"\"\"Whether to return routed experts.\"\"\"\n     max_logprobs: int = 20\n     \"\"\"Maximum number of log probabilities to return when `logprobs` is\n     specified in `SamplingParams`. The default value comes the default for the", "patch_sha256": "816340a952dd4e8e"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2524116752", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/mori_prepare_finalize.py\nReview Comment:\nIs this part of code included in CUDAGraph?\n\nOriginal Code Context:\n@@ -0,0 +1,121 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+import mori\n+import torch\n+\n+import vllm.model_executor.layers.fused_moe.modular_kernel as mk\n+from vllm.logger import init_logger\n+from vllm.model_executor.layers.fused_moe.config import FusedMoEQuantConfig\n+from vllm.platforms import current_platform\n+\n+logger = init_logger(__name__)\n+\n+\n+class MoriPrepareAndFinalize(mk.FusedMoEPrepareAndFinalize):\n+    \"\"\"\n+    Prepare/Finalize using MoRI kernels.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        mori_op: mori.ops.EpDispatchCombineOp,\n+        max_tokens_per_rank: int,\n+        num_dispatchers: int,\n+        use_fp8_dispatch: bool = False,\n+    ):\n+        super().__init__()\n+        self.mori_op = mori_op\n+        self.num_dispatchers_ = num_dispatchers\n+        self.max_tokens_per_rank = max_tokens_per_rank\n+        self.use_fp8_dispatch = use_fp8_dispatch\n+\n+    @property\n+    def activation_format(self) -> mk.FusedMoEActivationFormat:\n+        return mk.FusedMoEActivationFormat.Standard\n+\n+    def output_is_reduced(self) -> bool:\n+        return True\n+\n+    def num_dispatchers(self):\n+        return self.num_dispatchers_\n+\n+    def max_num_tokens_per_rank(self) -> int | None:\n+        return self.max_tokens_per_rank\n+\n+    def topk_indices_dtype(self) -> torch.dtype | None:\n+        return torch.int32\n+\n+    def supports_async(self) -> bool:\n+        return False\n+\n+    def prepare(\n+        self,\n+        a1: torch.Tensor,\n+        topk_weights: torch.Tensor,\n+        topk_ids: torch.Tensor,\n+        num_experts: int,\n+        expert_map: torch.Tensor | None,\n+        apply_router_weight_on_input: bool,\n+        quant_config: FusedMoEQuantConfig,\n+    ) -> mk.PrepareResultType:\n+        \"\"\"\n+        Returns a tuple of:\n+        - quantized + dispatched a.\n+        - Optional quantized + dispatched a1_scales.\n+        - Optional ExpertTokensMetadata containing gpu/cpu tensors\n+          as big as the number of local experts with the information about the\n+          number of tokens assigned to each local expert.\n+        - Optional dispatched expert topk IDs\n+        - Optional dispatched expert topk weight\n+        \"\"\"\n+        assert not apply_router_weight_on_input, (\n+            \"mori does not support apply_router_weight_on_input=True now.\"\n+        )\n+        scale = None\n+        if self.use_fp8_dispatch:\n+            from aiter import QuantType, get_hip_quant\n+\n+            if quant_config.is_block_quantized:\n+                quant_func = get_hip_quant(QuantType.per_1x128)\n", "materials": ["Is this part of code included in CUDAGraph?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,121 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+import mori\n+import torch\n+\n+import vllm.model_executor.layers.fused_moe.modular_kernel as mk\n+from vllm.logger import init_logger\n+from vllm.model_executor.layers.fused_moe.config import FusedMoEQuantConfig\n+from vllm.platforms import current_platform\n+\n+logger = init_logger(__name__)\n+\n+\n+class MoriPrepareAndFinalize(mk.FusedMoEPrepareAndFinalize):\n+    \"\"\"\n+    Prepare/Finalize using MoRI kernels.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        mori_op: mori.ops.EpDispatchCombineOp,\n+        max_tokens_per_rank: int,\n+        num_dispatchers: int,\n+        use_fp8_dispatch: bool = False,\n+    ):\n+        super().__init__()\n+        self.mori_op = mori_op\n+        self.num_dispatchers_ = num_dispatchers\n+        self.max_tokens_per_rank = max_tokens_per_rank\n+        self.use_fp8_dispatch = use_fp8_dispatch\n+\n+    @property\n+    def activation_format(self) -> mk.FusedMoEActivationFormat:\n+        return mk.FusedMoEActivationFormat.Standard\n+\n+    def output_is_reduced(self) -> bool:\n+        return True\n+\n+    def num_dispatchers(self):\n+        return self.num_dispatchers_\n+\n+    def max_num_tokens_per_rank(self) -> int | None:\n+        return self.max_tokens_per_rank\n+\n+    def topk_indices_dtype(self) -> torch.dtype | None:\n+        return torch.int32\n+\n+    def supports_async(self) -> bool:\n+        return False\n+\n+    def prepare(\n+        self,\n+        a1: torch.Tensor,\n+        topk_weights: torch.Tensor,\n+        topk_ids: torch.Tensor,\n+        num_experts: int,\n+        expert_map: torch.Tensor | None,\n+        apply_router_weight_on_input: bool,\n+        quant_config: FusedMoEQuantConfig,\n+    ) -> mk.PrepareResultType:\n+        \"\"\"\n+        Returns a tuple of:\n+        - quantized + dispatched a.\n+        - Optional quantized + dispatched a1_scales.\n+        - Optional ExpertTokensMetadata containing gpu/cpu tensors\n+          as big as the number of local experts with the information about the\n+          number of tokens assigned to each local expert.\n+        - Optional dispatched expert topk IDs\n+        - Optional dispatched expert topk weight\n+        \"\"\"\n+        assert not apply_router_weight_on_input, (\n+            \"mori does not support apply_router_weight_on_input=True now.\"\n+        )\n+        scale = None\n+        if self.use_fp8_dispatch:\n+            from aiter import QuantType, get_hip_quant\n+\n+            if quant_config.is_block_quantized:\n+                quant_func = get_hip_quant(QuantType.per_1x128)\n+                a1, scale = quant_func(a1, quant_dtype=current_platform.fp8_dtype())\n+            elif quant_config.is_per_act_token:\n+                quant_func = get_hip_quant(QuantType.per_Token)\n+                a1, scale = quant_func(a1, quant_dtype=current_platform.fp8_dtype())\n+\n+        (\n+            dispatch_a1,\n+            dispatch_weights,\n+            dispatch_scale,\n+            dispatch_ids,\n+            dispatch_recv_token_num,\n+        ) = self.mori_op.dispatch(a1, topk_weights, scale, topk_ids)\n+\n+        expert_tokens_meta = mk.ExpertTokensMetadata(\n+            expert_num_tokens=dispatch_recv_token_num, expert_num_tokens_cpu=None\n+        )\n+\n+        return (\n+            dispatch_a1,\n+            dispatch_scale,\n+            expert_tokens_meta,\n+            dispatch_ids,\n+            dispatch_weights,\n+        )\n+\n+    def finalize(\n+        self,\n+        output: torch.Tensor,\n+        fused_expert_output: torch.Tensor,\n+        topk_weights: torch.Tensor,\n+        topk_ids: torch.Tensor,\n+        apply_router_weight_on_input: bool,\n+        weight_and_reduce_impl: mk.TopKWeightAndReduce,\n+    ) -> None:\n+        num_token = output.shape[0]\n+        result = self.mori_op.combine(\n+            fused_expert_output,\n+            None,\n+            topk_ids,\n+        )[0]\n+        output.copy_(result[:num_token])", "patch_sha256": "884f8cc54826ea00"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2525416004", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/mori_prepare_finalize.py\nReview Comment:\nI’m not sure if I understand your question. Here is to do the FP8 quant before dispatch so that we can reduce communication overhead \n\nOriginal Code Context:\n@@ -0,0 +1,121 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+import mori\n+import torch\n+\n+import vllm.model_executor.layers.fused_moe.modular_kernel as mk\n+from vllm.logger import init_logger\n+from vllm.model_executor.layers.fused_moe.config import FusedMoEQuantConfig\n+from vllm.platforms import current_platform\n+\n+logger = init_logger(__name__)\n+\n+\n+class MoriPrepareAndFinalize(mk.FusedMoEPrepareAndFinalize):\n+    \"\"\"\n+    Prepare/Finalize using MoRI kernels.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        mori_op: mori.ops.EpDispatchCombineOp,\n+        max_tokens_per_rank: int,\n+        num_dispatchers: int,\n+        use_fp8_dispatch: bool = False,\n+    ):\n+        super().__init__()\n+        self.mori_op = mori_op\n+        self.num_dispatchers_ = num_dispatchers\n+        self.max_tokens_per_rank = max_tokens_per_rank\n+        self.use_fp8_dispatch = use_fp8_dispatch\n+\n+    @property\n+    def activation_format(self) -> mk.FusedMoEActivationFormat:\n+        return mk.FusedMoEActivationFormat.Standard\n+\n+    def output_is_reduced(self) -> bool:\n+        return True\n+\n+    def num_dispatchers(self):\n+        return self.num_dispatchers_\n+\n+    def max_num_tokens_per_rank(self) -> int | None:\n+        return self.max_tokens_per_rank\n+\n+    def topk_indices_dtype(self) -> torch.dtype | None:\n+        return torch.int32\n+\n+    def supports_async(self) -> bool:\n+        return False\n+\n+    def prepare(\n+        self,\n+        a1: torch.Tensor,\n+        topk_weights: torch.Tensor,\n+        topk_ids: torch.Tensor,\n+        num_experts: int,\n+        expert_map: torch.Tensor | None,\n+        apply_router_weight_on_input: bool,\n+        quant_config: FusedMoEQuantConfig,\n+    ) -> mk.PrepareResultType:\n+        \"\"\"\n+        Returns a tuple of:\n+        - quantized + dispatched a.\n+        - Optional quantized + dispatched a1_scales.\n+        - Optional ExpertTokensMetadata containing gpu/cpu tensors\n+          as big as the number of local experts with the information about the\n+          number of tokens assigned to each local expert.\n+        - Optional dispatched expert topk IDs\n+        - Optional dispatched expert topk weight\n+        \"\"\"\n+        assert not apply_router_weight_on_input, (\n+            \"mori does not support apply_router_weight_on_input=True now.\"\n+        )\n+        scale = None\n+        if self.use_fp8_dispatch:\n+            from aiter import QuantType, get_hip_quant\n+\n+            if quant_config.is_block_quantized:\n+                quant_func = get_hip_quant(QuantType.per_1x128)\n", "materials": ["I’m not sure if I understand your question. Here is to do the FP8 quant before dispatch so that we can reduce communication overhead "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,121 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+import mori\n+import torch\n+\n+import vllm.model_executor.layers.fused_moe.modular_kernel as mk\n+from vllm.logger import init_logger\n+from vllm.model_executor.layers.fused_moe.config import FusedMoEQuantConfig\n+from vllm.platforms import current_platform\n+\n+logger = init_logger(__name__)\n+\n+\n+class MoriPrepareAndFinalize(mk.FusedMoEPrepareAndFinalize):\n+    \"\"\"\n+    Prepare/Finalize using MoRI kernels.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        mori_op: mori.ops.EpDispatchCombineOp,\n+        max_tokens_per_rank: int,\n+        num_dispatchers: int,\n+        use_fp8_dispatch: bool = False,\n+    ):\n+        super().__init__()\n+        self.mori_op = mori_op\n+        self.num_dispatchers_ = num_dispatchers\n+        self.max_tokens_per_rank = max_tokens_per_rank\n+        self.use_fp8_dispatch = use_fp8_dispatch\n+\n+    @property\n+    def activation_format(self) -> mk.FusedMoEActivationFormat:\n+        return mk.FusedMoEActivationFormat.Standard\n+\n+    def output_is_reduced(self) -> bool:\n+        return True\n+\n+    def num_dispatchers(self):\n+        return self.num_dispatchers_\n+\n+    def max_num_tokens_per_rank(self) -> int | None:\n+        return self.max_tokens_per_rank\n+\n+    def topk_indices_dtype(self) -> torch.dtype | None:\n+        return torch.int32\n+\n+    def supports_async(self) -> bool:\n+        return False\n+\n+    def prepare(\n+        self,\n+        a1: torch.Tensor,\n+        topk_weights: torch.Tensor,\n+        topk_ids: torch.Tensor,\n+        num_experts: int,\n+        expert_map: torch.Tensor | None,\n+        apply_router_weight_on_input: bool,\n+        quant_config: FusedMoEQuantConfig,\n+    ) -> mk.PrepareResultType:\n+        \"\"\"\n+        Returns a tuple of:\n+        - quantized + dispatched a.\n+        - Optional quantized + dispatched a1_scales.\n+        - Optional ExpertTokensMetadata containing gpu/cpu tensors\n+          as big as the number of local experts with the information about the\n+          number of tokens assigned to each local expert.\n+        - Optional dispatched expert topk IDs\n+        - Optional dispatched expert topk weight\n+        \"\"\"\n+        assert not apply_router_weight_on_input, (\n+            \"mori does not support apply_router_weight_on_input=True now.\"\n+        )\n+        scale = None\n+        if self.use_fp8_dispatch:\n+            from aiter import QuantType, get_hip_quant\n+\n+            if quant_config.is_block_quantized:\n+                quant_func = get_hip_quant(QuantType.per_1x128)\n+                a1, scale = quant_func(a1, quant_dtype=current_platform.fp8_dtype())\n+            elif quant_config.is_per_act_token:\n+                quant_func = get_hip_quant(QuantType.per_Token)\n+                a1, scale = quant_func(a1, quant_dtype=current_platform.fp8_dtype())\n+\n+        (\n+            dispatch_a1,\n+            dispatch_weights,\n+            dispatch_scale,\n+            dispatch_ids,\n+            dispatch_recv_token_num,\n+        ) = self.mori_op.dispatch(a1, topk_weights, scale, topk_ids)\n+\n+        expert_tokens_meta = mk.ExpertTokensMetadata(\n+            expert_num_tokens=dispatch_recv_token_num, expert_num_tokens_cpu=None\n+        )\n+\n+        return (\n+            dispatch_a1,\n+            dispatch_scale,\n+            expert_tokens_meta,\n+            dispatch_ids,\n+            dispatch_weights,\n+        )\n+\n+    def finalize(\n+        self,\n+        output: torch.Tensor,\n+        fused_expert_output: torch.Tensor,\n+        topk_weights: torch.Tensor,\n+        topk_ids: torch.Tensor,\n+        apply_router_weight_on_input: bool,\n+        weight_and_reduce_impl: mk.TopKWeightAndReduce,\n+    ) -> None:\n+        num_token = output.shape[0]\n+        result = self.mori_op.combine(\n+            fused_expert_output,\n+            None,\n+            topk_ids,\n+        )[0]\n+        output.copy_(result[:num_token])", "patch_sha256": "884f8cc54826ea00"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2578052092", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/all2all_utils.py\nReview Comment:\nwhat about other models - llama4, gpt-oss, etc.?\n\nOriginal Code Context:\n@@ -167,5 +169,37 @@ def maybe_make_prepare_finalize(\n             physical_to_global=physical_to_global,\n             local_expert_global_ids=local_expert_global_ids,\n         )\n+    elif moe.use_mori_kernels:\n+        assert quant_config is not None\n+        # For PTPC (per token per channel) quant, the scale dim for each token is 1\n+        # For 1x128 quant, the scale dim for each token is hidden_dim // 128\n+        scale_dim = 1 if quant_config.is_per_act_token else moe.hidden_dim // 128\n", "materials": ["what about other models - llama4, gpt-oss, etc.?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -16,7 +16,7 @@\n     FusedMoEPrepareAndFinalize,\n )\n from vllm.platforms import current_platform\n-from vllm.utils.import_utils import has_deep_ep, has_pplx\n+from vllm.utils.import_utils import has_deep_ep, has_mori, has_pplx\n \n if current_platform.is_cuda_alike():\n     if has_pplx():\n@@ -30,6 +30,8 @@\n             DEEPEP_QUANT_BLOCK_SHAPE,\n             DeepEPLLPrepareAndFinalize,\n         )\n+    if has_mori():\n+        from .mori_prepare_finalize import MoriPrepareAndFinalize\n \n \n def maybe_roundup_layer_hidden_size(\n@@ -169,5 +171,36 @@ def maybe_make_prepare_finalize(\n             physical_to_global=physical_to_global,\n             local_expert_global_ids=local_expert_global_ids,\n         )\n+    elif moe.use_mori_kernels:\n+        assert quant_config is not None\n+\n+        # Note: We may want to use FP8 dispatch just to reduce\n+        # data movement.\n+        use_fp8_dispatch = (\n+            quant_config.is_per_act_token or quant_config.is_block_quantized\n+        )\n+        # For PTPC (per token per channel) quant, the scale dim for each token is 1\n+        # For 1x128 quant, the scale dim for each token is hidden_dim // 128\n+        scale_dim = 1 if quant_config.is_per_act_token else moe.hidden_dim // 128\n+        all_to_all_args = dict(\n+            rank=all2all_manager.rank,\n+            num_ep_ranks=all2all_manager.world_size,\n+            quant_dtype=quant_config.quant_dtype,\n+            token_hidden_size=moe.hidden_dim,\n+            scale_dim=scale_dim,\n+            scale_type_size=torch.float32.itemsize,\n+            max_num_tokens_per_dp_rank=moe.max_num_tokens,\n+            input_dtype=moe.in_dtype,\n+            num_local_experts=moe.num_experts // all2all_manager.world_size,\n+            num_experts_per_token=moe.experts_per_token,\n+        )\n+        handle = all2all_manager.get_handle(all_to_all_args)\n+\n+        prepare_finalize = MoriPrepareAndFinalize(\n+            handle,\n+            max_tokens_per_rank=moe.max_num_tokens,\n+            num_dispatchers=all2all_manager.world_size,\n+            use_fp8_dispatch=use_fp8_dispatch,\n+        )\n \n     return prepare_finalize", "patch_sha256": "cff7bf62ae2accc5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2578099858", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/mori_prepare_finalize.py\nReview Comment:\nPlease double check it works for both gfx942 and gfx950\n\nOriginal Code Context:\n@@ -0,0 +1,121 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+import mori\n+import torch\n+\n+import vllm.model_executor.layers.fused_moe.modular_kernel as mk\n+from vllm.logger import init_logger\n+from vllm.model_executor.layers.fused_moe.config import FusedMoEQuantConfig\n+from vllm.platforms import current_platform\n+\n+logger = init_logger(__name__)\n+\n+\n+class MoriPrepareAndFinalize(mk.FusedMoEPrepareAndFinalize):\n+    \"\"\"\n+    Prepare/Finalize using MoRI kernels.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        mori_op: mori.ops.EpDispatchCombineOp,\n+        max_tokens_per_rank: int,\n+        num_dispatchers: int,\n+        use_fp8_dispatch: bool = False,\n+    ):\n+        super().__init__()\n+        self.mori_op = mori_op\n+        self.num_dispatchers_ = num_dispatchers\n+        self.max_tokens_per_rank = max_tokens_per_rank\n+        self.use_fp8_dispatch = use_fp8_dispatch\n+\n+    @property\n+    def activation_format(self) -> mk.FusedMoEActivationFormat:\n+        return mk.FusedMoEActivationFormat.Standard\n+\n+    def output_is_reduced(self) -> bool:\n+        return True\n+\n+    def num_dispatchers(self):\n+        return self.num_dispatchers_\n+\n+    def max_num_tokens_per_rank(self) -> int | None:\n+        return self.max_tokens_per_rank\n+\n+    def topk_indices_dtype(self) -> torch.dtype | None:\n+        return torch.int32\n+\n+    def supports_async(self) -> bool:\n+        return False\n+\n+    def prepare(\n+        self,\n+        a1: torch.Tensor,\n+        topk_weights: torch.Tensor,\n+        topk_ids: torch.Tensor,\n+        num_experts: int,\n+        expert_map: torch.Tensor | None,\n+        apply_router_weight_on_input: bool,\n+        quant_config: FusedMoEQuantConfig,\n+    ) -> mk.PrepareResultType:\n+        \"\"\"\n+        Returns a tuple of:\n+        - quantized + dispatched a.\n+        - Optional quantized + dispatched a1_scales.\n+        - Optional ExpertTokensMetadata containing gpu/cpu tensors\n+          as big as the number of local experts with the information about the\n+          number of tokens assigned to each local expert.\n+        - Optional dispatched expert topk IDs\n+        - Optional dispatched expert topk weight\n+        \"\"\"\n+        assert not apply_router_weight_on_input, (\n+            \"mori does not support apply_router_weight_on_input=True now.\"\n+        )\n+        scale = None\n+        if self.use_fp8_dispatch:\n+            from aiter import QuantType, get_hip_quant\n+\n+            if quant_config.is_block_quantized:\n+                quant_func = get_hip_quant(QuantType.per_1x128)\n+                a1, scale = quant_func(a1, quant_dtype=current_platform.fp8_dtype())\n", "materials": ["Please double check it works for both gfx942 and gfx950"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,121 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+import mori\n+import torch\n+\n+import vllm.model_executor.layers.fused_moe.modular_kernel as mk\n+from vllm.logger import init_logger\n+from vllm.model_executor.layers.fused_moe.config import FusedMoEQuantConfig\n+from vllm.platforms import current_platform\n+\n+logger = init_logger(__name__)\n+\n+\n+class MoriPrepareAndFinalize(mk.FusedMoEPrepareAndFinalize):\n+    \"\"\"\n+    Prepare/Finalize using MoRI kernels.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        mori_op: mori.ops.EpDispatchCombineOp,\n+        max_tokens_per_rank: int,\n+        num_dispatchers: int,\n+        use_fp8_dispatch: bool = False,\n+    ):\n+        super().__init__()\n+        self.mori_op = mori_op\n+        self.num_dispatchers_ = num_dispatchers\n+        self.max_tokens_per_rank = max_tokens_per_rank\n+        self.use_fp8_dispatch = use_fp8_dispatch\n+\n+    @property\n+    def activation_format(self) -> mk.FusedMoEActivationFormat:\n+        return mk.FusedMoEActivationFormat.Standard\n+\n+    def output_is_reduced(self) -> bool:\n+        return True\n+\n+    def num_dispatchers(self):\n+        return self.num_dispatchers_\n+\n+    def max_num_tokens_per_rank(self) -> int | None:\n+        return self.max_tokens_per_rank\n+\n+    def topk_indices_dtype(self) -> torch.dtype | None:\n+        return torch.int32\n+\n+    def supports_async(self) -> bool:\n+        return False\n+\n+    def prepare(\n+        self,\n+        a1: torch.Tensor,\n+        topk_weights: torch.Tensor,\n+        topk_ids: torch.Tensor,\n+        num_experts: int,\n+        expert_map: torch.Tensor | None,\n+        apply_router_weight_on_input: bool,\n+        quant_config: FusedMoEQuantConfig,\n+    ) -> mk.PrepareResultType:\n+        \"\"\"\n+        Returns a tuple of:\n+        - quantized + dispatched a.\n+        - Optional quantized + dispatched a1_scales.\n+        - Optional ExpertTokensMetadata containing gpu/cpu tensors\n+          as big as the number of local experts with the information about the\n+          number of tokens assigned to each local expert.\n+        - Optional dispatched expert topk IDs\n+        - Optional dispatched expert topk weight\n+        \"\"\"\n+        assert not apply_router_weight_on_input, (\n+            \"mori does not support apply_router_weight_on_input=True now.\"\n+        )\n+        scale = None\n+        if self.use_fp8_dispatch:\n+            from aiter import QuantType, get_hip_quant\n+\n+            if quant_config.is_block_quantized:\n+                quant_func = get_hip_quant(QuantType.per_1x128)\n+                a1, scale = quant_func(a1, quant_dtype=current_platform.fp8_dtype())\n+            elif quant_config.is_per_act_token:\n+                quant_func = get_hip_quant(QuantType.per_Token)\n+                a1, scale = quant_func(a1, quant_dtype=current_platform.fp8_dtype())\n+\n+        (\n+            dispatch_a1,\n+            dispatch_weights,\n+            dispatch_scale,\n+            dispatch_ids,\n+            dispatch_recv_token_num,\n+        ) = self.mori_op.dispatch(a1, topk_weights, scale, topk_ids)\n+\n+        expert_tokens_meta = mk.ExpertTokensMetadata(\n+            expert_num_tokens=dispatch_recv_token_num, expert_num_tokens_cpu=None\n+        )\n+\n+        return (\n+            dispatch_a1,\n+            dispatch_scale,\n+            expert_tokens_meta,\n+            dispatch_ids,\n+            dispatch_weights,\n+        )\n+\n+    def finalize(\n+        self,\n+        output: torch.Tensor,\n+        fused_expert_output: torch.Tensor,\n+        topk_weights: torch.Tensor,\n+        topk_ids: torch.Tensor,\n+        apply_router_weight_on_input: bool,\n+        weight_and_reduce_impl: mk.TopKWeightAndReduce,\n+    ) -> None:\n+        num_token = output.shape[0]\n+        result = self.mori_op.combine(\n+            fused_expert_output,\n+            None,\n+            topk_ids,\n+        )[0]\n+        output.copy_(result[:num_token])", "patch_sha256": "884f8cc54826ea00"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2581005298", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/all2all_utils.py\nReview Comment:\nNot supported yet\n\nOriginal Code Context:\n@@ -167,5 +169,37 @@ def maybe_make_prepare_finalize(\n             physical_to_global=physical_to_global,\n             local_expert_global_ids=local_expert_global_ids,\n         )\n+    elif moe.use_mori_kernels:\n+        assert quant_config is not None\n+        # For PTPC (per token per channel) quant, the scale dim for each token is 1\n+        # For 1x128 quant, the scale dim for each token is hidden_dim // 128\n+        scale_dim = 1 if quant_config.is_per_act_token else moe.hidden_dim // 128\n", "materials": ["Not supported yet"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -16,7 +16,7 @@\n     FusedMoEPrepareAndFinalize,\n )\n from vllm.platforms import current_platform\n-from vllm.utils.import_utils import has_deep_ep, has_pplx\n+from vllm.utils.import_utils import has_deep_ep, has_mori, has_pplx\n \n if current_platform.is_cuda_alike():\n     if has_pplx():\n@@ -30,6 +30,8 @@\n             DEEPEP_QUANT_BLOCK_SHAPE,\n             DeepEPLLPrepareAndFinalize,\n         )\n+    if has_mori():\n+        from .mori_prepare_finalize import MoriPrepareAndFinalize\n \n \n def maybe_roundup_layer_hidden_size(\n@@ -169,5 +171,36 @@ def maybe_make_prepare_finalize(\n             physical_to_global=physical_to_global,\n             local_expert_global_ids=local_expert_global_ids,\n         )\n+    elif moe.use_mori_kernels:\n+        assert quant_config is not None\n+\n+        # Note: We may want to use FP8 dispatch just to reduce\n+        # data movement.\n+        use_fp8_dispatch = (\n+            quant_config.is_per_act_token or quant_config.is_block_quantized\n+        )\n+        # For PTPC (per token per channel) quant, the scale dim for each token is 1\n+        # For 1x128 quant, the scale dim for each token is hidden_dim // 128\n+        scale_dim = 1 if quant_config.is_per_act_token else moe.hidden_dim // 128\n+        all_to_all_args = dict(\n+            rank=all2all_manager.rank,\n+            num_ep_ranks=all2all_manager.world_size,\n+            quant_dtype=quant_config.quant_dtype,\n+            token_hidden_size=moe.hidden_dim,\n+            scale_dim=scale_dim,\n+            scale_type_size=torch.float32.itemsize,\n+            max_num_tokens_per_dp_rank=moe.max_num_tokens,\n+            input_dtype=moe.in_dtype,\n+            num_local_experts=moe.num_experts // all2all_manager.world_size,\n+            num_experts_per_token=moe.experts_per_token,\n+        )\n+        handle = all2all_manager.get_handle(all_to_all_args)\n+\n+        prepare_finalize = MoriPrepareAndFinalize(\n+            handle,\n+            max_tokens_per_rank=moe.max_num_tokens,\n+            num_dispatchers=all2all_manager.world_size,\n+            use_fp8_dispatch=use_fp8_dispatch,\n+        )\n \n     return prepare_finalize", "patch_sha256": "cff7bf62ae2accc5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2584351229", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/config.py\nReview Comment:\nIt’s used to determine the number of GPUs per node in `vllm/model_executor/layers/fused_moe/all2all_utils.py` within the `maybe_make_prepare_finalize()` function, without introducing `ParallelConfig`.\n\nOriginal Code Context:\n@@ -836,6 +841,7 @@ def make(\n                 ep_rank=0,\n                 use_ep=False,\n                 all2all_backend=vllm_parallel_config.all2all_backend,\n+                local_ep_size=1,\n             )\n         # DP + EP / TP + EP / DP + TP + EP\n         assert use_ep\n", "materials": ["It’s used to determine the number of GPUs per node in `vllm/model_executor/layers/fused_moe/all2all_utils.py` within the `maybe_make_prepare_finalize()` function, without introducing `ParallelConfig`."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -893,6 +893,10 @@ def use_naive_all2all_kernels(self):\n             self.all2all_backend in [\"naive\", \"allgather_reducescatter\"]\n         )\n \n+    @property\n+    def use_mori_kernels(self):\n+        return self.use_all2all_kernels and self.all2all_backend == \"mori\"\n+\n     @staticmethod\n     def flatten_tp_across_dp_and_pcp(\n         tp_size: int, dp_size: int, dp_rank: int, pcp_size: int, pcp_rank: int\n@@ -1136,6 +1140,10 @@ def use_deepep_ht_kernels(self):\n     def use_deepep_ll_kernels(self):\n         return self.moe_parallel_config.use_deepep_ll_kernels\n \n+    @property\n+    def use_mori_kernels(self):\n+        return self.moe_parallel_config.use_mori_kernels\n+\n     @property\n     def use_flashinfer_cutlass_kernels(self):\n         \"\"\"", "patch_sha256": "40354643dad63e0f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28973/review_2585743381", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28973", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu_model_runner.py\nReview Comment:\nwe should probably also throw here somehow if we're not in a streaming regime no? \n\nOriginal Code Context:\n@@ -994,6 +1001,39 @@ def _update_states_after_model_execute(\n         for i, num_tokens in enumerate(num_accepted_tokens):\n             self.input_batch.num_accepted_tokens_cpu[i] = num_tokens\n \n+    def _update_streaming_request(\n+        self, req_id: str, new_req_data: NewRequestData\n+    ) -> CachedRequestState:\n+        \"\"\"Updates streaming session request from `scheduled_new_reqs`.\n", "materials": ["we should probably also throw here somehow if we're not in a streaming regime no? "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -110,6 +110,7 @@\n     get_dcp_local_seq_lens,\n     reorder_batch_to_split_decodes_and_prefills,\n )\n+from vllm.v1.core.sched.output import NewRequestData\n from vllm.v1.cudagraph_dispatcher import CudagraphDispatcher\n from vllm.v1.kv_cache_interface import (\n     AttentionSpec,\n@@ -896,6 +897,12 @@ def _update_states(self, scheduler_output: \"SchedulerOutput\") -> None:\n         # Add new requests to the cached states.\n         for new_req_data in scheduler_output.scheduled_new_reqs:\n             req_id = new_req_data.req_id\n+            if req_id in self.requests:\n+                # For streaming case only.\n+                req_state = self._update_streaming_request(req_id, new_req_data)\n+                reqs_to_add.append(req_state)\n+                continue\n+\n             sampling_params = new_req_data.sampling_params\n             pooling_params = new_req_data.pooling_params\n \n@@ -1126,6 +1133,40 @@ def _update_states_after_model_execute(\n                 self.model.get_mamba_state_copy_func(),\n             )\n \n+    def _update_streaming_request(\n+        self, req_id: str, new_req_data: NewRequestData\n+    ) -> CachedRequestState:\n+        \"\"\"Updates streaming session request from `scheduled_new_reqs`.\n+\n+        Removes the request from InputBatch (if present), updates the cached\n+        state, and prepares it for re-addition to the batch.\n+\n+        NOTE: prompt_token_ids includes intermediate output tokens - tokens\n+        previously generated but now are input context (part of the prompt).\n+        \"\"\"\n+        self.input_batch.remove_request(req_id)\n+        req_state = self.requests[req_id]\n+\n+        req_state.prompt_token_ids = new_req_data.prompt_token_ids\n+        req_state.mm_features = new_req_data.mm_features\n+        req_state.prompt_embeds = new_req_data.prompt_embeds\n+        req_state.sampling_params = new_req_data.sampling_params\n+        req_state.pooling_params = new_req_data.pooling_params\n+        req_state.block_ids = new_req_data.block_ids\n+        req_state.num_computed_tokens = new_req_data.num_computed_tokens\n+        req_state.num_prompt_tokens = length_from_prompt_token_ids_or_embeds(\n+            req_state.prompt_token_ids, req_state.prompt_embeds\n+        )\n+\n+        # Clear `output_token_ids` as previous output tokens are now part of\n+        # `prompt_token_ids`.\n+        req_state.output_token_ids.clear()\n+\n+        if self.uses_mrope:\n+            self._init_mrope_positions(req_state)\n+\n+        return req_state\n+\n     def _init_mrope_positions(self, req_state: CachedRequestState):\n         model = self.get_model()\n         assert supports_mrope(model), \"M-RoPE support is not implemented.\"", "patch_sha256": "a386339c4614f4d5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28973/review_2586390977", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28973", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu_model_runner.py\nReview Comment:\nthis function is only called for streaming scenarios. the only way to check in the model runner is if for new scheduled requests, if the request already exists in model_runner.requests\n\nOriginal Code Context:\n@@ -994,6 +1001,39 @@ def _update_states_after_model_execute(\n         for i, num_tokens in enumerate(num_accepted_tokens):\n             self.input_batch.num_accepted_tokens_cpu[i] = num_tokens\n \n+    def _update_streaming_request(\n+        self, req_id: str, new_req_data: NewRequestData\n+    ) -> CachedRequestState:\n+        \"\"\"Updates streaming session request from `scheduled_new_reqs`.\n", "materials": ["this function is only called for streaming scenarios. the only way to check in the model runner is if for new scheduled requests, if the request already exists in model_runner.requests"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -110,6 +110,7 @@\n     get_dcp_local_seq_lens,\n     reorder_batch_to_split_decodes_and_prefills,\n )\n+from vllm.v1.core.sched.output import NewRequestData\n from vllm.v1.cudagraph_dispatcher import CudagraphDispatcher\n from vllm.v1.kv_cache_interface import (\n     AttentionSpec,\n@@ -896,6 +897,12 @@ def _update_states(self, scheduler_output: \"SchedulerOutput\") -> None:\n         # Add new requests to the cached states.\n         for new_req_data in scheduler_output.scheduled_new_reqs:\n             req_id = new_req_data.req_id\n+            if req_id in self.requests:\n+                # For streaming case only.\n+                req_state = self._update_streaming_request(req_id, new_req_data)\n+                reqs_to_add.append(req_state)\n+                continue\n+\n             sampling_params = new_req_data.sampling_params\n             pooling_params = new_req_data.pooling_params\n \n@@ -1126,6 +1133,40 @@ def _update_states_after_model_execute(\n                 self.model.get_mamba_state_copy_func(),\n             )\n \n+    def _update_streaming_request(\n+        self, req_id: str, new_req_data: NewRequestData\n+    ) -> CachedRequestState:\n+        \"\"\"Updates streaming session request from `scheduled_new_reqs`.\n+\n+        Removes the request from InputBatch (if present), updates the cached\n+        state, and prepares it for re-addition to the batch.\n+\n+        NOTE: prompt_token_ids includes intermediate output tokens - tokens\n+        previously generated but now are input context (part of the prompt).\n+        \"\"\"\n+        self.input_batch.remove_request(req_id)\n+        req_state = self.requests[req_id]\n+\n+        req_state.prompt_token_ids = new_req_data.prompt_token_ids\n+        req_state.mm_features = new_req_data.mm_features\n+        req_state.prompt_embeds = new_req_data.prompt_embeds\n+        req_state.sampling_params = new_req_data.sampling_params\n+        req_state.pooling_params = new_req_data.pooling_params\n+        req_state.block_ids = new_req_data.block_ids\n+        req_state.num_computed_tokens = new_req_data.num_computed_tokens\n+        req_state.num_prompt_tokens = length_from_prompt_token_ids_or_embeds(\n+            req_state.prompt_token_ids, req_state.prompt_embeds\n+        )\n+\n+        # Clear `output_token_ids` as previous output tokens are now part of\n+        # `prompt_token_ids`.\n+        req_state.output_token_ids.clear()\n+\n+        if self.uses_mrope:\n+            self._init_mrope_positions(req_state)\n+\n+        return req_state\n+\n     def _init_mrope_positions(self, req_state: CachedRequestState):\n         model = self.get_model()\n         assert supports_mrope(model), \"M-RoPE support is not implemented.\"", "patch_sha256": "a386339c4614f4d5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2631071743", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\n**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Honor hidden_size padding for non-gpt_oss MoE**\n\nThe padding result from `maybe_roundup_hidden_size` is only applied when `is_rounded_hidden_size` is true, yet that flag is set only for gpt_oss+mxfp4 in the helper (lines 250‑269). When other backends such as DeepEP round the hidden size inside `maybe_roundup_layer_hidden_size`, `is_rounded_hidden_size` stays false, so `self.hidden_size`/`self.moe_config` remain at the unpadded size while weights are built with the padded `hidden_size` later in `__init__`. DeepEP runs will then have layer metadata smaller than the actual weight shapes, leading to buffer shape mismatches at runtime.\n\nUseful? React with 👍 / 👎.\n\nOriginal Code Context:\n@@ -562,6 +556,36 @@ def _get_quant_method() -> FusedMoEMethodBase:\n         # for heuristic purposes, so it must be initialized first.\n         self.quant_method: FusedMoEMethodBase = _get_quant_method()\n \n+        self.model_type = getattr(self.vllm_config.model_config.hf_config, \"model_type\", None)\n+        self.is_mxfp4_quant, self.emulate_quant = self._is_mxfp4_and_emulate_quant(quant_config, self.quant_method)\n+\n+        # Round up hidden size if needed.\n+        hidden_size, is_rounded_hidden_size = maybe_roundup_hidden_size(\n+            hidden_size,\n+            moe_in_dtype,\n+            self.moe_parallel_config,\n+            self.model_type,\n+            self.is_mxfp4_quant,\n+            self.emulate_quant,\n+            is_lora_enabled=self.vllm_config.lora_config is not None,\n+        )\n+        print(f\"is_rounded_hidden_size is {is_rounded_hidden_size}\")\n+\n+        if is_rounded_hidden_size:\n+            self.hidden_size = hidden_size\n+            self.moe_config: FusedMoEConfig = FusedMoEConfig(\n", "materials": ["**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Honor hidden_size padding for non-gpt_oss MoE**\n\nThe padding result from `maybe_roundup_hidden_size` is only applied when `is_rounded_hidden_size` is true, yet that flag is set only for gpt_oss+mxfp4 in the helper (lines 250‑269). When other backends such as DeepEP round the hidden size inside `maybe_roundup_layer_hidden_size`, `is_rounded_hidden_size` stays false, so `self.hidden_size`/`self.moe_config` remain at the unpadded size while weights are built with the padded `hidden_size` later in `__init__`. DeepEP runs will then have layer metadata smaller than the actual weight shapes, leading to buffer shape mismatches at runtime.\n\nUseful? React with 👍 / 👎."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2631838571", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/utils/ocp_mx_utils.py\nReview Comment:\nfp8 is ambiguous - could you use instead `mxfp8_e5m2` and `mxfp8_e4m3`?\r\n\r\nhttps://www.opencompute.org/documents/ocp-microscaling-formats-mx-v1-0-spec-final-pdf\n\nOriginal Code Context:\n@@ -20,26 +20,38 @@\n \n \n class OCP_MX_Scheme(str, Enum):\n+    w_mxfp4 = \"w_mxfp4\"\n     w_mxfp4_a_mxfp4 = \"w_mxfp4_a_mxfp4\"\n     w_mxfp4_a_mxfp6_e3m2 = \"w_mxfp4_a_mxfp6_e3m2\"\n     w_mxfp4_a_mxfp6_e2m3 = \"w_mxfp4_a_mxfp6_e2m3\"\n+    w_mxfp4_a_fp8 = \"w_mxfp4_a_fp8\"\n", "materials": ["fp8 is ambiguous - could you use instead `mxfp8_e5m2` and `mxfp8_e4m3`?\r\n\r\nhttps://www.opencompute.org/documents/ocp-microscaling-formats-mx-v1-0-spec-final-pdf"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -20,26 +20,44 @@\n \n \n class OCP_MX_Scheme(str, Enum):\n+    w_mxfp4 = \"w_mxfp4\"\n     w_mxfp4_a_mxfp4 = \"w_mxfp4_a_mxfp4\"\n     w_mxfp4_a_mxfp6_e3m2 = \"w_mxfp4_a_mxfp6_e3m2\"\n     w_mxfp4_a_mxfp6_e2m3 = \"w_mxfp4_a_mxfp6_e2m3\"\n+    w_mxfp4_a_fp8 = \"w_mxfp4_a_fp8\"\n+    w_mxfp6_e3m2 = \"w_mxfp6_e3m2\"\n     w_mxfp6_e3m2_a_mxfp6_e3m2 = \"w_mxfp6_e3m2_a_mxfp6_e3m2\"\n+    w_mxfp6_e3m2_a_fp8 = \"w_mxfp6_e3m2_a_fp8\"\n+    w_mxfp6_e2m3 = \"w_mxfp6_e2m3\"\n     w_mxfp6_e2m3_a_mxfp6_e2m3 = \"w_mxfp6_e2m3_a_mxfp6_e2m3\"\n+    w_mxfp6_e2m3_a_fp8 = \"w_mxfp6_e2m3_a_fp8\"\n \n     @classmethod\n     def from_quant_dtype(cls, input_dtype: str | None, weight_dtype: str | None):\n-        if input_dtype not in OCP_MX_DTYPES or weight_dtype not in OCP_MX_DTYPES:\n+        if input_dtype not in OCP_MX_DTYPES and weight_dtype not in OCP_MX_DTYPES:\n             return None\n+        elif input_dtype is None and weight_dtype == \"mxfp4\":\n+            return cls.w_mxfp4\n+        elif input_dtype is None and weight_dtype == \"mxfp6_e3m2\":\n+            return cls.w_mxfp6_e3m2\n+        elif input_dtype is None and weight_dtype == \"mxfp6_e2m3\":\n+            return cls.w_mxfp6_e2m3\n         elif input_dtype == \"mxfp4\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp4\n         elif input_dtype == \"mxfp6_e3m2\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp6_e3m2\n         elif input_dtype == \"mxfp6_e2m3\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp6_e2m3\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp4\":\n+            return cls.w_mxfp4_a_fp8\n         elif input_dtype == \"mxfp6_e3m2\" and weight_dtype == \"mxfp6_e3m2\":\n             return cls.w_mxfp6_e3m2_a_mxfp6_e3m2\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp6_e3m2\":\n+            return cls.w_mxfp6_e3m2_a_fp8\n         elif input_dtype == \"mxfp6_e2m3\" and weight_dtype == \"mxfp6_e2m3\":\n             return cls.w_mxfp6_e2m3_a_mxfp6_e2m3\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp6_e2m3\":\n+            return cls.w_mxfp6_e2m3_a_fp8\n         else:\n             logger.warning(\n                 \"input_dtype='%s' and\"", "patch_sha256": "43f3fe8f1fd07fb8"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2631847763", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nThis should be handled in `moe_kernel_quantize_input` no?\n\nOriginal Code Context:\n@@ -1881,10 +1877,28 @@ def fused_experts_impl(\n \n         curr_topk_ids = topk_ids[begin_chunk_idx:end_chunk_idx]\n         curr_topk_weights = topk_weights[begin_chunk_idx:end_chunk_idx]\n+\n+        if ocp_mx_scheme is not None:\n+            if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+                pass\n+            elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+                # perform QDQ (quantize and dequantize) on activation for emulation purpose, \n+                # because of no native kernel for weight in ocp_mx_scheme and activation in FP8.\n+                qcurr_hidden_states, a1q_scale = ops.scaled_fp8_quant(\n+                    curr_hidden_states, a1_scale, use_per_token_if_dynamic=False\n+                )\n+                curr_hidden_states = per_tensor_dequantize(\n+                    qcurr_hidden_states, a1q_scale\n+                ).to(curr_hidden_states.dtype)\n+                input_quant_dtype = None\n", "materials": ["This should be handled in `moe_kernel_quantize_input` no?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2631848733", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nsame comment, if we can avoid duplicating/adding code here (specific to quark)\n\nOriginal Code Context:\n@@ -1941,10 +1955,27 @@ def fused_experts_impl(\n         else:\n             raise ValueError(f\"Unsupported FusedMoe activation: {activation}.\")\n \n+        if ocp_mx_scheme is not None:\n+            if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+                pass\n+            elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+                # perform QDQ (quantize and dequantize) on activation for emulation purpose,\n+                # because of no native kernel for weight in ocp_mx_scheme and activation in FP8.\n+                qintermediate_cache2, a2q_scale = ops.scaled_fp8_quant(\n+                    intermediate_cache2, a2_scale, use_per_token_if_dynamic=False\n+                )\n+                intermediate_cache2 = per_tensor_dequantize(\n+                    qintermediate_cache2, a2q_scale\n+                ).to(intermediate_cache2.dtype)\n+                input_quant_dtype = None\n+            else:\n+                # TODO: @felix for ocp_mx_scheme.endswith(\"fp6_e3m2\"), ocp_mx_scheme.endswith(\"fp6_e2m3\")\n+                raise NotImplementedError(f\"Unsupported ocp_mx_scheme={ocp_mx_scheme}\")\n", "materials": ["same comment, if we can avoid duplicating/adding code here (specific to quark)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2631865618", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nwhy this change?\n\nOriginal Code Context:\n@@ -246,7 +248,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant and not emulate_quant:\n", "materials": ["why this change?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2631885609", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nSee https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/quantization/utils/mxfp6_utils.py\n\nOriginal Code Context:\n@@ -1941,10 +1955,27 @@ def fused_experts_impl(\n         else:\n             raise ValueError(f\"Unsupported FusedMoe activation: {activation}.\")\n \n+        if ocp_mx_scheme is not None:\n+            if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+                pass\n+            elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+                # perform QDQ (quantize and dequantize) on activation for emulation purpose,\n+                # because of no native kernel for weight in ocp_mx_scheme and activation in FP8.\n+                qintermediate_cache2, a2q_scale = ops.scaled_fp8_quant(\n+                    intermediate_cache2, a2_scale, use_per_token_if_dynamic=False\n+                )\n+                intermediate_cache2 = per_tensor_dequantize(\n+                    qintermediate_cache2, a2q_scale\n+                ).to(intermediate_cache2.dtype)\n+                input_quant_dtype = None\n+            else:\n+                # TODO: @felix for ocp_mx_scheme.endswith(\"fp6_e3m2\"), ocp_mx_scheme.endswith(\"fp6_e2m3\")\n+                raise NotImplementedError(f\"Unsupported ocp_mx_scheme={ocp_mx_scheme}\")\n", "materials": ["See https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/quantization/utils/mxfp6_utils.py"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2631917218", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nSee the (rightful) pushback for adding more quantizer-specific code in `fused_moe.py` here https://github.com/vllm-project/vllm/issues/30621\n\nOriginal Code Context:\n@@ -1881,10 +1877,28 @@ def fused_experts_impl(\n \n         curr_topk_ids = topk_ids[begin_chunk_idx:end_chunk_idx]\n         curr_topk_weights = topk_weights[begin_chunk_idx:end_chunk_idx]\n+\n+        if ocp_mx_scheme is not None:\n+            if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+                pass\n+            elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+                # perform QDQ (quantize and dequantize) on activation for emulation purpose, \n+                # because of no native kernel for weight in ocp_mx_scheme and activation in FP8.\n+                qcurr_hidden_states, a1q_scale = ops.scaled_fp8_quant(\n+                    curr_hidden_states, a1_scale, use_per_token_if_dynamic=False\n+                )\n+                curr_hidden_states = per_tensor_dequantize(\n+                    qcurr_hidden_states, a1q_scale\n+                ).to(curr_hidden_states.dtype)\n+                input_quant_dtype = None\n", "materials": ["See the (rightful) pushback for adding more quantizer-specific code in `fused_moe.py` here https://github.com/vllm-project/vllm/issues/30621"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2634164003", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/utils/ocp_mx_utils.py\nReview Comment:\nIt's doable. Do you know if `mxfp4-mxfp8` native kernel is already provided or will be provided in a future?\r\nROCm has `mxfp4-fp8` use case, as the motivation to add the scheme for emulation at current moment.\n\nOriginal Code Context:\n@@ -20,26 +20,38 @@\n \n \n class OCP_MX_Scheme(str, Enum):\n+    w_mxfp4 = \"w_mxfp4\"\n     w_mxfp4_a_mxfp4 = \"w_mxfp4_a_mxfp4\"\n     w_mxfp4_a_mxfp6_e3m2 = \"w_mxfp4_a_mxfp6_e3m2\"\n     w_mxfp4_a_mxfp6_e2m3 = \"w_mxfp4_a_mxfp6_e2m3\"\n+    w_mxfp4_a_fp8 = \"w_mxfp4_a_fp8\"\n", "materials": ["It's doable. Do you know if `mxfp4-mxfp8` native kernel is already provided or will be provided in a future?\r\nROCm has `mxfp4-fp8` use case, as the motivation to add the scheme for emulation at current moment."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -20,26 +20,44 @@\n \n \n class OCP_MX_Scheme(str, Enum):\n+    w_mxfp4 = \"w_mxfp4\"\n     w_mxfp4_a_mxfp4 = \"w_mxfp4_a_mxfp4\"\n     w_mxfp4_a_mxfp6_e3m2 = \"w_mxfp4_a_mxfp6_e3m2\"\n     w_mxfp4_a_mxfp6_e2m3 = \"w_mxfp4_a_mxfp6_e2m3\"\n+    w_mxfp4_a_fp8 = \"w_mxfp4_a_fp8\"\n+    w_mxfp6_e3m2 = \"w_mxfp6_e3m2\"\n     w_mxfp6_e3m2_a_mxfp6_e3m2 = \"w_mxfp6_e3m2_a_mxfp6_e3m2\"\n+    w_mxfp6_e3m2_a_fp8 = \"w_mxfp6_e3m2_a_fp8\"\n+    w_mxfp6_e2m3 = \"w_mxfp6_e2m3\"\n     w_mxfp6_e2m3_a_mxfp6_e2m3 = \"w_mxfp6_e2m3_a_mxfp6_e2m3\"\n+    w_mxfp6_e2m3_a_fp8 = \"w_mxfp6_e2m3_a_fp8\"\n \n     @classmethod\n     def from_quant_dtype(cls, input_dtype: str | None, weight_dtype: str | None):\n-        if input_dtype not in OCP_MX_DTYPES or weight_dtype not in OCP_MX_DTYPES:\n+        if input_dtype not in OCP_MX_DTYPES and weight_dtype not in OCP_MX_DTYPES:\n             return None\n+        elif input_dtype is None and weight_dtype == \"mxfp4\":\n+            return cls.w_mxfp4\n+        elif input_dtype is None and weight_dtype == \"mxfp6_e3m2\":\n+            return cls.w_mxfp6_e3m2\n+        elif input_dtype is None and weight_dtype == \"mxfp6_e2m3\":\n+            return cls.w_mxfp6_e2m3\n         elif input_dtype == \"mxfp4\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp4\n         elif input_dtype == \"mxfp6_e3m2\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp6_e3m2\n         elif input_dtype == \"mxfp6_e2m3\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp6_e2m3\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp4\":\n+            return cls.w_mxfp4_a_fp8\n         elif input_dtype == \"mxfp6_e3m2\" and weight_dtype == \"mxfp6_e3m2\":\n             return cls.w_mxfp6_e3m2_a_mxfp6_e3m2\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp6_e3m2\":\n+            return cls.w_mxfp6_e3m2_a_fp8\n         elif input_dtype == \"mxfp6_e2m3\" and weight_dtype == \"mxfp6_e2m3\":\n             return cls.w_mxfp6_e2m3_a_mxfp6_e2m3\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp6_e2m3\":\n+            return cls.w_mxfp6_e2m3_a_fp8\n         else:\n             logger.warning(\n                 \"input_dtype='%s' and\"", "patch_sha256": "43f3fe8f1fd07fb8"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2634169500", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/utils/ocp_mx_utils.py\nReview Comment:\nIt's \"and\". Let me remove the comment.\n\nOriginal Code Context:\n@@ -20,26 +20,38 @@\n \n \n class OCP_MX_Scheme(str, Enum):\n+    w_mxfp4 = \"w_mxfp4\"\n     w_mxfp4_a_mxfp4 = \"w_mxfp4_a_mxfp4\"\n     w_mxfp4_a_mxfp6_e3m2 = \"w_mxfp4_a_mxfp6_e3m2\"\n     w_mxfp4_a_mxfp6_e2m3 = \"w_mxfp4_a_mxfp6_e2m3\"\n+    w_mxfp4_a_fp8 = \"w_mxfp4_a_fp8\"\n     w_mxfp6_e3m2_a_mxfp6_e3m2 = \"w_mxfp6_e3m2_a_mxfp6_e3m2\"\n+    w_mxfp6_e3m2_a_fp8 = \"w_mxfp6_e3m2_a_fp8\"\n     w_mxfp6_e2m3_a_mxfp6_e2m3 = \"w_mxfp6_e2m3_a_mxfp6_e2m3\"\n+    w_mxfp6_e2m3_a_fp8 = \"w_mxfp6_e2m3_a_fp8\"\n \n     @classmethod\n     def from_quant_dtype(cls, input_dtype: str | None, weight_dtype: str | None):\n-        if input_dtype not in OCP_MX_DTYPES or weight_dtype not in OCP_MX_DTYPES:\n+        if input_dtype not in OCP_MX_DTYPES and weight_dtype not in OCP_MX_DTYPES:  # or\n", "materials": ["It's \"and\". Let me remove the comment."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -20,26 +20,44 @@\n \n \n class OCP_MX_Scheme(str, Enum):\n+    w_mxfp4 = \"w_mxfp4\"\n     w_mxfp4_a_mxfp4 = \"w_mxfp4_a_mxfp4\"\n     w_mxfp4_a_mxfp6_e3m2 = \"w_mxfp4_a_mxfp6_e3m2\"\n     w_mxfp4_a_mxfp6_e2m3 = \"w_mxfp4_a_mxfp6_e2m3\"\n+    w_mxfp4_a_fp8 = \"w_mxfp4_a_fp8\"\n+    w_mxfp6_e3m2 = \"w_mxfp6_e3m2\"\n     w_mxfp6_e3m2_a_mxfp6_e3m2 = \"w_mxfp6_e3m2_a_mxfp6_e3m2\"\n+    w_mxfp6_e3m2_a_fp8 = \"w_mxfp6_e3m2_a_fp8\"\n+    w_mxfp6_e2m3 = \"w_mxfp6_e2m3\"\n     w_mxfp6_e2m3_a_mxfp6_e2m3 = \"w_mxfp6_e2m3_a_mxfp6_e2m3\"\n+    w_mxfp6_e2m3_a_fp8 = \"w_mxfp6_e2m3_a_fp8\"\n \n     @classmethod\n     def from_quant_dtype(cls, input_dtype: str | None, weight_dtype: str | None):\n-        if input_dtype not in OCP_MX_DTYPES or weight_dtype not in OCP_MX_DTYPES:\n+        if input_dtype not in OCP_MX_DTYPES and weight_dtype not in OCP_MX_DTYPES:\n             return None\n+        elif input_dtype is None and weight_dtype == \"mxfp4\":\n+            return cls.w_mxfp4\n+        elif input_dtype is None and weight_dtype == \"mxfp6_e3m2\":\n+            return cls.w_mxfp6_e3m2\n+        elif input_dtype is None and weight_dtype == \"mxfp6_e2m3\":\n+            return cls.w_mxfp6_e2m3\n         elif input_dtype == \"mxfp4\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp4\n         elif input_dtype == \"mxfp6_e3m2\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp6_e3m2\n         elif input_dtype == \"mxfp6_e2m3\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp6_e2m3\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp4\":\n+            return cls.w_mxfp4_a_fp8\n         elif input_dtype == \"mxfp6_e3m2\" and weight_dtype == \"mxfp6_e3m2\":\n             return cls.w_mxfp6_e3m2_a_mxfp6_e3m2\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp6_e3m2\":\n+            return cls.w_mxfp6_e3m2_a_fp8\n         elif input_dtype == \"mxfp6_e2m3\" and weight_dtype == \"mxfp6_e2m3\":\n             return cls.w_mxfp6_e2m3_a_mxfp6_e2m3\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp6_e2m3\":\n+            return cls.w_mxfp6_e2m3_a_fp8\n         else:\n             logger.warning(\n                 \"input_dtype='%s' and\"", "patch_sha256": "43f3fe8f1fd07fb8"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2634192486", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\n> This should be handled in `moe_kernel_quantize_input` no?\r\n\r\n`moe_kernel_quantize_input` functions as input quantization while the input QDQ is performed here. \n\nOriginal Code Context:\n@@ -1881,10 +1877,28 @@ def fused_experts_impl(\n \n         curr_topk_ids = topk_ids[begin_chunk_idx:end_chunk_idx]\n         curr_topk_weights = topk_weights[begin_chunk_idx:end_chunk_idx]\n+\n+        if ocp_mx_scheme is not None:\n+            if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+                pass\n+            elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+                # perform QDQ (quantize and dequantize) on activation for emulation purpose, \n+                # because of no native kernel for weight in ocp_mx_scheme and activation in FP8.\n+                qcurr_hidden_states, a1q_scale = ops.scaled_fp8_quant(\n+                    curr_hidden_states, a1_scale, use_per_token_if_dynamic=False\n+                )\n+                curr_hidden_states = per_tensor_dequantize(\n+                    qcurr_hidden_states, a1q_scale\n+                ).to(curr_hidden_states.dtype)\n+                input_quant_dtype = None\n", "materials": ["> This should be handled in `moe_kernel_quantize_input` no?\r\n\r\n`moe_kernel_quantize_input` functions as input quantization while the input QDQ is performed here. "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2634216512", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nyeah, I was aware of that. One concerning point is, the `fp8` QDQ functions like an emulation action but its implementation is via non-emulation ops and no specific to quark, which is different from `mxfp6` approaches. \n\nOriginal Code Context:\n@@ -1941,10 +1955,27 @@ def fused_experts_impl(\n         else:\n             raise ValueError(f\"Unsupported FusedMoe activation: {activation}.\")\n \n+        if ocp_mx_scheme is not None:\n+            if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+                pass\n+            elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+                # perform QDQ (quantize and dequantize) on activation for emulation purpose,\n+                # because of no native kernel for weight in ocp_mx_scheme and activation in FP8.\n+                qintermediate_cache2, a2q_scale = ops.scaled_fp8_quant(\n+                    intermediate_cache2, a2_scale, use_per_token_if_dynamic=False\n+                )\n+                intermediate_cache2 = per_tensor_dequantize(\n+                    qintermediate_cache2, a2q_scale\n+                ).to(intermediate_cache2.dtype)\n+                input_quant_dtype = None\n+            else:\n+                # TODO: @felix for ocp_mx_scheme.endswith(\"fp6_e3m2\"), ocp_mx_scheme.endswith(\"fp6_e2m3\")\n+                raise NotImplementedError(f\"Unsupported ocp_mx_scheme={ocp_mx_scheme}\")\n", "materials": ["yeah, I was aware of that. One concerning point is, the `fp8` QDQ functions like an emulation action but its implementation is via non-emulation ops and no specific to quark, which is different from `mxfp6` approaches. "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2634260093", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nTo add more cases (mxfp4 schemes, emulation or not) and specify the model name.\n\nOriginal Code Context:\n@@ -246,7 +248,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant and not emulate_quant:\n", "materials": ["To add more cases (mxfp4 schemes, emulation or not) and specify the model name."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2636702801", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/utils/ocp_mx_utils.py\nReview Comment:\nto clarify, w_mxfp4_a_fp8 refers to per-tensor fp8 e4m3 for activation, not mxfp8.\n\nOriginal Code Context:\n@@ -20,26 +20,38 @@\n \n \n class OCP_MX_Scheme(str, Enum):\n+    w_mxfp4 = \"w_mxfp4\"\n     w_mxfp4_a_mxfp4 = \"w_mxfp4_a_mxfp4\"\n     w_mxfp4_a_mxfp6_e3m2 = \"w_mxfp4_a_mxfp6_e3m2\"\n     w_mxfp4_a_mxfp6_e2m3 = \"w_mxfp4_a_mxfp6_e2m3\"\n+    w_mxfp4_a_fp8 = \"w_mxfp4_a_fp8\"\n", "materials": ["to clarify, w_mxfp4_a_fp8 refers to per-tensor fp8 e4m3 for activation, not mxfp8."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -20,26 +20,44 @@\n \n \n class OCP_MX_Scheme(str, Enum):\n+    w_mxfp4 = \"w_mxfp4\"\n     w_mxfp4_a_mxfp4 = \"w_mxfp4_a_mxfp4\"\n     w_mxfp4_a_mxfp6_e3m2 = \"w_mxfp4_a_mxfp6_e3m2\"\n     w_mxfp4_a_mxfp6_e2m3 = \"w_mxfp4_a_mxfp6_e2m3\"\n+    w_mxfp4_a_fp8 = \"w_mxfp4_a_fp8\"\n+    w_mxfp6_e3m2 = \"w_mxfp6_e3m2\"\n     w_mxfp6_e3m2_a_mxfp6_e3m2 = \"w_mxfp6_e3m2_a_mxfp6_e3m2\"\n+    w_mxfp6_e3m2_a_fp8 = \"w_mxfp6_e3m2_a_fp8\"\n+    w_mxfp6_e2m3 = \"w_mxfp6_e2m3\"\n     w_mxfp6_e2m3_a_mxfp6_e2m3 = \"w_mxfp6_e2m3_a_mxfp6_e2m3\"\n+    w_mxfp6_e2m3_a_fp8 = \"w_mxfp6_e2m3_a_fp8\"\n \n     @classmethod\n     def from_quant_dtype(cls, input_dtype: str | None, weight_dtype: str | None):\n-        if input_dtype not in OCP_MX_DTYPES or weight_dtype not in OCP_MX_DTYPES:\n+        if input_dtype not in OCP_MX_DTYPES and weight_dtype not in OCP_MX_DTYPES:\n             return None\n+        elif input_dtype is None and weight_dtype == \"mxfp4\":\n+            return cls.w_mxfp4\n+        elif input_dtype is None and weight_dtype == \"mxfp6_e3m2\":\n+            return cls.w_mxfp6_e3m2\n+        elif input_dtype is None and weight_dtype == \"mxfp6_e2m3\":\n+            return cls.w_mxfp6_e2m3\n         elif input_dtype == \"mxfp4\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp4\n         elif input_dtype == \"mxfp6_e3m2\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp6_e3m2\n         elif input_dtype == \"mxfp6_e2m3\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp6_e2m3\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp4\":\n+            return cls.w_mxfp4_a_fp8\n         elif input_dtype == \"mxfp6_e3m2\" and weight_dtype == \"mxfp6_e3m2\":\n             return cls.w_mxfp6_e3m2_a_mxfp6_e3m2\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp6_e3m2\":\n+            return cls.w_mxfp6_e3m2_a_fp8\n         elif input_dtype == \"mxfp6_e2m3\" and weight_dtype == \"mxfp6_e2m3\":\n             return cls.w_mxfp6_e2m3_a_mxfp6_e2m3\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp6_e2m3\":\n+            return cls.w_mxfp6_e2m3_a_fp8\n         else:\n             logger.warning(\n                 \"input_dtype='%s' and\"", "patch_sha256": "43f3fe8f1fd07fb8"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2638954452", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nJust thinking you will get pushback adding quantizer-specific logic directly  in `fused_experts_impl`\r\n\r\n> moe_kernel_quantize_input functions as input quantization while the input QDQ is performed here.\r\n\r\nNot exactly, for OCP MX FP6/FP4, `moe_kernel_quantize_input` just does QDQ. Kernel folks did not integrate MXFP4 GEMM in `fused_moe.py`, a separate code path was integrated in https://github.com/vllm-project/vllm/pull/26545\n\nOriginal Code Context:\n@@ -1881,10 +1877,28 @@ def fused_experts_impl(\n \n         curr_topk_ids = topk_ids[begin_chunk_idx:end_chunk_idx]\n         curr_topk_weights = topk_weights[begin_chunk_idx:end_chunk_idx]\n+\n+        if ocp_mx_scheme is not None:\n+            if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+                pass\n+            elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+                # perform QDQ (quantize and dequantize) on activation for emulation purpose, \n+                # because of no native kernel for weight in ocp_mx_scheme and activation in FP8.\n+                qcurr_hidden_states, a1q_scale = ops.scaled_fp8_quant(\n+                    curr_hidden_states, a1_scale, use_per_token_if_dynamic=False\n+                )\n+                curr_hidden_states = per_tensor_dequantize(\n+                    qcurr_hidden_states, a1q_scale\n+                ).to(curr_hidden_states.dtype)\n+                input_quant_dtype = None\n", "materials": ["Just thinking you will get pushback adding quantizer-specific logic directly  in `fused_experts_impl`\r\n\r\n> moe_kernel_quantize_input functions as input quantization while the input QDQ is performed here.\r\n\r\nNot exactly, for OCP MX FP6/FP4, `moe_kernel_quantize_input` just does QDQ. Kernel folks did not integrate MXFP4 GEMM in `fused_moe.py`, a separate code path was integrated in https://github.com/vllm-project/vllm/pull/26545"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2638955360", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/utils/ocp_mx_utils.py\nReview Comment:\nMaybe we can use `w_mxfp4_a_fp8_e4m3`?\n\nOriginal Code Context:\n@@ -20,26 +20,38 @@\n \n \n class OCP_MX_Scheme(str, Enum):\n+    w_mxfp4 = \"w_mxfp4\"\n     w_mxfp4_a_mxfp4 = \"w_mxfp4_a_mxfp4\"\n     w_mxfp4_a_mxfp6_e3m2 = \"w_mxfp4_a_mxfp6_e3m2\"\n     w_mxfp4_a_mxfp6_e2m3 = \"w_mxfp4_a_mxfp6_e2m3\"\n+    w_mxfp4_a_fp8 = \"w_mxfp4_a_fp8\"\n", "materials": ["Maybe we can use `w_mxfp4_a_fp8_e4m3`?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -20,26 +20,44 @@\n \n \n class OCP_MX_Scheme(str, Enum):\n+    w_mxfp4 = \"w_mxfp4\"\n     w_mxfp4_a_mxfp4 = \"w_mxfp4_a_mxfp4\"\n     w_mxfp4_a_mxfp6_e3m2 = \"w_mxfp4_a_mxfp6_e3m2\"\n     w_mxfp4_a_mxfp6_e2m3 = \"w_mxfp4_a_mxfp6_e2m3\"\n+    w_mxfp4_a_fp8 = \"w_mxfp4_a_fp8\"\n+    w_mxfp6_e3m2 = \"w_mxfp6_e3m2\"\n     w_mxfp6_e3m2_a_mxfp6_e3m2 = \"w_mxfp6_e3m2_a_mxfp6_e3m2\"\n+    w_mxfp6_e3m2_a_fp8 = \"w_mxfp6_e3m2_a_fp8\"\n+    w_mxfp6_e2m3 = \"w_mxfp6_e2m3\"\n     w_mxfp6_e2m3_a_mxfp6_e2m3 = \"w_mxfp6_e2m3_a_mxfp6_e2m3\"\n+    w_mxfp6_e2m3_a_fp8 = \"w_mxfp6_e2m3_a_fp8\"\n \n     @classmethod\n     def from_quant_dtype(cls, input_dtype: str | None, weight_dtype: str | None):\n-        if input_dtype not in OCP_MX_DTYPES or weight_dtype not in OCP_MX_DTYPES:\n+        if input_dtype not in OCP_MX_DTYPES and weight_dtype not in OCP_MX_DTYPES:\n             return None\n+        elif input_dtype is None and weight_dtype == \"mxfp4\":\n+            return cls.w_mxfp4\n+        elif input_dtype is None and weight_dtype == \"mxfp6_e3m2\":\n+            return cls.w_mxfp6_e3m2\n+        elif input_dtype is None and weight_dtype == \"mxfp6_e2m3\":\n+            return cls.w_mxfp6_e2m3\n         elif input_dtype == \"mxfp4\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp4\n         elif input_dtype == \"mxfp6_e3m2\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp6_e3m2\n         elif input_dtype == \"mxfp6_e2m3\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp6_e2m3\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp4\":\n+            return cls.w_mxfp4_a_fp8\n         elif input_dtype == \"mxfp6_e3m2\" and weight_dtype == \"mxfp6_e3m2\":\n             return cls.w_mxfp6_e3m2_a_mxfp6_e3m2\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp6_e3m2\":\n+            return cls.w_mxfp6_e3m2_a_fp8\n         elif input_dtype == \"mxfp6_e2m3\" and weight_dtype == \"mxfp6_e2m3\":\n             return cls.w_mxfp6_e2m3_a_mxfp6_e2m3\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp6_e2m3\":\n+            return cls.w_mxfp6_e2m3_a_fp8\n         else:\n             logger.warning(\n                 \"input_dtype='%s' and\"", "patch_sha256": "43f3fe8f1fd07fb8"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2638959730", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\n> via non-emulation ops\r\n\r\nDo you mean `ops.scaled_fp8_quant`? \n\nOriginal Code Context:\n@@ -1941,10 +1955,27 @@ def fused_experts_impl(\n         else:\n             raise ValueError(f\"Unsupported FusedMoe activation: {activation}.\")\n \n+        if ocp_mx_scheme is not None:\n+            if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+                pass\n+            elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+                # perform QDQ (quantize and dequantize) on activation for emulation purpose,\n+                # because of no native kernel for weight in ocp_mx_scheme and activation in FP8.\n+                qintermediate_cache2, a2q_scale = ops.scaled_fp8_quant(\n+                    intermediate_cache2, a2_scale, use_per_token_if_dynamic=False\n+                )\n+                intermediate_cache2 = per_tensor_dequantize(\n+                    qintermediate_cache2, a2q_scale\n+                ).to(intermediate_cache2.dtype)\n+                input_quant_dtype = None\n+            else:\n+                # TODO: @felix for ocp_mx_scheme.endswith(\"fp6_e3m2\"), ocp_mx_scheme.endswith(\"fp6_e2m3\")\n+                raise NotImplementedError(f\"Unsupported ocp_mx_scheme={ocp_mx_scheme}\")\n", "materials": ["> via non-emulation ops\r\n\r\nDo you mean `ops.scaled_fp8_quant`? "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2639435361", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nYes, it's irrelevant with quark\n\nOriginal Code Context:\n@@ -1941,10 +1955,27 @@ def fused_experts_impl(\n         else:\n             raise ValueError(f\"Unsupported FusedMoe activation: {activation}.\")\n \n+        if ocp_mx_scheme is not None:\n+            if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+                pass\n+            elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+                # perform QDQ (quantize and dequantize) on activation for emulation purpose,\n+                # because of no native kernel for weight in ocp_mx_scheme and activation in FP8.\n+                qintermediate_cache2, a2q_scale = ops.scaled_fp8_quant(\n+                    intermediate_cache2, a2_scale, use_per_token_if_dynamic=False\n+                )\n+                intermediate_cache2 = per_tensor_dequantize(\n+                    qintermediate_cache2, a2q_scale\n+                ).to(intermediate_cache2.dtype)\n+                input_quant_dtype = None\n+            else:\n+                # TODO: @felix for ocp_mx_scheme.endswith(\"fp6_e3m2\"), ocp_mx_scheme.endswith(\"fp6_e2m3\")\n+                raise NotImplementedError(f\"Unsupported ocp_mx_scheme={ocp_mx_scheme}\")\n", "materials": ["Yes, it's irrelevant with quark"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2676637895", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\n### NotImplementedError raised for valid mxfp6 quantization schemes\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe new code blocks check for specific `ocp_mx_scheme` values but only handle `{\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}` and schemes ending with `\"a_fp8\"`. Valid mxfp6 schemes like `\"w_mxfp4_a_mxfp6_e3m2\"`, `\"w_mxfp4_a_mxfp6_e2m3\"`, `\"w_mxfp6_e3m2_a_mxfp6_e3m2\"`, and `\"w_mxfp6_e2m3_a_mxfp6_e2m3\"` are correctly handled by `_get_input_quant_dtype` but now fall through to the `else` clause and raise `NotImplementedError`. These schemes would be handled correctly by `moe_kernel_quantize_input` if allowed to proceed.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2149-L2169\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2234-L2254\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`vllm/model_executor/layers/fused_moe/fused_moe.py#L2234-L2254`](https://github.com/vllm-project/vllm/blob/e23834d826c8443f15161f162db7d53a206a39a3/vllm/model_executor/layers/fused_moe/fused_moe.py#L2234-L2254)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..JMkWnk3PhnVFmE0ZGFCACLHe3GSX_Ye02dkUBvTw2RkZ3z4OCug0GAL5_AWOrqdZOSA_1XKGUKEi2UykxuoaFTzAwjzR01hkFaE8Tcng-3f-xAGa9zwSdJa7dDfUXqRGu3fvp7Q7TAvJFQHQI7khopblBGLHkEnMZhCyVgWR0vIzEYNjM6C8MBYoMFOLNRUHy5KLkUhc7RH9hgDpmC8TEH822jrFq4weZVeBNu3-ZaaOvx9AEG8qVz2HiqyZXFKoCUt3XuHyOPMatML3bxubGUtLt8raSSl2UmzVfzOEnUxvbDy3_UZeDIIENuqR8BgM67kTsSUre_VzNS5YfBfSdQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..sKFPcIM5ZEklaq4siFKqbsB5kT4NpaCIdWEfXdYwv5rOuAfqIpC-X8yX9ZRmAA8bOyHaIhrJEtX8I7lhAx1arWASW632rTxvcNmoj2uzaa5dB4oOjop4_cSeaZ2zC-CIMjm6VSB-TLptMWEjaj_MVpJhs-daReFK0K-WpDcK7CqdK31CV7B26tAatk5modoeEkt8sOYm2A5oOagzF368h_-eJLqm9RNXfmO4YWTqKWHX4GItOhOZZqtXqYDTTej8D6d2zNk0_pdqcdCqca-Ue74EjxWh-5AHIUU0-RxXlby1Zcd70InAQ7swC7xzWKUxRmna2U0nmr4410AOuYYXvA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -2150,10 +2146,32 @@ def fused_experts_impl(\n \n         curr_topk_ids = topk_ids[begin_chunk_idx:end_chunk_idx]\n         curr_topk_weights = topk_weights[begin_chunk_idx:end_chunk_idx]\n+\n+        if ocp_mx_scheme is not None:\n+            if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+                pass\n+            elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+                # perform QDQ (quantize and dequantize)\n+                # on activation for emulation purpose,\n+                # because of no native kernel for weight\n+                # in ocp_mx_scheme and activation in FP8.\n+                # However, the implementation is based on\n+                # existing non-emulation ops.\n+                qcurr_hidden_states, a1q_scale = ops.scaled_fp8_quant(\n+                    curr_hidden_states, a1_scale, use_per_token_if_dynamic=False\n+                )\n+                curr_hidden_states = per_tensor_dequantize(\n+                    qcurr_hidden_states, a1q_scale\n+                ).to(curr_hidden_states.dtype)\n+                input_quant_dtype = None\n+                a1_scale = None\n+            else:\n+                raise NotImplementedError(f\"Unsupported ocp_mx_scheme={ocp_mx_scheme}\")\n", "materials": ["### NotImplementedError raised for valid mxfp6 quantization schemes\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe new code blocks check for specific `ocp_mx_scheme` values but only handle `{\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}` and schemes ending with `\"a_fp8\"`. Valid mxfp6 schemes like `\"w_mxfp4_a_mxfp6_e3m2\"`, `\"w_mxfp4_a_mxfp6_e2m3\"`, `\"w_mxfp6_e3m2_a_mxfp6_e3m2\"`, and `\"w_mxfp6_e2m3_a_mxfp6_e2m3\"` are correctly handled by `_get_input_quant_dtype` but now fall through to the `else` clause and raise `NotImplementedError`. These schemes would be handled correctly by `moe_kernel_quantize_input` if allowed to proceed.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2149-L2169\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2234-L2254\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`vllm/model_executor/layers/fused_moe/fused_moe.py#L2234-L2254`](https://github.com/vllm-project/vllm/blob/e23834d826c8443f15161f162db7d53a206a39a3/vllm/model_executor/layers/fused_moe/fused_moe.py#L2234-L2254)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..JMkWnk3PhnVFmE0ZGFCACLHe3GSX_Ye02dkUBvTw2RkZ3z4OCug0GAL5_AWOrqdZOSA_1XKGUKEi2UykxuoaFTzAwjzR01hkFaE8Tcng-3f-xAGa9zwSdJa7dDfUXqRGu3fvp7Q7TAvJFQHQI7khopblBGLHkEnMZhCyVgWR0vIzEYNjM6C8MBYoMFOLNRUHy5KLkUhc7RH9hgDpmC8TEH822jrFq4weZVeBNu3-ZaaOvx9AEG8qVz2HiqyZXFKoCUt3XuHyOPMatML3bxubGUtLt8raSSl2UmzVfzOEnUxvbDy3_UZeDIIENuqR8BgM67kTsSUre_VzNS5YfBfSdQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..sKFPcIM5ZEklaq4siFKqbsB5kT4NpaCIdWEfXdYwv5rOuAfqIpC-X8yX9ZRmAA8bOyHaIhrJEtX8I7lhAx1arWASW632rTxvcNmoj2uzaa5dB4oOjop4_cSeaZ2zC-CIMjm6VSB-TLptMWEjaj_MVpJhs-daReFK0K-WpDcK7CqdK31CV7B26tAatk5modoeEkt8sOYm2A5oOagzF368h_-eJLqm9RNXfmO4YWTqKWHX4GItOhOZZqtXqYDTTej8D6d2zNk0_pdqcdCqca-Ue74EjxWh-5AHIUU0-RxXlby1Zcd70InAQ7swC7xzWKUxRmna2U0nmr4410AOuYYXvA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2677635681", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/config.py\nReview Comment:\nnit: the convention is:\r\n\r\n`mxfp4_w4a8_moe_quant_config`\n\nOriginal Code Context:\n@@ -628,6 +640,26 @@ def mxfp4_mxfp8_moe_quant_config(\n     )\n \n \n+def mxfp4_fp8_moe_quant_config(\n", "materials": ["nit: the convention is:\r\n\r\n`mxfp4_w4a8_moe_quant_config`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -386,6 +386,10 @@ def use_mxfp4_w4a4(self) -> bool:\n     def use_nvfp4_w4a4(self) -> bool:\n         return self.quant_dtype == \"nvfp4\"\n \n+    @property\n+    def use_mxfp4_w4a8(self) -> bool:\n+        return self._a1.dtype == \"fp8\" and self._w1.dtype == \"mxfp4\"\n+\n     def config_name(self, dtype: torch.dtype) -> str | None:\n         \"\"\"\n         Return a string used to construct the filename that contains the\n@@ -532,6 +536,8 @@ def fp8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n     per_out_ch_quant: bool = False,\n     block_shape: list[int] | None = None,\n@@ -549,6 +555,8 @@ def fp8_w8a8_moe_quant_config(\n         g1_alphas=g1_alphas,\n         w2_scale=w2_scale,\n         g2_alphas=g2_alphas,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_scale=a1_scale,\n         a1_gscale=a1_gscale,\n         a2_scale=a2_scale,\n@@ -564,6 +572,8 @@ def int8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None,\n     a2_scale: torch.Tensor | None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n@@ -575,6 +585,8 @@ def int8_w8a8_moe_quant_config(\n         w2_scale=w2_scale,\n         a1_scale=a1_scale,\n         a2_scale=a2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         per_act_token_quant=per_act_token_quant,\n         per_out_ch_quant=False,\n         block_shape=None,\n@@ -654,6 +666,26 @@ def mxfp4_mxfp8_moe_quant_config(\n     )\n \n \n+def mxfp4_w4a8_moe_quant_config(\n+    w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    w2_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    a1_scale: torch.Tensor | None = None,\n+    a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n+    block_shape: list[int] | None = None,\n+) -> FusedMoEQuantConfig:\n+    \"\"\"\n+    Construct a quant config for fp8 activations and mxfp4 weights.\n+    \"\"\"\n+    return FusedMoEQuantConfig(\n+        _a1=FusedMoEQuantDesc(\"fp8\", None, a1_scale, None, None, None),\n+        _a2=FusedMoEQuantDesc(\"fp8\", None, a2_scale, None, None, None),\n+        _w1=FusedMoEQuantDesc(\"mxfp4\", None, w1_scale, None, None, w1_bias),\n+        _w2=FusedMoEQuantDesc(\"mxfp4\", None, w2_scale, None, None, w2_bias),\n+    )\n+\n+\n def ocp_mx_moe_quant_config(\n     quant_dtype: str,\n     w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n@@ -691,6 +723,8 @@ def nvfp4_moe_quant_config(\n     a2_gscale: torch.Tensor,\n     w1_scale: torch.Tensor,\n     w2_scale: torch.Tensor,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n     Construct a quant config for mxfp4 activations and nvp4 weights.\n@@ -699,6 +733,8 @@ def nvfp4_moe_quant_config(\n         \"nvfp4\",\n         w1_scale=w1_scale,\n         w2_scale=w2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_gscale=a1_gscale,\n         a2_gscale=a2_gscale,\n         g1_alphas=g1_alphas,", "patch_sha256": "eea235a7c7a12e3b"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2677640717", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nWe are actively seeking to simplify this function: https://github.com/orgs/vllm-project/projects/45\r\n\r\nI do not want to add a special case for `mx` emulation here.\r\n\r\nIf you want to add mx_emulation, we should add it to `moe_kernel_quantize_input`\n\nOriginal Code Context:\n@@ -2150,10 +2146,32 @@ def fused_experts_impl(\n \n         curr_topk_ids = topk_ids[begin_chunk_idx:end_chunk_idx]\n         curr_topk_weights = topk_weights[begin_chunk_idx:end_chunk_idx]\n+\n+        if ocp_mx_scheme is not None:\n", "materials": ["We are actively seeking to simplify this function: https://github.com/orgs/vllm-project/projects/45\r\n\r\nI do not want to add a special case for `mx` emulation here.\r\n\r\nIf you want to add mx_emulation, we should add it to `moe_kernel_quantize_input`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2677642151", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nsame comment as above\n\nOriginal Code Context:\n@@ -2214,10 +2232,31 @@ def fused_experts_impl(\n         else:\n             raise ValueError(f\"Unsupported FusedMoe activation: {activation}.\")\n \n+        if ocp_mx_scheme is not None:\n", "materials": ["same comment as above"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2677696040", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/config.py\nReview Comment:\ncan you please add a docstring for this?\n\nOriginal Code Context:\n@@ -506,6 +510,8 @@ def fp8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n", "materials": ["can you please add a docstring for this?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -386,6 +386,10 @@ def use_mxfp4_w4a4(self) -> bool:\n     def use_nvfp4_w4a4(self) -> bool:\n         return self.quant_dtype == \"nvfp4\"\n \n+    @property\n+    def use_mxfp4_w4a8(self) -> bool:\n+        return self._a1.dtype == \"fp8\" and self._w1.dtype == \"mxfp4\"\n+\n     def config_name(self, dtype: torch.dtype) -> str | None:\n         \"\"\"\n         Return a string used to construct the filename that contains the\n@@ -532,6 +536,8 @@ def fp8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n     per_out_ch_quant: bool = False,\n     block_shape: list[int] | None = None,\n@@ -549,6 +555,8 @@ def fp8_w8a8_moe_quant_config(\n         g1_alphas=g1_alphas,\n         w2_scale=w2_scale,\n         g2_alphas=g2_alphas,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_scale=a1_scale,\n         a1_gscale=a1_gscale,\n         a2_scale=a2_scale,\n@@ -564,6 +572,8 @@ def int8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None,\n     a2_scale: torch.Tensor | None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n@@ -575,6 +585,8 @@ def int8_w8a8_moe_quant_config(\n         w2_scale=w2_scale,\n         a1_scale=a1_scale,\n         a2_scale=a2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         per_act_token_quant=per_act_token_quant,\n         per_out_ch_quant=False,\n         block_shape=None,\n@@ -654,6 +666,26 @@ def mxfp4_mxfp8_moe_quant_config(\n     )\n \n \n+def mxfp4_w4a8_moe_quant_config(\n+    w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    w2_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    a1_scale: torch.Tensor | None = None,\n+    a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n+    block_shape: list[int] | None = None,\n+) -> FusedMoEQuantConfig:\n+    \"\"\"\n+    Construct a quant config for fp8 activations and mxfp4 weights.\n+    \"\"\"\n+    return FusedMoEQuantConfig(\n+        _a1=FusedMoEQuantDesc(\"fp8\", None, a1_scale, None, None, None),\n+        _a2=FusedMoEQuantDesc(\"fp8\", None, a2_scale, None, None, None),\n+        _w1=FusedMoEQuantDesc(\"mxfp4\", None, w1_scale, None, None, w1_bias),\n+        _w2=FusedMoEQuantDesc(\"mxfp4\", None, w2_scale, None, None, w2_bias),\n+    )\n+\n+\n def ocp_mx_moe_quant_config(\n     quant_dtype: str,\n     w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n@@ -691,6 +723,8 @@ def nvfp4_moe_quant_config(\n     a2_gscale: torch.Tensor,\n     w1_scale: torch.Tensor,\n     w2_scale: torch.Tensor,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n     Construct a quant config for mxfp4 activations and nvp4 weights.\n@@ -699,6 +733,8 @@ def nvfp4_moe_quant_config(\n         \"nvfp4\",\n         w1_scale=w1_scale,\n         w2_scale=w2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_gscale=a1_gscale,\n         a2_gscale=a2_gscale,\n         g1_alphas=g1_alphas,", "patch_sha256": "eea235a7c7a12e3b"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2677704239", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/config.py\nReview Comment:\nwhy is the fp8 config being changed?\n\nOriginal Code Context:\n@@ -506,6 +510,8 @@ def fp8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n", "materials": ["why is the fp8 config being changed?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -386,6 +386,10 @@ def use_mxfp4_w4a4(self) -> bool:\n     def use_nvfp4_w4a4(self) -> bool:\n         return self.quant_dtype == \"nvfp4\"\n \n+    @property\n+    def use_mxfp4_w4a8(self) -> bool:\n+        return self._a1.dtype == \"fp8\" and self._w1.dtype == \"mxfp4\"\n+\n     def config_name(self, dtype: torch.dtype) -> str | None:\n         \"\"\"\n         Return a string used to construct the filename that contains the\n@@ -532,6 +536,8 @@ def fp8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n     per_out_ch_quant: bool = False,\n     block_shape: list[int] | None = None,\n@@ -549,6 +555,8 @@ def fp8_w8a8_moe_quant_config(\n         g1_alphas=g1_alphas,\n         w2_scale=w2_scale,\n         g2_alphas=g2_alphas,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_scale=a1_scale,\n         a1_gscale=a1_gscale,\n         a2_scale=a2_scale,\n@@ -564,6 +572,8 @@ def int8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None,\n     a2_scale: torch.Tensor | None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n@@ -575,6 +585,8 @@ def int8_w8a8_moe_quant_config(\n         w2_scale=w2_scale,\n         a1_scale=a1_scale,\n         a2_scale=a2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         per_act_token_quant=per_act_token_quant,\n         per_out_ch_quant=False,\n         block_shape=None,\n@@ -654,6 +666,26 @@ def mxfp4_mxfp8_moe_quant_config(\n     )\n \n \n+def mxfp4_w4a8_moe_quant_config(\n+    w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    w2_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    a1_scale: torch.Tensor | None = None,\n+    a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n+    block_shape: list[int] | None = None,\n+) -> FusedMoEQuantConfig:\n+    \"\"\"\n+    Construct a quant config for fp8 activations and mxfp4 weights.\n+    \"\"\"\n+    return FusedMoEQuantConfig(\n+        _a1=FusedMoEQuantDesc(\"fp8\", None, a1_scale, None, None, None),\n+        _a2=FusedMoEQuantDesc(\"fp8\", None, a2_scale, None, None, None),\n+        _w1=FusedMoEQuantDesc(\"mxfp4\", None, w1_scale, None, None, w1_bias),\n+        _w2=FusedMoEQuantDesc(\"mxfp4\", None, w2_scale, None, None, w2_bias),\n+    )\n+\n+\n def ocp_mx_moe_quant_config(\n     quant_dtype: str,\n     w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n@@ -691,6 +723,8 @@ def nvfp4_moe_quant_config(\n     a2_gscale: torch.Tensor,\n     w1_scale: torch.Tensor,\n     w2_scale: torch.Tensor,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n     Construct a quant config for mxfp4 activations and nvp4 weights.\n@@ -699,6 +733,8 @@ def nvfp4_moe_quant_config(\n         \"nvfp4\",\n         w1_scale=w1_scale,\n         w2_scale=w2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_gscale=a1_gscale,\n         a2_gscale=a2_gscale,\n         g1_alphas=g1_alphas,", "patch_sha256": "eea235a7c7a12e3b"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2677704883", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/config.py\nReview Comment:\nwhy is the int8 config being changed?\n\nOriginal Code Context:\n@@ -538,6 +546,8 @@ def int8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None,\n     a2_scale: torch.Tensor | None,\n+    w1_bias: torch.Tensor | None = None,\n", "materials": ["why is the int8 config being changed?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -386,6 +386,10 @@ def use_mxfp4_w4a4(self) -> bool:\n     def use_nvfp4_w4a4(self) -> bool:\n         return self.quant_dtype == \"nvfp4\"\n \n+    @property\n+    def use_mxfp4_w4a8(self) -> bool:\n+        return self._a1.dtype == \"fp8\" and self._w1.dtype == \"mxfp4\"\n+\n     def config_name(self, dtype: torch.dtype) -> str | None:\n         \"\"\"\n         Return a string used to construct the filename that contains the\n@@ -532,6 +536,8 @@ def fp8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n     per_out_ch_quant: bool = False,\n     block_shape: list[int] | None = None,\n@@ -549,6 +555,8 @@ def fp8_w8a8_moe_quant_config(\n         g1_alphas=g1_alphas,\n         w2_scale=w2_scale,\n         g2_alphas=g2_alphas,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_scale=a1_scale,\n         a1_gscale=a1_gscale,\n         a2_scale=a2_scale,\n@@ -564,6 +572,8 @@ def int8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None,\n     a2_scale: torch.Tensor | None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n@@ -575,6 +585,8 @@ def int8_w8a8_moe_quant_config(\n         w2_scale=w2_scale,\n         a1_scale=a1_scale,\n         a2_scale=a2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         per_act_token_quant=per_act_token_quant,\n         per_out_ch_quant=False,\n         block_shape=None,\n@@ -654,6 +666,26 @@ def mxfp4_mxfp8_moe_quant_config(\n     )\n \n \n+def mxfp4_w4a8_moe_quant_config(\n+    w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    w2_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    a1_scale: torch.Tensor | None = None,\n+    a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n+    block_shape: list[int] | None = None,\n+) -> FusedMoEQuantConfig:\n+    \"\"\"\n+    Construct a quant config for fp8 activations and mxfp4 weights.\n+    \"\"\"\n+    return FusedMoEQuantConfig(\n+        _a1=FusedMoEQuantDesc(\"fp8\", None, a1_scale, None, None, None),\n+        _a2=FusedMoEQuantDesc(\"fp8\", None, a2_scale, None, None, None),\n+        _w1=FusedMoEQuantDesc(\"mxfp4\", None, w1_scale, None, None, w1_bias),\n+        _w2=FusedMoEQuantDesc(\"mxfp4\", None, w2_scale, None, None, w2_bias),\n+    )\n+\n+\n def ocp_mx_moe_quant_config(\n     quant_dtype: str,\n     w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n@@ -691,6 +723,8 @@ def nvfp4_moe_quant_config(\n     a2_gscale: torch.Tensor,\n     w1_scale: torch.Tensor,\n     w2_scale: torch.Tensor,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n     Construct a quant config for mxfp4 activations and nvp4 weights.\n@@ -699,6 +733,8 @@ def nvfp4_moe_quant_config(\n         \"nvfp4\",\n         w1_scale=w1_scale,\n         w2_scale=w2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_gscale=a1_gscale,\n         a2_gscale=a2_gscale,\n         g1_alphas=g1_alphas,", "patch_sha256": "eea235a7c7a12e3b"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2677725281", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nits not clear to me why these are needed now but not before?\n\nOriginal Code Context:\n@@ -584,6 +576,38 @@ def _get_quant_method() -> FusedMoEMethodBase:\n         # for heuristic purposes, so it must be initialized first.\n         self.quant_method: FusedMoEMethodBase = _get_quant_method()\n \n+        self.model_type = (\n+            self.vllm_config.model_config.hf_config.model_type\n+            if self.vllm_config.model_config is not None\n+            else None\n+        )\n+        self.is_mxfp4_quant = self._is_mxfp4_quant(quant_config, self.quant_method)\n+\n+        # Round up hidden size if needed.\n+        hidden_size, is_rounded_hidden_size = maybe_roundup_hidden_size(\n+            hidden_size,\n+            moe_in_dtype,\n+            self.moe_parallel_config,\n+            self.model_type,\n+            self.is_mxfp4_quant,\n+            is_lora_enabled=self.vllm_config.lora_config is not None,\n+        )\n+\n+        if is_rounded_hidden_size:\n+            self.hidden_size = hidden_size\n", "materials": ["its not clear to me why these are needed now but not before?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2680810038", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nMaking sense to me.\n\nOriginal Code Context:\n@@ -2150,10 +2146,32 @@ def fused_experts_impl(\n \n         curr_topk_ids = topk_ids[begin_chunk_idx:end_chunk_idx]\n         curr_topk_weights = topk_weights[begin_chunk_idx:end_chunk_idx]\n+\n+        if ocp_mx_scheme is not None:\n", "materials": ["Making sense to me."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2680820476", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/config.py\nReview Comment:\nbecause bias are also needed to pass for fp8, same as other configs, that's why they are added.\n\nOriginal Code Context:\n@@ -506,6 +510,8 @@ def fp8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n", "materials": ["because bias are also needed to pass for fp8, same as other configs, that's why they are added."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -386,6 +386,10 @@ def use_mxfp4_w4a4(self) -> bool:\n     def use_nvfp4_w4a4(self) -> bool:\n         return self.quant_dtype == \"nvfp4\"\n \n+    @property\n+    def use_mxfp4_w4a8(self) -> bool:\n+        return self._a1.dtype == \"fp8\" and self._w1.dtype == \"mxfp4\"\n+\n     def config_name(self, dtype: torch.dtype) -> str | None:\n         \"\"\"\n         Return a string used to construct the filename that contains the\n@@ -532,6 +536,8 @@ def fp8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n     per_out_ch_quant: bool = False,\n     block_shape: list[int] | None = None,\n@@ -549,6 +555,8 @@ def fp8_w8a8_moe_quant_config(\n         g1_alphas=g1_alphas,\n         w2_scale=w2_scale,\n         g2_alphas=g2_alphas,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_scale=a1_scale,\n         a1_gscale=a1_gscale,\n         a2_scale=a2_scale,\n@@ -564,6 +572,8 @@ def int8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None,\n     a2_scale: torch.Tensor | None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n@@ -575,6 +585,8 @@ def int8_w8a8_moe_quant_config(\n         w2_scale=w2_scale,\n         a1_scale=a1_scale,\n         a2_scale=a2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         per_act_token_quant=per_act_token_quant,\n         per_out_ch_quant=False,\n         block_shape=None,\n@@ -654,6 +666,26 @@ def mxfp4_mxfp8_moe_quant_config(\n     )\n \n \n+def mxfp4_w4a8_moe_quant_config(\n+    w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    w2_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    a1_scale: torch.Tensor | None = None,\n+    a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n+    block_shape: list[int] | None = None,\n+) -> FusedMoEQuantConfig:\n+    \"\"\"\n+    Construct a quant config for fp8 activations and mxfp4 weights.\n+    \"\"\"\n+    return FusedMoEQuantConfig(\n+        _a1=FusedMoEQuantDesc(\"fp8\", None, a1_scale, None, None, None),\n+        _a2=FusedMoEQuantDesc(\"fp8\", None, a2_scale, None, None, None),\n+        _w1=FusedMoEQuantDesc(\"mxfp4\", None, w1_scale, None, None, w1_bias),\n+        _w2=FusedMoEQuantDesc(\"mxfp4\", None, w2_scale, None, None, w2_bias),\n+    )\n+\n+\n def ocp_mx_moe_quant_config(\n     quant_dtype: str,\n     w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n@@ -691,6 +723,8 @@ def nvfp4_moe_quant_config(\n     a2_gscale: torch.Tensor,\n     w1_scale: torch.Tensor,\n     w2_scale: torch.Tensor,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n     Construct a quant config for mxfp4 activations and nvp4 weights.\n@@ -699,6 +733,8 @@ def nvfp4_moe_quant_config(\n         \"nvfp4\",\n         w1_scale=w1_scale,\n         w2_scale=w2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_gscale=a1_gscale,\n         a2_gscale=a2_gscale,\n         g1_alphas=g1_alphas,", "patch_sha256": "eea235a7c7a12e3b"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2680821603", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/config.py\nReview Comment:\nsame as above, because bias are also needed to pass for int8, they can be a tensor or None.\n\nOriginal Code Context:\n@@ -538,6 +546,8 @@ def int8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None,\n     a2_scale: torch.Tensor | None,\n+    w1_bias: torch.Tensor | None = None,\n", "materials": ["same as above, because bias are also needed to pass for int8, they can be a tensor or None."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -386,6 +386,10 @@ def use_mxfp4_w4a4(self) -> bool:\n     def use_nvfp4_w4a4(self) -> bool:\n         return self.quant_dtype == \"nvfp4\"\n \n+    @property\n+    def use_mxfp4_w4a8(self) -> bool:\n+        return self._a1.dtype == \"fp8\" and self._w1.dtype == \"mxfp4\"\n+\n     def config_name(self, dtype: torch.dtype) -> str | None:\n         \"\"\"\n         Return a string used to construct the filename that contains the\n@@ -532,6 +536,8 @@ def fp8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n     per_out_ch_quant: bool = False,\n     block_shape: list[int] | None = None,\n@@ -549,6 +555,8 @@ def fp8_w8a8_moe_quant_config(\n         g1_alphas=g1_alphas,\n         w2_scale=w2_scale,\n         g2_alphas=g2_alphas,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_scale=a1_scale,\n         a1_gscale=a1_gscale,\n         a2_scale=a2_scale,\n@@ -564,6 +572,8 @@ def int8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None,\n     a2_scale: torch.Tensor | None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n@@ -575,6 +585,8 @@ def int8_w8a8_moe_quant_config(\n         w2_scale=w2_scale,\n         a1_scale=a1_scale,\n         a2_scale=a2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         per_act_token_quant=per_act_token_quant,\n         per_out_ch_quant=False,\n         block_shape=None,\n@@ -654,6 +666,26 @@ def mxfp4_mxfp8_moe_quant_config(\n     )\n \n \n+def mxfp4_w4a8_moe_quant_config(\n+    w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    w2_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    a1_scale: torch.Tensor | None = None,\n+    a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n+    block_shape: list[int] | None = None,\n+) -> FusedMoEQuantConfig:\n+    \"\"\"\n+    Construct a quant config for fp8 activations and mxfp4 weights.\n+    \"\"\"\n+    return FusedMoEQuantConfig(\n+        _a1=FusedMoEQuantDesc(\"fp8\", None, a1_scale, None, None, None),\n+        _a2=FusedMoEQuantDesc(\"fp8\", None, a2_scale, None, None, None),\n+        _w1=FusedMoEQuantDesc(\"mxfp4\", None, w1_scale, None, None, w1_bias),\n+        _w2=FusedMoEQuantDesc(\"mxfp4\", None, w2_scale, None, None, w2_bias),\n+    )\n+\n+\n def ocp_mx_moe_quant_config(\n     quant_dtype: str,\n     w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n@@ -691,6 +723,8 @@ def nvfp4_moe_quant_config(\n     a2_gscale: torch.Tensor,\n     w1_scale: torch.Tensor,\n     w2_scale: torch.Tensor,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n     Construct a quant config for mxfp4 activations and nvp4 weights.\n@@ -699,6 +733,8 @@ def nvfp4_moe_quant_config(\n         \"nvfp4\",\n         w1_scale=w1_scale,\n         w2_scale=w2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_gscale=a1_gscale,\n         a2_gscale=a2_gscale,\n         g1_alphas=g1_alphas,", "patch_sha256": "eea235a7c7a12e3b"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2681263119", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/config.py\nReview Comment:\nDo you want me add a docstring for just `w1_bias` in current config `fp8_w8a8_moe_quant_config` only?\r\nThis change added`w1_bias` & `w2_bias` and passed to `FusedMoEQuantConfig.make`, both of which are well-documented.\r\nhttps://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/fused_moe/config.py#L467-L468\n\nOriginal Code Context:\n@@ -506,6 +510,8 @@ def fp8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n", "materials": ["Do you want me add a docstring for just `w1_bias` in current config `fp8_w8a8_moe_quant_config` only?\r\nThis change added`w1_bias` & `w2_bias` and passed to `FusedMoEQuantConfig.make`, both of which are well-documented.\r\nhttps://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/fused_moe/config.py#L467-L468"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -386,6 +386,10 @@ def use_mxfp4_w4a4(self) -> bool:\n     def use_nvfp4_w4a4(self) -> bool:\n         return self.quant_dtype == \"nvfp4\"\n \n+    @property\n+    def use_mxfp4_w4a8(self) -> bool:\n+        return self._a1.dtype == \"fp8\" and self._w1.dtype == \"mxfp4\"\n+\n     def config_name(self, dtype: torch.dtype) -> str | None:\n         \"\"\"\n         Return a string used to construct the filename that contains the\n@@ -532,6 +536,8 @@ def fp8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n     per_out_ch_quant: bool = False,\n     block_shape: list[int] | None = None,\n@@ -549,6 +555,8 @@ def fp8_w8a8_moe_quant_config(\n         g1_alphas=g1_alphas,\n         w2_scale=w2_scale,\n         g2_alphas=g2_alphas,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_scale=a1_scale,\n         a1_gscale=a1_gscale,\n         a2_scale=a2_scale,\n@@ -564,6 +572,8 @@ def int8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None,\n     a2_scale: torch.Tensor | None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n@@ -575,6 +585,8 @@ def int8_w8a8_moe_quant_config(\n         w2_scale=w2_scale,\n         a1_scale=a1_scale,\n         a2_scale=a2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         per_act_token_quant=per_act_token_quant,\n         per_out_ch_quant=False,\n         block_shape=None,\n@@ -654,6 +666,26 @@ def mxfp4_mxfp8_moe_quant_config(\n     )\n \n \n+def mxfp4_w4a8_moe_quant_config(\n+    w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    w2_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    a1_scale: torch.Tensor | None = None,\n+    a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n+    block_shape: list[int] | None = None,\n+) -> FusedMoEQuantConfig:\n+    \"\"\"\n+    Construct a quant config for fp8 activations and mxfp4 weights.\n+    \"\"\"\n+    return FusedMoEQuantConfig(\n+        _a1=FusedMoEQuantDesc(\"fp8\", None, a1_scale, None, None, None),\n+        _a2=FusedMoEQuantDesc(\"fp8\", None, a2_scale, None, None, None),\n+        _w1=FusedMoEQuantDesc(\"mxfp4\", None, w1_scale, None, None, w1_bias),\n+        _w2=FusedMoEQuantDesc(\"mxfp4\", None, w2_scale, None, None, w2_bias),\n+    )\n+\n+\n def ocp_mx_moe_quant_config(\n     quant_dtype: str,\n     w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n@@ -691,6 +723,8 @@ def nvfp4_moe_quant_config(\n     a2_gscale: torch.Tensor,\n     w1_scale: torch.Tensor,\n     w2_scale: torch.Tensor,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n     Construct a quant config for mxfp4 activations and nvp4 weights.\n@@ -699,6 +733,8 @@ def nvfp4_moe_quant_config(\n         \"nvfp4\",\n         w1_scale=w1_scale,\n         w2_scale=w2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_gscale=a1_gscale,\n         a2_gscale=a2_gscale,\n         g1_alphas=g1_alphas,", "patch_sha256": "eea235a7c7a12e3b"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2681288594", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\n### Missing OCP MX scheme support causes regression\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe new QDQ emulation blocks only handle `ocp_mx_scheme` values in `{\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}` or those ending with `\"a_fp8\"`, raising `NotImplementedError` for everything else. This breaks previously supported schemes like `w_mxfp4_a_mxfp6_e3m2`, `w_mxfp4_a_mxfp6_e2m3`, `w_mxfp6_e3m2_a_mxfp6_e3m2`, and `w_mxfp6_e2m3_a_mxfp6_e2m3`. These schemes previously worked by dequantizing weights and letting `moe_kernel_quantize_input` handle activation quantization, but now fail before reaching that call.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2149-L2169\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2234-L2254\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`vllm/model_executor/layers/fused_moe/fused_moe.py#L2234-L2254`](https://github.com/vllm-project/vllm/blob/10c2323242d035248d6c92143ce98dfb2b7560dd/vllm/model_executor/layers/fused_moe/fused_moe.py#L2234-L2254)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..J3eVYlx8vh23kPgHK43gWOxqQjZjj1KUWbixVwQRUYOr5nj9SfovtAh5v_RoMlnAtcgC6VH7ZbtCmxnoFsC0dyzN-FWJ4lam42n0bEONT9gJI9cjH594I1VuKBqbbyGDxYI77wdtwg7Ixc8EjluoTzaZVPeO8BRfyjq8KqgqmJ5rloiazZoHNOgtQmxomq9lgMi0ucEknMcOO8p8wq_Z2_prXCC1IBDriLPhcDg5N3SSsXw4auLKr9bOiPjjlaYbd8VK2jCR3LqnS-00e4EdKRtrLXKHSQwJjLeCrFOm_pnN6K8samiopvmJjSbfdUvD5l4728_m4tv1HWwJ-tDTEQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..GNstf-WTSYpW82Dil0BJIDyvkRwCIrY3aMP9OCITfALsdaKVNWSKZYhy0HAwRHjEADFqZS3dwgzdovqrHHKQ8yoLDU1bB8zkYy1h38toMGGHwXuoTx_JXOmylMt6mg4wT4cvUn5suh1Tx7sD2AnjQBmoddrSQ7C2weLK5DTvJdolKKaQRA8AWexWBdhXCqn3oioG7QdS1-XZ_Y1mQTMgMCEosHzY-JYebYZxdnE268tZgmZHOIIfJm2bq0Xqf5q_SyGKzRec6oExa_vnTENmfGwKF2I3tFqBm0ZvhsQcZlm_rEPG9KKQhOx_6h5xsgYDJt73VU7Fj8JeISiYPK4B-g\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -2150,10 +2146,32 @@ def fused_experts_impl(\n \n         curr_topk_ids = topk_ids[begin_chunk_idx:end_chunk_idx]\n         curr_topk_weights = topk_weights[begin_chunk_idx:end_chunk_idx]\n+\n+        if ocp_mx_scheme is not None:\n+            if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+                pass\n+            elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+                # perform QDQ (quantize and dequantize)\n+                # on activation for emulation purpose,\n+                # because of no native kernel for weight\n+                # in ocp_mx_scheme and activation in FP8.\n+                # However, the implementation is based on\n+                # existing non-emulation ops.\n+                qcurr_hidden_states, a1q_scale = ops.scaled_fp8_quant(\n+                    curr_hidden_states, a1_scale, use_per_token_if_dynamic=False\n+                )\n+                curr_hidden_states = per_tensor_dequantize(\n+                    qcurr_hidden_states, a1q_scale\n+                ).to(curr_hidden_states.dtype)\n+                input_quant_dtype = None\n+                a1_scale = None\n+            else:\n+                raise NotImplementedError(f\"Unsupported ocp_mx_scheme={ocp_mx_scheme}\")\n", "materials": ["### Missing OCP MX scheme support causes regression\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe new QDQ emulation blocks only handle `ocp_mx_scheme` values in `{\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}` or those ending with `\"a_fp8\"`, raising `NotImplementedError` for everything else. This breaks previously supported schemes like `w_mxfp4_a_mxfp6_e3m2`, `w_mxfp4_a_mxfp6_e2m3`, `w_mxfp6_e3m2_a_mxfp6_e3m2`, and `w_mxfp6_e2m3_a_mxfp6_e2m3`. These schemes previously worked by dequantizing weights and letting `moe_kernel_quantize_input` handle activation quantization, but now fail before reaching that call.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2149-L2169\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2234-L2254\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`vllm/model_executor/layers/fused_moe/fused_moe.py#L2234-L2254`](https://github.com/vllm-project/vllm/blob/10c2323242d035248d6c92143ce98dfb2b7560dd/vllm/model_executor/layers/fused_moe/fused_moe.py#L2234-L2254)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..J3eVYlx8vh23kPgHK43gWOxqQjZjj1KUWbixVwQRUYOr5nj9SfovtAh5v_RoMlnAtcgC6VH7ZbtCmxnoFsC0dyzN-FWJ4lam42n0bEONT9gJI9cjH594I1VuKBqbbyGDxYI77wdtwg7Ixc8EjluoTzaZVPeO8BRfyjq8KqgqmJ5rloiazZoHNOgtQmxomq9lgMi0ucEknMcOO8p8wq_Z2_prXCC1IBDriLPhcDg5N3SSsXw4auLKr9bOiPjjlaYbd8VK2jCR3LqnS-00e4EdKRtrLXKHSQwJjLeCrFOm_pnN6K8samiopvmJjSbfdUvD5l4728_m4tv1HWwJ-tDTEQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..GNstf-WTSYpW82Dil0BJIDyvkRwCIrY3aMP9OCITfALsdaKVNWSKZYhy0HAwRHjEADFqZS3dwgzdovqrHHKQ8yoLDU1bB8zkYy1h38toMGGHwXuoTx_JXOmylMt6mg4wT4cvUn5suh1Tx7sD2AnjQBmoddrSQ7C2weLK5DTvJdolKKaQRA8AWexWBdhXCqn3oioG7QdS1-XZ_Y1mQTMgMCEosHzY-JYebYZxdnE268tZgmZHOIIfJm2bq0Xqf5q_SyGKzRec6oExa_vnTENmfGwKF2I3tFqBm0ZvhsQcZlm_rEPG9KKQhOx_6h5xsgYDJt73VU7Fj8JeISiYPK4B-g\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2681313370", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nIn terms of model specific support, this need is initially for gpt-oss\r\nhttps://github.com/xuebwang-amd/vllm/blob/xuebin_add_quark_format_mapping_in_gpt_oss/vllm/model_executor/layers/fused_moe/layer.py#L268\n\nOriginal Code Context:\n@@ -584,6 +576,38 @@ def _get_quant_method() -> FusedMoEMethodBase:\n         # for heuristic purposes, so it must be initialized first.\n         self.quant_method: FusedMoEMethodBase = _get_quant_method()\n \n+        self.model_type = (\n+            self.vllm_config.model_config.hf_config.model_type\n+            if self.vllm_config.model_config is not None\n+            else None\n+        )\n+        self.is_mxfp4_quant = self._is_mxfp4_quant(quant_config, self.quant_method)\n+\n+        # Round up hidden size if needed.\n+        hidden_size, is_rounded_hidden_size = maybe_roundup_hidden_size(\n+            hidden_size,\n+            moe_in_dtype,\n+            self.moe_parallel_config,\n+            self.model_type,\n+            self.is_mxfp4_quant,\n+            is_lora_enabled=self.vllm_config.lora_config is not None,\n+        )\n+\n+        if is_rounded_hidden_size:\n+            self.hidden_size = hidden_size\n", "materials": ["In terms of model specific support, this need is initially for gpt-oss\r\nhttps://github.com/xuebwang-amd/vllm/blob/xuebin_add_quark_format_mapping_in_gpt_oss/vllm/model_executor/layers/fused_moe/layer.py#L268"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2681715259", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\n### Loop variable mutation corrupts scales for subsequent chunks\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe variables `input_quant_dtype`, `a1_scale`, and `a2_scale` are computed before the chunk loop but set to `None` inside the loop for `a_fp8` schemes. When processing batches larger than `CHUNK_SIZE` (16K tokens), the first chunk uses the correct static scales, but subsequent chunks see `None` values. This causes `ops.scaled_fp8_quant` to fall back to dynamic scaling for later chunks, resulting in inconsistent quantization behavior across chunks that could impact model accuracy.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2165-L2167\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2251-L2253\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`vllm/model_executor/layers/fused_moe/fused_moe.py#L2251-L2253`](https://github.com/vllm-project/vllm/blob/887c71600f256728821b565e75ea52235b58d445/vllm/model_executor/layers/fused_moe/fused_moe.py#L2251-L2253)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Yb7Qy0aQQ4kFcr8HIRc77SN701X--YZPY5ZcJvWwkJg9Rb85Lae5xHfvzvMx3ZMWyA0aQYs97n-OIRlBThF0e1Brry-ffrLrXYT_bvtGmd0iW8fClCS592zJ4CWe5Luqh_VVKq9glETkV-QJzIFFdQbYxHtAPMCwMWiDsncBZ4T5rjnHQjw1G419aKbPFDp2x7YWEwKgdXWZYiWRvZv3yepu5JVzxgfB-XWyMOknBzugOtmW8piDjHZ4zZeuUVbc1WqwJJxGvCBCbhr-cVS5stx1AlrEbDA21Do_MPKV6g34i0Y_hnNJrNEC3kdHfgqqjSfD0mKHKlEsgWC8FWYp8g\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..d0gACh8_AN7UG6g4hO8CPvQv6d-tV_sZZglFkItJ0kPJoUyAdQQjgPVdqojMi5ikrw6MJBUB2L9m8qn_5LsOQNEX0rvVpWdbS3621k2ZAqYDA_Yqo8WjoApIHtvv7TTAZDeiHXA1dqicGR_e1d-tYDYkqlWahpromGvKA0xSHI_0c7XXbiKcGgNSNLeIk7fDJ_-yTFOUxAH1jadMFK_9x67zv2bqvMfczSKIASDGD5dzDi-02LobXoaxZHFS2BgOtCTEgK43EKwe16nKlC5xWW95fhvxeMPGnJMLgbBO7ozBYNWIfZNgE4S2qp5eGuMOoiQaYRjBmRMIMNIhnV-c2A\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -2150,10 +2146,33 @@ def fused_experts_impl(\n \n         curr_topk_ids = topk_ids[begin_chunk_idx:end_chunk_idx]\n         curr_topk_weights = topk_weights[begin_chunk_idx:end_chunk_idx]\n+\n+        if ocp_mx_scheme is not None:\n+            if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+                pass\n+            elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+                # perform QDQ (quantize and dequantize)\n+                # on activation for emulation purpose,\n+                # because of no native kernel for weight\n+                # in ocp_mx_scheme and activation in FP8.\n+                # However, the implementation is based on\n+                # existing non-emulation ops.\n+                qcurr_hidden_states, a1q_scale = ops.scaled_fp8_quant(\n+                    curr_hidden_states, a1_scale, use_per_token_if_dynamic=False\n+                )\n+                curr_hidden_states = per_tensor_dequantize(\n+                    qcurr_hidden_states, a1q_scale\n+                ).to(curr_hidden_states.dtype)\n+                input_quant_dtype = None\n+                a1_scale = None\n", "materials": ["### Loop variable mutation corrupts scales for subsequent chunks\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe variables `input_quant_dtype`, `a1_scale`, and `a2_scale` are computed before the chunk loop but set to `None` inside the loop for `a_fp8` schemes. When processing batches larger than `CHUNK_SIZE` (16K tokens), the first chunk uses the correct static scales, but subsequent chunks see `None` values. This causes `ops.scaled_fp8_quant` to fall back to dynamic scaling for later chunks, resulting in inconsistent quantization behavior across chunks that could impact model accuracy.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2165-L2167\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2251-L2253\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`vllm/model_executor/layers/fused_moe/fused_moe.py#L2251-L2253`](https://github.com/vllm-project/vllm/blob/887c71600f256728821b565e75ea52235b58d445/vllm/model_executor/layers/fused_moe/fused_moe.py#L2251-L2253)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Yb7Qy0aQQ4kFcr8HIRc77SN701X--YZPY5ZcJvWwkJg9Rb85Lae5xHfvzvMx3ZMWyA0aQYs97n-OIRlBThF0e1Brry-ffrLrXYT_bvtGmd0iW8fClCS592zJ4CWe5Luqh_VVKq9glETkV-QJzIFFdQbYxHtAPMCwMWiDsncBZ4T5rjnHQjw1G419aKbPFDp2x7YWEwKgdXWZYiWRvZv3yepu5JVzxgfB-XWyMOknBzugOtmW8piDjHZ4zZeuUVbc1WqwJJxGvCBCbhr-cVS5stx1AlrEbDA21Do_MPKV6g34i0Y_hnNJrNEC3kdHfgqqjSfD0mKHKlEsgWC8FWYp8g\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..d0gACh8_AN7UG6g4hO8CPvQv6d-tV_sZZglFkItJ0kPJoUyAdQQjgPVdqojMi5ikrw6MJBUB2L9m8qn_5LsOQNEX0rvVpWdbS3621k2ZAqYDA_Yqo8WjoApIHtvv7TTAZDeiHXA1dqicGR_e1d-tYDYkqlWahpromGvKA0xSHI_0c7XXbiKcGgNSNLeIk7fDJ_-yTFOUxAH1jadMFK_9x67zv2bqvMfczSKIASDGD5dzDi-02LobXoaxZHFS2BgOtCTEgK43EKwe16nKlC5xWW95fhvxeMPGnJMLgbBO7ozBYNWIfZNgE4S2qp5eGuMOoiQaYRjBmRMIMNIhnV-c2A\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2681715263", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\n### Direct attribute access may crash on older PyTorch versions\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe code directly accesses `torch.float4_e2m1fn_x2` which will raise `AttributeError` on PyTorch versions that don't have this dtype. This is inconsistent with `quark_moe.py` line 669, which safely uses `getattr(torch, \"float4_e2m1fn_x2\", None)`. When quark quantization with `weight_dtype == \"mxfp4\"` is used on an older PyTorch version, the comparison `quant_method.fp4_dtype == torch.float4_e2m1fn_x2` will crash instead of gracefully returning False.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/layer.py#L889-L890\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..NvrqB5j_FCTw_o1uep0ljltZPCtfXzeoF_Mx0k-AVt7gNuTawrmuqGhzSS0H3U_l5THJlsrqttntKxquEAodohDo67dye87H5HZd7_ewR0ZfYY8wJrOQAN6VFz5B55dFq_A027e1BWok5D_ZwOHhA8tI8MqrgJBPy5ioSn4q2ka6nOQyIUdMjhF2_38hcF_wjqmP9AC0QEmvDl9WVhRdu1-lbHLJ0TsUd78xLNKdMihN71-tfiTqd_CD91H5RXa7X9rdlGMaEbSRdougZ6TtiKVPGvXVgOJqG9vwrQNzsMg9zoSJpoY9O3ZLteprLFb_fpl21vVb2hzosOkqbyP8pw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..M9WJMe0NcMLtSng218O3PrGnjpd9ceXUswn8Wqo96ZyowgEh9iv_P5VgRggI4H2bwZADBjfm6vRMXFOLNMgwmZ1ipr1Sl1GLUIYLQqFCxZGYlgmpjETjqF6qDORAc0RB3WEP1cj-i35a11jEapu7L5IsKI3UKANt5-HgIL_SVXNdTQ-ixyWMoib8PjeLGSfpeDC_16jMTOKy_jHo4h8_puT-i0fLlulSb_7XJGshoDhzU09A4-0cPnbvPek5VWl2qncSOY-xqEX9srAYzdNJ90VUoHtyRsb9q_IXmlc_bmAT9qFaELSTSQwHIrKDgoOQpdVoVexepgzZkCZ8jjwIwQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -844,6 +868,30 @@ def update_expert_map(self):\n                     dp_size=get_dp_group().world_size,\n                 )\n \n+    def _is_mxfp4_quant(\n+        self,\n+        quant_config: QuantizationConfig | None,\n+        quant_method: FusedMoEMethodBase,\n+    ) -> bool:\n+        from vllm.model_executor.layers.quantization.quark.quark_moe import (\n+            QuarkOCP_MX_MoEMethod,\n+        )\n+\n+        if not quant_config:\n+            return False\n+\n+        name = quant_config.get_name()\n+        if name == \"mxfp4\":\n+            return True\n+\n+        if name == \"quark\" and isinstance(quant_method, QuarkOCP_MX_MoEMethod):\n+            return (\n+                quant_method.weight_dtype == \"mxfp4\"\n+                and quant_method.fp4_dtype == torch.float4_e2m1fn_x2\n", "materials": ["### Direct attribute access may crash on older PyTorch versions\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe code directly accesses `torch.float4_e2m1fn_x2` which will raise `AttributeError` on PyTorch versions that don't have this dtype. This is inconsistent with `quark_moe.py` line 669, which safely uses `getattr(torch, \"float4_e2m1fn_x2\", None)`. When quark quantization with `weight_dtype == \"mxfp4\"` is used on an older PyTorch version, the comparison `quant_method.fp4_dtype == torch.float4_e2m1fn_x2` will crash instead of gracefully returning False.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/layer.py#L889-L890\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..NvrqB5j_FCTw_o1uep0ljltZPCtfXzeoF_Mx0k-AVt7gNuTawrmuqGhzSS0H3U_l5THJlsrqttntKxquEAodohDo67dye87H5HZd7_ewR0ZfYY8wJrOQAN6VFz5B55dFq_A027e1BWok5D_ZwOHhA8tI8MqrgJBPy5ioSn4q2ka6nOQyIUdMjhF2_38hcF_wjqmP9AC0QEmvDl9WVhRdu1-lbHLJ0TsUd78xLNKdMihN71-tfiTqd_CD91H5RXa7X9rdlGMaEbSRdougZ6TtiKVPGvXVgOJqG9vwrQNzsMg9zoSJpoY9O3ZLteprLFb_fpl21vVb2hzosOkqbyP8pw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..M9WJMe0NcMLtSng218O3PrGnjpd9ceXUswn8Wqo96ZyowgEh9iv_P5VgRggI4H2bwZADBjfm6vRMXFOLNMgwmZ1ipr1Sl1GLUIYLQqFCxZGYlgmpjETjqF6qDORAc0RB3WEP1cj-i35a11jEapu7L5IsKI3UKANt5-HgIL_SVXNdTQ-ixyWMoib8PjeLGSfpeDC_16jMTOKy_jHo4h8_puT-i0fLlulSb_7XJGshoDhzU09A4-0cPnbvPek5VWl2qncSOY-xqEX9srAYzdNJ90VUoHtyRsb9q_IXmlc_bmAT9qFaELSTSQwHIrKDgoOQpdVoVexepgzZkCZ8jjwIwQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2681869998", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\n### Static FP8 scales corrupted across chunked loop iterations\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nWhen using `ocp_mx_scheme.endswith(\"a_fp8\")`, the variables `a1_scale`, `a2_scale`, and `input_quant_dtype` are set to `None` inside the chunking loop. This causes inconsistent quantization behavior: the first chunk uses the provided static scales for FP8 QDQ emulation, but subsequent chunks have `None` scales, triggering dynamic per-tensor quantization via `scaled_fp8_quant` instead. Since `a_fp8` schemes require static input scales (enforced in `QuarkOCP_MX_MoEMethod`), this inconsistency can degrade model accuracy when processing batches larger than `CHUNK_SIZE`.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2165-L2167\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2251-L2253\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`vllm/model_executor/layers/fused_moe/fused_moe.py#L2251-L2253`](https://github.com/vllm-project/vllm/blob/1bece5dbcf8f264af6976fd1ee424ab10c39d84e/vllm/model_executor/layers/fused_moe/fused_moe.py#L2251-L2253)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Zzn9zQajQxddx5-lAMFzSSTHrYtaxKKRu8UXas3tI36rYn4kr3H35XFGM6uXD4NEuCtuW1jGkVbF-BtRc-PKRN5GiTnCYFBFu3PETYQTMlRep8hyODIHxfFztCuRlhBpOba2RW5tAhKtUbmz6bnSMhL3n8eq7AFzPpzCZybNkF1GJGkhKobuFgQykX5WqJfw2A8BtcQsaTE0q5DeES-TgPdxqMEzQM3uPbPDylZjkCJQ66JPMPxRC2YwlKUYoqYXU3sTBbPaCk2pBMU2P_Iarb9ZXsf2zSJABeXPfIXHQZFq-n4Pj9fqxcmtZ04CDkzb-SqQVl2cNxgvK7m-IdTloQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..ey5rmW65YjewG2c12zJko8KS46jSl4J0p24JgiP8-Ri1haZUD712KzQ5bEjFyrssWX_UbqwMo0xw_gjnXMnreWAm3wo6upWr1_y3h_0DYSMZqb2fXgfRwmiD_Llxe2GBtEO3MF0LpX3ohcdmP_zIGgmPx7lmFKhqgtYwjdq8V5soaZAehjlR_2ZdME_3xWW7FMSvA7v8qv3a2XcXV4nf3Twku4gL7No_zmbfgq32O3mGWbCR132tlZc-RJYRZ2OPeHta5hnbsNJAY4hXWbIh6brNJhu0ZK31ljapzwzHBrBg0qbz6Zufpie9m2SitqpgJO_P5ZUUcSNI48W_ACe_Ug\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -2150,10 +2146,33 @@ def fused_experts_impl(\n \n         curr_topk_ids = topk_ids[begin_chunk_idx:end_chunk_idx]\n         curr_topk_weights = topk_weights[begin_chunk_idx:end_chunk_idx]\n+\n+        if ocp_mx_scheme is not None:\n+            if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+                pass\n+            elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+                # perform QDQ (quantize and dequantize)\n+                # on activation for emulation purpose,\n+                # because of no native kernel for weight\n+                # in ocp_mx_scheme and activation in FP8.\n+                # However, the implementation is based on\n+                # existing non-emulation ops.\n+                qcurr_hidden_states, a1q_scale = ops.scaled_fp8_quant(\n+                    curr_hidden_states, a1_scale, use_per_token_if_dynamic=False\n+                )\n+                curr_hidden_states = per_tensor_dequantize(\n+                    qcurr_hidden_states, a1q_scale\n+                ).to(curr_hidden_states.dtype)\n+                input_quant_dtype = None\n+                a1_scale = None\n", "materials": ["### Static FP8 scales corrupted across chunked loop iterations\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nWhen using `ocp_mx_scheme.endswith(\"a_fp8\")`, the variables `a1_scale`, `a2_scale`, and `input_quant_dtype` are set to `None` inside the chunking loop. This causes inconsistent quantization behavior: the first chunk uses the provided static scales for FP8 QDQ emulation, but subsequent chunks have `None` scales, triggering dynamic per-tensor quantization via `scaled_fp8_quant` instead. Since `a_fp8` schemes require static input scales (enforced in `QuarkOCP_MX_MoEMethod`), this inconsistency can degrade model accuracy when processing batches larger than `CHUNK_SIZE`.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2165-L2167\nvllm/model_executor/layers/fused_moe/fused_moe.py#L2251-L2253\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`vllm/model_executor/layers/fused_moe/fused_moe.py#L2251-L2253`](https://github.com/vllm-project/vllm/blob/1bece5dbcf8f264af6976fd1ee424ab10c39d84e/vllm/model_executor/layers/fused_moe/fused_moe.py#L2251-L2253)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Zzn9zQajQxddx5-lAMFzSSTHrYtaxKKRu8UXas3tI36rYn4kr3H35XFGM6uXD4NEuCtuW1jGkVbF-BtRc-PKRN5GiTnCYFBFu3PETYQTMlRep8hyODIHxfFztCuRlhBpOba2RW5tAhKtUbmz6bnSMhL3n8eq7AFzPpzCZybNkF1GJGkhKobuFgQykX5WqJfw2A8BtcQsaTE0q5DeES-TgPdxqMEzQM3uPbPDylZjkCJQ66JPMPxRC2YwlKUYoqYXU3sTBbPaCk2pBMU2P_Iarb9ZXsf2zSJABeXPfIXHQZFq-n4Pj9fqxcmtZ04CDkzb-SqQVl2cNxgvK7m-IdTloQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..ey5rmW65YjewG2c12zJko8KS46jSl4J0p24JgiP8-Ri1haZUD712KzQ5bEjFyrssWX_UbqwMo0xw_gjnXMnreWAm3wo6upWr1_y3h_0DYSMZqb2fXgfRwmiD_Llxe2GBtEO3MF0LpX3ohcdmP_zIGgmPx7lmFKhqgtYwjdq8V5soaZAehjlR_2ZdME_3xWW7FMSvA7v8qv3a2XcXV4nf3Twku4gL7No_zmbfgq32O3mGWbCR132tlZc-RJYRZ2OPeHta5hnbsNJAY4hXWbIh6brNJhu0ZK31ljapzwzHBrBg0qbz6Zufpie9m2SitqpgJO_P5ZUUcSNI48W_ACe_Ug\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2685403197", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\n### Tensor indexing with None causes incorrect behavior\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe type annotation for `expert_id` was changed from `int` to `int | None` with default `None` in `_load_single_value` and `_load_per_tensor_weight_scale`, but the implementations still use `param_data[expert_id]` for indexing. In PyTorch, `tensor[None]` adds a dimension rather than being a no-op, so if `expert_id` is `None` and these paths are reached, the indexing would produce incorrect results or errors. While the current GPT-OSS loading paths return early via `mxfp4_or_bias=True` before hitting this code, the type change creates a latent bug for future callers.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/layer.py#L1127-L1128\nvllm/model_executor/layers/fused_moe/layer.py#L991-L992\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`vllm/model_executor/layers/fused_moe/layer.py#L991-L992`](https://github.com/vllm-project/vllm/blob/41c90cce0a6b9c573c5eca070be74f929073b75b/vllm/model_executor/layers/fused_moe/layer.py#L991-L992)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..O5GpHT4KMuq2PtwBW7o9WQ_7pYnmzzjULodT_TioqKx_txSNaUirCokL5PjnPmgbuQ6AnynrJt3WXW5xbKrWOLTKwyb-T3IOmPDcAFAci4PHldUoMLv4CFDHPsXUDXzg0q2t7ruRnwDGhXsgHvki1Y-8LSGM4spLJ0HyIN9FlbkaSotwwWAktdXrAIzSp3calZ50U4CqXpbHYAVvuVqh2jb73MH-gocFGS9JpDUsAwu1tfY6_GW9DB56sjfTpY3MnSd-xGcCgxUW6qEXHxrCF9ebqbuyErtILRWJHee-HHZsV-pqMIJXIy2liLBI6cMb2YyxMGXqpdb-zu8HSANtag\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..n6FeSLRIaEbP1RfrQNkDaOFvq2aY-OYxHJSoq6htckYsOA2uRzjMMbz2F1ELlJJsgyRxi866Cleuj9wKfspj9DWTiychhKkOdAgAprqvmr1Ad04vtiJQrKWZAo4kUz1Z2XkKb86GM7pKfdH2PSGXUeTlaa_rwbmBRkL2y9g4nJHpZoJpVWh3Ihd1K8O-aYflw4Q9ca9wIacpzE7yKPfpF_6iS1YJTxR6zUb11a5DAgQMZcNtINT9p9_QMDCfB8FfhVxMERobq6yMMUlt8P_LBxs-BUWmAHSe3dH-CN4Go1DhjV7bLABlUUiZDH1pDB-WIB2KMJKdS4s0uzWYSuv73A\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -1069,7 +1117,10 @@ def _load_w2(\n         expert_data.copy_(loaded_weight)\n \n     def _load_single_value(\n-        self, param: torch.nn.Parameter, loaded_weight: torch.Tensor, expert_id: int\n+        self,\n+        param: torch.nn.Parameter,\n+        loaded_weight: torch.Tensor,\n+        expert_id: int | None,\n", "materials": ["### Tensor indexing with None causes incorrect behavior\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe type annotation for `expert_id` was changed from `int` to `int | None` with default `None` in `_load_single_value` and `_load_per_tensor_weight_scale`, but the implementations still use `param_data[expert_id]` for indexing. In PyTorch, `tensor[None]` adds a dimension rather than being a no-op, so if `expert_id` is `None` and these paths are reached, the indexing would produce incorrect results or errors. While the current GPT-OSS loading paths return early via `mxfp4_or_bias=True` before hitting this code, the type change creates a latent bug for future callers.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/layer.py#L1127-L1128\nvllm/model_executor/layers/fused_moe/layer.py#L991-L992\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`vllm/model_executor/layers/fused_moe/layer.py#L991-L992`](https://github.com/vllm-project/vllm/blob/41c90cce0a6b9c573c5eca070be74f929073b75b/vllm/model_executor/layers/fused_moe/layer.py#L991-L992)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..O5GpHT4KMuq2PtwBW7o9WQ_7pYnmzzjULodT_TioqKx_txSNaUirCokL5PjnPmgbuQ6AnynrJt3WXW5xbKrWOLTKwyb-T3IOmPDcAFAci4PHldUoMLv4CFDHPsXUDXzg0q2t7ruRnwDGhXsgHvki1Y-8LSGM4spLJ0HyIN9FlbkaSotwwWAktdXrAIzSp3calZ50U4CqXpbHYAVvuVqh2jb73MH-gocFGS9JpDUsAwu1tfY6_GW9DB56sjfTpY3MnSd-xGcCgxUW6qEXHxrCF9ebqbuyErtILRWJHee-HHZsV-pqMIJXIy2liLBI6cMb2YyxMGXqpdb-zu8HSANtag\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..n6FeSLRIaEbP1RfrQNkDaOFvq2aY-OYxHJSoq6htckYsOA2uRzjMMbz2F1ELlJJsgyRxi866Cleuj9wKfspj9DWTiychhKkOdAgAprqvmr1Ad04vtiJQrKWZAo4kUz1Z2XkKb86GM7pKfdH2PSGXUeTlaa_rwbmBRkL2y9g4nJHpZoJpVWh3Ihd1K8O-aYflw4Q9ca9wIacpzE7yKPfpF_6iS1YJTxR6zUb11a5DAgQMZcNtINT9p9_QMDCfB8FfhVxMERobq6yMMUlt8P_LBxs-BUWmAHSe3dH-CN4Go1DhjV7bLABlUUiZDH1pDB-WIB2KMJKdS4s0uzWYSuv73A\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2685654060", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nmove to `moe_kernel_quantize_input`. @robertgshaw2-redhat @fxmarty-amd \n\nOriginal Code Context:\n@@ -2150,10 +2146,32 @@ def fused_experts_impl(\n \n         curr_topk_ids = topk_ids[begin_chunk_idx:end_chunk_idx]\n         curr_topk_weights = topk_weights[begin_chunk_idx:end_chunk_idx]\n+\n+        if ocp_mx_scheme is not None:\n", "materials": ["move to `moe_kernel_quantize_input`. @robertgshaw2-redhat @fxmarty-amd "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2685654568", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nmove to `moe_kernel_quantize_input`. @robertgshaw2-redhat @fxmarty-amd \n\nOriginal Code Context:\n@@ -2214,10 +2232,31 @@ def fused_experts_impl(\n         else:\n             raise ValueError(f\"Unsupported FusedMoe activation: {activation}.\")\n \n+        if ocp_mx_scheme is not None:\n", "materials": ["move to `moe_kernel_quantize_input`. @robertgshaw2-redhat @fxmarty-amd "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2685665138", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\n### Inconsistent return type violates function contract\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `weight_loader` method returns `False` unconditionally at line 1235, but the return type is `bool | None` and the convention throughout the function (see line 1249) is to return `False if return_success else None`. When `return_success=False` (the default), the function should return `None` per its type contract. This inconsistency could cause unexpected behavior if callers check for `None` to determine if a weight loading operation was attempted. The pattern `return False if return_success else None` is used correctly elsewhere in the same function at line 1249.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/layer.py#L1234-L1235\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..w89IkbTid81kQV28CwQtXzo0RH2JTgkgZzyx3j5PDlYS8sCiczjN5X7DAnu5--LnlPqc3pSN3sJrT0J1W6cFNQ7EMf6EIXad_I5EbIj-s6Y8p61yoWxLpNZC0YP7Y2beJl5aVdhJsnRyEDoYlfEVTXRSdTnJ47KR75e4KAJrIwYqCobjcEMjiJ78b2MoSb-tXqtxbRvtmIg4mtkufRhKslFsVB1r0TQSbvegwocwfm5D5pKrWEHITXZcjVUWice0DoPQUeH4j11Iy726Yyqx_5E7FU2djrr6QYtjKbRfWg0WHtnsPbu-P2Ok9UUwEXv_AwDNRcmH_ZaSNANrezVxyA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..VPp4QX6vOWcm7GFQZ33m0p4s9XYM1Z1yHCpnXE78TOHAiNGjEMBVPnLhdCGWTTkWYM3ZwKFcRlxRFS5BeciSSh62Kb-gchIg-Is6evb3FjdbxEJOHfWhU0bmeqJYkMKggHQiNQvljS80Zq28qJLxWvokc4jYihEygCd5WVZV71os9za2ILGcTJQWv7N_PD2ciKvpAMTzS_yyPo8pK0BbYRcIE6-D_6cwq5gSmUQk9Q5aKx1NukRntIY59K0gyUrjQcWMs0vzFvUMIlcvViKNaCXWNujj_t_nzGMvO-vhEFt6XGPqS9R0LCxgJY39HiVFa4zx-uClI7iH_seJxHhR7g\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -1145,23 +1202,42 @@ def weight_loader(\n         loaded_weight: torch.Tensor,\n         weight_name: str,\n         shard_id: str,\n-        expert_id: int,\n+        expert_id: int | None = None,\n         return_success: bool = False,\n+        mxfp4_or_bias: bool | None = False,\n     ) -> bool | None:\n-        if self.quant_config and self.quant_config.get_name() == \"mxfp4\":\n-            # (FIXME) for gpt-oss all experts are combined\n-            if \"bias\" in weight_name:\n-                dim1 = loaded_weight.shape[1]\n-                param.data[:, :dim1].copy_(loaded_weight)\n-            else:\n-                dim1 = loaded_weight.shape[1]\n-                dim2 = loaded_weight.shape[2]\n-                param.data[:, :dim1, :dim2].copy_(loaded_weight)\n-            return True if return_success else None\n+        # TODO (xuebwang-amd): further unification without specifying the model type?\n+        if (\n+            self.quant_config is not None\n+            and self.model_type == \"gpt_oss\"\n+            and mxfp4_or_bias\n+        ):\n+            if any(\n+                self.quant_config.get_name() == quant_name\n+                for quant_name in [\"mxfp4\", \"quark\"]\n+            ):\n+                if expert_id is None:\n+                    dim1 = loaded_weight.shape[1]\n+                    if \"bias\" in weight_name:\n+                        param.data[:, :dim1].copy_(loaded_weight)\n+                    else:\n+                        dim2 = loaded_weight.shape[2]\n+                        param.data[:, :dim1, :dim2].copy_(loaded_weight)\n+                else:\n+                    expert_data = param.data[expert_id]\n+                    dim1 = loaded_weight.shape[0]\n+                    if \"bias\" in weight_name:\n+                        expert_data.data[:dim1].copy_(loaded_weight)\n+                    else:\n+                        dim2 = loaded_weight.shape[1]\n+                        expert_data.data[:dim1, :dim2].copy_(loaded_weight)\n+                return True if return_success else None\n+            return False\n", "materials": ["### Inconsistent return type violates function contract\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `weight_loader` method returns `False` unconditionally at line 1235, but the return type is `bool | None` and the convention throughout the function (see line 1249) is to return `False if return_success else None`. When `return_success=False` (the default), the function should return `None` per its type contract. This inconsistency could cause unexpected behavior if callers check for `None` to determine if a weight loading operation was attempted. The pattern `return False if return_success else None` is used correctly elsewhere in the same function at line 1249.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/layer.py#L1234-L1235\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..w89IkbTid81kQV28CwQtXzo0RH2JTgkgZzyx3j5PDlYS8sCiczjN5X7DAnu5--LnlPqc3pSN3sJrT0J1W6cFNQ7EMf6EIXad_I5EbIj-s6Y8p61yoWxLpNZC0YP7Y2beJl5aVdhJsnRyEDoYlfEVTXRSdTnJ47KR75e4KAJrIwYqCobjcEMjiJ78b2MoSb-tXqtxbRvtmIg4mtkufRhKslFsVB1r0TQSbvegwocwfm5D5pKrWEHITXZcjVUWice0DoPQUeH4j11Iy726Yyqx_5E7FU2djrr6QYtjKbRfWg0WHtnsPbu-P2Ok9UUwEXv_AwDNRcmH_ZaSNANrezVxyA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..VPp4QX6vOWcm7GFQZ33m0p4s9XYM1Z1yHCpnXE78TOHAiNGjEMBVPnLhdCGWTTkWYM3ZwKFcRlxRFS5BeciSSh62Kb-gchIg-Is6evb3FjdbxEJOHfWhU0bmeqJYkMKggHQiNQvljS80Zq28qJLxWvokc4jYihEygCd5WVZV71os9za2ILGcTJQWv7N_PD2ciKvpAMTzS_yyPo8pK0BbYRcIE6-D_6cwq5gSmUQk9Q5aKx1NukRntIY59K0gyUrjQcWMs0vzFvUMIlcvViKNaCXWNujj_t_nzGMvO-vhEFt6XGPqS9R0LCxgJY39HiVFa4zx-uClI7iH_seJxHhR7g\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2685665146", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/config.py\nReview Comment:\n### Unused `block_shape` parameter silently ignored\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `mxfp4_w4a8_moe_quant_config` function accepts a `block_shape` parameter but never uses it in the function body. Unlike similar functions like `fp8_w8a8_moe_quant_config` that properly pass `block_shape` to `FusedMoEQuantConfig.make()`, this function silently ignores the parameter. Callers passing a non-None `block_shape` value would expect it to affect quantization behavior, but it has no effect, leading to silently incorrect configurations.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/config.py#L646-L664\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..kvc0V-hBX07Fp6er3lj3kZOBxhBvTFLZfjaUfkeytTwy3mxbzDTw7JgX2esbGcrHCiTgu7jAPXmQwL9Kgx-bgf3EvsNhXwv-Fw9LwToApNGZvi4wehjUYmUVQI0369yQg0BMmF7Hi7wdTHHReyBN0yFCoWzBKyW4b_ZrZ7-s8MTY5rCnI1bKGvgYrmmrlt-WFAECNypBpQWt_uTHjMala46V7WB43GNaHyEAQ9AjCtGv7Gu6R1lQTHv9nmllocBMjSLLb6cKpsN6U9vybcHC2LeShLaj9u-lThnsd6QgMCzpzLRDYUJbRWCDA4yROAZEVQGOLKtpsNhX-jh-Uaqebg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..SVwuzJJhHtSZIlt-_BkOvb622LetrYE0bcaRhz_KpkTb_MIbtS_hNZSUZT2tvbQyVcoRSw80c3kAJ6Uf4LBEBA16XOBJPg8TxOFWsnKXuTg4SU979gzncAR9I7D4n5hVdZ93JwEu_ZnqVVG6HLNwxLD94Aa2Fv2bOni9d4kBXiOFMfdXIGA2bXvGvhs6TFePn_E1aQ8_Y_1WjTP3TEcDk3vUYN9NkqRjdNrtaPlAY-zIPjLivj_G4EkiHnwgUB0QsNhTL6yuG9DA4hGiPfgiqum2KtNTqJJJUyw7Wf_epx-_MGnDf8oeXmMWE9T48v09Aziod4wZWNiPdf3sTfKkVA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -632,6 +644,26 @@ def mxfp4_mxfp8_moe_quant_config(\n     )\n \n \n+def mxfp4_w4a8_moe_quant_config(\n+    w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    w2_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    a1_scale: torch.Tensor | None = None,\n+    a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n+    block_shape: list[int] | None = None,\n+) -> FusedMoEQuantConfig:\n+    \"\"\"\n+    Construct a quant config for fp8 activations and mxfp4 weights.\n+    \"\"\"\n+    return FusedMoEQuantConfig(\n+        _a1=FusedMoEQuantDesc(\"fp8\", None, a1_scale, None, None, None),\n+        _a2=FusedMoEQuantDesc(\"fp8\", None, a2_scale, None, None, None),\n+        _w1=FusedMoEQuantDesc(\"mxfp4\", None, w1_scale, None, None, w1_bias),\n+        _w2=FusedMoEQuantDesc(\"mxfp4\", None, w2_scale, None, None, w2_bias),\n+    )\n", "materials": ["### Unused `block_shape` parameter silently ignored\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `mxfp4_w4a8_moe_quant_config` function accepts a `block_shape` parameter but never uses it in the function body. Unlike similar functions like `fp8_w8a8_moe_quant_config` that properly pass `block_shape` to `FusedMoEQuantConfig.make()`, this function silently ignores the parameter. Callers passing a non-None `block_shape` value would expect it to affect quantization behavior, but it has no effect, leading to silently incorrect configurations.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/config.py#L646-L664\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..kvc0V-hBX07Fp6er3lj3kZOBxhBvTFLZfjaUfkeytTwy3mxbzDTw7JgX2esbGcrHCiTgu7jAPXmQwL9Kgx-bgf3EvsNhXwv-Fw9LwToApNGZvi4wehjUYmUVQI0369yQg0BMmF7Hi7wdTHHReyBN0yFCoWzBKyW4b_ZrZ7-s8MTY5rCnI1bKGvgYrmmrlt-WFAECNypBpQWt_uTHjMala46V7WB43GNaHyEAQ9AjCtGv7Gu6R1lQTHv9nmllocBMjSLLb6cKpsN6U9vybcHC2LeShLaj9u-lThnsd6QgMCzpzLRDYUJbRWCDA4yROAZEVQGOLKtpsNhX-jh-Uaqebg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..SVwuzJJhHtSZIlt-_BkOvb622LetrYE0bcaRhz_KpkTb_MIbtS_hNZSUZT2tvbQyVcoRSw80c3kAJ6Uf4LBEBA16XOBJPg8TxOFWsnKXuTg4SU979gzncAR9I7D4n5hVdZ93JwEu_ZnqVVG6HLNwxLD94Aa2Fv2bOni9d4kBXiOFMfdXIGA2bXvGvhs6TFePn_E1aQ8_Y_1WjTP3TEcDk3vUYN9NkqRjdNrtaPlAY-zIPjLivj_G4EkiHnwgUB0QsNhTL6yuG9DA4hGiPfgiqum2KtNTqJJJUyw7Wf_epx-_MGnDf8oeXmMWE9T48v09Aziod4wZWNiPdf3sTfKkVA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -386,6 +386,10 @@ def use_mxfp4_w4a4(self) -> bool:\n     def use_nvfp4_w4a4(self) -> bool:\n         return self.quant_dtype == \"nvfp4\"\n \n+    @property\n+    def use_mxfp4_w4a8(self) -> bool:\n+        return self._a1.dtype == \"fp8\" and self._w1.dtype == \"mxfp4\"\n+\n     def config_name(self, dtype: torch.dtype) -> str | None:\n         \"\"\"\n         Return a string used to construct the filename that contains the\n@@ -532,6 +536,8 @@ def fp8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n     per_out_ch_quant: bool = False,\n     block_shape: list[int] | None = None,\n@@ -549,6 +555,8 @@ def fp8_w8a8_moe_quant_config(\n         g1_alphas=g1_alphas,\n         w2_scale=w2_scale,\n         g2_alphas=g2_alphas,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_scale=a1_scale,\n         a1_gscale=a1_gscale,\n         a2_scale=a2_scale,\n@@ -564,6 +572,8 @@ def int8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None,\n     a2_scale: torch.Tensor | None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n@@ -575,6 +585,8 @@ def int8_w8a8_moe_quant_config(\n         w2_scale=w2_scale,\n         a1_scale=a1_scale,\n         a2_scale=a2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         per_act_token_quant=per_act_token_quant,\n         per_out_ch_quant=False,\n         block_shape=None,\n@@ -654,6 +666,26 @@ def mxfp4_mxfp8_moe_quant_config(\n     )\n \n \n+def mxfp4_w4a8_moe_quant_config(\n+    w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    w2_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    a1_scale: torch.Tensor | None = None,\n+    a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n+    block_shape: list[int] | None = None,\n+) -> FusedMoEQuantConfig:\n+    \"\"\"\n+    Construct a quant config for fp8 activations and mxfp4 weights.\n+    \"\"\"\n+    return FusedMoEQuantConfig(\n+        _a1=FusedMoEQuantDesc(\"fp8\", None, a1_scale, None, None, None),\n+        _a2=FusedMoEQuantDesc(\"fp8\", None, a2_scale, None, None, None),\n+        _w1=FusedMoEQuantDesc(\"mxfp4\", None, w1_scale, None, None, w1_bias),\n+        _w2=FusedMoEQuantDesc(\"mxfp4\", None, w2_scale, None, None, w2_bias),\n+    )\n+\n+\n def ocp_mx_moe_quant_config(\n     quant_dtype: str,\n     w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n@@ -691,6 +723,8 @@ def nvfp4_moe_quant_config(\n     a2_gscale: torch.Tensor,\n     w1_scale: torch.Tensor,\n     w2_scale: torch.Tensor,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n     Construct a quant config for mxfp4 activations and nvp4 weights.\n@@ -699,6 +733,8 @@ def nvfp4_moe_quant_config(\n         \"nvfp4\",\n         w1_scale=w1_scale,\n         w2_scale=w2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_gscale=a1_gscale,\n         a2_gscale=a2_gscale,\n         g1_alphas=g1_alphas,", "patch_sha256": "eea235a7c7a12e3b"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2686060328", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\n### is_rounded_hidden_size flag returns True without rounding\n\n**Low Severity**\n\n<!-- DESCRIPTION START -->\nThe `maybe_roundup_hidden_size` function returns `(hidden_size, True)` whenever `model_type == \"gpt_oss\"` and `is_mxfp4_quant` is true, regardless of whether the inner `if/elif` conditions for backend types actually match and rounding occurred. If the backend type doesn't match any of the specified conditions (e.g., if a new backend is added), no rounding happens but the function still returns `True` for `is_rounded_hidden_size`, which is semantically incorrect and could cause unnecessary `FusedMoEConfig` recreation in the caller.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/layer.py#L286-L287\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..JePO5OIYimwaSHyooQHGt0aa1b40x_HNXqjOmXKcN4enxSBcNxOPa04ZBSbDl3paYdKOL_43BdzbOPuB96CUB-kLe6Imustji48Upwmggz5x7lSzG6nFtFntbnt7b0Ubz7X-NMxfGozmbQlJN8WEGCeUevY0IiEGSwPb4UaSpapwRJ_D-4JmtdINK2jPdH-ja1XagcpmjBES7sYdcs0NzZoLY3J-Z4ORbraR_XvLB7P-4Y_j1WmyJBIuMlHr3M_q0HillOQGLeK2_qd6XdYQHdY4GVT8uStDvt0Ae9JOsgz7Q-nZ1632jIuxUqI6KjD-6hUbFL-7W3vOg1xCrC4deA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..pXVYq-mzRHy8wfASPTpwYX6_tzlY7NUzndN0680y5hhRjGMip47q2U3qset_PENLHVTegESZ48v1XBtG8TMKiOCuQTI5mE4M-HdltMUMDhya52QNpc9AG_tPoi_16FlN-f2BrpaUkBydhJJFE4FBeyVYAjqOw9dpfJjN2FHXnos2YMkyZWX1uamI5PFrXJdf4Kt03VZnENuN_7FblC7vPM5YBJbM6T8M6KcM_WhMIGIGTg7UezRiMIr6btuGQQM4qM_GMaezcHP6D-WGOlQ5BGNBF128EgXAv99qu0KfudqpY-Ah8CbSz72_LPJcsXkbSa1jGAdowmIKyPzXjqqIcw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -284,8 +284,9 @@ def maybe_roundup_hidden_size(\n             or current_mxfp4_backend == Mxfp4Backend.SM100_FI_MXFP4_BF16\n         ):\n             hidden_size = round_up(hidden_size, 256)\n+        return hidden_size, True\n", "materials": ["### is_rounded_hidden_size flag returns True without rounding\n\n**Low Severity**\n\n<!-- DESCRIPTION START -->\nThe `maybe_roundup_hidden_size` function returns `(hidden_size, True)` whenever `model_type == \"gpt_oss\"` and `is_mxfp4_quant` is true, regardless of whether the inner `if/elif` conditions for backend types actually match and rounding occurred. If the backend type doesn't match any of the specified conditions (e.g., if a new backend is added), no rounding happens but the function still returns `True` for `is_rounded_hidden_size`, which is semantically incorrect and could cause unnecessary `FusedMoEConfig` recreation in the caller.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/layer.py#L286-L287\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..JePO5OIYimwaSHyooQHGt0aa1b40x_HNXqjOmXKcN4enxSBcNxOPa04ZBSbDl3paYdKOL_43BdzbOPuB96CUB-kLe6Imustji48Upwmggz5x7lSzG6nFtFntbnt7b0Ubz7X-NMxfGozmbQlJN8WEGCeUevY0IiEGSwPb4UaSpapwRJ_D-4JmtdINK2jPdH-ja1XagcpmjBES7sYdcs0NzZoLY3J-Z4ORbraR_XvLB7P-4Y_j1WmyJBIuMlHr3M_q0HillOQGLeK2_qd6XdYQHdY4GVT8uStDvt0Ae9JOsgz7Q-nZ1632jIuxUqI6KjD-6hUbFL-7W3vOg1xCrC4deA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..pXVYq-mzRHy8wfASPTpwYX6_tzlY7NUzndN0680y5hhRjGMip47q2U3qset_PENLHVTegESZ48v1XBtG8TMKiOCuQTI5mE4M-HdltMUMDhya52QNpc9AG_tPoi_16FlN-f2BrpaUkBydhJJFE4FBeyVYAjqOw9dpfJjN2FHXnos2YMkyZWX1uamI5PFrXJdf4Kt03VZnENuN_7FblC7vPM5YBJbM6T8M6KcM_WhMIGIGTg7UezRiMIr6btuGQQM4qM_GMaezcHP6D-WGOlQ5BGNBF128EgXAv99qu0KfudqpY-Ah8CbSz72_LPJcsXkbSa1jGAdowmIKyPzXjqqIcw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2686060332", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\n### MXFP4 hidden_size rounding now restricted to gpt_oss only\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `maybe_roundup_hidden_size` function's condition changed from `quant_config.get_name() == \"mxfp4\"` to `model_type == \"gpt_oss\" and is_mxfp4_quant`. This restricts the MXFP4-specific hidden_size rounding (for EP buffer allocation) to only gpt_oss models. The original code applied this rounding to ALL mxfp4-quantized models. If any other model uses mxfp4 quantization with Expert Parallelism, it would no longer receive the necessary hidden_size padding, potentially causing buffer allocation issues or incorrect tensor shapes.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/layer.py#L268-L286\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..h-Yt6QmhhF8-V-9JWK2jEQ87S4MYKJNc8jh0ojp8i8IxYxovzjX5-mUSW5yoevg5rKA1c5DFOpTOBNPxzhhjB00R4l6zyVg8orguBgQHYtpp-MXCGlUA7HwYqhBnQDr8IGO-LkbbbPEJzJINMWg9YmmvADke4ou47gz-NI_eFyE7VW27tYzepl0SITCRUgnne7j5Pns0gk7fRXTQNnV7mqc_s9z0k43L0VhSdCQ9HkRChGRFRf7k1lehg_ad8FTWYC__i9ytkmJoFbEEcY6kG67YeG1VVokktI9xw75v2m6TVRtejK5f7YbbY222XOFHLY3zRrGdBuUTvhtV5VnYwQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..YgkDIMTFut7QH5diV6KSJTM5xa-7glVKKHVcEPYO77Eb3IYAgmd6_wn6_8l1ColYjlaOoVW4AQLVgxQh936k7yxxGwKSVCewPbalYg-6fC_t1HbUHlVd0eC56ot4sXXcNk0lwZkCijMUKTxV7znsuANYx7p8tc4l4r1Pl350wxfQLZd7ywSWuSg2IdpXtvuwmWYw5MlVcDJnbaFg7E9-S9SqAgJ0I1RkZcQXGm1tWYle9phtyZ3gsR9ruzg_OLjFDxZ5NhYyqd1bK4USx8VRg5qS1haukOsWoAXrmvnLU6y4JyuT6HQqiSz33yFpLDdkeUGwlLFAxAXcO9H5AhQzIA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -266,7 +266,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type is not None and model_type == \"gpt_oss\" and is_mxfp4_quant:\n", "materials": ["### MXFP4 hidden_size rounding now restricted to gpt_oss only\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `maybe_roundup_hidden_size` function's condition changed from `quant_config.get_name() == \"mxfp4\"` to `model_type == \"gpt_oss\" and is_mxfp4_quant`. This restricts the MXFP4-specific hidden_size rounding (for EP buffer allocation) to only gpt_oss models. The original code applied this rounding to ALL mxfp4-quantized models. If any other model uses mxfp4 quantization with Expert Parallelism, it would no longer receive the necessary hidden_size padding, potentially causing buffer allocation issues or incorrect tensor shapes.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/layer.py#L268-L286\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..h-Yt6QmhhF8-V-9JWK2jEQ87S4MYKJNc8jh0ojp8i8IxYxovzjX5-mUSW5yoevg5rKA1c5DFOpTOBNPxzhhjB00R4l6zyVg8orguBgQHYtpp-MXCGlUA7HwYqhBnQDr8IGO-LkbbbPEJzJINMWg9YmmvADke4ou47gz-NI_eFyE7VW27tYzepl0SITCRUgnne7j5Pns0gk7fRXTQNnV7mqc_s9z0k43L0VhSdCQ9HkRChGRFRf7k1lehg_ad8FTWYC__i9ytkmJoFbEEcY6kG67YeG1VVokktI9xw75v2m6TVRtejK5f7YbbY222XOFHLY3zRrGdBuUTvhtV5VnYwQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..YgkDIMTFut7QH5diV6KSJTM5xa-7glVKKHVcEPYO77Eb3IYAgmd6_wn6_8l1ColYjlaOoVW4AQLVgxQh936k7yxxGwKSVCewPbalYg-6fC_t1HbUHlVd0eC56ot4sXXcNk0lwZkCijMUKTxV7znsuANYx7p8tc4l4r1Pl350wxfQLZd7ywSWuSg2IdpXtvuwmWYw5MlVcDJnbaFg7E9-S9SqAgJ0I1RkZcQXGm1tWYle9phtyZ3gsR9ruzg_OLjFDxZ5NhYyqd1bK4USx8VRg5qS1haukOsWoAXrmvnLU6y4JyuT6HQqiSz33yFpLDdkeUGwlLFAxAXcO9H5AhQzIA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2686060334", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\n### MXFP4 hidden_size rounding now restricted to gpt_oss only\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `maybe_roundup_hidden_size` function's condition changed from `quant_config.get_name() == \"mxfp4\"` to `model_type == \"gpt_oss\" and is_mxfp4_quant`. This restricts the MXFP4-specific hidden_size rounding (for EP buffer allocation) to only gpt_oss models. The original code applied this rounding to ALL mxfp4-quantized models. If any other model uses mxfp4 quantization with Expert Parallelism, it would no longer receive the necessary hidden_size padding, potentially causing buffer allocation issues or incorrect tensor shapes.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/layer.py#L268-L286\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..ZkYO_AyWk4ga98NaUhyHoZ5gud2VD38qSJsNC_C047dA3EGV1Z9kaODhtYEjje4uBDAHKb7R4qi5HvMoQQzIct_oxUQNH5X48TPw8idlp3gcdUy_G5A32D68fVutjjIPtf1VFVuWprw_e8lK78Z1BUr-zHA4XnumdtfgbbT2UI8WfmbZ5Wp6uYNp4n8wsTMM8tDlyAHBcDZYTo_FxbKRLL5uR5-lggNP37Y8JcVJ3HzW4dKgQu7P2t0lFAH_l-OOtRBGQj2AXLbmJKIZgVujn0w-bGVGNXLDkxBvFjOi0TpNIXUDNQ54cG21Bv9cxBftvzLggoYbDiZ_0D2dMTS8Rg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..p_-YATh4j0yv4bWTh_QkqrDzITcy5prxS8zyxbs9Ru02MqS9DfQyFyj3vEcMb15iWdT8cpPGwIC1jcmpPSrq3t4aIBLdt-827EQ4Zqx_vbvZqp4Elb4QoTdIYeA2fF9IRd7CVf_R_c-DQ9KT1TwOp8duUg11VpZLN3cIz9UiVBm7klx5IMc3lL5JtvsfW3aG2i5SMq-t5BNCsVojOr0V-RLILyZXHXIdpIqizY8PPlpUWJyaIw5tfQoWd9st6TYWIINTGAiToB9jBntW4ob936NQr99ObCdmvZqldAqwSoO8WzloHj1hug999tScrIx_sb3XDBqgIDWJU8OVEwY-Vw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -266,7 +266,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type is not None and model_type == \"gpt_oss\" and is_mxfp4_quant:\n", "materials": ["### MXFP4 hidden_size rounding now restricted to gpt_oss only\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `maybe_roundup_hidden_size` function's condition changed from `quant_config.get_name() == \"mxfp4\"` to `model_type == \"gpt_oss\" and is_mxfp4_quant`. This restricts the MXFP4-specific hidden_size rounding (for EP buffer allocation) to only gpt_oss models. The original code applied this rounding to ALL mxfp4-quantized models. If any other model uses mxfp4 quantization with Expert Parallelism, it would no longer receive the necessary hidden_size padding, potentially causing buffer allocation issues or incorrect tensor shapes.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/model_executor/layers/fused_moe/layer.py#L268-L286\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..ZkYO_AyWk4ga98NaUhyHoZ5gud2VD38qSJsNC_C047dA3EGV1Z9kaODhtYEjje4uBDAHKb7R4qi5HvMoQQzIct_oxUQNH5X48TPw8idlp3gcdUy_G5A32D68fVutjjIPtf1VFVuWprw_e8lK78Z1BUr-zHA4XnumdtfgbbT2UI8WfmbZ5Wp6uYNp4n8wsTMM8tDlyAHBcDZYTo_FxbKRLL5uR5-lggNP37Y8JcVJ3HzW4dKgQu7P2t0lFAH_l-OOtRBGQj2AXLbmJKIZgVujn0w-bGVGNXLDkxBvFjOi0TpNIXUDNQ54cG21Bv9cxBftvzLggoYbDiZ_0D2dMTS8Rg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..p_-YATh4j0yv4bWTh_QkqrDzITcy5prxS8zyxbs9Ru02MqS9DfQyFyj3vEcMb15iWdT8cpPGwIC1jcmpPSrq3t4aIBLdt-827EQ4Zqx_vbvZqp4Elb4QoTdIYeA2fF9IRd7CVf_R_c-DQ9KT1TwOp8duUg11VpZLN3cIz9UiVBm7klx5IMc3lL5JtvsfW3aG2i5SMq-t5BNCsVojOr0V-RLILyZXHXIdpIqizY8PPlpUWJyaIw5tfQoWd9st6TYWIINTGAiToB9jBntW4ob936NQr99ObCdmvZqldAqwSoO8WzloHj1hug999tScrIx_sb3XDBqgIDWJU8OVEwY-Vw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2710663392", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/config.py\nReview Comment:\nnws, this makes sense\r\n\r\nThis `FusedMoEQuantConfig` has gotten overloaded. We need to refactor this soon.\n\nOriginal Code Context:\n@@ -506,6 +510,8 @@ def fp8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n", "materials": ["nws, this makes sense\r\n\r\nThis `FusedMoEQuantConfig` has gotten overloaded. We need to refactor this soon."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -386,6 +386,10 @@ def use_mxfp4_w4a4(self) -> bool:\n     def use_nvfp4_w4a4(self) -> bool:\n         return self.quant_dtype == \"nvfp4\"\n \n+    @property\n+    def use_mxfp4_w4a8(self) -> bool:\n+        return self._a1.dtype == \"fp8\" and self._w1.dtype == \"mxfp4\"\n+\n     def config_name(self, dtype: torch.dtype) -> str | None:\n         \"\"\"\n         Return a string used to construct the filename that contains the\n@@ -532,6 +536,8 @@ def fp8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n     per_out_ch_quant: bool = False,\n     block_shape: list[int] | None = None,\n@@ -549,6 +555,8 @@ def fp8_w8a8_moe_quant_config(\n         g1_alphas=g1_alphas,\n         w2_scale=w2_scale,\n         g2_alphas=g2_alphas,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_scale=a1_scale,\n         a1_gscale=a1_gscale,\n         a2_scale=a2_scale,\n@@ -564,6 +572,8 @@ def int8_w8a8_moe_quant_config(\n     w2_scale: torch.Tensor,\n     a1_scale: torch.Tensor | None,\n     a2_scale: torch.Tensor | None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n     per_act_token_quant: bool = False,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n@@ -575,6 +585,8 @@ def int8_w8a8_moe_quant_config(\n         w2_scale=w2_scale,\n         a1_scale=a1_scale,\n         a2_scale=a2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         per_act_token_quant=per_act_token_quant,\n         per_out_ch_quant=False,\n         block_shape=None,\n@@ -654,6 +666,26 @@ def mxfp4_mxfp8_moe_quant_config(\n     )\n \n \n+def mxfp4_w4a8_moe_quant_config(\n+    w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    w2_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n+    a1_scale: torch.Tensor | None = None,\n+    a2_scale: torch.Tensor | None = None,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n+    block_shape: list[int] | None = None,\n+) -> FusedMoEQuantConfig:\n+    \"\"\"\n+    Construct a quant config for fp8 activations and mxfp4 weights.\n+    \"\"\"\n+    return FusedMoEQuantConfig(\n+        _a1=FusedMoEQuantDesc(\"fp8\", None, a1_scale, None, None, None),\n+        _a2=FusedMoEQuantDesc(\"fp8\", None, a2_scale, None, None, None),\n+        _w1=FusedMoEQuantDesc(\"mxfp4\", None, w1_scale, None, None, w1_bias),\n+        _w2=FusedMoEQuantDesc(\"mxfp4\", None, w2_scale, None, None, w2_bias),\n+    )\n+\n+\n def ocp_mx_moe_quant_config(\n     quant_dtype: str,\n     w1_scale: Union[torch.Tensor, \"PrecisionConfig\"],\n@@ -691,6 +723,8 @@ def nvfp4_moe_quant_config(\n     a2_gscale: torch.Tensor,\n     w1_scale: torch.Tensor,\n     w2_scale: torch.Tensor,\n+    w1_bias: torch.Tensor | None = None,\n+    w2_bias: torch.Tensor | None = None,\n ) -> FusedMoEQuantConfig:\n     \"\"\"\n     Construct a quant config for mxfp4 activations and nvp4 weights.\n@@ -699,6 +733,8 @@ def nvfp4_moe_quant_config(\n         \"nvfp4\",\n         w1_scale=w1_scale,\n         w2_scale=w2_scale,\n+        w1_bias=w1_bias,\n+        w2_bias=w2_bias,\n         a1_gscale=a1_gscale,\n         a2_gscale=a2_gscale,\n         g1_alphas=g1_alphas,", "patch_sha256": "eea235a7c7a12e3b"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2710671045", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nnote to self: we need to migrate this stuff to `QuantKey`. All these fields are duplicative/confusing\n\nOriginal Code Context:\n@@ -1811,12 +1804,14 @@ def fused_experts_impl(\n \n         curr_topk_ids = topk_ids[begin_chunk_idx:end_chunk_idx]\n         curr_topk_weights = topk_weights[begin_chunk_idx:end_chunk_idx]\n+\n         qcurr_hidden_states, a1q_scale = moe_kernel_quantize_input(\n             A=curr_hidden_states,\n             A_scale=a1_scale,\n-            quant_dtype=quant_dtype,\n+            quant_dtype=input_quant_dtype,\n", "materials": ["note to self: we need to migrate this stuff to `QuantKey`. All these fields are duplicative/confusing"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2710672401", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nI would like it if we could move this emulation out into a separate kernel in a follow up.\r\n\r\nIts not great that its comingled with apply.\n\nOriginal Code Context:\n@@ -1755,17 +1752,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n", "materials": ["I would like it if we could move this emulation out into a separate kernel in a follow up.\r\n\r\nIts not great that its comingled with apply."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2710683709", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nnote to self: its inappropriate this is part of `layer.py`, we should migrate this into the relevant quantization integration.\n\nOriginal Code Context:\n@@ -221,18 +221,18 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n+    model_type: str | None,\n", "materials": ["note to self: its inappropriate this is part of `layer.py`, we should migrate this into the relevant quantization integration."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2710686185", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nI dont like the idea of creating the `moe_config` twice. Why don't we make this adjustment before we create the `moe_config`?\n\nOriginal Code Context:\n@@ -584,6 +576,38 @@ def _get_quant_method() -> FusedMoEMethodBase:\n         # for heuristic purposes, so it must be initialized first.\n         self.quant_method: FusedMoEMethodBase = _get_quant_method()\n \n+        self.model_type = (\n+            self.vllm_config.model_config.hf_config.model_type\n+            if self.vllm_config.model_config is not None\n+            else None\n+        )\n+        self.is_mxfp4_quant = self._is_mxfp4_quant(quant_config, self.quant_method)\n+\n+        # Round up hidden size if needed.\n+        hidden_size, is_rounded_hidden_size = maybe_roundup_hidden_size(\n+            hidden_size,\n+            moe_in_dtype,\n+            self.moe_parallel_config,\n+            self.model_type,\n+            self.is_mxfp4_quant,\n+            is_lora_enabled=self.vllm_config.lora_config is not None,\n+        )\n+\n+        if is_rounded_hidden_size:\n+            self.hidden_size = hidden_size\n", "materials": ["I dont like the idea of creating the `moe_config` twice. Why don't we make this adjustment before we create the `moe_config`?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2710687036", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nah, its chicken and egg.\n\nOriginal Code Context:\n@@ -584,6 +576,38 @@ def _get_quant_method() -> FusedMoEMethodBase:\n         # for heuristic purposes, so it must be initialized first.\n         self.quant_method: FusedMoEMethodBase = _get_quant_method()\n \n+        self.model_type = (\n+            self.vllm_config.model_config.hf_config.model_type\n+            if self.vllm_config.model_config is not None\n+            else None\n+        )\n+        self.is_mxfp4_quant = self._is_mxfp4_quant(quant_config, self.quant_method)\n+\n+        # Round up hidden size if needed.\n+        hidden_size, is_rounded_hidden_size = maybe_roundup_hidden_size(\n+            hidden_size,\n+            moe_in_dtype,\n+            self.moe_parallel_config,\n+            self.model_type,\n+            self.is_mxfp4_quant,\n+            is_lora_enabled=self.vllm_config.lora_config is not None,\n+        )\n+\n+        if is_rounded_hidden_size:\n+            self.hidden_size = hidden_size\n", "materials": ["ah, its chicken and egg."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2710690512", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nthis is actually actually incorrect, because now the `quant_method` and the `layer` have different `FusedMoEConfigs` \r\n\r\nThis is a problem because many kernels actually use the `hidden_size` in the `moe_config`\n\nOriginal Code Context:\n@@ -584,6 +576,38 @@ def _get_quant_method() -> FusedMoEMethodBase:\n         # for heuristic purposes, so it must be initialized first.\n         self.quant_method: FusedMoEMethodBase = _get_quant_method()\n \n+        self.model_type = (\n+            self.vllm_config.model_config.hf_config.model_type\n+            if self.vllm_config.model_config is not None\n+            else None\n+        )\n+        self.is_mxfp4_quant = self._is_mxfp4_quant(quant_config, self.quant_method)\n+\n+        # Round up hidden size if needed.\n+        hidden_size, is_rounded_hidden_size = maybe_roundup_hidden_size(\n+            hidden_size,\n+            moe_in_dtype,\n+            self.moe_parallel_config,\n+            self.model_type,\n+            self.is_mxfp4_quant,\n+            is_lora_enabled=self.vllm_config.lora_config is not None,\n+        )\n+\n+        if is_rounded_hidden_size:\n+            self.hidden_size = hidden_size\n", "materials": ["this is actually actually incorrect, because now the `quant_method` and the `layer` have different `FusedMoEConfigs` \r\n\r\nThis is a problem because many kernels actually use the `hidden_size` in the `moe_config`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2710693223", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nIm not sure how to solve this problem, but having multiple copies is not right. In general, this `maybe_round_up_hidden_size` `mxfp4`-specific logic is very hacky.\n\nOriginal Code Context:\n@@ -584,6 +576,38 @@ def _get_quant_method() -> FusedMoEMethodBase:\n         # for heuristic purposes, so it must be initialized first.\n         self.quant_method: FusedMoEMethodBase = _get_quant_method()\n \n+        self.model_type = (\n+            self.vllm_config.model_config.hf_config.model_type\n+            if self.vllm_config.model_config is not None\n+            else None\n+        )\n+        self.is_mxfp4_quant = self._is_mxfp4_quant(quant_config, self.quant_method)\n+\n+        # Round up hidden size if needed.\n+        hidden_size, is_rounded_hidden_size = maybe_roundup_hidden_size(\n+            hidden_size,\n+            moe_in_dtype,\n+            self.moe_parallel_config,\n+            self.model_type,\n+            self.is_mxfp4_quant,\n+            is_lora_enabled=self.vllm_config.lora_config is not None,\n+        )\n+\n+        if is_rounded_hidden_size:\n+            self.hidden_size = hidden_size\n", "materials": ["Im not sure how to solve this problem, but having multiple copies is not right. In general, this `maybe_round_up_hidden_size` `mxfp4`-specific logic is very hacky."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2710811016", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/utils.py\nReview Comment:\nIm just wondering, what is the point of this?\r\n\r\nWhy would we want to create a scheme like this?\n\nOriginal Code Context:\n@@ -244,7 +247,27 @@ def moe_kernel_quantize_input(\n     per_act_token_quant: bool,\n     block_shape: list[int] | None = None,\n     is_fp4_scale_swizzled: bool = True,\n+    ocp_mx_scheme: str | None = None,\n ) -> tuple[torch.Tensor, torch.Tensor | None]:\n+    # Handle OCP MX scheme that requires QDQ (quantize-dequantize) for emulation\n+    if ocp_mx_scheme is not None:\n", "materials": ["Im just wondering, what is the point of this?\r\n\r\nWhy would we want to create a scheme like this?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -23,6 +23,9 @@\n from vllm.model_executor.layers.quantization.utils.mxfp8_utils import (\n     mxfp8_e4m3_quantize,\n )\n+from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n+    per_tensor_dequantize,\n+)\n from vllm.triton_utils import tl, triton\n from vllm.utils.math_utils import cdiv\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n@@ -241,7 +244,27 @@ def moe_kernel_quantize_input(\n     per_act_token_quant: bool,\n     block_shape: list[int] | None = None,\n     is_fp4_scale_swizzled: bool = True,\n+    ocp_mx_scheme: str | None = None,\n ) -> tuple[torch.Tensor, torch.Tensor | None]:\n+    # Handle OCP MX scheme that requires QDQ (quantize-dequantize) for emulation\n+    if ocp_mx_scheme is not None:\n+        if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+            pass  # No QDQ needed for these schemes\n+        elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+            # Perform QDQ (quantize and dequantize) on activation for emulation\n+            # purpose, because there is no native kernel for weight in ocp_mx_scheme\n+            # and activation in FP8. The implementation is based on existing\n+            # non-emulation ops.\n+            qA, qA_scale = ops.scaled_fp8_quant(\n+                A, A_scale, use_per_token_if_dynamic=False\n+            )\n+            A = per_tensor_dequantize(qA, qA_scale).to(A.dtype)\n+            # After QDQ, we don't need further quantization\n+            return A, None\n+        # else: For other schemes (e.g., *_a_mxfp6_e3m2, *_a_mxfp6_e2m3),\n+        # weights are already dequantized, and we proceed with normal\n+        # activation quantization below.\n+\n     if quant_dtype == torch.float8_e4m3fn:\n         return _fp8_quantize(A, A_scale, per_act_token_quant, block_shape)\n     elif quant_dtype == torch.int8:", "patch_sha256": "215512b3e32dd8a7"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2710827991", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/utils.py\nReview Comment:\nWe can allow this. But there should be a PR that follows up and removes emulation from `FusedMoE` layer and handles it separately as a kernel backend.\n\nOriginal Code Context:\n@@ -244,7 +247,27 @@ def moe_kernel_quantize_input(\n     per_act_token_quant: bool,\n     block_shape: list[int] | None = None,\n     is_fp4_scale_swizzled: bool = True,\n+    ocp_mx_scheme: str | None = None,\n ) -> tuple[torch.Tensor, torch.Tensor | None]:\n+    # Handle OCP MX scheme that requires QDQ (quantize-dequantize) for emulation\n+    if ocp_mx_scheme is not None:\n", "materials": ["We can allow this. But there should be a PR that follows up and removes emulation from `FusedMoE` layer and handles it separately as a kernel backend."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -23,6 +23,9 @@\n from vllm.model_executor.layers.quantization.utils.mxfp8_utils import (\n     mxfp8_e4m3_quantize,\n )\n+from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n+    per_tensor_dequantize,\n+)\n from vllm.triton_utils import tl, triton\n from vllm.utils.math_utils import cdiv\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n@@ -241,7 +244,27 @@ def moe_kernel_quantize_input(\n     per_act_token_quant: bool,\n     block_shape: list[int] | None = None,\n     is_fp4_scale_swizzled: bool = True,\n+    ocp_mx_scheme: str | None = None,\n ) -> tuple[torch.Tensor, torch.Tensor | None]:\n+    # Handle OCP MX scheme that requires QDQ (quantize-dequantize) for emulation\n+    if ocp_mx_scheme is not None:\n+        if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+            pass  # No QDQ needed for these schemes\n+        elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+            # Perform QDQ (quantize and dequantize) on activation for emulation\n+            # purpose, because there is no native kernel for weight in ocp_mx_scheme\n+            # and activation in FP8. The implementation is based on existing\n+            # non-emulation ops.\n+            qA, qA_scale = ops.scaled_fp8_quant(\n+                A, A_scale, use_per_token_if_dynamic=False\n+            )\n+            A = per_tensor_dequantize(qA, qA_scale).to(A.dtype)\n+            # After QDQ, we don't need further quantization\n+            return A, None\n+        # else: For other schemes (e.g., *_a_mxfp6_e3m2, *_a_mxfp6_e2m3),\n+        # weights are already dequantized, and we proceed with normal\n+        # activation quantization below.\n+\n     if quant_dtype == torch.float8_e4m3fn:\n         return _fp8_quantize(A, A_scale, per_act_token_quant, block_shape)\n     elif quant_dtype == torch.int8:", "patch_sha256": "215512b3e32dd8a7"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2712743160", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nYeah, making sense to me. Let me see if we need another PR for this effort.\n\nOriginal Code Context:\n@@ -1755,17 +1752,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n", "materials": ["Yeah, making sense to me. Let me see if we need another PR for this effort."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2719314959", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/fused_moe.py\nReview Comment:\nhttps://github.com/vllm-project/vllm/pull/32120 this PR aims for this refactor.\n\nOriginal Code Context:\n@@ -1755,17 +1752,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n", "materials": ["https://github.com/vllm-project/vllm/pull/32120 this PR aims for this refactor."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,6 @@\n )\n from vllm.model_executor.layers.quantization.utils.mxfp4_utils import dequant_mxfp4\n from vllm.model_executor.layers.quantization.utils.mxfp6_utils import dequant_mxfp6\n-from vllm.model_executor.layers.quantization.utils.ocp_mx_utils import OCP_MX_Scheme\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     QuantKey,\n     kFp8Dynamic128Sym,\n@@ -1583,6 +1582,11 @@ def _get_config_quant_dtype(\n         return \"mxfp6_e3m2\"\n     elif ocp_mx_scheme in {\"w_mxfp4_a_mxfp6_e2m3\", \"w_mxfp6_e2m3_a_mxfp6_e2m3\"}:\n         return \"mxfp6_e2m3\"\n+    elif ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp6_e3m2\", \"w_mxfp6_e2m3\"}:\n+        return torch.bfloat16\n+    elif ocp_mx_scheme in {\"w_mxfp4_a_fp8\", \"w_mxfp6_e3m2_a_fp8\", \"w_mxfp6_e2m3_a_fp8\"}:\n+        return torch.float8_e4m3fn\n+\n     return None\n \n \n@@ -1617,17 +1621,10 @@ def fused_experts_impl(\n     if use_int4_w4a16:\n         assert hidden_states.size(1) // 2 == w1.size(2), \"Hidden size mismatch\"\n     elif ocp_mx_scheme is not None:\n-        if ocp_mx_scheme in {\n-            \"w_mxfp4_a_mxfp4\",\n-            \"w_mxfp4_a_mxfp6_e3m2\",\n-            \"w_mxfp4_a_mxfp6_e2m3\",\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # 16bit activation and fp4x2 packed weight\n             assert hidden_states.size(1) == w1.size(2) * 2, \"hidden size mismatch\"\n-        elif ocp_mx_scheme in {\n-            \"w_mxfp6_e3m2_a_mxfp6_e3m2\",\n-            \"w_mxfp6_e2m3_a_mxfp6_e2m3\",\n-        }:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6\"):\n             assert hidden_states.size(1) == (w1.size(2) * 4) // 3, (\n                 \"hidden size mismatch\"\n             )\n@@ -1717,17 +1714,13 @@ def fused_experts_impl(\n         # TODO: On platforms for which `current_platform.supports_mx()` is True\n         # and for which we have a native OCP mx fused MOE kernel,\n         # this dequantization step should not be done.\n-        if ocp_mx_scheme in {\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp4,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e3m2,\n-            OCP_MX_Scheme.w_mxfp4_a_mxfp6_e2m3,\n-        }:\n+        if ocp_mx_scheme.startswith(\"w_mxfp4\"):\n             # Weight has to be dequantized for mxfp4 emulation.\n             w1 = dequant_mxfp4(w1, w1_scale, hidden_states.dtype)\n             w1_scale = None\n             w2 = dequant_mxfp4(w2, w2_scale, hidden_states.dtype)\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e3m2_a_mxfp6_e3m2:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e3m2\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n@@ -1736,7 +1729,7 @@ def fused_experts_impl(\n                 w2, w2_scale, quant_dtype=\"fp6_e3m2\", float_dtype=hidden_states.dtype\n             )\n             w2_scale = None\n-        elif ocp_mx_scheme == OCP_MX_Scheme.w_mxfp6_e2m3_a_mxfp6_e2m3:\n+        elif ocp_mx_scheme.startswith(\"w_mxfp6_e2m3\"):\n             w1 = dequant_mxfp6(\n                 w1, w1_scale, quant_dtype=\"fp6_e2m3\", float_dtype=hidden_states.dtype\n             )\n@@ -1779,6 +1772,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         # SPARSITY_FACTOR is a heuristic margin ensuring tokens_in_chunk * top_k\n@@ -1846,6 +1840,7 @@ def fused_experts_impl(\n             quant_dtype=quant_dtype,\n             per_act_token_quant=per_channel_quant,\n             block_shape=block_shape,\n+            ocp_mx_scheme=ocp_mx_scheme,\n         )\n \n         if expert_map is not None:", "patch_sha256": "89bc11c0181e545f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2737539128", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nnit, dont touch unrelated lines\n\nOriginal Code Context:\n@@ -999,6 +1049,7 @@ def _load_w13(\n             loaded_weight = loaded_weight.narrow(\n                 shard_dim, shard_size * tp_rank, shard_size\n             )\n+\n", "materials": ["nit, dont touch unrelated lines"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2737677450", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nI agree this solves the problem, but this change is extremely brittle and complicated and is going to cause bugs in the future.\n\nInstead, can we just reject the deployment if the shapes don't work out?\n\n\nOriginal Code Context:\n@@ -595,10 +581,47 @@ def _get_quant_method() -> FusedMoEMethodBase:\n             assert isinstance(quant_method, FusedMoEMethodBase)\n             return quant_method\n \n+        # Create initial moe_config with original hidden_size\n+        # This is needed to determine is_mxfp4_quant for rounding decision\n+        self.moe_config: FusedMoEConfig = _create_moe_config(hidden_size)\n+\n+        if self.use_mori_kernels:\n+            assert self.rocm_aiter_fmoe_enabled, (\n+                \"Mori needs to be used with aiter fused_moe for now.\"\n+            )\n+            assert not self.aiter_fmoe_shared_expert_enabled, (\n+                \"Mori does not support fusion shared expert now. \"\n+                \"Turn it off by setting VLLM_ROCM_USE_AITER_FUSION_SHARED_EXPERTS=0\"\n+            )\n+\n         # Note: get_quant_method will look at the layer's local_num_experts\n         # for heuristic purposes, so it must be initialized first.\n         self.quant_method: FusedMoEMethodBase = _get_quant_method()\n \n+        self.model_type = (\n+            self.vllm_config.model_config.hf_config.model_type\n+            if self.vllm_config.model_config is not None\n+            else None\n+        )\n+        self.is_mxfp4_quant = self._is_mxfp4_quant(quant_config, self.quant_method)\n+\n+        # Round up hidden size if needed.\n+        hidden_size, is_rounded_hidden_size = maybe_roundup_hidden_size(\n+            hidden_size,\n+            moe_in_dtype,\n+            self.moe_parallel_config,\n+            self.model_type,\n+            self.is_mxfp4_quant,\n+            is_lora_enabled=self.vllm_config.lora_config is not None,\n+        )\n+\n+        # If hidden_size was rounded, recreate moe_config and quant_method\n", "materials": ["I agree this solves the problem, but this change is extremely brittle and complicated and is going to cause bugs in the future.\n\nInstead, can we just reject the deployment if the shapes don't work out?\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2737679292", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nSo, leave the logic the same. Then, when we create the kernels, reject if the shapes dont work out?\n\nOriginal Code Context:\n@@ -595,10 +581,47 @@ def _get_quant_method() -> FusedMoEMethodBase:\n             assert isinstance(quant_method, FusedMoEMethodBase)\n             return quant_method\n \n+        # Create initial moe_config with original hidden_size\n+        # This is needed to determine is_mxfp4_quant for rounding decision\n+        self.moe_config: FusedMoEConfig = _create_moe_config(hidden_size)\n+\n+        if self.use_mori_kernels:\n+            assert self.rocm_aiter_fmoe_enabled, (\n+                \"Mori needs to be used with aiter fused_moe for now.\"\n+            )\n+            assert not self.aiter_fmoe_shared_expert_enabled, (\n+                \"Mori does not support fusion shared expert now. \"\n+                \"Turn it off by setting VLLM_ROCM_USE_AITER_FUSION_SHARED_EXPERTS=0\"\n+            )\n+\n         # Note: get_quant_method will look at the layer's local_num_experts\n         # for heuristic purposes, so it must be initialized first.\n         self.quant_method: FusedMoEMethodBase = _get_quant_method()\n \n+        self.model_type = (\n+            self.vllm_config.model_config.hf_config.model_type\n+            if self.vllm_config.model_config is not None\n+            else None\n+        )\n+        self.is_mxfp4_quant = self._is_mxfp4_quant(quant_config, self.quant_method)\n+\n+        # Round up hidden size if needed.\n+        hidden_size, is_rounded_hidden_size = maybe_roundup_hidden_size(\n+            hidden_size,\n+            moe_in_dtype,\n+            self.moe_parallel_config,\n+            self.model_type,\n+            self.is_mxfp4_quant,\n+            is_lora_enabled=self.vllm_config.lora_config is not None,\n+        )\n+\n+        # If hidden_size was rounded, recreate moe_config and quant_method\n", "materials": ["So, leave the logic the same. Then, when we create the kernels, reject if the shapes dont work out?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2737698043", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nThis is the new logic for the the quark case.\n\nHowever, this is how experts are usually loaded. So the generic loading logic below should cover this case. Why is this special case needed?\n\nOriginal Code Context:\n@@ -1105,23 +1162,62 @@ def weight_loader(\n         loaded_weight: torch.Tensor,\n         weight_name: str,\n         shard_id: str,\n-        expert_id: int,\n+        expert_id: int | None = None,\n         return_success: bool = False,\n+        mxfp4_or_bias: bool | None = False,\n     ) -> bool | None:\n-        if self.quant_config and self.quant_config.get_name() == \"mxfp4\":\n-            # (FIXME) for gpt-oss all experts are combined\n-            if \"bias\" in weight_name:\n-                dim1 = loaded_weight.shape[1]\n-                param.data[:, :dim1].copy_(loaded_weight)\n-            else:\n-                dim1 = loaded_weight.shape[1]\n-                dim2 = loaded_weight.shape[2]\n-                param.data[:, :dim1, :dim2].copy_(loaded_weight)\n-            return True if return_success else None\n+        # Unified handling for gpt_oss model with mxfp4/quark quantization\n+        # Use helper functions where applicable for consistency\n+        if (\n+            self.quant_config is not None\n+            and self.model_type == \"gpt_oss\"\n+            and mxfp4_or_bias\n+        ):\n+            if any(\n+                self.quant_config.get_name() == quant_name\n+                for quant_name in [\"mxfp4\", \"quark\"]\n+            ):\n+                # Validate weight_name is recognized\n+                if shard_id is None and not (\n+                    \".w13_\" in weight_name\n+                    or \".gate_up_proj\" in weight_name\n+                    or \".w2_\" in weight_name\n+                    or \".down_proj\" in weight_name\n+                ):\n+                    raise ValueError(\n+                        f\"Cannot infer shard_id from weight_name: {weight_name}\"\n+                    )\n+\n+                # For mxfp4/quark, loaded_weight is already pre-sliced at model level\n+                # and in special packed format. Use slicing to handle potential\n+                # dimension mismatches due to intermediate_size rounding for alignment.\n+\n+                # All experts at once: loaded_weight shape is [num_experts, dim1, ...]\n+                if expert_id is None:\n+                    dim1 = loaded_weight.shape[1]\n+                    if \"bias\" in weight_name:\n+                        param.data[:, :dim1].copy_(loaded_weight)\n+                    else:\n+                        dim2 = loaded_weight.shape[2]\n+                        param.data[:, :dim1, :dim2].copy_(loaded_weight)\n+\n+                # Single expert loading: loaded_weight shape is [dim1, ...]\n+                else:\n", "materials": ["This is the new logic for the the quark case.\n\nHowever, this is how experts are usually loaded. So the generic loading logic below should cover this case. Why is this special case needed?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2737698777", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nwhy is this change needed?\n\nOriginal Code Context:\n@@ -1105,23 +1162,62 @@ def weight_loader(\n         loaded_weight: torch.Tensor,\n         weight_name: str,\n         shard_id: str,\n-        expert_id: int,\n+        expert_id: int | None = None,\n         return_success: bool = False,\n+        mxfp4_or_bias: bool | None = False,\n     ) -> bool | None:\n-        if self.quant_config and self.quant_config.get_name() == \"mxfp4\":\n-            # (FIXME) for gpt-oss all experts are combined\n-            if \"bias\" in weight_name:\n-                dim1 = loaded_weight.shape[1]\n-                param.data[:, :dim1].copy_(loaded_weight)\n-            else:\n-                dim1 = loaded_weight.shape[1]\n-                dim2 = loaded_weight.shape[2]\n-                param.data[:, :dim1, :dim2].copy_(loaded_weight)\n-            return True if return_success else None\n+        # Unified handling for gpt_oss model with mxfp4/quark quantization\n+        # Use helper functions where applicable for consistency\n+        if (\n+            self.quant_config is not None\n+            and self.model_type == \"gpt_oss\"\n+            and mxfp4_or_bias\n+        ):\n+            if any(\n+                self.quant_config.get_name() == quant_name\n+                for quant_name in [\"mxfp4\", \"quark\"]\n+            ):\n+                # Validate weight_name is recognized\n+                if shard_id is None and not (\n+                    \".w13_\" in weight_name\n+                    or \".gate_up_proj\" in weight_name\n+                    or \".w2_\" in weight_name\n+                    or \".down_proj\" in weight_name\n+                ):\n+                    raise ValueError(\n+                        f\"Cannot infer shard_id from weight_name: {weight_name}\"\n+                    )\n+\n+                # For mxfp4/quark, loaded_weight is already pre-sliced at model level\n+                # and in special packed format. Use slicing to handle potential\n+                # dimension mismatches due to intermediate_size rounding for alignment.\n+\n+                # All experts at once: loaded_weight shape is [num_experts, dim1, ...]\n+                if expert_id is None:\n+                    dim1 = loaded_weight.shape[1]\n+                    if \"bias\" in weight_name:\n+                        param.data[:, :dim1].copy_(loaded_weight)\n+                    else:\n+                        dim2 = loaded_weight.shape[2]\n+                        param.data[:, :dim1, :dim2].copy_(loaded_weight)\n+\n+                # Single expert loading: loaded_weight shape is [dim1, ...]\n+                else:\n+                    expert_data = param.data[expert_id]\n+                    dim1 = loaded_weight.shape[0]\n+                    if \"bias\" in weight_name:\n+                        expert_data.data[:dim1].copy_(loaded_weight)\n+                    else:\n+                        dim2 = loaded_weight.shape[1]\n+                        expert_data.data[:dim1, :dim2].copy_(loaded_weight)\n+\n+                return True if return_success else None\n+            return False if return_success else None\n \n         quant_method_name = self.quant_method.__class__.__name__\n         global_expert_id = expert_id\n-        expert_id = self._map_global_expert_id_to_local_expert_id(global_expert_id)\n+        if global_expert_id is not None:\n", "materials": ["why is this change needed?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2737706301", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/utils.py\nReview Comment:\nwhat is the point of supporting this?\n\nOriginal Code Context:\n@@ -241,7 +244,27 @@ def moe_kernel_quantize_input(\n     per_act_token_quant: bool,\n     block_shape: list[int] | None = None,\n     is_fp4_scale_swizzled: bool = True,\n+    ocp_mx_scheme: str | None = None,\n ) -> tuple[torch.Tensor, torch.Tensor | None]:\n+    # Handle OCP MX scheme that requires QDQ (quantize-dequantize) for emulation\n+    if ocp_mx_scheme is not None:\n+        if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+            pass  # No QDQ needed for these schemes\n+        elif ocp_mx_scheme.endswith(\"a_fp8\"):\n", "materials": ["what is the point of supporting this?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -23,6 +23,9 @@\n from vllm.model_executor.layers.quantization.utils.mxfp8_utils import (\n     mxfp8_e4m3_quantize,\n )\n+from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n+    per_tensor_dequantize,\n+)\n from vllm.triton_utils import tl, triton\n from vllm.utils.math_utils import cdiv\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n@@ -241,7 +244,27 @@ def moe_kernel_quantize_input(\n     per_act_token_quant: bool,\n     block_shape: list[int] | None = None,\n     is_fp4_scale_swizzled: bool = True,\n+    ocp_mx_scheme: str | None = None,\n ) -> tuple[torch.Tensor, torch.Tensor | None]:\n+    # Handle OCP MX scheme that requires QDQ (quantize-dequantize) for emulation\n+    if ocp_mx_scheme is not None:\n+        if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+            pass  # No QDQ needed for these schemes\n+        elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+            # Perform QDQ (quantize and dequantize) on activation for emulation\n+            # purpose, because there is no native kernel for weight in ocp_mx_scheme\n+            # and activation in FP8. The implementation is based on existing\n+            # non-emulation ops.\n+            qA, qA_scale = ops.scaled_fp8_quant(\n+                A, A_scale, use_per_token_if_dynamic=False\n+            )\n+            A = per_tensor_dequantize(qA, qA_scale).to(A.dtype)\n+            # After QDQ, we don't need further quantization\n+            return A, None\n+        # else: For other schemes (e.g., *_a_mxfp6_e3m2, *_a_mxfp6_e2m3),\n+        # weights are already dequantized, and we proceed with normal\n+        # activation quantization below.\n+\n     if quant_dtype == torch.float8_e4m3fn:\n         return _fp8_quantize(A, A_scale, per_act_token_quant, block_shape)\n     elif quant_dtype == torch.int8:", "patch_sha256": "215512b3e32dd8a7"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2756838920", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\ndont make model type attr, its only used locally\n\nOriginal Code Context:\n@@ -540,6 +532,29 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        self.model_type = (\n", "materials": ["dont make model type attr, its only used locally"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2756849500", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nthis should be a method of the `QuantizationConfig` rather than of `FusedMoE`\n\nAlso - can you explain why this hidden size rounding is needed for Quark? I understand why its needed for the oai triton kernels, but why also for quark?\n\nOriginal Code Context:\n@@ -834,6 +850,44 @@ def update_expert_map(self):\n                     dp_size=get_dp_group().world_size,\n                 )\n \n+    def _is_mxfp4_quant(\n", "materials": ["this should be a method of the `QuantizationConfig` rather than of `FusedMoE`\n\nAlso - can you explain why this hidden size rounding is needed for Quark? I understand why its needed for the oai triton kernels, but why also for quark?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2757557108", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\n`self.model_type` is also used in the `weight_loader` method (line 1204), so it needs to remain an instance attribute, do you want me to refactor the code to pass `model_type` as a parameter or compute it on-demand ?\n\nOriginal Code Context:\n@@ -540,6 +532,29 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        self.model_type = (\n", "materials": ["`self.model_type` is also used in the `weight_loader` method (line 1204), so it needs to remain an instance attribute, do you want me to refactor the code to pass `model_type` as a parameter or compute it on-demand ?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2758335824", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nOk, it's refactored. Hidden size rounding is for same alignment requirement. \n\nOriginal Code Context:\n@@ -834,6 +850,44 @@ def update_expert_map(self):\n                     dp_size=get_dp_group().world_size,\n                 )\n \n+    def _is_mxfp4_quant(\n", "materials": ["Ok, it's refactored. Hidden size rounding is for same alignment requirement. "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2761872356", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nlet's just pass the quant config to `maybe_round_up_hidden_size` --- this attr is only used in this function\n\nOriginal Code Context:\n@@ -540,6 +532,32 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        self.model_type = (\n+            self.vllm_config.model_config.hf_config.model_type\n+            if self.vllm_config.model_config is not None\n+            else None\n+        )\n+\n+        # Determine is_mxfp4_quant by inspecting quant_config directly.\n+        # This is used for proper hidden_size rounding before moe_config creation.\n+        self.is_mxfp4_quant = (\n", "materials": ["let's just pass the quant config to `maybe_round_up_hidden_size` --- this attr is only used in this function"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2761891416", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nThis change is not needed. There are two cases:\n- official openai mxfp4 checkpoint. this special case is needed because the expert weights are of shape [E, ...] on disk. previously we handled it like this:\n\n```\nif self.quant_config and self.quant_config.get_name() == \"mxfp4\":\n            # (FIXME) for gpt-oss all experts are combined\n            if \"bias\" in weight_name:\n                dim1 = loaded_weight.shape[1]\n                param.data[:, :dim1].copy_(loaded_weight)\n            else:\n                dim1 = loaded_weight.shape[1]\n                dim2 = loaded_weight.shape[2]\n                param.data[:, :dim1, :dim2].copy_(loaded_weight)\n            return True if return_success else None\n```\n\n^ We should revert the change and leave this part untouched.\n\n- quark mxfp4. A special case should not be needed for this. the standard weight loading logic for MoEs will work for Quark because the experts are in the expected on disk format.\n\n^ We should just use `_load_model_weight_or_group_weight_scale` below. Is there some reason why this is not able to be used?\n\nf an additional case is needed for bias that is fine to add.\n\nOriginal Code Context:\n@@ -1140,8 +1157,46 @@ def weight_loader(\n             else:\n                 loaded_weight = loaded_weight\n \n-        if shard_id not in (\"w1\", \"w2\", \"w3\"):\n-            raise ValueError(f\"shard_id must be ['w1','w2','w3'] but got {shard_id}.\")\n+        if shard_id not in (\"w1\", \"w2\", \"w3\", \"w13\"):\n", "materials": ["This change is not needed. There are two cases:\n- official openai mxfp4 checkpoint. this special case is needed because the expert weights are of shape [E, ...] on disk. previously we handled it like this:\n\n```\nif self.quant_config and self.quant_config.get_name() == \"mxfp4\":\n            # (FIXME) for gpt-oss all experts are combined\n            if \"bias\" in weight_name:\n                dim1 = loaded_weight.shape[1]\n                param.data[:, :dim1].copy_(loaded_weight)\n            else:\n                dim1 = loaded_weight.shape[1]\n                dim2 = loaded_weight.shape[2]\n                param.data[:, :dim1, :dim2].copy_(loaded_weight)\n            return True if return_success else None\n```\n\n^ We should revert the change and leave this part untouched.\n\n- quark mxfp4. A special case should not be needed for this. the standard weight loading logic for MoEs will work for Quark because the experts are in the expected on disk format.\n\n^ We should just use `_load_model_weight_or_group_weight_scale` below. Is there some reason why this is not able to be used?\n\nf an additional case is needed for bias that is fine to add."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2761933037", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nthis is the same point I made in: https://github.com/vllm-project/vllm/pull/29008#pullrequestreview-3718061970\r\n\r\n>> I believe the the special case logic mxfp4_or_bias is not needed. In the case of Quark, which uses # Single expert loading: loaded_weight shape is [dim1, ...], should be able to use the generic weight loading logic. This is because the generic weight loading logic also assumes this format\n\nOriginal Code Context:\n@@ -1140,8 +1157,46 @@ def weight_loader(\n             else:\n                 loaded_weight = loaded_weight\n \n-        if shard_id not in (\"w1\", \"w2\", \"w3\"):\n-            raise ValueError(f\"shard_id must be ['w1','w2','w3'] but got {shard_id}.\")\n+        if shard_id not in (\"w1\", \"w2\", \"w3\", \"w13\"):\n", "materials": ["this is the same point I made in: https://github.com/vllm-project/vllm/pull/29008#pullrequestreview-3718061970\r\n\r\n>> I believe the the special case logic mxfp4_or_bias is not needed. In the case of Quark, which uses # Single expert loading: loaded_weight shape is [dim1, ...], should be able to use the generic weight loading logic. This is because the generic weight loading logic also assumes this format"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2762742855", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/layer.py\nReview Comment:\nupdate with simplification.\n\nOriginal Code Context:\n@@ -540,6 +532,32 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        self.model_type = (\n+            self.vllm_config.model_config.hf_config.model_type\n+            if self.vllm_config.model_config is not None\n+            else None\n+        )\n+\n+        # Determine is_mxfp4_quant by inspecting quant_config directly.\n+        # This is used for proper hidden_size rounding before moe_config creation.\n+        self.is_mxfp4_quant = (\n", "materials": ["update with simplification."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -221,12 +221,14 @@ def get_compressed_expert_map(expert_map: torch.Tensor) -> str:\n     )\n \n \n+# TODO(rob): move this down to the kernel.\n def maybe_roundup_hidden_size(\n     hidden_size: int,\n     act_dtype: torch.dtype,\n-    quant_config: QuantizationConfig | None,\n     moe_parallel_config: FusedMoEParallelConfig,\n     is_lora_enabled: bool,\n+    model_type: str | None,\n+    is_mxfp4_quant: bool,\n ) -> int:\n     \"\"\"\n     Given layer hidden size and MoE configurations, round up hidden_size\n@@ -235,11 +237,12 @@ def maybe_roundup_hidden_size(\n     Args:\n         hidden_size: Layer hidden-size\n         act_dtype: Data type of the layer activations.\n-        quant_config: Fused MoE quantization configuration.\n         moe_parallel_config: Fused MoE parallelization strategy configuration.\n         is_lora_enabled: True if the engine is enabled with LoRA. This\n             is used in the case of mxfp4 quantization in selecting the\n             MxFP4Backend.\n+        model_type: for checking if gpt-oss\n+        is_mxfp4_quant: whether the layer is quantized with mxfp4\n \n     Return:\n         Rounded up hidden_size if rounding up is required based on the configs.\n@@ -254,7 +257,7 @@ def maybe_roundup_hidden_size(\n     )\n \n     # we are padding globally so EP buffer allocation works\n-    if quant_config and quant_config.get_name() == \"mxfp4\":\n+    if model_type == \"gpt_oss\" and is_mxfp4_quant:\n         from vllm.model_executor.layers.quantization.mxfp4 import (\n             Mxfp4Backend,\n             get_mxfp4_backend,\n@@ -398,15 +401,6 @@ def __init__(\n         # Expert mapping used in self.load_weights\n         self.expert_mapping = expert_mapping\n \n-        # Round up hidden size if needed.\n-        hidden_size = maybe_roundup_hidden_size(\n-            hidden_size,\n-            moe_in_dtype,\n-            quant_config,\n-            self.moe_parallel_config,\n-            is_lora_enabled=self.vllm_config.lora_config is not None,\n-        )\n-\n         # For smuggling this layer into the fused moe custom op\n         compilation_config = vllm_config.compilation_config\n         if prefix in compilation_config.static_forward_context:\n@@ -508,7 +502,6 @@ def __init__(\n             ), \"Aiter Fused MoE kernel only supports expert_map with 0 and 1s.\"\n \n         assert intermediate_size % self.tp_size == 0\n-        self.hidden_size = hidden_size\n         self.intermediate_size_per_partition = intermediate_size // self.tp_size\n         self.reduce_results = reduce_results\n         self.renormalize = renormalize\n@@ -548,6 +541,24 @@ def __init__(\n         )\n         self.routing_method_type: RoutingMethodType = self.router.routing_method_type\n \n+        # Round up hidden size before creating moe_config.\n+        # This way moe_config is created with the correct hidden_size from the start.\n+        hidden_size = maybe_roundup_hidden_size(\n+            hidden_size=hidden_size,\n+            act_dtype=moe_in_dtype,\n+            moe_parallel_config=self.moe_parallel_config,\n+            is_lora_enabled=vllm_config.lora_config is not None,\n+            model_type=(\n+                self.vllm_config.model_config.hf_config.model_type\n+                if self.vllm_config.model_config is not None\n+                else None\n+            ),\n+            is_mxfp4_quant=(\n+                quant_config is not None and quant_config.is_mxfp4_quant(prefix, self)\n+            ),\n+        )\n+        self.hidden_size = hidden_size\n+\n         self.moe_config: FusedMoEConfig = FusedMoEConfig(\n             num_experts=self.global_num_experts,\n             experts_per_token=top_k,", "patch_sha256": "887997be5f409f02"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2871615920", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/fused_moe/utils.py\nReview Comment:\n@xuebwang-amd this looks unnecessary. `quant_dtype` should already be properly set.\n\nOriginal Code Context:\n@@ -241,7 +244,27 @@ def moe_kernel_quantize_input(\n     per_act_token_quant: bool,\n     block_shape: list[int] | None = None,\n     is_fp4_scale_swizzled: bool = True,\n+    ocp_mx_scheme: str | None = None,\n ) -> tuple[torch.Tensor, torch.Tensor | None]:\n+    # Handle OCP MX scheme that requires QDQ (quantize-dequantize) for emulation\n+    if ocp_mx_scheme is not None:\n+        if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+            pass  # No QDQ needed for these schemes\n", "materials": ["@xuebwang-amd this looks unnecessary. `quant_dtype` should already be properly set."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -23,6 +23,9 @@\n from vllm.model_executor.layers.quantization.utils.mxfp8_utils import (\n     mxfp8_e4m3_quantize,\n )\n+from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n+    per_tensor_dequantize,\n+)\n from vllm.triton_utils import tl, triton\n from vllm.utils.math_utils import cdiv\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n@@ -241,7 +244,27 @@ def moe_kernel_quantize_input(\n     per_act_token_quant: bool,\n     block_shape: list[int] | None = None,\n     is_fp4_scale_swizzled: bool = True,\n+    ocp_mx_scheme: str | None = None,\n ) -> tuple[torch.Tensor, torch.Tensor | None]:\n+    # Handle OCP MX scheme that requires QDQ (quantize-dequantize) for emulation\n+    if ocp_mx_scheme is not None:\n+        if ocp_mx_scheme in {\"w_mxfp4\", \"w_mxfp4_a_mxfp4\"}:\n+            pass  # No QDQ needed for these schemes\n+        elif ocp_mx_scheme.endswith(\"a_fp8\"):\n+            # Perform QDQ (quantize and dequantize) on activation for emulation\n+            # purpose, because there is no native kernel for weight in ocp_mx_scheme\n+            # and activation in FP8. The implementation is based on existing\n+            # non-emulation ops.\n+            qA, qA_scale = ops.scaled_fp8_quant(\n+                A, A_scale, use_per_token_if_dynamic=False\n+            )\n+            A = per_tensor_dequantize(qA, qA_scale).to(A.dtype)\n+            # After QDQ, we don't need further quantization\n+            return A, None\n+        # else: For other schemes (e.g., *_a_mxfp6_e3m2, *_a_mxfp6_e2m3),\n+        # weights are already dequantized, and we proceed with normal\n+        # activation quantization below.\n+\n     if quant_dtype == torch.float8_e4m3fn:\n         return _fp8_quantize(A, A_scale, per_act_token_quant, block_shape)\n     elif quant_dtype == torch.int8:", "patch_sha256": "215512b3e32dd8a7"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29063/review_2544175409", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29063", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/models/lfm2_moe.py\nReview Comment:\n![critical](https://www.gstatic.com/codereviewagent/critical.svg)\n\nThe current weight name replacement for `.conv.` is not sufficient to correctly load all weights for the `ShortConv` module. Specifically, the `conv` submodule within `ShortConv` is named `conv` in the model, but its weights are expected to be named with `conv1d` in the checkpoint. The current replacement logic does not account for this `conv1d` -> `conv` mapping.\n\nThis will cause a `KeyError` when trying to load weights for `...short_conv.conv.weight` and `...short_conv.conv.bias` because the checkpoint names will be incorrectly mapped.\n\nTo fix this, we need to add another replacement to handle the `conv1d` part of the name.\n\n```suggestion\n            if \".conv.\" in name:\n                name = name.replace(\".conv.\", \".short_conv.\", 1)\n\n            if \".conv1d.\" in name:\n                # The submodule is named 'conv', but the checkpoint weight is 'conv1d'.\n                name = name.replace(\".conv1d.\", \".conv.\", 1)\n```\n\nOriginal Code Context:\n@@ -509,6 +509,10 @@ def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]) -> set[str]:\n             if \"expert_bias\" in name:\n                 name = name.replace(\"expert_bias\", \"gate.e_score_correction_bias\")\n \n+            if \".conv.\" in name:\n+                name = name.replace(\".conv.\", \".short_conv.\", 1)\n", "materials": ["![critical](https://www.gstatic.com/codereviewagent/critical.svg)\n\nThe current weight name replacement for `.conv.` is not sufficient to correctly load all weights for the `ShortConv` module. Specifically, the `conv` submodule within `ShortConv` is named `conv` in the model, but its weights are expected to be named with `conv1d` in the checkpoint. The current replacement logic does not account for this `conv1d` -> `conv` mapping.\n\nThis will cause a `KeyError` when trying to load weights for `...short_conv.conv.weight` and `...short_conv.conv.bias` because the checkpoint names will be incorrectly mapped.\n\nTo fix this, we need to add another replacement to handle the `conv1d` part of the name.\n\n```suggestion\n            if \".conv.\" in name:\n                name = name.replace(\".conv.\", \".short_conv.\", 1)\n\n            if \".conv1d.\" in name:\n                # The submodule is named 'conv', but the checkpoint weight is 'conv1d'.\n                name = name.replace(\".conv1d.\", \".conv.\", 1)\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -349,7 +349,7 @@ def __init__(\n     ) -> None:\n         super().__init__()\n         self.layer_idx = layer_idx\n-        self.conv = ShortConv(\n+        self.short_conv = ShortConv(\n             config=config,\n             dim=config.hidden_size,\n             layer_idx=layer_idx,\n@@ -388,7 +388,7 @@ def forward(\n         else:\n             hidden_states, residual = self.operator_norm(hidden_states, residual)\n         output = torch.empty_like(hidden_states)\n-        self.conv(\n+        self.short_conv(\n             hidden_states,\n             output,\n         )\n@@ -508,6 +508,9 @@ def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]) -> set[str]:\n             if \"expert_bias\" in name:\n                 name = name.replace(\"expert_bias\", \"gate.e_score_correction_bias\")\n \n+            if \".conv.\" in name:\n+                name = name.replace(\".conv.\", \".short_conv.\", 1)\n+\n             for param_name, weight_name, shard_id in stacked_params_mapping:\n                 # Skip non-stacked layers and experts (experts handled below).\n                 if weight_name not in name:\n@@ -594,6 +597,7 @@ class Lfm2MoeForCausalLM(\n             \"w1\",\n             \"w3\",\n         ],\n+        \"in_proj\": [\"in_proj\"],\n     }\n \n     # LoRA specific attributes", "patch_sha256": "30ce3451509dcd5e"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29063/review_2544183221", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29063", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/models/lfm2_moe.py\nReview Comment:\n**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  LoRA mapping treats fused in_proj as single slice**\n\nAdding `\"in_proj\": [\"in_proj\"]` makes the LoRA manager wrap `short_conv.in_proj` as a one-slice merged linear, so it will pick `ColumnParallelLinearWithLoRA`, whose `slice_lora_b` logic splits merged weights into two halves. However, `short_conv.in_proj` is a three-way `MergedColumnParallelLinear` (`output_sizes=[dim]*3` in `layers/mamba/short_conv.py:60-65`), so under LoRA this mapping will shard/mix the adapter weights incorrectly or fail when enabling LoRA on Lfm2Moe. The mapping should reflect all three slices (or stay unset) so the correct three-slice wrapper is used.\n\nUseful? React with 👍 / 👎.\n\nOriginal Code Context:\n@@ -595,6 +599,7 @@ class Lfm2MoeForCausalLM(\n             \"w1\",\n             \"w3\",\n         ],\n+        \"in_proj\": [\"in_proj\"],\n", "materials": ["**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  LoRA mapping treats fused in_proj as single slice**\n\nAdding `\"in_proj\": [\"in_proj\"]` makes the LoRA manager wrap `short_conv.in_proj` as a one-slice merged linear, so it will pick `ColumnParallelLinearWithLoRA`, whose `slice_lora_b` logic splits merged weights into two halves. However, `short_conv.in_proj` is a three-way `MergedColumnParallelLinear` (`output_sizes=[dim]*3` in `layers/mamba/short_conv.py:60-65`), so under LoRA this mapping will shard/mix the adapter weights incorrectly or fail when enabling LoRA on Lfm2Moe. The mapping should reflect all three slices (or stay unset) so the correct three-slice wrapper is used.\n\nUseful? React with 👍 / 👎."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -349,7 +349,7 @@ def __init__(\n     ) -> None:\n         super().__init__()\n         self.layer_idx = layer_idx\n-        self.conv = ShortConv(\n+        self.short_conv = ShortConv(\n             config=config,\n             dim=config.hidden_size,\n             layer_idx=layer_idx,\n@@ -388,7 +388,7 @@ def forward(\n         else:\n             hidden_states, residual = self.operator_norm(hidden_states, residual)\n         output = torch.empty_like(hidden_states)\n-        self.conv(\n+        self.short_conv(\n             hidden_states,\n             output,\n         )\n@@ -508,6 +508,9 @@ def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]) -> set[str]:\n             if \"expert_bias\" in name:\n                 name = name.replace(\"expert_bias\", \"gate.e_score_correction_bias\")\n \n+            if \".conv.\" in name:\n+                name = name.replace(\".conv.\", \".short_conv.\", 1)\n+\n             for param_name, weight_name, shard_id in stacked_params_mapping:\n                 # Skip non-stacked layers and experts (experts handled below).\n                 if weight_name not in name:\n@@ -594,6 +597,7 @@ class Lfm2MoeForCausalLM(\n             \"w1\",\n             \"w3\",\n         ],\n+        \"in_proj\": [\"in_proj\"],\n     }\n \n     # LoRA specific attributes", "patch_sha256": "30ce3451509dcd5e"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29063/review_2544195379", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29063", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/models/lfm2_moe.py\nReview Comment:\ndo you need to fix this?\n\nOriginal Code Context:\n@@ -509,6 +509,10 @@ def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]) -> set[str]:\n             if \"expert_bias\" in name:\n                 name = name.replace(\"expert_bias\", \"gate.e_score_correction_bias\")\n \n+            if \".conv.\" in name:\n+                name = name.replace(\".conv.\", \".short_conv.\", 1)\n", "materials": ["do you need to fix this?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -349,7 +349,7 @@ def __init__(\n     ) -> None:\n         super().__init__()\n         self.layer_idx = layer_idx\n-        self.conv = ShortConv(\n+        self.short_conv = ShortConv(\n             config=config,\n             dim=config.hidden_size,\n             layer_idx=layer_idx,\n@@ -388,7 +388,7 @@ def forward(\n         else:\n             hidden_states, residual = self.operator_norm(hidden_states, residual)\n         output = torch.empty_like(hidden_states)\n-        self.conv(\n+        self.short_conv(\n             hidden_states,\n             output,\n         )\n@@ -508,6 +508,9 @@ def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]) -> set[str]:\n             if \"expert_bias\" in name:\n                 name = name.replace(\"expert_bias\", \"gate.e_score_correction_bias\")\n \n+            if \".conv.\" in name:\n+                name = name.replace(\".conv.\", \".short_conv.\", 1)\n+\n             for param_name, weight_name, shard_id in stacked_params_mapping:\n                 # Skip non-stacked layers and experts (experts handled below).\n                 if weight_name not in name:\n@@ -594,6 +597,7 @@ class Lfm2MoeForCausalLM(\n             \"w1\",\n             \"w3\",\n         ],\n+        \"in_proj\": [\"in_proj\"],\n     }\n \n     # LoRA specific attributes", "patch_sha256": "30ce3451509dcd5e"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29063/review_2561689211", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29063", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/models/lfm2_moe.py\nReview Comment:\nyes the state dict is not as \"conv1d\". Can also see the transformers ref [here](https://github.com/huggingface/transformers/blob/47e795b874b5393802d5bb6ab17bda45183ef711/src/transformers/models/lfm2_moe/modeling_lfm2_moe.py#L623)\n\nOriginal Code Context:\n@@ -509,6 +509,10 @@ def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]) -> set[str]:\n             if \"expert_bias\" in name:\n                 name = name.replace(\"expert_bias\", \"gate.e_score_correction_bias\")\n \n+            if \".conv.\" in name:\n+                name = name.replace(\".conv.\", \".short_conv.\", 1)\n", "materials": ["yes the state dict is not as \"conv1d\". Can also see the transformers ref [here](https://github.com/huggingface/transformers/blob/47e795b874b5393802d5bb6ab17bda45183ef711/src/transformers/models/lfm2_moe/modeling_lfm2_moe.py#L623)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -349,7 +349,7 @@ def __init__(\n     ) -> None:\n         super().__init__()\n         self.layer_idx = layer_idx\n-        self.conv = ShortConv(\n+        self.short_conv = ShortConv(\n             config=config,\n             dim=config.hidden_size,\n             layer_idx=layer_idx,\n@@ -388,7 +388,7 @@ def forward(\n         else:\n             hidden_states, residual = self.operator_norm(hidden_states, residual)\n         output = torch.empty_like(hidden_states)\n-        self.conv(\n+        self.short_conv(\n             hidden_states,\n             output,\n         )\n@@ -508,6 +508,9 @@ def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]) -> set[str]:\n             if \"expert_bias\" in name:\n                 name = name.replace(\"expert_bias\", \"gate.e_score_correction_bias\")\n \n+            if \".conv.\" in name:\n+                name = name.replace(\".conv.\", \".short_conv.\", 1)\n+\n             for param_name, weight_name, shard_id in stacked_params_mapping:\n                 # Skip non-stacked layers and experts (experts handled below).\n                 if weight_name not in name:\n@@ -594,6 +597,7 @@ class Lfm2MoeForCausalLM(\n             \"w1\",\n             \"w3\",\n         ],\n+        \"in_proj\": [\"in_proj\"],\n     }\n \n     # LoRA specific attributes", "patch_sha256": "30ce3451509dcd5e"}}], "heldout_tasks": [{"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2295149721", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu_model_runner.py\nReview Comment:\n![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThis `reasoning_config` parameter is being passed to the `InputBatch` constructor, but it is unused there. This line should be removed along with the parameter in the `InputBatch.__init__` definition.\n\nOriginal Code Context:\n@@ -227,6 +244,7 @@ def __init__(\n                 self.is_pooling_model,\n                 self.vllm_config.model_config.logits_processors),\n             is_pooling_model=self.is_pooling_model,\n+            reasoning_config=self.vllm_config.reasoning_config,\n", "materials": ["![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThis `reasoning_config` parameter is being passed to the `InputBatch` constructor, but it is unused there. This line should be removed along with the parameter in the `InputBatch.__init__` definition."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2295149727", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu_model_runner.py\nReview Comment:\n![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThis `reasoning_config` parameter is being passed to the `InputBatch` constructor, but it is unused there. This line should be removed along with the parameter in the `InputBatch.__init__` definition.\n\nOriginal Code Context:\n@@ -2940,6 +2958,7 @@ def may_reinitialize_input_batch(self,\n                 is_spec_decode=bool(self.vllm_config.speculative_config),\n                 logitsprocs=self.input_batch.logitsprocs,\n                 is_pooling_model=self.is_pooling_model,\n+                reasoning_config=self.vllm_config.reasoning_config,\n", "materials": ["![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThis `reasoning_config` parameter is being passed to the `InputBatch` constructor, but it is unused there. This line should be removed along with the parameter in the `InputBatch.__init__` definition."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2400812247", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu_model_runner.py\nReview Comment:\nLet's not do this. Tokenizer shouldn't be initialized inside the GPU runner here.\n\nI prefer to have the think_token_ids computed beforehand.\n\nOriginal Code Context:\n@@ -191,6 +192,20 @@ def __init__(\n         self.speculative_config = vllm_config.speculative_config\n         self.observability_config = vllm_config.observability_config\n \n+        reasoning_config = self.vllm_config.reasoning_config\n+        has_reasoning_strings = (reasoning_config.think_start_str is not None\n+                                 and reasoning_config.think_end_str\n+                                 is not None)\n+        if has_reasoning_strings:\n+            tokenizer = init_tokenizer_from_configs(\n+                model_config=self.vllm_config.model_config)\n+            reasoning_config.think_start_token_ids = \\\n+                tokenizer.convert_tokens_to_ids(\n+                    tokenizer.tokenize(reasoning_config.think_start_str))\n+            reasoning_config.think_end_token_ids = \\\n+                tokenizer.convert_tokens_to_ids(\n+                    tokenizer.tokenize(reasoning_config.think_end_str))\n", "materials": ["Let's not do this. Tokenizer shouldn't be initialized inside the GPU runner here.\n\nI prefer to have the think_token_ids computed beforehand."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2400851966", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nI understand your opinion and share the same thought. The problem is that not every reasoning parser has explicit thinking start/end tokens, and their variable names differ. I could add a common interface, but that would require modifying all reasoning parsers in vLLM.\r\n\r\nThe second issue is that if I want to force thinking end tokens differently from what the reasoning parsers use, I would need to add additional arguments.\r\nFor example, the reasoning parser may use \"</think>\" as the end token, but I want to force it to finish reasoning mode with \" Now formulate the answers.</think>\".\n\nOriginal Code Context:\n@@ -0,0 +1,31 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from typing import Optional\n+\n+from pydantic.dataclasses import dataclass\n+\n+from vllm.config.utils import config\n+\n+\n+@config\n+@dataclass\n+class ReasoningConfig:\n", "materials": ["I understand your opinion and share the same thought. The problem is that not every reasoning parser has explicit thinking start/end tokens, and their variable names differ. I could add a common interface, but that would require modifying all reasoning parsers in vLLM.\r\n\r\nThe second issue is that if I want to force thinking end tokens differently from what the reasoning parsers use, I would need to add additional arguments.\r\nFor example, the reasoning parser may use \"</think>\" as the end token, but I want to force it to finish reasoning mode with \" Now formulate the answers.</think>\"."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2400948383", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu_model_runner.py\nReview Comment:\nIn the current implementation, I moved this part into the config part.\n\nOriginal Code Context:\n@@ -191,6 +192,20 @@ def __init__(\n         self.speculative_config = vllm_config.speculative_config\n         self.observability_config = vllm_config.observability_config\n \n+        reasoning_config = self.vllm_config.reasoning_config\n+        has_reasoning_strings = (reasoning_config.think_start_str is not None\n+                                 and reasoning_config.think_end_str\n+                                 is not None)\n+        if has_reasoning_strings:\n+            tokenizer = init_tokenizer_from_configs(\n+                model_config=self.vllm_config.model_config)\n+            reasoning_config.think_start_token_ids = \\\n+                tokenizer.convert_tokens_to_ids(\n+                    tokenizer.tokenize(reasoning_config.think_start_str))\n+            reasoning_config.think_end_token_ids = \\\n+                tokenizer.convert_tokens_to_ids(\n+                    tokenizer.tokenize(reasoning_config.think_end_str))\n", "materials": ["In the current implementation, I moved this part into the config part."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2481389502", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\n`Optional` is no longer allowed. You can use `| None` instead.\n\nOriginal Code Context:\n@@ -0,0 +1,48 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from typing import Optional\n", "materials": ["`Optional` is no longer allowed. You can use `| None` instead."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2875384590", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nIIUC these are model-specific values. Should they really be independently configurable? Shouldn't they part of / derived from the model config? and/or provided by the particular reasoning parser impl?\n\nOriginal Code Context:\n@@ -0,0 +1,44 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.tokenizers import cached_tokenizer_from_config\n+\n+\n+@config\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\"\"\"\n+\n+    think_start_str: str | None = None\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: str | None = None\n+    \"\"\"String that indicates the end of reasoning.\"\"\"\n", "materials": ["IIUC these are model-specific values. Should they really be independently configurable? Shouldn't they part of / derived from the model config? and/or provided by the particular reasoning parser impl?"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2926564856", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "models", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu_model_runner.py\nReview Comment:\n```suggestion\n            or self.vllm_config.reasoning_config is not None,\n```\n\nOriginal Code Context:\n@@ -619,7 +619,10 @@ def __init__(\n             ),\n             # We currently don't know whether a particular custom logits processor\n             # uses output token ids so we set this conservatively.\n-            logitsprocs_need_output_token_ids=bool(custom_logitsprocs),\n+            # ThinkingTokenBudgetLogitsProcessor also needs output token ids to\n+            # correctly track think start/end token sequences in async scheduling.\n+            logitsprocs_need_output_token_ids=bool(custom_logitsprocs)\n+            or (self.vllm_config.reasoning_config is not None),\n", "materials": ["```suggestion\n            or self.vllm_config.reasoning_config is not None,\n```"], "expected_artifacts": ["code_patch"], "desired_output": null}], "example_pack_status": {"desired_outputs_applied": 145, "desired_outputs_missing": 0, "desired_outputs_rejected": 0, "rejections": []}}
{"schema_version": "example-pack/v1", "builder_version": "code-pack-builder/0.1", "pack_id": "codebench_vllm-project_vllm_general", "split_id": "codebench::vllm::general", "source": "CodeBench", "learning_problem": "few_shot_skill_induction", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "input_boundary": {"auto_skill_module_can_use": ["train_examples.task_input", "train_examples.materials", "train_examples.desired_output.text"], "must_not_use_for_induction": ["private rubrics", "private judge prompts", "heldout tasks", "heldout scores", "CI logs", "maintainer-only notes"]}, "train_examples": [{"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#20320/review_2177930289", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20320", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/plugins/lora_resolvers/hf_hub_resolver.py\nReview Comment:\n![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThe `HfApi().list_repo_files()` call in `__init__` is a synchronous, blocking network operation that can delay server startup. Consider using `asyncio.to_thread` to perform this operation asynchronously to prevent blocking the main thread.\n\nOriginal Code Context:\n@@ -0,0 +1,55 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+import os\n+from typing import Optional\n+\n+from huggingface_hub import HfApi, snapshot_download\n+\n+import vllm.envs as envs\n+from vllm.lora.request import LoRARequest\n+from vllm.lora.resolver import LoRAResolverRegistry\n+from vllm.plugins.lora_resolvers.filesystem_resolver import FilesystemResolver\n+\n+\n+class HfHubResolver(FilesystemResolver):\n+\n+    def __init__(self, repo_name: str):\n+        self.repo_name = repo_name\n+        # At initialization time, get all directories\n+        # in the repo containing an adapter_config.json;\n+        # these are the dirs we will allows downloads for\n+        # for potential LoRA requests.\n+        repo_files = HfApi().list_repo_files(repo_id=repo_name)\n+        self.adapter_dirs = [\n+            name.split(\"/\")[0] for name in repo_files\n+            if name.endswith(\"adapter_config.json\")\n+        ]\n", "materials": ["![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThe `HfApi().list_repo_files()` call in `__init__` is a synchronous, blocking network operation that can delay server startup. Consider using `asyncio.to_thread` to perform this operation asynchronously to prevent blocking the main thread."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,143 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+import asyncio\n+import os\n+\n+from huggingface_hub import HfApi, snapshot_download\n+\n+import vllm.envs as envs\n+from vllm.logger import init_logger\n+from vllm.lora.request import LoRARequest\n+from vllm.lora.resolver import LoRAResolverRegistry\n+from vllm.plugins.lora_resolvers.filesystem_resolver import FilesystemResolver\n+\n+logger = init_logger(__name__)\n+\n+\n+class HfHubResolver(FilesystemResolver):\n+    def __init__(self, repo_list: list[str]):\n+        logger.warning(\n+            \"LoRA is allowing resolution from the following repositories on\"\n+            \" HF Hub: %s please note that allowing remote downloads\"\n+            \" is not secure, and that this plugin is not intended for use in\"\n+            \" production environments.\",\n+            repo_list,\n+        )\n+\n+        self.repo_list: list[str] = repo_list\n+        self.adapter_dirs: dict[str, set[str]] = {}\n+\n+    async def resolve_lora(\n+        self, base_model_name: str, lora_name: str\n+    ) -> LoRARequest | None:\n+        \"\"\"Resolves potential LoRA requests in a remote repo on HF Hub.\n+        This is effectively the same behavior as the filesystem resolver, but\n+        with a snapshot_download on dirs containing an adapter config prior\n+        to inspecting the cached dir to build a potential LoRA\n+        request.\n+        \"\"\"\n+        # If a LoRA name begins with the repository name, it's disambiguated\n+        maybe_repo = await self._resolve_repo(lora_name)\n+\n+        # If we haven't inspected this repo before, save available adapter dirs\n+        if maybe_repo is not None and maybe_repo not in self.adapter_dirs:\n+            self.adapter_dirs[maybe_repo] = await self._get_adapter_dirs(maybe_repo)\n+\n+        maybe_subpath = await self._resolve_repo_subpath(lora_name, maybe_repo)\n+\n+        if maybe_repo is None or maybe_subpath is None:\n+            return None\n+\n+        repo_path = await asyncio.to_thread(\n+            snapshot_download,\n+            repo_id=maybe_repo,\n+            allow_patterns=f\"{maybe_subpath}/*\" if maybe_subpath != \".\" else \"*\",\n+        )\n+\n+        lora_path = os.path.join(repo_path, maybe_subpath)\n+        maybe_lora_request = await self._get_lora_req_from_path(\n+            lora_name, lora_path, base_model_name\n+        )\n+        return maybe_lora_request\n+\n+    async def _resolve_repo(self, lora_name: str) -> str | None:\n+        \"\"\"Given a fully qualified path to a LoRA with respect to its HF Hub\n+        repo, match the right repo to potentially download from if one exists.\n+\n+        Args:\n+            lora_name: Path to LoRA in HF Hub, e.g., <org>/<repo>/<subpath>,\n+                match on <org>/<repo> (if it contains an adapter directly) or\n+                <org>/<repo>/ if it may have one in subdirs.\n+        \"\"\"\n+        for potential_repo in self.repo_list:\n+            if lora_name.startswith(potential_repo) and (\n+                len(lora_name) == len(potential_repo)\n+                or lora_name[len(potential_repo)] == \"/\"\n+            ):\n+                return potential_repo\n+        return None\n+\n+    async def _resolve_repo_subpath(\n+        self, lora_name: str, maybe_repo: str | None\n+    ) -> str | None:\n+        \"\"\"Given the fully qualified path of the LoRA with respect to the HF\n+        Repo, get the subpath to download from assuming it's actually got an\n+        adapter in it.\n+\n+        Args:\n+            lora_name: Path to LoRA in HF Hub, e.g., <org>/<repo>/<subpath>\n+            maybe_repo: Path to the repo to match against if one exists.\n+        \"\"\"\n+        if maybe_repo is None:\n+            return None\n+        repo_len = len(maybe_repo)\n+        if lora_name == maybe_repo or (\n+            len(lora_name) == repo_len + 1 and lora_name[-1] == \"/\"\n+        ):\n+            # Resolves to the root of the directory\n+            adapter_dir = \".\"\n+        else:\n+            # It's a subpath; removing trailing slashes if there are any\n+            adapter_dir = lora_name[repo_len + 1 :].rstrip(\"/\")\n+\n+        # Only download if the directory actually contains an adapter\n+        is_adapter = adapter_dir in self.adapter_dirs[maybe_repo]\n+        return adapter_dir if is_adapter else None\n+\n+    async def _get_adapter_dirs(self, repo_name: str) -> set[str]:\n+        \"\"\"Gets the subpaths within a HF repo that contain an adapter config.\n+\n+        Args:\n+            repo_name: Name of the HF hub repo to inspect.\n+        \"\"\"\n+        repo_files = await asyncio.to_thread(HfApi().list_repo_files, repo_id=repo_name)\n+        adapter_dirs = {\n+            os.path.dirname(name)\n+            for name in repo_files\n+            if name.endswith(\"adapter_config.json\")\n+        }\n+        if \"adapter_config.json\" in repo_files:\n+            adapter_dirs.add(\".\")\n+        return adapter_dirs\n+\n+\n+def register_hf_hub_resolver():\n+    \"\"\"Register the Hf hub LoRA Resolver with vLLM\"\"\"\n+\n+    hf_repo_list = envs.VLLM_LORA_RESOLVER_HF_REPO_LIST\n+    is_enabled = (\n+        envs.VLLM_PLUGINS is not None and \"lora_hf_hub_resolver\" in envs.VLLM_PLUGINS\n+    )\n+    if hf_repo_list:\n+        if not is_enabled:\n+            logger.warning(\n+                \"It appears that VLLM_LORA_RESOLVER_HF_REPO_LIST is set, but \"\n+                \"lora_hf_hub_resolver is not enabled in VLLM_PLUGINS; you must\"\n+                \" enable this resolver directly in VLLM_PLUGINS to use it \"\n+                \" because it allows remote downloads.\"\n+            )\n+        else:\n+            hf_hub_resolver = HfHubResolver(hf_repo_list.split(\",\"))\n+            LoRAResolverRegistry.register_resolver(\"Hf Hub Resolver\", hf_hub_resolver)\n+\n+    return", "patch_sha256": "8786f0c657ee819a"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#20320/review_2177930291", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20320", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/plugins/lora_resolvers/hf_hub_resolver.py\nReview Comment:\n![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThe logic for identifying adapter directories with `name.split(\"/\")[0]` is not robust and will fail for adapters in nested directories. Use `os.path.dirname(name)` to reliably extract the directory. Using a `set` for `self.adapter_dirs` will also improve lookup efficiency.\n\n```suggestion\n        self.adapter_dirs = {\n            os.path.dirname(name) for name in repo_files\n            if name.endswith(\"adapter_config.json\")\n        }\n```\n\nOriginal Code Context:\n@@ -0,0 +1,55 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+import os\n+from typing import Optional\n+\n+from huggingface_hub import HfApi, snapshot_download\n+\n+import vllm.envs as envs\n+from vllm.lora.request import LoRARequest\n+from vllm.lora.resolver import LoRAResolverRegistry\n+from vllm.plugins.lora_resolvers.filesystem_resolver import FilesystemResolver\n+\n+\n+class HfHubResolver(FilesystemResolver):\n+\n+    def __init__(self, repo_name: str):\n+        self.repo_name = repo_name\n+        # At initialization time, get all directories\n+        # in the repo containing an adapter_config.json;\n+        # these are the dirs we will allows downloads for\n+        # for potential LoRA requests.\n+        repo_files = HfApi().list_repo_files(repo_id=repo_name)\n+        self.adapter_dirs = [\n+            name.split(\"/\")[0] for name in repo_files\n+            if name.endswith(\"adapter_config.json\")\n+        ]\n", "materials": ["![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThe logic for identifying adapter directories with `name.split(\"/\")[0]` is not robust and will fail for adapters in nested directories. Use `os.path.dirname(name)` to reliably extract the directory. Using a `set` for `self.adapter_dirs` will also improve lookup efficiency.\n\n```suggestion\n        self.adapter_dirs = {\n            os.path.dirname(name) for name in repo_files\n            if name.endswith(\"adapter_config.json\")\n        }\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,143 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+import asyncio\n+import os\n+\n+from huggingface_hub import HfApi, snapshot_download\n+\n+import vllm.envs as envs\n+from vllm.logger import init_logger\n+from vllm.lora.request import LoRARequest\n+from vllm.lora.resolver import LoRAResolverRegistry\n+from vllm.plugins.lora_resolvers.filesystem_resolver import FilesystemResolver\n+\n+logger = init_logger(__name__)\n+\n+\n+class HfHubResolver(FilesystemResolver):\n+    def __init__(self, repo_list: list[str]):\n+        logger.warning(\n+            \"LoRA is allowing resolution from the following repositories on\"\n+            \" HF Hub: %s please note that allowing remote downloads\"\n+            \" is not secure, and that this plugin is not intended for use in\"\n+            \" production environments.\",\n+            repo_list,\n+        )\n+\n+        self.repo_list: list[str] = repo_list\n+        self.adapter_dirs: dict[str, set[str]] = {}\n+\n+    async def resolve_lora(\n+        self, base_model_name: str, lora_name: str\n+    ) -> LoRARequest | None:\n+        \"\"\"Resolves potential LoRA requests in a remote repo on HF Hub.\n+        This is effectively the same behavior as the filesystem resolver, but\n+        with a snapshot_download on dirs containing an adapter config prior\n+        to inspecting the cached dir to build a potential LoRA\n+        request.\n+        \"\"\"\n+        # If a LoRA name begins with the repository name, it's disambiguated\n+        maybe_repo = await self._resolve_repo(lora_name)\n+\n+        # If we haven't inspected this repo before, save available adapter dirs\n+        if maybe_repo is not None and maybe_repo not in self.adapter_dirs:\n+            self.adapter_dirs[maybe_repo] = await self._get_adapter_dirs(maybe_repo)\n+\n+        maybe_subpath = await self._resolve_repo_subpath(lora_name, maybe_repo)\n+\n+        if maybe_repo is None or maybe_subpath is None:\n+            return None\n+\n+        repo_path = await asyncio.to_thread(\n+            snapshot_download,\n+            repo_id=maybe_repo,\n+            allow_patterns=f\"{maybe_subpath}/*\" if maybe_subpath != \".\" else \"*\",\n+        )\n+\n+        lora_path = os.path.join(repo_path, maybe_subpath)\n+        maybe_lora_request = await self._get_lora_req_from_path(\n+            lora_name, lora_path, base_model_name\n+        )\n+        return maybe_lora_request\n+\n+    async def _resolve_repo(self, lora_name: str) -> str | None:\n+        \"\"\"Given a fully qualified path to a LoRA with respect to its HF Hub\n+        repo, match the right repo to potentially download from if one exists.\n+\n+        Args:\n+            lora_name: Path to LoRA in HF Hub, e.g., <org>/<repo>/<subpath>,\n+                match on <org>/<repo> (if it contains an adapter directly) or\n+                <org>/<repo>/ if it may have one in subdirs.\n+        \"\"\"\n+        for potential_repo in self.repo_list:\n+            if lora_name.startswith(potential_repo) and (\n+                len(lora_name) == len(potential_repo)\n+                or lora_name[len(potential_repo)] == \"/\"\n+            ):\n+                return potential_repo\n+        return None\n+\n+    async def _resolve_repo_subpath(\n+        self, lora_name: str, maybe_repo: str | None\n+    ) -> str | None:\n+        \"\"\"Given the fully qualified path of the LoRA with respect to the HF\n+        Repo, get the subpath to download from assuming it's actually got an\n+        adapter in it.\n+\n+        Args:\n+            lora_name: Path to LoRA in HF Hub, e.g., <org>/<repo>/<subpath>\n+            maybe_repo: Path to the repo to match against if one exists.\n+        \"\"\"\n+        if maybe_repo is None:\n+            return None\n+        repo_len = len(maybe_repo)\n+        if lora_name == maybe_repo or (\n+            len(lora_name) == repo_len + 1 and lora_name[-1] == \"/\"\n+        ):\n+            # Resolves to the root of the directory\n+            adapter_dir = \".\"\n+        else:\n+            # It's a subpath; removing trailing slashes if there are any\n+            adapter_dir = lora_name[repo_len + 1 :].rstrip(\"/\")\n+\n+        # Only download if the directory actually contains an adapter\n+        is_adapter = adapter_dir in self.adapter_dirs[maybe_repo]\n+        return adapter_dir if is_adapter else None\n+\n+    async def _get_adapter_dirs(self, repo_name: str) -> set[str]:\n+        \"\"\"Gets the subpaths within a HF repo that contain an adapter config.\n+\n+        Args:\n+            repo_name: Name of the HF hub repo to inspect.\n+        \"\"\"\n+        repo_files = await asyncio.to_thread(HfApi().list_repo_files, repo_id=repo_name)\n+        adapter_dirs = {\n+            os.path.dirname(name)\n+            for name in repo_files\n+            if name.endswith(\"adapter_config.json\")\n+        }\n+        if \"adapter_config.json\" in repo_files:\n+            adapter_dirs.add(\".\")\n+        return adapter_dirs\n+\n+\n+def register_hf_hub_resolver():\n+    \"\"\"Register the Hf hub LoRA Resolver with vLLM\"\"\"\n+\n+    hf_repo_list = envs.VLLM_LORA_RESOLVER_HF_REPO_LIST\n+    is_enabled = (\n+        envs.VLLM_PLUGINS is not None and \"lora_hf_hub_resolver\" in envs.VLLM_PLUGINS\n+    )\n+    if hf_repo_list:\n+        if not is_enabled:\n+            logger.warning(\n+                \"It appears that VLLM_LORA_RESOLVER_HF_REPO_LIST is set, but \"\n+                \"lora_hf_hub_resolver is not enabled in VLLM_PLUGINS; you must\"\n+                \" enable this resolver directly in VLLM_PLUGINS to use it \"\n+                \" because it allows remote downloads.\"\n+            )\n+        else:\n+            hf_hub_resolver = HfHubResolver(hf_repo_list.split(\",\"))\n+            LoRAResolverRegistry.register_resolver(\"Hf Hub Resolver\", hf_hub_resolver)\n+\n+    return", "patch_sha256": "8786f0c657ee819a"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#20320/review_2177930297", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20320", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/plugins/lora_resolvers/hf_hub_resolver.py\nReview Comment:\n![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThe `snapshot_download()` function is a synchronous I/O operation that will block the asyncio event loop. Use `asyncio.to_thread()` to perform this operation in a separate thread.\n\n```suggestion\n            repo_path = await asyncio.to_thread(\n                snapshot_download,\n                repo_id=self.repo_name,\n                allow_patterns=f\"{lora_name}/*\"\n            )\n```\n\nOriginal Code Context:\n@@ -0,0 +1,55 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+import os\n+from typing import Optional\n+\n+from huggingface_hub import HfApi, snapshot_download\n+\n+import vllm.envs as envs\n+from vllm.lora.request import LoRARequest\n+from vllm.lora.resolver import LoRAResolverRegistry\n+from vllm.plugins.lora_resolvers.filesystem_resolver import FilesystemResolver\n+\n+\n+class HfHubResolver(FilesystemResolver):\n+\n+    def __init__(self, repo_name: str):\n+        self.repo_name = repo_name\n+        # At initialization time, get all directories\n+        # in the repo containing an adapter_config.json;\n+        # these are the dirs we will allows downloads for\n+        # for potential LoRA requests.\n+        repo_files = HfApi().list_repo_files(repo_id=repo_name)\n+        self.adapter_dirs = [\n+            name.split(\"/\")[0] for name in repo_files\n+            if name.endswith(\"adapter_config.json\")\n+        ]\n+\n+    async def resolve_lora(self, base_model_name: str,\n+                           lora_name: str) -> Optional[LoRARequest]:\n+        \"\"\"Resolves potential LoRA requests in a remote repo on HF Hub.\n+        This is effectively the same behavior as the filesystem resolver, but\n+        with an extra guard + snapshot_download on dirs containing an adapter\n+        config prior to inspecting the cached dir to build a potential LoRA\n+        request.\n+        \"\"\"\n+        if lora_name in self.adapter_dirs:\n+            repo_path = snapshot_download(repo_id=self.repo_name,\n+                                          allow_patterns=f\"{lora_name}/*\")\n", "materials": ["![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThe `snapshot_download()` function is a synchronous I/O operation that will block the asyncio event loop. Use `asyncio.to_thread()` to perform this operation in a separate thread.\n\n```suggestion\n            repo_path = await asyncio.to_thread(\n                snapshot_download,\n                repo_id=self.repo_name,\n                allow_patterns=f\"{lora_name}/*\"\n            )\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,143 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+import asyncio\n+import os\n+\n+from huggingface_hub import HfApi, snapshot_download\n+\n+import vllm.envs as envs\n+from vllm.logger import init_logger\n+from vllm.lora.request import LoRARequest\n+from vllm.lora.resolver import LoRAResolverRegistry\n+from vllm.plugins.lora_resolvers.filesystem_resolver import FilesystemResolver\n+\n+logger = init_logger(__name__)\n+\n+\n+class HfHubResolver(FilesystemResolver):\n+    def __init__(self, repo_list: list[str]):\n+        logger.warning(\n+            \"LoRA is allowing resolution from the following repositories on\"\n+            \" HF Hub: %s please note that allowing remote downloads\"\n+            \" is not secure, and that this plugin is not intended for use in\"\n+            \" production environments.\",\n+            repo_list,\n+        )\n+\n+        self.repo_list: list[str] = repo_list\n+        self.adapter_dirs: dict[str, set[str]] = {}\n+\n+    async def resolve_lora(\n+        self, base_model_name: str, lora_name: str\n+    ) -> LoRARequest | None:\n+        \"\"\"Resolves potential LoRA requests in a remote repo on HF Hub.\n+        This is effectively the same behavior as the filesystem resolver, but\n+        with a snapshot_download on dirs containing an adapter config prior\n+        to inspecting the cached dir to build a potential LoRA\n+        request.\n+        \"\"\"\n+        # If a LoRA name begins with the repository name, it's disambiguated\n+        maybe_repo = await self._resolve_repo(lora_name)\n+\n+        # If we haven't inspected this repo before, save available adapter dirs\n+        if maybe_repo is not None and maybe_repo not in self.adapter_dirs:\n+            self.adapter_dirs[maybe_repo] = await self._get_adapter_dirs(maybe_repo)\n+\n+        maybe_subpath = await self._resolve_repo_subpath(lora_name, maybe_repo)\n+\n+        if maybe_repo is None or maybe_subpath is None:\n+            return None\n+\n+        repo_path = await asyncio.to_thread(\n+            snapshot_download,\n+            repo_id=maybe_repo,\n+            allow_patterns=f\"{maybe_subpath}/*\" if maybe_subpath != \".\" else \"*\",\n+        )\n+\n+        lora_path = os.path.join(repo_path, maybe_subpath)\n+        maybe_lora_request = await self._get_lora_req_from_path(\n+            lora_name, lora_path, base_model_name\n+        )\n+        return maybe_lora_request\n+\n+    async def _resolve_repo(self, lora_name: str) -> str | None:\n+        \"\"\"Given a fully qualified path to a LoRA with respect to its HF Hub\n+        repo, match the right repo to potentially download from if one exists.\n+\n+        Args:\n+            lora_name: Path to LoRA in HF Hub, e.g., <org>/<repo>/<subpath>,\n+                match on <org>/<repo> (if it contains an adapter directly) or\n+                <org>/<repo>/ if it may have one in subdirs.\n+        \"\"\"\n+        for potential_repo in self.repo_list:\n+            if lora_name.startswith(potential_repo) and (\n+                len(lora_name) == len(potential_repo)\n+                or lora_name[len(potential_repo)] == \"/\"\n+            ):\n+                return potential_repo\n+        return None\n+\n+    async def _resolve_repo_subpath(\n+        self, lora_name: str, maybe_repo: str | None\n+    ) -> str | None:\n+        \"\"\"Given the fully qualified path of the LoRA with respect to the HF\n+        Repo, get the subpath to download from assuming it's actually got an\n+        adapter in it.\n+\n+        Args:\n+            lora_name: Path to LoRA in HF Hub, e.g., <org>/<repo>/<subpath>\n+            maybe_repo: Path to the repo to match against if one exists.\n+        \"\"\"\n+        if maybe_repo is None:\n+            return None\n+        repo_len = len(maybe_repo)\n+        if lora_name == maybe_repo or (\n+            len(lora_name) == repo_len + 1 and lora_name[-1] == \"/\"\n+        ):\n+            # Resolves to the root of the directory\n+            adapter_dir = \".\"\n+        else:\n+            # It's a subpath; removing trailing slashes if there are any\n+            adapter_dir = lora_name[repo_len + 1 :].rstrip(\"/\")\n+\n+        # Only download if the directory actually contains an adapter\n+        is_adapter = adapter_dir in self.adapter_dirs[maybe_repo]\n+        return adapter_dir if is_adapter else None\n+\n+    async def _get_adapter_dirs(self, repo_name: str) -> set[str]:\n+        \"\"\"Gets the subpaths within a HF repo that contain an adapter config.\n+\n+        Args:\n+            repo_name: Name of the HF hub repo to inspect.\n+        \"\"\"\n+        repo_files = await asyncio.to_thread(HfApi().list_repo_files, repo_id=repo_name)\n+        adapter_dirs = {\n+            os.path.dirname(name)\n+            for name in repo_files\n+            if name.endswith(\"adapter_config.json\")\n+        }\n+        if \"adapter_config.json\" in repo_files:\n+            adapter_dirs.add(\".\")\n+        return adapter_dirs\n+\n+\n+def register_hf_hub_resolver():\n+    \"\"\"Register the Hf hub LoRA Resolver with vLLM\"\"\"\n+\n+    hf_repo_list = envs.VLLM_LORA_RESOLVER_HF_REPO_LIST\n+    is_enabled = (\n+        envs.VLLM_PLUGINS is not None and \"lora_hf_hub_resolver\" in envs.VLLM_PLUGINS\n+    )\n+    if hf_repo_list:\n+        if not is_enabled:\n+            logger.warning(\n+                \"It appears that VLLM_LORA_RESOLVER_HF_REPO_LIST is set, but \"\n+                \"lora_hf_hub_resolver is not enabled in VLLM_PLUGINS; you must\"\n+                \" enable this resolver directly in VLLM_PLUGINS to use it \"\n+                \" because it allows remote downloads.\"\n+            )\n+        else:\n+            hf_hub_resolver = HfHubResolver(hf_repo_list.split(\",\"))\n+            LoRAResolverRegistry.register_resolver(\"Hf Hub Resolver\", hf_hub_resolver)\n+\n+    return", "patch_sha256": "8786f0c657ee819a"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#20320/review_2178089496", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20320", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/plugins/lora_resolvers/hf_hub_resolver.py\nReview Comment:\nMoved this to the first call of the lora adapter to call in an async thread\n\nOriginal Code Context:\n@@ -0,0 +1,55 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+import os\n+from typing import Optional\n+\n+from huggingface_hub import HfApi, snapshot_download\n+\n+import vllm.envs as envs\n+from vllm.lora.request import LoRARequest\n+from vllm.lora.resolver import LoRAResolverRegistry\n+from vllm.plugins.lora_resolvers.filesystem_resolver import FilesystemResolver\n+\n+\n+class HfHubResolver(FilesystemResolver):\n+\n+    def __init__(self, repo_name: str):\n+        self.repo_name = repo_name\n+        # At initialization time, get all directories\n+        # in the repo containing an adapter_config.json;\n+        # these are the dirs we will allows downloads for\n+        # for potential LoRA requests.\n+        repo_files = HfApi().list_repo_files(repo_id=repo_name)\n+        self.adapter_dirs = [\n+            name.split(\"/\")[0] for name in repo_files\n+            if name.endswith(\"adapter_config.json\")\n+        ]\n", "materials": ["Moved this to the first call of the lora adapter to call in an async thread"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,143 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+import asyncio\n+import os\n+\n+from huggingface_hub import HfApi, snapshot_download\n+\n+import vllm.envs as envs\n+from vllm.logger import init_logger\n+from vllm.lora.request import LoRARequest\n+from vllm.lora.resolver import LoRAResolverRegistry\n+from vllm.plugins.lora_resolvers.filesystem_resolver import FilesystemResolver\n+\n+logger = init_logger(__name__)\n+\n+\n+class HfHubResolver(FilesystemResolver):\n+    def __init__(self, repo_list: list[str]):\n+        logger.warning(\n+            \"LoRA is allowing resolution from the following repositories on\"\n+            \" HF Hub: %s please note that allowing remote downloads\"\n+            \" is not secure, and that this plugin is not intended for use in\"\n+            \" production environments.\",\n+            repo_list,\n+        )\n+\n+        self.repo_list: list[str] = repo_list\n+        self.adapter_dirs: dict[str, set[str]] = {}\n+\n+    async def resolve_lora(\n+        self, base_model_name: str, lora_name: str\n+    ) -> LoRARequest | None:\n+        \"\"\"Resolves potential LoRA requests in a remote repo on HF Hub.\n+        This is effectively the same behavior as the filesystem resolver, but\n+        with a snapshot_download on dirs containing an adapter config prior\n+        to inspecting the cached dir to build a potential LoRA\n+        request.\n+        \"\"\"\n+        # If a LoRA name begins with the repository name, it's disambiguated\n+        maybe_repo = await self._resolve_repo(lora_name)\n+\n+        # If we haven't inspected this repo before, save available adapter dirs\n+        if maybe_repo is not None and maybe_repo not in self.adapter_dirs:\n+            self.adapter_dirs[maybe_repo] = await self._get_adapter_dirs(maybe_repo)\n+\n+        maybe_subpath = await self._resolve_repo_subpath(lora_name, maybe_repo)\n+\n+        if maybe_repo is None or maybe_subpath is None:\n+            return None\n+\n+        repo_path = await asyncio.to_thread(\n+            snapshot_download,\n+            repo_id=maybe_repo,\n+            allow_patterns=f\"{maybe_subpath}/*\" if maybe_subpath != \".\" else \"*\",\n+        )\n+\n+        lora_path = os.path.join(repo_path, maybe_subpath)\n+        maybe_lora_request = await self._get_lora_req_from_path(\n+            lora_name, lora_path, base_model_name\n+        )\n+        return maybe_lora_request\n+\n+    async def _resolve_repo(self, lora_name: str) -> str | None:\n+        \"\"\"Given a fully qualified path to a LoRA with respect to its HF Hub\n+        repo, match the right repo to potentially download from if one exists.\n+\n+        Args:\n+            lora_name: Path to LoRA in HF Hub, e.g., <org>/<repo>/<subpath>,\n+                match on <org>/<repo> (if it contains an adapter directly) or\n+                <org>/<repo>/ if it may have one in subdirs.\n+        \"\"\"\n+        for potential_repo in self.repo_list:\n+            if lora_name.startswith(potential_repo) and (\n+                len(lora_name) == len(potential_repo)\n+                or lora_name[len(potential_repo)] == \"/\"\n+            ):\n+                return potential_repo\n+        return None\n+\n+    async def _resolve_repo_subpath(\n+        self, lora_name: str, maybe_repo: str | None\n+    ) -> str | None:\n+        \"\"\"Given the fully qualified path of the LoRA with respect to the HF\n+        Repo, get the subpath to download from assuming it's actually got an\n+        adapter in it.\n+\n+        Args:\n+            lora_name: Path to LoRA in HF Hub, e.g., <org>/<repo>/<subpath>\n+            maybe_repo: Path to the repo to match against if one exists.\n+        \"\"\"\n+        if maybe_repo is None:\n+            return None\n+        repo_len = len(maybe_repo)\n+        if lora_name == maybe_repo or (\n+            len(lora_name) == repo_len + 1 and lora_name[-1] == \"/\"\n+        ):\n+            # Resolves to the root of the directory\n+            adapter_dir = \".\"\n+        else:\n+            # It's a subpath; removing trailing slashes if there are any\n+            adapter_dir = lora_name[repo_len + 1 :].rstrip(\"/\")\n+\n+        # Only download if the directory actually contains an adapter\n+        is_adapter = adapter_dir in self.adapter_dirs[maybe_repo]\n+        return adapter_dir if is_adapter else None\n+\n+    async def _get_adapter_dirs(self, repo_name: str) -> set[str]:\n+        \"\"\"Gets the subpaths within a HF repo that contain an adapter config.\n+\n+        Args:\n+            repo_name: Name of the HF hub repo to inspect.\n+        \"\"\"\n+        repo_files = await asyncio.to_thread(HfApi().list_repo_files, repo_id=repo_name)\n+        adapter_dirs = {\n+            os.path.dirname(name)\n+            for name in repo_files\n+            if name.endswith(\"adapter_config.json\")\n+        }\n+        if \"adapter_config.json\" in repo_files:\n+            adapter_dirs.add(\".\")\n+        return adapter_dirs\n+\n+\n+def register_hf_hub_resolver():\n+    \"\"\"Register the Hf hub LoRA Resolver with vLLM\"\"\"\n+\n+    hf_repo_list = envs.VLLM_LORA_RESOLVER_HF_REPO_LIST\n+    is_enabled = (\n+        envs.VLLM_PLUGINS is not None and \"lora_hf_hub_resolver\" in envs.VLLM_PLUGINS\n+    )\n+    if hf_repo_list:\n+        if not is_enabled:\n+            logger.warning(\n+                \"It appears that VLLM_LORA_RESOLVER_HF_REPO_LIST is set, but \"\n+                \"lora_hf_hub_resolver is not enabled in VLLM_PLUGINS; you must\"\n+                \" enable this resolver directly in VLLM_PLUGINS to use it \"\n+                \" because it allows remote downloads.\"\n+            )\n+        else:\n+            hf_hub_resolver = HfHubResolver(hf_repo_list.split(\",\"))\n+            LoRAResolverRegistry.register_resolver(\"Hf Hub Resolver\", hf_hub_resolver)\n+\n+    return", "patch_sha256": "8786f0c657ee819a"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#20320/review_2683120165", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20320", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/plugins/lora_resolvers/hf_hub_resolver.py\nReview Comment:\n### Trailing slash causes nested adapter lookup to fail\n\n**Low Severity**\n\n<!-- DESCRIPTION START -->\nThe `_resolve_repo_subpath` method handles trailing slashes for root adapter paths (e.g., `\"org/repo/\"` → `\".\"`) but not for nested paths. When a user requests `\"org/repo/adapter/\"` with a trailing slash, `adapter_dir` becomes `\"adapter/\"` while `adapter_dirs` contains `\"adapter\"` (from `os.path.dirname`). The lookup `\"adapter/\" in {\"adapter\"}` fails, returning `None` even though the adapter exists. This inconsistency could cause silent failures when users accidentally include trailing slashes on nested adapter paths.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/plugins/lora_resolvers/hf_hub_resolver.py#L87-L89\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..D4GAHs3OJ3UYIj57oDheTuPSG5pcGqEcMSBjZe9sm9HIA99_hyNn9nKUjQKaSdczsPFIcdWlRDwUvIRTxCQ7RMaODUSwv7ta0fM0AiroPk27JEh1iW64sjXibp9SRS9UQNW7PSuQuL0YgIcScdovU_TXGsyDAlh3yAuF05eUpiz0f4fdo415D88ijjH3s1oGirNMIRrLH8Dg8fEy_O7Bc2sDmdGPjPgTXSbIsS9f-GH2ABjtoJzPcoELnOry26u0pW_9UoVruxueXEQw0sg82ArgOsYsetal8SE17J87wXeVemZ2DKNKvJwc4tF8osOlXs-JpR5B6A_8uqIoKi-XjA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..0BBeZV_hftW8evngkvlLMCJpwAfBrD9lNaoARAHBikzqVMQhmpMFupC2UFqkZ412QdW9N_3MvVucxQAQvTptO8CVBXKOrTsdJap45s3vMpIq5eGZtzNnLKv2B7wgbHjipWSm044RALjAv8ZXyILDRMfOewDiKRGFrgypQVJJX7sPlQF2t17yppDllC5Vzae77S2QVScV9QESq8_L3y6fJ13OqAE_z5PTUP0ak9JXraxMy4xl6fwTcXtW9SCDTlt0nlQ2wQ0PaEfVz1MReXMimWzDA8B-P3xrp7ZCzGcDzoRvd98ZOQ3dYbnaxnaZQsktKrKdBvH-kybutH64yqa2EA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -0,0 +1,120 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+import asyncio\n+import os\n+\n+from huggingface_hub import HfApi, snapshot_download\n+\n+import vllm.envs as envs\n+from vllm.lora.request import LoRARequest\n+from vllm.lora.resolver import LoRAResolverRegistry\n+from vllm.plugins.lora_resolvers.filesystem_resolver import FilesystemResolver\n+\n+\n+class HfHubResolver(FilesystemResolver):\n+    def __init__(self, repo_list: list[str]):\n+        self.repo_list: list[str] = repo_list\n+        self.adapter_dirs: dict[str, set[str]] = {}\n+\n+    async def resolve_lora(\n+        self, base_model_name: str, lora_name: str\n+    ) -> LoRARequest | None:\n+        \"\"\"Resolves potential LoRA requests in a remote repo on HF Hub.\n+        This is effectively the same behavior as the filesystem resolver, but\n+        with a snapshot_download on dirs containing an adapter config prior\n+        to inspecting the cached dir to build a potential LoRA\n+        request.\n+        \"\"\"\n+        # If a LoRA name begins with the repository name, it's disambiguated\n+        maybe_repo = await self._resolve_repo(lora_name)\n+\n+        # If we haven't inspected this repo before, save available adapter dirs\n+        if maybe_repo is not None and maybe_repo not in self.adapter_dirs:\n+            self.adapter_dirs[maybe_repo] = await self._get_adapter_dirs(maybe_repo)\n+\n+        maybe_subpath = await self._resolve_repo_subpath(lora_name, maybe_repo)\n+\n+        if maybe_repo is None or maybe_subpath is None:\n+            return None\n+\n+        repo_path = await asyncio.to_thread(\n+            snapshot_download,\n+            repo_id=maybe_repo,\n+            allow_patterns=f\"{maybe_subpath}/*\" if maybe_subpath != \".\" else \"*\",\n+        )\n+\n+        lora_path = os.path.join(repo_path, maybe_subpath)\n+        maybe_lora_request = await self._get_lora_req_from_path(\n+            lora_name, lora_path, base_model_name\n+        )\n+        return maybe_lora_request\n+\n+    async def _resolve_repo(self, lora_name: str) -> str | None:\n+        \"\"\"Given a fully qualified path to a LoRA with respect to its HF Hub\n+        repo, match the right repo to potentially download from if one exists.\n+\n+        Args:\n+            lora_name: Path to LoRA in HF Hub, e.g., <org>/<repo>/<subpath>,\n+                match on <org>/<repo> (if it contains an adapter directly) or\n+                <org>/<repo>/ if it may have one in subdirs.\n+        \"\"\"\n+        for potential_repo in self.repo_list:\n+            if lora_name.startswith(potential_repo) and (\n+                len(lora_name) == len(potential_repo)\n+                or lora_name[len(potential_repo)] == \"/\"\n+            ):\n+                return potential_repo\n+        return None\n+\n+    async def _resolve_repo_subpath(\n+        self, lora_name: str, maybe_repo: str | None\n+    ) -> str | None:\n+        \"\"\"Given the fully qualified path of the LoRA with respect to the HF\n+        Repo, get the subpath to download from assuming it's actually got an\n+        adapter in it.\n+\n+        Args:\n+            lora_name: Path to LoRA in HF Hub, e.g., <org>/<repo>/<subpath>\n+            maybe_repo: Path to the repo to match against if one exists.\n+        \"\"\"\n+        if maybe_repo is None:\n+            return None\n+        repo_len = len(maybe_repo)\n+        if lora_name == maybe_repo or (\n+            len(lora_name) == repo_len + 1 and lora_name[-1] == \"/\"\n+        ):\n+            # Resolves to the root of the directory\n+            adapter_dir = \".\"\n+        else:\n+            adapter_dir = lora_name[repo_len + 1 :]\n", "materials": ["### Trailing slash causes nested adapter lookup to fail\n\n**Low Severity**\n\n<!-- DESCRIPTION START -->\nThe `_resolve_repo_subpath` method handles trailing slashes for root adapter paths (e.g., `\"org/repo/\"` → `\".\"`) but not for nested paths. When a user requests `\"org/repo/adapter/\"` with a trailing slash, `adapter_dir` becomes `\"adapter/\"` while `adapter_dirs` contains `\"adapter\"` (from `os.path.dirname`). The lookup `\"adapter/\" in {\"adapter\"}` fails, returning `None` even though the adapter exists. This inconsistency could cause silent failures when users accidentally include trailing slashes on nested adapter paths.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/plugins/lora_resolvers/hf_hub_resolver.py#L87-L89\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..D4GAHs3OJ3UYIj57oDheTuPSG5pcGqEcMSBjZe9sm9HIA99_hyNn9nKUjQKaSdczsPFIcdWlRDwUvIRTxCQ7RMaODUSwv7ta0fM0AiroPk27JEh1iW64sjXibp9SRS9UQNW7PSuQuL0YgIcScdovU_TXGsyDAlh3yAuF05eUpiz0f4fdo415D88ijjH3s1oGirNMIRrLH8Dg8fEy_O7Bc2sDmdGPjPgTXSbIsS9f-GH2ABjtoJzPcoELnOry26u0pW_9UoVruxueXEQw0sg82ArgOsYsetal8SE17J87wXeVemZ2DKNKvJwc4tF8osOlXs-JpR5B6A_8uqIoKi-XjA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..0BBeZV_hftW8evngkvlLMCJpwAfBrD9lNaoARAHBikzqVMQhmpMFupC2UFqkZ412QdW9N_3MvVucxQAQvTptO8CVBXKOrTsdJap45s3vMpIq5eGZtzNnLKv2B7wgbHjipWSm044RALjAv8ZXyILDRMfOewDiKRGFrgypQVJJX7sPlQF2t17yppDllC5Vzae77S2QVScV9QESq8_L3y6fJ13OqAE_z5PTUP0ak9JXraxMy4xl6fwTcXtW9SCDTlt0nlQ2wQ0PaEfVz1MReXMimWzDA8B-P3xrp7ZCzGcDzoRvd98ZOQ3dYbnaxnaZQsktKrKdBvH-kybutH64yqa2EA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,143 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+import asyncio\n+import os\n+\n+from huggingface_hub import HfApi, snapshot_download\n+\n+import vllm.envs as envs\n+from vllm.logger import init_logger\n+from vllm.lora.request import LoRARequest\n+from vllm.lora.resolver import LoRAResolverRegistry\n+from vllm.plugins.lora_resolvers.filesystem_resolver import FilesystemResolver\n+\n+logger = init_logger(__name__)\n+\n+\n+class HfHubResolver(FilesystemResolver):\n+    def __init__(self, repo_list: list[str]):\n+        logger.warning(\n+            \"LoRA is allowing resolution from the following repositories on\"\n+            \" HF Hub: %s please note that allowing remote downloads\"\n+            \" is not secure, and that this plugin is not intended for use in\"\n+            \" production environments.\",\n+            repo_list,\n+        )\n+\n+        self.repo_list: list[str] = repo_list\n+        self.adapter_dirs: dict[str, set[str]] = {}\n+\n+    async def resolve_lora(\n+        self, base_model_name: str, lora_name: str\n+    ) -> LoRARequest | None:\n+        \"\"\"Resolves potential LoRA requests in a remote repo on HF Hub.\n+        This is effectively the same behavior as the filesystem resolver, but\n+        with a snapshot_download on dirs containing an adapter config prior\n+        to inspecting the cached dir to build a potential LoRA\n+        request.\n+        \"\"\"\n+        # If a LoRA name begins with the repository name, it's disambiguated\n+        maybe_repo = await self._resolve_repo(lora_name)\n+\n+        # If we haven't inspected this repo before, save available adapter dirs\n+        if maybe_repo is not None and maybe_repo not in self.adapter_dirs:\n+            self.adapter_dirs[maybe_repo] = await self._get_adapter_dirs(maybe_repo)\n+\n+        maybe_subpath = await self._resolve_repo_subpath(lora_name, maybe_repo)\n+\n+        if maybe_repo is None or maybe_subpath is None:\n+            return None\n+\n+        repo_path = await asyncio.to_thread(\n+            snapshot_download,\n+            repo_id=maybe_repo,\n+            allow_patterns=f\"{maybe_subpath}/*\" if maybe_subpath != \".\" else \"*\",\n+        )\n+\n+        lora_path = os.path.join(repo_path, maybe_subpath)\n+        maybe_lora_request = await self._get_lora_req_from_path(\n+            lora_name, lora_path, base_model_name\n+        )\n+        return maybe_lora_request\n+\n+    async def _resolve_repo(self, lora_name: str) -> str | None:\n+        \"\"\"Given a fully qualified path to a LoRA with respect to its HF Hub\n+        repo, match the right repo to potentially download from if one exists.\n+\n+        Args:\n+            lora_name: Path to LoRA in HF Hub, e.g., <org>/<repo>/<subpath>,\n+                match on <org>/<repo> (if it contains an adapter directly) or\n+                <org>/<repo>/ if it may have one in subdirs.\n+        \"\"\"\n+        for potential_repo in self.repo_list:\n+            if lora_name.startswith(potential_repo) and (\n+                len(lora_name) == len(potential_repo)\n+                or lora_name[len(potential_repo)] == \"/\"\n+            ):\n+                return potential_repo\n+        return None\n+\n+    async def _resolve_repo_subpath(\n+        self, lora_name: str, maybe_repo: str | None\n+    ) -> str | None:\n+        \"\"\"Given the fully qualified path of the LoRA with respect to the HF\n+        Repo, get the subpath to download from assuming it's actually got an\n+        adapter in it.\n+\n+        Args:\n+            lora_name: Path to LoRA in HF Hub, e.g., <org>/<repo>/<subpath>\n+            maybe_repo: Path to the repo to match against if one exists.\n+        \"\"\"\n+        if maybe_repo is None:\n+            return None\n+        repo_len = len(maybe_repo)\n+        if lora_name == maybe_repo or (\n+            len(lora_name) == repo_len + 1 and lora_name[-1] == \"/\"\n+        ):\n+            # Resolves to the root of the directory\n+            adapter_dir = \".\"\n+        else:\n+            # It's a subpath; removing trailing slashes if there are any\n+            adapter_dir = lora_name[repo_len + 1 :].rstrip(\"/\")\n+\n+        # Only download if the directory actually contains an adapter\n+        is_adapter = adapter_dir in self.adapter_dirs[maybe_repo]\n+        return adapter_dir if is_adapter else None\n+\n+    async def _get_adapter_dirs(self, repo_name: str) -> set[str]:\n+        \"\"\"Gets the subpaths within a HF repo that contain an adapter config.\n+\n+        Args:\n+            repo_name: Name of the HF hub repo to inspect.\n+        \"\"\"\n+        repo_files = await asyncio.to_thread(HfApi().list_repo_files, repo_id=repo_name)\n+        adapter_dirs = {\n+            os.path.dirname(name)\n+            for name in repo_files\n+            if name.endswith(\"adapter_config.json\")\n+        }\n+        if \"adapter_config.json\" in repo_files:\n+            adapter_dirs.add(\".\")\n+        return adapter_dirs\n+\n+\n+def register_hf_hub_resolver():\n+    \"\"\"Register the Hf hub LoRA Resolver with vLLM\"\"\"\n+\n+    hf_repo_list = envs.VLLM_LORA_RESOLVER_HF_REPO_LIST\n+    is_enabled = (\n+        envs.VLLM_PLUGINS is not None and \"lora_hf_hub_resolver\" in envs.VLLM_PLUGINS\n+    )\n+    if hf_repo_list:\n+        if not is_enabled:\n+            logger.warning(\n+                \"It appears that VLLM_LORA_RESOLVER_HF_REPO_LIST is set, but \"\n+                \"lora_hf_hub_resolver is not enabled in VLLM_PLUGINS; you must\"\n+                \" enable this resolver directly in VLLM_PLUGINS to use it \"\n+                \" because it allows remote downloads.\"\n+            )\n+        else:\n+            hf_hub_resolver = HfHubResolver(hf_repo_list.split(\",\"))\n+            LoRAResolverRegistry.register_resolver(\"Hf Hub Resolver\", hf_hub_resolver)\n+\n+    return", "patch_sha256": "8786f0c657ee819a"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#20320/review_2684684157", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20320", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/envs.py\nReview Comment:\nThis should be next to `VLLM_LORA_RESOLVER_CACHE_DIR`\n\nOriginal Code Context:\n@@ -92,6 +92,7 @@\n     # Deprecated env variables for profiling, kept for backward compatibility\n     # See also vllm/config/profiler.py and `--profiler-config` argument\n     VLLM_TORCH_CUDA_PROFILE: str | None = None\n+    VLLM_LORA_RESOLVER_HF_REPO_LIST: str | None = None\n", "materials": ["This should be next to `VLLM_LORA_RESOLVER_CACHE_DIR`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -89,6 +89,7 @@\n     VLLM_HTTP_TIMEOUT_KEEP_ALIVE: int = 5  # seconds\n     VLLM_PLUGINS: list[str] | None = None\n     VLLM_LORA_RESOLVER_CACHE_DIR: str | None = None\n+    VLLM_LORA_RESOLVER_HF_REPO_LIST: str | None = None\n     # Deprecated env variables for profiling, kept for backward compatibility\n     # See also vllm/config/profiler.py and `--profiler-config` argument\n     VLLM_TORCH_CUDA_PROFILE: str | None = None\n@@ -863,6 +864,13 @@ def get_vllm_port() -> int | None:\n     \"VLLM_LORA_RESOLVER_CACHE_DIR\": lambda: os.getenv(\n         \"VLLM_LORA_RESOLVER_CACHE_DIR\", None\n     ),\n+    # A remote HF repo(s) containing one or more LoRA adapters, which\n+    # may be downloaded and leveraged as needed. Only works if plugins\n+    # are enabled and VLLM_ALLOW_RUNTIME_LORA_UPDATING is enabled.\n+    # Values should be comma separated.\n+    \"VLLM_LORA_RESOLVER_HF_REPO_LIST\": lambda: os.getenv(\n+        \"VLLM_LORA_RESOLVER_HF_REPO_LIST\", None\n+    ),\n     # Enables torch CUDA profiling if set to 1.\n     # Deprecated, see profiler_config.\n     \"VLLM_TORCH_CUDA_PROFILE\": lambda: os.getenv(\"VLLM_TORCH_CUDA_PROFILE\"),", "patch_sha256": "4e98e8581e7d189f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#20320/review_2688758906", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20320", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/plugins/lora_resolvers/hf_hub_resolver.py\nReview Comment:\nI checked, and the general plugins actually _were_ loaded by default (although still need to set the repo list for this one to be able to do anything), so added another guard\n\nOriginal Code Context:\n@@ -114,8 +125,19 @@ def register_hf_hub_resolver():\n     \"\"\"Register the Hf hub LoRA Resolver with vLLM\"\"\"\n \n     hf_repo_list = envs.VLLM_LORA_RESOLVER_HF_REPO_LIST\n+    is_enabled = (\n", "materials": ["I checked, and the general plugins actually _were_ loaded by default (although still need to set the repo list for this one to be able to do anything), so added another guard"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,143 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+import asyncio\n+import os\n+\n+from huggingface_hub import HfApi, snapshot_download\n+\n+import vllm.envs as envs\n+from vllm.logger import init_logger\n+from vllm.lora.request import LoRARequest\n+from vllm.lora.resolver import LoRAResolverRegistry\n+from vllm.plugins.lora_resolvers.filesystem_resolver import FilesystemResolver\n+\n+logger = init_logger(__name__)\n+\n+\n+class HfHubResolver(FilesystemResolver):\n+    def __init__(self, repo_list: list[str]):\n+        logger.warning(\n+            \"LoRA is allowing resolution from the following repositories on\"\n+            \" HF Hub: %s please note that allowing remote downloads\"\n+            \" is not secure, and that this plugin is not intended for use in\"\n+            \" production environments.\",\n+            repo_list,\n+        )\n+\n+        self.repo_list: list[str] = repo_list\n+        self.adapter_dirs: dict[str, set[str]] = {}\n+\n+    async def resolve_lora(\n+        self, base_model_name: str, lora_name: str\n+    ) -> LoRARequest | None:\n+        \"\"\"Resolves potential LoRA requests in a remote repo on HF Hub.\n+        This is effectively the same behavior as the filesystem resolver, but\n+        with a snapshot_download on dirs containing an adapter config prior\n+        to inspecting the cached dir to build a potential LoRA\n+        request.\n+        \"\"\"\n+        # If a LoRA name begins with the repository name, it's disambiguated\n+        maybe_repo = await self._resolve_repo(lora_name)\n+\n+        # If we haven't inspected this repo before, save available adapter dirs\n+        if maybe_repo is not None and maybe_repo not in self.adapter_dirs:\n+            self.adapter_dirs[maybe_repo] = await self._get_adapter_dirs(maybe_repo)\n+\n+        maybe_subpath = await self._resolve_repo_subpath(lora_name, maybe_repo)\n+\n+        if maybe_repo is None or maybe_subpath is None:\n+            return None\n+\n+        repo_path = await asyncio.to_thread(\n+            snapshot_download,\n+            repo_id=maybe_repo,\n+            allow_patterns=f\"{maybe_subpath}/*\" if maybe_subpath != \".\" else \"*\",\n+        )\n+\n+        lora_path = os.path.join(repo_path, maybe_subpath)\n+        maybe_lora_request = await self._get_lora_req_from_path(\n+            lora_name, lora_path, base_model_name\n+        )\n+        return maybe_lora_request\n+\n+    async def _resolve_repo(self, lora_name: str) -> str | None:\n+        \"\"\"Given a fully qualified path to a LoRA with respect to its HF Hub\n+        repo, match the right repo to potentially download from if one exists.\n+\n+        Args:\n+            lora_name: Path to LoRA in HF Hub, e.g., <org>/<repo>/<subpath>,\n+                match on <org>/<repo> (if it contains an adapter directly) or\n+                <org>/<repo>/ if it may have one in subdirs.\n+        \"\"\"\n+        for potential_repo in self.repo_list:\n+            if lora_name.startswith(potential_repo) and (\n+                len(lora_name) == len(potential_repo)\n+                or lora_name[len(potential_repo)] == \"/\"\n+            ):\n+                return potential_repo\n+        return None\n+\n+    async def _resolve_repo_subpath(\n+        self, lora_name: str, maybe_repo: str | None\n+    ) -> str | None:\n+        \"\"\"Given the fully qualified path of the LoRA with respect to the HF\n+        Repo, get the subpath to download from assuming it's actually got an\n+        adapter in it.\n+\n+        Args:\n+            lora_name: Path to LoRA in HF Hub, e.g., <org>/<repo>/<subpath>\n+            maybe_repo: Path to the repo to match against if one exists.\n+        \"\"\"\n+        if maybe_repo is None:\n+            return None\n+        repo_len = len(maybe_repo)\n+        if lora_name == maybe_repo or (\n+            len(lora_name) == repo_len + 1 and lora_name[-1] == \"/\"\n+        ):\n+            # Resolves to the root of the directory\n+            adapter_dir = \".\"\n+        else:\n+            # It's a subpath; removing trailing slashes if there are any\n+            adapter_dir = lora_name[repo_len + 1 :].rstrip(\"/\")\n+\n+        # Only download if the directory actually contains an adapter\n+        is_adapter = adapter_dir in self.adapter_dirs[maybe_repo]\n+        return adapter_dir if is_adapter else None\n+\n+    async def _get_adapter_dirs(self, repo_name: str) -> set[str]:\n+        \"\"\"Gets the subpaths within a HF repo that contain an adapter config.\n+\n+        Args:\n+            repo_name: Name of the HF hub repo to inspect.\n+        \"\"\"\n+        repo_files = await asyncio.to_thread(HfApi().list_repo_files, repo_id=repo_name)\n+        adapter_dirs = {\n+            os.path.dirname(name)\n+            for name in repo_files\n+            if name.endswith(\"adapter_config.json\")\n+        }\n+        if \"adapter_config.json\" in repo_files:\n+            adapter_dirs.add(\".\")\n+        return adapter_dirs\n+\n+\n+def register_hf_hub_resolver():\n+    \"\"\"Register the Hf hub LoRA Resolver with vLLM\"\"\"\n+\n+    hf_repo_list = envs.VLLM_LORA_RESOLVER_HF_REPO_LIST\n+    is_enabled = (\n+        envs.VLLM_PLUGINS is not None and \"lora_hf_hub_resolver\" in envs.VLLM_PLUGINS\n+    )\n+    if hf_repo_list:\n+        if not is_enabled:\n+            logger.warning(\n+                \"It appears that VLLM_LORA_RESOLVER_HF_REPO_LIST is set, but \"\n+                \"lora_hf_hub_resolver is not enabled in VLLM_PLUGINS; you must\"\n+                \" enable this resolver directly in VLLM_PLUGINS to use it \"\n+                \" because it allows remote downloads.\"\n+            )\n+        else:\n+            hf_hub_resolver = HfHubResolver(hf_repo_list.split(\",\"))\n+            LoRAResolverRegistry.register_resolver(\"Hf Hub Resolver\", hf_hub_resolver)\n+\n+    return", "patch_sha256": "8786f0c657ee819a"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#23465/review_2296255608", "source": "CodeBench", "source_task_id": "vllm-project/vllm#23465", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: cmake/external_projects/vllm_flash_attn.cmake\nReview Comment:\n![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nPinning dependencies to a specific commit hash is good for reproducibility. However, for long-term maintenance and release builds, it's better to use a git tag. Commit hashes that are not part of a branch or tag can be garbage collected by git, or become hard to track. Since this is a work-in-progress, it's acceptable for now, but it would be best to create a tag in the `vllm-project/flash-attention` repository for this commit before this PR is merged.\n\nOriginal Code Context:\n@@ -38,7 +38,7 @@ else()\n   FetchContent_Declare(\n           vllm-flash-attn\n           GIT_REPOSITORY https://github.com/vllm-project/flash-attention.git\n-          GIT_TAG 57b4e68b9f9d94750b46de8f8dbd2bfcc86edd4f\n+          GIT_TAG c1756482dc3eb11442fc2acd1637920852a66f31\n", "materials": ["![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nPinning dependencies to a specific commit hash is good for reproducibility. However, for long-term maintenance and release builds, it's better to use a git tag. Commit hashes that are not part of a branch or tag can be garbage collected by git, or become hard to track. Since this is a work-in-progress, it's acceptable for now, but it would be best to create a tag in the `vllm-project/flash-attention` repository for this commit before this PR is merged."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -38,7 +38,7 @@ else()\n   FetchContent_Declare(\n           vllm-flash-attn\n           GIT_REPOSITORY https://github.com/vllm-project/flash-attention.git\n-          GIT_TAG 188be16520ceefdc625fdf71365585d2ee348fe2\n+          GIT_TAG 2adfc8c2177c5b0e8ddeedfd5a8990d80eb496ff\n           GIT_PROGRESS TRUE\n           # Don't share the vllm-flash-attn build between build types\n           BINARY_DIR ${CMAKE_BINARY_DIR}/vllm-flash-attn", "patch_sha256": "6ab32016b61bda11"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2324999206", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/utils.py\nReview Comment:\n![critical](https://www.gstatic.com/codereviewagent/critical.svg)\n\nThe refactoring of this function removes an important safety check for models with multiple attention layers at the same index (e.g., self-attention and cross-attention in encoder-decoder models). The previous implementation would raise a `NotImplementedError` for this case, preventing potential issues. The new implementation implicitly handles this by sorting layers alphabetically when their indices are the same. This can lead to an incorrect ordering of KV caches in `runner_kv_caches`, which is critical for features like distributed KV cache transfer. This could cause subtle and hard-to-debug correctness issues. It's recommended to either restore the explicit check to prevent ambiguity or implement a more robust sorting mechanism if the goal is to extend support for such models.\n\nOriginal Code Context:\n@@ -255,25 +254,11 @@ def bind_kv_cache(\n             layers with layer names as keys.\n         runner_kv_caches: The kv_cache declared by ModelRunner.\n     \"\"\"\n-    # Bind kv_caches to ModelRunner\n-    assert len(runner_kv_caches) == 0\n-\n-    # Convert kv_caches dict to a list of tensors in the order of layer_index.\n-    index2name = defaultdict(list)\n-    for layer_name in kv_caches:\n-        index2name[extract_layer_index(layer_name)].append(layer_name)\n-\n-    for layer_index in sorted(index2name.keys()):\n-        layer_names = index2name[layer_index]\n-        if len(layer_names) > 1:\n-            # One typical case is encoder-decoder model, e.g., bart.\n-            # The cross attention and self attention in the same decoder layer\n-            # has different layer_name but the same layer_index.\n-            raise NotImplementedError\n-        layer_name = layer_names[0]\n-        runner_kv_caches.append(kv_caches[layer_name])\n-\n-    # Bind kv_caches to forward context\n-    for layer_name, kv_cache in kv_caches.items():\n+    layer_names1 = set(kv_caches.keys())\n+    layer_names2 = set(forward_context.keys())\n+    assert layer_names1 == layer_names2\n+    sorted_layers: list[str] = sorted(layer_names1, key=extract_layer_index)\n+    for layer in sorted_layers:\n         # NOTE: Use list because of v0 PP virtual engine.\n-        forward_context[layer_name].kv_cache = [kv_cache]\n+        forward_context[layer].kv_cache = [kv_caches[layer]]\n+        runner_kv_caches.append(kv_caches[layer])\n", "materials": ["![critical](https://www.gstatic.com/codereviewagent/critical.svg)\n\nThe refactoring of this function removes an important safety check for models with multiple attention layers at the same index (e.g., self-attention and cross-attention in encoder-decoder models). The previous implementation would raise a `NotImplementedError` for this case, preventing potential issues. The new implementation implicitly handles this by sorting layers alphabetically when their indices are the same. This can lead to an incorrect ordering of KV caches in `runner_kv_caches`, which is critical for features like distributed KV cache transfer. This could cause subtle and hard-to-debug correctness issues. It's recommended to either restore the explicit check to prevent ambiguity or implement a more robust sorting mechanism if the goal is to extend support for such models."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -352,8 +352,8 @@ def bind_kv_cache(\n                 pass\n             else:\n                 raise NotImplementedError\n-        layer_name = layer_names[0]\n-        runner_kv_caches.append(kv_caches[layer_name])\n+        for layer_name in layer_names:\n+            runner_kv_caches.append(kv_caches[layer_name])\n \n     # Bind kv_caches to forward context\n     for layer_name, kv_cache in kv_caches.items():", "patch_sha256": "1c180d544e104ffe"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2329934141", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/engine/arg_utils.py\nReview Comment:\nWe should not be returning here, there are still more cases where `False` could be returned below. Simply remove the error\r\n\r\n```suggestion\r\n```\n\nOriginal Code Context:\n@@ -1486,10 +1486,7 @@ def _is_v1_supported_oracle(self, model_config: ModelConfig) -> bool:\n         # V1 supports N-gram, Medusa, and Eagle speculative decoding.\n         if (self.speculative_config is not None\n                 and self.speculative_config.get(\"method\") == \"draft_model\"):\n-            raise NotImplementedError(\n-                \"Speculative decoding with draft model is not supported yet. \"\n-                \"Please consider using other speculative decoding methods \"\n-                \"such as ngram, medusa, eagle, or deepseek_mtp.\")\n+            return True\n", "materials": ["We should not be returning here, there are still more cases where `False` could be returned below. Simply remove the error\r\n\r\n```suggestion\r\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1776,21 +1776,6 @@ def _check_feature_supported(self, model_config: ModelConfig):\n         ):\n             _raise_unsupported_error(feature_name=\"Concurrent Partial Prefill\")\n \n-        # N-gram, Medusa, and Eagle are supported for speculative decoding.\n-        if self.speculative_config is not None:\n-            # speculative_config could still be a dict at this point\n-            if isinstance(self.speculative_config, dict):\n-                method = self.speculative_config.get(\"method\", None)\n-            else:\n-                method = self.speculative_config.method\n-\n-            if method == \"draft_model\":\n-                raise NotImplementedError(\n-                    \"Draft model speculative decoding is not supported yet. \"\n-                    \"Please consider using other speculative decoding methods \"\n-                    \"such as ngram, medusa, eagle, or mtp.\"\n-                )\n-\n         if self.pipeline_parallel_size > 1:\n             supports_pp = getattr(\n                 self.distributed_executor_backend, \"supports_pp\", False", "patch_sha256": "6a0eef802606af72"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2329943097", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/engine/arg_utils.py\nReview Comment:\nMakes sense!\n\nOriginal Code Context:\n@@ -1486,10 +1486,7 @@ def _is_v1_supported_oracle(self, model_config: ModelConfig) -> bool:\n         # V1 supports N-gram, Medusa, and Eagle speculative decoding.\n         if (self.speculative_config is not None\n                 and self.speculative_config.get(\"method\") == \"draft_model\"):\n-            raise NotImplementedError(\n-                \"Speculative decoding with draft model is not supported yet. \"\n-                \"Please consider using other speculative decoding methods \"\n-                \"such as ngram, medusa, eagle, or deepseek_mtp.\")\n+            return True\n", "materials": ["Makes sense!"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1776,21 +1776,6 @@ def _check_feature_supported(self, model_config: ModelConfig):\n         ):\n             _raise_unsupported_error(feature_name=\"Concurrent Partial Prefill\")\n \n-        # N-gram, Medusa, and Eagle are supported for speculative decoding.\n-        if self.speculative_config is not None:\n-            # speculative_config could still be a dict at this point\n-            if isinstance(self.speculative_config, dict):\n-                method = self.speculative_config.get(\"method\", None)\n-            else:\n-                method = self.speculative_config.method\n-\n-            if method == \"draft_model\":\n-                raise NotImplementedError(\n-                    \"Draft model speculative decoding is not supported yet. \"\n-                    \"Please consider using other speculative decoding methods \"\n-                    \"such as ngram, medusa, eagle, or mtp.\"\n-                )\n-\n         if self.pipeline_parallel_size > 1:\n             supports_pp = getattr(\n                 self.distributed_executor_backend, \"supports_pp\", False", "patch_sha256": "6a0eef802606af72"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2330055185", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/utils.py\nReview Comment:\nCan you reevaluate your review?\n\nOriginal Code Context:\n@@ -255,25 +254,11 @@ def bind_kv_cache(\n             layers with layer names as keys.\n         runner_kv_caches: The kv_cache declared by ModelRunner.\n     \"\"\"\n-    # Bind kv_caches to ModelRunner\n-    assert len(runner_kv_caches) == 0\n-\n-    # Convert kv_caches dict to a list of tensors in the order of layer_index.\n-    index2name = defaultdict(list)\n-    for layer_name in kv_caches:\n-        index2name[extract_layer_index(layer_name)].append(layer_name)\n-\n-    for layer_index in sorted(index2name.keys()):\n-        layer_names = index2name[layer_index]\n-        if len(layer_names) > 1:\n-            # One typical case is encoder-decoder model, e.g., bart.\n-            # The cross attention and self attention in the same decoder layer\n-            # has different layer_name but the same layer_index.\n-            raise NotImplementedError\n-        layer_name = layer_names[0]\n-        runner_kv_caches.append(kv_caches[layer_name])\n-\n-    # Bind kv_caches to forward context\n-    for layer_name, kv_cache in kv_caches.items():\n+    layer_names1 = set(kv_caches.keys())\n+    layer_names2 = set(forward_context.keys())\n+    assert layer_names1 == layer_names2\n+    sorted_layers: list[str] = sorted(layer_names1, key=extract_layer_index)\n+    for layer in sorted_layers:\n         # NOTE: Use list because of v0 PP virtual engine.\n-        forward_context[layer_name].kv_cache = [kv_cache]\n+        forward_context[layer].kv_cache = [kv_caches[layer]]\n+        runner_kv_caches.append(kv_caches[layer])\n", "materials": ["Can you reevaluate your review?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -352,8 +352,8 @@ def bind_kv_cache(\n                 pass\n             else:\n                 raise NotImplementedError\n-        layer_name = layer_names[0]\n-        runner_kv_caches.append(kv_caches[layer_name])\n+        for layer_name in layer_names:\n+            runner_kv_caches.append(kv_caches[layer_name])\n \n     # Bind kv_caches to forward context\n     for layer_name, kv_cache in kv_caches.items():", "patch_sha256": "1c180d544e104ffe"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2674763408", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/attention/backends/utils.py\nReview Comment:\n### Missing `dcp_local_seq_lens` field when creating new CommonAttentionMetadata for draft model\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `extend_all_queries_by_1` function creates a new `CommonAttentionMetadata` object but doesn't copy the `dcp_local_seq_lens` field from the original metadata. This field is used for decode context parallelism (DCP) - a feature that distributes KV cache across multiple GPUs.\n\nWhen DCP is enabled (`decode_context_parallel_size > 1`), the attention backends (e.g., `FlashAttention`, `FlashInfer`) use `dcp_local_seq_lens` to compute the local sequence lengths for each rank. If this field is `None` when it should have a value, it could cause incorrect attention computation or runtime errors.\n\nFor comparison, the `prepare_inputs_padded` method in `vllm/v1/spec_decode/eagle.py` (line 730) correctly copies this field when creating new `CommonAttentionMetadata`.\n\n<details>\n<summary>🔬 Verification Test</summary>\n\n**Code analysis showing the issue:**\n\nIn `extend_all_queries_by_1` (vllm/v1/attention/backends/utils.py lines 217-231):\n```python\nnew_cad = CommonAttentionMetadata(\n    query_start_loc=new_query_start_loc,\n    query_start_loc_cpu=new_query_start_loc_cpu,\n    seq_lens=new_seq_lens,\n    num_reqs=cad.num_reqs,\n    num_actual_tokens=cad.num_actual_tokens + cad.batch_size(),\n    max_query_len=cad.max_query_len + 1,\n    max_seq_len=cad.max_seq_len + 1,\n    block_table_tensor=cad.block_table_tensor,\n    slot_mapping=new_slot_mapping,\n    # Missing: dcp_local_seq_lens=cad.dcp_local_seq_lens\n)\n```\n\nCompared to `prepare_inputs_padded` in eagle.py (lines 717-731):\n```python\nspec_common_attn_metadata = CommonAttentionMetadata(\n    # ... other fields ...\n    dcp_local_seq_lens=common_attn_metadata.dcp_local_seq_lens,  # Correctly copied\n)\n```\n\n**Why this proves the bug:** The `dcp_local_seq_lens` field defaults to `None` when not explicitly set. If DCP is enabled and the original metadata has a non-None `dcp_local_seq_lens`, the new metadata will incorrectly have `None`, potentially causing incorrect attention calculations or errors in the attention backend when using draft model speculative decoding with DCP.\n</details>\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/attention/backends/utils.py#L196-L231\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Fjmv1zRUUflVOucF16H6dCnDUzQZwAnOT2dzFLazYD_XfMuitYv-YUWnY2dbHY-pRoO_h43He4YdAsNfx9QMAyvsy5iRWPyqh8P3Eievy4mVoRmiMKnmyaoj0ULyktE8pAp4h6WDRdE4e-ro6rFAGfhuv9h83rrVScs05GTe6XOaYli_FjPu4a8ojnWrQkj3VX9iqFVI-NyKUycaHF-smEmli6Jfo806cVdfEY_RXwI6QX_7QVy-1rNOH70n2BBxtAgYqD9nbBtI3wsPpuZPxYRZgVe8WUtGAJou68UBmz4GWFdIrrWx85WF8wWGrPLnOvG1OhcJgOL8fphn3dYBSA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..IuVU7ULcL-i0h0YJrf6sY9UUzvZn43BVkZ-RJb_irtff_eQnz80Q1WqOqosSf1a6ovYGfyLipAxH7kaEmDXKt1pDiWqduiYteJYfHsO0gZ49iBb7y07C_CmvYdvKiAJXZd8kN_xbjhB8KqragB7IRewpQ1M38uArNKW3XmrvDfAxbDdcsWhn9TlQeyiA3cyeVfIkAf89XgKdq32ZdUNlXKJ6qUed5_eIEVhAwcib5QXR8VJUrVNkuoylYtvea6xip1-SwH7UFdSyIYs9wod4KQ8b-JXKC1CWERTnQZuGXJb9h5egnD_V7ytOZh-GBCYUjdLPrRTq2Asq6WgEHd_BLQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -187,6 +194,43 @@ def slice_query_start_locs(\n     )\n \n \n+def extend_all_queries_by_1(\n+    common_attn_metadata: CommonAttentionMetadata,\n+    arange: torch.Tensor,\n+    new_slot_mapping: torch.Tensor,\n+) -> CommonAttentionMetadata:\n+    \"\"\"\n+    Creates a new CommonAttentionMetadata with all query lengths increased by 1.\n+    Also all seq lens are increased by 1.\n+    This is useful e.g. in speculative decoding with draft models, where we\n+    extend each sequence by 1 token.\n+    The slot mapping is computed externally, as it requires more information.\n+    \"\"\"\n+    cad = common_attn_metadata\n+    # query start loc must be increased by [+0, +1, +2, ..., +batch_size]\n+    new_query_start_loc = cad.query_start_loc + arange[: len(cad.query_start_loc)]\n+    new_query_start_loc_cpu = cad.query_start_loc_cpu + torch.arange(\n+        len(cad.query_start_loc_cpu)\n+    )\n+    new_seq_lens = cad.seq_lens + 1\n+\n+    new_cad = CommonAttentionMetadata(\n+        query_start_loc=new_query_start_loc,\n+        query_start_loc_cpu=new_query_start_loc_cpu,\n+        seq_lens=new_seq_lens,\n+        num_reqs=cad.num_reqs,  # num requests stays unchanged\n+        # each request is extended by 1 token -> batch_size tokens are added\n+        num_actual_tokens=cad.num_actual_tokens + cad.batch_size(),\n+        # All query lens increase by 1, so max query len increases by 1\n+        max_query_len=cad.max_query_len + 1,\n+        max_seq_len=cad.max_seq_len + 1,\n+        # block table tensor depends on num requests, which stays constant\n+        block_table_tensor=cad.block_table_tensor,\n+        slot_mapping=new_slot_mapping,\n+    )\n+    return new_cad\n", "materials": ["### Missing `dcp_local_seq_lens` field when creating new CommonAttentionMetadata for draft model\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `extend_all_queries_by_1` function creates a new `CommonAttentionMetadata` object but doesn't copy the `dcp_local_seq_lens` field from the original metadata. This field is used for decode context parallelism (DCP) - a feature that distributes KV cache across multiple GPUs.\n\nWhen DCP is enabled (`decode_context_parallel_size > 1`), the attention backends (e.g., `FlashAttention`, `FlashInfer`) use `dcp_local_seq_lens` to compute the local sequence lengths for each rank. If this field is `None` when it should have a value, it could cause incorrect attention computation or runtime errors.\n\nFor comparison, the `prepare_inputs_padded` method in `vllm/v1/spec_decode/eagle.py` (line 730) correctly copies this field when creating new `CommonAttentionMetadata`.\n\n<details>\n<summary>🔬 Verification Test</summary>\n\n**Code analysis showing the issue:**\n\nIn `extend_all_queries_by_1` (vllm/v1/attention/backends/utils.py lines 217-231):\n```python\nnew_cad = CommonAttentionMetadata(\n    query_start_loc=new_query_start_loc,\n    query_start_loc_cpu=new_query_start_loc_cpu,\n    seq_lens=new_seq_lens,\n    num_reqs=cad.num_reqs,\n    num_actual_tokens=cad.num_actual_tokens + cad.batch_size(),\n    max_query_len=cad.max_query_len + 1,\n    max_seq_len=cad.max_seq_len + 1,\n    block_table_tensor=cad.block_table_tensor,\n    slot_mapping=new_slot_mapping,\n    # Missing: dcp_local_seq_lens=cad.dcp_local_seq_lens\n)\n```\n\nCompared to `prepare_inputs_padded` in eagle.py (lines 717-731):\n```python\nspec_common_attn_metadata = CommonAttentionMetadata(\n    # ... other fields ...\n    dcp_local_seq_lens=common_attn_metadata.dcp_local_seq_lens,  # Correctly copied\n)\n```\n\n**Why this proves the bug:** The `dcp_local_seq_lens` field defaults to `None` when not explicitly set. If DCP is enabled and the original metadata has a non-None `dcp_local_seq_lens`, the new metadata will incorrectly have `None`, potentially causing incorrect attention calculations or errors in the attention backend when using draft model speculative decoding with DCP.\n</details>\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/attention/backends/utils.py#L196-L231\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Fjmv1zRUUflVOucF16H6dCnDUzQZwAnOT2dzFLazYD_XfMuitYv-YUWnY2dbHY-pRoO_h43He4YdAsNfx9QMAyvsy5iRWPyqh8P3Eievy4mVoRmiMKnmyaoj0ULyktE8pAp4h6WDRdE4e-ro6rFAGfhuv9h83rrVScs05GTe6XOaYli_FjPu4a8ojnWrQkj3VX9iqFVI-NyKUycaHF-smEmli6Jfo806cVdfEY_RXwI6QX_7QVy-1rNOH70n2BBxtAgYqD9nbBtI3wsPpuZPxYRZgVe8WUtGAJou68UBmz4GWFdIrrWx85WF8wWGrPLnOvG1OhcJgOL8fphn3dYBSA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..IuVU7ULcL-i0h0YJrf6sY9UUzvZn43BVkZ-RJb_irtff_eQnz80Q1WqOqosSf1a6ovYGfyLipAxH7kaEmDXKt1pDiWqduiYteJYfHsO0gZ49iBb7y07C_CmvYdvKiAJXZd8kN_xbjhB8KqragB7IRewpQ1M38uArNKW3XmrvDfAxbDdcsWhn9TlQeyiA3cyeVfIkAf89XgKdq32ZdUNlXKJ6qUed5_eIEVhAwcib5QXR8VJUrVNkuoylYtvea6xip1-SwH7UFdSyIYs9wod4KQ8b-JXKC1CWERTnQZuGXJb9h5egnD_V7ytOZh-GBCYUjdLPrRTq2Asq6WgEHd_BLQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -818,3 +818,35 @@ def get_dcp_local_seq_lens(\n     )\n     dcp_local_seq_lens = base + remainder\n     return dcp_local_seq_lens.squeeze(1)\n+\n+\n+def extend_all_queries_by_1(\n+    common_attn_metadata: CommonAttentionMetadata,\n+    arange: torch.Tensor,\n+    new_slot_mapping: torch.Tensor,\n+) -> CommonAttentionMetadata:\n+    \"\"\"\n+    Creates a new CommonAttentionMetadata with all query lengths increased by 1.\n+    Also all seq lens are increased by 1.\n+    This is useful e.g. in speculative decoding with draft models, where we\n+    extend each sequence by 1 token.\n+    The slot mapping is computed externally, as it requires more information.\n+    \"\"\"\n+    cad = common_attn_metadata\n+    # query start loc must be increased by [+0, +1, +2, ..., +batch_size]\n+    new_query_start_loc = cad.query_start_loc + arange[: len(cad.query_start_loc)]\n+    new_query_start_loc_cpu = cad.query_start_loc_cpu + torch.arange(\n+        len(cad.query_start_loc_cpu), dtype=torch.int32\n+    )\n+    new_cad = cad.replace(\n+        query_start_loc=new_query_start_loc,\n+        query_start_loc_cpu=new_query_start_loc_cpu,\n+        seq_lens=cad.seq_lens + 1,\n+        # each request is extended by 1 token -> batch_size tokens are added\n+        num_actual_tokens=cad.num_actual_tokens + cad.batch_size(),\n+        # All query lens increase by 1, so max query len increases by 1\n+        max_query_len=cad.max_query_len + 1,\n+        max_seq_len=cad.max_seq_len + 1,\n+        slot_mapping=new_slot_mapping,\n+    )\n+    return new_cad", "patch_sha256": "0125ee6161e5f9fc"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2674763412", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/attention/backends/utils.py\nReview Comment:\n### Missing `dcp_local_seq_lens` field when creating new CommonAttentionMetadata for draft model\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `extend_all_queries_by_1` function creates a new `CommonAttentionMetadata` object but doesn't copy the `dcp_local_seq_lens` field from the original metadata. This field is used for decode context parallelism (DCP) which distributes KV cache across multiple GPUs.\n\nWhen DCP is enabled (`decode_context_parallel_size > 1`), the attention backends use `dcp_local_seq_lens` to compute local sequence lengths for each rank. If this field is `None` when it should have a value, it could cause incorrect attention computation or runtime errors.\n\nFor comparison, the `prepare_inputs_padded` method in `vllm/v1/spec_decode/eagle.py` (line 730) correctly copies this field when creating new `CommonAttentionMetadata`. The fix should add `dcp_local_seq_lens=cad.dcp_local_seq_lens` to the CommonAttentionMetadata constructor in `extend_all_queries_by_1`.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/attention/backends/utils.py#L196-L231\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..siXxg0kOcj5CcgLj11LnHozv0izCKGNgyAY1ZKjkwH7IDj-2pc8me7djqN_fsPBIkzlrApJ-eutpTUDT4kPm1kiCy57ZJbpehfBu7RPyS87_yQ0wONiDLxK2ODSpaYFstsq-dwkDzT2Nbowi5WDXyb7Ta0U-uHDppaqDZaue88Kgk-HPcxBRM52PRkDmPUP5F3wEtf6OyFitCpKsZw5QCqAUu744mUohyUvrzE06u0RVPN8O488DQno83eHG23pJJrvtZIV8BM082tovK6wxTAvNZYdgPPbNGSifgXrfyQKs_mpTtEKMEARkyLpUFPrGEl6KyN0O2tGvQDjm8XYD2A\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..YPvkscQpRl_cCZJttB9MHojxmswQHzBoW8HvjWYOE9F1UNnbgJINUkyt0smKNzREcLmRbDULvPJJzu9PvBkqIM8wP9vPuppvlNsO3olzmz46Z8Yg7uNv9LTkRJdu7JLehTBa3OAP4XUgr22v7ZsR_PCHRjpXppQBdXhuE-_oD1QEhBDuGmBThTwNUB0VKVXy_MjZj9G9J989Eu_c_GrakyYc0gNNP1WiGqEW8BueCOKDfEGhvg7Sk8Uv-_MH0PbEcL2p5R7sgPCvD1Kvq5FaD0ulj-cmmPXkJ5rIJNhlqBMY-RL04aVCiMPHFm7283vY_7Jl3jjhC0N7gh4np-YDsw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -187,6 +194,43 @@ def slice_query_start_locs(\n     )\n \n \n+def extend_all_queries_by_1(\n+    common_attn_metadata: CommonAttentionMetadata,\n+    arange: torch.Tensor,\n+    new_slot_mapping: torch.Tensor,\n+) -> CommonAttentionMetadata:\n+    \"\"\"\n+    Creates a new CommonAttentionMetadata with all query lengths increased by 1.\n+    Also all seq lens are increased by 1.\n+    This is useful e.g. in speculative decoding with draft models, where we\n+    extend each sequence by 1 token.\n+    The slot mapping is computed externally, as it requires more information.\n+    \"\"\"\n+    cad = common_attn_metadata\n+    # query start loc must be increased by [+0, +1, +2, ..., +batch_size]\n+    new_query_start_loc = cad.query_start_loc + arange[: len(cad.query_start_loc)]\n+    new_query_start_loc_cpu = cad.query_start_loc_cpu + torch.arange(\n+        len(cad.query_start_loc_cpu)\n+    )\n+    new_seq_lens = cad.seq_lens + 1\n+\n+    new_cad = CommonAttentionMetadata(\n+        query_start_loc=new_query_start_loc,\n+        query_start_loc_cpu=new_query_start_loc_cpu,\n+        seq_lens=new_seq_lens,\n+        num_reqs=cad.num_reqs,  # num requests stays unchanged\n+        # each request is extended by 1 token -> batch_size tokens are added\n+        num_actual_tokens=cad.num_actual_tokens + cad.batch_size(),\n+        # All query lens increase by 1, so max query len increases by 1\n+        max_query_len=cad.max_query_len + 1,\n+        max_seq_len=cad.max_seq_len + 1,\n+        # block table tensor depends on num requests, which stays constant\n+        block_table_tensor=cad.block_table_tensor,\n+        slot_mapping=new_slot_mapping,\n+    )\n+    return new_cad\n", "materials": ["### Missing `dcp_local_seq_lens` field when creating new CommonAttentionMetadata for draft model\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `extend_all_queries_by_1` function creates a new `CommonAttentionMetadata` object but doesn't copy the `dcp_local_seq_lens` field from the original metadata. This field is used for decode context parallelism (DCP) which distributes KV cache across multiple GPUs.\n\nWhen DCP is enabled (`decode_context_parallel_size > 1`), the attention backends use `dcp_local_seq_lens` to compute local sequence lengths for each rank. If this field is `None` when it should have a value, it could cause incorrect attention computation or runtime errors.\n\nFor comparison, the `prepare_inputs_padded` method in `vllm/v1/spec_decode/eagle.py` (line 730) correctly copies this field when creating new `CommonAttentionMetadata`. The fix should add `dcp_local_seq_lens=cad.dcp_local_seq_lens` to the CommonAttentionMetadata constructor in `extend_all_queries_by_1`.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/attention/backends/utils.py#L196-L231\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..siXxg0kOcj5CcgLj11LnHozv0izCKGNgyAY1ZKjkwH7IDj-2pc8me7djqN_fsPBIkzlrApJ-eutpTUDT4kPm1kiCy57ZJbpehfBu7RPyS87_yQ0wONiDLxK2ODSpaYFstsq-dwkDzT2Nbowi5WDXyb7Ta0U-uHDppaqDZaue88Kgk-HPcxBRM52PRkDmPUP5F3wEtf6OyFitCpKsZw5QCqAUu744mUohyUvrzE06u0RVPN8O488DQno83eHG23pJJrvtZIV8BM082tovK6wxTAvNZYdgPPbNGSifgXrfyQKs_mpTtEKMEARkyLpUFPrGEl6KyN0O2tGvQDjm8XYD2A\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..YPvkscQpRl_cCZJttB9MHojxmswQHzBoW8HvjWYOE9F1UNnbgJINUkyt0smKNzREcLmRbDULvPJJzu9PvBkqIM8wP9vPuppvlNsO3olzmz46Z8Yg7uNv9LTkRJdu7JLehTBa3OAP4XUgr22v7ZsR_PCHRjpXppQBdXhuE-_oD1QEhBDuGmBThTwNUB0VKVXy_MjZj9G9J989Eu_c_GrakyYc0gNNP1WiGqEW8BueCOKDfEGhvg7Sk8Uv-_MH0PbEcL2p5R7sgPCvD1Kvq5FaD0ulj-cmmPXkJ5rIJNhlqBMY-RL04aVCiMPHFm7283vY_7Jl3jjhC0N7gh4np-YDsw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -818,3 +818,35 @@ def get_dcp_local_seq_lens(\n     )\n     dcp_local_seq_lens = base + remainder\n     return dcp_local_seq_lens.squeeze(1)\n+\n+\n+def extend_all_queries_by_1(\n+    common_attn_metadata: CommonAttentionMetadata,\n+    arange: torch.Tensor,\n+    new_slot_mapping: torch.Tensor,\n+) -> CommonAttentionMetadata:\n+    \"\"\"\n+    Creates a new CommonAttentionMetadata with all query lengths increased by 1.\n+    Also all seq lens are increased by 1.\n+    This is useful e.g. in speculative decoding with draft models, where we\n+    extend each sequence by 1 token.\n+    The slot mapping is computed externally, as it requires more information.\n+    \"\"\"\n+    cad = common_attn_metadata\n+    # query start loc must be increased by [+0, +1, +2, ..., +batch_size]\n+    new_query_start_loc = cad.query_start_loc + arange[: len(cad.query_start_loc)]\n+    new_query_start_loc_cpu = cad.query_start_loc_cpu + torch.arange(\n+        len(cad.query_start_loc_cpu), dtype=torch.int32\n+    )\n+    new_cad = cad.replace(\n+        query_start_loc=new_query_start_loc,\n+        query_start_loc_cpu=new_query_start_loc_cpu,\n+        seq_lens=cad.seq_lens + 1,\n+        # each request is extended by 1 token -> batch_size tokens are added\n+        num_actual_tokens=cad.num_actual_tokens + cad.batch_size(),\n+        # All query lens increase by 1, so max query len increases by 1\n+        max_query_len=cad.max_query_len + 1,\n+        max_seq_len=cad.max_seq_len + 1,\n+        slot_mapping=new_slot_mapping,\n+    )\n+    return new_cad", "patch_sha256": "0125ee6161e5f9fc"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2677081109", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/attention/backends/utils.py\nReview Comment:\nThis comment is duplicated\n\nOriginal Code Context:\n@@ -187,6 +194,43 @@ def slice_query_start_locs(\n     )\n \n \n+def extend_all_queries_by_1(\n+    common_attn_metadata: CommonAttentionMetadata,\n+    arange: torch.Tensor,\n+    new_slot_mapping: torch.Tensor,\n+) -> CommonAttentionMetadata:\n+    \"\"\"\n+    Creates a new CommonAttentionMetadata with all query lengths increased by 1.\n+    Also all seq lens are increased by 1.\n+    This is useful e.g. in speculative decoding with draft models, where we\n+    extend each sequence by 1 token.\n+    The slot mapping is computed externally, as it requires more information.\n+    \"\"\"\n+    cad = common_attn_metadata\n+    # query start loc must be increased by [+0, +1, +2, ..., +batch_size]\n+    new_query_start_loc = cad.query_start_loc + arange[: len(cad.query_start_loc)]\n+    new_query_start_loc_cpu = cad.query_start_loc_cpu + torch.arange(\n+        len(cad.query_start_loc_cpu)\n+    )\n+    new_seq_lens = cad.seq_lens + 1\n+\n+    new_cad = CommonAttentionMetadata(\n+        query_start_loc=new_query_start_loc,\n+        query_start_loc_cpu=new_query_start_loc_cpu,\n+        seq_lens=new_seq_lens,\n+        num_reqs=cad.num_reqs,  # num requests stays unchanged\n+        # each request is extended by 1 token -> batch_size tokens are added\n+        num_actual_tokens=cad.num_actual_tokens + cad.batch_size(),\n+        # All query lens increase by 1, so max query len increases by 1\n+        max_query_len=cad.max_query_len + 1,\n+        max_seq_len=cad.max_seq_len + 1,\n+        # block table tensor depends on num requests, which stays constant\n+        block_table_tensor=cad.block_table_tensor,\n+        slot_mapping=new_slot_mapping,\n+    )\n+    return new_cad\n", "materials": ["This comment is duplicated"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -818,3 +818,35 @@ def get_dcp_local_seq_lens(\n     )\n     dcp_local_seq_lens = base + remainder\n     return dcp_local_seq_lens.squeeze(1)\n+\n+\n+def extend_all_queries_by_1(\n+    common_attn_metadata: CommonAttentionMetadata,\n+    arange: torch.Tensor,\n+    new_slot_mapping: torch.Tensor,\n+) -> CommonAttentionMetadata:\n+    \"\"\"\n+    Creates a new CommonAttentionMetadata with all query lengths increased by 1.\n+    Also all seq lens are increased by 1.\n+    This is useful e.g. in speculative decoding with draft models, where we\n+    extend each sequence by 1 token.\n+    The slot mapping is computed externally, as it requires more information.\n+    \"\"\"\n+    cad = common_attn_metadata\n+    # query start loc must be increased by [+0, +1, +2, ..., +batch_size]\n+    new_query_start_loc = cad.query_start_loc + arange[: len(cad.query_start_loc)]\n+    new_query_start_loc_cpu = cad.query_start_loc_cpu + torch.arange(\n+        len(cad.query_start_loc_cpu), dtype=torch.int32\n+    )\n+    new_cad = cad.replace(\n+        query_start_loc=new_query_start_loc,\n+        query_start_loc_cpu=new_query_start_loc_cpu,\n+        seq_lens=cad.seq_lens + 1,\n+        # each request is extended by 1 token -> batch_size tokens are added\n+        num_actual_tokens=cad.num_actual_tokens + cad.batch_size(),\n+        # All query lens increase by 1, so max query len increases by 1\n+        max_query_len=cad.max_query_len + 1,\n+        max_seq_len=cad.max_seq_len + 1,\n+        slot_mapping=new_slot_mapping,\n+    )\n+    return new_cad", "patch_sha256": "0125ee6161e5f9fc"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2688852922", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/benchmarks/lib/ready_checker.py\nReview Comment:\nWhat is this for?\n\nOriginal Code Context:\n@@ -61,6 +65,8 @@ async def wait_for_endpoint(\n                 if output.success:\n                     pbar.close()\n                     return output\n+                else:\n+                    logger.warning(\"Endpoint is not ready. Error='%s'\", output.error)\n", "materials": ["What is this for?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -8,8 +8,12 @@\n import aiohttp\n from tqdm.asyncio import tqdm\n \n+from vllm.logger import init_logger\n+\n from .endpoint_request_func import RequestFunc, RequestFuncInput, RequestFuncOutput\n \n+logger = init_logger(__name__)\n+\n \n async def wait_for_endpoint(\n     request_func: RequestFunc,\n@@ -61,6 +65,8 @@ async def wait_for_endpoint(\n                 if output.success:\n                     pbar.close()\n                     return output\n+                else:\n+                    logger.warning(\"Endpoint is not ready. Error='%s'\", output.error)\n             except aiohttp.ClientConnectorError:\n                 pass\n ", "patch_sha256": "e80a79d56a4e66ab"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2688853700", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/parallel.py\nReview Comment:\nWhat does this do?\n\nOriginal Code Context:\n@@ -691,3 +692,6 @@ def _verify_args(self) -> Self:\n             )\n \n         return self\n+\n+    def replace(self, **kwargs) -> Self:\n", "materials": ["What does this do?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -3,6 +3,7 @@\n \n import os\n from collections.abc import Callable\n+from dataclasses import replace\n from typing import TYPE_CHECKING, Any, Literal\n \n import torch\n@@ -709,3 +710,6 @@ def _verify_args(self) -> Self:\n             )\n \n         return self\n+\n+    def replace(self, **kwargs) -> Self:\n+        return replace(self, **kwargs)", "patch_sha256": "2d70abcdb9635ee5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2688856111", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/speculative.py\nReview Comment:\n```suggestion\r\n    \"\"\"Users should pass \"draft_tensor_parallel_size\". This parameter's purpose is to warn\r\n    users when they mistakenly provide the wrong argument.\"\"\"\r\n```\n\nOriginal Code Context:\n@@ -74,6 +74,9 @@ class SpeculativeConfig:\n     draft_tensor_parallel_size: int | None = Field(default=None, ge=1)\n     \"\"\"The degree of the tensor parallelism for the draft model. Can only be 1\n     or the same as the target model's tensor parallel size.\"\"\"\n+    tensor_parallel_size: int | None = None\n+    \"\"\"Users should pass \"draft_tensor_parallel_size\". This parameters is only \n+    to reject it if passed.\"\"\"\n", "materials": ["```suggestion\r\n    \"\"\"Users should pass \"draft_tensor_parallel_size\". This parameter's purpose is to warn\r\n    users when they mistakenly provide the wrong argument.\"\"\"\r\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -77,6 +77,9 @@ class SpeculativeConfig:\n     draft_tensor_parallel_size: int | None = Field(default=None, ge=1)\n     \"\"\"The degree of the tensor parallelism for the draft model. Can only be 1\n     or the same as the target model's tensor parallel size.\"\"\"\n+    tensor_parallel_size: int | None = None\n+    \"\"\"Users should pass \"draft_tensor_parallel_size\". This parameter's purpose is to\n+    warn users when they mistakenly provide the wrong argument.\"\"\"\n \n     # Draft model configuration\n     quantization: me_quant.QuantizationMethods | None = None\n@@ -397,13 +400,11 @@ def __post_init__(self):\n                             \"one layer. Might need some code changes \"\n                             \"to support multiple layers.\"\n                         )\n+                elif self.method == \"draft_model\":\n+                    pass\n                 else:\n-                    self.method = \"draft_model\"\n                     raise NotImplementedError(\n-                        \"Speculative decoding with draft model is not \"\n-                        \"supported yet. Please consider using other \"\n-                        \"speculative decoding methods such as ngram, medusa, \"\n-                        \"eagle, or mtp.\"\n+                        f\"Unsupported speculative method: '{self.method}'\"\n                     )\n \n                 # Replace hf_config for EAGLE draft_model\n@@ -631,6 +632,12 @@ def create_draft_parallel_config(\n \n     @model_validator(mode=\"after\")\n     def _verify_args(self) -> Self:\n+        if self.tensor_parallel_size is not None:\n+            raise ValueError(\n+                \"'tensor_parallel_size' is not a valid argument in the \"\n+                \"speculative_config. Please pass 'draft_tensor_parallel_size' instead.\"\n+            )\n+\n         if self.num_speculative_tokens is None:\n             raise ValueError(\n                 \"num_speculative_tokens must be provided with \"\n@@ -669,12 +676,32 @@ def _verify_args(self) -> Self:\n                 f\"Eagle3 is only supported for {eagle3_target_supported} models. \"  # noqa: E501\n                 f\"Got {self.target_model_config.hf_text_config.model_type=}\"\n             )\n-\n+        self.verify_equal_vocab_size_if_draft_model()\n         return self\n \n+    def verify_equal_vocab_size_if_draft_model(self):\n+        if (\n+            self.method == \"draft_model\"\n+            and self.target_model_config is not None\n+            and self.draft_model_config is not None\n+        ):\n+            target_vocab_size = self.target_model_config.get_vocab_size()\n+            draft_vocab_size = self.draft_model_config.get_vocab_size()\n+            if target_vocab_size != draft_vocab_size:\n+                raise ValueError(\n+                    f\"Target and draft model should have the same vocabulary size. \"\n+                    f\"Target model vocab_size={target_vocab_size}. \"\n+                    f\"Draft model vocab_size={draft_vocab_size}. \"\n+                    f\"Using models with different tokenizers can cause out-of-bounds \"\n+                    f\"errors during speculative decoding.\"\n+                )\n+\n     def use_eagle(self) -> bool:\n         return self.method in (\"eagle\", \"eagle3\", \"mtp\")\n \n+    def uses_draft_model(self) -> bool:\n+        return self.method == \"draft_model\"\n+\n     def __repr__(self) -> str:\n         method = self.method\n         model = None if method in (\"ngram\", \"suffix\") else self.draft_model_config.model", "patch_sha256": "fc07136431edcfad"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2688862648", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/attention/backends/utils.py\nReview Comment:\nWhy would this ever _not_ be the case? This is how we compute `query_lens` pretty much everywhere.\n\nOriginal Code Context:\n@@ -96,6 +96,16 @@ class CommonAttentionMetadata:\n     dcp_local_seq_lens_cpu: torch.Tensor | None = None\n     \"\"\"Sequence lengths of the local rank in decode context parallelism world\"\"\"\n \n+    def batch_size(self) -> int:\n+        return self.seq_lens.shape[0]\n+\n+    def naive_query_lens(self) -> torch.Tensor:\n+        \"\"\"Naive because it assumes that query ends where the next query starts.\"\"\"\n", "materials": ["Why would this ever _not_ be the case? This is how we compute `query_lens` pretty much everywhere."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -818,3 +818,35 @@ def get_dcp_local_seq_lens(\n     )\n     dcp_local_seq_lens = base + remainder\n     return dcp_local_seq_lens.squeeze(1)\n+\n+\n+def extend_all_queries_by_1(\n+    common_attn_metadata: CommonAttentionMetadata,\n+    arange: torch.Tensor,\n+    new_slot_mapping: torch.Tensor,\n+) -> CommonAttentionMetadata:\n+    \"\"\"\n+    Creates a new CommonAttentionMetadata with all query lengths increased by 1.\n+    Also all seq lens are increased by 1.\n+    This is useful e.g. in speculative decoding with draft models, where we\n+    extend each sequence by 1 token.\n+    The slot mapping is computed externally, as it requires more information.\n+    \"\"\"\n+    cad = common_attn_metadata\n+    # query start loc must be increased by [+0, +1, +2, ..., +batch_size]\n+    new_query_start_loc = cad.query_start_loc + arange[: len(cad.query_start_loc)]\n+    new_query_start_loc_cpu = cad.query_start_loc_cpu + torch.arange(\n+        len(cad.query_start_loc_cpu), dtype=torch.int32\n+    )\n+    new_cad = cad.replace(\n+        query_start_loc=new_query_start_loc,\n+        query_start_loc_cpu=new_query_start_loc_cpu,\n+        seq_lens=cad.seq_lens + 1,\n+        # each request is extended by 1 token -> batch_size tokens are added\n+        num_actual_tokens=cad.num_actual_tokens + cad.batch_size(),\n+        # All query lens increase by 1, so max query len increases by 1\n+        max_query_len=cad.max_query_len + 1,\n+        max_seq_len=cad.max_seq_len + 1,\n+        slot_mapping=new_slot_mapping,\n+    )\n+    return new_cad", "patch_sha256": "0125ee6161e5f9fc"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2690445478", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/benchmarks/lib/ready_checker.py\nReview Comment:\nThis function waits for an endpoint to be be ready accept requests (while-True loop).\nHowever, when the endpoint has some error that is not recoverable, this functions silently ignores that and stays in the while-loop.\nThe idea of the warning is to surface the reason why endpoint is not ready yet, and avoid endlessly waiting without feedback.\n\nOriginal Code Context:\n@@ -61,6 +65,8 @@ async def wait_for_endpoint(\n                 if output.success:\n                     pbar.close()\n                     return output\n+                else:\n+                    logger.warning(\"Endpoint is not ready. Error='%s'\", output.error)\n", "materials": ["This function waits for an endpoint to be be ready accept requests (while-True loop).\nHowever, when the endpoint has some error that is not recoverable, this functions silently ignores that and stays in the while-loop.\nThe idea of the warning is to surface the reason why endpoint is not ready yet, and avoid endlessly waiting without feedback."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -8,8 +8,12 @@\n import aiohttp\n from tqdm.asyncio import tqdm\n \n+from vllm.logger import init_logger\n+\n from .endpoint_request_func import RequestFunc, RequestFuncInput, RequestFuncOutput\n \n+logger = init_logger(__name__)\n+\n \n async def wait_for_endpoint(\n     request_func: RequestFunc,\n@@ -61,6 +65,8 @@ async def wait_for_endpoint(\n                 if output.success:\n                     pbar.close()\n                     return output\n+                else:\n+                    logger.warning(\"Endpoint is not ready. Error='%s'\", output.error)\n             except aiohttp.ClientConnectorError:\n                 pass\n ", "patch_sha256": "e80a79d56a4e66ab"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2690474968", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/parallel.py\nReview Comment:\nThe `.replace()` API creates a copy of the object and replaces only the attributes passed with new values. Here is a good example from stackoverflow: https://stackoverflow.com/a/50319825/5730291 The API highlights the attributes that change in the new copy, while hiding all others. It's used concretely in `create_vllm_config_for_draft_model()`.\n\nOriginal Code Context:\n@@ -691,3 +692,6 @@ def _verify_args(self) -> Self:\n             )\n \n         return self\n+\n+    def replace(self, **kwargs) -> Self:\n", "materials": ["The `.replace()` API creates a copy of the object and replaces only the attributes passed with new values. Here is a good example from stackoverflow: https://stackoverflow.com/a/50319825/5730291 The API highlights the attributes that change in the new copy, while hiding all others. It's used concretely in `create_vllm_config_for_draft_model()`."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -3,6 +3,7 @@\n \n import os\n from collections.abc import Callable\n+from dataclasses import replace\n from typing import TYPE_CHECKING, Any, Literal\n \n import torch\n@@ -709,3 +710,6 @@ def _verify_args(self) -> Self:\n             )\n \n         return self\n+\n+    def replace(self, **kwargs) -> Self:\n+        return replace(self, **kwargs)", "patch_sha256": "2d70abcdb9635ee5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2690503466", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/attention/backends/utils.py\nReview Comment:\nWhen we have rejected tokens acting as padding, then the `query_end_loc` is not equal to `query_start_loc[:-1]` anymore, right? That is why we explicitly pass `last_token_indices` around. Otherwise, we would just set `last_token_indices = cad.query_start_loc[:-1]`. Let me know if I'm missing something.\n\nOriginal Code Context:\n@@ -96,6 +96,16 @@ class CommonAttentionMetadata:\n     dcp_local_seq_lens_cpu: torch.Tensor | None = None\n     \"\"\"Sequence lengths of the local rank in decode context parallelism world\"\"\"\n \n+    def batch_size(self) -> int:\n+        return self.seq_lens.shape[0]\n+\n+    def naive_query_lens(self) -> torch.Tensor:\n+        \"\"\"Naive because it assumes that query ends where the next query starts.\"\"\"\n", "materials": ["When we have rejected tokens acting as padding, then the `query_end_loc` is not equal to `query_start_loc[:-1]` anymore, right? That is why we explicitly pass `last_token_indices` around. Otherwise, we would just set `last_token_indices = cad.query_start_loc[:-1]`. Let me know if I'm missing something."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -818,3 +818,35 @@ def get_dcp_local_seq_lens(\n     )\n     dcp_local_seq_lens = base + remainder\n     return dcp_local_seq_lens.squeeze(1)\n+\n+\n+def extend_all_queries_by_1(\n+    common_attn_metadata: CommonAttentionMetadata,\n+    arange: torch.Tensor,\n+    new_slot_mapping: torch.Tensor,\n+) -> CommonAttentionMetadata:\n+    \"\"\"\n+    Creates a new CommonAttentionMetadata with all query lengths increased by 1.\n+    Also all seq lens are increased by 1.\n+    This is useful e.g. in speculative decoding with draft models, where we\n+    extend each sequence by 1 token.\n+    The slot mapping is computed externally, as it requires more information.\n+    \"\"\"\n+    cad = common_attn_metadata\n+    # query start loc must be increased by [+0, +1, +2, ..., +batch_size]\n+    new_query_start_loc = cad.query_start_loc + arange[: len(cad.query_start_loc)]\n+    new_query_start_loc_cpu = cad.query_start_loc_cpu + torch.arange(\n+        len(cad.query_start_loc_cpu), dtype=torch.int32\n+    )\n+    new_cad = cad.replace(\n+        query_start_loc=new_query_start_loc,\n+        query_start_loc_cpu=new_query_start_loc_cpu,\n+        seq_lens=cad.seq_lens + 1,\n+        # each request is extended by 1 token -> batch_size tokens are added\n+        num_actual_tokens=cad.num_actual_tokens + cad.batch_size(),\n+        # All query lens increase by 1, so max query len increases by 1\n+        max_query_len=cad.max_query_len + 1,\n+        max_seq_len=cad.max_seq_len + 1,\n+        slot_mapping=new_slot_mapping,\n+    )\n+    return new_cad", "patch_sha256": "0125ee6161e5f9fc"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2690970335", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/attention/backends/utils.py\nReview Comment:\nNote: torch.arrange has dtype int64, please take a look whether this will affect\n\nOriginal Code Context:\n@@ -818,3 +818,35 @@ def get_dcp_local_seq_lens(\n     )\n     dcp_local_seq_lens = base + remainder\n     return dcp_local_seq_lens.squeeze(1)\n+\n+\n+def extend_all_queries_by_1(\n+    common_attn_metadata: CommonAttentionMetadata,\n+    arange: torch.Tensor,\n+    new_slot_mapping: torch.Tensor,\n+) -> CommonAttentionMetadata:\n+    \"\"\"\n+    Creates a new CommonAttentionMetadata with all query lengths increased by 1.\n+    Also all seq lens are increased by 1.\n+    This is useful e.g. in speculative decoding with draft models, where we\n+    extend each sequence by 1 token.\n+    The slot mapping is computed externally, as it requires more information.\n+    \"\"\"\n+    cad = common_attn_metadata\n+    # query start loc must be increased by [+0, +1, +2, ..., +batch_size]\n+    new_query_start_loc = cad.query_start_loc + arange[: len(cad.query_start_loc)]\n+    new_query_start_loc_cpu = cad.query_start_loc_cpu + torch.arange(\n", "materials": ["Note: torch.arrange has dtype int64, please take a look whether this will affect"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -818,3 +818,35 @@ def get_dcp_local_seq_lens(\n     )\n     dcp_local_seq_lens = base + remainder\n     return dcp_local_seq_lens.squeeze(1)\n+\n+\n+def extend_all_queries_by_1(\n+    common_attn_metadata: CommonAttentionMetadata,\n+    arange: torch.Tensor,\n+    new_slot_mapping: torch.Tensor,\n+) -> CommonAttentionMetadata:\n+    \"\"\"\n+    Creates a new CommonAttentionMetadata with all query lengths increased by 1.\n+    Also all seq lens are increased by 1.\n+    This is useful e.g. in speculative decoding with draft models, where we\n+    extend each sequence by 1 token.\n+    The slot mapping is computed externally, as it requires more information.\n+    \"\"\"\n+    cad = common_attn_metadata\n+    # query start loc must be increased by [+0, +1, +2, ..., +batch_size]\n+    new_query_start_loc = cad.query_start_loc + arange[: len(cad.query_start_loc)]\n+    new_query_start_loc_cpu = cad.query_start_loc_cpu + torch.arange(\n+        len(cad.query_start_loc_cpu), dtype=torch.int32\n+    )\n+    new_cad = cad.replace(\n+        query_start_loc=new_query_start_loc,\n+        query_start_loc_cpu=new_query_start_loc_cpu,\n+        seq_lens=cad.seq_lens + 1,\n+        # each request is extended by 1 token -> batch_size tokens are added\n+        num_actual_tokens=cad.num_actual_tokens + cad.batch_size(),\n+        # All query lens increase by 1, so max query len increases by 1\n+        max_query_len=cad.max_query_len + 1,\n+        max_seq_len=cad.max_seq_len + 1,\n+        slot_mapping=new_slot_mapping,\n+    )\n+    return new_cad", "patch_sha256": "0125ee6161e5f9fc"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2695975770", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/attention/backends/utils.py\nReview Comment:\nI changed to dtype=int32 to prevent potential issues\n\nOriginal Code Context:\n@@ -818,3 +818,35 @@ def get_dcp_local_seq_lens(\n     )\n     dcp_local_seq_lens = base + remainder\n     return dcp_local_seq_lens.squeeze(1)\n+\n+\n+def extend_all_queries_by_1(\n+    common_attn_metadata: CommonAttentionMetadata,\n+    arange: torch.Tensor,\n+    new_slot_mapping: torch.Tensor,\n+) -> CommonAttentionMetadata:\n+    \"\"\"\n+    Creates a new CommonAttentionMetadata with all query lengths increased by 1.\n+    Also all seq lens are increased by 1.\n+    This is useful e.g. in speculative decoding with draft models, where we\n+    extend each sequence by 1 token.\n+    The slot mapping is computed externally, as it requires more information.\n+    \"\"\"\n+    cad = common_attn_metadata\n+    # query start loc must be increased by [+0, +1, +2, ..., +batch_size]\n+    new_query_start_loc = cad.query_start_loc + arange[: len(cad.query_start_loc)]\n+    new_query_start_loc_cpu = cad.query_start_loc_cpu + torch.arange(\n", "materials": ["I changed to dtype=int32 to prevent potential issues"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -818,3 +818,35 @@ def get_dcp_local_seq_lens(\n     )\n     dcp_local_seq_lens = base + remainder\n     return dcp_local_seq_lens.squeeze(1)\n+\n+\n+def extend_all_queries_by_1(\n+    common_attn_metadata: CommonAttentionMetadata,\n+    arange: torch.Tensor,\n+    new_slot_mapping: torch.Tensor,\n+) -> CommonAttentionMetadata:\n+    \"\"\"\n+    Creates a new CommonAttentionMetadata with all query lengths increased by 1.\n+    Also all seq lens are increased by 1.\n+    This is useful e.g. in speculative decoding with draft models, where we\n+    extend each sequence by 1 token.\n+    The slot mapping is computed externally, as it requires more information.\n+    \"\"\"\n+    cad = common_attn_metadata\n+    # query start loc must be increased by [+0, +1, +2, ..., +batch_size]\n+    new_query_start_loc = cad.query_start_loc + arange[: len(cad.query_start_loc)]\n+    new_query_start_loc_cpu = cad.query_start_loc_cpu + torch.arange(\n+        len(cad.query_start_loc_cpu), dtype=torch.int32\n+    )\n+    new_cad = cad.replace(\n+        query_start_loc=new_query_start_loc,\n+        query_start_loc_cpu=new_query_start_loc_cpu,\n+        seq_lens=cad.seq_lens + 1,\n+        # each request is extended by 1 token -> batch_size tokens are added\n+        num_actual_tokens=cad.num_actual_tokens + cad.batch_size(),\n+        # All query lens increase by 1, so max query len increases by 1\n+        max_query_len=cad.max_query_len + 1,\n+        max_seq_len=cad.max_seq_len + 1,\n+        slot_mapping=new_slot_mapping,\n+    )\n+    return new_cad", "patch_sha256": "0125ee6161e5f9fc"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2753368760", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/benchmarks/lib/ready_checker.py\nReview Comment:\nThis fills my terminal with \"Endpoint is not ready\" messages while the `vllm serve` process is loading the model and starting up. The old behavior was more sane. Can you revert or hide the new behavior under a flag?\n\nOriginal Code Context:\n@@ -61,6 +65,8 @@ async def wait_for_endpoint(\n                 if output.success:\n                     pbar.close()\n                     return output\n+                else:\n+                    logger.warning(\"Endpoint is not ready. Error='%s'\", output.error)\n", "materials": ["This fills my terminal with \"Endpoint is not ready\" messages while the `vllm serve` process is loading the model and starting up. The old behavior was more sane. Can you revert or hide the new behavior under a flag?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -8,8 +8,12 @@\n import aiohttp\n from tqdm.asyncio import tqdm\n \n+from vllm.logger import init_logger\n+\n from .endpoint_request_func import RequestFunc, RequestFuncInput, RequestFuncOutput\n \n+logger = init_logger(__name__)\n+\n \n async def wait_for_endpoint(\n     request_func: RequestFunc,\n@@ -61,6 +65,8 @@ async def wait_for_endpoint(\n                 if output.success:\n                     pbar.close()\n                     return output\n+                else:\n+                    logger.warning(\"Endpoint is not ready. Error='%s'\", output.error)\n             except aiohttp.ClientConnectorError:\n                 pass\n ", "patch_sha256": "e80a79d56a4e66ab"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2753861118", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/benchmarks/lib/ready_checker.py\nReview Comment:\n@mgehre-amd Agree, I noticed recently how verbose this became. Will create a fix.\r\n**Edit**: Also feel free to create a fix yourself, if you have time.\n\nOriginal Code Context:\n@@ -61,6 +65,8 @@ async def wait_for_endpoint(\n                 if output.success:\n                     pbar.close()\n                     return output\n+                else:\n+                    logger.warning(\"Endpoint is not ready. Error='%s'\", output.error)\n", "materials": ["@mgehre-amd Agree, I noticed recently how verbose this became. Will create a fix.\r\n**Edit**: Also feel free to create a fix yourself, if you have time."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -8,8 +8,12 @@\n import aiohttp\n from tqdm.asyncio import tqdm\n \n+from vllm.logger import init_logger\n+\n from .endpoint_request_func import RequestFunc, RequestFuncInput, RequestFuncOutput\n \n+logger = init_logger(__name__)\n+\n \n async def wait_for_endpoint(\n     request_func: RequestFunc,\n@@ -61,6 +65,8 @@ async def wait_for_endpoint(\n                 if output.success:\n                     pbar.close()\n                     return output\n+                else:\n+                    logger.warning(\"Endpoint is not ready. Error='%s'\", output.error)\n             except aiohttp.ClientConnectorError:\n                 pass\n ", "patch_sha256": "e80a79d56a4e66ab"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2794059635", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/benchmarks/lib/ready_checker.py\nReview Comment:\nhttps://github.com/vllm-project/vllm/pull/34349\n\nOriginal Code Context:\n@@ -61,6 +65,8 @@ async def wait_for_endpoint(\n                 if output.success:\n                     pbar.close()\n                     return output\n+                else:\n+                    logger.warning(\"Endpoint is not ready. Error='%s'\", output.error)\n", "materials": ["https://github.com/vllm-project/vllm/pull/34349"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -8,8 +8,12 @@\n import aiohttp\n from tqdm.asyncio import tqdm\n \n+from vllm.logger import init_logger\n+\n from .endpoint_request_func import RequestFunc, RequestFuncInput, RequestFuncOutput\n \n+logger = init_logger(__name__)\n+\n \n async def wait_for_endpoint(\n     request_func: RequestFunc,\n@@ -61,6 +65,8 @@ async def wait_for_endpoint(\n                 if output.success:\n                     pbar.close()\n                     return output\n+                else:\n+                    logger.warning(\"Endpoint is not ready. Error='%s'\", output.error)\n             except aiohttp.ClientConnectorError:\n                 pass\n ", "patch_sha256": "e80a79d56a4e66ab"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24498/review_2544952595", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24498", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/vllm.py\nReview Comment:\nDoes this no longer need to be scaled by the number of ranks?\n\nOriginal Code Context:\n@@ -321,12 +321,8 @@ def _post_init_kv_transfer_config(self) -> None:\n \n         if kv_offloading_backend == \"native\":\n             self.kv_transfer_config.kv_connector = \"OffloadingConnector\"\n-            kv_bytes_per_rank = kv_offloading_size * (1 << 30) / num_kv_ranks\n-\n-            # NOTE(ApostaC): the actual calculation for num_cpu_blocks should be\n-            # done after the model's KV cache is initialized\n             self.kv_transfer_config.kv_connector_extra_config.update(\n-                {\"kv_bytes_per_rank\": kv_bytes_per_rank, \"num_cpu_blocks\": 0}\n+                {\"cpu_bytes_to_use\": kv_offloading_size * (1 << 30)}\n", "materials": ["Does this no longer need to be scaled by the number of ranks?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -516,12 +516,8 @@ def _post_init_kv_transfer_config(self) -> None:\n \n         if kv_offloading_backend == \"native\":\n             self.kv_transfer_config.kv_connector = \"OffloadingConnector\"\n-            kv_bytes_per_rank = kv_offloading_size * (1 << 30) / num_kv_ranks\n-\n-            # NOTE(ApostaC): the actual calculation for num_cpu_blocks should be\n-            # done after the model's KV cache is initialized\n             self.kv_transfer_config.kv_connector_extra_config.update(\n-                {\"kv_bytes_per_rank\": kv_bytes_per_rank, \"num_cpu_blocks\": 0}\n+                {\"cpu_bytes_to_use\": kv_offloading_size * (1 << 30)}\n             )\n         elif kv_offloading_backend == \"lmcache\":\n             self.kv_transfer_config.kv_connector = \"LMCacheConnectorV1\"", "patch_sha256": "1e0133860683c273"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24498/review_2545097415", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24498", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/vllm.py\nReview Comment:\nIt never needed scaling in the first place.\n\nOriginal Code Context:\n@@ -321,12 +321,8 @@ def _post_init_kv_transfer_config(self) -> None:\n \n         if kv_offloading_backend == \"native\":\n             self.kv_transfer_config.kv_connector = \"OffloadingConnector\"\n-            kv_bytes_per_rank = kv_offloading_size * (1 << 30) / num_kv_ranks\n-\n-            # NOTE(ApostaC): the actual calculation for num_cpu_blocks should be\n-            # done after the model's KV cache is initialized\n             self.kv_transfer_config.kv_connector_extra_config.update(\n-                {\"kv_bytes_per_rank\": kv_bytes_per_rank, \"num_cpu_blocks\": 0}\n+                {\"cpu_bytes_to_use\": kv_offloading_size * (1 << 30)}\n", "materials": ["It never needed scaling in the first place."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -516,12 +516,8 @@ def _post_init_kv_transfer_config(self) -> None:\n \n         if kv_offloading_backend == \"native\":\n             self.kv_transfer_config.kv_connector = \"OffloadingConnector\"\n-            kv_bytes_per_rank = kv_offloading_size * (1 << 30) / num_kv_ranks\n-\n-            # NOTE(ApostaC): the actual calculation for num_cpu_blocks should be\n-            # done after the model's KV cache is initialized\n             self.kv_transfer_config.kv_connector_extra_config.update(\n-                {\"kv_bytes_per_rank\": kv_bytes_per_rank, \"num_cpu_blocks\": 0}\n+                {\"cpu_bytes_to_use\": kv_offloading_size * (1 << 30)}\n             )\n         elif kv_offloading_backend == \"lmcache\":\n             self.kv_transfer_config.kv_connector = \"LMCacheConnectorV1\"", "patch_sha256": "1e0133860683c273"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24498/review_2628370164", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24498", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/cpu.py\nReview Comment:\n![critical](https://www.gstatic.com/codereviewagent/critical.svg)\n\nThere's a potential `ZeroDivisionError` here if `kv_bytes_per_offloaded_block` is 0. This can happen for models without a KV cache (like attention-free models), where `kv_cache_config.kv_bytes_per_block` would be 0. This would cause the application to crash. It's important to handle this case to prevent runtime errors.\n\n```suggestion\n        self.num_blocks = (\n            int(cpu_bytes_to_use) // kv_bytes_per_offloaded_block\n            if kv_bytes_per_offloaded_block > 0\n            else 0\n        )\n```\n\nOriginal Code Context:\n@@ -18,15 +19,20 @@\n \n \n class CPUOffloadingSpec(OffloadingSpec):\n-    def __init__(self, vllm_config: VllmConfig):\n-        super().__init__(vllm_config)\n+    def __init__(self, vllm_config: VllmConfig, kv_cache_config: KVCacheConfig):\n+        super().__init__(vllm_config, kv_cache_config)\n \n-        num_cpu_blocks = self.extra_config.get(\"num_cpu_blocks\")\n-        if not num_cpu_blocks:\n+        cpu_bytes_to_use = self.extra_config.get(\"cpu_bytes_to_use\")\n+        if not cpu_bytes_to_use:\n             raise Exception(\n-                \"num_cpu_blocks must be specified in kv_connector_extra_config\"\n+                \"cpu_bytes_to_use must be specified in kv_connector_extra_config\"\n             )\n-        self.num_cpu_blocks: int = num_cpu_blocks\n+\n+        assert kv_cache_config is not None\n+        kv_bytes_per_offloaded_block = kv_cache_config.kv_bytes_per_block * (\n+            self.offloaded_block_size // self.gpu_block_size\n+        )\n+        self.num_blocks = int(cpu_bytes_to_use) // kv_bytes_per_offloaded_block\n", "materials": ["![critical](https://www.gstatic.com/codereviewagent/critical.svg)\n\nThere's a potential `ZeroDivisionError` here if `kv_bytes_per_offloaded_block` is 0. This can happen for models without a KV cache (like attention-free models), where `kv_cache_config.kv_bytes_per_block` would be 0. This would cause the application to crash. It's important to handle this case to prevent runtime errors.\n\n```suggestion\n        self.num_blocks = (\n            int(cpu_bytes_to_use) // kv_bytes_per_offloaded_block\n            if kv_bytes_per_offloaded_block > 0\n            else 0\n        )\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -7,6 +7,7 @@\n from vllm.config import VllmConfig\n from vllm.platforms import current_platform\n from vllm.v1.attention.backend import AttentionBackend\n+from vllm.v1.kv_cache_interface import KVCacheConfig\n from vllm.v1.kv_offload.abstract import LoadStoreSpec, OffloadingManager\n from vllm.v1.kv_offload.arc_manager import ARCOffloadingManager\n from vllm.v1.kv_offload.backends.cpu import CPUBackend\n@@ -18,15 +19,37 @@\n \n \n class CPUOffloadingSpec(OffloadingSpec):\n-    def __init__(self, vllm_config: VllmConfig):\n-        super().__init__(vllm_config)\n+    def __init__(self, vllm_config: VllmConfig, kv_cache_config: KVCacheConfig):\n+        super().__init__(vllm_config, kv_cache_config)\n \n-        num_cpu_blocks = self.extra_config.get(\"num_cpu_blocks\")\n-        if not num_cpu_blocks:\n+        cpu_bytes_to_use = self.extra_config.get(\"cpu_bytes_to_use\")\n+        if not cpu_bytes_to_use:\n             raise Exception(\n-                \"num_cpu_blocks must be specified in kv_connector_extra_config\"\n+                \"cpu_bytes_to_use must be specified in kv_connector_extra_config\"\n             )\n-        self.num_cpu_blocks: int = num_cpu_blocks\n+\n+        # calculate kv_bytes_per_offloaded_block\n+        assert kv_cache_config is not None\n+        page_sizes = {\n+            kv_cache_group.kv_cache_spec.page_size_bytes\n+            for kv_cache_group in kv_cache_config.kv_cache_groups\n+        }\n+        assert len(page_sizes) == 1\n+        page_size_bytes = page_sizes.pop()\n+        kv_bytes_per_block = (\n+            page_size_bytes\n+            * len(kv_cache_config.kv_cache_tensors)\n+            * vllm_config.parallel_config.world_size\n+        )\n+        kv_bytes_per_offloaded_block = kv_bytes_per_block * (\n+            self.offloaded_block_size // self.gpu_block_size\n+        )\n+\n+        self.num_blocks = (\n+            int(cpu_bytes_to_use) // kv_bytes_per_offloaded_block\n+            if kv_bytes_per_offloaded_block > 0\n+            else 0\n+        )\n \n         # scheduler-side\n         self._manager: OffloadingManager | None = None\n@@ -44,7 +67,7 @@ def get_manager(self) -> OffloadingManager:\n             )\n \n             backend = CPUBackend(\n-                block_size=self.offloaded_block_size, num_blocks=self.num_cpu_blocks\n+                block_size=self.offloaded_block_size, num_blocks=self.num_blocks\n             )\n \n             if self.eviction_policy == \"lru\":\n@@ -77,7 +100,7 @@ def get_handlers(\n                 attn_backends=attn_backends,\n                 gpu_block_size=self.gpu_block_size,\n                 cpu_block_size=self.offloaded_block_size,\n-                num_cpu_blocks=self.num_cpu_blocks,\n+                num_cpu_blocks=self.num_blocks,\n                 gpu_caches=kv_caches,\n             )\n ", "patch_sha256": "7cdc33af8fecdc2f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2414660061", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/decorators.py\nReview Comment:\nInstead of detecting the \"last\" run inside decorator, I think it sounds simpler to do this directly in gpu_worker (after the warmup is finished). Some plumbing work may be needed to expose a save() function from model runner. \r\n\r\nThis way seems we don't require a `_aot_compiled_all_shapes` helper.\n\nOriginal Code Context:\n@@ -354,13 +391,19 @@ def patched_inline_call(parent, func, args, kwargs):\n                     ), maybe_use_cudagraph_partition_wrapper(\n                         self.vllm_config), _torch27_patch_tensor_subclasses():\n                 if envs.VLLM_USE_AOT_COMPILE:\n-                    self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n+                    if getattr(self, \"aot_compiled_fn\", None) is None:\n+                        self.aot_compiled_fn = self.aot_compile(\n+                            *args, **kwargs)\n                     output = self.aot_compiled_fn(self, *args, **kwargs)\n-                    assert aot_compilation_path is not None\n-                    assert cache_dir is not None\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(\n-                        aot_compilation_path)\n+                    if _aot_compiled_all_shapes(self.aot_compiled_fn):\n+                        self.precompile_compiled_all_shapes = True\n+                        assert aot_compilation_path is not None\n+                        assert cache_dir is not None\n+                        os.makedirs(cache_dir, exist_ok=True)\n+                        logger.debug(\"saving aot compiled fn at %s\",\n+                                     aot_compilation_path)\n+                        self.aot_compiled_fn.save_compiled_function(\n+                            aot_compilation_path)\n", "materials": ["Instead of detecting the \"last\" run inside decorator, I think it sounds simpler to do this directly in gpu_worker (after the warmup is finished). Some plumbing work may be needed to expose a save() function from model runner. \r\n\r\nThis way seems we don't require a `_aot_compiled_all_shapes` helper."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -320,7 +320,7 @@ def __init__(\n             return\n \n         self._check_shape_invariants = shape_invariants\n-\n+        self.was_aot_compile_fn_loaded_from_disk = False\n         compilation_counter.num_models_seen += 1\n         self.compiled = False\n \n@@ -417,9 +417,9 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n             serialized backend artifacts), then we need to generate a new AOT\n             compile artifact from scratch.\n             \"\"\"\n-            from .caching import compilation_config_hash_factors\n+            from .caching import aot_compile_hash_factors\n \n-            factors: list[str] = compilation_config_hash_factors(self.vllm_config)\n+            factors: list[str] = aot_compile_hash_factors(self.vllm_config)\n \n             factors.append(_model_hash_key(self.forward))\n             hash_key = hashlib.sha256(str(factors).encode()).hexdigest()\n@@ -446,6 +446,7 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n                 if not self.compilation_config.dynamic_shapes_config.evaluate_guards:\n                     loaded_fn.disable_guard_check()\n                 self.aot_compiled_fn = loaded_fn\n+                self.was_aot_compile_fn_loaded_from_disk = True\n             except Exception as e:\n                 if os.path.exists(aot_compilation_path):\n                     logger.warning(\n@@ -547,26 +548,45 @@ def patched_inline_call(self_: Any) -> Any:\n                 logger.warning(\"Detected eager backend, disabling AOT compile.\")\n                 use_aot_compile = False\n             if use_aot_compile:\n-                self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n-                output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                from vllm.compilation.backends import set_on_compilation_complete\n+\n+                # store the path for saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                # set callback in context so it's available when compilation completes\n+                with set_on_compilation_complete(self.save_aot_compiled_function):\n+                    self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n+                    output = self.aot_compiled_fn(self, *args, **kwargs)\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    # triggers VllmSerializableFunction.serialize()\n+    def save_aot_compiled_function(self):\n+        if self.was_aot_compile_fn_loaded_from_disk:\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        assert (\n+            self.aot_compiled_fn and self._aot_compilation_path and self._aot_cache_dir\n+        )\n+\n+        logger.info(\"saving AOT compiled function to %s\", self._aot_compilation_path)\n+        try:\n+            os.makedirs(self._aot_cache_dir, exist_ok=True)\n+            self.aot_compiled_fn.save_compiled_function(self._aot_compilation_path)\n+            logger.info(\"saved AOT compiled function to %s\", self._aot_compilation_path)\n+        except Exception as e:\n+            logger.warning(\n+                \"unable to save AOT compiled function to %s: %s\",\n+                self._aot_compilation_path,\n+                e,\n+            )\n+\n     cls.__call__ = __call__\n+    cls.save_aot_compiled_function = save_aot_compiled_function\n     return cls\n \n ", "patch_sha256": "da871cc06d969b66"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2461788527", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/compiler_interface.py\nReview Comment:\nCan we not just check pytorch version > 2.10? Preferably in the place we check other pytorch versions, and then threaded down to here via a variable? \n\nOriginal Code Context:\n@@ -200,21 +201,47 @@ def compile(\n         if compiler_config is not None:\n             current_config.update(compiler_config)\n         set_inductor_config(current_config, runtime_shape)\n-        set_functorch_config()\n \n         if isinstance(runtime_shape, int):\n             dynamic_shapes = \"from_example_inputs\"\n         else:\n             dynamic_shapes = \"from_tracing_context\"\n \n+        # Check if standalone_compile supports 'aot' parameter\n+        # This was added in PyTorch 2.10+\n+        import inspect\n", "materials": ["Can we not just check pytorch version > 2.10? Preferably in the place we check other pytorch versions, and then threaded down to here via a variable? "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -16,9 +16,12 @@\n from vllm.compilation.counter import compilation_counter\n from vllm.config import VllmConfig\n from vllm.config.utils import Range\n+from vllm.logger import init_logger\n from vllm.utils.hashing import safe_hash\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n \n+logger = init_logger(__name__)\n+\n \n class CompilerInterface:\n     \"\"\"\n@@ -230,12 +233,42 @@ def compile(\n \n         from torch._inductor import standalone_compile\n \n-        compiled_graph = standalone_compile(\n-            graph,\n-            example_inputs,\n-            dynamic_shapes=dynamic_shapes,\n-            options={\"config_patches\": current_config},\n-        )\n+        supports_aot = is_torch_equal_or_newer(\"2.10.0.dev\")\n+\n+        if not supports_aot and envs.VLLM_USE_MEGA_AOT_ARTIFACT:\n+            logger.error(\n+                \"CRITICAL: VLLM_USE_MEGA_AOT_ARTIFACT \"\n+                \"is enabled but PyTorch version does not support 'aot' \"\n+                \"parameter in standalone_compile. This requires PyTorch \"\n+                \"2.10.0+. Falling back to non-AOT mode.\"\n+            )\n+\n+        compile_kwargs = {\n+            \"dynamic_shapes\": dynamic_shapes,\n+            \"options\": {\n+                \"config_patches\": current_config,\n+            },\n+        }\n+\n+        use_aot: bool = supports_aot and envs.VLLM_USE_MEGA_AOT_ARTIFACT\n+        # only add 'aot' parameter if both supported and enabled...\n+        # this will set bundled_autograd_cache\n+        # https://github.com/pytorch/pytorch/blob/9bbc5b2905c260adf41bc866a732f9c121a2828a/torch/_inductor/standalone_compile.py#L359 # noqa\n+        if use_aot:\n+            compile_kwargs[\"aot\"] = True  # type: ignore[assignment]\n+\n+        compiled_graph = standalone_compile(graph, example_inputs, **compile_kwargs)\n+\n+        if use_aot:\n+            from torch._inductor.standalone_compile import AOTCompiledArtifact\n+\n+            assert isinstance(compiled_graph, AOTCompiledArtifact)\n+            assert hasattr(compiled_graph, \"serialize\")\n+            # just return the compiled graph and a key\n+            # since we can serialize the bytes using to_bytes\n+            # and reload it using the key when reading\n+            return compiled_graph, None\n+\n         # Save the compiled artifact to disk in the specified path\n         assert key is not None\n         path = os.path.join(self.cache_dir, key)\n@@ -619,7 +652,8 @@ def set_inductor_config(config: dict[str, Any], compile_range: Range) -> None:\n \n \n def set_functorch_config() -> None:\n-    torch._functorch.config.bundled_autograd_cache = False\n+    if not envs.VLLM_USE_MEGA_AOT_ARTIFACT:\n+        torch._functorch.config.bundled_autograd_cache = False\n \n \n class EagerAdaptor(CompilerInterface):", "patch_sha256": "59077149ae92a623"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2461817154", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/compiler_interface.py\nReview Comment:\nnit: same comment about 2.10 check, or at least put this into a common helper\n\nOriginal Code Context:\n@@ -200,21 +201,47 @@ def compile(\n         if compiler_config is not None:\n             current_config.update(compiler_config)\n         set_inductor_config(current_config, runtime_shape)\n-        set_functorch_config()\n \n         if isinstance(runtime_shape, int):\n             dynamic_shapes = \"from_example_inputs\"\n         else:\n             dynamic_shapes = \"from_tracing_context\"\n \n+        # Check if standalone_compile supports 'aot' parameter\n+        # This was added in PyTorch 2.10+\n+        import inspect\n+\n         from torch._inductor import standalone_compile\n \n-        compiled_graph = standalone_compile(\n-            graph,\n-            example_inputs,\n-            dynamic_shapes=dynamic_shapes,\n-            options={\"config_patches\": current_config},\n-        )\n+        standalone_compile_sig = inspect.signature(standalone_compile)\n", "materials": ["nit: same comment about 2.10 check, or at least put this into a common helper"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -16,9 +16,12 @@\n from vllm.compilation.counter import compilation_counter\n from vllm.config import VllmConfig\n from vllm.config.utils import Range\n+from vllm.logger import init_logger\n from vllm.utils.hashing import safe_hash\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n \n+logger = init_logger(__name__)\n+\n \n class CompilerInterface:\n     \"\"\"\n@@ -230,12 +233,42 @@ def compile(\n \n         from torch._inductor import standalone_compile\n \n-        compiled_graph = standalone_compile(\n-            graph,\n-            example_inputs,\n-            dynamic_shapes=dynamic_shapes,\n-            options={\"config_patches\": current_config},\n-        )\n+        supports_aot = is_torch_equal_or_newer(\"2.10.0.dev\")\n+\n+        if not supports_aot and envs.VLLM_USE_MEGA_AOT_ARTIFACT:\n+            logger.error(\n+                \"CRITICAL: VLLM_USE_MEGA_AOT_ARTIFACT \"\n+                \"is enabled but PyTorch version does not support 'aot' \"\n+                \"parameter in standalone_compile. This requires PyTorch \"\n+                \"2.10.0+. Falling back to non-AOT mode.\"\n+            )\n+\n+        compile_kwargs = {\n+            \"dynamic_shapes\": dynamic_shapes,\n+            \"options\": {\n+                \"config_patches\": current_config,\n+            },\n+        }\n+\n+        use_aot: bool = supports_aot and envs.VLLM_USE_MEGA_AOT_ARTIFACT\n+        # only add 'aot' parameter if both supported and enabled...\n+        # this will set bundled_autograd_cache\n+        # https://github.com/pytorch/pytorch/blob/9bbc5b2905c260adf41bc866a732f9c121a2828a/torch/_inductor/standalone_compile.py#L359 # noqa\n+        if use_aot:\n+            compile_kwargs[\"aot\"] = True  # type: ignore[assignment]\n+\n+        compiled_graph = standalone_compile(graph, example_inputs, **compile_kwargs)\n+\n+        if use_aot:\n+            from torch._inductor.standalone_compile import AOTCompiledArtifact\n+\n+            assert isinstance(compiled_graph, AOTCompiledArtifact)\n+            assert hasattr(compiled_graph, \"serialize\")\n+            # just return the compiled graph and a key\n+            # since we can serialize the bytes using to_bytes\n+            # and reload it using the key when reading\n+            return compiled_graph, None\n+\n         # Save the compiled artifact to disk in the specified path\n         assert key is not None\n         path = os.path.join(self.cache_dir, key)\n@@ -619,7 +652,8 @@ def set_inductor_config(config: dict[str, Any], compile_range: Range) -> None:\n \n \n def set_functorch_config() -> None:\n-    torch._functorch.config.bundled_autograd_cache = False\n+    if not envs.VLLM_USE_MEGA_AOT_ARTIFACT:\n+        torch._functorch.config.bundled_autograd_cache = False\n \n \n class EagerAdaptor(CompilerInterface):", "patch_sha256": "59077149ae92a623"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2677322765", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/compiler_interface.py\nReview Comment:\n### Unconditional import fails on older PyTorch versions\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe top-level import `from torch._inductor.standalone_compile import AOTCompiledArtifact` is unconditional but `AOTCompiledArtifact` only exists in PyTorch 2.10.0+. The code checks for version compatibility at runtime (`is_torch_equal_or_newer(\"2.10.0.dev\")`), but this import occurs before any version check can run, causing the entire module to fail to import on PyTorch < 2.10. Since vLLM builds with torch 2.9.1, this could break the compilation module entirely. The import needs to be made conditional or moved inside the method that uses it.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/compilation/compiler_interface.py#L13-L14\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..QCnJ3L6b-AKcaxfPjCiDN4CP6Ifj5O6PfE36e5co0FxuN0ZkPLXzkzcRQxCSXdMDqZ9xH_8MqcV5C67EF2dMH3pl1CFIs1_spqs05diSxXJKt9H0T6kEPcLQ9CQ5HMhDmKOG_VyryVvbIwqRmvix-7bBW740DvS5r9KjTF5R5uUcTYsM8jRpStgIBUZtOiXikU5TazP8oE3fTUvjdTvvadbUumTjqJdFjoWs5WFF26RmjPR51juhkXwOyJADonTIQjEmDuZ2rvN1ZZJUWyqb-cVcLIJMp4gcRJVavT4q2TjypNwdrJif6KAv4PYObfZVlA9yKb5WXEi1NDeX5LbBTA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..icmfzQh_19F-eXpeEx1eSV962l0qN0Hk8I5z6gNc4hOKZVeCTPpohudOgxfYvQZXowCld-h6Ax9NDavEIpSwA2xtKC_8otk248lkSMVXKg8VQtn0xbzz8VaBFsoAtp727fOcb1lNYInPz4CiC9stVmbYjTbn4nGMrhoe9Rd-MmBDbbFVQFsJV-1ehSgSWkaZ8AMxF5wFRlSdMC3gbDKJ1iWk9N_BzkiKSCu4uMQdACKNSs8cMtxR_v-hxEdt3Sj-LHMOG2ZBDzAE6UqDbbU11XqiAnyoN4p6Hb2GXQlqghT0qtza4GK8g__LEKngU4ZHqQ5zE1Izwi87MKKwXhk8Bg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -11,14 +11,18 @@\n import torch\n import torch._inductor.compile_fx\n import torch.fx as fx\n+from torch._inductor.standalone_compile import AOTCompiledArtifact\n", "materials": ["### Unconditional import fails on older PyTorch versions\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe top-level import `from torch._inductor.standalone_compile import AOTCompiledArtifact` is unconditional but `AOTCompiledArtifact` only exists in PyTorch 2.10.0+. The code checks for version compatibility at runtime (`is_torch_equal_or_newer(\"2.10.0.dev\")`), but this import occurs before any version check can run, causing the entire module to fail to import on PyTorch < 2.10. Since vLLM builds with torch 2.9.1, this could break the compilation module entirely. The import needs to be made conditional or moved inside the method that uses it.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/compilation/compiler_interface.py#L13-L14\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..QCnJ3L6b-AKcaxfPjCiDN4CP6Ifj5O6PfE36e5co0FxuN0ZkPLXzkzcRQxCSXdMDqZ9xH_8MqcV5C67EF2dMH3pl1CFIs1_spqs05diSxXJKt9H0T6kEPcLQ9CQ5HMhDmKOG_VyryVvbIwqRmvix-7bBW740DvS5r9KjTF5R5uUcTYsM8jRpStgIBUZtOiXikU5TazP8oE3fTUvjdTvvadbUumTjqJdFjoWs5WFF26RmjPR51juhkXwOyJADonTIQjEmDuZ2rvN1ZZJUWyqb-cVcLIJMp4gcRJVavT4q2TjypNwdrJif6KAv4PYObfZVlA9yKb5WXEi1NDeX5LbBTA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..icmfzQh_19F-eXpeEx1eSV962l0qN0Hk8I5z6gNc4hOKZVeCTPpohudOgxfYvQZXowCld-h6Ax9NDavEIpSwA2xtKC_8otk248lkSMVXKg8VQtn0xbzz8VaBFsoAtp727fOcb1lNYInPz4CiC9stVmbYjTbn4nGMrhoe9Rd-MmBDbbFVQFsJV-1ehSgSWkaZ8AMxF5wFRlSdMC3gbDKJ1iWk9N_BzkiKSCu4uMQdACKNSs8cMtxR_v-hxEdt3Sj-LHMOG2ZBDzAE6UqDbbU11XqiAnyoN4p6Hb2GXQlqghT0qtza4GK8g__LEKngU4ZHqQ5zE1Izwi87MKKwXhk8Bg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -16,9 +16,12 @@\n from vllm.compilation.counter import compilation_counter\n from vllm.config import VllmConfig\n from vllm.config.utils import Range\n+from vllm.logger import init_logger\n from vllm.utils.hashing import safe_hash\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n \n+logger = init_logger(__name__)\n+\n \n class CompilerInterface:\n     \"\"\"\n@@ -230,12 +233,42 @@ def compile(\n \n         from torch._inductor import standalone_compile\n \n-        compiled_graph = standalone_compile(\n-            graph,\n-            example_inputs,\n-            dynamic_shapes=dynamic_shapes,\n-            options={\"config_patches\": current_config},\n-        )\n+        supports_aot = is_torch_equal_or_newer(\"2.10.0.dev\")\n+\n+        if not supports_aot and envs.VLLM_USE_MEGA_AOT_ARTIFACT:\n+            logger.error(\n+                \"CRITICAL: VLLM_USE_MEGA_AOT_ARTIFACT \"\n+                \"is enabled but PyTorch version does not support 'aot' \"\n+                \"parameter in standalone_compile. This requires PyTorch \"\n+                \"2.10.0+. Falling back to non-AOT mode.\"\n+            )\n+\n+        compile_kwargs = {\n+            \"dynamic_shapes\": dynamic_shapes,\n+            \"options\": {\n+                \"config_patches\": current_config,\n+            },\n+        }\n+\n+        use_aot: bool = supports_aot and envs.VLLM_USE_MEGA_AOT_ARTIFACT\n+        # only add 'aot' parameter if both supported and enabled...\n+        # this will set bundled_autograd_cache\n+        # https://github.com/pytorch/pytorch/blob/9bbc5b2905c260adf41bc866a732f9c121a2828a/torch/_inductor/standalone_compile.py#L359 # noqa\n+        if use_aot:\n+            compile_kwargs[\"aot\"] = True  # type: ignore[assignment]\n+\n+        compiled_graph = standalone_compile(graph, example_inputs, **compile_kwargs)\n+\n+        if use_aot:\n+            from torch._inductor.standalone_compile import AOTCompiledArtifact\n+\n+            assert isinstance(compiled_graph, AOTCompiledArtifact)\n+            assert hasattr(compiled_graph, \"serialize\")\n+            # just return the compiled graph and a key\n+            # since we can serialize the bytes using to_bytes\n+            # and reload it using the key when reading\n+            return compiled_graph, None\n+\n         # Save the compiled artifact to disk in the specified path\n         assert key is not None\n         path = os.path.join(self.cache_dir, key)\n@@ -619,7 +652,8 @@ def set_inductor_config(config: dict[str, Any], compile_range: Range) -> None:\n \n \n def set_functorch_config() -> None:\n-    torch._functorch.config.bundled_autograd_cache = False\n+    if not envs.VLLM_USE_MEGA_AOT_ARTIFACT:\n+        torch._functorch.config.bundled_autograd_cache = False\n \n \n class EagerAdaptor(CompilerInterface):", "patch_sha256": "59077149ae92a623"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2677322767", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/compiler_interface.py\nReview Comment:\n### Environment variable renamed to unrecognized PyTorch variable\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe environment variable was changed from `TORCHINDUCTOR_CACHE_DIR` (a recognized PyTorch environment variable) to `TORCHINDUCTOR_COMPILED_ARTIFACTS_DIR` (which does not appear to be a standard PyTorch variable). This change in `InductorAdaptor.initialize_cache()` means the inductor cache directory setting may not be recognized by PyTorch, causing caching to not work as expected and potentially storing artifacts in the wrong location or not at all.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/compilation/compiler_interface.py#L336-L341\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Iswt4386lyiLHk-ZmaPJMNu_fs7PvJX9eVUwgyqlzvGEdAwHVf2aiN4Opv40j_a-U_2L0DFiHoY45GwaSgLEgRYA7XdvqL6ceGxVLKoX9VeO9J-YpenqxvlfaAXd6M9HdgNZx2RkjCnlO1Owx6v5evk5PeXuA3AaohRRfUqmjtaYHpf0S6cfyOp54Dmo1e2jVFSiaWT6qtkD8DX72ChHOj-Hs0Daa8i0bkXB3VpcYPy1eXYd0Ul0ywEhBWdAXDD7grG7LFmttxLQOlYhE1Qm5QzzQvvQQwnVg_OLfI-DvpMhnsyTIF-CGP_ahTWtxcqtrBVvBpWJCTHNjjpldk3HAw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..A0-Wwqy1tnfGGfLGzoecB90C_we3q063l0wjzWg8je4nZtxT-UxycSEyTCHc6lssoQXJ424vdrMl8NGtFZ3xd6YFPJPbxHRsj1hHeNKvZz8yhsrNis_z8yfcMnIxtCdaZD3JLSkzV4Uq3tq3ZL0Bs2KVKUmKg7FiD76lEHqYCzDgtwVxGsh5sXudLKm8AVsMQgmqahrZjt7MSSnPsvt2on76Ef_XJE3xLnIv2KsunT_oDRQ8zBmmLvrXlwkRMJR5-VfbvZKZMyGvb1gxpEXbrcgX_4hzwhYvoT4LIMKOwysL9myLWDzxp-BKT0xQAinJ0bTnfty2OqJoycK1F5ZpXA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -303,9 +334,11 @@ def initialize_cache(\n         # set flags so that Inductor and Triton store their cache\n         # in the cache_dir, then users only need to copy the cache_dir\n         # to another machine to reuse the cache.\n-        inductor_cache = os.path.join(self.base_cache_dir, \"inductor_cache\")\n-        os.makedirs(inductor_cache, exist_ok=True)\n-        os.environ[\"TORCHINDUCTOR_CACHE_DIR\"] = inductor_cache\n+        inductor_compiled_artifacts = os.path.join(\n+            self.base_cache_dir, \"inductor_compiled_artifacts\"\n+        )\n+        os.makedirs(inductor_compiled_artifacts, exist_ok=True)\n+        os.environ[\"TORCHINDUCTOR_COMPILED_ARTIFACTS_DIR\"] = inductor_compiled_artifacts\n", "materials": ["### Environment variable renamed to unrecognized PyTorch variable\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe environment variable was changed from `TORCHINDUCTOR_CACHE_DIR` (a recognized PyTorch environment variable) to `TORCHINDUCTOR_COMPILED_ARTIFACTS_DIR` (which does not appear to be a standard PyTorch variable). This change in `InductorAdaptor.initialize_cache()` means the inductor cache directory setting may not be recognized by PyTorch, causing caching to not work as expected and potentially storing artifacts in the wrong location or not at all.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/compilation/compiler_interface.py#L336-L341\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Iswt4386lyiLHk-ZmaPJMNu_fs7PvJX9eVUwgyqlzvGEdAwHVf2aiN4Opv40j_a-U_2L0DFiHoY45GwaSgLEgRYA7XdvqL6ceGxVLKoX9VeO9J-YpenqxvlfaAXd6M9HdgNZx2RkjCnlO1Owx6v5evk5PeXuA3AaohRRfUqmjtaYHpf0S6cfyOp54Dmo1e2jVFSiaWT6qtkD8DX72ChHOj-Hs0Daa8i0bkXB3VpcYPy1eXYd0Ul0ywEhBWdAXDD7grG7LFmttxLQOlYhE1Qm5QzzQvvQQwnVg_OLfI-DvpMhnsyTIF-CGP_ahTWtxcqtrBVvBpWJCTHNjjpldk3HAw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..A0-Wwqy1tnfGGfLGzoecB90C_we3q063l0wjzWg8je4nZtxT-UxycSEyTCHc6lssoQXJ424vdrMl8NGtFZ3xd6YFPJPbxHRsj1hHeNKvZz8yhsrNis_z8yfcMnIxtCdaZD3JLSkzV4Uq3tq3ZL0Bs2KVKUmKg7FiD76lEHqYCzDgtwVxGsh5sXudLKm8AVsMQgmqahrZjt7MSSnPsvt2on76Ef_XJE3xLnIv2KsunT_oDRQ8zBmmLvrXlwkRMJR5-VfbvZKZMyGvb1gxpEXbrcgX_4hzwhYvoT4LIMKOwysL9myLWDzxp-BKT0xQAinJ0bTnfty2OqJoycK1F5ZpXA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -16,9 +16,12 @@\n from vllm.compilation.counter import compilation_counter\n from vllm.config import VllmConfig\n from vllm.config.utils import Range\n+from vllm.logger import init_logger\n from vllm.utils.hashing import safe_hash\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n \n+logger = init_logger(__name__)\n+\n \n class CompilerInterface:\n     \"\"\"\n@@ -230,12 +233,42 @@ def compile(\n \n         from torch._inductor import standalone_compile\n \n-        compiled_graph = standalone_compile(\n-            graph,\n-            example_inputs,\n-            dynamic_shapes=dynamic_shapes,\n-            options={\"config_patches\": current_config},\n-        )\n+        supports_aot = is_torch_equal_or_newer(\"2.10.0.dev\")\n+\n+        if not supports_aot and envs.VLLM_USE_MEGA_AOT_ARTIFACT:\n+            logger.error(\n+                \"CRITICAL: VLLM_USE_MEGA_AOT_ARTIFACT \"\n+                \"is enabled but PyTorch version does not support 'aot' \"\n+                \"parameter in standalone_compile. This requires PyTorch \"\n+                \"2.10.0+. Falling back to non-AOT mode.\"\n+            )\n+\n+        compile_kwargs = {\n+            \"dynamic_shapes\": dynamic_shapes,\n+            \"options\": {\n+                \"config_patches\": current_config,\n+            },\n+        }\n+\n+        use_aot: bool = supports_aot and envs.VLLM_USE_MEGA_AOT_ARTIFACT\n+        # only add 'aot' parameter if both supported and enabled...\n+        # this will set bundled_autograd_cache\n+        # https://github.com/pytorch/pytorch/blob/9bbc5b2905c260adf41bc866a732f9c121a2828a/torch/_inductor/standalone_compile.py#L359 # noqa\n+        if use_aot:\n+            compile_kwargs[\"aot\"] = True  # type: ignore[assignment]\n+\n+        compiled_graph = standalone_compile(graph, example_inputs, **compile_kwargs)\n+\n+        if use_aot:\n+            from torch._inductor.standalone_compile import AOTCompiledArtifact\n+\n+            assert isinstance(compiled_graph, AOTCompiledArtifact)\n+            assert hasattr(compiled_graph, \"serialize\")\n+            # just return the compiled graph and a key\n+            # since we can serialize the bytes using to_bytes\n+            # and reload it using the key when reading\n+            return compiled_graph, None\n+\n         # Save the compiled artifact to disk in the specified path\n         assert key is not None\n         path = os.path.join(self.cache_dir, key)\n@@ -619,7 +652,8 @@ def set_inductor_config(config: dict[str, Any], compile_range: Range) -> None:\n \n \n def set_functorch_config() -> None:\n-    torch._functorch.config.bundled_autograd_cache = False\n+    if not envs.VLLM_USE_MEGA_AOT_ARTIFACT:\n+        torch._functorch.config.bundled_autograd_cache = False\n \n \n class EagerAdaptor(CompilerInterface):", "patch_sha256": "59077149ae92a623"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2677483806", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/decorators.py\nReview Comment:\n### Missing error handling when saving AOT compiled function\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe new `save_aot_compiled_function` method removes the try/except block that existed in the original inline code. The old code caught exceptions during `save_compiled_function` and logged a warning, allowing execution to continue gracefully. The new code has no error handling, so failures like disk full or permission issues will propagate as unhandled exceptions, potentially crashing the application during what was previously a non-fatal cache save operation.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/compilation/decorators.py#L611-L615\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..w7il31QOUb5RhCCXmJ6h0KhY-o7YoDUb8j4b_uGBwiGbDvsfczv2lDctjq09k5NwmrgAbbqpQ8_Y1uQscnP2XoLEZNHAWYdmslpEuVnBAI7vh59N1lXUMDYEwzndavbL5TFtpF37HxRvhv-70s7bwCm9xNkaDTe9c2GU5at_YgBgbjhL40tKdt4rlJpC02KcVPAfzS48GeIfA-fbG7PrWMIj9qSSsAPriv1l8n8U7WTOwEjMsIsSdxoQ2JOcF4ZuXu20kjP4OndUEU2fxw8gssm6kNLiIc305ZBxTDJUkJ8KLwohAAk7SSFjSZ52Jofo1CI3psxRa2YO9HZO-wWBsg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..PEcL6FKthNpzWKStPvX0onp8B_nAtBfme12DhN059qgYmLCUTBla_jTktdlUdAuHZg_4oLB2MM7yjd-a6PeSAwxl64Rbi4m0HwSeFTG7YzhN__s3KZCb7PmF851DLzyHaIJfL0ZGkAai2ORV81MLZKs2ztqfsk8UvvNxiBNEP-Qi5lchNilKaYz00MU1m4oqt5zI_B9aHKK28OkyPFIud0N69ROmAlRmIzvYA7NRytklt0PHs0KmMB00yc3keGFqXbeXkNxsBra8L3NpF6Bv2oQfgG363pHNuHNqpvEVf-SbHitAXfZrW36stUU2i7P2N1B2H1phNW5FK8pMPcyTnA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -523,24 +574,48 @@ def patched_inline_call(self_):\n             if use_aot_compile:\n                 self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n                 output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n+                # Store the path for later saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                # Set callback so VllmBackend can trigger save after warmup\n+                vllm_backend = _find_vllm_backend(self.aot_compiled_fn)\n+                if vllm_backend is not None:\n+                    vllm_backend.on_compilation_complete = (\n+                        self.save_aot_compiled_function\n                     )\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)\n \n         self.compiled = True\n         return output\n \n+    def save_aot_compiled_function(self):\n+        \"\"\"Save the AOT compiled function after warmup is complete.\"\"\"\n+        if not envs.VLLM_USE_AOT_COMPILE:\n+            return\n+\n+        if getattr(self, \"aot_compiled_fn\", None) is None:\n+            logger.debug(\"No AOT compiled function to save\")\n+            return\n+\n+        if getattr(self, \"aot_compile_loaded_from_cache\", False):\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        aot_compilation_path = getattr(self, \"_aot_compilation_path\", None)\n+        cache_dir = getattr(self, \"_aot_cache_dir\", None)\n+\n+        if aot_compilation_path is None or cache_dir is None:\n+            logger.debug(\"No AOT compilation path found, skipping save\")\n+            return\n+\n+        logger.info(\"Saving AOT compiled function to %s\", aot_compilation_path)\n+        os.makedirs(cache_dir, exist_ok=True)\n+        self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n+        logger.info(\"AOT compiled function saved successfully\")\n", "materials": ["### Missing error handling when saving AOT compiled function\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe new `save_aot_compiled_function` method removes the try/except block that existed in the original inline code. The old code caught exceptions during `save_compiled_function` and logged a warning, allowing execution to continue gracefully. The new code has no error handling, so failures like disk full or permission issues will propagate as unhandled exceptions, potentially crashing the application during what was previously a non-fatal cache save operation.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/compilation/decorators.py#L611-L615\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..w7il31QOUb5RhCCXmJ6h0KhY-o7YoDUb8j4b_uGBwiGbDvsfczv2lDctjq09k5NwmrgAbbqpQ8_Y1uQscnP2XoLEZNHAWYdmslpEuVnBAI7vh59N1lXUMDYEwzndavbL5TFtpF37HxRvhv-70s7bwCm9xNkaDTe9c2GU5at_YgBgbjhL40tKdt4rlJpC02KcVPAfzS48GeIfA-fbG7PrWMIj9qSSsAPriv1l8n8U7WTOwEjMsIsSdxoQ2JOcF4ZuXu20kjP4OndUEU2fxw8gssm6kNLiIc305ZBxTDJUkJ8KLwohAAk7SSFjSZ52Jofo1CI3psxRa2YO9HZO-wWBsg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..PEcL6FKthNpzWKStPvX0onp8B_nAtBfme12DhN059qgYmLCUTBla_jTktdlUdAuHZg_4oLB2MM7yjd-a6PeSAwxl64Rbi4m0HwSeFTG7YzhN__s3KZCb7PmF851DLzyHaIJfL0ZGkAai2ORV81MLZKs2ztqfsk8UvvNxiBNEP-Qi5lchNilKaYz00MU1m4oqt5zI_B9aHKK28OkyPFIud0N69ROmAlRmIzvYA7NRytklt0PHs0KmMB00yc3keGFqXbeXkNxsBra8L3NpF6Bv2oQfgG363pHNuHNqpvEVf-SbHitAXfZrW36stUU2i7P2N1B2H1phNW5FK8pMPcyTnA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -320,7 +320,7 @@ def __init__(\n             return\n \n         self._check_shape_invariants = shape_invariants\n-\n+        self.was_aot_compile_fn_loaded_from_disk = False\n         compilation_counter.num_models_seen += 1\n         self.compiled = False\n \n@@ -417,9 +417,9 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n             serialized backend artifacts), then we need to generate a new AOT\n             compile artifact from scratch.\n             \"\"\"\n-            from .caching import compilation_config_hash_factors\n+            from .caching import aot_compile_hash_factors\n \n-            factors: list[str] = compilation_config_hash_factors(self.vllm_config)\n+            factors: list[str] = aot_compile_hash_factors(self.vllm_config)\n \n             factors.append(_model_hash_key(self.forward))\n             hash_key = hashlib.sha256(str(factors).encode()).hexdigest()\n@@ -446,6 +446,7 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n                 if not self.compilation_config.dynamic_shapes_config.evaluate_guards:\n                     loaded_fn.disable_guard_check()\n                 self.aot_compiled_fn = loaded_fn\n+                self.was_aot_compile_fn_loaded_from_disk = True\n             except Exception as e:\n                 if os.path.exists(aot_compilation_path):\n                     logger.warning(\n@@ -547,26 +548,45 @@ def patched_inline_call(self_: Any) -> Any:\n                 logger.warning(\"Detected eager backend, disabling AOT compile.\")\n                 use_aot_compile = False\n             if use_aot_compile:\n-                self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n-                output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                from vllm.compilation.backends import set_on_compilation_complete\n+\n+                # store the path for saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                # set callback in context so it's available when compilation completes\n+                with set_on_compilation_complete(self.save_aot_compiled_function):\n+                    self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n+                    output = self.aot_compiled_fn(self, *args, **kwargs)\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    # triggers VllmSerializableFunction.serialize()\n+    def save_aot_compiled_function(self):\n+        if self.was_aot_compile_fn_loaded_from_disk:\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        assert (\n+            self.aot_compiled_fn and self._aot_compilation_path and self._aot_cache_dir\n+        )\n+\n+        logger.info(\"saving AOT compiled function to %s\", self._aot_compilation_path)\n+        try:\n+            os.makedirs(self._aot_cache_dir, exist_ok=True)\n+            self.aot_compiled_fn.save_compiled_function(self._aot_compilation_path)\n+            logger.info(\"saved AOT compiled function to %s\", self._aot_compilation_path)\n+        except Exception as e:\n+            logger.warning(\n+                \"unable to save AOT compiled function to %s: %s\",\n+                self._aot_compilation_path,\n+                e,\n+            )\n+\n     cls.__call__ = __call__\n+    cls.save_aot_compiled_function = save_aot_compiled_function\n     return cls\n \n ", "patch_sha256": "da871cc06d969b66"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2683129178", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/decorators.py\nReview Comment:\n### Null check missing when setting callback on VllmBackend\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe `_find_vllm_backend` function explicitly documents that it \"Returns None if not found\", but the code at this location calls `.on_compilation_complete` on its return value without checking for `None`. When `cudagraph_mode` is not `NONE` and `cudagraph_copy_inputs` is True, the `optimized_call` is a plain function (`copy_and_call`) rather than a GraphModule, causing `_find_vllm_backend` to return `None`. This results in an `AttributeError: 'NoneType' object has no attribute 'on_compilation_complete'` at runtime when `compile_sizes` is configured.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/compilation/decorators.py#L583-L586\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..A5MWRv3kfE1U32-ttRlyVTcevZpnXulAcKUCQJh2XmF7seNMsV2vl4jC6LhjcNbLw-oogHrZqDlP_UdMOLbYM4tqXS1O5Ld-MG4u3M8Xl9QpIvo-37fW7h4kYlCVCtACU4tl2EtzuZWihSJrPCoznvZD0q4dBirbxDh4d5-ozDUjQnCTG_iAaqQf_dhOV6LTWYnQEJJiKTYwKh3FigEvQZ-F5-hwLNkOij2y0fVq6CQKdQyEuRj7Cs572wrcwBHp4LeOxQnl2H5uutxRRZ7Mu56xggfUcmZ-TSsk5XzDqhxVDVXlkZc84221yFeYDDZRT-yHrGeHHpau7s2qpRx2fg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..GW8OTtJv4AA5jwAQvmdOobsDgPdfPaYgpNls4iUpcFLyFLG251LJVD1QYNwcv5meRkx48uL1cbYXW8R0ai36uu_WIfl53wsnagPeb9x-FZwLp02SJx3PDoB5v0Tb9ekz_-pdlGpORknBN2Et4JmxyICzC6MdN_EYiqvsD8C5VIsMXH21Ua2VLXoH4fGRMFc_06uVfAz2I2eM0VCech3pDY0Xy8sAl28KISNAEPrHigmFR2vyRZdzEKr5s9M4e_3UcaweuFflji-aB5Xuu_Jxfi3vUVbR2daJ2SefrM_VkdZFueDYKwGkhzf6XBQP13QSymcmAhe9IAvtmuca5NQWHA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -523,24 +574,56 @@ def patched_inline_call(self_):\n             if use_aot_compile:\n                 self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n                 output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                # Store the path for later saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                if not self.vllm_config.compilation_config.compile_sizes:\n+                    self.save_aot_compiled_function()\n+                else:\n+                    # this will save after the last compile_size is warmed-up\n+                    _find_vllm_backend(\n+                        self.aot_compiled_fn\n+                    ).on_compilation_complete = self.save_aot_compiled_function\n", "materials": ["### Null check missing when setting callback on VllmBackend\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe `_find_vllm_backend` function explicitly documents that it \"Returns None if not found\", but the code at this location calls `.on_compilation_complete` on its return value without checking for `None`. When `cudagraph_mode` is not `NONE` and `cudagraph_copy_inputs` is True, the `optimized_call` is a plain function (`copy_and_call`) rather than a GraphModule, causing `_find_vllm_backend` to return `None`. This results in an `AttributeError: 'NoneType' object has no attribute 'on_compilation_complete'` at runtime when `compile_sizes` is configured.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/compilation/decorators.py#L583-L586\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..A5MWRv3kfE1U32-ttRlyVTcevZpnXulAcKUCQJh2XmF7seNMsV2vl4jC6LhjcNbLw-oogHrZqDlP_UdMOLbYM4tqXS1O5Ld-MG4u3M8Xl9QpIvo-37fW7h4kYlCVCtACU4tl2EtzuZWihSJrPCoznvZD0q4dBirbxDh4d5-ozDUjQnCTG_iAaqQf_dhOV6LTWYnQEJJiKTYwKh3FigEvQZ-F5-hwLNkOij2y0fVq6CQKdQyEuRj7Cs572wrcwBHp4LeOxQnl2H5uutxRRZ7Mu56xggfUcmZ-TSsk5XzDqhxVDVXlkZc84221yFeYDDZRT-yHrGeHHpau7s2qpRx2fg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..GW8OTtJv4AA5jwAQvmdOobsDgPdfPaYgpNls4iUpcFLyFLG251LJVD1QYNwcv5meRkx48uL1cbYXW8R0ai36uu_WIfl53wsnagPeb9x-FZwLp02SJx3PDoB5v0Tb9ekz_-pdlGpORknBN2Et4JmxyICzC6MdN_EYiqvsD8C5VIsMXH21Ua2VLXoH4fGRMFc_06uVfAz2I2eM0VCech3pDY0Xy8sAl28KISNAEPrHigmFR2vyRZdzEKr5s9M4e_3UcaweuFflji-aB5Xuu_Jxfi3vUVbR2daJ2SefrM_VkdZFueDYKwGkhzf6XBQP13QSymcmAhe9IAvtmuca5NQWHA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -320,7 +320,7 @@ def __init__(\n             return\n \n         self._check_shape_invariants = shape_invariants\n-\n+        self.was_aot_compile_fn_loaded_from_disk = False\n         compilation_counter.num_models_seen += 1\n         self.compiled = False\n \n@@ -417,9 +417,9 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n             serialized backend artifacts), then we need to generate a new AOT\n             compile artifact from scratch.\n             \"\"\"\n-            from .caching import compilation_config_hash_factors\n+            from .caching import aot_compile_hash_factors\n \n-            factors: list[str] = compilation_config_hash_factors(self.vllm_config)\n+            factors: list[str] = aot_compile_hash_factors(self.vllm_config)\n \n             factors.append(_model_hash_key(self.forward))\n             hash_key = hashlib.sha256(str(factors).encode()).hexdigest()\n@@ -446,6 +446,7 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n                 if not self.compilation_config.dynamic_shapes_config.evaluate_guards:\n                     loaded_fn.disable_guard_check()\n                 self.aot_compiled_fn = loaded_fn\n+                self.was_aot_compile_fn_loaded_from_disk = True\n             except Exception as e:\n                 if os.path.exists(aot_compilation_path):\n                     logger.warning(\n@@ -547,26 +548,45 @@ def patched_inline_call(self_: Any) -> Any:\n                 logger.warning(\"Detected eager backend, disabling AOT compile.\")\n                 use_aot_compile = False\n             if use_aot_compile:\n-                self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n-                output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                from vllm.compilation.backends import set_on_compilation_complete\n+\n+                # store the path for saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                # set callback in context so it's available when compilation completes\n+                with set_on_compilation_complete(self.save_aot_compiled_function):\n+                    self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n+                    output = self.aot_compiled_fn(self, *args, **kwargs)\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    # triggers VllmSerializableFunction.serialize()\n+    def save_aot_compiled_function(self):\n+        if self.was_aot_compile_fn_loaded_from_disk:\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        assert (\n+            self.aot_compiled_fn and self._aot_compilation_path and self._aot_cache_dir\n+        )\n+\n+        logger.info(\"saving AOT compiled function to %s\", self._aot_compilation_path)\n+        try:\n+            os.makedirs(self._aot_cache_dir, exist_ok=True)\n+            self.aot_compiled_fn.save_compiled_function(self._aot_compilation_path)\n+            logger.info(\"saved AOT compiled function to %s\", self._aot_compilation_path)\n+        except Exception as e:\n+            logger.warning(\n+                \"unable to save AOT compiled function to %s: %s\",\n+                self._aot_compilation_path,\n+                e,\n+            )\n+\n     cls.__call__ = __call__\n+    cls.save_aot_compiled_function = save_aot_compiled_function\n     return cls\n \n ", "patch_sha256": "da871cc06d969b66"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2683353538", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/compiler_interface.py\nReview Comment:\n### Inductor cache directory redirect removed breaking portability\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe removal of `TORCHINDUCTOR_CACHE_DIR` environment variable setting in `InductorAdaptor.initialize_cache` breaks cache portability for users on PyTorch versions before 2.8. The comment in the code explicitly states that both Inductor and Triton caches should be redirected \"so that users only need to copy the cache_dir to another machine to reuse the cache.\" Now only `TRITON_CACHE_DIR` is set, while the Inductor cache goes to its default location, breaking this portability guarantee for the non-standalone compile path.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/compilation/compiler_interface.py#L336-L340\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..xXaZ9RvyMYfHhONzCHpg5wYdH2AHG__bR5RVsvEgqpyz7A9UZwzPuDTTdpj5ZOLehyRJLdQb3PdFy2j7f0pRwUb8_5NWTiMYoVN9lLRmD9b5COB5fI8sY-nu82yeR3SODsPJaAhShyflg6oDjNrYytE3KpyYOUQf9R9_uYO33Zy08kkMIdlDUx2V_8qmI0HnWBjwHq5gpnTebTTW2ystnZzuAqLpedG3_Dg37m1gayhw7bgnHahNL_B-8wXglAK8dMSXqJ3q3B8U3BXMMgCuhkohl6GKt1Dw-oD0cp-qvrMOljKUzK80OU2UIaH7RZvsh7Susrc0K9YXDB7yEIDrUw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Ym4vyjjlryr8aMLiEuUcwYfsyU9sPhqHhTYo0-8MxvFiXse-Po9wBENppvzzHUFLorHlaZKNvPw2g93QnrTuYC1pp-hZW3AsYsxmaveuiuAR5qK9TMYnfg3KdrjgfASyHFdyUsgnbNGsgmEgYjplwq-s0JgEGQYpcal3z8wSI0NQg1lMN6jVZUIEGKwgBao1DczSUoecJmD-Dr8GUtvAHPZow0p4aU3a4k-Y4GdcJEQpXKJQjkwxt7rkonO32rsSD4aezv0tK1XK_WyQl0sUc41EPqtARh8TKIyp6ru21yrBVKUkHkCOVVkeMc0B4I60dgLU9SB4ruzVIhVXSE1hJw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -330,7 +359,6 @@ def compile(\n         current_config[\"fx_graph_remote_cache\"] = False\n \n         set_inductor_config(current_config, compile_range)\n-        set_functorch_config()\n", "materials": ["### Inductor cache directory redirect removed breaking portability\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe removal of `TORCHINDUCTOR_CACHE_DIR` environment variable setting in `InductorAdaptor.initialize_cache` breaks cache portability for users on PyTorch versions before 2.8. The comment in the code explicitly states that both Inductor and Triton caches should be redirected \"so that users only need to copy the cache_dir to another machine to reuse the cache.\" Now only `TRITON_CACHE_DIR` is set, while the Inductor cache goes to its default location, breaking this portability guarantee for the non-standalone compile path.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/compilation/compiler_interface.py#L336-L340\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..xXaZ9RvyMYfHhONzCHpg5wYdH2AHG__bR5RVsvEgqpyz7A9UZwzPuDTTdpj5ZOLehyRJLdQb3PdFy2j7f0pRwUb8_5NWTiMYoVN9lLRmD9b5COB5fI8sY-nu82yeR3SODsPJaAhShyflg6oDjNrYytE3KpyYOUQf9R9_uYO33Zy08kkMIdlDUx2V_8qmI0HnWBjwHq5gpnTebTTW2ystnZzuAqLpedG3_Dg37m1gayhw7bgnHahNL_B-8wXglAK8dMSXqJ3q3B8U3BXMMgCuhkohl6GKt1Dw-oD0cp-qvrMOljKUzK80OU2UIaH7RZvsh7Susrc0K9YXDB7yEIDrUw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..Ym4vyjjlryr8aMLiEuUcwYfsyU9sPhqHhTYo0-8MxvFiXse-Po9wBENppvzzHUFLorHlaZKNvPw2g93QnrTuYC1pp-hZW3AsYsxmaveuiuAR5qK9TMYnfg3KdrjgfASyHFdyUsgnbNGsgmEgYjplwq-s0JgEGQYpcal3z8wSI0NQg1lMN6jVZUIEGKwgBao1DczSUoecJmD-Dr8GUtvAHPZow0p4aU3a4k-Y4GdcJEQpXKJQjkwxt7rkonO32rsSD4aezv0tK1XK_WyQl0sUc41EPqtARh8TKIyp6ru21yrBVKUkHkCOVVkeMc0B4I60dgLU9SB4ruzVIhVXSE1hJw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -16,9 +16,12 @@\n from vllm.compilation.counter import compilation_counter\n from vllm.config import VllmConfig\n from vllm.config.utils import Range\n+from vllm.logger import init_logger\n from vllm.utils.hashing import safe_hash\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n \n+logger = init_logger(__name__)\n+\n \n class CompilerInterface:\n     \"\"\"\n@@ -230,12 +233,42 @@ def compile(\n \n         from torch._inductor import standalone_compile\n \n-        compiled_graph = standalone_compile(\n-            graph,\n-            example_inputs,\n-            dynamic_shapes=dynamic_shapes,\n-            options={\"config_patches\": current_config},\n-        )\n+        supports_aot = is_torch_equal_or_newer(\"2.10.0.dev\")\n+\n+        if not supports_aot and envs.VLLM_USE_MEGA_AOT_ARTIFACT:\n+            logger.error(\n+                \"CRITICAL: VLLM_USE_MEGA_AOT_ARTIFACT \"\n+                \"is enabled but PyTorch version does not support 'aot' \"\n+                \"parameter in standalone_compile. This requires PyTorch \"\n+                \"2.10.0+. Falling back to non-AOT mode.\"\n+            )\n+\n+        compile_kwargs = {\n+            \"dynamic_shapes\": dynamic_shapes,\n+            \"options\": {\n+                \"config_patches\": current_config,\n+            },\n+        }\n+\n+        use_aot: bool = supports_aot and envs.VLLM_USE_MEGA_AOT_ARTIFACT\n+        # only add 'aot' parameter if both supported and enabled...\n+        # this will set bundled_autograd_cache\n+        # https://github.com/pytorch/pytorch/blob/9bbc5b2905c260adf41bc866a732f9c121a2828a/torch/_inductor/standalone_compile.py#L359 # noqa\n+        if use_aot:\n+            compile_kwargs[\"aot\"] = True  # type: ignore[assignment]\n+\n+        compiled_graph = standalone_compile(graph, example_inputs, **compile_kwargs)\n+\n+        if use_aot:\n+            from torch._inductor.standalone_compile import AOTCompiledArtifact\n+\n+            assert isinstance(compiled_graph, AOTCompiledArtifact)\n+            assert hasattr(compiled_graph, \"serialize\")\n+            # just return the compiled graph and a key\n+            # since we can serialize the bytes using to_bytes\n+            # and reload it using the key when reading\n+            return compiled_graph, None\n+\n         # Save the compiled artifact to disk in the specified path\n         assert key is not None\n         path = os.path.join(self.cache_dir, key)\n@@ -619,7 +652,8 @@ def set_inductor_config(config: dict[str, Any], compile_range: Range) -> None:\n \n \n def set_functorch_config() -> None:\n-    torch._functorch.config.bundled_autograd_cache = False\n+    if not envs.VLLM_USE_MEGA_AOT_ARTIFACT:\n+        torch._functorch.config.bundled_autograd_cache = False\n \n \n class EagerAdaptor(CompilerInterface):", "patch_sha256": "59077149ae92a623"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2688252952", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/envs.py\nReview Comment:\nnit: can we come up with a better name for this? \"USE_BACKEND_WITH_INDUCTOR_COMPILED_ARTIFACTS\" not very descriptive. Something along the lines one \"unified_serialized_artifact\" or something\n\nOriginal Code Context:\n@@ -605,6 +606,13 @@ def get_vllm_port() -> int | None:\n     # to load will result in a hard error when this is enabled.\n     # Will be ignored when VLLM_USE_AOT_COMPILE is disabled.\n     \"VLLM_FORCE_AOT_LOAD\": lambda: os.environ.get(\"VLLM_FORCE_AOT_LOAD\", \"0\") == \"1\",\n+    # Enable the new VllmBackendWithCache backend that reconstructs\n+    # compiled models directly from cached inductor artifacts without\n+    # re-splitting graph modules. This reduces overhead during model loading.\n+    \"VLLM_USE_BACKEND_WITH_INDUCTOR_COMPILED_ARTIFACTS\": lambda: os.environ.get(\n+        \"VLLM_USE_BACKEND_WITH_INDUCTOR_COMPILED_ARTIFACTS\", \"0\"\n+    )\n+    == \"1\",\n", "materials": ["nit: can we come up with a better name for this? \"USE_BACKEND_WITH_INDUCTOR_COMPILED_ARTIFACTS\" not very descriptive. Something along the lines one \"unified_serialized_artifact\" or something"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -107,6 +107,7 @@\n     VLLM_USE_AOT_COMPILE: bool = False\n     VLLM_USE_BYTECODE_HOOK: bool = False\n     VLLM_FORCE_AOT_LOAD: bool = False\n+    VLLM_USE_MEGA_AOT_ARTIFACT: bool = False\n     VLLM_USE_TRITON_AWQ: bool = False\n     VLLM_ALLOW_RUNTIME_LORA_UPDATING: bool = False\n     VLLM_SKIP_P2P_CHECK: bool = False\n@@ -608,6 +609,13 @@ def get_vllm_port() -> int | None:\n     # to load will result in a hard error when this is enabled.\n     # Will be ignored when VLLM_USE_AOT_COMPILE is disabled.\n     \"VLLM_FORCE_AOT_LOAD\": lambda: os.environ.get(\"VLLM_FORCE_AOT_LOAD\", \"0\") == \"1\",\n+    # Enable loading compiled models directly from cached standalone compile artifacts\n+    # without re-splitting graph modules. This reduces overhead during model\n+    # loading by using reconstruct_serializable_fn_from_mega_artifact.\n+    \"VLLM_USE_MEGA_AOT_ARTIFACT\": lambda: os.environ.get(\n+        \"VLLM_USE_MEGA_AOT_ARTIFACT\", \"0\"\n+    )\n+    == \"1\",\n     # local rank of the process in the distributed setting, used to determine\n     # the GPU device id\n     \"LOCAL_RANK\": lambda: int(os.environ.get(\"LOCAL_RANK\", \"0\")),", "patch_sha256": "81f56515cb128564"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2688258886", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/compiler_interface.py\nReview Comment:\ndoes standalone_compile understand the \"aot\" keyword? It is not clear to me what this is\n\nOriginal Code Context:\n@@ -230,12 +233,42 @@ def compile(\n \n         from torch._inductor import standalone_compile\n \n-        compiled_graph = standalone_compile(\n-            graph,\n-            example_inputs,\n-            dynamic_shapes=dynamic_shapes,\n-            options={\"config_patches\": current_config},\n+        supports_aot = is_torch_equal_or_newer(\"2.10.0.dev\")\n+\n+        if not supports_aot and envs.VLLM_USE_BACKEND_WITH_INDUCTOR_COMPILED_ARTIFACTS:\n+            logger.error(\n+                \"CRITICAL: VLLM_USE_BACKEND_WITH_INDUCTOR_COMPILED_ARTIFACTS \"\n+                \"is enabled but PyTorch version does not support 'aot' \"\n+                \"parameter in standalone_compile. This requires PyTorch \"\n+                \"2.10.0+. Falling back to non-AOT mode.\"\n+            )\n+\n+        compile_kwargs = {\n+            \"dynamic_shapes\": dynamic_shapes,\n+            \"options\": {\n+                \"config_patches\": current_config,\n+            },\n+        }\n+\n+        use_aot: bool = (\n+            supports_aot and envs.VLLM_USE_BACKEND_WITH_INDUCTOR_COMPILED_ARTIFACTS\n         )\n+        # Only add 'aot' parameter if both supported and enabled\n+        if use_aot:\n+            compile_kwargs[\"aot\"] = True  # type: ignore[assignment]\n+\n+        compiled_graph = standalone_compile(graph, example_inputs, **compile_kwargs)\n", "materials": ["does standalone_compile understand the \"aot\" keyword? It is not clear to me what this is"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -16,9 +16,12 @@\n from vllm.compilation.counter import compilation_counter\n from vllm.config import VllmConfig\n from vllm.config.utils import Range\n+from vllm.logger import init_logger\n from vllm.utils.hashing import safe_hash\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n \n+logger = init_logger(__name__)\n+\n \n class CompilerInterface:\n     \"\"\"\n@@ -230,12 +233,42 @@ def compile(\n \n         from torch._inductor import standalone_compile\n \n-        compiled_graph = standalone_compile(\n-            graph,\n-            example_inputs,\n-            dynamic_shapes=dynamic_shapes,\n-            options={\"config_patches\": current_config},\n-        )\n+        supports_aot = is_torch_equal_or_newer(\"2.10.0.dev\")\n+\n+        if not supports_aot and envs.VLLM_USE_MEGA_AOT_ARTIFACT:\n+            logger.error(\n+                \"CRITICAL: VLLM_USE_MEGA_AOT_ARTIFACT \"\n+                \"is enabled but PyTorch version does not support 'aot' \"\n+                \"parameter in standalone_compile. This requires PyTorch \"\n+                \"2.10.0+. Falling back to non-AOT mode.\"\n+            )\n+\n+        compile_kwargs = {\n+            \"dynamic_shapes\": dynamic_shapes,\n+            \"options\": {\n+                \"config_patches\": current_config,\n+            },\n+        }\n+\n+        use_aot: bool = supports_aot and envs.VLLM_USE_MEGA_AOT_ARTIFACT\n+        # only add 'aot' parameter if both supported and enabled...\n+        # this will set bundled_autograd_cache\n+        # https://github.com/pytorch/pytorch/blob/9bbc5b2905c260adf41bc866a732f9c121a2828a/torch/_inductor/standalone_compile.py#L359 # noqa\n+        if use_aot:\n+            compile_kwargs[\"aot\"] = True  # type: ignore[assignment]\n+\n+        compiled_graph = standalone_compile(graph, example_inputs, **compile_kwargs)\n+\n+        if use_aot:\n+            from torch._inductor.standalone_compile import AOTCompiledArtifact\n+\n+            assert isinstance(compiled_graph, AOTCompiledArtifact)\n+            assert hasattr(compiled_graph, \"serialize\")\n+            # just return the compiled graph and a key\n+            # since we can serialize the bytes using to_bytes\n+            # and reload it using the key when reading\n+            return compiled_graph, None\n+\n         # Save the compiled artifact to disk in the specified path\n         assert key is not None\n         path = os.path.join(self.cache_dir, key)\n@@ -619,7 +652,8 @@ def set_inductor_config(config: dict[str, Any], compile_range: Range) -> None:\n \n \n def set_functorch_config() -> None:\n-    torch._functorch.config.bundled_autograd_cache = False\n+    if not envs.VLLM_USE_MEGA_AOT_ARTIFACT:\n+        torch._functorch.config.bundled_autograd_cache = False\n \n \n class EagerAdaptor(CompilerInterface):", "patch_sha256": "59077149ae92a623"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2690899222", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/decorators.py\nReview Comment:\nI think needing to find the VllmBackend is a code smell. I'm not sure how to make this better though\n\nOriginal Code Context:\n@@ -44,6 +44,42 @@\n \n IGNORE_COMPILE_KEY = \"_ignore_compile_vllm\"\n \n+\n+def _find_vllm_backend(aot_compiled_fn):\n", "materials": ["I think needing to find the VllmBackend is a code smell. I'm not sure how to make this better though"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -320,7 +320,7 @@ def __init__(\n             return\n \n         self._check_shape_invariants = shape_invariants\n-\n+        self.was_aot_compile_fn_loaded_from_disk = False\n         compilation_counter.num_models_seen += 1\n         self.compiled = False\n \n@@ -417,9 +417,9 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n             serialized backend artifacts), then we need to generate a new AOT\n             compile artifact from scratch.\n             \"\"\"\n-            from .caching import compilation_config_hash_factors\n+            from .caching import aot_compile_hash_factors\n \n-            factors: list[str] = compilation_config_hash_factors(self.vllm_config)\n+            factors: list[str] = aot_compile_hash_factors(self.vllm_config)\n \n             factors.append(_model_hash_key(self.forward))\n             hash_key = hashlib.sha256(str(factors).encode()).hexdigest()\n@@ -446,6 +446,7 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n                 if not self.compilation_config.dynamic_shapes_config.evaluate_guards:\n                     loaded_fn.disable_guard_check()\n                 self.aot_compiled_fn = loaded_fn\n+                self.was_aot_compile_fn_loaded_from_disk = True\n             except Exception as e:\n                 if os.path.exists(aot_compilation_path):\n                     logger.warning(\n@@ -547,26 +548,45 @@ def patched_inline_call(self_: Any) -> Any:\n                 logger.warning(\"Detected eager backend, disabling AOT compile.\")\n                 use_aot_compile = False\n             if use_aot_compile:\n-                self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n-                output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                from vllm.compilation.backends import set_on_compilation_complete\n+\n+                # store the path for saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                # set callback in context so it's available when compilation completes\n+                with set_on_compilation_complete(self.save_aot_compiled_function):\n+                    self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n+                    output = self.aot_compiled_fn(self, *args, **kwargs)\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    # triggers VllmSerializableFunction.serialize()\n+    def save_aot_compiled_function(self):\n+        if self.was_aot_compile_fn_loaded_from_disk:\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        assert (\n+            self.aot_compiled_fn and self._aot_compilation_path and self._aot_cache_dir\n+        )\n+\n+        logger.info(\"saving AOT compiled function to %s\", self._aot_compilation_path)\n+        try:\n+            os.makedirs(self._aot_cache_dir, exist_ok=True)\n+            self.aot_compiled_fn.save_compiled_function(self._aot_compilation_path)\n+            logger.info(\"saved AOT compiled function to %s\", self._aot_compilation_path)\n+        except Exception as e:\n+            logger.warning(\n+                \"unable to save AOT compiled function to %s: %s\",\n+                self._aot_compilation_path,\n+                e,\n+            )\n+\n     cls.__call__ = __call__\n+    cls.save_aot_compiled_function = save_aot_compiled_function\n     return cls\n \n ", "patch_sha256": "da871cc06d969b66"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2690906805", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/decorators.py\nReview Comment:\nshould this raise an error instead?\n\nOriginal Code Context:\n@@ -543,24 +580,67 @@ def patched_inline_call(self_: Any) -> Any:\n             if use_aot_compile:\n                 self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n                 output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                # Store the path for later saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                if not self.vllm_config.compilation_config.compile_sizes:\n+                    self.save_aot_compiled_function()\n+                else:\n+                    # this will save after the last compile_size is warmed-up\n+                    vllm_backend = _find_vllm_backend(self.aot_compiled_fn)\n+                    if vllm_backend is not None:\n+                        vllm_backend.on_compilation_complete = (\n+                            self.save_aot_compiled_function\n+                        )\n+                    else:\n+                        # can't find backend...\n+                        # save immediately instead of waiting for warmup\n+                        logger.warning(\n+                            \"Could not find VllmBackend to register \"\n+                            \"on_compilation_complete callback. Saving AOT \"\n+                            \"compiled function immediately.\"\n+                        )\n+                        self.save_aot_compiled_function()\n", "materials": ["should this raise an error instead?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -320,7 +320,7 @@ def __init__(\n             return\n \n         self._check_shape_invariants = shape_invariants\n-\n+        self.was_aot_compile_fn_loaded_from_disk = False\n         compilation_counter.num_models_seen += 1\n         self.compiled = False\n \n@@ -417,9 +417,9 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n             serialized backend artifacts), then we need to generate a new AOT\n             compile artifact from scratch.\n             \"\"\"\n-            from .caching import compilation_config_hash_factors\n+            from .caching import aot_compile_hash_factors\n \n-            factors: list[str] = compilation_config_hash_factors(self.vllm_config)\n+            factors: list[str] = aot_compile_hash_factors(self.vllm_config)\n \n             factors.append(_model_hash_key(self.forward))\n             hash_key = hashlib.sha256(str(factors).encode()).hexdigest()\n@@ -446,6 +446,7 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n                 if not self.compilation_config.dynamic_shapes_config.evaluate_guards:\n                     loaded_fn.disable_guard_check()\n                 self.aot_compiled_fn = loaded_fn\n+                self.was_aot_compile_fn_loaded_from_disk = True\n             except Exception as e:\n                 if os.path.exists(aot_compilation_path):\n                     logger.warning(\n@@ -547,26 +548,45 @@ def patched_inline_call(self_: Any) -> Any:\n                 logger.warning(\"Detected eager backend, disabling AOT compile.\")\n                 use_aot_compile = False\n             if use_aot_compile:\n-                self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n-                output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                from vllm.compilation.backends import set_on_compilation_complete\n+\n+                # store the path for saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                # set callback in context so it's available when compilation completes\n+                with set_on_compilation_complete(self.save_aot_compiled_function):\n+                    self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n+                    output = self.aot_compiled_fn(self, *args, **kwargs)\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    # triggers VllmSerializableFunction.serialize()\n+    def save_aot_compiled_function(self):\n+        if self.was_aot_compile_fn_loaded_from_disk:\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        assert (\n+            self.aot_compiled_fn and self._aot_compilation_path and self._aot_cache_dir\n+        )\n+\n+        logger.info(\"saving AOT compiled function to %s\", self._aot_compilation_path)\n+        try:\n+            os.makedirs(self._aot_cache_dir, exist_ok=True)\n+            self.aot_compiled_fn.save_compiled_function(self._aot_compilation_path)\n+            logger.info(\"saved AOT compiled function to %s\", self._aot_compilation_path)\n+        except Exception as e:\n+            logger.warning(\n+                \"unable to save AOT compiled function to %s: %s\",\n+                self._aot_compilation_path,\n+                e,\n+            )\n+\n     cls.__call__ = __call__\n+    cls.save_aot_compiled_function = save_aot_compiled_function\n     return cls\n \n ", "patch_sha256": "da871cc06d969b66"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2690936335", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/decorators.py\nReview Comment:\nIs this function actually being used anywhere (aside from the tests?)\n\nIf it's only being used in the tests:\n1) I don't think we need the hasattr calls, we could just do assertions? (the hasattr makes it really difficult to reason about what is going on)\n2) document it as only being used in the tests\n\nOriginal Code Context:\n@@ -44,6 +44,42 @@\n \n IGNORE_COMPILE_KEY = \"_ignore_compile_vllm\"\n \n+\n+def _find_vllm_backend(aot_compiled_fn):\n+    \"\"\"Find the VllmBackend instance from an AOT compiled function.\n+\n+    Returns None if not found.\n+    \"\"\"\n+    if not hasattr(aot_compiled_fn, \"_artifacts\"):\n+        return None\n+\n+    artifacts = aot_compiled_fn._artifacts\n+    if not hasattr(artifacts, \"compiled_fn\"):\n+        return None\n+\n+    compiled_fn = artifacts.compiled_fn\n+\n+    # Access vllm_backend directly from VllmSerializableFunction\n+    if hasattr(compiled_fn, \"vllm_backend\") and compiled_fn.vllm_backend is not None:\n+        return compiled_fn.vllm_backend\n+\n+    return None\n+\n+\n+def save_compile_cache(compiled_module: torch.nn.Module) -> None:\n+    # Save the AOT compiled function artifacts\n+    if hasattr(compiled_module, \"save_aot_compiled_function\"):\n+        compiled_module.save_aot_compiled_function()\n+\n+    # Save the compiler manager cache (vllm_compile_cache.py)\n+    if not hasattr(compiled_module, \"aot_compiled_fn\"):\n+        return\n+\n+    vllm_backend = _find_vllm_backend(compiled_module.aot_compiled_fn)\n+    if vllm_backend is not None and hasattr(vllm_backend, \"compiler_manager\"):\n+        vllm_backend.compiler_manager.save_to_file()\n", "materials": ["Is this function actually being used anywhere (aside from the tests?)\n\nIf it's only being used in the tests:\n1) I don't think we need the hasattr calls, we could just do assertions? (the hasattr makes it really difficult to reason about what is going on)\n2) document it as only being used in the tests"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -320,7 +320,7 @@ def __init__(\n             return\n \n         self._check_shape_invariants = shape_invariants\n-\n+        self.was_aot_compile_fn_loaded_from_disk = False\n         compilation_counter.num_models_seen += 1\n         self.compiled = False\n \n@@ -417,9 +417,9 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n             serialized backend artifacts), then we need to generate a new AOT\n             compile artifact from scratch.\n             \"\"\"\n-            from .caching import compilation_config_hash_factors\n+            from .caching import aot_compile_hash_factors\n \n-            factors: list[str] = compilation_config_hash_factors(self.vllm_config)\n+            factors: list[str] = aot_compile_hash_factors(self.vllm_config)\n \n             factors.append(_model_hash_key(self.forward))\n             hash_key = hashlib.sha256(str(factors).encode()).hexdigest()\n@@ -446,6 +446,7 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n                 if not self.compilation_config.dynamic_shapes_config.evaluate_guards:\n                     loaded_fn.disable_guard_check()\n                 self.aot_compiled_fn = loaded_fn\n+                self.was_aot_compile_fn_loaded_from_disk = True\n             except Exception as e:\n                 if os.path.exists(aot_compilation_path):\n                     logger.warning(\n@@ -547,26 +548,45 @@ def patched_inline_call(self_: Any) -> Any:\n                 logger.warning(\"Detected eager backend, disabling AOT compile.\")\n                 use_aot_compile = False\n             if use_aot_compile:\n-                self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n-                output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                from vllm.compilation.backends import set_on_compilation_complete\n+\n+                # store the path for saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                # set callback in context so it's available when compilation completes\n+                with set_on_compilation_complete(self.save_aot_compiled_function):\n+                    self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n+                    output = self.aot_compiled_fn(self, *args, **kwargs)\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    # triggers VllmSerializableFunction.serialize()\n+    def save_aot_compiled_function(self):\n+        if self.was_aot_compile_fn_loaded_from_disk:\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        assert (\n+            self.aot_compiled_fn and self._aot_compilation_path and self._aot_cache_dir\n+        )\n+\n+        logger.info(\"saving AOT compiled function to %s\", self._aot_compilation_path)\n+        try:\n+            os.makedirs(self._aot_cache_dir, exist_ok=True)\n+            self.aot_compiled_fn.save_compiled_function(self._aot_compilation_path)\n+            logger.info(\"saved AOT compiled function to %s\", self._aot_compilation_path)\n+        except Exception as e:\n+            logger.warning(\n+                \"unable to save AOT compiled function to %s: %s\",\n+                self._aot_compilation_path,\n+                e,\n+            )\n+\n     cls.__call__ = __call__\n+    cls.save_aot_compiled_function = save_aot_compiled_function\n     return cls\n \n ", "patch_sha256": "da871cc06d969b66"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2690949841", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/decorators.py\nReview Comment:\nnit: `was_aot_compile_fn_loaded_from_disk`, just having \"aot_compile\" in the name was confusing. Also we sometime say \"precompile\" is not \"caching\" so I want to make it clear\n\nOriginal Code Context:\n@@ -440,6 +476,7 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n                 if not self.compilation_config.dynamic_shapes_config.evaluate_guards:\n                     loaded_fn.disable_guard_check()\n                 self.aot_compiled_fn = loaded_fn\n+                self.aot_compile_loaded_from_cache = True\n", "materials": ["nit: `was_aot_compile_fn_loaded_from_disk`, just having \"aot_compile\" in the name was confusing. Also we sometime say \"precompile\" is not \"caching\" so I want to make it clear"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -320,7 +320,7 @@ def __init__(\n             return\n \n         self._check_shape_invariants = shape_invariants\n-\n+        self.was_aot_compile_fn_loaded_from_disk = False\n         compilation_counter.num_models_seen += 1\n         self.compiled = False\n \n@@ -417,9 +417,9 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n             serialized backend artifacts), then we need to generate a new AOT\n             compile artifact from scratch.\n             \"\"\"\n-            from .caching import compilation_config_hash_factors\n+            from .caching import aot_compile_hash_factors\n \n-            factors: list[str] = compilation_config_hash_factors(self.vllm_config)\n+            factors: list[str] = aot_compile_hash_factors(self.vllm_config)\n \n             factors.append(_model_hash_key(self.forward))\n             hash_key = hashlib.sha256(str(factors).encode()).hexdigest()\n@@ -446,6 +446,7 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n                 if not self.compilation_config.dynamic_shapes_config.evaluate_guards:\n                     loaded_fn.disable_guard_check()\n                 self.aot_compiled_fn = loaded_fn\n+                self.was_aot_compile_fn_loaded_from_disk = True\n             except Exception as e:\n                 if os.path.exists(aot_compilation_path):\n                     logger.warning(\n@@ -547,26 +548,45 @@ def patched_inline_call(self_: Any) -> Any:\n                 logger.warning(\"Detected eager backend, disabling AOT compile.\")\n                 use_aot_compile = False\n             if use_aot_compile:\n-                self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n-                output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                from vllm.compilation.backends import set_on_compilation_complete\n+\n+                # store the path for saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                # set callback in context so it's available when compilation completes\n+                with set_on_compilation_complete(self.save_aot_compiled_function):\n+                    self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n+                    output = self.aot_compiled_fn(self, *args, **kwargs)\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    # triggers VllmSerializableFunction.serialize()\n+    def save_aot_compiled_function(self):\n+        if self.was_aot_compile_fn_loaded_from_disk:\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        assert (\n+            self.aot_compiled_fn and self._aot_compilation_path and self._aot_cache_dir\n+        )\n+\n+        logger.info(\"saving AOT compiled function to %s\", self._aot_compilation_path)\n+        try:\n+            os.makedirs(self._aot_cache_dir, exist_ok=True)\n+            self.aot_compiled_fn.save_compiled_function(self._aot_compilation_path)\n+            logger.info(\"saved AOT compiled function to %s\", self._aot_compilation_path)\n+        except Exception as e:\n+            logger.warning(\n+                \"unable to save AOT compiled function to %s: %s\",\n+                self._aot_compilation_path,\n+                e,\n+            )\n+\n     cls.__call__ = __call__\n+    cls.save_aot_compiled_function = save_aot_compiled_function\n     return cls\n \n ", "patch_sha256": "da871cc06d969b66"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2690960651", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/decorators.py\nReview Comment:\nI would prefer you build a helper class here that is AOTCompiledFunctionSaver that explicitly takes aot_compilation_path and a cache_dir as an input, and then set the `vllm_backend.on_compilation_complete = aot_compiled_function_saver_instance` This avoids the `getattr(self, \"aot_compile_loaded_from_cache\", False):`. In general the getattr/setattr stuff is really difficult to reason about.\n\nOriginal Code Context:\n@@ -543,24 +580,67 @@ def patched_inline_call(self_: Any) -> Any:\n             if use_aot_compile:\n                 self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n                 output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                # Store the path for later saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n", "materials": ["I would prefer you build a helper class here that is AOTCompiledFunctionSaver that explicitly takes aot_compilation_path and a cache_dir as an input, and then set the `vllm_backend.on_compilation_complete = aot_compiled_function_saver_instance` This avoids the `getattr(self, \"aot_compile_loaded_from_cache\", False):`. In general the getattr/setattr stuff is really difficult to reason about."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -320,7 +320,7 @@ def __init__(\n             return\n \n         self._check_shape_invariants = shape_invariants\n-\n+        self.was_aot_compile_fn_loaded_from_disk = False\n         compilation_counter.num_models_seen += 1\n         self.compiled = False\n \n@@ -417,9 +417,9 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n             serialized backend artifacts), then we need to generate a new AOT\n             compile artifact from scratch.\n             \"\"\"\n-            from .caching import compilation_config_hash_factors\n+            from .caching import aot_compile_hash_factors\n \n-            factors: list[str] = compilation_config_hash_factors(self.vllm_config)\n+            factors: list[str] = aot_compile_hash_factors(self.vllm_config)\n \n             factors.append(_model_hash_key(self.forward))\n             hash_key = hashlib.sha256(str(factors).encode()).hexdigest()\n@@ -446,6 +446,7 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n                 if not self.compilation_config.dynamic_shapes_config.evaluate_guards:\n                     loaded_fn.disable_guard_check()\n                 self.aot_compiled_fn = loaded_fn\n+                self.was_aot_compile_fn_loaded_from_disk = True\n             except Exception as e:\n                 if os.path.exists(aot_compilation_path):\n                     logger.warning(\n@@ -547,26 +548,45 @@ def patched_inline_call(self_: Any) -> Any:\n                 logger.warning(\"Detected eager backend, disabling AOT compile.\")\n                 use_aot_compile = False\n             if use_aot_compile:\n-                self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n-                output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                from vllm.compilation.backends import set_on_compilation_complete\n+\n+                # store the path for saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                # set callback in context so it's available when compilation completes\n+                with set_on_compilation_complete(self.save_aot_compiled_function):\n+                    self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n+                    output = self.aot_compiled_fn(self, *args, **kwargs)\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    # triggers VllmSerializableFunction.serialize()\n+    def save_aot_compiled_function(self):\n+        if self.was_aot_compile_fn_loaded_from_disk:\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        assert (\n+            self.aot_compiled_fn and self._aot_compilation_path and self._aot_cache_dir\n+        )\n+\n+        logger.info(\"saving AOT compiled function to %s\", self._aot_compilation_path)\n+        try:\n+            os.makedirs(self._aot_cache_dir, exist_ok=True)\n+            self.aot_compiled_fn.save_compiled_function(self._aot_compilation_path)\n+            logger.info(\"saved AOT compiled function to %s\", self._aot_compilation_path)\n+        except Exception as e:\n+            logger.warning(\n+                \"unable to save AOT compiled function to %s: %s\",\n+                self._aot_compilation_path,\n+                e,\n+            )\n+\n     cls.__call__ = __call__\n+    cls.save_aot_compiled_function = save_aot_compiled_function\n     return cls\n \n ", "patch_sha256": "da871cc06d969b66"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2690967920", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/decorators.py\nReview Comment:\nIt's not clear to me when we are unable to find a VllmBackend, we should make it so that it's always possible to find the VllmBackend.\n\nFor example, we could just have some thread-local state that sets the \"on compilation callback\" for the duration of the `__call__` and have VllmBackend read from it. That would definitely always work\n\nOriginal Code Context:\n@@ -543,24 +580,67 @@ def patched_inline_call(self_: Any) -> Any:\n             if use_aot_compile:\n                 self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n                 output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                # Store the path for later saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                if not self.vllm_config.compilation_config.compile_sizes:\n+                    self.save_aot_compiled_function()\n+                else:\n+                    # this will save after the last compile_size is warmed-up\n+                    vllm_backend = _find_vllm_backend(self.aot_compiled_fn)\n+                    if vllm_backend is not None:\n+                        vllm_backend.on_compilation_complete = (\n+                            self.save_aot_compiled_function\n+                        )\n+                    else:\n+                        # can't find backend...\n+                        # save immediately instead of waiting for warmup\n+                        logger.warning(\n+                            \"Could not find VllmBackend to register \"\n+                            \"on_compilation_complete callback. Saving AOT \"\n+                            \"compiled function immediately.\"\n+                        )\n+                        self.save_aot_compiled_function()\n", "materials": ["It's not clear to me when we are unable to find a VllmBackend, we should make it so that it's always possible to find the VllmBackend.\n\nFor example, we could just have some thread-local state that sets the \"on compilation callback\" for the duration of the `__call__` and have VllmBackend read from it. That would definitely always work"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -320,7 +320,7 @@ def __init__(\n             return\n \n         self._check_shape_invariants = shape_invariants\n-\n+        self.was_aot_compile_fn_loaded_from_disk = False\n         compilation_counter.num_models_seen += 1\n         self.compiled = False\n \n@@ -417,9 +417,9 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n             serialized backend artifacts), then we need to generate a new AOT\n             compile artifact from scratch.\n             \"\"\"\n-            from .caching import compilation_config_hash_factors\n+            from .caching import aot_compile_hash_factors\n \n-            factors: list[str] = compilation_config_hash_factors(self.vllm_config)\n+            factors: list[str] = aot_compile_hash_factors(self.vllm_config)\n \n             factors.append(_model_hash_key(self.forward))\n             hash_key = hashlib.sha256(str(factors).encode()).hexdigest()\n@@ -446,6 +446,7 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n                 if not self.compilation_config.dynamic_shapes_config.evaluate_guards:\n                     loaded_fn.disable_guard_check()\n                 self.aot_compiled_fn = loaded_fn\n+                self.was_aot_compile_fn_loaded_from_disk = True\n             except Exception as e:\n                 if os.path.exists(aot_compilation_path):\n                     logger.warning(\n@@ -547,26 +548,45 @@ def patched_inline_call(self_: Any) -> Any:\n                 logger.warning(\"Detected eager backend, disabling AOT compile.\")\n                 use_aot_compile = False\n             if use_aot_compile:\n-                self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n-                output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                from vllm.compilation.backends import set_on_compilation_complete\n+\n+                # store the path for saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                # set callback in context so it's available when compilation completes\n+                with set_on_compilation_complete(self.save_aot_compiled_function):\n+                    self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n+                    output = self.aot_compiled_fn(self, *args, **kwargs)\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    # triggers VllmSerializableFunction.serialize()\n+    def save_aot_compiled_function(self):\n+        if self.was_aot_compile_fn_loaded_from_disk:\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        assert (\n+            self.aot_compiled_fn and self._aot_compilation_path and self._aot_cache_dir\n+        )\n+\n+        logger.info(\"saving AOT compiled function to %s\", self._aot_compilation_path)\n+        try:\n+            os.makedirs(self._aot_cache_dir, exist_ok=True)\n+            self.aot_compiled_fn.save_compiled_function(self._aot_compilation_path)\n+            logger.info(\"saved AOT compiled function to %s\", self._aot_compilation_path)\n+        except Exception as e:\n+            logger.warning(\n+                \"unable to save AOT compiled function to %s: %s\",\n+                self._aot_compilation_path,\n+                e,\n+            )\n+\n     cls.__call__ = __call__\n+    cls.save_aot_compiled_function = save_aot_compiled_function\n     return cls\n \n ", "patch_sha256": "da871cc06d969b66"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2690971542", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/decorators.py\nReview Comment:\nthis return should never trigger right? We shouldn't attach the callback if VLLM_USE_AOT_COMPILE is False. So this is better as an assertion\n\nOriginal Code Context:\n@@ -543,24 +580,67 @@ def patched_inline_call(self_: Any) -> Any:\n             if use_aot_compile:\n                 self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n                 output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                # Store the path for later saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                if not self.vllm_config.compilation_config.compile_sizes:\n+                    self.save_aot_compiled_function()\n+                else:\n+                    # this will save after the last compile_size is warmed-up\n+                    vllm_backend = _find_vllm_backend(self.aot_compiled_fn)\n+                    if vllm_backend is not None:\n+                        vllm_backend.on_compilation_complete = (\n+                            self.save_aot_compiled_function\n+                        )\n+                    else:\n+                        # can't find backend...\n+                        # save immediately instead of waiting for warmup\n+                        logger.warning(\n+                            \"Could not find VllmBackend to register \"\n+                            \"on_compilation_complete callback. Saving AOT \"\n+                            \"compiled function immediately.\"\n+                        )\n+                        self.save_aot_compiled_function()\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    def save_aot_compiled_function(self):\n+        \"\"\"Save the AOT compiled function after warmup is complete.\"\"\"\n+        if not envs.VLLM_USE_AOT_COMPILE:\n+            return\n", "materials": ["this return should never trigger right? We shouldn't attach the callback if VLLM_USE_AOT_COMPILE is False. So this is better as an assertion"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -320,7 +320,7 @@ def __init__(\n             return\n \n         self._check_shape_invariants = shape_invariants\n-\n+        self.was_aot_compile_fn_loaded_from_disk = False\n         compilation_counter.num_models_seen += 1\n         self.compiled = False\n \n@@ -417,9 +417,9 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n             serialized backend artifacts), then we need to generate a new AOT\n             compile artifact from scratch.\n             \"\"\"\n-            from .caching import compilation_config_hash_factors\n+            from .caching import aot_compile_hash_factors\n \n-            factors: list[str] = compilation_config_hash_factors(self.vllm_config)\n+            factors: list[str] = aot_compile_hash_factors(self.vllm_config)\n \n             factors.append(_model_hash_key(self.forward))\n             hash_key = hashlib.sha256(str(factors).encode()).hexdigest()\n@@ -446,6 +446,7 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n                 if not self.compilation_config.dynamic_shapes_config.evaluate_guards:\n                     loaded_fn.disable_guard_check()\n                 self.aot_compiled_fn = loaded_fn\n+                self.was_aot_compile_fn_loaded_from_disk = True\n             except Exception as e:\n                 if os.path.exists(aot_compilation_path):\n                     logger.warning(\n@@ -547,26 +548,45 @@ def patched_inline_call(self_: Any) -> Any:\n                 logger.warning(\"Detected eager backend, disabling AOT compile.\")\n                 use_aot_compile = False\n             if use_aot_compile:\n-                self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n-                output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                from vllm.compilation.backends import set_on_compilation_complete\n+\n+                # store the path for saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                # set callback in context so it's available when compilation completes\n+                with set_on_compilation_complete(self.save_aot_compiled_function):\n+                    self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n+                    output = self.aot_compiled_fn(self, *args, **kwargs)\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    # triggers VllmSerializableFunction.serialize()\n+    def save_aot_compiled_function(self):\n+        if self.was_aot_compile_fn_loaded_from_disk:\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        assert (\n+            self.aot_compiled_fn and self._aot_compilation_path and self._aot_cache_dir\n+        )\n+\n+        logger.info(\"saving AOT compiled function to %s\", self._aot_compilation_path)\n+        try:\n+            os.makedirs(self._aot_cache_dir, exist_ok=True)\n+            self.aot_compiled_fn.save_compiled_function(self._aot_compilation_path)\n+            logger.info(\"saved AOT compiled function to %s\", self._aot_compilation_path)\n+        except Exception as e:\n+            logger.warning(\n+                \"unable to save AOT compiled function to %s: %s\",\n+                self._aot_compilation_path,\n+                e,\n+            )\n+\n     cls.__call__ = __call__\n+    cls.save_aot_compiled_function = save_aot_compiled_function\n     return cls\n \n ", "patch_sha256": "da871cc06d969b66"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2690995441", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/decorators.py\nReview Comment:\nthe contract between pytorch and vllm isn't very clear (aot_compiled_fn is a pytorch thing right?) I'd leave a comment here that this triggers VllmSerializableFunction.serialize() or whatever it is\n\nOriginal Code Context:\n@@ -543,24 +580,67 @@ def patched_inline_call(self_: Any) -> Any:\n             if use_aot_compile:\n                 self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n                 output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                # Store the path for later saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                if not self.vllm_config.compilation_config.compile_sizes:\n+                    self.save_aot_compiled_function()\n+                else:\n+                    # this will save after the last compile_size is warmed-up\n+                    vllm_backend = _find_vllm_backend(self.aot_compiled_fn)\n+                    if vllm_backend is not None:\n+                        vllm_backend.on_compilation_complete = (\n+                            self.save_aot_compiled_function\n+                        )\n+                    else:\n+                        # can't find backend...\n+                        # save immediately instead of waiting for warmup\n+                        logger.warning(\n+                            \"Could not find VllmBackend to register \"\n+                            \"on_compilation_complete callback. Saving AOT \"\n+                            \"compiled function immediately.\"\n+                        )\n+                        self.save_aot_compiled_function()\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    def save_aot_compiled_function(self):\n+        \"\"\"Save the AOT compiled function after warmup is complete.\"\"\"\n+        if not envs.VLLM_USE_AOT_COMPILE:\n+            return\n+\n+        if getattr(self, \"aot_compiled_fn\", None) is None:\n+            logger.debug(\"No AOT compiled function to save\")\n+            return\n+\n+        if getattr(self, \"aot_compile_loaded_from_cache\", False):\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        aot_compilation_path = getattr(self, \"_aot_compilation_path\", None)\n+        cache_dir = getattr(self, \"_aot_cache_dir\", None)\n+\n+        if aot_compilation_path is None or cache_dir is None:\n+            logger.debug(\"No AOT compilation path found, skipping save\")\n+            return\n+\n+        logger.info(\"Saving AOT compiled function to %s\", aot_compilation_path)\n+        try:\n+            os.makedirs(cache_dir, exist_ok=True)\n+            self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n", "materials": ["the contract between pytorch and vllm isn't very clear (aot_compiled_fn is a pytorch thing right?) I'd leave a comment here that this triggers VllmSerializableFunction.serialize() or whatever it is"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -320,7 +320,7 @@ def __init__(\n             return\n \n         self._check_shape_invariants = shape_invariants\n-\n+        self.was_aot_compile_fn_loaded_from_disk = False\n         compilation_counter.num_models_seen += 1\n         self.compiled = False\n \n@@ -417,9 +417,9 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n             serialized backend artifacts), then we need to generate a new AOT\n             compile artifact from scratch.\n             \"\"\"\n-            from .caching import compilation_config_hash_factors\n+            from .caching import aot_compile_hash_factors\n \n-            factors: list[str] = compilation_config_hash_factors(self.vllm_config)\n+            factors: list[str] = aot_compile_hash_factors(self.vllm_config)\n \n             factors.append(_model_hash_key(self.forward))\n             hash_key = hashlib.sha256(str(factors).encode()).hexdigest()\n@@ -446,6 +446,7 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n                 if not self.compilation_config.dynamic_shapes_config.evaluate_guards:\n                     loaded_fn.disable_guard_check()\n                 self.aot_compiled_fn = loaded_fn\n+                self.was_aot_compile_fn_loaded_from_disk = True\n             except Exception as e:\n                 if os.path.exists(aot_compilation_path):\n                     logger.warning(\n@@ -547,26 +548,45 @@ def patched_inline_call(self_: Any) -> Any:\n                 logger.warning(\"Detected eager backend, disabling AOT compile.\")\n                 use_aot_compile = False\n             if use_aot_compile:\n-                self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n-                output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                from vllm.compilation.backends import set_on_compilation_complete\n+\n+                # store the path for saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                # set callback in context so it's available when compilation completes\n+                with set_on_compilation_complete(self.save_aot_compiled_function):\n+                    self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n+                    output = self.aot_compiled_fn(self, *args, **kwargs)\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    # triggers VllmSerializableFunction.serialize()\n+    def save_aot_compiled_function(self):\n+        if self.was_aot_compile_fn_loaded_from_disk:\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        assert (\n+            self.aot_compiled_fn and self._aot_compilation_path and self._aot_cache_dir\n+        )\n+\n+        logger.info(\"saving AOT compiled function to %s\", self._aot_compilation_path)\n+        try:\n+            os.makedirs(self._aot_cache_dir, exist_ok=True)\n+            self.aot_compiled_fn.save_compiled_function(self._aot_compilation_path)\n+            logger.info(\"saved AOT compiled function to %s\", self._aot_compilation_path)\n+        except Exception as e:\n+            logger.warning(\n+                \"unable to save AOT compiled function to %s: %s\",\n+                self._aot_compilation_path,\n+                e,\n+            )\n+\n     cls.__call__ = __call__\n+    cls.save_aot_compiled_function = save_aot_compiled_function\n     return cls\n \n ", "patch_sha256": "da871cc06d969b66"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2691207837", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/compiler_interface.py\nReview Comment:\ncan you explain what the aot is doing and why it's needed? I didn't think it was necessary, standalone_compile already produces artifacts that can be serialized. Also should go into a code comment somewhere (because it's not clear what the contract between pytorch and vLLM is)\n\nOriginal Code Context:\n@@ -230,12 +233,42 @@ def compile(\n \n         from torch._inductor import standalone_compile\n \n-        compiled_graph = standalone_compile(\n-            graph,\n-            example_inputs,\n-            dynamic_shapes=dynamic_shapes,\n-            options={\"config_patches\": current_config},\n+        supports_aot = is_torch_equal_or_newer(\"2.10.0.dev\")\n+\n+        if not supports_aot and envs.VLLM_USE_BACKEND_WITH_INDUCTOR_COMPILED_ARTIFACTS:\n+            logger.error(\n+                \"CRITICAL: VLLM_USE_BACKEND_WITH_INDUCTOR_COMPILED_ARTIFACTS \"\n+                \"is enabled but PyTorch version does not support 'aot' \"\n+                \"parameter in standalone_compile. This requires PyTorch \"\n+                \"2.10.0+. Falling back to non-AOT mode.\"\n+            )\n+\n+        compile_kwargs = {\n+            \"dynamic_shapes\": dynamic_shapes,\n+            \"options\": {\n+                \"config_patches\": current_config,\n+            },\n+        }\n+\n+        use_aot: bool = (\n+            supports_aot and envs.VLLM_USE_BACKEND_WITH_INDUCTOR_COMPILED_ARTIFACTS\n         )\n+        # Only add 'aot' parameter if both supported and enabled\n+        if use_aot:\n+            compile_kwargs[\"aot\"] = True  # type: ignore[assignment]\n+\n+        compiled_graph = standalone_compile(graph, example_inputs, **compile_kwargs)\n", "materials": ["can you explain what the aot is doing and why it's needed? I didn't think it was necessary, standalone_compile already produces artifacts that can be serialized. Also should go into a code comment somewhere (because it's not clear what the contract between pytorch and vLLM is)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -16,9 +16,12 @@\n from vllm.compilation.counter import compilation_counter\n from vllm.config import VllmConfig\n from vllm.config.utils import Range\n+from vllm.logger import init_logger\n from vllm.utils.hashing import safe_hash\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n \n+logger = init_logger(__name__)\n+\n \n class CompilerInterface:\n     \"\"\"\n@@ -230,12 +233,42 @@ def compile(\n \n         from torch._inductor import standalone_compile\n \n-        compiled_graph = standalone_compile(\n-            graph,\n-            example_inputs,\n-            dynamic_shapes=dynamic_shapes,\n-            options={\"config_patches\": current_config},\n-        )\n+        supports_aot = is_torch_equal_or_newer(\"2.10.0.dev\")\n+\n+        if not supports_aot and envs.VLLM_USE_MEGA_AOT_ARTIFACT:\n+            logger.error(\n+                \"CRITICAL: VLLM_USE_MEGA_AOT_ARTIFACT \"\n+                \"is enabled but PyTorch version does not support 'aot' \"\n+                \"parameter in standalone_compile. This requires PyTorch \"\n+                \"2.10.0+. Falling back to non-AOT mode.\"\n+            )\n+\n+        compile_kwargs = {\n+            \"dynamic_shapes\": dynamic_shapes,\n+            \"options\": {\n+                \"config_patches\": current_config,\n+            },\n+        }\n+\n+        use_aot: bool = supports_aot and envs.VLLM_USE_MEGA_AOT_ARTIFACT\n+        # only add 'aot' parameter if both supported and enabled...\n+        # this will set bundled_autograd_cache\n+        # https://github.com/pytorch/pytorch/blob/9bbc5b2905c260adf41bc866a732f9c121a2828a/torch/_inductor/standalone_compile.py#L359 # noqa\n+        if use_aot:\n+            compile_kwargs[\"aot\"] = True  # type: ignore[assignment]\n+\n+        compiled_graph = standalone_compile(graph, example_inputs, **compile_kwargs)\n+\n+        if use_aot:\n+            from torch._inductor.standalone_compile import AOTCompiledArtifact\n+\n+            assert isinstance(compiled_graph, AOTCompiledArtifact)\n+            assert hasattr(compiled_graph, \"serialize\")\n+            # just return the compiled graph and a key\n+            # since we can serialize the bytes using to_bytes\n+            # and reload it using the key when reading\n+            return compiled_graph, None\n+\n         # Save the compiled artifact to disk in the specified path\n         assert key is not None\n         path = os.path.join(self.cache_dir, key)\n@@ -619,7 +652,8 @@ def set_inductor_config(config: dict[str, Any], compile_range: Range) -> None:\n \n \n def set_functorch_config() -> None:\n-    torch._functorch.config.bundled_autograd_cache = False\n+    if not envs.VLLM_USE_MEGA_AOT_ARTIFACT:\n+        torch._functorch.config.bundled_autograd_cache = False\n \n \n class EagerAdaptor(CompilerInterface):", "patch_sha256": "59077149ae92a623"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2691409593", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/decorators.py\nReview Comment:\nyeah, this is much better!\n\nOriginal Code Context:\n@@ -543,24 +580,67 @@ def patched_inline_call(self_: Any) -> Any:\n             if use_aot_compile:\n                 self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n                 output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                # Store the path for later saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                if not self.vllm_config.compilation_config.compile_sizes:\n+                    self.save_aot_compiled_function()\n+                else:\n+                    # this will save after the last compile_size is warmed-up\n+                    vllm_backend = _find_vllm_backend(self.aot_compiled_fn)\n+                    if vllm_backend is not None:\n+                        vllm_backend.on_compilation_complete = (\n+                            self.save_aot_compiled_function\n+                        )\n+                    else:\n+                        # can't find backend...\n+                        # save immediately instead of waiting for warmup\n+                        logger.warning(\n+                            \"Could not find VllmBackend to register \"\n+                            \"on_compilation_complete callback. Saving AOT \"\n+                            \"compiled function immediately.\"\n+                        )\n+                        self.save_aot_compiled_function()\n", "materials": ["yeah, this is much better!"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -320,7 +320,7 @@ def __init__(\n             return\n \n         self._check_shape_invariants = shape_invariants\n-\n+        self.was_aot_compile_fn_loaded_from_disk = False\n         compilation_counter.num_models_seen += 1\n         self.compiled = False\n \n@@ -417,9 +417,9 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n             serialized backend artifacts), then we need to generate a new AOT\n             compile artifact from scratch.\n             \"\"\"\n-            from .caching import compilation_config_hash_factors\n+            from .caching import aot_compile_hash_factors\n \n-            factors: list[str] = compilation_config_hash_factors(self.vllm_config)\n+            factors: list[str] = aot_compile_hash_factors(self.vllm_config)\n \n             factors.append(_model_hash_key(self.forward))\n             hash_key = hashlib.sha256(str(factors).encode()).hexdigest()\n@@ -446,6 +446,7 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n                 if not self.compilation_config.dynamic_shapes_config.evaluate_guards:\n                     loaded_fn.disable_guard_check()\n                 self.aot_compiled_fn = loaded_fn\n+                self.was_aot_compile_fn_loaded_from_disk = True\n             except Exception as e:\n                 if os.path.exists(aot_compilation_path):\n                     logger.warning(\n@@ -547,26 +548,45 @@ def patched_inline_call(self_: Any) -> Any:\n                 logger.warning(\"Detected eager backend, disabling AOT compile.\")\n                 use_aot_compile = False\n             if use_aot_compile:\n-                self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n-                output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                from vllm.compilation.backends import set_on_compilation_complete\n+\n+                # store the path for saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                # set callback in context so it's available when compilation completes\n+                with set_on_compilation_complete(self.save_aot_compiled_function):\n+                    self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n+                    output = self.aot_compiled_fn(self, *args, **kwargs)\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    # triggers VllmSerializableFunction.serialize()\n+    def save_aot_compiled_function(self):\n+        if self.was_aot_compile_fn_loaded_from_disk:\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        assert (\n+            self.aot_compiled_fn and self._aot_compilation_path and self._aot_cache_dir\n+        )\n+\n+        logger.info(\"saving AOT compiled function to %s\", self._aot_compilation_path)\n+        try:\n+            os.makedirs(self._aot_cache_dir, exist_ok=True)\n+            self.aot_compiled_fn.save_compiled_function(self._aot_compilation_path)\n+            logger.info(\"saved AOT compiled function to %s\", self._aot_compilation_path)\n+        except Exception as e:\n+            logger.warning(\n+                \"unable to save AOT compiled function to %s: %s\",\n+                self._aot_compilation_path,\n+                e,\n+            )\n+\n     cls.__call__ = __call__\n+    cls.save_aot_compiled_function = save_aot_compiled_function\n     return cls\n \n ", "patch_sha256": "da871cc06d969b66"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2691708397", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/decorators.py\nReview Comment:\nwe can remove after https://github.com/vllm-project/vllm/pull/32343 (or at least move it to the test file)\n\nOriginal Code Context:\n@@ -44,6 +44,42 @@\n \n IGNORE_COMPILE_KEY = \"_ignore_compile_vllm\"\n \n+\n+def _find_vllm_backend(aot_compiled_fn):\n", "materials": ["we can remove after https://github.com/vllm-project/vllm/pull/32343 (or at least move it to the test file)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -320,7 +320,7 @@ def __init__(\n             return\n \n         self._check_shape_invariants = shape_invariants\n-\n+        self.was_aot_compile_fn_loaded_from_disk = False\n         compilation_counter.num_models_seen += 1\n         self.compiled = False\n \n@@ -417,9 +417,9 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n             serialized backend artifacts), then we need to generate a new AOT\n             compile artifact from scratch.\n             \"\"\"\n-            from .caching import compilation_config_hash_factors\n+            from .caching import aot_compile_hash_factors\n \n-            factors: list[str] = compilation_config_hash_factors(self.vllm_config)\n+            factors: list[str] = aot_compile_hash_factors(self.vllm_config)\n \n             factors.append(_model_hash_key(self.forward))\n             hash_key = hashlib.sha256(str(factors).encode()).hexdigest()\n@@ -446,6 +446,7 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n                 if not self.compilation_config.dynamic_shapes_config.evaluate_guards:\n                     loaded_fn.disable_guard_check()\n                 self.aot_compiled_fn = loaded_fn\n+                self.was_aot_compile_fn_loaded_from_disk = True\n             except Exception as e:\n                 if os.path.exists(aot_compilation_path):\n                     logger.warning(\n@@ -547,26 +548,45 @@ def patched_inline_call(self_: Any) -> Any:\n                 logger.warning(\"Detected eager backend, disabling AOT compile.\")\n                 use_aot_compile = False\n             if use_aot_compile:\n-                self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n-                output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                from vllm.compilation.backends import set_on_compilation_complete\n+\n+                # store the path for saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                # set callback in context so it's available when compilation completes\n+                with set_on_compilation_complete(self.save_aot_compiled_function):\n+                    self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n+                    output = self.aot_compiled_fn(self, *args, **kwargs)\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    # triggers VllmSerializableFunction.serialize()\n+    def save_aot_compiled_function(self):\n+        if self.was_aot_compile_fn_loaded_from_disk:\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        assert (\n+            self.aot_compiled_fn and self._aot_compilation_path and self._aot_cache_dir\n+        )\n+\n+        logger.info(\"saving AOT compiled function to %s\", self._aot_compilation_path)\n+        try:\n+            os.makedirs(self._aot_cache_dir, exist_ok=True)\n+            self.aot_compiled_fn.save_compiled_function(self._aot_compilation_path)\n+            logger.info(\"saved AOT compiled function to %s\", self._aot_compilation_path)\n+        except Exception as e:\n+            logger.warning(\n+                \"unable to save AOT compiled function to %s: %s\",\n+                self._aot_compilation_path,\n+                e,\n+            )\n+\n     cls.__call__ = __call__\n+    cls.save_aot_compiled_function = save_aot_compiled_function\n     return cls\n \n ", "patch_sha256": "da871cc06d969b66"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2691711553", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/decorators.py\nReview Comment:\nif we defer all compilations to warmup then we don't need any of this actually.\n\nOriginal Code Context:\n@@ -543,24 +580,67 @@ def patched_inline_call(self_: Any) -> Any:\n             if use_aot_compile:\n                 self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n                 output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                # Store the path for later saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                if not self.vllm_config.compilation_config.compile_sizes:\n+                    self.save_aot_compiled_function()\n+                else:\n+                    # this will save after the last compile_size is warmed-up\n+                    vllm_backend = _find_vllm_backend(self.aot_compiled_fn)\n+                    if vllm_backend is not None:\n+                        vllm_backend.on_compilation_complete = (\n+                            self.save_aot_compiled_function\n+                        )\n+                    else:\n+                        # can't find backend...\n+                        # save immediately instead of waiting for warmup\n+                        logger.warning(\n+                            \"Could not find VllmBackend to register \"\n+                            \"on_compilation_complete callback. Saving AOT \"\n+                            \"compiled function immediately.\"\n+                        )\n+                        self.save_aot_compiled_function()\n", "materials": ["if we defer all compilations to warmup then we don't need any of this actually."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -320,7 +320,7 @@ def __init__(\n             return\n \n         self._check_shape_invariants = shape_invariants\n-\n+        self.was_aot_compile_fn_loaded_from_disk = False\n         compilation_counter.num_models_seen += 1\n         self.compiled = False\n \n@@ -417,9 +417,9 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n             serialized backend artifacts), then we need to generate a new AOT\n             compile artifact from scratch.\n             \"\"\"\n-            from .caching import compilation_config_hash_factors\n+            from .caching import aot_compile_hash_factors\n \n-            factors: list[str] = compilation_config_hash_factors(self.vllm_config)\n+            factors: list[str] = aot_compile_hash_factors(self.vllm_config)\n \n             factors.append(_model_hash_key(self.forward))\n             hash_key = hashlib.sha256(str(factors).encode()).hexdigest()\n@@ -446,6 +446,7 @@ def __call__(self: _T, *args: Any, **kwargs: Any) -> Any:\n                 if not self.compilation_config.dynamic_shapes_config.evaluate_guards:\n                     loaded_fn.disable_guard_check()\n                 self.aot_compiled_fn = loaded_fn\n+                self.was_aot_compile_fn_loaded_from_disk = True\n             except Exception as e:\n                 if os.path.exists(aot_compilation_path):\n                     logger.warning(\n@@ -547,26 +548,45 @@ def patched_inline_call(self_: Any) -> Any:\n                 logger.warning(\"Detected eager backend, disabling AOT compile.\")\n                 use_aot_compile = False\n             if use_aot_compile:\n-                self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n-                output = self.aot_compiled_fn(self, *args, **kwargs)\n-                assert aot_compilation_path is not None\n-                assert cache_dir is not None\n-                try:\n-                    os.makedirs(cache_dir, exist_ok=True)\n-                    self.aot_compiled_fn.save_compiled_function(aot_compilation_path)\n-                except Exception as e:\n-                    logger.warning(\n-                        \"Cannot save aot compilation to path %s, error: %s\",\n-                        aot_compilation_path,\n-                        str(e),\n-                    )\n+                from vllm.compilation.backends import set_on_compilation_complete\n+\n+                # store the path for saving after warmup\n+                self._aot_compilation_path = aot_compilation_path\n+                self._aot_cache_dir = cache_dir\n+                # set callback in context so it's available when compilation completes\n+                with set_on_compilation_complete(self.save_aot_compiled_function):\n+                    self.aot_compiled_fn = self.aot_compile(*args, **kwargs)\n+                    output = self.aot_compiled_fn(self, *args, **kwargs)\n             else:\n                 output = TorchCompileWithNoGuardsWrapper.__call__(self, *args, **kwargs)  # type: ignore[arg-type]\n \n         self.compiled = True\n         return output\n \n+    # triggers VllmSerializableFunction.serialize()\n+    def save_aot_compiled_function(self):\n+        if self.was_aot_compile_fn_loaded_from_disk:\n+            logger.debug(\"AOT compiled function was loaded from cache, skipping save\")\n+            return\n+\n+        assert (\n+            self.aot_compiled_fn and self._aot_compilation_path and self._aot_cache_dir\n+        )\n+\n+        logger.info(\"saving AOT compiled function to %s\", self._aot_compilation_path)\n+        try:\n+            os.makedirs(self._aot_cache_dir, exist_ok=True)\n+            self.aot_compiled_fn.save_compiled_function(self._aot_compilation_path)\n+            logger.info(\"saved AOT compiled function to %s\", self._aot_compilation_path)\n+        except Exception as e:\n+            logger.warning(\n+                \"unable to save AOT compiled function to %s: %s\",\n+                self._aot_compilation_path,\n+                e,\n+            )\n+\n     cls.__call__ = __call__\n+    cls.save_aot_compiled_function = save_aot_compiled_function\n     return cls\n \n ", "patch_sha256": "da871cc06d969b66"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25205/review_2695533199", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25205", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tools/pre_commit/check_pickle_imports.py\nReview Comment:\nWe should really think about the threat model here, cc @russellb\n\nOriginal Code Context:\n@@ -21,15 +21,17 @@\n     \"vllm/transformers_utils/config.py\",\n     \"vllm/model_executor/models/registry.py\",\n     \"vllm/compilation/caching.py\",\n+    \"vllm/compilation/piecewise_backend.py\",\n", "materials": ["We should really think about the threat model here, cc @russellb"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -21,6 +21,7 @@\n     \"vllm/transformers_utils/config.py\",\n     \"vllm/model_executor/models/registry.py\",\n     \"vllm/compilation/caching.py\",\n+    \"vllm/compilation/piecewise_backend.py\",\n     \"vllm/distributed/utils.py\",\n     \"vllm/distributed/parallel_state.py\",\n     \"vllm/distributed/device_communicators/all_reduce_utils.py\",\n@@ -30,6 +31,7 @@\n     \"tests/multimodal/media/test_base.py\",\n     \"tests/tokenizers_/test_hf.py\",\n     \"tests/utils_/test_hashing.py\",\n+    \"tests/compile/test_aot_compile.py\",\n     \"benchmarks/kernels/graph_machete_bench.py\",\n     \"benchmarks/kernels/benchmark_lora.py\",\n     \"benchmarks/kernels/benchmark_machete.py\",", "patch_sha256": "a8e23980b81ebdf7"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2391907638", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\n\n```suggestion\n    if include_kv_cache_update:\n        maybe_save_kv_layer_to_connector(layer_name, kv_cache)\n```\n\nOriginal Code Context:\n@@ -616,7 +672,8 @@ def unified_attention_with_output(\n                       attn_metadata,\n                       output=output,\n                       output_scale=output_scale,\n-                      output_block_scale=output_block_scale)\n+                      output_block_scale=output_block_scale,\n+                      should_do_kv_cache_update=include_kv_cache_update)\n \n     maybe_save_kv_layer_to_connector(layer_name, kv_cache)\n", "materials": ["\n```suggestion\n    if include_kv_cache_update:\n        maybe_save_kv_layer_to_connector(layer_name, kv_cache)\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2399967795", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nThis can also directly call `unified_attention_with_output`\n\nOriginal Code Context:\n@@ -326,18 +325,29 @@ def forward(\n                                   attn_metadata,\n                                   output=output)\n", "materials": ["This can also directly call `unified_attention_with_output`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2399970414", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\n```suggestion\n    if include_kv_cache_update:\n        wait_for_kv_layer_from_connector(layer_name)\n```\n\nOriginal Code Context:\n@@ -639,7 +657,9 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: Optional[torch.Tensor] = None,\n     output_block_scale: Optional[torch.Tensor] = None,\n+    include_kv_cache_update: bool = True,\n ) -> None:\n+    # if include_kv_cache_update:\n     wait_for_kv_layer_from_connector(layer_name)\n", "materials": ["```suggestion\n    if include_kv_cache_update:\n        wait_for_kv_layer_from_connector(layer_name)\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2407491168", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\n**<sub><sub>![P0 Badge](https://img.shields.io/badge/P0-red?style=flat)</sub></sub>  Pass None when attention metadata is skipped**\n\nWhen a dummy run captures attention only for backends that split KV-cache updates (`ForceAttention.SEPARATE_KV_UPDATE_ONLY`), `GPUModelRunner` leaves `forward_context.attn_metadata` without entries for other layers. The new logic in `unified_attention_with_output` (and the similar helper functions) now just skips the dictionary lookup if the layer is missing, which means `attn_metadata` remains the dictionary itself. Backend implementations such as `FlashAttentionImpl.forward` immediately access attributes like `attn_metadata.num_actual_tokens`, so receiving a plain dict raises `AttributeError` and the warmup/cuda-graph capture aborts whenever there is any non-split backend (or when `has_separate_kv_update` is misdetected). These helpers should default to `None` (or another sentinel) when the layer key is absent so that backends can short-circuit, rather than passing the raw dict.\n\nUseful? React with 👍 / 👎.\n\nOriginal Code Context:\n@@ -685,13 +727,14 @@ def unified_attention_with_output(\n     output_scale: Optional[torch.Tensor] = None,\n     output_block_scale: Optional[torch.Tensor] = None,\n ) -> None:\n-    wait_for_kv_layer_from_connector(layer_name)\n     forward_context: ForwardContext = get_forward_context()\n     attn_metadata = forward_context.attn_metadata\n-    if isinstance(attn_metadata, dict):\n+    if isinstance(attn_metadata, dict) and layer_name in attn_metadata:\n         attn_metadata = attn_metadata[layer_name]\n     self = forward_context.no_compile_layers[layer_name]\n     kv_cache = self.kv_cache[forward_context.virtual_engine]\n+    if self.attn_backend.include_kv_cache:\n+        wait_for_kv_layer_from_connector(layer_name)\n     self.impl.forward(\n", "materials": ["**<sub><sub>![P0 Badge](https://img.shields.io/badge/P0-red?style=flat)</sub></sub>  Pass None when attention metadata is skipped**\n\nWhen a dummy run captures attention only for backends that split KV-cache updates (`ForceAttention.SEPARATE_KV_UPDATE_ONLY`), `GPUModelRunner` leaves `forward_context.attn_metadata` without entries for other layers. The new logic in `unified_attention_with_output` (and the similar helper functions) now just skips the dictionary lookup if the layer is missing, which means `attn_metadata` remains the dictionary itself. Backend implementations such as `FlashAttentionImpl.forward` immediately access attributes like `attn_metadata.num_actual_tokens`, so receiving a plain dict raises `AttributeError` and the warmup/cuda-graph capture aborts whenever there is any non-split backend (or when `has_separate_kv_update` is misdetected). These helpers should default to `None` (or another sentinel) when the layer key is absent so that backends can short-circuit, rather than passing the raw dict.\n\nUseful? React with 👍 / 👎."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2410657435", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nI think that there are times where there's only a single value of attention metadata so it's not actually per-layer. In those cases we still don't want it to be None, so I think we can simplify like this:\n```suggestion\n    if isinstance(attn_metadata, dict):\n        attn_metadata = attn_metadata.get(layer_name)\n```\n\nOriginal Code Context:\n@@ -649,8 +646,10 @@ def unified_attention(\n \n     forward_context: ForwardContext = get_forward_context()\n     attn_metadata = forward_context.attn_metadata\n-    if isinstance(attn_metadata, dict):\n+    if isinstance(attn_metadata, dict) and layer_name in attn_metadata:\n         attn_metadata = attn_metadata[layer_name]\n+    else:\n+        attn_metadata = None\n", "materials": ["I think that there are times where there's only a single value of attention metadata so it's not actually per-layer. In those cases we still don't want it to be None, so I think we can simplify like this:\n```suggestion\n    if isinstance(attn_metadata, dict):\n        attn_metadata = attn_metadata.get(layer_name)\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2410659204", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nRemove TODO (I think this is good)\n\nOriginal Code Context:\n@@ -676,6 +675,51 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    wait_for_kv_layer_from_connector(layer_name)\n+    forward_context: ForwardContext = get_forward_context()\n+    attn_metadata = forward_context.attn_metadata\n+    if isinstance(attn_metadata, dict):\n+        attn_metadata = attn_metadata[layer_name]\n+    self = forward_context.no_compile_layers[layer_name]\n+    kv_cache = self.kv_cache[forward_context.virtual_engine]\n+    self.impl.do_kv_cache_update(\n+        self,\n+        key,\n+        value,\n+        kv_cache,\n+        attn_metadata,\n+    )\n+\n+    # TODO keep one of these two\n+    maybe_save_kv_layer_to_connector(layer_name, kv_cache)\n", "materials": ["Remove TODO (I think this is good)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2410660821", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\n```suggestion\n    mutates_args=[\"output\"],  # fake dependency to make sure op is not optimized out\n```\n\nOriginal Code Context:\n@@ -676,6 +675,51 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    wait_for_kv_layer_from_connector(layer_name)\n+    forward_context: ForwardContext = get_forward_context()\n+    attn_metadata = forward_context.attn_metadata\n+    if isinstance(attn_metadata, dict):\n+        attn_metadata = attn_metadata[layer_name]\n+    self = forward_context.no_compile_layers[layer_name]\n+    kv_cache = self.kv_cache[forward_context.virtual_engine]\n+    self.impl.do_kv_cache_update(\n+        self,\n+        key,\n+        value,\n+        kv_cache,\n+        attn_metadata,\n+    )\n+\n+    # TODO keep one of these two\n+    maybe_save_kv_layer_to_connector(layer_name, kv_cache)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    return\n+\n+\n+# TODO call a method on backend and this method should be in superclass of\n+# all backends\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[\"output\"],  # fake dependency hack for piecewise cudagraphs\n", "materials": ["```suggestion\n    mutates_args=[\"output\"],  # fake dependency to make sure op is not optimized out\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2410662258", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\n```suggestion\n    if isinstance(attn_metadata, dict):\n        attn_metadata = attn_metadata.get(layer_name)\n```\n\nOriginal Code Context:\n@@ -685,13 +729,16 @@ def unified_attention_with_output(\n     output_scale: Optional[torch.Tensor] = None,\n     output_block_scale: Optional[torch.Tensor] = None,\n ) -> None:\n-    wait_for_kv_layer_from_connector(layer_name)\n     forward_context: ForwardContext = get_forward_context()\n     attn_metadata = forward_context.attn_metadata\n-    if isinstance(attn_metadata, dict):\n+    if isinstance(attn_metadata, dict) and layer_name in attn_metadata:\n         attn_metadata = attn_metadata[layer_name]\n+    else:\n+        attn_metadata = None\n", "materials": ["```suggestion\n    if isinstance(attn_metadata, dict):\n        attn_metadata = attn_metadata.get(layer_name)\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2410670374", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/attention/backends/flash_attn.py\nReview Comment:\nShould we maybe rename this to `forward_includes_kv_cache`? Still short but slightly more descriptive?\n\nOriginal Code Context:\n@@ -47,6 +47,7 @@\n \n class FlashAttentionBackend(AttentionBackend):\n     accept_output_buffer: bool = True\n+    include_kv_cache: bool = False\n", "materials": ["Should we maybe rename this to `forward_includes_kv_cache`? Still short but slightly more descriptive?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -79,6 +79,8 @@ def get_supported_kernel_block_sizes() -> list[int | MultipleOf]:\n             return [16, 32, 64]\n         return [MultipleOf(16)]\n \n+    forward_includes_kv_cache_update: bool = False\n+\n     @staticmethod\n     def get_name() -> str:\n         return \"FLASH_ATTN\"\n@@ -652,32 +654,6 @@ def forward(\n         # For decoder and cross-attention, use KV cache as before\n         key_cache, value_cache = kv_cache.unbind(0)\n \n-        # key and value may be None in the case of cross attention. They are\n-        # calculated once based on the output from the encoder and then cached\n-        # in KV cache.\n-        if (\n-            self.kv_sharing_target_layer_name is None\n-            and key is not None\n-            and value is not None\n-        ):\n-            # Reshape the input keys and values and store them in the cache.\n-            # Skip this if sharing KV cache with an earlier attention layer.\n-            # NOTE(woosuk): Here, key and value are padded while slot_mapping is\n-            # not padded. However, we don't need to do key[:num_actual_tokens]\n-            # and value[:num_actual_tokens] because the reshape_and_cache_flash\n-            # op uses the slot_mapping's shape to determine the number of\n-            # actual tokens.\n-            reshape_and_cache_flash(\n-                key,\n-                value,\n-                key_cache,\n-                value_cache,\n-                attn_metadata.slot_mapping,\n-                self.kv_cache_dtype,\n-                layer._k_scale,\n-                layer._v_scale,\n-            )\n-\n         if self.kv_cache_dtype.startswith(\"fp8\"):\n             # queries are quantized in the attention layer\n             dtype = FlashAttentionBackend.get_fp8_dtype_for_flashattn(\n@@ -774,6 +750,49 @@ def forward(\n         )\n         return output\n \n+    def do_kv_cache_update(\n+        self,\n+        layer: torch.nn.Module,\n+        key: torch.Tensor,\n+        value: torch.Tensor,\n+        kv_cache: torch.Tensor,\n+        slot_mapping: torch.Tensor,\n+    ) -> None:\n+        if self.attn_type in (AttentionType.ENCODER_ONLY, AttentionType.ENCODER):\n+            # For encoder attention,\n+            # we use direct Q, K, V tensors without caching\n+            return\n+\n+        # key and value may be None in the case of cross attention. They are\n+        # calculated once based on the output from the encoder and then cached\n+        # in KV cache.\n+        if (\n+            self.kv_sharing_target_layer_name is not None\n+            or key is None\n+            or value is None\n+        ):\n+            return\n+\n+        key_cache, value_cache = kv_cache.unbind(0)\n+\n+        # Reshape the input keys and values and store them in the cache.\n+        # Skip this if sharing KV cache with an earlier attention layer.\n+        # NOTE(woosuk): Here, key and value are padded while slot_mapping is\n+        # not padded. However, we don't need to do key[:num_actual_tokens]\n+        # and value[:num_actual_tokens] because the reshape_and_cache_flash\n+        # op uses the slot_mapping's shape to determine the number of\n+        # actual tokens.\n+        reshape_and_cache_flash(\n+            key,\n+            value,\n+            key_cache,\n+            value_cache,\n+            slot_mapping,\n+            self.kv_cache_dtype,\n+            layer._k_scale,\n+            layer._v_scale,\n+        )\n+\n     def _forward_with_dcp(\n         self,\n         query: torch.Tensor,", "patch_sha256": "5824c8edc9763307"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2410674818", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/attention/backends/flash_attn.py\nReview Comment:\nI would continue the early exit pattern\n```suggestion\n        # key and value may be None in the case of cross attention. They are\n        # calculated once based on the output from the encoder and then cached\n        # in KV cache.\n        if (\n            self.kv_sharing_target_layer_name is not None\n            or key is None\n            or value is None\n        ):\n            return\n\n        key_cache, value_cache = kv_cache.unbind(0)\n```\n\nOriginal Code Context:\n@@ -610,6 +585,51 @@ def forward(\n         )\n         return output\n \n+    def do_kv_cache_update(\n+        self,\n+        layer: torch.nn.Module,\n+        key: torch.Tensor,\n+        value: torch.Tensor,\n+        kv_cache: torch.Tensor,\n+        attn_metadata: FlashAttentionMetadata,\n+    ) -> None:\n+        if attn_metadata is None:\n+            # Profiling run.\n+            return\n+\n+        if self.attn_type in (AttentionType.ENCODER_ONLY, AttentionType.ENCODER):\n+            # For encoder attention,\n+            # we use direct Q, K, V tensors without caching\n+            return\n+\n+        key_cache, value_cache = kv_cache.unbind(0)\n+\n+        # key and value may be None in the case of cross attention. They are\n+        # calculated once based on the output from the encoder and then cached\n+        # in KV cache.\n+        if (\n+            self.kv_sharing_target_layer_name is None\n+            and key is not None\n+            and value is not None\n+        ):\n", "materials": ["I would continue the early exit pattern\n```suggestion\n        # key and value may be None in the case of cross attention. They are\n        # calculated once based on the output from the encoder and then cached\n        # in KV cache.\n        if (\n            self.kv_sharing_target_layer_name is not None\n            or key is None\n            or value is None\n        ):\n            return\n\n        key_cache, value_cache = kv_cache.unbind(0)\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -79,6 +79,8 @@ def get_supported_kernel_block_sizes() -> list[int | MultipleOf]:\n             return [16, 32, 64]\n         return [MultipleOf(16)]\n \n+    forward_includes_kv_cache_update: bool = False\n+\n     @staticmethod\n     def get_name() -> str:\n         return \"FLASH_ATTN\"\n@@ -652,32 +654,6 @@ def forward(\n         # For decoder and cross-attention, use KV cache as before\n         key_cache, value_cache = kv_cache.unbind(0)\n \n-        # key and value may be None in the case of cross attention. They are\n-        # calculated once based on the output from the encoder and then cached\n-        # in KV cache.\n-        if (\n-            self.kv_sharing_target_layer_name is None\n-            and key is not None\n-            and value is not None\n-        ):\n-            # Reshape the input keys and values and store them in the cache.\n-            # Skip this if sharing KV cache with an earlier attention layer.\n-            # NOTE(woosuk): Here, key and value are padded while slot_mapping is\n-            # not padded. However, we don't need to do key[:num_actual_tokens]\n-            # and value[:num_actual_tokens] because the reshape_and_cache_flash\n-            # op uses the slot_mapping's shape to determine the number of\n-            # actual tokens.\n-            reshape_and_cache_flash(\n-                key,\n-                value,\n-                key_cache,\n-                value_cache,\n-                attn_metadata.slot_mapping,\n-                self.kv_cache_dtype,\n-                layer._k_scale,\n-                layer._v_scale,\n-            )\n-\n         if self.kv_cache_dtype.startswith(\"fp8\"):\n             # queries are quantized in the attention layer\n             dtype = FlashAttentionBackend.get_fp8_dtype_for_flashattn(\n@@ -774,6 +750,49 @@ def forward(\n         )\n         return output\n \n+    def do_kv_cache_update(\n+        self,\n+        layer: torch.nn.Module,\n+        key: torch.Tensor,\n+        value: torch.Tensor,\n+        kv_cache: torch.Tensor,\n+        slot_mapping: torch.Tensor,\n+    ) -> None:\n+        if self.attn_type in (AttentionType.ENCODER_ONLY, AttentionType.ENCODER):\n+            # For encoder attention,\n+            # we use direct Q, K, V tensors without caching\n+            return\n+\n+        # key and value may be None in the case of cross attention. They are\n+        # calculated once based on the output from the encoder and then cached\n+        # in KV cache.\n+        if (\n+            self.kv_sharing_target_layer_name is not None\n+            or key is None\n+            or value is None\n+        ):\n+            return\n+\n+        key_cache, value_cache = kv_cache.unbind(0)\n+\n+        # Reshape the input keys and values and store them in the cache.\n+        # Skip this if sharing KV cache with an earlier attention layer.\n+        # NOTE(woosuk): Here, key and value are padded while slot_mapping is\n+        # not padded. However, we don't need to do key[:num_actual_tokens]\n+        # and value[:num_actual_tokens] because the reshape_and_cache_flash\n+        # op uses the slot_mapping's shape to determine the number of\n+        # actual tokens.\n+        reshape_and_cache_flash(\n+            key,\n+            value,\n+            key_cache,\n+            value_cache,\n+            slot_mapping,\n+            self.kv_cache_dtype,\n+            layer._k_scale,\n+            layer._v_scale,\n+        )\n+\n     def _forward_with_dcp(\n         self,\n         query: torch.Tensor,", "patch_sha256": "5824c8edc9763307"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2413743304", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nThis is for the case when there are multiple different backends and some support the split kv cache, while some do not. In this case, when we do piecewise compile cudagraphs, attention metadata is built only for the ones that do the split (trying to build for all results in e.g. mla hitting asserts for when metadata could not be built)\n\nOriginal Code Context:\n@@ -649,8 +646,10 @@ def unified_attention(\n \n     forward_context: ForwardContext = get_forward_context()\n     attn_metadata = forward_context.attn_metadata\n-    if isinstance(attn_metadata, dict):\n+    if isinstance(attn_metadata, dict) and layer_name in attn_metadata:\n         attn_metadata = attn_metadata[layer_name]\n+    else:\n+        attn_metadata = None\n", "materials": ["This is for the case when there are multiple different backends and some support the split kv cache, while some do not. In this case, when we do piecewise compile cudagraphs, attention metadata is built only for the ones that do the split (trying to build for all results in e.g. mla hitting asserts for when metadata could not be built)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2413808935", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nI think that's handled by the get method which will return none for those layers\n\nOriginal Code Context:\n@@ -649,8 +646,10 @@ def unified_attention(\n \n     forward_context: ForwardContext = get_forward_context()\n     attn_metadata = forward_context.attn_metadata\n-    if isinstance(attn_metadata, dict):\n+    if isinstance(attn_metadata, dict) and layer_name in attn_metadata:\n         attn_metadata = attn_metadata[layer_name]\n+    else:\n+        attn_metadata = None\n", "materials": ["I think that's handled by the get method which will return none for those layers"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2417807037", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nTODO resolved?\n\nOriginal Code Context:\n@@ -676,6 +673,50 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    wait_for_kv_layer_from_connector(layer_name)\n+    forward_context: ForwardContext = get_forward_context()\n+    attn_metadata = forward_context.attn_metadata\n+    if isinstance(attn_metadata, dict):\n+        attn_metadata = attn_metadata[layer_name]\n+    self = forward_context.no_compile_layers[layer_name]\n+    kv_cache = self.kv_cache[forward_context.virtual_engine]\n+    self.impl.do_kv_cache_update(\n+        self,\n+        key,\n+        value,\n+        kv_cache,\n+        attn_metadata,\n+    )\n+\n+    maybe_save_kv_layer_to_connector(layer_name, kv_cache)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    return\n+\n+\n+# TODO call a method on backend and this method should be in superclass of\n", "materials": ["TODO resolved?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2417907235", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nnit: can get rid of the `noqa`\n\nOriginal Code Context:\n@@ -363,29 +363,26 @@ def forward(\n                 if value is not None:\n                     value = value.view(-1, self.num_kv_heads, self.head_size)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                if not self.attn_backend.forward_includes_kv_cache:\n+                    unified_kv_cache_update(key, value, output, self.layer_name)\n+                unified_attention_with_output(\n+                    query, key, value, output, self.layer_name\n                 )\n             else:\n+                if not self.attn_backend.forward_includes_kv_cache:\n+                    torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, output, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n                     query, key, value, output, self.layer_name\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache, (\n+                \"Attention backend does not support kv cache\"\n+            )  # noqa: E501\n", "materials": ["nit: can get rid of the `noqa`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2535728152", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nDo we need to remove these and execute them conditionally based on the `forward_includes_kv_cache` property of the backend? cc @NickLucche \n\nOriginal Code Context:\n@@ -931,6 +928,43 @@ def unified_attention_fake(\n )\n \n \n+@maybe_transfer_kv_layer\n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    attn_metadata, self, kv_cache = get_attention_context(layer_name)\n+    assert hasattr(self.impl, \"do_kv_cache_update\"), (\n+        f\"{self.impl.__class__.__name__} does not support kv cache update\"\n+    )\n+    self.impl.do_kv_cache_update(\n+        self,\n+        key,\n+        value,\n+        kv_cache,\n+        attn_metadata,\n+    )\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    return\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[\"output\"],  # fake dependency to make sure op is not optimized out\n+)\n+\n+\n @maybe_transfer_kv_layer\n", "materials": ["Do we need to remove these and execute them conditionally based on the `forward_includes_kv_cache` property of the backend? cc @NickLucche "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2548441516", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\n@zou3519 is there a way for us to specify side-effects and dependency between `unified_kv_cache_update` and `unified_attention_with_output`?\n\nOriginal Code Context:\n@@ -931,6 +928,43 @@ def unified_attention_fake(\n )\n \n \n+@maybe_transfer_kv_layer\n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    attn_metadata, self, kv_cache = get_attention_context(layer_name)\n+    assert hasattr(self.impl, \"do_kv_cache_update\"), (\n+        f\"{self.impl.__class__.__name__} does not support kv cache update\"\n+    )\n+    self.impl.do_kv_cache_update(\n+        self,\n+        key,\n+        value,\n+        kv_cache,\n+        attn_metadata,\n+    )\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    return\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[\"output\"],  # fake dependency to make sure op is not optimized out\n", "materials": ["@zou3519 is there a way for us to specify side-effects and dependency between `unified_kv_cache_update` and `unified_attention_with_output`?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2549118521", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nActually the cache update should just \"append\" the newly computed values on D, ignoring the unloaded ones.\nWe only need those once we compute attn. Hence I believe we should skip `maybe_transfer_kv_layer` during `unified_kv_cache_update` and postpone it to `unified_attention_with_output` since the load is anyway blocking @ElizaWszola .\n\ncc @KuntaiDu for interactions with lm-cache that I may be overlooking.\n\nOriginal Code Context:\n@@ -931,6 +928,43 @@ def unified_attention_fake(\n )\n \n \n+@maybe_transfer_kv_layer\n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    attn_metadata, self, kv_cache = get_attention_context(layer_name)\n+    assert hasattr(self.impl, \"do_kv_cache_update\"), (\n+        f\"{self.impl.__class__.__name__} does not support kv cache update\"\n+    )\n+    self.impl.do_kv_cache_update(\n+        self,\n+        key,\n+        value,\n+        kv_cache,\n+        attn_metadata,\n+    )\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    return\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[\"output\"],  # fake dependency to make sure op is not optimized out\n+)\n+\n+\n @maybe_transfer_kv_layer\n", "materials": ["Actually the cache update should just \"append\" the newly computed values on D, ignoring the unloaded ones.\nWe only need those once we compute attn. Hence I believe we should skip `maybe_transfer_kv_layer` during `unified_kv_cache_update` and postpone it to `unified_attention_with_output` since the load is anyway blocking @ElizaWszola .\n\ncc @KuntaiDu for interactions with lm-cache that I may be overlooking."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2549120533", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nI would remove this and postpone the load->save to the actual attn computation, as per comment below. \n\nOriginal Code Context:\n@@ -931,6 +928,43 @@ def unified_attention_fake(\n )\n \n \n+@maybe_transfer_kv_layer\n", "materials": ["I would remove this and postpone the load->save to the actual attn computation, as per comment below. "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2549855485", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nOk nice that would also make the CG splitting logic easier as we always only have to exclude attention from piecewise CG. \n\nOriginal Code Context:\n@@ -931,6 +928,43 @@ def unified_attention_fake(\n )\n \n \n+@maybe_transfer_kv_layer\n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    attn_metadata, self, kv_cache = get_attention_context(layer_name)\n+    assert hasattr(self.impl, \"do_kv_cache_update\"), (\n+        f\"{self.impl.__class__.__name__} does not support kv cache update\"\n+    )\n+    self.impl.do_kv_cache_update(\n+        self,\n+        key,\n+        value,\n+        kv_cache,\n+        attn_metadata,\n+    )\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    return\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[\"output\"],  # fake dependency to make sure op is not optimized out\n+)\n+\n+\n @maybe_transfer_kv_layer\n", "materials": ["Ok nice that would also make the CG splitting logic easier as we always only have to exclude attention from piecewise CG. "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2581627614", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nReplied offline, copy-pasting reply:\r\n\r\nUnfortunately I think mutating output might be the best way to do this right now. The alternatives are:\r\n1) write everything in a functional way. this means actually having a kvcache tensor and threading it through the operations\r\n2) Create a dummy output tensor for unified_kv_cache_update and have unified_attention_with_output accept it as an input. This can just be a size [0] tensor so it doesn't have very much overhead.\r\n3) marking unified_kv_cache_update as side-effectful. (there's a new torch._library.register_effects somewhere). What it ends up doing is that it wraps the unified_kv_cache_update custom operator in a HOP (e.g. with_effects(unified_kv_cache_update), similar to auto_functionalized)), and then we go down the route of it being difficult to pattern match again lol.\n\nOriginal Code Context:\n@@ -931,6 +928,43 @@ def unified_attention_fake(\n )\n \n \n+@maybe_transfer_kv_layer\n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    attn_metadata, self, kv_cache = get_attention_context(layer_name)\n+    assert hasattr(self.impl, \"do_kv_cache_update\"), (\n+        f\"{self.impl.__class__.__name__} does not support kv cache update\"\n+    )\n+    self.impl.do_kv_cache_update(\n+        self,\n+        key,\n+        value,\n+        kv_cache,\n+        attn_metadata,\n+    )\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    return\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[\"output\"],  # fake dependency to make sure op is not optimized out\n", "materials": ["Replied offline, copy-pasting reply:\r\n\r\nUnfortunately I think mutating output might be the best way to do this right now. The alternatives are:\r\n1) write everything in a functional way. this means actually having a kvcache tensor and threading it through the operations\r\n2) Create a dummy output tensor for unified_kv_cache_update and have unified_attention_with_output accept it as an input. This can just be a size [0] tensor so it doesn't have very much overhead.\r\n3) marking unified_kv_cache_update as side-effectful. (there's a new torch._library.register_effects somewhere). What it ends up doing is that it wraps the unified_kv_cache_update custom operator in a HOP (e.g. with_effects(unified_kv_cache_update), similar to auto_functionalized)), and then we go down the route of it being difficult to pattern match again lol."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2581675348", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nSounds good, let's keep this for now, once we move all custom ops to functional we can do option 2 (use a dummy tensor for the kv-cache dependency between update and attention)\n\nOriginal Code Context:\n@@ -931,6 +928,43 @@ def unified_attention_fake(\n )\n \n \n+@maybe_transfer_kv_layer\n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    attn_metadata, self, kv_cache = get_attention_context(layer_name)\n+    assert hasattr(self.impl, \"do_kv_cache_update\"), (\n+        f\"{self.impl.__class__.__name__} does not support kv cache update\"\n+    )\n+    self.impl.do_kv_cache_update(\n+        self,\n+        key,\n+        value,\n+        kv_cache,\n+        attn_metadata,\n+    )\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    return\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[\"output\"],  # fake dependency to make sure op is not optimized out\n", "materials": ["Sounds good, let's keep this for now, once we move all custom ops to functional we can do option 2 (use a dummy tensor for the kv-cache dependency between update and attention)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2675139303", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\n`output` is a dummy argument that is not used in the function implementation, and also not used by subsequent ops. It will be eliminated in post cleanup pass if using a full graph(`cudagraph_mode=FULL_DECODE_ONLY` + `splitting_ops=[]` + `pass_config.fuse_attn_quant=True`, llama3.3):\n<img width=\"3135\" height=\"536\" alt=\"Image\" src=\"https://github.com/user-attachments/assets/59e789c4-e5bc-48f6-a346-0a6ff06778f0\" />\n\n\nIt won't be cleaned up in GPT-OSS(piecewise graph, no attn fusion):\n<img width=\"1599\" height=\"248\" alt=\"Image\" src=\"https://github.com/user-attachments/assets/3277b1a7-33cb-48ea-a480-f96e6e96eb1f\" />\n\nOriginal Code Context:\n@@ -792,6 +789,42 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    attn_metadata, self, kv_cache = get_attention_context(layer_name)\n+    assert hasattr(self.impl, \"do_kv_cache_update\"), (\n+        f\"{self.impl.__class__.__name__} does not support kv cache update\"\n+    )\n+    self.impl.do_kv_cache_update(\n+        self,\n+        key,\n+        value,\n+        kv_cache,\n+        attn_metadata,\n+    )\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    return\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[\"output\"],  # fake dependency to make sure op is not optimized out\n", "materials": ["`output` is a dummy argument that is not used in the function implementation, and also not used by subsequent ops. It will be eliminated in post cleanup pass if using a full graph(`cudagraph_mode=FULL_DECODE_ONLY` + `splitting_ops=[]` + `pass_config.fuse_attn_quant=True`, llama3.3):\n<img width=\"3135\" height=\"536\" alt=\"Image\" src=\"https://github.com/user-attachments/assets/59e789c4-e5bc-48f6-a346-0a6ff06778f0\" />\n\n\nIt won't be cleaned up in GPT-OSS(piecewise graph, no attn fusion):\n<img width=\"1599\" height=\"248\" alt=\"Image\" src=\"https://github.com/user-attachments/assets/3277b1a7-33cb-48ea-a480-f96e6e96eb1f\" />"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688099662", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\n### Ubatch wrapper missing slot_mapping causes skipped KV updates\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe `_make_ubatch_metadata` method in `gpu_ubatch_wrapper.py` calls `create_forward_context` without passing the `slot_mapping` argument. When DBO (Double Buffer Optimization) is enabled with a backend that has `forward_includes_kv_cache=False` (currently FlashAttn), the `unified_kv_cache_update` function checks `forward_context.slot_mapping`, finds an empty dict (the default), and returns early without updating the KV cache. This silently causes incorrect attention outputs because the key-value data is never written to the cache.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/attention/layer.py#L803-L810\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..JEse4ahB5nZ59u0dSz_SQ556q9Kf__xkk11BTqA0yvFVbN3kShW0t6E9PIBtjmz7Gp_5oH_c6wQC2Yy5-tMYZOGDpyuLSpM_3JmK4PVrzH-RoCXgcaJDSJQ3cUB_G3pOUZJdGZk1D_ezraLnYvdqj8Otwp6kPmGzw3tguRSNRsT5oVZsXTzIG3W3S_Gfmoq21QDIj11_27Mtw0xiqBpZrYFfFzSNS6IMiF2ia-1Avm_uGOooHqJXMHNkr8aiFGJZuV_uNvXm3MfEQdai6787ltNGJl-HFzaALKqkuYqhtZ1h2LFY6sG7kA3zqmiEKjrcN4yfMdXWQoQXBzUE4nujxg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..yJPD9uBb0AfGKcq9zmEGlzwLt2kDDiOvoUZQjhNC8OrMVMAFNLRk1Jauyh598pAsaWNFWTveVGqlmCg5hDBuB-qDXxDWHFq8VjGrW13niQ639u3CyAW8HWOdgh23nQF1xQXjmlVmKPvnAgx0TEfKrFrvNvh0FN_Us-jlFrdj5DBwE8wicBHuBc1Ix5uKzcPi5Jy0VKn46pijlc14gVut0tftjykkvBmd1YxhS2xyILGIhVCog3wIGIf5iObeD0F__C8icQaqsy7HtcehPwVeAhgXzDPLCSYjzd4_pRq_RZnCO8gSzr2uNXk9QseIiyLI9aqT69OPZWbiRpz3TYohrA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -792,6 +791,54 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    if isinstance(slot_mapping, dict):\n+        slot_mapping = slot_mapping.get(layer_name)\n+\n+    if slot_mapping is None:\n+        # No slot_mapping available (e.g., profiling run)\n+        return output\n", "materials": ["### Ubatch wrapper missing slot_mapping causes skipped KV updates\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe `_make_ubatch_metadata` method in `gpu_ubatch_wrapper.py` calls `create_forward_context` without passing the `slot_mapping` argument. When DBO (Double Buffer Optimization) is enabled with a backend that has `forward_includes_kv_cache=False` (currently FlashAttn), the `unified_kv_cache_update` function checks `forward_context.slot_mapping`, finds an empty dict (the default), and returns early without updating the KV cache. This silently causes incorrect attention outputs because the key-value data is never written to the cache.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/attention/layer.py#L803-L810\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..JEse4ahB5nZ59u0dSz_SQ556q9Kf__xkk11BTqA0yvFVbN3kShW0t6E9PIBtjmz7Gp_5oH_c6wQC2Yy5-tMYZOGDpyuLSpM_3JmK4PVrzH-RoCXgcaJDSJQ3cUB_G3pOUZJdGZk1D_ezraLnYvdqj8Otwp6kPmGzw3tguRSNRsT5oVZsXTzIG3W3S_Gfmoq21QDIj11_27Mtw0xiqBpZrYFfFzSNS6IMiF2ia-1Avm_uGOooHqJXMHNkr8aiFGJZuV_uNvXm3MfEQdai6787ltNGJl-HFzaALKqkuYqhtZ1h2LFY6sG7kA3zqmiEKjrcN4yfMdXWQoQXBzUE4nujxg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..yJPD9uBb0AfGKcq9zmEGlzwLt2kDDiOvoUZQjhNC8OrMVMAFNLRk1Jauyh598pAsaWNFWTveVGqlmCg5hDBuB-qDXxDWHFq8VjGrW13niQ639u3CyAW8HWOdgh23nQF1xQXjmlVmKPvnAgx0TEfKrFrvNvh0FN_Us-jlFrdj5DBwE8wicBHuBc1Ix5uKzcPi5Jy0VKn46pijlc14gVut0tftjykkvBmd1YxhS2xyILGIhVCog3wIGIf5iObeD0F__C8icQaqsy7HtcehPwVeAhgXzDPLCSYjzd4_pRq_RZnCO8gSzr2uNXk9QseIiyLI9aqT69OPZWbiRpz3TYohrA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688099666", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\n### Missing list type handling causes crash in unified_kv_cache_update\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `unified_kv_cache_update` function only checks `isinstance(slot_mapping, dict)` but the `ForwardContext.slot_mapping` field can also be a `list[dict[str, torch.Tensor]]` according to its type annotation. When `slot_mapping` is a list (which occurs when ubatch_slices is provided to `_get_slot_mappings`), the dict check fails, the list passes the `is None` check, and the raw list is passed to `do_kv_cache_update` which expects a `torch.Tensor`. This would cause `reshape_and_cache_flash` to receive a list instead of a tensor, resulting in a crash.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/attention/layer.py#L803-L806\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..CSEz_QFnxpG9fAHMTrJgCh7-JgzZtSssi-Dy4-rUodZoD5oYdfISKbODFciIBNX_fNwFpxHHhIW7ZkPf84LE1jhmPyIUuB4UnR0khJNNOmnjXPxvWK5LBug_ucXJXThXII6DG1MlEL1xjiVvcclTL2-Mx2t08aKkSrF4g8-VMLUzBg4xZp8F1A-zgUmCQqhn6Gx8y6AS9H1IruRBlydaXFB15RHcYPWgLRhMAj72iSwTcUGXbC47IVE4hBBqpQOKy7df6aXq0zSBxeH1muASjZohYhLMKmVuP5VM0JzJb1REna8UFfXBuTZMc747PRCyeU5q2HJSgqK4k4jk2Nc3Sw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..fdp7XIgeW_PI3hKK9Meu6kEvt5ZdXcZkgy70XXGrON9zYucl151P1k-82KBGXWjLdyfAI-j7w00h4lKnDeu0L0HUvzoJMtyHfisjZQdX_JEcc4jV_xbUc2KUd7Wfu1ZI1foZ-OG-_R0pT8BClJANZyPCGT2RO4py5ddqJzR3n1WFJWYswSdpRfMyYuoKNyyjl4VwrbJ7rOMB5hP2EBjXMPfean8qKKbz_TUk_VRHZpj29MEZUhM77n3QtluUV7UxJrjcjMraenZP_YIJuLEW8Rco-Y2owcTYL8uHUWau5zc_r_y-8InQ7VdnV-dPwJMqVrthNmjJK6jCtcCIKZZCPA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -792,6 +791,54 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    if isinstance(slot_mapping, dict):\n+        slot_mapping = slot_mapping.get(layer_name)\n", "materials": ["### Missing list type handling causes crash in unified_kv_cache_update\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `unified_kv_cache_update` function only checks `isinstance(slot_mapping, dict)` but the `ForwardContext.slot_mapping` field can also be a `list[dict[str, torch.Tensor]]` according to its type annotation. When `slot_mapping` is a list (which occurs when ubatch_slices is provided to `_get_slot_mappings`), the dict check fails, the list passes the `is None` check, and the raw list is passed to `do_kv_cache_update` which expects a `torch.Tensor`. This would cause `reshape_and_cache_flash` to receive a list instead of a tensor, resulting in a crash.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/attention/layer.py#L803-L806\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..CSEz_QFnxpG9fAHMTrJgCh7-JgzZtSssi-Dy4-rUodZoD5oYdfISKbODFciIBNX_fNwFpxHHhIW7ZkPf84LE1jhmPyIUuB4UnR0khJNNOmnjXPxvWK5LBug_ucXJXThXII6DG1MlEL1xjiVvcclTL2-Mx2t08aKkSrF4g8-VMLUzBg4xZp8F1A-zgUmCQqhn6Gx8y6AS9H1IruRBlydaXFB15RHcYPWgLRhMAj72iSwTcUGXbC47IVE4hBBqpQOKy7df6aXq0zSBxeH1muASjZohYhLMKmVuP5VM0JzJb1REna8UFfXBuTZMc747PRCyeU5q2HJSgqK4k4jk2Nc3Sw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..fdp7XIgeW_PI3hKK9Meu6kEvt5ZdXcZkgy70XXGrON9zYucl151P1k-82KBGXWjLdyfAI-j7w00h4lKnDeu0L0HUvzoJMtyHfisjZQdX_JEcc4jV_xbUc2KUd7Wfu1ZI1foZ-OG-_R0pT8BClJANZyPCGT2RO4py5ddqJzR3n1WFJWYswSdpRfMyYuoKNyyjl4VwrbJ7rOMB5hP2EBjXMPfean8qKKbz_TUk_VRHZpj29MEZUhM77n3QtluUV7UxJrjcjMraenZP_YIJuLEW8Rco-Y2owcTYL8uHUWau5zc_r_y-8InQ7VdnV-dPwJMqVrthNmjJK6jCtcCIKZZCPA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688099671", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\n### Missing list type handling causes crash in unified_kv_cache_update\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `unified_kv_cache_update` function only checks `isinstance(slot_mapping, dict)` but the `ForwardContext.slot_mapping` field can also be a `list[dict[str, torch.Tensor]]` according to its type annotation. When `slot_mapping` is a list (which occurs when ubatch_slices is provided to `_get_slot_mappings`), the dict check fails, the list passes the `is None` check, and the raw list is passed to `do_kv_cache_update` which expects a `torch.Tensor`. This would cause `reshape_and_cache_flash` to receive a list instead of a tensor, resulting in a crash.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/attention/layer.py#L803-L806\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..VfWwholK7cGNwq28W4z0HJr_tkRDzllcbHroWv2fJ_hoDdQlGTzE2EGRxDi-nb3pRsnO9Xrcu4hB7DZTwIRL9PU1KFqR2Oo7z2UXvMAx9e6FyDAWW9-WrN0oq-gfVPESxni5F-doWuS5S2dLyPhoFjHVdML8-4HzMEgBDD86NxlQwsnfBOzf4JdxfDw0X3u54m0MlvUrtRANJizIEn6iREZNIGB-DDZwERzvPAb-uZrfe5lVljpXeXf5ML0UIn1rqw2zCr0d2rgfpYIobmfNjoKDzIgYRrNCgKKfAsXSwy5pL6j8WjTyw928MYm4nlz8blLrEQAptVRN4eO0z-Y_Ew\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..qyzHJplUyKtv1W3b34PWizLd44rfPjkqD_LzET_5DHxWI4X-V78-sPAzb1mGXnXBPVAgOQxXGmfJSq2SFI9wwY-7wWuxaa_hU-VbWAaHlVkgsRMznJGtpNa3JWxebey5UzUehTZcU5X2OEkGmTjlEUXB0VGqt3l-4rXnzWX7qDT1PXmI_D16YEZpKu3gFAUHuJm_fDND7KP0bJ84II6dmlrDCQMtzqlSjCRwaMW6kvdOZtC2UCNUth0_kHqDtZQ4Hp5WZGzNzVPTGzLcQKxzVh91k59VgQImliFZRYciqmhKaiAOMfL3EBIT_Lz3UR9xUdzZMDL59knfNIiEcul_wA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -792,6 +791,54 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    if isinstance(slot_mapping, dict):\n+        slot_mapping = slot_mapping.get(layer_name)\n", "materials": ["### Missing list type handling causes crash in unified_kv_cache_update\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `unified_kv_cache_update` function only checks `isinstance(slot_mapping, dict)` but the `ForwardContext.slot_mapping` field can also be a `list[dict[str, torch.Tensor]]` according to its type annotation. When `slot_mapping` is a list (which occurs when ubatch_slices is provided to `_get_slot_mappings`), the dict check fails, the list passes the `is None` check, and the raw list is passed to `do_kv_cache_update` which expects a `torch.Tensor`. This would cause `reshape_and_cache_flash` to receive a list instead of a tensor, resulting in a crash.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/attention/layer.py#L803-L806\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..VfWwholK7cGNwq28W4z0HJr_tkRDzllcbHroWv2fJ_hoDdQlGTzE2EGRxDi-nb3pRsnO9Xrcu4hB7DZTwIRL9PU1KFqR2Oo7z2UXvMAx9e6FyDAWW9-WrN0oq-gfVPESxni5F-doWuS5S2dLyPhoFjHVdML8-4HzMEgBDD86NxlQwsnfBOzf4JdxfDw0X3u54m0MlvUrtRANJizIEn6iREZNIGB-DDZwERzvPAb-uZrfe5lVljpXeXf5ML0UIn1rqw2zCr0d2rgfpYIobmfNjoKDzIgYRrNCgKKfAsXSwy5pL6j8WjTyw928MYm4nlz8blLrEQAptVRN4eO0z-Y_Ew\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..qyzHJplUyKtv1W3b34PWizLd44rfPjkqD_LzET_5DHxWI4X-V78-sPAzb1mGXnXBPVAgOQxXGmfJSq2SFI9wwY-7wWuxaa_hU-VbWAaHlVkgsRMznJGtpNa3JWxebey5UzUehTZcU5X2OEkGmTjlEUXB0VGqt3l-4rXnzWX7qDT1PXmI_D16YEZpKu3gFAUHuJm_fDND7KP0bJ84II6dmlrDCQMtzqlSjCRwaMW6kvdOZtC2UCNUth0_kHqDtZQ4Hp5WZGzNzVPTGzLcQKxzVh91k59VgQImliFZRYciqmhKaiAOMfL3EBIT_Lz3UR9xUdzZMDL59knfNIiEcul_wA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688207469", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nDone in [32a083f](https://github.com/vllm-project/vllm/pull/25954/commits/32a083f5df82a98e9c07e0e870865cc56d85e7ed)\n\nOriginal Code Context:\n@@ -792,6 +789,42 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    attn_metadata, self, kv_cache = get_attention_context(layer_name)\n+    assert hasattr(self.impl, \"do_kv_cache_update\"), (\n+        f\"{self.impl.__class__.__name__} does not support kv cache update\"\n+    )\n+    self.impl.do_kv_cache_update(\n+        self,\n+        key,\n+        value,\n+        kv_cache,\n+        attn_metadata,\n+    )\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> None:\n+    return\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[\"output\"],  # fake dependency to make sure op is not optimized out\n", "materials": ["Done in [32a083f](https://github.com/vllm-project/vllm/pull/25954/commits/32a083f5df82a98e9c07e0e870865cc56d85e7ed)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688276152", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nFixed in [64ab956](https://github.com/vllm-project/vllm/pull/25954/commits/64ab956210aa84861598f3d21402ddb5c7c99394)\n\nOriginal Code Context:\n@@ -792,6 +791,54 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    if isinstance(slot_mapping, dict):\n+        slot_mapping = slot_mapping.get(layer_name)\n", "materials": ["Fixed in [64ab956](https://github.com/vllm-project/vllm/pull/25954/commits/64ab956210aa84861598f3d21402ddb5c7c99394)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688276322", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nFixed in [64ab956](https://github.com/vllm-project/vllm/pull/25954/commits/64ab956210aa84861598f3d21402ddb5c7c99394)\n\nOriginal Code Context:\n@@ -792,6 +791,54 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    output: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    if isinstance(slot_mapping, dict):\n+        slot_mapping = slot_mapping.get(layer_name)\n", "materials": ["Fixed in [64ab956](https://github.com/vllm-project/vllm/pull/25954/commits/64ab956210aa84861598f3d21402ddb5c7c99394)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688594863", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nDoes this mean we're doing kv_cache_update for all indices in the slot mapping?\n\nOriginal Code Context:\n@@ -792,6 +803,68 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    if isinstance(slot_mapping, list):\n+        ubatch_slices = forward_context.ubatch_slices\n+        for i, slot_mapping_dict in enumerate(slot_mapping):\n+            layer_slot_mapping = slot_mapping_dict.get(layer_name)\n", "materials": ["Does this mean we're doing kv_cache_update for all indices in the slot mapping?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688612141", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nthis is kinda gross :( , is there no otherway to handle this?\n\nOriginal Code Context:\n@@ -801,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_update_out: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_update_out is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_update_out\n", "materials": ["this is kinda gross :( , is there no otherway to handle this?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688727920", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nCorrect - `layer_slot_mapping` should only be `None` for layers that don't have a kv cache, e.g. non-`DECODER` types and KV sharing layers\n\nOriginal Code Context:\n@@ -792,6 +803,68 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    if isinstance(slot_mapping, list):\n+        ubatch_slices = forward_context.ubatch_slices\n+        for i, slot_mapping_dict in enumerate(slot_mapping):\n+            layer_slot_mapping = slot_mapping_dict.get(layer_name)\n", "materials": ["Correct - `layer_slot_mapping` should only be `None` for layers that don't have a kv cache, e.g. non-`DECODER` types and KV sharing layers"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688753552", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nI did this based on https://github.com/vllm-project/vllm/pull/25954#discussion_r2678008342, I'm not sure if there's a better way. I might be able to get rid of this line though; not sure if the argument has to actually be touched in the function to prevent it from getting eliminated?\r\ncc @ProExpertProg \n\nOriginal Code Context:\n@@ -801,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_update_out: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_update_out is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_update_out\n", "materials": ["I did this based on https://github.com/vllm-project/vllm/pull/25954#discussion_r2678008342, I'm not sure if there's a better way. I might be able to get rid of this line though; not sure if the argument has to actually be touched in the function to prevent it from getting eliminated?\r\ncc @ProExpertProg "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688759111", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nIt seems a bit ugly for the kv cache op to be aware of ubatch slices but oh well\n\nOriginal Code Context:\n@@ -792,6 +803,68 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    if isinstance(slot_mapping, list):\n+        ubatch_slices = forward_context.ubatch_slices\n+        for i, slot_mapping_dict in enumerate(slot_mapping):\n+            layer_slot_mapping = slot_mapping_dict.get(layer_name)\n", "materials": ["It seems a bit ugly for the kv cache op to be aware of ubatch slices but oh well"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688762935", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\n@LucasWilkinson yeah we need to tell torch.compile that 1) `kv_cache_update` is not dead code and 2) that attention depends on it, all while keeping the op functional. We could call it `kv_cache_dependency` or `kv_cache_dummy` if you prefer? But this is the simplest/cleanest implementation.\n\nOriginal Code Context:\n@@ -801,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_update_out: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_update_out is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_update_out\n", "materials": ["@LucasWilkinson yeah we need to tell torch.compile that 1) `kv_cache_update` is not dead code and 2) that attention depends on it, all while keeping the op functional. We could call it `kv_cache_dependency` or `kv_cache_dummy` if you prefer? But this is the simplest/cleanest implementation."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688909574", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nid call it `kv_cache_dummy_dependency`, just to indicate its \"garbage\" and also a dependency \n\nOriginal Code Context:\n@@ -801,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_update_out: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_update_out is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_update_out\n", "materials": ["id call it `kv_cache_dummy_dependency`, just to indicate its \"garbage\" and also a dependency "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688916191", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\nsorry this is not correct for DBO; the list should be unwrapped like attn_metadata here: https://github.com/vllm-project/vllm/blob/90c0836902daf1766205f1644322231c274c3d7e/vllm/v1/worker/gpu_ubatch_wrapper.py#L311-L317 \n\nOriginal Code Context:\n@@ -792,6 +806,68 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    if isinstance(slot_mapping, list):\n+        ubatch_slices = forward_context.ubatch_slices\n+        for i, slot_mapping_dict in enumerate(slot_mapping):\n", "materials": ["sorry this is not correct for DBO; the list should be unwrapped like attn_metadata here: https://github.com/vllm-project/vllm/blob/90c0836902daf1766205f1644322231c274c3d7e/vllm/v1/worker/gpu_ubatch_wrapper.py#L311-L317 "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2688919651", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/attention/layer.py\nReview Comment:\n(I can probably just fix this :+1:)\n\nOriginal Code Context:\n@@ -792,6 +806,68 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    if isinstance(slot_mapping, list):\n+        ubatch_slices = forward_context.ubatch_slices\n+        for i, slot_mapping_dict in enumerate(slot_mapping):\n", "materials": ["(I can probably just fix this :+1:)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -390,29 +390,43 @@ def forward(\n             if value is not None:\n                 value = value.view(-1, self.num_kv_heads, self.head_size_v)\n             if self.use_direct_call:\n-                forward_context: ForwardContext = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata, output=output\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update:\n+                    kv_cache_dummy_dep = unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n+                unified_attention_with_output(\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             else:\n+                kv_cache_dummy_dep = None\n+                if not self.attn_backend.forward_includes_kv_cache_update and (\n+                    # torch can only dispatch custom op if a tensor is passed\n+                    key is not None or value is not None\n+                ):\n+                    kv_cache_dummy_dep = torch.ops.vllm.unified_kv_cache_update(\n+                        key, value, self.layer_name\n+                    )\n                 torch.ops.vllm.unified_attention_with_output(\n-                    query, key, value, output, self.layer_name\n+                    query,\n+                    key,\n+                    value,\n+                    output,\n+                    self.layer_name,\n+                    kv_cache_dummy_dep=kv_cache_dummy_dep,\n                 )\n             return output.view(-1, hidden_size)\n         else:\n+            assert self.attn_backend.forward_includes_kv_cache_update, (\n+                \"Split KV cache update not supported when output tensor not provided.\"\n+            )\n             if self.use_direct_call:\n-                forward_context = get_forward_context()\n-                attn_metadata = forward_context.attn_metadata\n-                if isinstance(attn_metadata, dict):\n-                    attn_metadata = attn_metadata[self.layer_name]\n-                self_kv_cache = self.kv_cache[forward_context.virtual_engine]\n-                return self.impl.forward(\n-                    self, query, key, value, self_kv_cache, attn_metadata\n-                )\n+                return unified_attention(query, key, value, self.layer_name)\n             else:\n                 return torch.ops.vllm.unified_attention(\n                     query, key, value, self.layer_name\n@@ -802,6 +816,55 @@ def unified_attention_fake(\n )\n \n \n+def unified_kv_cache_update(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    \"\"\"\n+    Returns a dummy that is passed to unified_attention to signal a side effect and\n+    the data dependency between them to ensure torch.compile preserves ordering.\n+    \"\"\"\n+    forward_context = get_forward_context()\n+    attn_layer = forward_context.no_compile_layers[layer_name]\n+    kv_cache = attn_layer.kv_cache[forward_context.virtual_engine]\n+\n+    slot_mapping = forward_context.slot_mapping\n+    assert isinstance(slot_mapping, dict), (\n+        f\"Expected slot_mapping to be a dict, got {type(slot_mapping)}. \"\n+    )\n+    layer_slot_mapping = slot_mapping.get(layer_name)\n+    if layer_slot_mapping is not None:\n+        assert hasattr(attn_layer.impl, \"do_kv_cache_update\"), (\n+            f\"{attn_layer.impl.__class__.__name__} does not support kv cache update\"\n+        )\n+        attn_layer.impl.do_kv_cache_update(\n+            attn_layer,\n+            key,\n+            value,\n+            kv_cache,\n+            layer_slot_mapping,\n+        )\n+\n+    return torch.empty(0, device=kv_cache.device, dtype=kv_cache.dtype)\n+\n+\n+def unified_kv_cache_update_fake(\n+    key: torch.Tensor,\n+    value: torch.Tensor,\n+    layer_name: str,\n+) -> torch.Tensor:\n+    return torch.empty(0, device=key.device, dtype=key.dtype)\n+\n+\n+direct_register_custom_op(\n+    op_name=\"unified_kv_cache_update\",\n+    op_func=unified_kv_cache_update,\n+    fake_impl=unified_kv_cache_update_fake,\n+    mutates_args=[],\n+)\n+\n+\n @maybe_transfer_kv_layer\n def unified_attention_with_output(\n     query: torch.Tensor,\n@@ -811,7 +874,12 @@ def unified_attention_with_output(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n+    # kv_cache_dummy_dep is not used but accepting it creates a data dependency\n+    # that ensures torch.compile preserves ordering between KV cache update and\n+    # attention forward.\n+    del kv_cache_dummy_dep\n     attn_metadata, self, kv_cache = get_attention_context(layer_name)\n \n     self.impl.forward(\n@@ -835,6 +903,7 @@ def unified_attention_with_output_fake(\n     layer_name: str,\n     output_scale: torch.Tensor | None = None,\n     output_block_scale: torch.Tensor | None = None,\n+    kv_cache_dummy_dep: torch.Tensor | None = None,\n ) -> None:\n     return\n ", "patch_sha256": "7b93e691bdfe07b1"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2689033576", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/forward_context.py\nReview Comment:\nnit: can we move this to after `attn_metadata: Any,` since they are semi-related and make it non-optoinal?\n\nOriginal Code Context:\n@@ -239,12 +240,14 @@ def create_forward_context(\n     cudagraph_runtime_mode: CUDAGraphMode = CUDAGraphMode.NONE,\n     batch_descriptor: BatchDescriptor | None = None,\n     ubatch_slices: UBatchSlices | None = None,\n+    slot_mapping: dict[str, torch.Tensor] | list[dict[str, torch.Tensor]] | None = None,\n", "materials": ["nit: can we move this to after `attn_metadata: Any,` since they are semi-related and make it non-optoinal?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -189,6 +189,7 @@ class ForwardContext:\n     # copy from vllm_config.compilation_config.static_forward_context\n     no_compile_layers: dict[str, Any]\n     attn_metadata: dict[str, AttentionMetadata] | list[dict[str, AttentionMetadata]]\n+    slot_mapping: dict[str, torch.Tensor] | list[dict[str, torch.Tensor]]\n     \"\"\"\n     Type Dict[str, AttentionMetadata] for v1, map from layer_name of each \n     attention layer to its attention metadata\n@@ -266,6 +267,7 @@ def create_forward_context(\n     cudagraph_runtime_mode: CUDAGraphMode = CUDAGraphMode.NONE,\n     batch_descriptor: BatchDescriptor | None = None,\n     ubatch_slices: UBatchSlices | None = None,\n+    slot_mapping: dict[str, torch.Tensor] | None = None,\n     additional_kwargs: dict[str, Any] | None = None,\n     skip_compiled: bool = False,\n ):\n@@ -282,6 +284,7 @@ def create_forward_context(\n         remaining_moe_layers=remaining_moe_layers,\n         virtual_engine=virtual_engine,\n         attn_metadata=attn_metadata,\n+        slot_mapping=slot_mapping or {},\n         dp_metadata=dp_metadata,\n         cudagraph_runtime_mode=cudagraph_runtime_mode,\n         batch_descriptor=batch_descriptor,\n@@ -316,6 +319,7 @@ def set_forward_context(\n     cudagraph_runtime_mode: CUDAGraphMode = CUDAGraphMode.NONE,\n     batch_descriptor: BatchDescriptor | None = None,\n     ubatch_slices: UBatchSlices | None = None,\n+    slot_mapping: dict[str, torch.Tensor] | list[dict[str, torch.Tensor]] | None = None,\n     skip_compiled: bool = False,\n ):\n     \"\"\"A context manager that stores the current forward context,\n@@ -374,6 +378,7 @@ def set_forward_context(\n         cudagraph_runtime_mode,\n         batch_descriptor,\n         ubatch_slices,\n+        slot_mapping,\n         additional_kwargs,\n         skip_compiled,\n     )", "patch_sha256": "4978c58d965f7339"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2690603477", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/forward_context.py\nReview Comment:\nYep agreed\n\nOriginal Code Context:\n@@ -239,12 +240,14 @@ def create_forward_context(\n     cudagraph_runtime_mode: CUDAGraphMode = CUDAGraphMode.NONE,\n     batch_descriptor: BatchDescriptor | None = None,\n     ubatch_slices: UBatchSlices | None = None,\n+    slot_mapping: dict[str, torch.Tensor] | list[dict[str, torch.Tensor]] | None = None,\n", "materials": ["Yep agreed"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -189,6 +189,7 @@ class ForwardContext:\n     # copy from vllm_config.compilation_config.static_forward_context\n     no_compile_layers: dict[str, Any]\n     attn_metadata: dict[str, AttentionMetadata] | list[dict[str, AttentionMetadata]]\n+    slot_mapping: dict[str, torch.Tensor] | list[dict[str, torch.Tensor]]\n     \"\"\"\n     Type Dict[str, AttentionMetadata] for v1, map from layer_name of each \n     attention layer to its attention metadata\n@@ -266,6 +267,7 @@ def create_forward_context(\n     cudagraph_runtime_mode: CUDAGraphMode = CUDAGraphMode.NONE,\n     batch_descriptor: BatchDescriptor | None = None,\n     ubatch_slices: UBatchSlices | None = None,\n+    slot_mapping: dict[str, torch.Tensor] | None = None,\n     additional_kwargs: dict[str, Any] | None = None,\n     skip_compiled: bool = False,\n ):\n@@ -282,6 +284,7 @@ def create_forward_context(\n         remaining_moe_layers=remaining_moe_layers,\n         virtual_engine=virtual_engine,\n         attn_metadata=attn_metadata,\n+        slot_mapping=slot_mapping or {},\n         dp_metadata=dp_metadata,\n         cudagraph_runtime_mode=cudagraph_runtime_mode,\n         batch_descriptor=batch_descriptor,\n@@ -316,6 +319,7 @@ def set_forward_context(\n     cudagraph_runtime_mode: CUDAGraphMode = CUDAGraphMode.NONE,\n     batch_descriptor: BatchDescriptor | None = None,\n     ubatch_slices: UBatchSlices | None = None,\n+    slot_mapping: dict[str, torch.Tensor] | list[dict[str, torch.Tensor]] | None = None,\n     skip_compiled: bool = False,\n ):\n     \"\"\"A context manager that stores the current forward context,\n@@ -374,6 +378,7 @@ def set_forward_context(\n         cudagraph_runtime_mode,\n         batch_descriptor,\n         ubatch_slices,\n+        slot_mapping,\n         additional_kwargs,\n         skip_compiled,\n     )", "patch_sha256": "4978c58d965f7339"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2718869450", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/spec_decode/eagle.py\nReview Comment:\nwhat is the point of `self._cached_slot_mapping_views`? appears to just a mirror of `self._slot_mapping_buffer`\n\nOriginal Code Context:\n@@ -234,19 +235,27 @@ def _set_positions(self, num_tokens: int, positions: torch.Tensor):\n                 positions = positions[0]\n             self.positions[:num_tokens] = positions\n \n-    def _build_slot_mapping_dict(\n-        self, slot_mapping: torch.Tensor, num_tokens: int\n+    def _get_slot_mapping(\n+        self,\n+        num_tokens: int,\n+        slot_mapping: torch.Tensor | None = None,\n     ) -> dict[str, torch.Tensor]:\n-        slot_len = slot_mapping.shape[0]\n-        self.slot_mapping_buffer[:slot_len].copy_(slot_mapping)\n-        slot_mapping_view = self.slot_mapping_buffer[:num_tokens]\n+        \"\"\"Return slot_mapping dict using cached view for cudagraph identity.\n \n-        result = {}\n-        for layer_name in self.attn_layer_names:\n-            result[layer_name] = slot_mapping_view\n-        for layer_name in self.indexer_layer_names:\n-            result[layer_name] = slot_mapping_view\n-        return result\n+        If slot_mapping is provided, copies it into the buffer first.\n+        \"\"\"\n+        if slot_mapping is not None:\n+            num_actual = slot_mapping.shape[0]\n+            self._slot_mapping_buffer[:num_actual].copy_(slot_mapping)\n+            if num_tokens > num_actual:\n+                self._slot_mapping_buffer[num_actual:num_tokens].fill_(PADDING_SLOT_ID)\n+\n+        if num_tokens not in self._cached_slot_mapping_views:\n+            self._cached_slot_mapping_views[num_tokens] = self._slot_mapping_buffer[\n", "materials": ["what is the point of `self._cached_slot_mapping_views`? appears to just a mirror of `self._slot_mapping_buffer`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -159,6 +159,10 @@ def __init__(\n             with_numpy=True,\n         )\n \n+        self._slot_mapping_buffer = torch.zeros(\n+            self.max_num_tokens, dtype=torch.int64, device=device\n+        )\n+\n         # Determine allowed attention backends once during initialization.\n         self.allowed_attn_types: tuple | None = None\n         if current_platform.is_rocm():\n@@ -230,6 +234,24 @@ def _set_positions(self, num_tokens: int, positions: torch.Tensor):\n                 positions = positions[0]\n             self.positions[:num_tokens] = positions\n \n+    def _get_slot_mapping(\n+        self,\n+        num_tokens: int,\n+        slot_mapping: torch.Tensor | None = None,\n+    ) -> dict[str, torch.Tensor]:\n+        \"\"\"Return slot_mapping dict for EAGLE layers.\n+\n+        If slot_mapping is provided, copies it into the buffer first.\n+        \"\"\"\n+        if slot_mapping is not None:\n+            num_actual = slot_mapping.shape[0]\n+            self._slot_mapping_buffer[:num_actual].copy_(slot_mapping)\n+            if num_tokens > num_actual:\n+                self._slot_mapping_buffer[num_actual:num_tokens].fill_(PADDING_SLOT_ID)\n+\n+        view = self._slot_mapping_buffer[:num_tokens]\n+        return {name: view for name in self.attn_layer_names + self.indexer_layer_names}\n+\n     def initialize_cudagraph_keys(self, cudagraph_mode: CUDAGraphMode) -> None:\n         \"\"\"Initialize cudagraph dispatcher keys for eagle.\n \n@@ -262,6 +284,9 @@ def propose(\n         sampling_metadata: SamplingMetadata,\n         mm_embed_inputs: tuple[list[torch.Tensor], torch.Tensor] | None = None,\n         num_rejected_tokens_gpu: torch.Tensor | None = None,\n+        slot_mappings: dict[str, torch.Tensor]\n+        | list[dict[str, torch.Tensor]]\n+        | None = None,\n     ) -> torch.Tensor:\n         batch_size = common_attn_metadata.batch_size()\n \n@@ -358,6 +383,9 @@ def propose(\n             num_tokens=num_input_tokens,\n             num_tokens_across_dp=num_tokens_across_dp,\n             cudagraph_runtime_mode=cudagraph_runtime_mode,\n+            slot_mapping=self._get_slot_mapping(\n+                num_input_tokens, common_attn_metadata.slot_mapping\n+            ),\n         ):\n             ret_hidden_states = self.model(**model_kwargs)\n             if not self.model_returns_tuple():\n@@ -396,6 +424,7 @@ def propose(\n                 positions=positions,\n                 hidden_states=hidden_states,\n                 common_attn_metadata=common_attn_metadata,\n+                slot_mappings=slot_mappings,\n             )\n             # [batch_size, num_tree_tokens]\n             return torch.cat(draft_token_ids_list, dim=1)\n@@ -553,6 +582,9 @@ def propose(\n                 num_tokens=input_batch_size,\n                 num_tokens_across_dp=batch_size_across_dp,\n                 cudagraph_runtime_mode=cudagraph_runtime_mode,\n+                slot_mapping=self._get_slot_mapping(\n+                    input_batch_size, common_attn_metadata.slot_mapping\n+                ),\n             ):\n                 ret_hidden_states = self.model(**model_kwargs)\n                 if not self.model_returns_tuple():\n@@ -760,6 +792,9 @@ def propose_tree(\n         # [num_tokens, hidden_size]\n         hidden_states: torch.Tensor,\n         common_attn_metadata: CommonAttentionMetadata,\n+        slot_mappings: dict[str, torch.Tensor]\n+        | list[dict[str, torch.Tensor]]\n+        | None = None,\n     ) -> list[torch.Tensor]:\n         tree_attn_metadata_builder = self.runner.attn_groups[0][\n             0\n@@ -881,6 +916,9 @@ def propose_tree(\n                 self.vllm_config,\n                 num_tokens=num_input_tokens,\n                 cudagraph_runtime_mode=cudagraph_runtime_mode,\n+                slot_mapping=self._get_slot_mapping(\n+                    num_input_tokens, attn_metadata.slot_mapping\n+                ),\n             ):\n                 last_hidden_states, hidden_states = self.model(\n                     input_ids=self.input_ids[:num_input_tokens],\n@@ -1212,6 +1250,7 @@ def dummy_run(\n         num_tokens: int,\n         use_cudagraphs: bool = True,\n         is_graph_capturing: bool = False,\n+        slot_mappings: dict[str, torch.Tensor] | None = None,\n     ) -> None:\n         # FIXME: when using tree-based specdec, adjust number of forward-passes\n         # according to the depth of the tree.\n@@ -1233,12 +1272,23 @@ def dummy_run(\n                 if num_tokens_across_dp is not None:\n                     num_tokens_across_dp[self.dp_rank] = num_input_tokens\n \n+            # Make sure to use EAGLE's own buffer during cudagraph capture.\n+            if (\n+                self.attn_layer_names\n+                and slot_mappings is not None\n+                and self.attn_layer_names[0] in slot_mappings\n+            ):\n+                slot_mapping_dict = self._get_slot_mapping(num_input_tokens)\n+            else:\n+                slot_mapping_dict = slot_mappings or {}\n+\n             with set_forward_context(\n                 None,\n                 self.vllm_config,\n                 num_tokens=num_input_tokens,\n                 num_tokens_across_dp=num_tokens_across_dp,\n                 cudagraph_runtime_mode=cudagraph_runtime_mode,\n+                slot_mapping=slot_mapping_dict,\n             ):\n                 if self.supports_mm_inputs:\n                     input_ids = None", "patch_sha256": "57006f29e83b7035"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2718869632", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/spec_decode/eagle.py\nReview Comment:\ncan you please add a comment here explaining this? \n\nOriginal Code Context:\n@@ -1264,24 +1273,23 @@ def dummy_run(\n                 if num_tokens_across_dp is not None:\n                     num_tokens_across_dp[self.dp_rank] = num_input_tokens\n \n-            if is_graph_capturing and self.attn_layer_names:\n-                slot_mapping_view = self.slot_mapping_buffer[:num_input_tokens]\n-                slot_mapping_dict: dict[str, torch.Tensor] = {}\n-                for layer_name in self.attn_layer_names:\n-                    slot_mapping_dict[layer_name] = slot_mapping_view\n-                for layer_name in self.indexer_layer_names:\n-                    slot_mapping_dict[layer_name] = slot_mapping_view\n+            use_eagle_buffer = (\n+                self.attn_layer_names\n+                and slot_mappings is not None\n+                and self.attn_layer_names[0] in slot_mappings\n+            )\n", "materials": ["can you please add a comment here explaining this? "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -159,6 +159,10 @@ def __init__(\n             with_numpy=True,\n         )\n \n+        self._slot_mapping_buffer = torch.zeros(\n+            self.max_num_tokens, dtype=torch.int64, device=device\n+        )\n+\n         # Determine allowed attention backends once during initialization.\n         self.allowed_attn_types: tuple | None = None\n         if current_platform.is_rocm():\n@@ -230,6 +234,24 @@ def _set_positions(self, num_tokens: int, positions: torch.Tensor):\n                 positions = positions[0]\n             self.positions[:num_tokens] = positions\n \n+    def _get_slot_mapping(\n+        self,\n+        num_tokens: int,\n+        slot_mapping: torch.Tensor | None = None,\n+    ) -> dict[str, torch.Tensor]:\n+        \"\"\"Return slot_mapping dict for EAGLE layers.\n+\n+        If slot_mapping is provided, copies it into the buffer first.\n+        \"\"\"\n+        if slot_mapping is not None:\n+            num_actual = slot_mapping.shape[0]\n+            self._slot_mapping_buffer[:num_actual].copy_(slot_mapping)\n+            if num_tokens > num_actual:\n+                self._slot_mapping_buffer[num_actual:num_tokens].fill_(PADDING_SLOT_ID)\n+\n+        view = self._slot_mapping_buffer[:num_tokens]\n+        return {name: view for name in self.attn_layer_names + self.indexer_layer_names}\n+\n     def initialize_cudagraph_keys(self, cudagraph_mode: CUDAGraphMode) -> None:\n         \"\"\"Initialize cudagraph dispatcher keys for eagle.\n \n@@ -262,6 +284,9 @@ def propose(\n         sampling_metadata: SamplingMetadata,\n         mm_embed_inputs: tuple[list[torch.Tensor], torch.Tensor] | None = None,\n         num_rejected_tokens_gpu: torch.Tensor | None = None,\n+        slot_mappings: dict[str, torch.Tensor]\n+        | list[dict[str, torch.Tensor]]\n+        | None = None,\n     ) -> torch.Tensor:\n         batch_size = common_attn_metadata.batch_size()\n \n@@ -358,6 +383,9 @@ def propose(\n             num_tokens=num_input_tokens,\n             num_tokens_across_dp=num_tokens_across_dp,\n             cudagraph_runtime_mode=cudagraph_runtime_mode,\n+            slot_mapping=self._get_slot_mapping(\n+                num_input_tokens, common_attn_metadata.slot_mapping\n+            ),\n         ):\n             ret_hidden_states = self.model(**model_kwargs)\n             if not self.model_returns_tuple():\n@@ -396,6 +424,7 @@ def propose(\n                 positions=positions,\n                 hidden_states=hidden_states,\n                 common_attn_metadata=common_attn_metadata,\n+                slot_mappings=slot_mappings,\n             )\n             # [batch_size, num_tree_tokens]\n             return torch.cat(draft_token_ids_list, dim=1)\n@@ -553,6 +582,9 @@ def propose(\n                 num_tokens=input_batch_size,\n                 num_tokens_across_dp=batch_size_across_dp,\n                 cudagraph_runtime_mode=cudagraph_runtime_mode,\n+                slot_mapping=self._get_slot_mapping(\n+                    input_batch_size, common_attn_metadata.slot_mapping\n+                ),\n             ):\n                 ret_hidden_states = self.model(**model_kwargs)\n                 if not self.model_returns_tuple():\n@@ -760,6 +792,9 @@ def propose_tree(\n         # [num_tokens, hidden_size]\n         hidden_states: torch.Tensor,\n         common_attn_metadata: CommonAttentionMetadata,\n+        slot_mappings: dict[str, torch.Tensor]\n+        | list[dict[str, torch.Tensor]]\n+        | None = None,\n     ) -> list[torch.Tensor]:\n         tree_attn_metadata_builder = self.runner.attn_groups[0][\n             0\n@@ -881,6 +916,9 @@ def propose_tree(\n                 self.vllm_config,\n                 num_tokens=num_input_tokens,\n                 cudagraph_runtime_mode=cudagraph_runtime_mode,\n+                slot_mapping=self._get_slot_mapping(\n+                    num_input_tokens, attn_metadata.slot_mapping\n+                ),\n             ):\n                 last_hidden_states, hidden_states = self.model(\n                     input_ids=self.input_ids[:num_input_tokens],\n@@ -1212,6 +1250,7 @@ def dummy_run(\n         num_tokens: int,\n         use_cudagraphs: bool = True,\n         is_graph_capturing: bool = False,\n+        slot_mappings: dict[str, torch.Tensor] | None = None,\n     ) -> None:\n         # FIXME: when using tree-based specdec, adjust number of forward-passes\n         # according to the depth of the tree.\n@@ -1233,12 +1272,23 @@ def dummy_run(\n                 if num_tokens_across_dp is not None:\n                     num_tokens_across_dp[self.dp_rank] = num_input_tokens\n \n+            # Make sure to use EAGLE's own buffer during cudagraph capture.\n+            if (\n+                self.attn_layer_names\n+                and slot_mappings is not None\n+                and self.attn_layer_names[0] in slot_mappings\n+            ):\n+                slot_mapping_dict = self._get_slot_mapping(num_input_tokens)\n+            else:\n+                slot_mapping_dict = slot_mappings or {}\n+\n             with set_forward_context(\n                 None,\n                 self.vllm_config,\n                 num_tokens=num_input_tokens,\n                 num_tokens_across_dp=num_tokens_across_dp,\n                 cudagraph_runtime_mode=cudagraph_runtime_mode,\n+                slot_mapping=slot_mapping_dict,\n             ):\n                 if self.supports_mm_inputs:\n                     input_ids = None", "patch_sha256": "57006f29e83b7035"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2718874197", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/attention/backends/flash_attn.py\nReview Comment:\nnit: `forward_includes_kv_cache` -> `forward_includes_kv_cache_update`\n\nOriginal Code Context:\n@@ -79,6 +79,8 @@ def get_supported_kernel_block_sizes() -> list[int | MultipleOf]:\n             return [16, 32, 64]\n         return [MultipleOf(16)]\n \n+    forward_includes_kv_cache: bool = False\n", "materials": ["nit: `forward_includes_kv_cache` -> `forward_includes_kv_cache_update`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -79,6 +79,8 @@ def get_supported_kernel_block_sizes() -> list[int | MultipleOf]:\n             return [16, 32, 64]\n         return [MultipleOf(16)]\n \n+    forward_includes_kv_cache_update: bool = False\n+\n     @staticmethod\n     def get_name() -> str:\n         return \"FLASH_ATTN\"\n@@ -652,32 +654,6 @@ def forward(\n         # For decoder and cross-attention, use KV cache as before\n         key_cache, value_cache = kv_cache.unbind(0)\n \n-        # key and value may be None in the case of cross attention. They are\n-        # calculated once based on the output from the encoder and then cached\n-        # in KV cache.\n-        if (\n-            self.kv_sharing_target_layer_name is None\n-            and key is not None\n-            and value is not None\n-        ):\n-            # Reshape the input keys and values and store them in the cache.\n-            # Skip this if sharing KV cache with an earlier attention layer.\n-            # NOTE(woosuk): Here, key and value are padded while slot_mapping is\n-            # not padded. However, we don't need to do key[:num_actual_tokens]\n-            # and value[:num_actual_tokens] because the reshape_and_cache_flash\n-            # op uses the slot_mapping's shape to determine the number of\n-            # actual tokens.\n-            reshape_and_cache_flash(\n-                key,\n-                value,\n-                key_cache,\n-                value_cache,\n-                attn_metadata.slot_mapping,\n-                self.kv_cache_dtype,\n-                layer._k_scale,\n-                layer._v_scale,\n-            )\n-\n         if self.kv_cache_dtype.startswith(\"fp8\"):\n             # queries are quantized in the attention layer\n             dtype = FlashAttentionBackend.get_fp8_dtype_for_flashattn(\n@@ -774,6 +750,49 @@ def forward(\n         )\n         return output\n \n+    def do_kv_cache_update(\n+        self,\n+        layer: torch.nn.Module,\n+        key: torch.Tensor,\n+        value: torch.Tensor,\n+        kv_cache: torch.Tensor,\n+        slot_mapping: torch.Tensor,\n+    ) -> None:\n+        if self.attn_type in (AttentionType.ENCODER_ONLY, AttentionType.ENCODER):\n+            # For encoder attention,\n+            # we use direct Q, K, V tensors without caching\n+            return\n+\n+        # key and value may be None in the case of cross attention. They are\n+        # calculated once based on the output from the encoder and then cached\n+        # in KV cache.\n+        if (\n+            self.kv_sharing_target_layer_name is not None\n+            or key is None\n+            or value is None\n+        ):\n+            return\n+\n+        key_cache, value_cache = kv_cache.unbind(0)\n+\n+        # Reshape the input keys and values and store them in the cache.\n+        # Skip this if sharing KV cache with an earlier attention layer.\n+        # NOTE(woosuk): Here, key and value are padded while slot_mapping is\n+        # not padded. However, we don't need to do key[:num_actual_tokens]\n+        # and value[:num_actual_tokens] because the reshape_and_cache_flash\n+        # op uses the slot_mapping's shape to determine the number of\n+        # actual tokens.\n+        reshape_and_cache_flash(\n+            key,\n+            value,\n+            key_cache,\n+            value_cache,\n+            slot_mapping,\n+            self.kv_cache_dtype,\n+            layer._k_scale,\n+            layer._v_scale,\n+        )\n+\n     def _forward_with_dcp(\n         self,\n         query: torch.Tensor,", "patch_sha256": "5824c8edc9763307"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2718979788", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/attention/backends/flash_attn.py\nReview Comment:\nDone in [238ae7e](https://github.com/vllm-project/vllm/pull/25954/commits/238ae7e0fcc3d9e28e4a685c2e18da35698777d0)\n\nOriginal Code Context:\n@@ -79,6 +79,8 @@ def get_supported_kernel_block_sizes() -> list[int | MultipleOf]:\n             return [16, 32, 64]\n         return [MultipleOf(16)]\n \n+    forward_includes_kv_cache: bool = False\n", "materials": ["Done in [238ae7e](https://github.com/vllm-project/vllm/pull/25954/commits/238ae7e0fcc3d9e28e4a685c2e18da35698777d0)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -79,6 +79,8 @@ def get_supported_kernel_block_sizes() -> list[int | MultipleOf]:\n             return [16, 32, 64]\n         return [MultipleOf(16)]\n \n+    forward_includes_kv_cache_update: bool = False\n+\n     @staticmethod\n     def get_name() -> str:\n         return \"FLASH_ATTN\"\n@@ -652,32 +654,6 @@ def forward(\n         # For decoder and cross-attention, use KV cache as before\n         key_cache, value_cache = kv_cache.unbind(0)\n \n-        # key and value may be None in the case of cross attention. They are\n-        # calculated once based on the output from the encoder and then cached\n-        # in KV cache.\n-        if (\n-            self.kv_sharing_target_layer_name is None\n-            and key is not None\n-            and value is not None\n-        ):\n-            # Reshape the input keys and values and store them in the cache.\n-            # Skip this if sharing KV cache with an earlier attention layer.\n-            # NOTE(woosuk): Here, key and value are padded while slot_mapping is\n-            # not padded. However, we don't need to do key[:num_actual_tokens]\n-            # and value[:num_actual_tokens] because the reshape_and_cache_flash\n-            # op uses the slot_mapping's shape to determine the number of\n-            # actual tokens.\n-            reshape_and_cache_flash(\n-                key,\n-                value,\n-                key_cache,\n-                value_cache,\n-                attn_metadata.slot_mapping,\n-                self.kv_cache_dtype,\n-                layer._k_scale,\n-                layer._v_scale,\n-            )\n-\n         if self.kv_cache_dtype.startswith(\"fp8\"):\n             # queries are quantized in the attention layer\n             dtype = FlashAttentionBackend.get_fp8_dtype_for_flashattn(\n@@ -774,6 +750,49 @@ def forward(\n         )\n         return output\n \n+    def do_kv_cache_update(\n+        self,\n+        layer: torch.nn.Module,\n+        key: torch.Tensor,\n+        value: torch.Tensor,\n+        kv_cache: torch.Tensor,\n+        slot_mapping: torch.Tensor,\n+    ) -> None:\n+        if self.attn_type in (AttentionType.ENCODER_ONLY, AttentionType.ENCODER):\n+            # For encoder attention,\n+            # we use direct Q, K, V tensors without caching\n+            return\n+\n+        # key and value may be None in the case of cross attention. They are\n+        # calculated once based on the output from the encoder and then cached\n+        # in KV cache.\n+        if (\n+            self.kv_sharing_target_layer_name is not None\n+            or key is None\n+            or value is None\n+        ):\n+            return\n+\n+        key_cache, value_cache = kv_cache.unbind(0)\n+\n+        # Reshape the input keys and values and store them in the cache.\n+        # Skip this if sharing KV cache with an earlier attention layer.\n+        # NOTE(woosuk): Here, key and value are padded while slot_mapping is\n+        # not padded. However, we don't need to do key[:num_actual_tokens]\n+        # and value[:num_actual_tokens] because the reshape_and_cache_flash\n+        # op uses the slot_mapping's shape to determine the number of\n+        # actual tokens.\n+        reshape_and_cache_flash(\n+            key,\n+            value,\n+            key_cache,\n+            value_cache,\n+            slot_mapping,\n+            self.kv_cache_dtype,\n+            layer._k_scale,\n+            layer._v_scale,\n+        )\n+\n     def _forward_with_dcp(\n         self,\n         query: torch.Tensor,", "patch_sha256": "5824c8edc9763307"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2719002781", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/spec_decode/eagle.py\nReview Comment:\nDone in [de1e5dc](https://github.com/vllm-project/vllm/pull/25954/commits/de1e5dc52532654fecb9dc6edaeee5399f8f15f2)\n\nOriginal Code Context:\n@@ -1264,24 +1273,23 @@ def dummy_run(\n                 if num_tokens_across_dp is not None:\n                     num_tokens_across_dp[self.dp_rank] = num_input_tokens\n \n-            if is_graph_capturing and self.attn_layer_names:\n-                slot_mapping_view = self.slot_mapping_buffer[:num_input_tokens]\n-                slot_mapping_dict: dict[str, torch.Tensor] = {}\n-                for layer_name in self.attn_layer_names:\n-                    slot_mapping_dict[layer_name] = slot_mapping_view\n-                for layer_name in self.indexer_layer_names:\n-                    slot_mapping_dict[layer_name] = slot_mapping_view\n+            use_eagle_buffer = (\n+                self.attn_layer_names\n+                and slot_mappings is not None\n+                and self.attn_layer_names[0] in slot_mappings\n+            )\n", "materials": ["Done in [de1e5dc](https://github.com/vllm-project/vllm/pull/25954/commits/de1e5dc52532654fecb9dc6edaeee5399f8f15f2)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -159,6 +159,10 @@ def __init__(\n             with_numpy=True,\n         )\n \n+        self._slot_mapping_buffer = torch.zeros(\n+            self.max_num_tokens, dtype=torch.int64, device=device\n+        )\n+\n         # Determine allowed attention backends once during initialization.\n         self.allowed_attn_types: tuple | None = None\n         if current_platform.is_rocm():\n@@ -230,6 +234,24 @@ def _set_positions(self, num_tokens: int, positions: torch.Tensor):\n                 positions = positions[0]\n             self.positions[:num_tokens] = positions\n \n+    def _get_slot_mapping(\n+        self,\n+        num_tokens: int,\n+        slot_mapping: torch.Tensor | None = None,\n+    ) -> dict[str, torch.Tensor]:\n+        \"\"\"Return slot_mapping dict for EAGLE layers.\n+\n+        If slot_mapping is provided, copies it into the buffer first.\n+        \"\"\"\n+        if slot_mapping is not None:\n+            num_actual = slot_mapping.shape[0]\n+            self._slot_mapping_buffer[:num_actual].copy_(slot_mapping)\n+            if num_tokens > num_actual:\n+                self._slot_mapping_buffer[num_actual:num_tokens].fill_(PADDING_SLOT_ID)\n+\n+        view = self._slot_mapping_buffer[:num_tokens]\n+        return {name: view for name in self.attn_layer_names + self.indexer_layer_names}\n+\n     def initialize_cudagraph_keys(self, cudagraph_mode: CUDAGraphMode) -> None:\n         \"\"\"Initialize cudagraph dispatcher keys for eagle.\n \n@@ -262,6 +284,9 @@ def propose(\n         sampling_metadata: SamplingMetadata,\n         mm_embed_inputs: tuple[list[torch.Tensor], torch.Tensor] | None = None,\n         num_rejected_tokens_gpu: torch.Tensor | None = None,\n+        slot_mappings: dict[str, torch.Tensor]\n+        | list[dict[str, torch.Tensor]]\n+        | None = None,\n     ) -> torch.Tensor:\n         batch_size = common_attn_metadata.batch_size()\n \n@@ -358,6 +383,9 @@ def propose(\n             num_tokens=num_input_tokens,\n             num_tokens_across_dp=num_tokens_across_dp,\n             cudagraph_runtime_mode=cudagraph_runtime_mode,\n+            slot_mapping=self._get_slot_mapping(\n+                num_input_tokens, common_attn_metadata.slot_mapping\n+            ),\n         ):\n             ret_hidden_states = self.model(**model_kwargs)\n             if not self.model_returns_tuple():\n@@ -396,6 +424,7 @@ def propose(\n                 positions=positions,\n                 hidden_states=hidden_states,\n                 common_attn_metadata=common_attn_metadata,\n+                slot_mappings=slot_mappings,\n             )\n             # [batch_size, num_tree_tokens]\n             return torch.cat(draft_token_ids_list, dim=1)\n@@ -553,6 +582,9 @@ def propose(\n                 num_tokens=input_batch_size,\n                 num_tokens_across_dp=batch_size_across_dp,\n                 cudagraph_runtime_mode=cudagraph_runtime_mode,\n+                slot_mapping=self._get_slot_mapping(\n+                    input_batch_size, common_attn_metadata.slot_mapping\n+                ),\n             ):\n                 ret_hidden_states = self.model(**model_kwargs)\n                 if not self.model_returns_tuple():\n@@ -760,6 +792,9 @@ def propose_tree(\n         # [num_tokens, hidden_size]\n         hidden_states: torch.Tensor,\n         common_attn_metadata: CommonAttentionMetadata,\n+        slot_mappings: dict[str, torch.Tensor]\n+        | list[dict[str, torch.Tensor]]\n+        | None = None,\n     ) -> list[torch.Tensor]:\n         tree_attn_metadata_builder = self.runner.attn_groups[0][\n             0\n@@ -881,6 +916,9 @@ def propose_tree(\n                 self.vllm_config,\n                 num_tokens=num_input_tokens,\n                 cudagraph_runtime_mode=cudagraph_runtime_mode,\n+                slot_mapping=self._get_slot_mapping(\n+                    num_input_tokens, attn_metadata.slot_mapping\n+                ),\n             ):\n                 last_hidden_states, hidden_states = self.model(\n                     input_ids=self.input_ids[:num_input_tokens],\n@@ -1212,6 +1250,7 @@ def dummy_run(\n         num_tokens: int,\n         use_cudagraphs: bool = True,\n         is_graph_capturing: bool = False,\n+        slot_mappings: dict[str, torch.Tensor] | None = None,\n     ) -> None:\n         # FIXME: when using tree-based specdec, adjust number of forward-passes\n         # according to the depth of the tree.\n@@ -1233,12 +1272,23 @@ def dummy_run(\n                 if num_tokens_across_dp is not None:\n                     num_tokens_across_dp[self.dp_rank] = num_input_tokens\n \n+            # Make sure to use EAGLE's own buffer during cudagraph capture.\n+            if (\n+                self.attn_layer_names\n+                and slot_mappings is not None\n+                and self.attn_layer_names[0] in slot_mappings\n+            ):\n+                slot_mapping_dict = self._get_slot_mapping(num_input_tokens)\n+            else:\n+                slot_mapping_dict = slot_mappings or {}\n+\n             with set_forward_context(\n                 None,\n                 self.vllm_config,\n                 num_tokens=num_input_tokens,\n                 num_tokens_across_dp=num_tokens_across_dp,\n                 cudagraph_runtime_mode=cudagraph_runtime_mode,\n+                slot_mapping=slot_mapping_dict,\n             ):\n                 if self.supports_mm_inputs:\n                     input_ids = None", "patch_sha256": "57006f29e83b7035"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#26291/review_2670411798", "source": "CodeBench", "source_task_id": "vllm-project/vllm#26291", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/entrypoints/openai/chat_completion/serving.py\nReview Comment:\nYes, that's correct. I can revert this changes if you find them unnecessary\n\nOriginal Code Context:\n@@ -1127,17 +1135,23 @@ async def chat_completion_stream_generator(\n \n                     # Log streaming delta if output logging is enabled\n                     if self.enable_log_outputs and self.request_logger:\n-                        delta_content = \"\"\n+                        delta_content_parts = []\n                         if delta_message.content:\n-                            delta_content = delta_message.content\n-                        elif delta_message.tool_calls:\n-                            delta_content = \"\".join(\n+                            delta_content_parts.append(delta_message.content)\n+                        if delta_message.reasoning_content:\n+                            reasoning = delta_message.reasoning_content\n+                            delta_content_parts.append(f\"[reasoning: {reasoning}]\")\n+                        if delta_message.tool_calls:\n+                            tool_args = \"\".join(\n                                 tc.function.arguments\n                                 for tc in delta_message.tool_calls\n                                 if tc.function and tc.function.arguments\n                             )\n+                            if tool_args:\n+                                delta_content_parts.append(f\"[tool_calls: {tool_args}]\")\n \n-                        if delta_content and not self.exclude_log_deltas:\n+                        if delta_content_parts and not self.exclude_log_deltas:\n+                            delta_content = \" \".join(delta_content_parts)\n", "materials": ["Yes, that's correct. I can revert this changes if you find them unnecessary"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -36,6 +36,7 @@\n     ChatMessage,\n )\n from vllm.entrypoints.openai.chat_completion.stream_harmony import (\n+    TokenState,\n     extract_harmony_streaming_delta,\n )\n from vllm.entrypoints.openai.engine.protocol import (\n@@ -826,12 +827,22 @@ async def chat_completion_stream_generator(\n                     if self.use_harmony:\n                         harmony_parser = harmony_parsers[i]\n                         prev_recipient = harmony_parser.current_recipient\n-                        delta_text = \"\"\n+\n+                        # Track accumulated content per token with their state\n+                        token_states: list[TokenState] = []\n                         for token_id in output.token_ids:\n                             harmony_parser.process(token_id)\n-                            delta_text += harmony_parser.last_content_delta or \"\"\n+                            token_delta = harmony_parser.last_content_delta or \"\"\n+                            token_states.append(\n+                                TokenState(\n+                                    harmony_parser.current_channel,\n+                                    harmony_parser.current_recipient,\n+                                    token_delta,\n+                                )\n+                            )\n+                        delta_text = \"\".join(delta for _, _, delta in token_states)\n                         cur_channel = harmony_parser.current_channel\n-                        cur_recipient = harmony_parser.current_recipient\n+\n                         # handle the case where several tokens where generated at once\n                         # including the final token, leading to a delta in the text\n                         # but the current channel to be empty (start state)\n@@ -869,10 +880,8 @@ async def chat_completion_stream_generator(\n                         delta_message, tools_streamed_flag = (\n                             extract_harmony_streaming_delta(\n                                 harmony_parser=harmony_parser,\n-                                cur_channel=cur_channel,\n-                                cur_recipient=cur_recipient,\n+                                token_states=token_states,\n                                 prev_recipient=prev_recipient,\n-                                delta_text=delta_text,\n                                 include_reasoning=request.include_reasoning,\n                             )\n                         )\n@@ -1139,17 +1148,23 @@ async def chat_completion_stream_generator(\n \n                     # Log streaming delta if output logging is enabled\n                     if self.enable_log_outputs and self.request_logger:\n-                        delta_content = \"\"\n+                        delta_content_parts = []\n                         if delta_message.content:\n-                            delta_content = delta_message.content\n-                        elif delta_message.tool_calls:\n-                            delta_content = \"\".join(\n+                            delta_content_parts.append(delta_message.content)\n+                        if delta_message.reasoning_content:\n+                            reasoning = delta_message.reasoning_content\n+                            delta_content_parts.append(f\"[reasoning: {reasoning}]\")\n+                        if delta_message.tool_calls:\n+                            tool_args = \"\".join(\n                                 tc.function.arguments\n                                 for tc in delta_message.tool_calls\n                                 if tc.function and tc.function.arguments\n                             )\n+                            if tool_args:\n+                                delta_content_parts.append(f\"[tool_calls: {tool_args}]\")\n \n-                        if delta_content and self.enable_log_deltas:\n+                        if delta_content_parts and self.enable_log_deltas:\n+                            delta_content = \" \".join(delta_content_parts)\n                             self.request_logger.log_outputs(\n                                 request_id=request_id,\n                                 outputs=delta_content,", "patch_sha256": "25ad65935b5ed33e"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#26822/review_2429785071", "source": "CodeBench", "source_task_id": "vllm-project/vllm#26822", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/sampling_params.py\nReview Comment:\n![critical](https://www.gstatic.com/codereviewagent/critical.svg)\n\nThis new validation is a great improvement for ensuring `StructuredOutputsParams` is always in a valid state. However, it introduces a potential regression for deprecated parameters.\n\nSpecifically, the logic for handling deprecated `guided_*` parameters in `vllm/entrypoints/openai/protocol.py` (lines 794-806) can now raise this `ValueError`. If a user provides only `guided_whitespace_pattern` (which maps to `whitespace_pattern`), the code will attempt to create `StructuredOutputsParams` with only a non-constraint parameter. This will cause `count` to be 0 here, triggering this error.\n\nWhile the problematic code is not in this diff, this change makes it faulty. To prevent this regression, the logic for handling deprecated parameters should be updated to only construct `StructuredOutputsParams` if at least one *constraint* parameter (e.g., `guided_json`, `guided_regex`) is provided.\n\nFor example, in `ChatCompletionRequest.to_sampling_params` in `vllm/entrypoints/openai/protocol.py`, the logic could be adjusted:\n\n```python\n# ... inside to_sampling_params, after collecting kwargs from deprecated params\n            kwargs = {k: v for k, v in kwargs.items() if v is not None}\n            constraint_keys = {'json', 'regex', 'choice', 'grammar', 'structural_tag'}\n            if any(k in constraint_keys for k in kwargs):\n                self.structured_outputs = StructuredOutputsParams(**kwargs)\n```\n\nThis would ensure backward compatibility for the deprecated parameters while upholding the new, stricter validation.\n\nOriginal Code Context:\n@@ -58,13 +58,19 @@ def __post_init__(self):\n                 self.choice is not None,\n                 self.grammar is not None,\n                 self.json_object is not None,\n+                self.structural_tag is not None,\n             ]\n         )\n         if count > 1:\n             raise ValueError(\n                 \"You can only use one kind of structured outputs constraint \"\n                 f\"but multiple are specified: {self.__dict__}\"\n             )\n+        if count < 1:\n+            raise ValueError(\n+                \"You must use one kind of structured outputs constraint \"\n+                f\"but none are specified: {self.__dict__}\"\n+            )\n", "materials": ["![critical](https://www.gstatic.com/codereviewagent/critical.svg)\n\nThis new validation is a great improvement for ensuring `StructuredOutputsParams` is always in a valid state. However, it introduces a potential regression for deprecated parameters.\n\nSpecifically, the logic for handling deprecated `guided_*` parameters in `vllm/entrypoints/openai/protocol.py` (lines 794-806) can now raise this `ValueError`. If a user provides only `guided_whitespace_pattern` (which maps to `whitespace_pattern`), the code will attempt to create `StructuredOutputsParams` with only a non-constraint parameter. This will cause `count` to be 0 here, triggering this error.\n\nWhile the problematic code is not in this diff, this change makes it faulty. To prevent this regression, the logic for handling deprecated parameters should be updated to only construct `StructuredOutputsParams` if at least one *constraint* parameter (e.g., `guided_json`, `guided_regex`) is provided.\n\nFor example, in `ChatCompletionRequest.to_sampling_params` in `vllm/entrypoints/openai/protocol.py`, the logic could be adjusted:\n\n```python\n# ... inside to_sampling_params, after collecting kwargs from deprecated params\n            kwargs = {k: v for k, v in kwargs.items() if v is not None}\n            constraint_keys = {'json', 'regex', 'choice', 'grammar', 'structural_tag'}\n            if any(k in constraint_keys for k in kwargs):\n                self.structured_outputs = StructuredOutputsParams(**kwargs)\n```\n\nThis would ensure backward compatibility for the deprecated parameters while upholding the new, stricter validation."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -67,6 +67,11 @@ def __post_init__(self):\n                 \"You can only use one kind of structured outputs constraint \"\n                 f\"but multiple are specified: {self.__dict__}\"\n             )\n+        if count < 1:\n+            raise ValueError(\n+                \"You must use one kind of structured outputs constraint \"\n+                f\"but none are specified: {self.__dict__}\"\n+            )\n \n     def all_constraints_none(self) -> bool:\n         \"\"\"", "patch_sha256": "5ac6d87ae9b4d839"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#26822/review_2429802250", "source": "CodeBench", "source_task_id": "vllm-project/vllm#26822", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/sampling_params.py\nReview Comment:\nAFAIK `guided_whitespace_pattern` must be given with one of the structural constraints, otherwise the same no structured parameter error is raised (so it is not a newly introduced regression but pre-existing bug).\r\n\r\nThough I'd welcome a better way to validate the structured outputs params.\n\nOriginal Code Context:\n@@ -58,13 +58,19 @@ def __post_init__(self):\n                 self.choice is not None,\n                 self.grammar is not None,\n                 self.json_object is not None,\n+                self.structural_tag is not None,\n             ]\n         )\n         if count > 1:\n             raise ValueError(\n                 \"You can only use one kind of structured outputs constraint \"\n                 f\"but multiple are specified: {self.__dict__}\"\n             )\n+        if count < 1:\n+            raise ValueError(\n+                \"You must use one kind of structured outputs constraint \"\n+                f\"but none are specified: {self.__dict__}\"\n+            )\n", "materials": ["AFAIK `guided_whitespace_pattern` must be given with one of the structural constraints, otherwise the same no structured parameter error is raised (so it is not a newly introduced regression but pre-existing bug).\r\n\r\nThough I'd welcome a better way to validate the structured outputs params."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -67,6 +67,11 @@ def __post_init__(self):\n                 \"You can only use one kind of structured outputs constraint \"\n                 f\"but multiple are specified: {self.__dict__}\"\n             )\n+        if count < 1:\n+            raise ValueError(\n+                \"You must use one kind of structured outputs constraint \"\n+                f\"but none are specified: {self.__dict__}\"\n+            )\n \n     def all_constraints_none(self) -> bool:\n         \"\"\"", "patch_sha256": "5ac6d87ae9b4d839"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#26822/review_2686702565", "source": "CodeBench", "source_task_id": "vllm-project/vllm#26822", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/tool_parsers/abstract_tool_parser.py\nReview Comment:\n### Missing response_format clearing breaks tool override behavior\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe refactoring removed the line `request.response_format = None` that was present in the old code. The comment explicitly states that \"tool_choice: 'Forced Function' or 'required' will override structured output json settings to make tool calling work correctly.\" Without clearing `response_format`, when a user specifies both tools with forced/required choice AND a `response_format` (like `json_object`), the `to_sampling_params` method will later attempt to merge both constraints via `replace()`. This triggers the new validation in `StructuredOutputsParams.__post_init__` that raises a ValueError when multiple constraints are specified, causing unexpected failures for valid use cases that worked before.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/tool_parsers/abstract_tool_parser.py#L67-L70\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..B5jtGQFXS85bu1T1d36hCsxNc8OtdhVit9ydLXgvrgBHiU_auFFgDZ_3Jt2Wo5nYG_B5P6qqPPnCAlNkm728Sx0ZS2OrJguo2NB259kw_bTmNndqKtrUFVpxu1nk4HU0L9puQzKJ34NyXlrmY_I9OjHTS9SS-BFFanf7K0bBLy1KfGyUlhGG5P70d3CD7MiB7hnGp_JrGVLh6wwmDH4U1vASWFYrUMOfWzTrqZLnhu2j7SPv9Kczt28HczkXBiLMX9vB02hGAiJo6rvjpVwZOV3B3UaIwB9oX_rO_TCD0sbAEPDZKYxIpDG7LuUlnqiuOmfXweVKa65xRwDoX-VLIA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..w7gEsaXhMRnll1TtrEaKwaSpxs0G_xLjHNIlyZyQkzwQz7l9_1Rw8J0A5k4CTrRiRCTvC3FEeEK5QQSMDgyNEAFCQw-6Tb6aVk4mqiILAzlsqLD89Y6IiecXxfkYE_MSpOMwv01INFZH6VGbizmANUr01n2ZuodJajOUHF2y1XJn4__s0uVBPaJkUzMmViXhl0RLicQouDXaQ3-ibXl2_QJ7mmHyqE0poWLd9HVmKMhL3Nwem0xhlAIDuxGLM4WMTlYOZQPsnSpC3eP8stxFgdE2WJZL1yEISxq6e8Och1kCeaCSeXxFcZgksIBEmyl_YxoAzpYDZd0_titJ5J9yDg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -63,11 +63,11 @@ def adjust_request(self, request: ChatCompletionRequest) -> ChatCompletionReques\n         # Set structured output params for tool calling\n         if json_schema_from_tool is not None:\n             if isinstance(request, ChatCompletionRequest):\n-                request.structured_outputs = StructuredOutputsParams()\n                 # tool_choice: \"Forced Function\" or \"required\" will override\n                 # structured output json settings to make tool calling work correctly\n-                request.structured_outputs.json = json_schema_from_tool\n-                request.response_format = None\n+                request.structured_outputs = StructuredOutputsParams(\n+                    json=json_schema_from_tool\n+                )\n", "materials": ["### Missing response_format clearing breaks tool override behavior\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe refactoring removed the line `request.response_format = None` that was present in the old code. The comment explicitly states that \"tool_choice: 'Forced Function' or 'required' will override structured output json settings to make tool calling work correctly.\" Without clearing `response_format`, when a user specifies both tools with forced/required choice AND a `response_format` (like `json_object`), the `to_sampling_params` method will later attempt to merge both constraints via `replace()`. This triggers the new validation in `StructuredOutputsParams.__post_init__` that raises a ValueError when multiple constraints are specified, causing unexpected failures for valid use cases that worked before.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/tool_parsers/abstract_tool_parser.py#L67-L70\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..B5jtGQFXS85bu1T1d36hCsxNc8OtdhVit9ydLXgvrgBHiU_auFFgDZ_3Jt2Wo5nYG_B5P6qqPPnCAlNkm728Sx0ZS2OrJguo2NB259kw_bTmNndqKtrUFVpxu1nk4HU0L9puQzKJ34NyXlrmY_I9OjHTS9SS-BFFanf7K0bBLy1KfGyUlhGG5P70d3CD7MiB7hnGp_JrGVLh6wwmDH4U1vASWFYrUMOfWzTrqZLnhu2j7SPv9Kczt28HczkXBiLMX9vB02hGAiJo6rvjpVwZOV3B3UaIwB9oX_rO_TCD0sbAEPDZKYxIpDG7LuUlnqiuOmfXweVKa65xRwDoX-VLIA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..w7gEsaXhMRnll1TtrEaKwaSpxs0G_xLjHNIlyZyQkzwQz7l9_1Rw8J0A5k4CTrRiRCTvC3FEeEK5QQSMDgyNEAFCQw-6Tb6aVk4mqiILAzlsqLD89Y6IiecXxfkYE_MSpOMwv01INFZH6VGbizmANUr01n2ZuodJajOUHF2y1XJn4__s0uVBPaJkUzMmViXhl0RLicQouDXaQ3-ibXl2_QJ7mmHyqE0poWLd9HVmKMhL3Nwem0xhlAIDuxGLM4WMTlYOZQPsnSpC3eP8stxFgdE2WJZL1yEISxq6e8Och1kCeaCSeXxFcZgksIBEmyl_YxoAzpYDZd0_titJ5J9yDg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -65,10 +65,11 @@ def adjust_request(self, request: ChatCompletionRequest) -> ChatCompletionReques\n         # Set structured output params for tool calling\n         if json_schema_from_tool is not None:\n             if isinstance(request, ChatCompletionRequest):\n-                request.structured_outputs = StructuredOutputsParams()\n                 # tool_choice: \"Forced Function\" or \"required\" will override\n                 # structured output json settings to make tool calling work correctly\n-                request.structured_outputs.json = json_schema_from_tool\n+                request.structured_outputs = StructuredOutputsParams(\n+                    json=json_schema_from_tool\n+                )\n                 request.response_format = None\n             if isinstance(request, ResponsesRequest):\n                 request.text = ResponseTextConfig()", "patch_sha256": "03056e704c00aab4"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#26822/review_2686702571", "source": "CodeBench", "source_task_id": "vllm-project/vllm#26822", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/entrypoints/openai/responses/serving.py\nReview Comment:\n### Missing null check on prepare_structured_tag return value\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe base class `prepare_structured_tag` returns `None` and most reasoning parsers (Qwen3, DeepSeek, Mistral, etc.) don't override it. When a user specifies a `structural_tag` response format with one of these reasoning parsers, `prepare_structured_tag` returns `None`, and `replace(struct_out, structural_tag=None)` creates a `StructuredOutputsParams` with no constraints. This triggers the new validation requiring at least one constraint, causing a crash. The return value of `prepare_structured_tag` needs to be checked for `None` before calling `replace`.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/entrypoints/openai/serving_responses.py#L473-L479\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..y0kwLc3I7rEBYOwy4j4qXWYMZ1mdRIe2COr_Mz3Yywyiwe4ZWUmVkipy2WEbELBojYIUEbPcUhiAZzcC5u7Qplg_ENNn1cQiyBK5y5jmOXKFhWgZEAUzdzM9fJH7cSBYh6qH7OaWA_ZTK7n-fMMKjAJoT7wJfJB5yBL8HRG9uQ-hv5h8fWuSQhodHUgQ8xNSua3-hox0lcUhR7B6YLGt-Qr0D6qCVHkylMojot3qgA2PXyObFbYMKYmQEeV5G6MA6jwub-LeBtRICWIWB7fMmg8aUpIBcSqThzqp7SiitoCq3Tqe64dReSeOFYZRZJnnePc8d0i8TtASiWSzPqzfQQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..l8sqoPi9uPFj3_T6o3AIkIrkdkjp3BPcW2I4gMS7OeT-FGibi9tA14t2x2kzK-7W3PWAfWe0ff7YDBaMMoGXdDtxa4YWWYHmnC87SJb4B08I3ze-a_jTtU4aIzC8xufj9qLg9fftOgwUW0_lRlHmY-D7jnRpaSSG0piNBdcHGkzPawI2Ll0TJtyFWYRO1WegNDDA0TqCtBDVaVUpnZ52wkp2nmRzcgdw8CN17mOfPDmvKjmN2TUodc5VOhrwd4pTMv_dStOZNj8MZQvzNHUE5M1qdXHOT_jQYz9_c405cY2IGiPUsMS7PLBoLxFFvmT-mK511C7BKMLT0ajfiAYCOQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -464,15 +464,18 @@ async def create_responses(\n \n                 if self.reasoning_parser is not None:\n                     reasoning_parser = self.reasoning_parser(tokenizer)\n-                    if sampling_params.structured_outputs is None:\n-                        sampling_params.structured_outputs = StructuredOutputsParams()\n-                    struct_out = sampling_params.structured_outputs\n-                    if struct_out.all_non_structural_tag_constraints_none():\n-                        sampling_params.structured_outputs.structural_tag = (\n-                            reasoning_parser.prepare_structured_tag(\n-                                sampling_params.structured_outputs.structural_tag,\n-                                self.tool_server,\n-                            )\n+                    if (\n+                        isinstance(\n+                            struct_out := sampling_params.structured_outputs,\n+                            StructuredOutputsParams,\n+                        )\n+                        and struct_out.all_non_structural_tag_constraints_none()\n+                    ):\n+                        sampling_params.structured_outputs = replace(\n+                            struct_out,\n+                            structural_tag=reasoning_parser.prepare_structured_tag(\n+                                struct_out.structural_tag, self.tool_server\n+                            ),\n", "materials": ["### Missing null check on prepare_structured_tag return value\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe base class `prepare_structured_tag` returns `None` and most reasoning parsers (Qwen3, DeepSeek, Mistral, etc.) don't override it. When a user specifies a `structural_tag` response format with one of these reasoning parsers, `prepare_structured_tag` returns `None`, and `replace(struct_out, structural_tag=None)` creates a `StructuredOutputsParams` with no constraints. This triggers the new validation requiring at least one constraint, causing a crash. The return value of `prepare_structured_tag` needs to be checked for `None` before calling `replace`.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/entrypoints/openai/serving_responses.py#L473-L479\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..y0kwLc3I7rEBYOwy4j4qXWYMZ1mdRIe2COr_Mz3Yywyiwe4ZWUmVkipy2WEbELBojYIUEbPcUhiAZzcC5u7Qplg_ENNn1cQiyBK5y5jmOXKFhWgZEAUzdzM9fJH7cSBYh6qH7OaWA_ZTK7n-fMMKjAJoT7wJfJB5yBL8HRG9uQ-hv5h8fWuSQhodHUgQ8xNSua3-hox0lcUhR7B6YLGt-Qr0D6qCVHkylMojot3qgA2PXyObFbYMKYmQEeV5G6MA6jwub-LeBtRICWIWB7fMmg8aUpIBcSqThzqp7SiitoCq3Tqe64dReSeOFYZRZJnnePc8d0i8TtASiWSzPqzfQQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..l8sqoPi9uPFj3_T6o3AIkIrkdkjp3BPcW2I4gMS7OeT-FGibi9tA14t2x2kzK-7W3PWAfWe0ff7YDBaMMoGXdDtxa4YWWYHmnC87SJb4B08I3ze-a_jTtU4aIzC8xufj9qLg9fftOgwUW0_lRlHmY-D7jnRpaSSG0piNBdcHGkzPawI2Ll0TJtyFWYRO1WegNDDA0TqCtBDVaVUpnZ52wkp2nmRzcgdw8CN17mOfPDmvKjmN2TUodc5VOhrwd4pTMv_dStOZNj8MZQvzNHUE5M1qdXHOT_jQYz9_c405cY2IGiPUsMS7PLBoLxFFvmT-mK511C7BKMLT0ajfiAYCOQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -9,7 +9,7 @@\n from collections.abc import AsyncGenerator, AsyncIterator, Callable, Sequence\n from contextlib import AsyncExitStack\n from copy import copy\n-from dataclasses import dataclass\n+from dataclasses import dataclass, replace\n from http import HTTPStatus\n from typing import Final\n \n@@ -467,15 +467,18 @@ async def create_responses(\n \n                 if self.reasoning_parser is not None:\n                     reasoning_parser = self.reasoning_parser(tokenizer)\n-                    if sampling_params.structured_outputs is None:\n-                        sampling_params.structured_outputs = StructuredOutputsParams()\n-                    struct_out = sampling_params.structured_outputs\n-                    if struct_out.all_non_structural_tag_constraints_none():\n-                        sampling_params.structured_outputs.structural_tag = (\n-                            reasoning_parser.prepare_structured_tag(\n-                                sampling_params.structured_outputs.structural_tag,\n-                                self.tool_server,\n-                            )\n+                    if (\n+                        isinstance(\n+                            struct_out := sampling_params.structured_outputs,\n+                            StructuredOutputsParams,\n+                        )\n+                        and struct_out.all_non_structural_tag_constraints_none()\n+                    ):\n+                        sampling_params.structured_outputs = replace(\n+                            struct_out,\n+                            structural_tag=reasoning_parser.prepare_structured_tag(\n+                                struct_out.structural_tag, self.tool_server\n+                            ),\n                         )\n                 generator = self._generate_with_builtin_tools(\n                     request_id=request.request_id,", "patch_sha256": "bcb7c254865d7d95"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#26822/review_2686801295", "source": "CodeBench", "source_task_id": "vllm-project/vllm#26822", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/entrypoints/openai/responses/serving.py\nReview Comment:\n### Reasoning parser structural tag not applied when structured_outputs is None\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nWhen `sampling_params.structured_outputs` is `None` (e.g., when `response_format` is `{\"type\": \"text\"}` or not specified), the new code skips the entire block that applies the reasoning parser's `structural_tag`. The old code would create a `StructuredOutputsParams` and set `structural_tag` via `prepare_structured_tag()`. For gpt-oss models, `prepare_structured_tag()` always returns a non-None default tag essential for proper reasoning output formatting. The new `isinstance()` check returns `False` when `structured_outputs` is `None`, causing the structural tag to never be applied. This is a regression that could break reasoning extraction for gpt-oss models when no structured output constraints are specified.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/entrypoints/openai/serving_responses.py#L466-L479\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..XMCCGBfH8-Z1Jiis54rGVRDty62UIvY8ToA1gy6lg6SWNFUJ33F3kf5PXA8660SqDtkDxQBWzd1YJGJTuzHzCgar3xiEFJf16JQL_MQODXm83eRn0053E1VOt7aX7QoqL6hmNdquZpkzoV7vS0QXgwXGiHFTqTO_jlCMuQiAehLZkXgs8wTrtPpfPrfGLYJC6VJl3MHGLMTv8UUEbPgbCwJgdLK4L0KfjMnDruPsi9RIc5J9cB-LMgESyvGHZNp84wGzLSX8hk4SuOtBIEelxsPkP0j5iwiH_66I_xz7o1TOIzJxL9tTHppj4j0syCiHFVCFymhklDjd6_Ng1QbX0g\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..IFzaNqMN5YItmi7Mtusb1mppP3kjaL-I8ko0G606Jh7ROrczdKiVrmLsaoJLPbx29sWTpPtpi0CiLOs590f1a3gATq20aVK1mFHLGLvebIx3rQRbJBkM9pt8GEdqXX1hv86E0PmbyhPfkpEMXaT6uflckc8RudvHnRFKNWZx8ANy9bES4UT-tgDhUEgkr44_usfnGu_FNsxmzE7HwhHBt7O-w_WToPnNHi5CvOtNmEDMBBL5H-8aSEuJ1oy5bhJCfFbsDjgyy-Jn96SsD1Qev1QOkUfVTC8JTVVEbeAPDMCwVUMIW1J2_hYOeuAqOozroVXejkZesnIKDVPHeRI7Jw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -464,15 +464,18 @@ async def create_responses(\n \n                 if self.reasoning_parser is not None:\n                     reasoning_parser = self.reasoning_parser(tokenizer)\n-                    if sampling_params.structured_outputs is None:\n-                        sampling_params.structured_outputs = StructuredOutputsParams()\n-                    struct_out = sampling_params.structured_outputs\n-                    if struct_out.all_non_structural_tag_constraints_none():\n-                        sampling_params.structured_outputs.structural_tag = (\n-                            reasoning_parser.prepare_structured_tag(\n-                                sampling_params.structured_outputs.structural_tag,\n-                                self.tool_server,\n-                            )\n+                    if (\n+                        isinstance(\n+                            struct_out := sampling_params.structured_outputs,\n+                            StructuredOutputsParams,\n+                        )\n+                        and struct_out.all_non_structural_tag_constraints_none()\n+                    ):\n+                        sampling_params.structured_outputs = replace(\n+                            struct_out,\n+                            structural_tag=reasoning_parser.prepare_structured_tag(\n+                                struct_out.structural_tag, self.tool_server\n+                            ),\n", "materials": ["### Reasoning parser structural tag not applied when structured_outputs is None\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nWhen `sampling_params.structured_outputs` is `None` (e.g., when `response_format` is `{\"type\": \"text\"}` or not specified), the new code skips the entire block that applies the reasoning parser's `structural_tag`. The old code would create a `StructuredOutputsParams` and set `structural_tag` via `prepare_structured_tag()`. For gpt-oss models, `prepare_structured_tag()` always returns a non-None default tag essential for proper reasoning output formatting. The new `isinstance()` check returns `False` when `structured_outputs` is `None`, causing the structural tag to never be applied. This is a regression that could break reasoning extraction for gpt-oss models when no structured output constraints are specified.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/entrypoints/openai/serving_responses.py#L466-L479\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..XMCCGBfH8-Z1Jiis54rGVRDty62UIvY8ToA1gy6lg6SWNFUJ33F3kf5PXA8660SqDtkDxQBWzd1YJGJTuzHzCgar3xiEFJf16JQL_MQODXm83eRn0053E1VOt7aX7QoqL6hmNdquZpkzoV7vS0QXgwXGiHFTqTO_jlCMuQiAehLZkXgs8wTrtPpfPrfGLYJC6VJl3MHGLMTv8UUEbPgbCwJgdLK4L0KfjMnDruPsi9RIc5J9cB-LMgESyvGHZNp84wGzLSX8hk4SuOtBIEelxsPkP0j5iwiH_66I_xz7o1TOIzJxL9tTHppj4j0syCiHFVCFymhklDjd6_Ng1QbX0g\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..IFzaNqMN5YItmi7Mtusb1mppP3kjaL-I8ko0G606Jh7ROrczdKiVrmLsaoJLPbx29sWTpPtpi0CiLOs590f1a3gATq20aVK1mFHLGLvebIx3rQRbJBkM9pt8GEdqXX1hv86E0PmbyhPfkpEMXaT6uflckc8RudvHnRFKNWZx8ANy9bES4UT-tgDhUEgkr44_usfnGu_FNsxmzE7HwhHBt7O-w_WToPnNHi5CvOtNmEDMBBL5H-8aSEuJ1oy5bhJCfFbsDjgyy-Jn96SsD1Qev1QOkUfVTC8JTVVEbeAPDMCwVUMIW1J2_hYOeuAqOozroVXejkZesnIKDVPHeRI7Jw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -9,7 +9,7 @@\n from collections.abc import AsyncGenerator, AsyncIterator, Callable, Sequence\n from contextlib import AsyncExitStack\n from copy import copy\n-from dataclasses import dataclass\n+from dataclasses import dataclass, replace\n from http import HTTPStatus\n from typing import Final\n \n@@ -467,15 +467,18 @@ async def create_responses(\n \n                 if self.reasoning_parser is not None:\n                     reasoning_parser = self.reasoning_parser(tokenizer)\n-                    if sampling_params.structured_outputs is None:\n-                        sampling_params.structured_outputs = StructuredOutputsParams()\n-                    struct_out = sampling_params.structured_outputs\n-                    if struct_out.all_non_structural_tag_constraints_none():\n-                        sampling_params.structured_outputs.structural_tag = (\n-                            reasoning_parser.prepare_structured_tag(\n-                                sampling_params.structured_outputs.structural_tag,\n-                                self.tool_server,\n-                            )\n+                    if (\n+                        isinstance(\n+                            struct_out := sampling_params.structured_outputs,\n+                            StructuredOutputsParams,\n+                        )\n+                        and struct_out.all_non_structural_tag_constraints_none()\n+                    ):\n+                        sampling_params.structured_outputs = replace(\n+                            struct_out,\n+                            structural_tag=reasoning_parser.prepare_structured_tag(\n+                                struct_out.structural_tag, self.tool_server\n+                            ),\n                         )\n                 generator = self._generate_with_builtin_tools(\n                     request_id=request.request_id,", "patch_sha256": "bcb7c254865d7d95"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#26822/review_2693766431", "source": "CodeBench", "source_task_id": "vllm-project/vllm#26822", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/entrypoints/openai/completion/protocol.py\nReview Comment:\n```suggestion\n            structured_outputs_kwargs = dict[str, Any]()\n```\n\nOriginal Code Context:\n@@ -247,18 +248,15 @@ def to_sampling_params(\n \n         response_format = self.response_format\n         if response_format is not None:\n-            # If structured outputs wasn't already enabled,\n-            # we must enable it for these features to work\n-            if self.structured_outputs is None:\n-                self.structured_outputs = StructuredOutputsParams()\n+            kwargs_changes = dict[str, Any]()\n", "materials": ["```suggestion\n            structured_outputs_kwargs = dict[str, Any]()\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -5,6 +5,7 @@\n # https://github.com/lm-sys/FastChat/blob/168ccc29d3f7edc50823016105c024fe2282732a/fastchat/protocol/openai_api_protocol.py\n import json\n import time\n+from dataclasses import replace\n from typing import Annotated, Any, Literal\n \n import torch\n@@ -247,18 +248,15 @@ def to_sampling_params(\n \n         response_format = self.response_format\n         if response_format is not None:\n-            # If structured outputs wasn't already enabled,\n-            # we must enable it for these features to work\n-            if self.structured_outputs is None:\n-                self.structured_outputs = StructuredOutputsParams()\n+            structured_outputs_kwargs = dict[str, Any]()\n \n             # Set structured output params for response format\n             if response_format.type == \"json_object\":\n-                self.structured_outputs.json_object = True\n+                structured_outputs_kwargs[\"json_object\"] = True\n             elif response_format.type == \"json_schema\":\n                 json_schema = response_format.json_schema\n                 assert json_schema is not None\n-                self.structured_outputs.json = json_schema.json_schema\n+                structured_outputs_kwargs[\"json\"] = json_schema.json_schema\n             elif response_format.type == \"structural_tag\":\n                 structural_tag = response_format\n                 assert structural_tag is not None and isinstance(\n@@ -269,7 +267,16 @@ def to_sampling_params(\n                     ),\n                 )\n                 s_tag_obj = structural_tag.model_dump(by_alias=True)\n-                self.structured_outputs.structural_tag = json.dumps(s_tag_obj)\n+                structured_outputs_kwargs[\"structural_tag\"] = json.dumps(s_tag_obj)\n+\n+            # If structured outputs wasn't already enabled,\n+            # we must enable it for these features to work\n+            if len(structured_outputs_kwargs) > 0:\n+                self.structured_outputs = (\n+                    StructuredOutputsParams(**structured_outputs_kwargs)\n+                    if self.structured_outputs is None\n+                    else replace(self.structured_outputs, **structured_outputs_kwargs)\n+                )\n \n         extra_args: dict[str, Any] = self.vllm_xargs if self.vllm_xargs else {}\n         if self.kv_transfer_params:", "patch_sha256": "6001c230874b4d8b"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2490686372", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/fp8.py\nReview Comment:\nThat was a mistake, fixed.\n\nOriginal Code Context:\n@@ -442,6 +451,7 @@ def create_weights(\n                 weight_loader=weight_loader,\n             )\n         layer.register_parameter(\"weight\", weight)\n+        layer.register_parameter(\"input_scale_ub\", None)\n", "materials": ["That was a mistake, fixed."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -48,6 +48,9 @@\n     QuantizationConfig,\n     QuantizeMethodBase,\n )\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n from vllm.model_executor.layers.quantization.kv_cache import BaseKVCacheMethod\n from vllm.model_executor.layers.quantization.utils.flashinfer_utils import (\n     apply_fi_trtllm_fp8_per_tensor_moe,\n@@ -76,12 +79,13 @@\n from vllm.model_executor.layers.quantization.utils.quant_utils import (\n     GroupShape,\n     is_layer_skipped,\n+    kFp8DynamicTensorSym,\n+    kFp8DynamicTokenSym,\n+    kFp8StaticTensorSym,\n )\n from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n-    Fp8LinearOp,\n     cutlass_block_fp8_supported,\n     cutlass_fp8_supported,\n-    maybe_create_device_identity,\n     normalize_e4m3fn_to_e4m3fnuz,\n )\n from vllm.model_executor.parameter import (\n@@ -328,28 +332,30 @@ def __init__(self, quant_config: Fp8Config):\n         self.weight_block_size = self.quant_config.weight_block_size\n         self.block_quant = self.weight_block_size is not None\n         self.act_q_static = self.quant_config.activation_scheme == \"static\"\n-        if self.weight_block_size:\n-            self.act_q_group_shape = GroupShape(1, self.weight_block_size[0])\n-        else:\n-            # Use per-token quantization for better perf if dynamic and cutlass\n-            if not self.act_q_static and cutlass_fp8_supported():\n-                self.act_q_group_shape = GroupShape.PER_TOKEN\n-            else:\n-                self.act_q_group_shape = GroupShape.PER_TENSOR\n \n         if self.block_quant:\n             assert not self.act_q_static\n             assert self.weight_block_size is not None\n             self.w8a8_block_fp8_linear = W8A8BlockFp8LinearOp(\n                 weight_group_shape=GroupShape(*self.weight_block_size),\n-                act_quant_group_shape=self.act_q_group_shape,\n+                act_quant_group_shape=GroupShape(1, self.weight_block_size[0]),\n                 cutlass_block_fp8_supported=self.cutlass_block_fp8_supported,\n                 use_aiter_and_is_supported=self.use_aiter_and_is_supported,\n             )\n         else:\n-            self.fp8_linear = Fp8LinearOp(\n-                act_quant_static=self.act_q_static,\n-                act_quant_group_shape=self.act_q_group_shape,\n+            # Use per-token quantization for better perf if dynamic and cutlass\n+            if self.act_q_static:\n+                activation_quant_key = kFp8StaticTensorSym\n+            elif cutlass_fp8_supported():\n+                activation_quant_key = kFp8DynamicTokenSym\n+            else:\n+                activation_quant_key = kFp8DynamicTensorSym\n+\n+            self.fp8_linear = init_fp8_linear_kernel(\n+                activation_quant_key=activation_quant_key,\n+                weight_quant_key=kFp8StaticTensorSym,\n+                out_dtype=torch.get_default_dtype(),\n+                module_name=self.__class__.__name__,\n             )\n \n     def create_weights(\n@@ -362,8 +368,6 @@ def create_weights(\n         params_dtype: torch.dtype,\n         **extra_weight_attrs,\n     ):\n-        maybe_create_device_identity()\n-\n         output_size_per_partition = sum(output_partition_sizes)\n         weight_loader = extra_weight_attrs.get(\"weight_loader\")\n         layer.logical_widths = output_partition_sizes\n@@ -462,8 +466,6 @@ def patched_weight_loader(param, loaded_weight, *args, **kwargs):\n                 scale = create_fp8_input_scale(output_partition_sizes, weight_loader)\n                 set_weight_attrs(scale, {\"scale_type\": \"input_scale\"})\n                 layer.register_parameter(\"input_scale\", scale)\n-            else:\n-                layer.register_parameter(\"input_scale\", None)\n \n     def process_weights_after_loading(self, layer: Module) -> None:\n         if getattr(layer, \"_already_called_process_weights_after_loading\", False):\n@@ -602,14 +604,7 @@ def apply(\n                 bias=bias,\n             )\n \n-        return self.fp8_linear.apply(\n-            input=x,\n-            weight=layer.weight,\n-            weight_scale=layer.weight_scale,\n-            out_dtype=self.out_dtype,\n-            input_scale=layer.input_scale,\n-            bias=bias,\n-        )\n+        return self.fp8_linear.apply_weights(layer, x, bias)\n \n \n class Fp8MoEMethod(FusedMoEMethodBase):", "patch_sha256": "642b41eb0eed9aa5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2682453083", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/worker/worker.py\nReview Comment:\n### Removed `wait` method still called causing AttributeError\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe `wait` method was removed from `OffloadingWorker` class in this PR, but `OffloadingConnectorWorker.handle_preemptions` still calls `self.worker.wait(job_ids)` at line 609. When preemptions occur, this will raise an `AttributeError` because the `wait` method no longer exists on the `OffloadingWorker` instance. The `wait` method still exists on `CpuGpuHandler` but there's no method on `OffloadingWorker` to delegate to it.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/kv_offload/worker/worker.py#L148-L166\nvllm/distributed/kv_transfer/kv_connector/v1/offloading_connector.py#L608-L609\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`vllm/distributed/kv_transfer/kv_connector/v1/offloading_connector.py#L608-L609`](https://github.com/vllm-project/vllm/blob/4a67a8038b98546ac7a0bf4283cdd1e4aa7055eb/vllm/distributed/kv_transfer/kv_connector/v1/offloading_connector.py#L608-L609)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..rDRGk1k2HPBHswlWDRCdIMNm3597IMQViRVgE4D5yuDSa2eUVnibRA2rAOzMHDIFhQCzXRbUw0ziMVCRv8RH6reggCKQiGDFSbgNP0QsIcCmipMbHCFdblw23agX4SxGu0TFODxzNfYuoV2WMYQzWQXMoblxPowDUn0f2ffIZHe-oxTwBbqfYRVkez5kHGLdO1XlhPD7IfZWfyXnakI05D7ziaYy12gQU3BoimKnF1BS80anSGGUHHOECgKeRHNIM7mjjCcfhF5CQyL6GVj7K-CsvGLsVU0ymVcJDC0CDxH0mrwybKkqdHGtT_0ZVzbYyrxQqAPj-YlMiChk2Pdt1w\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..SY7eE06lhpLGdahe4Nc59xnJlBgvg-01RwZn4a9k2Xzf0QpTCI2g2RUD_Wx-7_TJGSQw4MUVAEjyJ_54vTwQsSNoRv7DuLEdxszA_JiHP4eqb9UaMLRwGoaEEOaUhlZY5UReITNnku_d5EDXMzQh6w8oxfa_kA2Wydo1HveAOqNkei2MxgWBD5ZAVUAhkv33gCcMVRgPO13e3d9vCYcQyMQJ3GBq-HPDE-3wVyhsD2YK2i4OPpJJG_2TBIpeSj7sheItZNjAtMSbscwmklzOmjC0Bl5Ks0sZGHDQSltwqbQG0nRnL6x_rIQfU19ObpMiGMpUH29T2YMyuSUhjn6CYw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -137,27 +144,30 @@ def transfer_async(self, job_id: int, spec: TransferSpec) -> bool:\n             logger.warning(\"Failed to submit %r transfer %d\", transfer_type, job_id)\n         else:\n             logger.debug(\"Submitted %r transfer %d: %r\", transfer_type, job_id, spec)\n-\n         return success\n \n     def get_finished(self) -> list[TransferResult]:\n         \"\"\"\n         Get transfers finished since last call.\n \n         Returns:\n-            A list of (job_id, success) of transfers.\n+            A list of TransferResults\n         \"\"\"\n         finished = []\n         for handler in self.handlers:\n             finished.extend(handler.get_finished())\n+        for transfer_result in finished:\n+            transfer_stats = TransferStats(\n+                transfer_result.transfer_size,\n+                transfer_result.transfer_time,\n+                transfer_result.transfer_type,\n+            )\n+            self._transfer_stats[transfer_result.job_id] = transfer_stats\n", "materials": ["### Removed `wait` method still called causing AttributeError\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe `wait` method was removed from `OffloadingWorker` class in this PR, but `OffloadingConnectorWorker.handle_preemptions` still calls `self.worker.wait(job_ids)` at line 609. When preemptions occur, this will raise an `AttributeError` because the `wait` method no longer exists on the `OffloadingWorker` instance. The `wait` method still exists on `CpuGpuHandler` but there's no method on `OffloadingWorker` to delegate to it.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/kv_offload/worker/worker.py#L148-L166\nvllm/distributed/kv_transfer/kv_connector/v1/offloading_connector.py#L608-L609\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`vllm/distributed/kv_transfer/kv_connector/v1/offloading_connector.py#L608-L609`](https://github.com/vllm-project/vllm/blob/4a67a8038b98546ac7a0bf4283cdd1e4aa7055eb/vllm/distributed/kv_transfer/kv_connector/v1/offloading_connector.py#L608-L609)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..rDRGk1k2HPBHswlWDRCdIMNm3597IMQViRVgE4D5yuDSa2eUVnibRA2rAOzMHDIFhQCzXRbUw0ziMVCRv8RH6reggCKQiGDFSbgNP0QsIcCmipMbHCFdblw23agX4SxGu0TFODxzNfYuoV2WMYQzWQXMoblxPowDUn0f2ffIZHe-oxTwBbqfYRVkez5kHGLdO1XlhPD7IfZWfyXnakI05D7ziaYy12gQU3BoimKnF1BS80anSGGUHHOECgKeRHNIM7mjjCcfhF5CQyL6GVj7K-CsvGLsVU0ymVcJDC0CDxH0mrwybKkqdHGtT_0ZVzbYyrxQqAPj-YlMiChk2Pdt1w\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..SY7eE06lhpLGdahe4Nc59xnJlBgvg-01RwZn4a9k2Xzf0QpTCI2g2RUD_Wx-7_TJGSQw4MUVAEjyJ_54vTwQsSNoRv7DuLEdxszA_JiHP4eqb9UaMLRwGoaEEOaUhlZY5UReITNnku_d5EDXMzQh6w8oxfa_kA2Wydo1HveAOqNkei2MxgWBD5ZAVUAhkv33gCcMVRgPO13e3d9vCYcQyMQJ3GBq-HPDE-3wVyhsD2YK2i4OPpJJG_2TBIpeSj7sheItZNjAtMSbscwmklzOmjC0Bl5Ks0sZGHDQSltwqbQG0nRnL6x_rIQfU19ObpMiGMpUH29T2YMyuSUhjn6CYw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,6 +1,7 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n from abc import ABC, abstractmethod\n+from dataclasses import dataclass\n \n from vllm.logger import init_logger\n from vllm.v1.kv_offload.abstract import LoadStoreSpec\n@@ -9,12 +10,19 @@\n TransferSpec = tuple[LoadStoreSpec, LoadStoreSpec]\n # transfers are forwarded to workers by (src_medium, dst_medium)\n TransferType = tuple[str, str]\n-# transfer result (job_id, success)\n-TransferResult = tuple[int, bool]\n \n logger = init_logger(__name__)\n \n \n+@dataclass\n+class TransferResult:\n+    job_id: int\n+    success: bool\n+    transfer_size: int | None = None  # Size in bytes\n+    transfer_time: float | None = None\n+    transfer_type: TransferType | None = None\n+\n+\n class OffloadingHandler(ABC):\n     \"\"\"\n     OffloadingHandler class for managing asynchronous KV data transfers\n@@ -57,7 +65,6 @@ def get_finished(self) -> list[TransferResult]:\n     def wait(self, job_ids: set[int]) -> None:\n         \"\"\"\n         Wait for jobs to finish (blocking).\n-\n         Args:\n             job_ids: The set of job IDs to wait for.\n         \"\"\"\n@@ -120,7 +127,6 @@ def transfer_async(self, job_id: int, spec: TransferSpec) -> bool:\n         transfer_type = (src.medium(), dst.medium())\n         handler = self.transfer_type_to_handler.get(transfer_type)\n         assert handler is not None\n-\n         try:\n             success = handler.transfer_async(job_id, spec)\n         except Exception as e:\n@@ -137,15 +143,14 @@ def transfer_async(self, job_id: int, spec: TransferSpec) -> bool:\n             logger.warning(\"Failed to submit %r transfer %d\", transfer_type, job_id)\n         else:\n             logger.debug(\"Submitted %r transfer %d: %r\", transfer_type, job_id, spec)\n-\n         return success\n \n     def get_finished(self) -> list[TransferResult]:\n         \"\"\"\n         Get transfers finished since last call.\n \n         Returns:\n-            A list of (job_id, success) of transfers.\n+            A list of TransferResults\n         \"\"\"\n         finished = []\n         for handler in self.handlers:", "patch_sha256": "09ea7eae1278ac87"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2682453088", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/worker/cpu_gpu.py\nReview Comment:\n### Incorrect block count recorded causes inflated metrics\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `Transfer` object stores `src_sub_block_count` for metrics tracking, but the actual number of blocks transferred is `dst_sub_block_count` (as shown in line 136 where `src_to_dst` is sized by `dst_sub_block_count`). When `src_sub_blocks_to_skip > 0` (which occurs when block sizes don't align perfectly), `src_sub_block_count` is larger than `dst_sub_block_count`, causing the transfer size metrics to be overestimated. The `Transfer` at line 191 stores the wrong count value.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/kv_offload/worker/cpu_gpu.py#L190-L191\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..fydmWV6P-QpVRnNMdF12yXlh5EOhlre3AdW6JC1Ip4mH1rZ7DnrWRXDrgqCMUOOL1s7veZYcp1-VTeRFgYilZXg39Fn7aFv3ez3TJqSvIOSIw1Z4dZkqhSQyKYs2HAhKLcrT2wlhKAHa_0Y37GKo4CY6K2j2WucO1VzZdTU16UPB1xzyj8eDLOcPGUBFMWYzqoVllc0Pd1xKyTn658ZzOGdg5dsYQ2c9eNr1kGTea--BqcxXHRlYfzmZ_Lrjv5Cmm-TnsjeGClfgw9cyO1BmD17Z6ifrS8h7zgVtggLLS0ltJppK2tnYZpsE-94Iyovqsq23O2ZPGnsIoz1EZJgRLg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..qPsKx8FWj9cwF3Yo_ln4PsdGpJPUyPZxxHo6Ln1bHy1O1TIWOqA4odDFEp17L86V8j_OPh7Ulw3pjUwOd0pX4O8Sizy2GSs0LVIcc21grxHaa3tLxkSsJ9TTqaV6WxUnN52xD6wfxYH9PAogsWfoAdNi7i88elSwVEwyacQL3q5aMFK7GakzzwqDRSTnde21iGGqmnZUUeLYVsVTkxdiSuUfxQqCp9YVM75jcw3WbdwaDy8fBV07_pG9wt_xg-w7rvRZRQadAAoN5QeCh_u4kagK02_wxY30dtCNKKobT6NYo6NkGXtOGN0RMImmV3GMe9NYwnuZqDc9T6uylg1idQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -152,29 +175,54 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     ops.swap_blocks(src_value_cache, dst_value_cache, src_to_dst_tensor)\n                 else:\n                     ops.swap_blocks(src_tensor, dst_tensor, src_to_dst_tensor)\n-            event.record(stream)\n+            end_event.record(stream)\n \n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+        self._transfer_events[job_id] = (start_event, end_event)\n+        transfer_type: TransferType = (\n+            transfer_spec[0].medium(),\n+            transfer_spec[1].medium(),\n+        )\n+        self._transfers.append(\n+            Transfer(\n+                job_id,\n+                stream,\n+                start_event,\n+                end_event,\n+                src_sub_block_count,\n", "materials": ["### Incorrect block count recorded causes inflated metrics\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `Transfer` object stores `src_sub_block_count` for metrics tracking, but the actual number of blocks transferred is `dst_sub_block_count` (as shown in line 136 where `src_to_dst` is sized by `dst_sub_block_count`). When `src_sub_blocks_to_skip > 0` (which occurs when block sizes don't align perfectly), `src_sub_block_count` is larger than `dst_sub_block_count`, causing the transfer size metrics to be overestimated. The `Transfer` at line 191 stores the wrong count value.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/kv_offload/worker/cpu_gpu.py#L190-L191\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..fydmWV6P-QpVRnNMdF12yXlh5EOhlre3AdW6JC1Ip4mH1rZ7DnrWRXDrgqCMUOOL1s7veZYcp1-VTeRFgYilZXg39Fn7aFv3ez3TJqSvIOSIw1Z4dZkqhSQyKYs2HAhKLcrT2wlhKAHa_0Y37GKo4CY6K2j2WucO1VzZdTU16UPB1xzyj8eDLOcPGUBFMWYzqoVllc0Pd1xKyTn658ZzOGdg5dsYQ2c9eNr1kGTea--BqcxXHRlYfzmZ_Lrjv5Cmm-TnsjeGClfgw9cyO1BmD17Z6ifrS8h7zgVtggLLS0ltJppK2tnYZpsE-94Iyovqsq23O2ZPGnsIoz1EZJgRLg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..qPsKx8FWj9cwF3Yo_ln4PsdGpJPUyPZxxHo6Ln1bHy1O1TIWOqA4odDFEp17L86V8j_OPh7Ulw3pjUwOd0pX4O8Sizy2GSs0LVIcc21grxHaa3tLxkSsJ9TTqaV6WxUnN52xD6wfxYH9PAogsWfoAdNi7i88elSwVEwyacQL3q5aMFK7GakzzwqDRSTnde21iGGqmnZUUeLYVsVTkxdiSuUfxQqCp9YVM75jcw3WbdwaDy8fBV07_pG9wt_xg-w7rvRZRQadAAoN5QeCh_u4kagK02_wxY30dtCNKKobT6NYo6NkGXtOGN0RMImmV3GMe9NYwnuZqDc9T6uylg1idQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,6 +1,7 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n from collections import deque\n+from dataclasses import dataclass\n \n import numpy as np\n import torch\n@@ -19,6 +20,15 @@\n logger = init_logger(__name__)\n \n \n+@dataclass\n+class Transfer:\n+    job_id: int\n+    stream: torch.cuda.Stream\n+    start_event: torch.Event\n+    end_event: torch.Event\n+    num_bytes: int\n+\n+\n def expand_block_ids(\n     block_ids: np.ndarray,\n     block_size_factor: int,\n@@ -92,14 +102,15 @@ def __init__(\n             tensor.element_size() * tensor.stride(0) * min_block_size_factor\n             for tensor in src_tensors\n         ]\n+        self.total_block_size_in_bytes = sum(self.block_size_in_bytes)\n \n         assert len(src_tensors) > 0\n         self.gpu_to_cpu: bool = self.src_tensors[0].is_cuda\n-\n+        self.transfer_type = (\"GPU\", \"CPU\") if self.gpu_to_cpu else (\"CPU\", \"GPU\")\n         # job_id -> event\n         self._transfer_events: dict[int, torch.Event] = {}\n         # queue of transfers (job_id, stream, event)\n-        self._transfers: deque[tuple[int, torch.cuda.Stream, torch.Event]] = deque()\n+        self._transfers: deque[Transfer] = deque()\n         # list of CUDA streams available for re-use\n         self._stream_pool: list[torch.cuda.Stream] = []\n         # list of CUDA events available for re-use\n@@ -132,16 +143,27 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n         src_to_dst_tensor = torch.from_numpy(src_to_dst)\n \n         stream = self._stream_pool.pop() if self._stream_pool else torch.cuda.Stream()\n-        event = self._event_pool.pop() if self._event_pool else torch.Event()\n+        start_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n+        end_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n \n         if self.gpu_to_cpu:\n             # wait for model computation to finish before offloading\n             stream.wait_stream(torch.cuda.current_stream())\n         if self._transfers:\n-            _, _, last_event = self._transfers[-1]\n+            last_transfer: Transfer = self._transfers[-1]\n+            last_event = last_transfer.end_event\n             # assure job will start only after the previous one completes\n             stream.wait_event(last_event)\n         with torch.cuda.stream(stream):\n+            start_event.record(stream)\n             for src_tensor, dst_tensor, block_size_in_bytes in zip(\n                 self.src_tensors,\n                 self.dst_tensors,\n@@ -153,22 +175,42 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     block_size_in_bytes,\n                     src_to_dst_tensor,\n                 )\n-            event.record(stream)\n-\n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+            end_event.record(stream)\n+\n+        self._transfer_events[job_id] = end_event\n+        self._transfers.append(\n+            Transfer(\n+                job_id=job_id,\n+                stream=stream,\n+                start_event=start_event,\n+                end_event=end_event,\n+                num_bytes=dst_sub_block_count * self.total_block_size_in_bytes,\n+            )\n+        )\n \n         # success\n         return True\n \n     def get_finished(self) -> list[TransferResult]:\n         results: list[TransferResult] = []\n-        while self._transfers and self._transfers[0][2].query():\n-            job_id, stream, event = self._transfers.popleft()\n-            results.append((job_id, True))\n-            self._stream_pool.append(stream)\n-            self._event_pool.append(event)\n-            del self._transfer_events[job_id]\n+        while self._transfers and self._transfers[0].end_event.query():\n+            transfer = self._transfers.popleft()\n+            transfer_time = (\n+                transfer.start_event.elapsed_time(transfer.end_event) * 1e-3\n+            )  # elapsed_time is in miliseconds\n+            result = TransferResult(\n+                job_id=transfer.job_id,\n+                success=True,\n+                transfer_size=transfer.num_bytes,\n+                transfer_time=transfer_time,\n+                transfer_type=self.transfer_type,\n+            )\n+\n+            results.append(result)\n+            self._stream_pool.append(transfer.stream)\n+            self._event_pool.append(transfer.end_event)\n+            self._event_pool.append(transfer.start_event)\n+            del self._transfer_events[transfer.job_id]\n         return results\n \n     def wait(self, job_ids: set[int]):", "patch_sha256": "559048399e0ddd6c"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2685074821", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/worker/cpu_gpu.py\nReview Comment:\nLet's remove the `transfer_type` field from the `Transfer` dataclass.\r\nInstead, introduce `self.transfer_type` in `SingleDirectionOffloadingHandler.__init__`.\n\nOriginal Code Context:\n@@ -152,29 +175,54 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     ops.swap_blocks(src_value_cache, dst_value_cache, src_to_dst_tensor)\n                 else:\n                     ops.swap_blocks(src_tensor, dst_tensor, src_to_dst_tensor)\n-            event.record(stream)\n+            end_event.record(stream)\n \n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+        self._transfer_events[job_id] = (start_event, end_event)\n+        transfer_type: TransferType = (\n+            transfer_spec[0].medium(),\n+            transfer_spec[1].medium(),\n+        )\n+        self._transfers.append(\n+            Transfer(\n+                job_id,\n+                stream,\n+                start_event,\n+                end_event,\n+                src_sub_block_count,\n+                transfer_type,\n+            )\n+        )\n \n         # success\n         return True\n \n     def get_finished(self) -> list[TransferResult]:\n         results: list[TransferResult] = []\n-        while self._transfers and self._transfers[0][2].query():\n-            job_id, stream, event = self._transfers.popleft()\n-            results.append((job_id, True))\n-            self._stream_pool.append(stream)\n-            self._event_pool.append(event)\n-            del self._transfer_events[job_id]\n+        while self._transfers and self._transfers[0].end_event.query():\n+            transfer = self._transfers.popleft()\n+            transfer_time = transfer.start_event.elapsed_time(transfer.end_event)\n+            result = TransferResult(\n+                transfer.job_id,\n+                True,\n+                transfer.num_blocks,\n+                transfer_time,\n+                transfer.transfer_type,\n", "materials": ["Let's remove the `transfer_type` field from the `Transfer` dataclass.\r\nInstead, introduce `self.transfer_type` in `SingleDirectionOffloadingHandler.__init__`."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,6 +1,7 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n from collections import deque\n+from dataclasses import dataclass\n \n import numpy as np\n import torch\n@@ -19,6 +20,15 @@\n logger = init_logger(__name__)\n \n \n+@dataclass\n+class Transfer:\n+    job_id: int\n+    stream: torch.cuda.Stream\n+    start_event: torch.Event\n+    end_event: torch.Event\n+    num_bytes: int\n+\n+\n def expand_block_ids(\n     block_ids: np.ndarray,\n     block_size_factor: int,\n@@ -92,14 +102,15 @@ def __init__(\n             tensor.element_size() * tensor.stride(0) * min_block_size_factor\n             for tensor in src_tensors\n         ]\n+        self.total_block_size_in_bytes = sum(self.block_size_in_bytes)\n \n         assert len(src_tensors) > 0\n         self.gpu_to_cpu: bool = self.src_tensors[0].is_cuda\n-\n+        self.transfer_type = (\"GPU\", \"CPU\") if self.gpu_to_cpu else (\"CPU\", \"GPU\")\n         # job_id -> event\n         self._transfer_events: dict[int, torch.Event] = {}\n         # queue of transfers (job_id, stream, event)\n-        self._transfers: deque[tuple[int, torch.cuda.Stream, torch.Event]] = deque()\n+        self._transfers: deque[Transfer] = deque()\n         # list of CUDA streams available for re-use\n         self._stream_pool: list[torch.cuda.Stream] = []\n         # list of CUDA events available for re-use\n@@ -132,16 +143,27 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n         src_to_dst_tensor = torch.from_numpy(src_to_dst)\n \n         stream = self._stream_pool.pop() if self._stream_pool else torch.cuda.Stream()\n-        event = self._event_pool.pop() if self._event_pool else torch.Event()\n+        start_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n+        end_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n \n         if self.gpu_to_cpu:\n             # wait for model computation to finish before offloading\n             stream.wait_stream(torch.cuda.current_stream())\n         if self._transfers:\n-            _, _, last_event = self._transfers[-1]\n+            last_transfer: Transfer = self._transfers[-1]\n+            last_event = last_transfer.end_event\n             # assure job will start only after the previous one completes\n             stream.wait_event(last_event)\n         with torch.cuda.stream(stream):\n+            start_event.record(stream)\n             for src_tensor, dst_tensor, block_size_in_bytes in zip(\n                 self.src_tensors,\n                 self.dst_tensors,\n@@ -153,22 +175,42 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     block_size_in_bytes,\n                     src_to_dst_tensor,\n                 )\n-            event.record(stream)\n-\n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+            end_event.record(stream)\n+\n+        self._transfer_events[job_id] = end_event\n+        self._transfers.append(\n+            Transfer(\n+                job_id=job_id,\n+                stream=stream,\n+                start_event=start_event,\n+                end_event=end_event,\n+                num_bytes=dst_sub_block_count * self.total_block_size_in_bytes,\n+            )\n+        )\n \n         # success\n         return True\n \n     def get_finished(self) -> list[TransferResult]:\n         results: list[TransferResult] = []\n-        while self._transfers and self._transfers[0][2].query():\n-            job_id, stream, event = self._transfers.popleft()\n-            results.append((job_id, True))\n-            self._stream_pool.append(stream)\n-            self._event_pool.append(event)\n-            del self._transfer_events[job_id]\n+        while self._transfers and self._transfers[0].end_event.query():\n+            transfer = self._transfers.popleft()\n+            transfer_time = (\n+                transfer.start_event.elapsed_time(transfer.end_event) * 1e-3\n+            )  # elapsed_time is in miliseconds\n+            result = TransferResult(\n+                job_id=transfer.job_id,\n+                success=True,\n+                transfer_size=transfer.num_bytes,\n+                transfer_time=transfer_time,\n+                transfer_type=self.transfer_type,\n+            )\n+\n+            results.append(result)\n+            self._stream_pool.append(transfer.stream)\n+            self._event_pool.append(transfer.end_event)\n+            self._event_pool.append(transfer.start_event)\n+            del self._transfer_events[transfer.job_id]\n         return results\n \n     def wait(self, job_ids: set[int]):", "patch_sha256": "559048399e0ddd6c"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2685084226", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/worker/cpu_gpu.py\nReview Comment:\nJust to be on the safe side for any future modifications of `self._transfer_events`:\r\n```suggestion\r\n            if events is not None:\r\n```\n\nOriginal Code Context:\n@@ -152,29 +175,54 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     ops.swap_blocks(src_value_cache, dst_value_cache, src_to_dst_tensor)\n                 else:\n                     ops.swap_blocks(src_tensor, dst_tensor, src_to_dst_tensor)\n-            event.record(stream)\n+            end_event.record(stream)\n \n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+        self._transfer_events[job_id] = (start_event, end_event)\n+        transfer_type: TransferType = (\n+            transfer_spec[0].medium(),\n+            transfer_spec[1].medium(),\n+        )\n+        self._transfers.append(\n+            Transfer(\n+                job_id,\n+                stream,\n+                start_event,\n+                end_event,\n+                src_sub_block_count,\n+                transfer_type,\n+            )\n+        )\n \n         # success\n         return True\n \n     def get_finished(self) -> list[TransferResult]:\n         results: list[TransferResult] = []\n-        while self._transfers and self._transfers[0][2].query():\n-            job_id, stream, event = self._transfers.popleft()\n-            results.append((job_id, True))\n-            self._stream_pool.append(stream)\n-            self._event_pool.append(event)\n-            del self._transfer_events[job_id]\n+        while self._transfers and self._transfers[0].end_event.query():\n+            transfer = self._transfers.popleft()\n+            transfer_time = transfer.start_event.elapsed_time(transfer.end_event)\n+            result = TransferResult(\n+                transfer.job_id,\n+                True,\n+                transfer.num_blocks,\n+                transfer_time,\n+                transfer.transfer_type,\n+            )\n+\n+            results.append(result)\n+            self._stream_pool.append(transfer.stream)\n+            self._event_pool.append(transfer.end_event)\n+            self._event_pool.append(transfer.start_event)\n+            del self._transfer_events[transfer.job_id]\n         return results\n \n     def wait(self, job_ids: set[int]):\n         for job_id in job_ids:\n-            event = self._transfer_events.get(job_id)\n-            if event is not None:\n-                event.synchronize()\n+            events = self._transfer_events.get(job_id)\n+            if events:\n", "materials": ["Just to be on the safe side for any future modifications of `self._transfer_events`:\r\n```suggestion\r\n            if events is not None:\r\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,6 +1,7 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n from collections import deque\n+from dataclasses import dataclass\n \n import numpy as np\n import torch\n@@ -19,6 +20,15 @@\n logger = init_logger(__name__)\n \n \n+@dataclass\n+class Transfer:\n+    job_id: int\n+    stream: torch.cuda.Stream\n+    start_event: torch.Event\n+    end_event: torch.Event\n+    num_bytes: int\n+\n+\n def expand_block_ids(\n     block_ids: np.ndarray,\n     block_size_factor: int,\n@@ -92,14 +102,15 @@ def __init__(\n             tensor.element_size() * tensor.stride(0) * min_block_size_factor\n             for tensor in src_tensors\n         ]\n+        self.total_block_size_in_bytes = sum(self.block_size_in_bytes)\n \n         assert len(src_tensors) > 0\n         self.gpu_to_cpu: bool = self.src_tensors[0].is_cuda\n-\n+        self.transfer_type = (\"GPU\", \"CPU\") if self.gpu_to_cpu else (\"CPU\", \"GPU\")\n         # job_id -> event\n         self._transfer_events: dict[int, torch.Event] = {}\n         # queue of transfers (job_id, stream, event)\n-        self._transfers: deque[tuple[int, torch.cuda.Stream, torch.Event]] = deque()\n+        self._transfers: deque[Transfer] = deque()\n         # list of CUDA streams available for re-use\n         self._stream_pool: list[torch.cuda.Stream] = []\n         # list of CUDA events available for re-use\n@@ -132,16 +143,27 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n         src_to_dst_tensor = torch.from_numpy(src_to_dst)\n \n         stream = self._stream_pool.pop() if self._stream_pool else torch.cuda.Stream()\n-        event = self._event_pool.pop() if self._event_pool else torch.Event()\n+        start_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n+        end_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n \n         if self.gpu_to_cpu:\n             # wait for model computation to finish before offloading\n             stream.wait_stream(torch.cuda.current_stream())\n         if self._transfers:\n-            _, _, last_event = self._transfers[-1]\n+            last_transfer: Transfer = self._transfers[-1]\n+            last_event = last_transfer.end_event\n             # assure job will start only after the previous one completes\n             stream.wait_event(last_event)\n         with torch.cuda.stream(stream):\n+            start_event.record(stream)\n             for src_tensor, dst_tensor, block_size_in_bytes in zip(\n                 self.src_tensors,\n                 self.dst_tensors,\n@@ -153,22 +175,42 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     block_size_in_bytes,\n                     src_to_dst_tensor,\n                 )\n-            event.record(stream)\n-\n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+            end_event.record(stream)\n+\n+        self._transfer_events[job_id] = end_event\n+        self._transfers.append(\n+            Transfer(\n+                job_id=job_id,\n+                stream=stream,\n+                start_event=start_event,\n+                end_event=end_event,\n+                num_bytes=dst_sub_block_count * self.total_block_size_in_bytes,\n+            )\n+        )\n \n         # success\n         return True\n \n     def get_finished(self) -> list[TransferResult]:\n         results: list[TransferResult] = []\n-        while self._transfers and self._transfers[0][2].query():\n-            job_id, stream, event = self._transfers.popleft()\n-            results.append((job_id, True))\n-            self._stream_pool.append(stream)\n-            self._event_pool.append(event)\n-            del self._transfer_events[job_id]\n+        while self._transfers and self._transfers[0].end_event.query():\n+            transfer = self._transfers.popleft()\n+            transfer_time = (\n+                transfer.start_event.elapsed_time(transfer.end_event) * 1e-3\n+            )  # elapsed_time is in miliseconds\n+            result = TransferResult(\n+                job_id=transfer.job_id,\n+                success=True,\n+                transfer_size=transfer.num_bytes,\n+                transfer_time=transfer_time,\n+                transfer_type=self.transfer_type,\n+            )\n+\n+            results.append(result)\n+            self._stream_pool.append(transfer.stream)\n+            self._event_pool.append(transfer.end_event)\n+            self._event_pool.append(transfer.start_event)\n+            del self._transfer_events[transfer.job_id]\n         return results\n \n     def wait(self, job_ids: set[int]):", "patch_sha256": "559048399e0ddd6c"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2685088642", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/worker/cpu_gpu.py\nReview Comment:\nThis sync on the `start_event` is unnecessary, as it is covered by the sync of `end_event`.\n\nOriginal Code Context:\n@@ -152,29 +175,54 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     ops.swap_blocks(src_value_cache, dst_value_cache, src_to_dst_tensor)\n                 else:\n                     ops.swap_blocks(src_tensor, dst_tensor, src_to_dst_tensor)\n-            event.record(stream)\n+            end_event.record(stream)\n \n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+        self._transfer_events[job_id] = (start_event, end_event)\n+        transfer_type: TransferType = (\n+            transfer_spec[0].medium(),\n+            transfer_spec[1].medium(),\n+        )\n+        self._transfers.append(\n+            Transfer(\n+                job_id,\n+                stream,\n+                start_event,\n+                end_event,\n+                src_sub_block_count,\n+                transfer_type,\n+            )\n+        )\n \n         # success\n         return True\n \n     def get_finished(self) -> list[TransferResult]:\n         results: list[TransferResult] = []\n-        while self._transfers and self._transfers[0][2].query():\n-            job_id, stream, event = self._transfers.popleft()\n-            results.append((job_id, True))\n-            self._stream_pool.append(stream)\n-            self._event_pool.append(event)\n-            del self._transfer_events[job_id]\n+        while self._transfers and self._transfers[0].end_event.query():\n+            transfer = self._transfers.popleft()\n+            transfer_time = transfer.start_event.elapsed_time(transfer.end_event)\n+            result = TransferResult(\n+                transfer.job_id,\n+                True,\n+                transfer.num_blocks,\n+                transfer_time,\n+                transfer.transfer_type,\n+            )\n+\n+            results.append(result)\n+            self._stream_pool.append(transfer.stream)\n+            self._event_pool.append(transfer.end_event)\n+            self._event_pool.append(transfer.start_event)\n+            del self._transfer_events[transfer.job_id]\n         return results\n \n     def wait(self, job_ids: set[int]):\n         for job_id in job_ids:\n-            event = self._transfer_events.get(job_id)\n-            if event is not None:\n-                event.synchronize()\n+            events = self._transfer_events.get(job_id)\n+            if events:\n+                start_event, end_event = events\n+                start_event.synchronize()\n", "materials": ["This sync on the `start_event` is unnecessary, as it is covered by the sync of `end_event`."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,6 +1,7 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n from collections import deque\n+from dataclasses import dataclass\n \n import numpy as np\n import torch\n@@ -19,6 +20,15 @@\n logger = init_logger(__name__)\n \n \n+@dataclass\n+class Transfer:\n+    job_id: int\n+    stream: torch.cuda.Stream\n+    start_event: torch.Event\n+    end_event: torch.Event\n+    num_bytes: int\n+\n+\n def expand_block_ids(\n     block_ids: np.ndarray,\n     block_size_factor: int,\n@@ -92,14 +102,15 @@ def __init__(\n             tensor.element_size() * tensor.stride(0) * min_block_size_factor\n             for tensor in src_tensors\n         ]\n+        self.total_block_size_in_bytes = sum(self.block_size_in_bytes)\n \n         assert len(src_tensors) > 0\n         self.gpu_to_cpu: bool = self.src_tensors[0].is_cuda\n-\n+        self.transfer_type = (\"GPU\", \"CPU\") if self.gpu_to_cpu else (\"CPU\", \"GPU\")\n         # job_id -> event\n         self._transfer_events: dict[int, torch.Event] = {}\n         # queue of transfers (job_id, stream, event)\n-        self._transfers: deque[tuple[int, torch.cuda.Stream, torch.Event]] = deque()\n+        self._transfers: deque[Transfer] = deque()\n         # list of CUDA streams available for re-use\n         self._stream_pool: list[torch.cuda.Stream] = []\n         # list of CUDA events available for re-use\n@@ -132,16 +143,27 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n         src_to_dst_tensor = torch.from_numpy(src_to_dst)\n \n         stream = self._stream_pool.pop() if self._stream_pool else torch.cuda.Stream()\n-        event = self._event_pool.pop() if self._event_pool else torch.Event()\n+        start_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n+        end_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n \n         if self.gpu_to_cpu:\n             # wait for model computation to finish before offloading\n             stream.wait_stream(torch.cuda.current_stream())\n         if self._transfers:\n-            _, _, last_event = self._transfers[-1]\n+            last_transfer: Transfer = self._transfers[-1]\n+            last_event = last_transfer.end_event\n             # assure job will start only after the previous one completes\n             stream.wait_event(last_event)\n         with torch.cuda.stream(stream):\n+            start_event.record(stream)\n             for src_tensor, dst_tensor, block_size_in_bytes in zip(\n                 self.src_tensors,\n                 self.dst_tensors,\n@@ -153,22 +175,42 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     block_size_in_bytes,\n                     src_to_dst_tensor,\n                 )\n-            event.record(stream)\n-\n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+            end_event.record(stream)\n+\n+        self._transfer_events[job_id] = end_event\n+        self._transfers.append(\n+            Transfer(\n+                job_id=job_id,\n+                stream=stream,\n+                start_event=start_event,\n+                end_event=end_event,\n+                num_bytes=dst_sub_block_count * self.total_block_size_in_bytes,\n+            )\n+        )\n \n         # success\n         return True\n \n     def get_finished(self) -> list[TransferResult]:\n         results: list[TransferResult] = []\n-        while self._transfers and self._transfers[0][2].query():\n-            job_id, stream, event = self._transfers.popleft()\n-            results.append((job_id, True))\n-            self._stream_pool.append(stream)\n-            self._event_pool.append(event)\n-            del self._transfer_events[job_id]\n+        while self._transfers and self._transfers[0].end_event.query():\n+            transfer = self._transfers.popleft()\n+            transfer_time = (\n+                transfer.start_event.elapsed_time(transfer.end_event) * 1e-3\n+            )  # elapsed_time is in miliseconds\n+            result = TransferResult(\n+                job_id=transfer.job_id,\n+                success=True,\n+                transfer_size=transfer.num_bytes,\n+                transfer_time=transfer_time,\n+                transfer_type=self.transfer_type,\n+            )\n+\n+            results.append(result)\n+            self._stream_pool.append(transfer.stream)\n+            self._event_pool.append(transfer.end_event)\n+            self._event_pool.append(transfer.start_event)\n+            del self._transfer_events[transfer.job_id]\n         return results\n \n     def wait(self, job_ids: set[int]):", "patch_sha256": "559048399e0ddd6c"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2685096140", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/worker/worker.py\nReview Comment:\nlooks like you accidentally dropped this function while rebasing.\n\nOriginal Code Context:\n@@ -137,27 +144,30 @@ def transfer_async(self, job_id: int, spec: TransferSpec) -> bool:\n             logger.warning(\"Failed to submit %r transfer %d\", transfer_type, job_id)\n         else:\n             logger.debug(\"Submitted %r transfer %d: %r\", transfer_type, job_id, spec)\n-\n         return success\n \n     def get_finished(self) -> list[TransferResult]:\n         \"\"\"\n         Get transfers finished since last call.\n \n         Returns:\n-            A list of (job_id, success) of transfers.\n+            A list of TransferResults\n         \"\"\"\n         finished = []\n         for handler in self.handlers:\n             finished.extend(handler.get_finished())\n+        for transfer_result in finished:\n+            transfer_stats = TransferStats(\n+                transfer_result.transfer_size,\n+                transfer_result.transfer_time,\n+                transfer_result.transfer_type,\n+            )\n+            self._transfer_stats[transfer_result.job_id] = transfer_stats\n         return finished\n \n-    def wait(self, job_ids: set[int]) -> None:\n-        \"\"\"\n-        Wait for jobs to finish (blocking).\n-\n-        Args:\n-            job_ids: The set of job IDs to wait for.\n-        \"\"\"\n-        for handler in self.handlers:\n-            handler.wait(job_ids)\n", "materials": ["looks like you accidentally dropped this function while rebasing."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,6 +1,7 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n from abc import ABC, abstractmethod\n+from dataclasses import dataclass\n \n from vllm.logger import init_logger\n from vllm.v1.kv_offload.abstract import LoadStoreSpec\n@@ -9,12 +10,19 @@\n TransferSpec = tuple[LoadStoreSpec, LoadStoreSpec]\n # transfers are forwarded to workers by (src_medium, dst_medium)\n TransferType = tuple[str, str]\n-# transfer result (job_id, success)\n-TransferResult = tuple[int, bool]\n \n logger = init_logger(__name__)\n \n \n+@dataclass\n+class TransferResult:\n+    job_id: int\n+    success: bool\n+    transfer_size: int | None = None  # Size in bytes\n+    transfer_time: float | None = None\n+    transfer_type: TransferType | None = None\n+\n+\n class OffloadingHandler(ABC):\n     \"\"\"\n     OffloadingHandler class for managing asynchronous KV data transfers\n@@ -57,7 +65,6 @@ def get_finished(self) -> list[TransferResult]:\n     def wait(self, job_ids: set[int]) -> None:\n         \"\"\"\n         Wait for jobs to finish (blocking).\n-\n         Args:\n             job_ids: The set of job IDs to wait for.\n         \"\"\"\n@@ -120,7 +127,6 @@ def transfer_async(self, job_id: int, spec: TransferSpec) -> bool:\n         transfer_type = (src.medium(), dst.medium())\n         handler = self.transfer_type_to_handler.get(transfer_type)\n         assert handler is not None\n-\n         try:\n             success = handler.transfer_async(job_id, spec)\n         except Exception as e:\n@@ -137,15 +143,14 @@ def transfer_async(self, job_id: int, spec: TransferSpec) -> bool:\n             logger.warning(\"Failed to submit %r transfer %d\", transfer_type, job_id)\n         else:\n             logger.debug(\"Submitted %r transfer %d: %r\", transfer_type, job_id, spec)\n-\n         return success\n \n     def get_finished(self) -> list[TransferResult]:\n         \"\"\"\n         Get transfers finished since last call.\n \n         Returns:\n-            A list of (job_id, success) of transfers.\n+            A list of TransferResults\n         \"\"\"\n         finished = []\n         for handler in self.handlers:", "patch_sha256": "09ea7eae1278ac87"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2685110581", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/worker/worker.py\nReview Comment:\nWe can remove this class, as all is returned by `TransferResult`.\n\nOriginal Code Context:\n@@ -9,12 +10,26 @@\n TransferSpec = tuple[LoadStoreSpec, LoadStoreSpec]\n # transfers are forwarded to workers by (src_medium, dst_medium)\n TransferType = tuple[str, str]\n-# transfer result (job_id, success)\n-TransferResult = tuple[int, bool]\n \n logger = init_logger(__name__)\n \n \n+@dataclass\n+class TransferStats:\n+    num_blocks: int\n+    time: float  # Can be start_time or duration\n+    transfer_type: TransferType\n+\n+\n", "materials": ["We can remove this class, as all is returned by `TransferResult`."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,6 +1,7 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n from abc import ABC, abstractmethod\n+from dataclasses import dataclass\n \n from vllm.logger import init_logger\n from vllm.v1.kv_offload.abstract import LoadStoreSpec\n@@ -9,12 +10,19 @@\n TransferSpec = tuple[LoadStoreSpec, LoadStoreSpec]\n # transfers are forwarded to workers by (src_medium, dst_medium)\n TransferType = tuple[str, str]\n-# transfer result (job_id, success)\n-TransferResult = tuple[int, bool]\n \n logger = init_logger(__name__)\n \n \n+@dataclass\n+class TransferResult:\n+    job_id: int\n+    success: bool\n+    transfer_size: int | None = None  # Size in bytes\n+    transfer_time: float | None = None\n+    transfer_type: TransferType | None = None\n+\n+\n class OffloadingHandler(ABC):\n     \"\"\"\n     OffloadingHandler class for managing asynchronous KV data transfers\n@@ -57,7 +65,6 @@ def get_finished(self) -> list[TransferResult]:\n     def wait(self, job_ids: set[int]) -> None:\n         \"\"\"\n         Wait for jobs to finish (blocking).\n-\n         Args:\n             job_ids: The set of job IDs to wait for.\n         \"\"\"\n@@ -120,7 +127,6 @@ def transfer_async(self, job_id: int, spec: TransferSpec) -> bool:\n         transfer_type = (src.medium(), dst.medium())\n         handler = self.transfer_type_to_handler.get(transfer_type)\n         assert handler is not None\n-\n         try:\n             success = handler.transfer_async(job_id, spec)\n         except Exception as e:\n@@ -137,15 +143,14 @@ def transfer_async(self, job_id: int, spec: TransferSpec) -> bool:\n             logger.warning(\"Failed to submit %r transfer %d\", transfer_type, job_id)\n         else:\n             logger.debug(\"Submitted %r transfer %d: %r\", transfer_type, job_id, spec)\n-\n         return success\n \n     def get_finished(self) -> list[TransferResult]:\n         \"\"\"\n         Get transfers finished since last call.\n \n         Returns:\n-            A list of (job_id, success) of transfers.\n+            A list of TransferResults\n         \"\"\"\n         finished = []\n         for handler in self.handlers:", "patch_sha256": "09ea7eae1278ac87"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2685113449", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/worker/cpu_gpu.py\nReview Comment:\ncan you use explicit kwargs?\r\ni.e. `Transfer(job_id=job_id, ...)`\n\nOriginal Code Context:\n@@ -152,29 +175,54 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     ops.swap_blocks(src_value_cache, dst_value_cache, src_to_dst_tensor)\n                 else:\n                     ops.swap_blocks(src_tensor, dst_tensor, src_to_dst_tensor)\n-            event.record(stream)\n+            end_event.record(stream)\n \n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+        self._transfer_events[job_id] = (start_event, end_event)\n+        transfer_type: TransferType = (\n+            transfer_spec[0].medium(),\n+            transfer_spec[1].medium(),\n+        )\n+        self._transfers.append(\n+            Transfer(\n", "materials": ["can you use explicit kwargs?\r\ni.e. `Transfer(job_id=job_id, ...)`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,6 +1,7 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n from collections import deque\n+from dataclasses import dataclass\n \n import numpy as np\n import torch\n@@ -19,6 +20,15 @@\n logger = init_logger(__name__)\n \n \n+@dataclass\n+class Transfer:\n+    job_id: int\n+    stream: torch.cuda.Stream\n+    start_event: torch.Event\n+    end_event: torch.Event\n+    num_bytes: int\n+\n+\n def expand_block_ids(\n     block_ids: np.ndarray,\n     block_size_factor: int,\n@@ -92,14 +102,15 @@ def __init__(\n             tensor.element_size() * tensor.stride(0) * min_block_size_factor\n             for tensor in src_tensors\n         ]\n+        self.total_block_size_in_bytes = sum(self.block_size_in_bytes)\n \n         assert len(src_tensors) > 0\n         self.gpu_to_cpu: bool = self.src_tensors[0].is_cuda\n-\n+        self.transfer_type = (\"GPU\", \"CPU\") if self.gpu_to_cpu else (\"CPU\", \"GPU\")\n         # job_id -> event\n         self._transfer_events: dict[int, torch.Event] = {}\n         # queue of transfers (job_id, stream, event)\n-        self._transfers: deque[tuple[int, torch.cuda.Stream, torch.Event]] = deque()\n+        self._transfers: deque[Transfer] = deque()\n         # list of CUDA streams available for re-use\n         self._stream_pool: list[torch.cuda.Stream] = []\n         # list of CUDA events available for re-use\n@@ -132,16 +143,27 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n         src_to_dst_tensor = torch.from_numpy(src_to_dst)\n \n         stream = self._stream_pool.pop() if self._stream_pool else torch.cuda.Stream()\n-        event = self._event_pool.pop() if self._event_pool else torch.Event()\n+        start_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n+        end_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n \n         if self.gpu_to_cpu:\n             # wait for model computation to finish before offloading\n             stream.wait_stream(torch.cuda.current_stream())\n         if self._transfers:\n-            _, _, last_event = self._transfers[-1]\n+            last_transfer: Transfer = self._transfers[-1]\n+            last_event = last_transfer.end_event\n             # assure job will start only after the previous one completes\n             stream.wait_event(last_event)\n         with torch.cuda.stream(stream):\n+            start_event.record(stream)\n             for src_tensor, dst_tensor, block_size_in_bytes in zip(\n                 self.src_tensors,\n                 self.dst_tensors,\n@@ -153,22 +175,42 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     block_size_in_bytes,\n                     src_to_dst_tensor,\n                 )\n-            event.record(stream)\n-\n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+            end_event.record(stream)\n+\n+        self._transfer_events[job_id] = end_event\n+        self._transfers.append(\n+            Transfer(\n+                job_id=job_id,\n+                stream=stream,\n+                start_event=start_event,\n+                end_event=end_event,\n+                num_bytes=dst_sub_block_count * self.total_block_size_in_bytes,\n+            )\n+        )\n \n         # success\n         return True\n \n     def get_finished(self) -> list[TransferResult]:\n         results: list[TransferResult] = []\n-        while self._transfers and self._transfers[0][2].query():\n-            job_id, stream, event = self._transfers.popleft()\n-            results.append((job_id, True))\n-            self._stream_pool.append(stream)\n-            self._event_pool.append(event)\n-            del self._transfer_events[job_id]\n+        while self._transfers and self._transfers[0].end_event.query():\n+            transfer = self._transfers.popleft()\n+            transfer_time = (\n+                transfer.start_event.elapsed_time(transfer.end_event) * 1e-3\n+            )  # elapsed_time is in miliseconds\n+            result = TransferResult(\n+                job_id=transfer.job_id,\n+                success=True,\n+                transfer_size=transfer.num_bytes,\n+                transfer_time=transfer_time,\n+                transfer_type=self.transfer_type,\n+            )\n+\n+            results.append(result)\n+            self._stream_pool.append(transfer.stream)\n+            self._event_pool.append(transfer.end_event)\n+            self._event_pool.append(transfer.start_event)\n+            del self._transfer_events[transfer.job_id]\n         return results\n \n     def wait(self, job_ids: set[int]):", "patch_sha256": "559048399e0ddd6c"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2685116095", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/worker/cpu_gpu.py\nReview Comment:\nuse kwargs here as well\n\nOriginal Code Context:\n@@ -152,29 +175,54 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     ops.swap_blocks(src_value_cache, dst_value_cache, src_to_dst_tensor)\n                 else:\n                     ops.swap_blocks(src_tensor, dst_tensor, src_to_dst_tensor)\n-            event.record(stream)\n+            end_event.record(stream)\n \n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+        self._transfer_events[job_id] = (start_event, end_event)\n+        transfer_type: TransferType = (\n+            transfer_spec[0].medium(),\n+            transfer_spec[1].medium(),\n+        )\n+        self._transfers.append(\n+            Transfer(\n+                job_id,\n+                stream,\n+                start_event,\n+                end_event,\n+                src_sub_block_count,\n+                transfer_type,\n+            )\n+        )\n \n         # success\n         return True\n \n     def get_finished(self) -> list[TransferResult]:\n         results: list[TransferResult] = []\n-        while self._transfers and self._transfers[0][2].query():\n-            job_id, stream, event = self._transfers.popleft()\n-            results.append((job_id, True))\n-            self._stream_pool.append(stream)\n-            self._event_pool.append(event)\n-            del self._transfer_events[job_id]\n+        while self._transfers and self._transfers[0].end_event.query():\n+            transfer = self._transfers.popleft()\n+            transfer_time = transfer.start_event.elapsed_time(transfer.end_event)\n+            result = TransferResult(\n", "materials": ["use kwargs here as well"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,6 +1,7 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n from collections import deque\n+from dataclasses import dataclass\n \n import numpy as np\n import torch\n@@ -19,6 +20,15 @@\n logger = init_logger(__name__)\n \n \n+@dataclass\n+class Transfer:\n+    job_id: int\n+    stream: torch.cuda.Stream\n+    start_event: torch.Event\n+    end_event: torch.Event\n+    num_bytes: int\n+\n+\n def expand_block_ids(\n     block_ids: np.ndarray,\n     block_size_factor: int,\n@@ -92,14 +102,15 @@ def __init__(\n             tensor.element_size() * tensor.stride(0) * min_block_size_factor\n             for tensor in src_tensors\n         ]\n+        self.total_block_size_in_bytes = sum(self.block_size_in_bytes)\n \n         assert len(src_tensors) > 0\n         self.gpu_to_cpu: bool = self.src_tensors[0].is_cuda\n-\n+        self.transfer_type = (\"GPU\", \"CPU\") if self.gpu_to_cpu else (\"CPU\", \"GPU\")\n         # job_id -> event\n         self._transfer_events: dict[int, torch.Event] = {}\n         # queue of transfers (job_id, stream, event)\n-        self._transfers: deque[tuple[int, torch.cuda.Stream, torch.Event]] = deque()\n+        self._transfers: deque[Transfer] = deque()\n         # list of CUDA streams available for re-use\n         self._stream_pool: list[torch.cuda.Stream] = []\n         # list of CUDA events available for re-use\n@@ -132,16 +143,27 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n         src_to_dst_tensor = torch.from_numpy(src_to_dst)\n \n         stream = self._stream_pool.pop() if self._stream_pool else torch.cuda.Stream()\n-        event = self._event_pool.pop() if self._event_pool else torch.Event()\n+        start_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n+        end_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n \n         if self.gpu_to_cpu:\n             # wait for model computation to finish before offloading\n             stream.wait_stream(torch.cuda.current_stream())\n         if self._transfers:\n-            _, _, last_event = self._transfers[-1]\n+            last_transfer: Transfer = self._transfers[-1]\n+            last_event = last_transfer.end_event\n             # assure job will start only after the previous one completes\n             stream.wait_event(last_event)\n         with torch.cuda.stream(stream):\n+            start_event.record(stream)\n             for src_tensor, dst_tensor, block_size_in_bytes in zip(\n                 self.src_tensors,\n                 self.dst_tensors,\n@@ -153,22 +175,42 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     block_size_in_bytes,\n                     src_to_dst_tensor,\n                 )\n-            event.record(stream)\n-\n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+            end_event.record(stream)\n+\n+        self._transfer_events[job_id] = end_event\n+        self._transfers.append(\n+            Transfer(\n+                job_id=job_id,\n+                stream=stream,\n+                start_event=start_event,\n+                end_event=end_event,\n+                num_bytes=dst_sub_block_count * self.total_block_size_in_bytes,\n+            )\n+        )\n \n         # success\n         return True\n \n     def get_finished(self) -> list[TransferResult]:\n         results: list[TransferResult] = []\n-        while self._transfers and self._transfers[0][2].query():\n-            job_id, stream, event = self._transfers.popleft()\n-            results.append((job_id, True))\n-            self._stream_pool.append(stream)\n-            self._event_pool.append(event)\n-            del self._transfer_events[job_id]\n+        while self._transfers and self._transfers[0].end_event.query():\n+            transfer = self._transfers.popleft()\n+            transfer_time = (\n+                transfer.start_event.elapsed_time(transfer.end_event) * 1e-3\n+            )  # elapsed_time is in miliseconds\n+            result = TransferResult(\n+                job_id=transfer.job_id,\n+                success=True,\n+                transfer_size=transfer.num_bytes,\n+                transfer_time=transfer_time,\n+                transfer_type=self.transfer_type,\n+            )\n+\n+            results.append(result)\n+            self._stream_pool.append(transfer.stream)\n+            self._event_pool.append(transfer.end_event)\n+            self._event_pool.append(transfer.start_event)\n+            del self._transfer_events[transfer.job_id]\n         return results\n \n     def wait(self, job_ids: set[int]):", "patch_sha256": "559048399e0ddd6c"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2685136701", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/worker/cpu_gpu.py\nReview Comment:\nwe need to report size in bytes here.\r\nIt should be `dst_sub_block_count * self.block_size_in_bytes`\r\nInitialize `self.block_size_in_bytes` in `SingleDirectionOffloadingHandler.__init__` like this:\r\n```\r\n        self.block_size_in_bytes = 0\r\n        for tensor, split_k_and_v in zip(src_tensors, kv_dim_before_num_blocks):\r\n            if split_k_and_v:\r\n                self.block_size_in_bytes += 2 * tensor.stride(1) * tensor.element_size()\r\n            else:\r\n                self.block_size_in_bytes += tensor.stride(0) * tensor.element_size()\r\n```\n\nOriginal Code Context:\n@@ -152,29 +175,54 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     ops.swap_blocks(src_value_cache, dst_value_cache, src_to_dst_tensor)\n                 else:\n                     ops.swap_blocks(src_tensor, dst_tensor, src_to_dst_tensor)\n-            event.record(stream)\n+            end_event.record(stream)\n \n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+        self._transfer_events[job_id] = (start_event, end_event)\n+        transfer_type: TransferType = (\n+            transfer_spec[0].medium(),\n+            transfer_spec[1].medium(),\n+        )\n+        self._transfers.append(\n+            Transfer(\n+                job_id,\n+                stream,\n+                start_event,\n+                end_event,\n+                src_sub_block_count,\n", "materials": ["we need to report size in bytes here.\r\nIt should be `dst_sub_block_count * self.block_size_in_bytes`\r\nInitialize `self.block_size_in_bytes` in `SingleDirectionOffloadingHandler.__init__` like this:\r\n```\r\n        self.block_size_in_bytes = 0\r\n        for tensor, split_k_and_v in zip(src_tensors, kv_dim_before_num_blocks):\r\n            if split_k_and_v:\r\n                self.block_size_in_bytes += 2 * tensor.stride(1) * tensor.element_size()\r\n            else:\r\n                self.block_size_in_bytes += tensor.stride(0) * tensor.element_size()\r\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,6 +1,7 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n from collections import deque\n+from dataclasses import dataclass\n \n import numpy as np\n import torch\n@@ -19,6 +20,15 @@\n logger = init_logger(__name__)\n \n \n+@dataclass\n+class Transfer:\n+    job_id: int\n+    stream: torch.cuda.Stream\n+    start_event: torch.Event\n+    end_event: torch.Event\n+    num_bytes: int\n+\n+\n def expand_block_ids(\n     block_ids: np.ndarray,\n     block_size_factor: int,\n@@ -92,14 +102,15 @@ def __init__(\n             tensor.element_size() * tensor.stride(0) * min_block_size_factor\n             for tensor in src_tensors\n         ]\n+        self.total_block_size_in_bytes = sum(self.block_size_in_bytes)\n \n         assert len(src_tensors) > 0\n         self.gpu_to_cpu: bool = self.src_tensors[0].is_cuda\n-\n+        self.transfer_type = (\"GPU\", \"CPU\") if self.gpu_to_cpu else (\"CPU\", \"GPU\")\n         # job_id -> event\n         self._transfer_events: dict[int, torch.Event] = {}\n         # queue of transfers (job_id, stream, event)\n-        self._transfers: deque[tuple[int, torch.cuda.Stream, torch.Event]] = deque()\n+        self._transfers: deque[Transfer] = deque()\n         # list of CUDA streams available for re-use\n         self._stream_pool: list[torch.cuda.Stream] = []\n         # list of CUDA events available for re-use\n@@ -132,16 +143,27 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n         src_to_dst_tensor = torch.from_numpy(src_to_dst)\n \n         stream = self._stream_pool.pop() if self._stream_pool else torch.cuda.Stream()\n-        event = self._event_pool.pop() if self._event_pool else torch.Event()\n+        start_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n+        end_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n \n         if self.gpu_to_cpu:\n             # wait for model computation to finish before offloading\n             stream.wait_stream(torch.cuda.current_stream())\n         if self._transfers:\n-            _, _, last_event = self._transfers[-1]\n+            last_transfer: Transfer = self._transfers[-1]\n+            last_event = last_transfer.end_event\n             # assure job will start only after the previous one completes\n             stream.wait_event(last_event)\n         with torch.cuda.stream(stream):\n+            start_event.record(stream)\n             for src_tensor, dst_tensor, block_size_in_bytes in zip(\n                 self.src_tensors,\n                 self.dst_tensors,\n@@ -153,22 +175,42 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     block_size_in_bytes,\n                     src_to_dst_tensor,\n                 )\n-            event.record(stream)\n-\n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+            end_event.record(stream)\n+\n+        self._transfer_events[job_id] = end_event\n+        self._transfers.append(\n+            Transfer(\n+                job_id=job_id,\n+                stream=stream,\n+                start_event=start_event,\n+                end_event=end_event,\n+                num_bytes=dst_sub_block_count * self.total_block_size_in_bytes,\n+            )\n+        )\n \n         # success\n         return True\n \n     def get_finished(self) -> list[TransferResult]:\n         results: list[TransferResult] = []\n-        while self._transfers and self._transfers[0][2].query():\n-            job_id, stream, event = self._transfers.popleft()\n-            results.append((job_id, True))\n-            self._stream_pool.append(stream)\n-            self._event_pool.append(event)\n-            del self._transfer_events[job_id]\n+        while self._transfers and self._transfers[0].end_event.query():\n+            transfer = self._transfers.popleft()\n+            transfer_time = (\n+                transfer.start_event.elapsed_time(transfer.end_event) * 1e-3\n+            )  # elapsed_time is in miliseconds\n+            result = TransferResult(\n+                job_id=transfer.job_id,\n+                success=True,\n+                transfer_size=transfer.num_bytes,\n+                transfer_time=transfer_time,\n+                transfer_type=self.transfer_type,\n+            )\n+\n+            results.append(result)\n+            self._stream_pool.append(transfer.stream)\n+            self._event_pool.append(transfer.end_event)\n+            self._event_pool.append(transfer.start_event)\n+            del self._transfer_events[transfer.job_id]\n         return results\n \n     def wait(self, job_ids: set[int]):", "patch_sha256": "559048399e0ddd6c"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2685200587", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/worker/cpu_gpu.py\nReview Comment:\nno need to track start_event here, as `wait` only needs to sync on `end_event` (see my comment on `wait`).\n\nOriginal Code Context:\n@@ -96,10 +108,10 @@ def __init__(\n         assert len(src_tensors) > 0\n         self.gpu_to_cpu: bool = self.src_tensors[0].is_cuda\n \n-        # job_id -> event\n-        self._transfer_events: dict[int, torch.Event] = {}\n+        # job_id -> (start_event, end_event)\n+        self._transfer_events: dict[int, tuple[torch.Event, torch.Event]] = {}\n", "materials": ["no need to track start_event here, as `wait` only needs to sync on `end_event` (see my comment on `wait`)."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,6 +1,7 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n from collections import deque\n+from dataclasses import dataclass\n \n import numpy as np\n import torch\n@@ -19,6 +20,15 @@\n logger = init_logger(__name__)\n \n \n+@dataclass\n+class Transfer:\n+    job_id: int\n+    stream: torch.cuda.Stream\n+    start_event: torch.Event\n+    end_event: torch.Event\n+    num_bytes: int\n+\n+\n def expand_block_ids(\n     block_ids: np.ndarray,\n     block_size_factor: int,\n@@ -92,14 +102,15 @@ def __init__(\n             tensor.element_size() * tensor.stride(0) * min_block_size_factor\n             for tensor in src_tensors\n         ]\n+        self.total_block_size_in_bytes = sum(self.block_size_in_bytes)\n \n         assert len(src_tensors) > 0\n         self.gpu_to_cpu: bool = self.src_tensors[0].is_cuda\n-\n+        self.transfer_type = (\"GPU\", \"CPU\") if self.gpu_to_cpu else (\"CPU\", \"GPU\")\n         # job_id -> event\n         self._transfer_events: dict[int, torch.Event] = {}\n         # queue of transfers (job_id, stream, event)\n-        self._transfers: deque[tuple[int, torch.cuda.Stream, torch.Event]] = deque()\n+        self._transfers: deque[Transfer] = deque()\n         # list of CUDA streams available for re-use\n         self._stream_pool: list[torch.cuda.Stream] = []\n         # list of CUDA events available for re-use\n@@ -132,16 +143,27 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n         src_to_dst_tensor = torch.from_numpy(src_to_dst)\n \n         stream = self._stream_pool.pop() if self._stream_pool else torch.cuda.Stream()\n-        event = self._event_pool.pop() if self._event_pool else torch.Event()\n+        start_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n+        end_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n \n         if self.gpu_to_cpu:\n             # wait for model computation to finish before offloading\n             stream.wait_stream(torch.cuda.current_stream())\n         if self._transfers:\n-            _, _, last_event = self._transfers[-1]\n+            last_transfer: Transfer = self._transfers[-1]\n+            last_event = last_transfer.end_event\n             # assure job will start only after the previous one completes\n             stream.wait_event(last_event)\n         with torch.cuda.stream(stream):\n+            start_event.record(stream)\n             for src_tensor, dst_tensor, block_size_in_bytes in zip(\n                 self.src_tensors,\n                 self.dst_tensors,\n@@ -153,22 +175,42 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     block_size_in_bytes,\n                     src_to_dst_tensor,\n                 )\n-            event.record(stream)\n-\n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+            end_event.record(stream)\n+\n+        self._transfer_events[job_id] = end_event\n+        self._transfers.append(\n+            Transfer(\n+                job_id=job_id,\n+                stream=stream,\n+                start_event=start_event,\n+                end_event=end_event,\n+                num_bytes=dst_sub_block_count * self.total_block_size_in_bytes,\n+            )\n+        )\n \n         # success\n         return True\n \n     def get_finished(self) -> list[TransferResult]:\n         results: list[TransferResult] = []\n-        while self._transfers and self._transfers[0][2].query():\n-            job_id, stream, event = self._transfers.popleft()\n-            results.append((job_id, True))\n-            self._stream_pool.append(stream)\n-            self._event_pool.append(event)\n-            del self._transfer_events[job_id]\n+        while self._transfers and self._transfers[0].end_event.query():\n+            transfer = self._transfers.popleft()\n+            transfer_time = (\n+                transfer.start_event.elapsed_time(transfer.end_event) * 1e-3\n+            )  # elapsed_time is in miliseconds\n+            result = TransferResult(\n+                job_id=transfer.job_id,\n+                success=True,\n+                transfer_size=transfer.num_bytes,\n+                transfer_time=transfer_time,\n+                transfer_type=self.transfer_type,\n+            )\n+\n+            results.append(result)\n+            self._stream_pool.append(transfer.stream)\n+            self._event_pool.append(transfer.end_event)\n+            self._event_pool.append(transfer.start_event)\n+            del self._transfer_events[transfer.job_id]\n         return results\n \n     def wait(self, job_ids: set[int]):", "patch_sha256": "559048399e0ddd6c"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2685203243", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/worker/worker.py\nReview Comment:\nLet's make these optional:\r\n```suggestion\r\n    transfer_size: int | None = None\r\n    transfer_time: float | None = None\r\n    transfer_type: TransferType | None = None\r\n```\n\nOriginal Code Context:\n@@ -9,12 +10,26 @@\n TransferSpec = tuple[LoadStoreSpec, LoadStoreSpec]\n # transfers are forwarded to workers by (src_medium, dst_medium)\n TransferType = tuple[str, str]\n-# transfer result (job_id, success)\n-TransferResult = tuple[int, bool]\n \n logger = init_logger(__name__)\n \n \n+@dataclass\n+class TransferStats:\n+    num_blocks: int\n+    time: float  # Can be start_time or duration\n+    transfer_type: TransferType\n+\n+\n+@dataclass\n+class TransferResult:\n+    job_id: int\n+    success: bool\n+    transfer_size: int\n+    transfer_time: float\n+    transfer_type: TransferType\n", "materials": ["Let's make these optional:\r\n```suggestion\r\n    transfer_size: int | None = None\r\n    transfer_time: float | None = None\r\n    transfer_type: TransferType | None = None\r\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,6 +1,7 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n from abc import ABC, abstractmethod\n+from dataclasses import dataclass\n \n from vllm.logger import init_logger\n from vllm.v1.kv_offload.abstract import LoadStoreSpec\n@@ -9,12 +10,19 @@\n TransferSpec = tuple[LoadStoreSpec, LoadStoreSpec]\n # transfers are forwarded to workers by (src_medium, dst_medium)\n TransferType = tuple[str, str]\n-# transfer result (job_id, success)\n-TransferResult = tuple[int, bool]\n \n logger = init_logger(__name__)\n \n \n+@dataclass\n+class TransferResult:\n+    job_id: int\n+    success: bool\n+    transfer_size: int | None = None  # Size in bytes\n+    transfer_time: float | None = None\n+    transfer_type: TransferType | None = None\n+\n+\n class OffloadingHandler(ABC):\n     \"\"\"\n     OffloadingHandler class for managing asynchronous KV data transfers\n@@ -57,7 +65,6 @@ def get_finished(self) -> list[TransferResult]:\n     def wait(self, job_ids: set[int]) -> None:\n         \"\"\"\n         Wait for jobs to finish (blocking).\n-\n         Args:\n             job_ids: The set of job IDs to wait for.\n         \"\"\"\n@@ -120,7 +127,6 @@ def transfer_async(self, job_id: int, spec: TransferSpec) -> bool:\n         transfer_type = (src.medium(), dst.medium())\n         handler = self.transfer_type_to_handler.get(transfer_type)\n         assert handler is not None\n-\n         try:\n             success = handler.transfer_async(job_id, spec)\n         except Exception as e:\n@@ -137,15 +143,14 @@ def transfer_async(self, job_id: int, spec: TransferSpec) -> bool:\n             logger.warning(\"Failed to submit %r transfer %d\", transfer_type, job_id)\n         else:\n             logger.debug(\"Submitted %r transfer %d: %r\", transfer_type, job_id, spec)\n-\n         return success\n \n     def get_finished(self) -> list[TransferResult]:\n         \"\"\"\n         Get transfers finished since last call.\n \n         Returns:\n-            A list of (job_id, success) of transfers.\n+            A list of TransferResults\n         \"\"\"\n         finished = []\n         for handler in self.handlers:", "patch_sha256": "09ea7eae1278ac87"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2685204993", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/worker/worker.py\nReview Comment:\nmaybe rename to `transfer_size_bytes` or add a comment explaining this is a size in bytes.\n\nOriginal Code Context:\n@@ -9,12 +10,26 @@\n TransferSpec = tuple[LoadStoreSpec, LoadStoreSpec]\n # transfers are forwarded to workers by (src_medium, dst_medium)\n TransferType = tuple[str, str]\n-# transfer result (job_id, success)\n-TransferResult = tuple[int, bool]\n \n logger = init_logger(__name__)\n \n \n+@dataclass\n+class TransferStats:\n+    num_blocks: int\n+    time: float  # Can be start_time or duration\n+    transfer_type: TransferType\n+\n+\n+@dataclass\n+class TransferResult:\n+    job_id: int\n+    success: bool\n+    transfer_size: int\n", "materials": ["maybe rename to `transfer_size_bytes` or add a comment explaining this is a size in bytes."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,6 +1,7 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n from abc import ABC, abstractmethod\n+from dataclasses import dataclass\n \n from vllm.logger import init_logger\n from vllm.v1.kv_offload.abstract import LoadStoreSpec\n@@ -9,12 +10,19 @@\n TransferSpec = tuple[LoadStoreSpec, LoadStoreSpec]\n # transfers are forwarded to workers by (src_medium, dst_medium)\n TransferType = tuple[str, str]\n-# transfer result (job_id, success)\n-TransferResult = tuple[int, bool]\n \n logger = init_logger(__name__)\n \n \n+@dataclass\n+class TransferResult:\n+    job_id: int\n+    success: bool\n+    transfer_size: int | None = None  # Size in bytes\n+    transfer_time: float | None = None\n+    transfer_type: TransferType | None = None\n+\n+\n class OffloadingHandler(ABC):\n     \"\"\"\n     OffloadingHandler class for managing asynchronous KV data transfers\n@@ -57,7 +65,6 @@ def get_finished(self) -> list[TransferResult]:\n     def wait(self, job_ids: set[int]) -> None:\n         \"\"\"\n         Wait for jobs to finish (blocking).\n-\n         Args:\n             job_ids: The set of job IDs to wait for.\n         \"\"\"\n@@ -120,7 +127,6 @@ def transfer_async(self, job_id: int, spec: TransferSpec) -> bool:\n         transfer_type = (src.medium(), dst.medium())\n         handler = self.transfer_type_to_handler.get(transfer_type)\n         assert handler is not None\n-\n         try:\n             success = handler.transfer_async(job_id, spec)\n         except Exception as e:\n@@ -137,15 +143,14 @@ def transfer_async(self, job_id: int, spec: TransferSpec) -> bool:\n             logger.warning(\"Failed to submit %r transfer %d\", transfer_type, job_id)\n         else:\n             logger.debug(\"Submitted %r transfer %d: %r\", transfer_type, job_id, spec)\n-\n         return success\n \n     def get_finished(self) -> list[TransferResult]:\n         \"\"\"\n         Get transfers finished since last call.\n \n         Returns:\n-            A list of (job_id, success) of transfers.\n+            A list of TransferResults\n         \"\"\"\n         finished = []\n         for handler in self.handlers:", "patch_sha256": "09ea7eae1278ac87"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2702361883", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/worker/cpu_gpu.py\nReview Comment:\n```suggestion\r\n        self.transfer_type = (\"GPU\", \"CPU\") if self.gpu_to_cpu else (\"CPU\", \"GPU\")\r\n```\n\nOriginal Code Context:\n@@ -92,18 +102,25 @@ def __init__(\n         self.kv_dim_before_num_blocks: list[bool] = kv_dim_before_num_blocks\n         self.src_block_size_factor: int = src_block_size_factor\n         self.dst_block_size_factor: int = dst_block_size_factor\n-\n         assert len(src_tensors) > 0\n         self.gpu_to_cpu: bool = self.src_tensors[0].is_cuda\n-\n+        self.transfer_type: tuple[str, str] = (\"CPU\", \"GPU\")\n+        if self.gpu_to_cpu:\n+            self.transfer_type = (\"GPU\", \"CPU\")\n", "materials": ["```suggestion\r\n        self.transfer_type = (\"GPU\", \"CPU\") if self.gpu_to_cpu else (\"CPU\", \"GPU\")\r\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,6 +1,7 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n from collections import deque\n+from dataclasses import dataclass\n \n import numpy as np\n import torch\n@@ -19,6 +20,15 @@\n logger = init_logger(__name__)\n \n \n+@dataclass\n+class Transfer:\n+    job_id: int\n+    stream: torch.cuda.Stream\n+    start_event: torch.Event\n+    end_event: torch.Event\n+    num_bytes: int\n+\n+\n def expand_block_ids(\n     block_ids: np.ndarray,\n     block_size_factor: int,\n@@ -92,14 +102,15 @@ def __init__(\n             tensor.element_size() * tensor.stride(0) * min_block_size_factor\n             for tensor in src_tensors\n         ]\n+        self.total_block_size_in_bytes = sum(self.block_size_in_bytes)\n \n         assert len(src_tensors) > 0\n         self.gpu_to_cpu: bool = self.src_tensors[0].is_cuda\n-\n+        self.transfer_type = (\"GPU\", \"CPU\") if self.gpu_to_cpu else (\"CPU\", \"GPU\")\n         # job_id -> event\n         self._transfer_events: dict[int, torch.Event] = {}\n         # queue of transfers (job_id, stream, event)\n-        self._transfers: deque[tuple[int, torch.cuda.Stream, torch.Event]] = deque()\n+        self._transfers: deque[Transfer] = deque()\n         # list of CUDA streams available for re-use\n         self._stream_pool: list[torch.cuda.Stream] = []\n         # list of CUDA events available for re-use\n@@ -132,16 +143,27 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n         src_to_dst_tensor = torch.from_numpy(src_to_dst)\n \n         stream = self._stream_pool.pop() if self._stream_pool else torch.cuda.Stream()\n-        event = self._event_pool.pop() if self._event_pool else torch.Event()\n+        start_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n+        end_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n \n         if self.gpu_to_cpu:\n             # wait for model computation to finish before offloading\n             stream.wait_stream(torch.cuda.current_stream())\n         if self._transfers:\n-            _, _, last_event = self._transfers[-1]\n+            last_transfer: Transfer = self._transfers[-1]\n+            last_event = last_transfer.end_event\n             # assure job will start only after the previous one completes\n             stream.wait_event(last_event)\n         with torch.cuda.stream(stream):\n+            start_event.record(stream)\n             for src_tensor, dst_tensor, block_size_in_bytes in zip(\n                 self.src_tensors,\n                 self.dst_tensors,\n@@ -153,22 +175,42 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     block_size_in_bytes,\n                     src_to_dst_tensor,\n                 )\n-            event.record(stream)\n-\n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+            end_event.record(stream)\n+\n+        self._transfer_events[job_id] = end_event\n+        self._transfers.append(\n+            Transfer(\n+                job_id=job_id,\n+                stream=stream,\n+                start_event=start_event,\n+                end_event=end_event,\n+                num_bytes=dst_sub_block_count * self.total_block_size_in_bytes,\n+            )\n+        )\n \n         # success\n         return True\n \n     def get_finished(self) -> list[TransferResult]:\n         results: list[TransferResult] = []\n-        while self._transfers and self._transfers[0][2].query():\n-            job_id, stream, event = self._transfers.popleft()\n-            results.append((job_id, True))\n-            self._stream_pool.append(stream)\n-            self._event_pool.append(event)\n-            del self._transfer_events[job_id]\n+        while self._transfers and self._transfers[0].end_event.query():\n+            transfer = self._transfers.popleft()\n+            transfer_time = (\n+                transfer.start_event.elapsed_time(transfer.end_event) * 1e-3\n+            )  # elapsed_time is in miliseconds\n+            result = TransferResult(\n+                job_id=transfer.job_id,\n+                success=True,\n+                transfer_size=transfer.num_bytes,\n+                transfer_time=transfer_time,\n+                transfer_type=self.transfer_type,\n+            )\n+\n+            results.append(result)\n+            self._stream_pool.append(transfer.stream)\n+            self._event_pool.append(transfer.end_event)\n+            self._event_pool.append(transfer.start_event)\n+            del self._transfer_events[transfer.job_id]\n         return results\n \n     def wait(self, job_ids: set[int]):", "patch_sha256": "559048399e0ddd6c"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2704412336", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/distributed/kv_transfer/kv_connector/v1/metrics.py\nReview Comment:\nThis shouldn't be needed if you move the per-transfer-type metric creation to the constructor\n\nOriginal Code Context:\n@@ -124,7 +124,7 @@ def __init__(\n         self._counter_cls = metric_types[Counter]\n         self._histogram_cls = metric_types[Histogram]\n         self._labelnames = labelnames\n-        self._per_engine_labelvalues = per_engine_labelvalues\n+        self.per_engine_labelvalues = per_engine_labelvalues\n", "materials": ["This shouldn't be needed if you move the per-transfer-type metric creation to the constructor"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -124,7 +124,7 @@ def __init__(\n         self._counter_cls = metric_types[Counter]\n         self._histogram_cls = metric_types[Histogram]\n         self._labelnames = labelnames\n-        self._per_engine_labelvalues = per_engine_labelvalues\n+        self.per_engine_labelvalues = per_engine_labelvalues\n \n     def make_per_engine(self, metric: PromMetric) -> dict[int, PromMetric]:\n         \"\"\"\n@@ -134,7 +134,7 @@ def make_per_engine(self, metric: PromMetric) -> dict[int, PromMetric]:\n         \"\"\"\n         return {\n             idx: metric.labels(*labelvalues)\n-            for idx, labelvalues in self._per_engine_labelvalues.items()\n+            for idx, labelvalues in self.per_engine_labelvalues.items()\n         }\n \n     def observe(self, transfer_stats_data: dict[str, Any], engine_idx: int = 0):", "patch_sha256": "f410095923297be2"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2709006652", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/distributed/kv_transfer/kv_connector/v1/metrics.py\nReview Comment:\nUnderstand now that externally loaded transfer types is what's driving the need for this flexibility\n\nOriginal Code Context:\n@@ -124,7 +124,7 @@ def __init__(\n         self._counter_cls = metric_types[Counter]\n         self._histogram_cls = metric_types[Histogram]\n         self._labelnames = labelnames\n-        self._per_engine_labelvalues = per_engine_labelvalues\n+        self.per_engine_labelvalues = per_engine_labelvalues\n", "materials": ["Understand now that externally loaded transfer types is what's driving the need for this flexibility"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -124,7 +124,7 @@ def __init__(\n         self._counter_cls = metric_types[Counter]\n         self._histogram_cls = metric_types[Histogram]\n         self._labelnames = labelnames\n-        self._per_engine_labelvalues = per_engine_labelvalues\n+        self.per_engine_labelvalues = per_engine_labelvalues\n \n     def make_per_engine(self, metric: PromMetric) -> dict[int, PromMetric]:\n         \"\"\"\n@@ -134,7 +134,7 @@ def make_per_engine(self, metric: PromMetric) -> dict[int, PromMetric]:\n         \"\"\"\n         return {\n             idx: metric.labels(*labelvalues)\n-            for idx, labelvalues in self._per_engine_labelvalues.items()\n+            for idx, labelvalues in self.per_engine_labelvalues.items()\n         }\n \n     def observe(self, transfer_stats_data: dict[str, Any], engine_idx: int = 0):", "patch_sha256": "f410095923297be2"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2717155674", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/kv_offload/worker/cpu_gpu.py\nReview Comment:\nI think `self.block_size_in_bytes` is a list.\r\nCan you define `self.total_block_size_in_bytes` which is the sum of this list and use it instead?\n\nOriginal Code Context:\n@@ -153,22 +174,42 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     block_size_in_bytes,\n                     src_to_dst_tensor,\n                 )\n-            event.record(stream)\n-\n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+            end_event.record(stream)\n+\n+        self._transfer_events[job_id] = end_event\n+        self._transfers.append(\n+            Transfer(\n+                job_id=job_id,\n+                stream=stream,\n+                start_event=start_event,\n+                end_event=end_event,\n+                num_bytes=dst_sub_block_count * self.block_size_in_bytes,\n", "materials": ["I think `self.block_size_in_bytes` is a list.\r\nCan you define `self.total_block_size_in_bytes` which is the sum of this list and use it instead?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1,6 +1,7 @@\n # SPDX-License-Identifier: Apache-2.0\n # SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n from collections import deque\n+from dataclasses import dataclass\n \n import numpy as np\n import torch\n@@ -19,6 +20,15 @@\n logger = init_logger(__name__)\n \n \n+@dataclass\n+class Transfer:\n+    job_id: int\n+    stream: torch.cuda.Stream\n+    start_event: torch.Event\n+    end_event: torch.Event\n+    num_bytes: int\n+\n+\n def expand_block_ids(\n     block_ids: np.ndarray,\n     block_size_factor: int,\n@@ -92,14 +102,15 @@ def __init__(\n             tensor.element_size() * tensor.stride(0) * min_block_size_factor\n             for tensor in src_tensors\n         ]\n+        self.total_block_size_in_bytes = sum(self.block_size_in_bytes)\n \n         assert len(src_tensors) > 0\n         self.gpu_to_cpu: bool = self.src_tensors[0].is_cuda\n-\n+        self.transfer_type = (\"GPU\", \"CPU\") if self.gpu_to_cpu else (\"CPU\", \"GPU\")\n         # job_id -> event\n         self._transfer_events: dict[int, torch.Event] = {}\n         # queue of transfers (job_id, stream, event)\n-        self._transfers: deque[tuple[int, torch.cuda.Stream, torch.Event]] = deque()\n+        self._transfers: deque[Transfer] = deque()\n         # list of CUDA streams available for re-use\n         self._stream_pool: list[torch.cuda.Stream] = []\n         # list of CUDA events available for re-use\n@@ -132,16 +143,27 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n         src_to_dst_tensor = torch.from_numpy(src_to_dst)\n \n         stream = self._stream_pool.pop() if self._stream_pool else torch.cuda.Stream()\n-        event = self._event_pool.pop() if self._event_pool else torch.Event()\n+        start_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n+        end_event = (\n+            self._event_pool.pop()\n+            if self._event_pool\n+            else torch.Event(enable_timing=True)\n+        )\n \n         if self.gpu_to_cpu:\n             # wait for model computation to finish before offloading\n             stream.wait_stream(torch.cuda.current_stream())\n         if self._transfers:\n-            _, _, last_event = self._transfers[-1]\n+            last_transfer: Transfer = self._transfers[-1]\n+            last_event = last_transfer.end_event\n             # assure job will start only after the previous one completes\n             stream.wait_event(last_event)\n         with torch.cuda.stream(stream):\n+            start_event.record(stream)\n             for src_tensor, dst_tensor, block_size_in_bytes in zip(\n                 self.src_tensors,\n                 self.dst_tensors,\n@@ -153,22 +175,42 @@ def transfer_async(self, job_id: int, transfer_spec: TransferSpec) -> bool:\n                     block_size_in_bytes,\n                     src_to_dst_tensor,\n                 )\n-            event.record(stream)\n-\n-        self._transfer_events[job_id] = event\n-        self._transfers.append((job_id, stream, event))\n+            end_event.record(stream)\n+\n+        self._transfer_events[job_id] = end_event\n+        self._transfers.append(\n+            Transfer(\n+                job_id=job_id,\n+                stream=stream,\n+                start_event=start_event,\n+                end_event=end_event,\n+                num_bytes=dst_sub_block_count * self.total_block_size_in_bytes,\n+            )\n+        )\n \n         # success\n         return True\n \n     def get_finished(self) -> list[TransferResult]:\n         results: list[TransferResult] = []\n-        while self._transfers and self._transfers[0][2].query():\n-            job_id, stream, event = self._transfers.popleft()\n-            results.append((job_id, True))\n-            self._stream_pool.append(stream)\n-            self._event_pool.append(event)\n-            del self._transfer_events[job_id]\n+        while self._transfers and self._transfers[0].end_event.query():\n+            transfer = self._transfers.popleft()\n+            transfer_time = (\n+                transfer.start_event.elapsed_time(transfer.end_event) * 1e-3\n+            )  # elapsed_time is in miliseconds\n+            result = TransferResult(\n+                job_id=transfer.job_id,\n+                success=True,\n+                transfer_size=transfer.num_bytes,\n+                transfer_time=transfer_time,\n+                transfer_type=self.transfer_type,\n+            )\n+\n+            results.append(result)\n+            self._stream_pool.append(transfer.stream)\n+            self._event_pool.append(transfer.end_event)\n+            self._event_pool.append(transfer.start_event)\n+            del self._transfer_events[transfer.job_id]\n         return results\n \n     def wait(self, job_ids: set[int]):", "patch_sha256": "559048399e0ddd6c"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28284/review_2502443246", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28284", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/engine/arg_utils.py\nReview Comment:\n![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThe argument `enable_return_routed_experts` is defined twice in the `EngineArgs` class (the first definition is at line 351). This is redundant and can lead to confusion. Please remove this second definition.\n\nOriginal Code Context:\n@@ -442,6 +443,7 @@ class EngineArgs:\n     tokenizer_revision: str | None = ModelConfig.tokenizer_revision\n     quantization: QuantizationMethods | None = ModelConfig.quantization\n     enforce_eager: bool = ModelConfig.enforce_eager\n+    enable_return_routed_experts: bool = ModelConfig.enable_return_routed_experts\n", "materials": ["![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThe argument `enable_return_routed_experts` is defined twice in the `EngineArgs` class (the first definition is at line 351). This is redundant and can lead to confusion. Please remove this second definition."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -354,6 +354,7 @@ class EngineArgs:\n     \"\"\"Arguments for vLLM engine.\"\"\"\n \n     model: str = ModelConfig.model\n+    enable_return_routed_experts: bool = ModelConfig.enable_return_routed_experts\n     model_weights: str = ModelConfig.model_weights\n     served_model_name: str | list[str] | None = ModelConfig.served_model_name\n     tokenizer: str | None = ModelConfig.tokenizer\n@@ -657,6 +658,10 @@ def add_cli_args(parser: FlexibleArgumentParser) -> FlexibleArgumentParser:\n             **model_kwargs[\"allow_deprecated_quantization\"],\n         )\n         model_group.add_argument(\"--enforce-eager\", **model_kwargs[\"enforce_eager\"])\n+        model_group.add_argument(\n+            \"--enable-return-routed-experts\",\n+            **model_kwargs[\"enable_return_routed_experts\"],\n+        )\n         model_group.add_argument(\"--max-logprobs\", **model_kwargs[\"max_logprobs\"])\n         model_group.add_argument(\"--logprobs-mode\", **model_kwargs[\"logprobs_mode\"])\n         model_group.add_argument(\n@@ -1239,6 +1244,7 @@ def create_model_config(self) -> ModelConfig:\n             quantization=self.quantization,\n             allow_deprecated_quantization=self.allow_deprecated_quantization,\n             enforce_eager=self.enforce_eager,\n+            enable_return_routed_experts=self.enable_return_routed_experts,\n             max_logprobs=self.max_logprobs,\n             logprobs_mode=self.logprobs_mode,\n             disable_sliding_window=self.disable_sliding_window,", "patch_sha256": "4fae94480febabc6"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28284/review_2502443278", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28284", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/core/sched/scheduler.py\nReview Comment:\n![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThe comments in this section are in Chinese. Please write comments in English for consistency and maintainability.\n\n```suggestion\n                    # Calculate slot mapping\n                    block_ids_array = np.array(block_ids, dtype=np.int32)\n                    num_blocks = len(block_ids)\n                    block_size = self.block_size\n\n                    # Generate in-block offsets\n                    block_offsets = np.arange(0, block_size)\n\n                    # Calculate slot mapping: slot = block_id * block_size + offset\n                    slot_mapping = (\n                        block_offsets.reshape((1, block_size))\n                        + block_ids_array.reshape((num_blocks, 1)) * block_size\n                    ).flatten()[:num_tokens]\n```\n\nOriginal Code Context:\n@@ -980,7 +989,30 @@ def update_from_output(\n                 pooler_output = pooler_outputs[req_index]\n                 stopped = check_stop(request, self.max_model_len, pooler_output)\n \n+            routed_experts = None\n             if stopped:\n+                if self.vllm_config.model_config.enable_return_routed_experts:\n+                    assert len(self.kv_cache_config.kv_cache_groups) == 1\n+\n+                    kv_blocks = self.kv_cache_manager.get_blocks(request.request_id)  \n+                    block_ids = kv_blocks.get_block_ids()[0] \n+                    num_tokens = request.num_tokens-1\n+\n+                    # 计算slot mapping  \n+                    block_ids_array = np.array(block_ids, dtype=np.int32)  \n+                    num_blocks = len(block_ids)  \n+                    block_size = self.block_size  \n+\n+                    # 生成block内偏移  \n+                    block_offsets = np.arange(0, block_size)  \n+\n+                    # 计算slot mapping: slot = block_id * block_size + offset  \n+                    slot_mapping = (  \n+                        block_offsets.reshape((1, block_size))  \n+                        + block_ids_array.reshape((num_blocks, 1)) * block_size  \n+                    ).flatten()[:num_tokens]  \n", "materials": ["![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThe comments in this section are in Chinese. Please write comments in English for consistency and maintainability.\n\n```suggestion\n                    # Calculate slot mapping\n                    block_ids_array = np.array(block_ids, dtype=np.int32)\n                    num_blocks = len(block_ids)\n                    block_size = self.block_size\n\n                    # Generate in-block offsets\n                    block_offsets = np.arange(0, block_size)\n\n                    # Calculate slot mapping: slot = block_id * block_size + offset\n                    slot_mapping = (\n                        block_offsets.reshape((1, block_size))\n                        + block_ids_array.reshape((num_blocks, 1)) * block_size\n                    ).flatten()[:num_tokens]\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -6,6 +6,8 @@\n from collections.abc import Iterable\n from typing import Any\n \n+import numpy as np\n+\n from vllm import envs\n from vllm.compilation.cuda_graph import CUDAGraphStat\n from vllm.config import VllmConfig\n@@ -24,6 +26,9 @@\n from vllm.distributed.kv_transfer.kv_connector.v1.base import KVConnectorMetadata\n from vllm.distributed.kv_transfer.kv_connector.v1.metrics import KVConnectorStats\n from vllm.logger import init_logger\n+from vllm.model_executor.layers.fused_moe.routed_experts_capturer import (\n+    RoutedExpertsReader,\n+)\n from vllm.multimodal import MULTIMODAL_REGISTRY, MultiModalRegistry\n from vllm.v1.core.encoder_cache_manager import (\n     EncoderCacheManager,\n@@ -219,11 +224,31 @@ def __init__(\n         )\n         self.use_pp = self.parallel_config.pipeline_parallel_size > 1\n         self.use_v2_model_runner = envs.VLLM_USE_V2_MODEL_RUNNER\n-\n         self.perf_metrics: ModelMetrics | None = None\n         if self.log_stats and vllm_config.observability_config.enable_mfu_metrics:\n             self.perf_metrics = ModelMetrics(vllm_config)\n \n+        if self.vllm_config.model_config.enable_return_routed_experts:\n+            assert self.dcp_world_size == 1 and self.pcp_world_size == 1, (\n+                \"enable_return_routed_experts does not support context parallelism \"\n+                \"(dcp_world_size > 1 or pcp_world_size > 1)\"\n+            )\n+\n+            self.routed_experts_reader = RoutedExpertsReader.create()\n+\n+            assert len(kv_cache_config.kv_cache_groups) > 0, (\n+                \"enable_return_routed_experts requires at least one kv cache group\"\n+            )\n+            self.max_num_kv_tokens = (\n+                kv_cache_config.num_blocks // len(kv_cache_config.kv_cache_groups) + 1\n+            ) * self.block_size\n+\n+            self.routed_experts_reader.attach_buffer(\n+                max_num_kv_tokens=self.max_num_kv_tokens,\n+                model_config=self.vllm_config.model_config,\n+                instance_id=self.vllm_config.instance_id,\n+            )\n+\n     def schedule(self) -> SchedulerOutput:\n         # NOTE(woosuk) on the scheduling algorithm:\n         # There's no \"decoding phase\" nor \"prefill phase\" in the scheduler.\n@@ -1162,7 +1187,30 @@ def update_from_output(\n                 request.status = RequestStatus.FINISHED_STOPPED\n                 stopped = True\n \n+            routed_experts = None\n             if stopped:\n+                if self.vllm_config.model_config.enable_return_routed_experts:\n+                    kv_blocks = self.kv_cache_manager.get_blocks(request.request_id)\n+                    block_ids = kv_blocks.get_block_ids()[0]\n+                    num_tokens = request.num_tokens - 1\n+\n+                    # compute slot mapping\n+                    block_ids_array = np.array(block_ids, dtype=np.int32)\n+                    num_blocks = len(block_ids)\n+                    block_size = self.block_size\n+\n+                    # generate block offsets\n+                    block_offsets = np.arange(0, block_size)\n+\n+                    # compute slot mapping: slot = block_id * block_size + offset\n+                    slot_mapping = (\n+                        block_offsets.reshape((1, block_size))\n+                        + block_ids_array.reshape((num_blocks, 1)) * block_size\n+                    ).flatten()[:num_tokens]\n+\n+                    routed_experts = self.routed_experts_reader.get_routed_experts(\n+                        indices=slot_mapping\n+                    )\n                 kv_transfer_params = self._free_request(request)\n                 if status_before_stop == RequestStatus.RUNNING:\n                     stopped_running_reqs.add(request)\n@@ -1209,6 +1257,7 @@ def update_from_output(\n                         kv_transfer_params=kv_transfer_params,\n                         trace_headers=request.trace_headers,\n                         num_cached_tokens=request.num_cached_tokens,\n+                        routed_experts=routed_experts,\n                         num_nans_in_logits=request.num_nans_in_logits,\n                     )\n                 )", "patch_sha256": "818f0328ac44b0db"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28284/review_2652808705", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28284", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/core/sched/scheduler.py\nReview Comment:\n# when world_size==1 (e.g. tp=1 with multiple independent servers)\r\n# use vllm_config.instance_id to ensure each server have a unique ID\r\n\r\nif world_size==1:\r\n   self.instance_id=self.vllm_config.instance_id\r\nelse:\r\n  self.instance_id = f\"rank_{rank // world_size}\"\r\n\n\nOriginal Code Context:\n@@ -219,11 +224,31 @@ def __init__(\n         )\n         self.use_pp = self.parallel_config.pipeline_parallel_size > 1\n         self.use_v2_model_runner = envs.VLLM_USE_V2_MODEL_RUNNER\n-\n         self.perf_metrics: ModelMetrics | None = None\n         if self.log_stats and vllm_config.observability_config.enable_mfu_metrics:\n             self.perf_metrics = ModelMetrics(vllm_config)\n \n+        self.max_num_kv_tokens = (\n+            kv_cache_config.num_blocks // len(kv_cache_config.kv_cache_groups) + 1\n+        ) * self.block_size\n+\n+        self.routed_experts_reader = RoutedExpertsReader.create(\n+            enable=self.vllm_config.model_config.enable_return_routed_experts\n+        )\n+\n+        if \":\" in self.vllm_config.instance_id:  # for async mode in verl\n+            self.instance_id = self.vllm_config.instance_id.rsplit(\":\", 1)[-1]\n+        else:  # sync mode in verl\n+            rank = self.vllm_config.parallel_config.rank\n+            world_size = self.vllm_config.parallel_config.world_size\n+            self.instance_id = f\"rank_{rank // world_size}\"\n+\n", "materials": ["# when world_size==1 (e.g. tp=1 with multiple independent servers)\r\n# use vllm_config.instance_id to ensure each server have a unique ID\r\n\r\nif world_size==1:\r\n   self.instance_id=self.vllm_config.instance_id\r\nelse:\r\n  self.instance_id = f\"rank_{rank // world_size}\"\r\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -6,6 +6,8 @@\n from collections.abc import Iterable\n from typing import Any\n \n+import numpy as np\n+\n from vllm import envs\n from vllm.compilation.cuda_graph import CUDAGraphStat\n from vllm.config import VllmConfig\n@@ -24,6 +26,9 @@\n from vllm.distributed.kv_transfer.kv_connector.v1.base import KVConnectorMetadata\n from vllm.distributed.kv_transfer.kv_connector.v1.metrics import KVConnectorStats\n from vllm.logger import init_logger\n+from vllm.model_executor.layers.fused_moe.routed_experts_capturer import (\n+    RoutedExpertsReader,\n+)\n from vllm.multimodal import MULTIMODAL_REGISTRY, MultiModalRegistry\n from vllm.v1.core.encoder_cache_manager import (\n     EncoderCacheManager,\n@@ -219,11 +224,31 @@ def __init__(\n         )\n         self.use_pp = self.parallel_config.pipeline_parallel_size > 1\n         self.use_v2_model_runner = envs.VLLM_USE_V2_MODEL_RUNNER\n-\n         self.perf_metrics: ModelMetrics | None = None\n         if self.log_stats and vllm_config.observability_config.enable_mfu_metrics:\n             self.perf_metrics = ModelMetrics(vllm_config)\n \n+        if self.vllm_config.model_config.enable_return_routed_experts:\n+            assert self.dcp_world_size == 1 and self.pcp_world_size == 1, (\n+                \"enable_return_routed_experts does not support context parallelism \"\n+                \"(dcp_world_size > 1 or pcp_world_size > 1)\"\n+            )\n+\n+            self.routed_experts_reader = RoutedExpertsReader.create()\n+\n+            assert len(kv_cache_config.kv_cache_groups) > 0, (\n+                \"enable_return_routed_experts requires at least one kv cache group\"\n+            )\n+            self.max_num_kv_tokens = (\n+                kv_cache_config.num_blocks // len(kv_cache_config.kv_cache_groups) + 1\n+            ) * self.block_size\n+\n+            self.routed_experts_reader.attach_buffer(\n+                max_num_kv_tokens=self.max_num_kv_tokens,\n+                model_config=self.vllm_config.model_config,\n+                instance_id=self.vllm_config.instance_id,\n+            )\n+\n     def schedule(self) -> SchedulerOutput:\n         # NOTE(woosuk) on the scheduling algorithm:\n         # There's no \"decoding phase\" nor \"prefill phase\" in the scheduler.\n@@ -1162,7 +1187,30 @@ def update_from_output(\n                 request.status = RequestStatus.FINISHED_STOPPED\n                 stopped = True\n \n+            routed_experts = None\n             if stopped:\n+                if self.vllm_config.model_config.enable_return_routed_experts:\n+                    kv_blocks = self.kv_cache_manager.get_blocks(request.request_id)\n+                    block_ids = kv_blocks.get_block_ids()[0]\n+                    num_tokens = request.num_tokens - 1\n+\n+                    # compute slot mapping\n+                    block_ids_array = np.array(block_ids, dtype=np.int32)\n+                    num_blocks = len(block_ids)\n+                    block_size = self.block_size\n+\n+                    # generate block offsets\n+                    block_offsets = np.arange(0, block_size)\n+\n+                    # compute slot mapping: slot = block_id * block_size + offset\n+                    slot_mapping = (\n+                        block_offsets.reshape((1, block_size))\n+                        + block_ids_array.reshape((num_blocks, 1)) * block_size\n+                    ).flatten()[:num_tokens]\n+\n+                    routed_experts = self.routed_experts_reader.get_routed_experts(\n+                        indices=slot_mapping\n+                    )\n                 kv_transfer_params = self._free_request(request)\n                 if status_before_stop == RequestStatus.RUNNING:\n                     stopped_running_reqs.add(request)\n@@ -1209,6 +1257,7 @@ def update_from_output(\n                         kv_transfer_params=kv_transfer_params,\n                         trace_headers=request.trace_headers,\n                         num_cached_tokens=request.num_cached_tokens,\n+                        routed_experts=routed_experts,\n                         num_nans_in_logits=request.num_nans_in_logits,\n                     )\n                 )", "patch_sha256": "818f0328ac44b0db"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28284/review_2674553876", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28284", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/core/sched/scheduler.py\nReview Comment:\n### Potential Off-by-One Error in Token Count for Routed Experts Retrieval\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nWhen retrieving routed experts for a completed request, the code subtracts 1 from the token count:\n\n```python\nnum_tokens = request.num_tokens - 1\n```\n\nThis means the last token's routing information is excluded from the returned `routed_experts` array. If `request.num_tokens` represents all tokens that have been processed through the MoE layers, this subtraction causes data loss. The slot_mapping is then calculated for only `num_tokens` tokens:\n\n```python\nslot_mapping = (...).flatten()[:num_tokens]  # Missing last token\n```\n\nIf the intent is to exclude an unprocessed token, this should be documented. Otherwise, this appears to be an off-by-one error that causes the routing decision for the final generated token to be missing from the output.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/core/sched/scheduler.py#L1179-L1180\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..fj7F_7CqcvISsJxkQ1-uulUrRptrgQzTe71x2AfqdoZlOes_wXZl2pQHLsCTYhoPU9VaIYnHHdXbmZnBqTkWpDMp8uqQvM-OK5o9NnhDFxBC2GjZ54PYMo6cGwYaWXFcuJD5x-yY6wHAulo5sPzJMlkFCQ6k6nT950R1E8iYSLue57ynIXe8g_eddAKfGgYMQVfiUhPUFHcatg3aMqHNN41Poj86zmZXZ7BknA1qEUATxRT00fCwtxUrJx0vBRDVQ0UtaQ-0QM-gQZrfsevHdFs-KV_Xgt_rbafwCumHUKSk76ogmJYLKbiq9eL3KX-sH4JmmN-kRECsMRTHLd_lxg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..0xVCFbsWWcwdzA-Arm_dbCaV8sbRMN4wpYy_31ba8OpKHnuV8MppGxRMolkdLnbdRKjfCkuIS6YtSTFx8gbij1CE9gsCIEMT0-2xpZ-iccpgLnaPVQQ_M5m65c6twhgolVnn7ns41XaIxFO-0Z1x9xzN4mSJ6-Fc0LxNcp46skDF9iKginoSSn_3gvt5L3Bre4pPAlB7CDsP3OpOplxT0mXj5xifIrX-V-woVmamCEdRjsGEp5ktkfvFZ82kbhBeg9r825uuYb0Mme1x7wlPOiIanUq9YRgBjCNvFc7ma9H06_RdtJoWx0WbXY61ZEsVXCggqj84l2mMkzbVyMDA2Q\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -1152,7 +1172,30 @@ def update_from_output(\n                 request.status = RequestStatus.FINISHED_STOPPED\n                 stopped = True\n \n+            routed_experts = None\n             if stopped:\n+                if self.vllm_config.model_config.enable_return_routed_experts:\n+                    kv_blocks = self.kv_cache_manager.get_blocks(request.request_id)\n+                    block_ids = kv_blocks.get_block_ids()[0]\n+                    num_tokens = request.num_tokens - 1\n", "materials": ["### Potential Off-by-One Error in Token Count for Routed Experts Retrieval\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nWhen retrieving routed experts for a completed request, the code subtracts 1 from the token count:\n\n```python\nnum_tokens = request.num_tokens - 1\n```\n\nThis means the last token's routing information is excluded from the returned `routed_experts` array. If `request.num_tokens` represents all tokens that have been processed through the MoE layers, this subtraction causes data loss. The slot_mapping is then calculated for only `num_tokens` tokens:\n\n```python\nslot_mapping = (...).flatten()[:num_tokens]  # Missing last token\n```\n\nIf the intent is to exclude an unprocessed token, this should be documented. Otherwise, this appears to be an off-by-one error that causes the routing decision for the final generated token to be missing from the output.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/core/sched/scheduler.py#L1179-L1180\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..fj7F_7CqcvISsJxkQ1-uulUrRptrgQzTe71x2AfqdoZlOes_wXZl2pQHLsCTYhoPU9VaIYnHHdXbmZnBqTkWpDMp8uqQvM-OK5o9NnhDFxBC2GjZ54PYMo6cGwYaWXFcuJD5x-yY6wHAulo5sPzJMlkFCQ6k6nT950R1E8iYSLue57ynIXe8g_eddAKfGgYMQVfiUhPUFHcatg3aMqHNN41Poj86zmZXZ7BknA1qEUATxRT00fCwtxUrJx0vBRDVQ0UtaQ-0QM-gQZrfsevHdFs-KV_Xgt_rbafwCumHUKSk76ogmJYLKbiq9eL3KX-sH4JmmN-kRECsMRTHLd_lxg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..0xVCFbsWWcwdzA-Arm_dbCaV8sbRMN4wpYy_31ba8OpKHnuV8MppGxRMolkdLnbdRKjfCkuIS6YtSTFx8gbij1CE9gsCIEMT0-2xpZ-iccpgLnaPVQQ_M5m65c6twhgolVnn7ns41XaIxFO-0Z1x9xzN4mSJ6-Fc0LxNcp46skDF9iKginoSSn_3gvt5L3Bre4pPAlB7CDsP3OpOplxT0mXj5xifIrX-V-woVmamCEdRjsGEp5ktkfvFZ82kbhBeg9r825uuYb0Mme1x7wlPOiIanUq9YRgBjCNvFc7ma9H06_RdtJoWx0WbXY61ZEsVXCggqj84l2mMkzbVyMDA2Q\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -6,6 +6,8 @@\n from collections.abc import Iterable\n from typing import Any\n \n+import numpy as np\n+\n from vllm import envs\n from vllm.compilation.cuda_graph import CUDAGraphStat\n from vllm.config import VllmConfig\n@@ -24,6 +26,9 @@\n from vllm.distributed.kv_transfer.kv_connector.v1.base import KVConnectorMetadata\n from vllm.distributed.kv_transfer.kv_connector.v1.metrics import KVConnectorStats\n from vllm.logger import init_logger\n+from vllm.model_executor.layers.fused_moe.routed_experts_capturer import (\n+    RoutedExpertsReader,\n+)\n from vllm.multimodal import MULTIMODAL_REGISTRY, MultiModalRegistry\n from vllm.v1.core.encoder_cache_manager import (\n     EncoderCacheManager,\n@@ -219,11 +224,31 @@ def __init__(\n         )\n         self.use_pp = self.parallel_config.pipeline_parallel_size > 1\n         self.use_v2_model_runner = envs.VLLM_USE_V2_MODEL_RUNNER\n-\n         self.perf_metrics: ModelMetrics | None = None\n         if self.log_stats and vllm_config.observability_config.enable_mfu_metrics:\n             self.perf_metrics = ModelMetrics(vllm_config)\n \n+        if self.vllm_config.model_config.enable_return_routed_experts:\n+            assert self.dcp_world_size == 1 and self.pcp_world_size == 1, (\n+                \"enable_return_routed_experts does not support context parallelism \"\n+                \"(dcp_world_size > 1 or pcp_world_size > 1)\"\n+            )\n+\n+            self.routed_experts_reader = RoutedExpertsReader.create()\n+\n+            assert len(kv_cache_config.kv_cache_groups) > 0, (\n+                \"enable_return_routed_experts requires at least one kv cache group\"\n+            )\n+            self.max_num_kv_tokens = (\n+                kv_cache_config.num_blocks // len(kv_cache_config.kv_cache_groups) + 1\n+            ) * self.block_size\n+\n+            self.routed_experts_reader.attach_buffer(\n+                max_num_kv_tokens=self.max_num_kv_tokens,\n+                model_config=self.vllm_config.model_config,\n+                instance_id=self.vllm_config.instance_id,\n+            )\n+\n     def schedule(self) -> SchedulerOutput:\n         # NOTE(woosuk) on the scheduling algorithm:\n         # There's no \"decoding phase\" nor \"prefill phase\" in the scheduler.\n@@ -1162,7 +1187,30 @@ def update_from_output(\n                 request.status = RequestStatus.FINISHED_STOPPED\n                 stopped = True\n \n+            routed_experts = None\n             if stopped:\n+                if self.vllm_config.model_config.enable_return_routed_experts:\n+                    kv_blocks = self.kv_cache_manager.get_blocks(request.request_id)\n+                    block_ids = kv_blocks.get_block_ids()[0]\n+                    num_tokens = request.num_tokens - 1\n+\n+                    # compute slot mapping\n+                    block_ids_array = np.array(block_ids, dtype=np.int32)\n+                    num_blocks = len(block_ids)\n+                    block_size = self.block_size\n+\n+                    # generate block offsets\n+                    block_offsets = np.arange(0, block_size)\n+\n+                    # compute slot mapping: slot = block_id * block_size + offset\n+                    slot_mapping = (\n+                        block_offsets.reshape((1, block_size))\n+                        + block_ids_array.reshape((num_blocks, 1)) * block_size\n+                    ).flatten()[:num_tokens]\n+\n+                    routed_experts = self.routed_experts_reader.get_routed_experts(\n+                        indices=slot_mapping\n+                    )\n                 kv_transfer_params = self._free_request(request)\n                 if status_before_stop == RequestStatus.RUNNING:\n                     stopped_running_reqs.add(request)\n@@ -1209,6 +1257,7 @@ def update_from_output(\n                         kv_transfer_params=kv_transfer_params,\n                         trace_headers=request.trace_headers,\n                         num_cached_tokens=request.num_cached_tokens,\n+                        routed_experts=routed_experts,\n                         num_nans_in_logits=request.num_nans_in_logits,\n                     )\n                 )", "patch_sha256": "818f0328ac44b0db"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28284/review_2674603956", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28284", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/core/sched/scheduler.py\nReview Comment:\n### Slot mapping calculation mismatch with context parallelism\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe scheduler computes slot_mapping using a simple formula: `slot = block_id * block_size + offset`. However, when context parallelism is used (DCP > 1 or PCP > 1), the GPU model runner's BlockTable uses a more complex interleaved slot mapping calculation involving virtual blocks and masking (see block_table.py lines 143-178). The scheduler uses `scheduler_block_size` which includes the CP factors, but the formula itself doesn't account for the interleaved storage pattern. This mismatch means the scheduler will read from incorrect positions in shared memory, returning corrupted expert indices when context parallelism is enabled.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/core/sched/scheduler.py#L1187-L1194\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..TuThk3w56zmemI4esYYq3Iqf61t7tn6DCxzyYaHFqm16dG876bX0bR0aKQXTQmxlrB4OKbWPTMlvTWA-YkLmxZLo3H6kESSV-8jxn6XRy2FQP3yObnwOvW7e8qdBMNKkjL7CU7s_aYqMSUV5VfxHniaXRreGVy_VwSY9sJ7nhhJz3fWwEDENAhzDqCR6965aezYFMuAr9DFR9LHf-vV7nDy_XRZaaK7NS9lZV9eJv6g1AAc6knnXtArmCB2DSPxnNAAvmD0MriAZF55w6QV96qsIwhTfmK_t27VvTKprewRaA7FObs_v9EcaELmvd5TvqY5Cto3ZOHVBAxrJ2DZRlA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..arHd_lheCARYD_CcR0rpB1s7lkXjKdpC0gAys_H0z-w1jIakgyY3K4GZOed__kUyRkblZgKSqwrThSc36cyXuW7U4eKBNLNEDJ2BtZlwiPQ4zWZFEISZ2xRc3ZCyVFm7WMRngTvA7xCKUmHWRshqKWklW8aokvMVRoXtE4JdmqhOy0iA72Vxmk74gLyBR-Mr9WIfJ0ITNwq9xhleuuhO1UPkZVZWywlrt14xT6Xc37aKNkg59LsciW7RLJq9SScNLdv2jMyDFomluBHq6h6-B_OOpISttPRA5AosLxe_pfy-doRcqWrIVZSo34Ay_AHFu_BKv1RtqEshUFEXGZwtlg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -1152,7 +1172,30 @@ def update_from_output(\n                 request.status = RequestStatus.FINISHED_STOPPED\n                 stopped = True\n \n+            routed_experts = None\n             if stopped:\n+                if self.vllm_config.model_config.enable_return_routed_experts:\n+                    kv_blocks = self.kv_cache_manager.get_blocks(request.request_id)\n+                    block_ids = kv_blocks.get_block_ids()[0]\n+                    num_tokens = request.num_tokens - 1\n+\n+                    # compute slot mapping\n+                    block_ids_array = np.array(block_ids, dtype=np.int32)\n+                    num_blocks = len(block_ids)\n+                    block_size = self.block_size\n+\n+                    # generate block offsets\n+                    block_offsets = np.arange(0, block_size)\n+\n+                    # compute slot mapping: slot = block_id * block_size + offset\n+                    slot_mapping = (\n+                        block_offsets.reshape((1, block_size))\n+                        + block_ids_array.reshape((num_blocks, 1)) * block_size\n+                    ).flatten()[:num_tokens]\n", "materials": ["### Slot mapping calculation mismatch with context parallelism\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe scheduler computes slot_mapping using a simple formula: `slot = block_id * block_size + offset`. However, when context parallelism is used (DCP > 1 or PCP > 1), the GPU model runner's BlockTable uses a more complex interleaved slot mapping calculation involving virtual blocks and masking (see block_table.py lines 143-178). The scheduler uses `scheduler_block_size` which includes the CP factors, but the formula itself doesn't account for the interleaved storage pattern. This mismatch means the scheduler will read from incorrect positions in shared memory, returning corrupted expert indices when context parallelism is enabled.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/core/sched/scheduler.py#L1187-L1194\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..TuThk3w56zmemI4esYYq3Iqf61t7tn6DCxzyYaHFqm16dG876bX0bR0aKQXTQmxlrB4OKbWPTMlvTWA-YkLmxZLo3H6kESSV-8jxn6XRy2FQP3yObnwOvW7e8qdBMNKkjL7CU7s_aYqMSUV5VfxHniaXRreGVy_VwSY9sJ7nhhJz3fWwEDENAhzDqCR6965aezYFMuAr9DFR9LHf-vV7nDy_XRZaaK7NS9lZV9eJv6g1AAc6knnXtArmCB2DSPxnNAAvmD0MriAZF55w6QV96qsIwhTfmK_t27VvTKprewRaA7FObs_v9EcaELmvd5TvqY5Cto3ZOHVBAxrJ2DZRlA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..arHd_lheCARYD_CcR0rpB1s7lkXjKdpC0gAys_H0z-w1jIakgyY3K4GZOed__kUyRkblZgKSqwrThSc36cyXuW7U4eKBNLNEDJ2BtZlwiPQ4zWZFEISZ2xRc3ZCyVFm7WMRngTvA7xCKUmHWRshqKWklW8aokvMVRoXtE4JdmqhOy0iA72Vxmk74gLyBR-Mr9WIfJ0ITNwq9xhleuuhO1UPkZVZWywlrt14xT6Xc37aKNkg59LsciW7RLJq9SScNLdv2jMyDFomluBHq6h6-B_OOpISttPRA5AosLxe_pfy-doRcqWrIVZSo34Ay_AHFu_BKv1RtqEshUFEXGZwtlg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -6,6 +6,8 @@\n from collections.abc import Iterable\n from typing import Any\n \n+import numpy as np\n+\n from vllm import envs\n from vllm.compilation.cuda_graph import CUDAGraphStat\n from vllm.config import VllmConfig\n@@ -24,6 +26,9 @@\n from vllm.distributed.kv_transfer.kv_connector.v1.base import KVConnectorMetadata\n from vllm.distributed.kv_transfer.kv_connector.v1.metrics import KVConnectorStats\n from vllm.logger import init_logger\n+from vllm.model_executor.layers.fused_moe.routed_experts_capturer import (\n+    RoutedExpertsReader,\n+)\n from vllm.multimodal import MULTIMODAL_REGISTRY, MultiModalRegistry\n from vllm.v1.core.encoder_cache_manager import (\n     EncoderCacheManager,\n@@ -219,11 +224,31 @@ def __init__(\n         )\n         self.use_pp = self.parallel_config.pipeline_parallel_size > 1\n         self.use_v2_model_runner = envs.VLLM_USE_V2_MODEL_RUNNER\n-\n         self.perf_metrics: ModelMetrics | None = None\n         if self.log_stats and vllm_config.observability_config.enable_mfu_metrics:\n             self.perf_metrics = ModelMetrics(vllm_config)\n \n+        if self.vllm_config.model_config.enable_return_routed_experts:\n+            assert self.dcp_world_size == 1 and self.pcp_world_size == 1, (\n+                \"enable_return_routed_experts does not support context parallelism \"\n+                \"(dcp_world_size > 1 or pcp_world_size > 1)\"\n+            )\n+\n+            self.routed_experts_reader = RoutedExpertsReader.create()\n+\n+            assert len(kv_cache_config.kv_cache_groups) > 0, (\n+                \"enable_return_routed_experts requires at least one kv cache group\"\n+            )\n+            self.max_num_kv_tokens = (\n+                kv_cache_config.num_blocks // len(kv_cache_config.kv_cache_groups) + 1\n+            ) * self.block_size\n+\n+            self.routed_experts_reader.attach_buffer(\n+                max_num_kv_tokens=self.max_num_kv_tokens,\n+                model_config=self.vllm_config.model_config,\n+                instance_id=self.vllm_config.instance_id,\n+            )\n+\n     def schedule(self) -> SchedulerOutput:\n         # NOTE(woosuk) on the scheduling algorithm:\n         # There's no \"decoding phase\" nor \"prefill phase\" in the scheduler.\n@@ -1162,7 +1187,30 @@ def update_from_output(\n                 request.status = RequestStatus.FINISHED_STOPPED\n                 stopped = True\n \n+            routed_experts = None\n             if stopped:\n+                if self.vllm_config.model_config.enable_return_routed_experts:\n+                    kv_blocks = self.kv_cache_manager.get_blocks(request.request_id)\n+                    block_ids = kv_blocks.get_block_ids()[0]\n+                    num_tokens = request.num_tokens - 1\n+\n+                    # compute slot mapping\n+                    block_ids_array = np.array(block_ids, dtype=np.int32)\n+                    num_blocks = len(block_ids)\n+                    block_size = self.block_size\n+\n+                    # generate block offsets\n+                    block_offsets = np.arange(0, block_size)\n+\n+                    # compute slot mapping: slot = block_id * block_size + offset\n+                    slot_mapping = (\n+                        block_offsets.reshape((1, block_size))\n+                        + block_ids_array.reshape((num_blocks, 1)) * block_size\n+                    ).flatten()[:num_tokens]\n+\n+                    routed_experts = self.routed_experts_reader.get_routed_experts(\n+                        indices=slot_mapping\n+                    )\n                 kv_transfer_params = self._free_request(request)\n                 if status_before_stop == RequestStatus.RUNNING:\n                     stopped_running_reqs.add(request)\n@@ -1209,6 +1257,7 @@ def update_from_output(\n                         kv_transfer_params=kv_transfer_params,\n                         trace_headers=request.trace_headers,\n                         num_cached_tokens=request.num_cached_tokens,\n+                        routed_experts=routed_experts,\n                         num_nans_in_logits=request.num_nans_in_logits,\n                     )\n                 )", "patch_sha256": "818f0328ac44b0db"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28284/review_2674603961", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28284", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/core/sched/scheduler.py\nReview Comment:\n### Shared memory name collision in multi-node or independent server deployments\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe shared memory and lock file names are derived from `self.instance_id = self.vllm_config.instance_id`. In certain deployment scenarios (e.g., multiple independent `tp=1` servers on the same node, or multi-node setups), this instance_id may not be globally unique, leading to shared memory name collisions. When collisions occur, different vLLM instances would read/write to the same shared memory buffer, causing data corruption. The PR discussion mentions this concern with a proposed fix using different naming strategies based on world_size, but the current implementation doesn't implement this safeguard.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/core/sched/scheduler.py#L238-L245\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..UAStv0QBFxEo09fZMsC-dSb_cikDhvzUY6e7E2Sun1K2mE9oGWYMGf7irw3RzAWhAteYBB6gXsyE9OZCW3z82geSqXkSoHika0REWnWkBqY7S1X7GAGB3huJz4sDk4rfTkesWdoT6SlGZPxfbYupptdAZcYI6C-arCJCPBvLUOSeuDKtxmr0w3jEd7khK_3ciXW6VbiizyCEg88OEqVldfLfuJ6h--5tVPX2fGbMEW772hqCWlMzESKovwMIDv_f1X3IrKw7XUqv2Gy7jVvNtpJ9SvyIWHh-og1NJqrkgKMk6BYTg-BCepY-cnL8KzvS-dfpg3ybp8S97l6HpkYncA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..c84JMg4akfB0kSAd--Zr1hxg_H_pJP7FfcjMdjCa1SbAFGLSDSJkFXWiAdLbVDdKUkCNfTJQ7uevvZuulomWYiEI7XTNxQuc4siK7cnSBM-AhcOC9H_nGJI9mOevxlFRF5hARs4y-CpaV-amuqXcq2qai-kmwAFcQEjdoKQZ0-2RlRdALaDWzBtqr67eDK_t6oV_K-lEpSt0_hwBaO62DYY4ZOA9daLe2rOx3ICAcL7AXrJgtBifAcXoNrtmZKw_0UhQIewgdaWfIkjso3Fdtlo5fSAuX2NXmKQ1XFWvrkdIQid3S0KZ4XgGbigotl47auL5XaTBwBz6RvXDDfmJ5g\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -219,11 +224,26 @@ def __init__(\n         )\n         self.use_pp = self.parallel_config.pipeline_parallel_size > 1\n         self.use_v2_model_runner = envs.VLLM_USE_V2_MODEL_RUNNER\n-\n         self.perf_metrics: ModelMetrics | None = None\n         if self.log_stats and vllm_config.observability_config.enable_mfu_metrics:\n             self.perf_metrics = ModelMetrics(vllm_config)\n \n+        self.max_num_kv_tokens = (\n+            kv_cache_config.num_blocks // len(kv_cache_config.kv_cache_groups) + 1\n+        ) * self.block_size\n+\n+        self.routed_experts_reader = RoutedExpertsReader.create(\n+            enable=self.vllm_config.model_config.enable_return_routed_experts\n+        )\n+\n+        self.instance_id = self.vllm_config.instance_id\n+\n+        self.routed_experts_reader.attach_buffer(\n+            max_num_kv_tokens=self.max_num_kv_tokens,\n+            model_config=self.vllm_config.model_config,\n+            instance_id=self.instance_id,\n+        )\n", "materials": ["### Shared memory name collision in multi-node or independent server deployments\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe shared memory and lock file names are derived from `self.instance_id = self.vllm_config.instance_id`. In certain deployment scenarios (e.g., multiple independent `tp=1` servers on the same node, or multi-node setups), this instance_id may not be globally unique, leading to shared memory name collisions. When collisions occur, different vLLM instances would read/write to the same shared memory buffer, causing data corruption. The PR discussion mentions this concern with a proposed fix using different naming strategies based on world_size, but the current implementation doesn't implement this safeguard.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/core/sched/scheduler.py#L238-L245\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..UAStv0QBFxEo09fZMsC-dSb_cikDhvzUY6e7E2Sun1K2mE9oGWYMGf7irw3RzAWhAteYBB6gXsyE9OZCW3z82geSqXkSoHika0REWnWkBqY7S1X7GAGB3huJz4sDk4rfTkesWdoT6SlGZPxfbYupptdAZcYI6C-arCJCPBvLUOSeuDKtxmr0w3jEd7khK_3ciXW6VbiizyCEg88OEqVldfLfuJ6h--5tVPX2fGbMEW772hqCWlMzESKovwMIDv_f1X3IrKw7XUqv2Gy7jVvNtpJ9SvyIWHh-og1NJqrkgKMk6BYTg-BCepY-cnL8KzvS-dfpg3ybp8S97l6HpkYncA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..c84JMg4akfB0kSAd--Zr1hxg_H_pJP7FfcjMdjCa1SbAFGLSDSJkFXWiAdLbVDdKUkCNfTJQ7uevvZuulomWYiEI7XTNxQuc4siK7cnSBM-AhcOC9H_nGJI9mOevxlFRF5hARs4y-CpaV-amuqXcq2qai-kmwAFcQEjdoKQZ0-2RlRdALaDWzBtqr67eDK_t6oV_K-lEpSt0_hwBaO62DYY4ZOA9daLe2rOx3ICAcL7AXrJgtBifAcXoNrtmZKw_0UhQIewgdaWfIkjso3Fdtlo5fSAuX2NXmKQ1XFWvrkdIQid3S0KZ4XgGbigotl47auL5XaTBwBz6RvXDDfmJ5g\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -6,6 +6,8 @@\n from collections.abc import Iterable\n from typing import Any\n \n+import numpy as np\n+\n from vllm import envs\n from vllm.compilation.cuda_graph import CUDAGraphStat\n from vllm.config import VllmConfig\n@@ -24,6 +26,9 @@\n from vllm.distributed.kv_transfer.kv_connector.v1.base import KVConnectorMetadata\n from vllm.distributed.kv_transfer.kv_connector.v1.metrics import KVConnectorStats\n from vllm.logger import init_logger\n+from vllm.model_executor.layers.fused_moe.routed_experts_capturer import (\n+    RoutedExpertsReader,\n+)\n from vllm.multimodal import MULTIMODAL_REGISTRY, MultiModalRegistry\n from vllm.v1.core.encoder_cache_manager import (\n     EncoderCacheManager,\n@@ -219,11 +224,31 @@ def __init__(\n         )\n         self.use_pp = self.parallel_config.pipeline_parallel_size > 1\n         self.use_v2_model_runner = envs.VLLM_USE_V2_MODEL_RUNNER\n-\n         self.perf_metrics: ModelMetrics | None = None\n         if self.log_stats and vllm_config.observability_config.enable_mfu_metrics:\n             self.perf_metrics = ModelMetrics(vllm_config)\n \n+        if self.vllm_config.model_config.enable_return_routed_experts:\n+            assert self.dcp_world_size == 1 and self.pcp_world_size == 1, (\n+                \"enable_return_routed_experts does not support context parallelism \"\n+                \"(dcp_world_size > 1 or pcp_world_size > 1)\"\n+            )\n+\n+            self.routed_experts_reader = RoutedExpertsReader.create()\n+\n+            assert len(kv_cache_config.kv_cache_groups) > 0, (\n+                \"enable_return_routed_experts requires at least one kv cache group\"\n+            )\n+            self.max_num_kv_tokens = (\n+                kv_cache_config.num_blocks // len(kv_cache_config.kv_cache_groups) + 1\n+            ) * self.block_size\n+\n+            self.routed_experts_reader.attach_buffer(\n+                max_num_kv_tokens=self.max_num_kv_tokens,\n+                model_config=self.vllm_config.model_config,\n+                instance_id=self.vllm_config.instance_id,\n+            )\n+\n     def schedule(self) -> SchedulerOutput:\n         # NOTE(woosuk) on the scheduling algorithm:\n         # There's no \"decoding phase\" nor \"prefill phase\" in the scheduler.\n@@ -1162,7 +1187,30 @@ def update_from_output(\n                 request.status = RequestStatus.FINISHED_STOPPED\n                 stopped = True\n \n+            routed_experts = None\n             if stopped:\n+                if self.vllm_config.model_config.enable_return_routed_experts:\n+                    kv_blocks = self.kv_cache_manager.get_blocks(request.request_id)\n+                    block_ids = kv_blocks.get_block_ids()[0]\n+                    num_tokens = request.num_tokens - 1\n+\n+                    # compute slot mapping\n+                    block_ids_array = np.array(block_ids, dtype=np.int32)\n+                    num_blocks = len(block_ids)\n+                    block_size = self.block_size\n+\n+                    # generate block offsets\n+                    block_offsets = np.arange(0, block_size)\n+\n+                    # compute slot mapping: slot = block_id * block_size + offset\n+                    slot_mapping = (\n+                        block_offsets.reshape((1, block_size))\n+                        + block_ids_array.reshape((num_blocks, 1)) * block_size\n+                    ).flatten()[:num_tokens]\n+\n+                    routed_experts = self.routed_experts_reader.get_routed_experts(\n+                        indices=slot_mapping\n+                    )\n                 kv_transfer_params = self._free_request(request)\n                 if status_before_stop == RequestStatus.RUNNING:\n                     stopped_running_reqs.add(request)\n@@ -1209,6 +1257,7 @@ def update_from_output(\n                         kv_transfer_params=kv_transfer_params,\n                         trace_headers=request.trace_headers,\n                         num_cached_tokens=request.num_cached_tokens,\n+                        routed_experts=routed_experts,\n                         num_nans_in_logits=request.num_nans_in_logits,\n                     )\n                 )", "patch_sha256": "818f0328ac44b0db"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28284/review_2681910247", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28284", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/vllm.py\nReview Comment:\n### Missing async scheduling disable for routed experts feature\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe PR notes explicitly state that async scheduling should be disabled when `enable_return_routed_experts=True`, but this isn't implemented. The async scheduling logic in `VllmConfig.__post_init__` handles various incompatibility cases (PP > 1, speculative decoding, executor backend) but doesn't include any handling for `enable_return_routed_experts`. Users in the PR discussion are reporting significant latency issues (10X slower), which could be related to async scheduling interference with the capture/save operations for routed experts.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/config/vllm.py#L593-L634\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..0uG0c1pAXovEZtSAgPViTr1gHunfEptmB5nT_NU7cOevHlDJ5RQonmruPkJtbUXjkHF_d5XBTDIxL6djCJq0g-Uk4aT09I2CitDicHWXNbQeoIWgueKfkDnj1JHlZJl3a6TjBq-qmCkuLbnu-p9IedxgrG4QW-LOJ0juzl_fD5CaSJRcnIrHgByLXlTkuIsOGeT5RIoMaLAiZyRsCm5lILwepIvUuDUwmUsZRCWclrrrrUTLJEpUpXZMTBQI2eete47mQsFb7POwl6OMmregMUZ_e3rD3nq0O3lzIFHA0VljdhVYVA3DlfIgL1fQso3DZSxYpFGiFXtIHVGniffM8w\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..S5AXNe3IJhxBdeLs32vfV5nMCdtMCHFoFfnbGFN_SlUBGNwMrYtFdUD0jOjsklHEhby2u8i3jmYnApyILU0e0z_SLIghcOjuKx4ZZmHXn9yiiFaTLyLpJcu2N0n6fJpFMKnMKWIr9r1RpLH95oK7TXqM69rB4TtH9oSomV-woCiVIFkG3VNLZ6j7bKq4On4gZIFUzTmWWe-dNvuzbtnT-42lFjgFD38kxkYKSncvYvLVzD1K9Hscxsj7mak2kMACjb76Fefuxk_pZD0vBjx5e5HfppINDnS1tp1BZDMxAJkHRGPzJ4IH6pL2MceRBLdWfTtBJj6JYrf_RxyYhxLcfQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -1352,6 +1352,7 @@ def __str__(self):\n             f\"disable_custom_all_reduce={self.parallel_config.disable_custom_all_reduce}, \"  # noqa\n             f\"quantization={self.model_config.quantization}, \"\n             f\"enforce_eager={self.model_config.enforce_eager}, \"\n+            f\"enable_return_routed_experts={self.model_config.enable_return_routed_experts}, \"  # noqa\n", "materials": ["### Missing async scheduling disable for routed experts feature\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe PR notes explicitly state that async scheduling should be disabled when `enable_return_routed_experts=True`, but this isn't implemented. The async scheduling logic in `VllmConfig.__post_init__` handles various incompatibility cases (PP > 1, speculative decoding, executor backend) but doesn't include any handling for `enable_return_routed_experts`. Users in the PR discussion are reporting significant latency issues (10X slower), which could be related to async scheduling interference with the capture/save operations for routed experts.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/config/vllm.py#L593-L634\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..0uG0c1pAXovEZtSAgPViTr1gHunfEptmB5nT_NU7cOevHlDJ5RQonmruPkJtbUXjkHF_d5XBTDIxL6djCJq0g-Uk4aT09I2CitDicHWXNbQeoIWgueKfkDnj1JHlZJl3a6TjBq-qmCkuLbnu-p9IedxgrG4QW-LOJ0juzl_fD5CaSJRcnIrHgByLXlTkuIsOGeT5RIoMaLAiZyRsCm5lILwepIvUuDUwmUsZRCWclrrrrUTLJEpUpXZMTBQI2eete47mQsFb7POwl6OMmregMUZ_e3rD3nq0O3lzIFHA0VljdhVYVA3DlfIgL1fQso3DZSxYpFGiFXtIHVGniffM8w\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..S5AXNe3IJhxBdeLs32vfV5nMCdtMCHFoFfnbGFN_SlUBGNwMrYtFdUD0jOjsklHEhby2u8i3jmYnApyILU0e0z_SLIghcOjuKx4ZZmHXn9yiiFaTLyLpJcu2N0n6fJpFMKnMKWIr9r1RpLH95oK7TXqM69rB4TtH9oSomV-woCiVIFkG3VNLZ6j7bKq4On4gZIFUzTmWWe-dNvuzbtnT-42lFjgFD38kxkYKSncvYvLVzD1K9Hscxsj7mak2kMACjb76Fefuxk_pZD0vBjx5e5HfppINDnS1tp1BZDMxAJkHRGPzJ4IH6pL2MceRBLdWfTtBJj6JYrf_RxyYhxLcfQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -1352,6 +1352,7 @@ def __str__(self):\n             f\"disable_custom_all_reduce={self.parallel_config.disable_custom_all_reduce}, \"  # noqa\n             f\"quantization={self.model_config.quantization}, \"\n             f\"enforce_eager={self.model_config.enforce_eager}, \"\n+            f\"enable_return_routed_experts={self.model_config.enable_return_routed_experts}, \"  # noqa\n             f\"kv_cache_dtype={self.cache_config.cache_dtype}, \"\n             f\"device_config={self.device_config.device}, \"\n             f\"structured_outputs_config={self.structured_outputs_config!r}, \"", "patch_sha256": "3ef48f357aee2860"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28506/review_2625207634", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28506", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/executor/multiproc_executor.py\nReview Comment:\nwill this code path be called if pp = 1, tp =xx , or dp = xx?\r\nif yes, the self.world_size = pp size may not be right here.\n\nOriginal Code Context:\n@@ -104,16 +105,24 @@ def _init_executor(self) -> None:\n         self.shutdown_event = threading.Event()\n         self.failure_callback: FailureCallback | None = None\n \n-        self.world_size = self.parallel_config.world_size\n-        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n-            f\"global world_size ({self.parallel_config.world_size}) must be \"\n-            f\"divisible by nnodes_within_dp \"\n-            f\"({self.parallel_config.nnodes_within_dp}). \"\n-        )\n-        self.local_world_size = self.parallel_config.local_world_size\n-        tp_size = self.parallel_config.tensor_parallel_size\n-        pp_size = self.parallel_config.pipeline_parallel_size\n-        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        if not USE_TPU_INFERENCE:\n+            self.world_size = self.parallel_config.world_size\n+            assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n+                f\"global world_size ({self.parallel_config.world_size}) must be \"\n+                f\"divisible by nnodes_within_dp \"\n+                f\"({self.parallel_config.nnodes_within_dp}). \"\n+            )\n+            self.local_world_size = self.parallel_config.local_world_size\n+            tp_size = self.parallel_config.tensor_parallel_size\n+            pp_size = self.parallel_config.pipeline_parallel_size\n+            pcp_size = self.parallel_config.prefill_context_parallel_size\n+        else:\n+            # Jax handles TP, PCP with SPMD, world_size = pp_size.\n+            self.world_size = self.parallel_config.pipeline_parallel_size\n", "materials": ["will this code path be called if pp = 1, tp =xx , or dp = xx?\r\nif yes, the self.world_size = pp size may not be right here."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -104,16 +104,7 @@ def _init_executor(self) -> None:\n         self.shutdown_event = threading.Event()\n         self.failure_callback: FailureCallback | None = None\n \n-        self.world_size = self.parallel_config.world_size\n-        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n-            f\"global world_size ({self.parallel_config.world_size}) must be \"\n-            f\"divisible by nnodes_within_dp \"\n-            f\"({self.parallel_config.nnodes_within_dp}). \"\n-        )\n-        self.local_world_size = self.parallel_config.local_world_size\n-        tp_size = self.parallel_config.tensor_parallel_size\n-        pp_size = self.parallel_config.pipeline_parallel_size\n-        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        tp_size, pp_size, pcp_size = self._get_parallel_sizes()\n         assert self.world_size == tp_size * pp_size * pcp_size, (\n             f\"world_size ({self.world_size}) must be equal to the \"\n             f\"tensor_parallel_size ({tp_size}) x pipeline\"\n@@ -154,6 +145,7 @@ def _init_executor(self) -> None:\n             )\n             for local_rank in range(self.local_world_size):\n                 global_rank = global_start_rank + local_rank\n+                is_driver_worker = self._is_driver_worker(global_rank)\n                 unready_workers.append(\n                     WorkerProc.make_worker_process(\n                         vllm_config=self.vllm_config,\n@@ -162,6 +154,7 @@ def _init_executor(self) -> None:\n                         distributed_init_method=distributed_init_method,\n                         input_shm_handle=scheduler_output_handle,\n                         shared_worker_lock=shared_worker_lock,\n+                        is_driver_worker=is_driver_worker,\n                     )\n                 )\n \n@@ -199,6 +192,11 @@ def _init_executor(self) -> None:\n             # Wait for all remote response mqs to be ready.\n             for response_mq in self.response_mqs:\n                 response_mq.wait_until_ready()\n+\n+            self.futures_queue = deque[tuple[FutureWrapper, Callable]]()\n+\n+            self._post_init_executor()\n+\n             success = True\n         finally:\n             if not success:\n@@ -209,10 +207,27 @@ def _init_executor(self) -> None:\n                         uw.death_writer.close()\n                 self._ensure_worker_termination([uw.proc for uw in unready_workers])\n \n-        self.futures_queue = deque[tuple[FutureWrapper, Callable]]()\n-\n         self.output_rank = self._get_output_rank()\n \n+    def _get_parallel_sizes(self) -> tuple[int, int, int]:\n+        self.world_size = self.parallel_config.world_size\n+        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n+            f\"global world_size ({self.parallel_config.world_size}) must be \"\n+            f\"divisible by nnodes_within_dp \"\n+            f\"({self.parallel_config.nnodes_within_dp}). \"\n+        )\n+        self.local_world_size = self.parallel_config.local_world_size\n+        tp_size = self.parallel_config.tensor_parallel_size\n+        pp_size = self.parallel_config.pipeline_parallel_size\n+        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        return tp_size, pp_size, pcp_size\n+\n+    def _post_init_executor(self) -> None:\n+        pass\n+\n+    def _is_driver_worker(self, rank: int) -> bool:\n+        return rank % self.parallel_config.tensor_parallel_size == 0\n+\n     def start_worker_monitor(self, inline=False) -> None:\n         workers = self.workers\n         self_ref = weakref.ref(self)\n@@ -517,14 +532,14 @@ def __init__(\n         distributed_init_method: str,\n         input_shm_handle: Handle,\n         shared_worker_lock: LockType,\n+        is_driver_worker: bool,\n     ):\n         self.rank = rank\n         wrapper = WorkerWrapperBase(rpc_rank=local_rank, global_rank=rank)\n         # TODO: move `init_worker` to executor level as a collective rpc call\n         all_kwargs: list[dict] = [\n             {} for _ in range(vllm_config.parallel_config.world_size)\n         ]\n-        is_driver_worker = rank % vllm_config.parallel_config.tensor_parallel_size == 0\n         all_kwargs[local_rank] = {\n             \"vllm_config\": vllm_config,\n             \"local_rank\": local_rank,\n@@ -571,6 +586,7 @@ def make_worker_process(\n         distributed_init_method: str,\n         input_shm_handle,  # Receive SchedulerOutput\n         shared_worker_lock: LockType,\n+        is_driver_worker: bool,\n     ) -> UnreadyWorkerProcHandle:\n         context = get_mp_context()\n         # (reader, writer)\n@@ -588,6 +604,7 @@ def make_worker_process(\n             \"ready_pipe\": (reader, writer),\n             \"death_pipe\": death_reader,\n             \"shared_worker_lock\": shared_worker_lock,\n+            \"is_driver_worker\": is_driver_worker,\n         }\n         # Run EngineCore busy loop in background process.\n         proc = context.Process(", "patch_sha256": "48ce475737c8301e"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28506/review_2628275224", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28506", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/executor/multiproc_executor.py\nReview Comment:\nwill this code path be called if pp = 1, tp =xx , or dp = xx? -No\r\nIf pp=1, it will use UniprocExecutor, this multiProcExecutor is only used when pp > 1\n\nOriginal Code Context:\n@@ -104,16 +105,24 @@ def _init_executor(self) -> None:\n         self.shutdown_event = threading.Event()\n         self.failure_callback: FailureCallback | None = None\n \n-        self.world_size = self.parallel_config.world_size\n-        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n-            f\"global world_size ({self.parallel_config.world_size}) must be \"\n-            f\"divisible by nnodes_within_dp \"\n-            f\"({self.parallel_config.nnodes_within_dp}). \"\n-        )\n-        self.local_world_size = self.parallel_config.local_world_size\n-        tp_size = self.parallel_config.tensor_parallel_size\n-        pp_size = self.parallel_config.pipeline_parallel_size\n-        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        if not USE_TPU_INFERENCE:\n+            self.world_size = self.parallel_config.world_size\n+            assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n+                f\"global world_size ({self.parallel_config.world_size}) must be \"\n+                f\"divisible by nnodes_within_dp \"\n+                f\"({self.parallel_config.nnodes_within_dp}). \"\n+            )\n+            self.local_world_size = self.parallel_config.local_world_size\n+            tp_size = self.parallel_config.tensor_parallel_size\n+            pp_size = self.parallel_config.pipeline_parallel_size\n+            pcp_size = self.parallel_config.prefill_context_parallel_size\n+        else:\n+            # Jax handles TP, PCP with SPMD, world_size = pp_size.\n+            self.world_size = self.parallel_config.pipeline_parallel_size\n", "materials": ["will this code path be called if pp = 1, tp =xx , or dp = xx? -No\r\nIf pp=1, it will use UniprocExecutor, this multiProcExecutor is only used when pp > 1"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -104,16 +104,7 @@ def _init_executor(self) -> None:\n         self.shutdown_event = threading.Event()\n         self.failure_callback: FailureCallback | None = None\n \n-        self.world_size = self.parallel_config.world_size\n-        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n-            f\"global world_size ({self.parallel_config.world_size}) must be \"\n-            f\"divisible by nnodes_within_dp \"\n-            f\"({self.parallel_config.nnodes_within_dp}). \"\n-        )\n-        self.local_world_size = self.parallel_config.local_world_size\n-        tp_size = self.parallel_config.tensor_parallel_size\n-        pp_size = self.parallel_config.pipeline_parallel_size\n-        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        tp_size, pp_size, pcp_size = self._get_parallel_sizes()\n         assert self.world_size == tp_size * pp_size * pcp_size, (\n             f\"world_size ({self.world_size}) must be equal to the \"\n             f\"tensor_parallel_size ({tp_size}) x pipeline\"\n@@ -154,6 +145,7 @@ def _init_executor(self) -> None:\n             )\n             for local_rank in range(self.local_world_size):\n                 global_rank = global_start_rank + local_rank\n+                is_driver_worker = self._is_driver_worker(global_rank)\n                 unready_workers.append(\n                     WorkerProc.make_worker_process(\n                         vllm_config=self.vllm_config,\n@@ -162,6 +154,7 @@ def _init_executor(self) -> None:\n                         distributed_init_method=distributed_init_method,\n                         input_shm_handle=scheduler_output_handle,\n                         shared_worker_lock=shared_worker_lock,\n+                        is_driver_worker=is_driver_worker,\n                     )\n                 )\n \n@@ -199,6 +192,11 @@ def _init_executor(self) -> None:\n             # Wait for all remote response mqs to be ready.\n             for response_mq in self.response_mqs:\n                 response_mq.wait_until_ready()\n+\n+            self.futures_queue = deque[tuple[FutureWrapper, Callable]]()\n+\n+            self._post_init_executor()\n+\n             success = True\n         finally:\n             if not success:\n@@ -209,10 +207,27 @@ def _init_executor(self) -> None:\n                         uw.death_writer.close()\n                 self._ensure_worker_termination([uw.proc for uw in unready_workers])\n \n-        self.futures_queue = deque[tuple[FutureWrapper, Callable]]()\n-\n         self.output_rank = self._get_output_rank()\n \n+    def _get_parallel_sizes(self) -> tuple[int, int, int]:\n+        self.world_size = self.parallel_config.world_size\n+        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n+            f\"global world_size ({self.parallel_config.world_size}) must be \"\n+            f\"divisible by nnodes_within_dp \"\n+            f\"({self.parallel_config.nnodes_within_dp}). \"\n+        )\n+        self.local_world_size = self.parallel_config.local_world_size\n+        tp_size = self.parallel_config.tensor_parallel_size\n+        pp_size = self.parallel_config.pipeline_parallel_size\n+        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        return tp_size, pp_size, pcp_size\n+\n+    def _post_init_executor(self) -> None:\n+        pass\n+\n+    def _is_driver_worker(self, rank: int) -> bool:\n+        return rank % self.parallel_config.tensor_parallel_size == 0\n+\n     def start_worker_monitor(self, inline=False) -> None:\n         workers = self.workers\n         self_ref = weakref.ref(self)\n@@ -517,14 +532,14 @@ def __init__(\n         distributed_init_method: str,\n         input_shm_handle: Handle,\n         shared_worker_lock: LockType,\n+        is_driver_worker: bool,\n     ):\n         self.rank = rank\n         wrapper = WorkerWrapperBase(rpc_rank=local_rank, global_rank=rank)\n         # TODO: move `init_worker` to executor level as a collective rpc call\n         all_kwargs: list[dict] = [\n             {} for _ in range(vllm_config.parallel_config.world_size)\n         ]\n-        is_driver_worker = rank % vllm_config.parallel_config.tensor_parallel_size == 0\n         all_kwargs[local_rank] = {\n             \"vllm_config\": vllm_config,\n             \"local_rank\": local_rank,\n@@ -571,6 +586,7 @@ def make_worker_process(\n         distributed_init_method: str,\n         input_shm_handle,  # Receive SchedulerOutput\n         shared_worker_lock: LockType,\n+        is_driver_worker: bool,\n     ) -> UnreadyWorkerProcHandle:\n         context = get_mp_context()\n         # (reader, writer)\n@@ -588,6 +604,7 @@ def make_worker_process(\n             \"ready_pipe\": (reader, writer),\n             \"death_pipe\": death_reader,\n             \"shared_worker_lock\": shared_worker_lock,\n+            \"is_driver_worker\": is_driver_worker,\n         }\n         # Run EngineCore busy loop in background process.\n         proc = context.Process(", "patch_sha256": "48ce475737c8301e"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28506/review_2628578125", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28506", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/executor/multiproc_executor.py\nReview Comment:\nIs it possible to abstract a method to set different parallelism sizes? Then each hardware plugin can override it.\n\nOriginal Code Context:\n@@ -104,16 +105,24 @@ def _init_executor(self) -> None:\n         self.shutdown_event = threading.Event()\n         self.failure_callback: FailureCallback | None = None\n \n-        self.world_size = self.parallel_config.world_size\n-        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n-            f\"global world_size ({self.parallel_config.world_size}) must be \"\n-            f\"divisible by nnodes_within_dp \"\n-            f\"({self.parallel_config.nnodes_within_dp}). \"\n-        )\n-        self.local_world_size = self.parallel_config.local_world_size\n-        tp_size = self.parallel_config.tensor_parallel_size\n-        pp_size = self.parallel_config.pipeline_parallel_size\n-        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        if not USE_TPU_INFERENCE:\n+            self.world_size = self.parallel_config.world_size\n+            assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n+                f\"global world_size ({self.parallel_config.world_size}) must be \"\n+                f\"divisible by nnodes_within_dp \"\n+                f\"({self.parallel_config.nnodes_within_dp}). \"\n+            )\n+            self.local_world_size = self.parallel_config.local_world_size\n+            tp_size = self.parallel_config.tensor_parallel_size\n+            pp_size = self.parallel_config.pipeline_parallel_size\n+            pcp_size = self.parallel_config.prefill_context_parallel_size\n+        else:\n+            # Jax handles TP, PCP with SPMD, world_size = pp_size.\n+            self.world_size = self.parallel_config.pipeline_parallel_size\n", "materials": ["Is it possible to abstract a method to set different parallelism sizes? Then each hardware plugin can override it."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -104,16 +104,7 @@ def _init_executor(self) -> None:\n         self.shutdown_event = threading.Event()\n         self.failure_callback: FailureCallback | None = None\n \n-        self.world_size = self.parallel_config.world_size\n-        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n-            f\"global world_size ({self.parallel_config.world_size}) must be \"\n-            f\"divisible by nnodes_within_dp \"\n-            f\"({self.parallel_config.nnodes_within_dp}). \"\n-        )\n-        self.local_world_size = self.parallel_config.local_world_size\n-        tp_size = self.parallel_config.tensor_parallel_size\n-        pp_size = self.parallel_config.pipeline_parallel_size\n-        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        tp_size, pp_size, pcp_size = self._get_parallel_sizes()\n         assert self.world_size == tp_size * pp_size * pcp_size, (\n             f\"world_size ({self.world_size}) must be equal to the \"\n             f\"tensor_parallel_size ({tp_size}) x pipeline\"\n@@ -154,6 +145,7 @@ def _init_executor(self) -> None:\n             )\n             for local_rank in range(self.local_world_size):\n                 global_rank = global_start_rank + local_rank\n+                is_driver_worker = self._is_driver_worker(global_rank)\n                 unready_workers.append(\n                     WorkerProc.make_worker_process(\n                         vllm_config=self.vllm_config,\n@@ -162,6 +154,7 @@ def _init_executor(self) -> None:\n                         distributed_init_method=distributed_init_method,\n                         input_shm_handle=scheduler_output_handle,\n                         shared_worker_lock=shared_worker_lock,\n+                        is_driver_worker=is_driver_worker,\n                     )\n                 )\n \n@@ -199,6 +192,11 @@ def _init_executor(self) -> None:\n             # Wait for all remote response mqs to be ready.\n             for response_mq in self.response_mqs:\n                 response_mq.wait_until_ready()\n+\n+            self.futures_queue = deque[tuple[FutureWrapper, Callable]]()\n+\n+            self._post_init_executor()\n+\n             success = True\n         finally:\n             if not success:\n@@ -209,10 +207,27 @@ def _init_executor(self) -> None:\n                         uw.death_writer.close()\n                 self._ensure_worker_termination([uw.proc for uw in unready_workers])\n \n-        self.futures_queue = deque[tuple[FutureWrapper, Callable]]()\n-\n         self.output_rank = self._get_output_rank()\n \n+    def _get_parallel_sizes(self) -> tuple[int, int, int]:\n+        self.world_size = self.parallel_config.world_size\n+        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n+            f\"global world_size ({self.parallel_config.world_size}) must be \"\n+            f\"divisible by nnodes_within_dp \"\n+            f\"({self.parallel_config.nnodes_within_dp}). \"\n+        )\n+        self.local_world_size = self.parallel_config.local_world_size\n+        tp_size = self.parallel_config.tensor_parallel_size\n+        pp_size = self.parallel_config.pipeline_parallel_size\n+        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        return tp_size, pp_size, pcp_size\n+\n+    def _post_init_executor(self) -> None:\n+        pass\n+\n+    def _is_driver_worker(self, rank: int) -> bool:\n+        return rank % self.parallel_config.tensor_parallel_size == 0\n+\n     def start_worker_monitor(self, inline=False) -> None:\n         workers = self.workers\n         self_ref = weakref.ref(self)\n@@ -517,14 +532,14 @@ def __init__(\n         distributed_init_method: str,\n         input_shm_handle: Handle,\n         shared_worker_lock: LockType,\n+        is_driver_worker: bool,\n     ):\n         self.rank = rank\n         wrapper = WorkerWrapperBase(rpc_rank=local_rank, global_rank=rank)\n         # TODO: move `init_worker` to executor level as a collective rpc call\n         all_kwargs: list[dict] = [\n             {} for _ in range(vllm_config.parallel_config.world_size)\n         ]\n-        is_driver_worker = rank % vllm_config.parallel_config.tensor_parallel_size == 0\n         all_kwargs[local_rank] = {\n             \"vllm_config\": vllm_config,\n             \"local_rank\": local_rank,\n@@ -571,6 +586,7 @@ def make_worker_process(\n         distributed_init_method: str,\n         input_shm_handle,  # Receive SchedulerOutput\n         shared_worker_lock: LockType,\n+        is_driver_worker: bool,\n     ) -> UnreadyWorkerProcHandle:\n         context = get_mp_context()\n         # (reader, writer)\n@@ -588,6 +604,7 @@ def make_worker_process(\n             \"ready_pipe\": (reader, writer),\n             \"death_pipe\": death_reader,\n             \"shared_worker_lock\": shared_worker_lock,\n+            \"is_driver_worker\": is_driver_worker,\n         }\n         # Run EngineCore busy loop in background process.\n         proc = context.Process(", "patch_sha256": "48ce475737c8301e"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28506/review_2628581319", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28506", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/executor/multiproc_executor.py\nReview Comment:\nIs it possible to add a `_post_init_executor` for hardware plugins to override?\n\nOriginal Code Context:\n@@ -199,6 +208,16 @@ def _init_executor(self) -> None:\n             # Wait for all remote response mqs to be ready.\n             for response_mq in self.response_mqs:\n                 response_mq.wait_until_ready()\n+\n+            self.futures_queue = deque[tuple[FutureWrapper, Callable]]()\n+\n+            # set up jax transfer connection.\n", "materials": ["Is it possible to add a `_post_init_executor` for hardware plugins to override?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -104,16 +104,7 @@ def _init_executor(self) -> None:\n         self.shutdown_event = threading.Event()\n         self.failure_callback: FailureCallback | None = None\n \n-        self.world_size = self.parallel_config.world_size\n-        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n-            f\"global world_size ({self.parallel_config.world_size}) must be \"\n-            f\"divisible by nnodes_within_dp \"\n-            f\"({self.parallel_config.nnodes_within_dp}). \"\n-        )\n-        self.local_world_size = self.parallel_config.local_world_size\n-        tp_size = self.parallel_config.tensor_parallel_size\n-        pp_size = self.parallel_config.pipeline_parallel_size\n-        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        tp_size, pp_size, pcp_size = self._get_parallel_sizes()\n         assert self.world_size == tp_size * pp_size * pcp_size, (\n             f\"world_size ({self.world_size}) must be equal to the \"\n             f\"tensor_parallel_size ({tp_size}) x pipeline\"\n@@ -154,6 +145,7 @@ def _init_executor(self) -> None:\n             )\n             for local_rank in range(self.local_world_size):\n                 global_rank = global_start_rank + local_rank\n+                is_driver_worker = self._is_driver_worker(global_rank)\n                 unready_workers.append(\n                     WorkerProc.make_worker_process(\n                         vllm_config=self.vllm_config,\n@@ -162,6 +154,7 @@ def _init_executor(self) -> None:\n                         distributed_init_method=distributed_init_method,\n                         input_shm_handle=scheduler_output_handle,\n                         shared_worker_lock=shared_worker_lock,\n+                        is_driver_worker=is_driver_worker,\n                     )\n                 )\n \n@@ -199,6 +192,11 @@ def _init_executor(self) -> None:\n             # Wait for all remote response mqs to be ready.\n             for response_mq in self.response_mqs:\n                 response_mq.wait_until_ready()\n+\n+            self.futures_queue = deque[tuple[FutureWrapper, Callable]]()\n+\n+            self._post_init_executor()\n+\n             success = True\n         finally:\n             if not success:\n@@ -209,10 +207,27 @@ def _init_executor(self) -> None:\n                         uw.death_writer.close()\n                 self._ensure_worker_termination([uw.proc for uw in unready_workers])\n \n-        self.futures_queue = deque[tuple[FutureWrapper, Callable]]()\n-\n         self.output_rank = self._get_output_rank()\n \n+    def _get_parallel_sizes(self) -> tuple[int, int, int]:\n+        self.world_size = self.parallel_config.world_size\n+        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n+            f\"global world_size ({self.parallel_config.world_size}) must be \"\n+            f\"divisible by nnodes_within_dp \"\n+            f\"({self.parallel_config.nnodes_within_dp}). \"\n+        )\n+        self.local_world_size = self.parallel_config.local_world_size\n+        tp_size = self.parallel_config.tensor_parallel_size\n+        pp_size = self.parallel_config.pipeline_parallel_size\n+        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        return tp_size, pp_size, pcp_size\n+\n+    def _post_init_executor(self) -> None:\n+        pass\n+\n+    def _is_driver_worker(self, rank: int) -> bool:\n+        return rank % self.parallel_config.tensor_parallel_size == 0\n+\n     def start_worker_monitor(self, inline=False) -> None:\n         workers = self.workers\n         self_ref = weakref.ref(self)\n@@ -517,14 +532,14 @@ def __init__(\n         distributed_init_method: str,\n         input_shm_handle: Handle,\n         shared_worker_lock: LockType,\n+        is_driver_worker: bool,\n     ):\n         self.rank = rank\n         wrapper = WorkerWrapperBase(rpc_rank=local_rank, global_rank=rank)\n         # TODO: move `init_worker` to executor level as a collective rpc call\n         all_kwargs: list[dict] = [\n             {} for _ in range(vllm_config.parallel_config.world_size)\n         ]\n-        is_driver_worker = rank % vllm_config.parallel_config.tensor_parallel_size == 0\n         all_kwargs[local_rank] = {\n             \"vllm_config\": vllm_config,\n             \"local_rank\": local_rank,\n@@ -571,6 +586,7 @@ def make_worker_process(\n         distributed_init_method: str,\n         input_shm_handle,  # Receive SchedulerOutput\n         shared_worker_lock: LockType,\n+        is_driver_worker: bool,\n     ) -> UnreadyWorkerProcHandle:\n         context = get_mp_context()\n         # (reader, writer)\n@@ -588,6 +604,7 @@ def make_worker_process(\n             \"ready_pipe\": (reader, writer),\n             \"death_pipe\": death_reader,\n             \"shared_worker_lock\": shared_worker_lock,\n+            \"is_driver_worker\": is_driver_worker,\n         }\n         # Run EngineCore busy loop in background process.\n         proc = context.Process(", "patch_sha256": "48ce475737c8301e"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28506/review_2628587558", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28506", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/executor/multiproc_executor.py\nReview Comment:\nThis method is very small, we can override it directly.\n\nOriginal Code Context:\n@@ -425,6 +442,8 @@ def _get_output_rank(self) -> int:\n         # 16-23, PP rank 2\n         # 24-31, PP rank 3\n         # so world_size - tp_size = 32 - 8 = 24 should be PP rank = -1 (i.e. 3)\n+        if USE_TPU_INFERENCE:\n", "materials": ["This method is very small, we can override it directly."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -104,16 +104,7 @@ def _init_executor(self) -> None:\n         self.shutdown_event = threading.Event()\n         self.failure_callback: FailureCallback | None = None\n \n-        self.world_size = self.parallel_config.world_size\n-        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n-            f\"global world_size ({self.parallel_config.world_size}) must be \"\n-            f\"divisible by nnodes_within_dp \"\n-            f\"({self.parallel_config.nnodes_within_dp}). \"\n-        )\n-        self.local_world_size = self.parallel_config.local_world_size\n-        tp_size = self.parallel_config.tensor_parallel_size\n-        pp_size = self.parallel_config.pipeline_parallel_size\n-        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        tp_size, pp_size, pcp_size = self._get_parallel_sizes()\n         assert self.world_size == tp_size * pp_size * pcp_size, (\n             f\"world_size ({self.world_size}) must be equal to the \"\n             f\"tensor_parallel_size ({tp_size}) x pipeline\"\n@@ -154,6 +145,7 @@ def _init_executor(self) -> None:\n             )\n             for local_rank in range(self.local_world_size):\n                 global_rank = global_start_rank + local_rank\n+                is_driver_worker = self._is_driver_worker(global_rank)\n                 unready_workers.append(\n                     WorkerProc.make_worker_process(\n                         vllm_config=self.vllm_config,\n@@ -162,6 +154,7 @@ def _init_executor(self) -> None:\n                         distributed_init_method=distributed_init_method,\n                         input_shm_handle=scheduler_output_handle,\n                         shared_worker_lock=shared_worker_lock,\n+                        is_driver_worker=is_driver_worker,\n                     )\n                 )\n \n@@ -199,6 +192,11 @@ def _init_executor(self) -> None:\n             # Wait for all remote response mqs to be ready.\n             for response_mq in self.response_mqs:\n                 response_mq.wait_until_ready()\n+\n+            self.futures_queue = deque[tuple[FutureWrapper, Callable]]()\n+\n+            self._post_init_executor()\n+\n             success = True\n         finally:\n             if not success:\n@@ -209,10 +207,27 @@ def _init_executor(self) -> None:\n                         uw.death_writer.close()\n                 self._ensure_worker_termination([uw.proc for uw in unready_workers])\n \n-        self.futures_queue = deque[tuple[FutureWrapper, Callable]]()\n-\n         self.output_rank = self._get_output_rank()\n \n+    def _get_parallel_sizes(self) -> tuple[int, int, int]:\n+        self.world_size = self.parallel_config.world_size\n+        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n+            f\"global world_size ({self.parallel_config.world_size}) must be \"\n+            f\"divisible by nnodes_within_dp \"\n+            f\"({self.parallel_config.nnodes_within_dp}). \"\n+        )\n+        self.local_world_size = self.parallel_config.local_world_size\n+        tp_size = self.parallel_config.tensor_parallel_size\n+        pp_size = self.parallel_config.pipeline_parallel_size\n+        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        return tp_size, pp_size, pcp_size\n+\n+    def _post_init_executor(self) -> None:\n+        pass\n+\n+    def _is_driver_worker(self, rank: int) -> bool:\n+        return rank % self.parallel_config.tensor_parallel_size == 0\n+\n     def start_worker_monitor(self, inline=False) -> None:\n         workers = self.workers\n         self_ref = weakref.ref(self)\n@@ -517,14 +532,14 @@ def __init__(\n         distributed_init_method: str,\n         input_shm_handle: Handle,\n         shared_worker_lock: LockType,\n+        is_driver_worker: bool,\n     ):\n         self.rank = rank\n         wrapper = WorkerWrapperBase(rpc_rank=local_rank, global_rank=rank)\n         # TODO: move `init_worker` to executor level as a collective rpc call\n         all_kwargs: list[dict] = [\n             {} for _ in range(vllm_config.parallel_config.world_size)\n         ]\n-        is_driver_worker = rank % vllm_config.parallel_config.tensor_parallel_size == 0\n         all_kwargs[local_rank] = {\n             \"vllm_config\": vllm_config,\n             \"local_rank\": local_rank,\n@@ -571,6 +586,7 @@ def make_worker_process(\n         distributed_init_method: str,\n         input_shm_handle,  # Receive SchedulerOutput\n         shared_worker_lock: LockType,\n+        is_driver_worker: bool,\n     ) -> UnreadyWorkerProcHandle:\n         context = get_mp_context()\n         # (reader, writer)\n@@ -588,6 +604,7 @@ def make_worker_process(\n             \"ready_pipe\": (reader, writer),\n             \"death_pipe\": death_reader,\n             \"shared_worker_lock\": shared_worker_lock,\n+            \"is_driver_worker\": is_driver_worker,\n         }\n         # Run EngineCore busy loop in background process.\n         proc = context.Process(", "patch_sha256": "48ce475737c8301e"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28506/review_2665988881", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28506", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/executor/multiproc_executor.py\nReview Comment:\nYes, added `_get_parallel_sizes` method.\n\nOriginal Code Context:\n@@ -104,16 +105,24 @@ def _init_executor(self) -> None:\n         self.shutdown_event = threading.Event()\n         self.failure_callback: FailureCallback | None = None\n \n-        self.world_size = self.parallel_config.world_size\n-        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n-            f\"global world_size ({self.parallel_config.world_size}) must be \"\n-            f\"divisible by nnodes_within_dp \"\n-            f\"({self.parallel_config.nnodes_within_dp}). \"\n-        )\n-        self.local_world_size = self.parallel_config.local_world_size\n-        tp_size = self.parallel_config.tensor_parallel_size\n-        pp_size = self.parallel_config.pipeline_parallel_size\n-        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        if not USE_TPU_INFERENCE:\n+            self.world_size = self.parallel_config.world_size\n+            assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n+                f\"global world_size ({self.parallel_config.world_size}) must be \"\n+                f\"divisible by nnodes_within_dp \"\n+                f\"({self.parallel_config.nnodes_within_dp}). \"\n+            )\n+            self.local_world_size = self.parallel_config.local_world_size\n+            tp_size = self.parallel_config.tensor_parallel_size\n+            pp_size = self.parallel_config.pipeline_parallel_size\n+            pcp_size = self.parallel_config.prefill_context_parallel_size\n+        else:\n+            # Jax handles TP, PCP with SPMD, world_size = pp_size.\n+            self.world_size = self.parallel_config.pipeline_parallel_size\n", "materials": ["Yes, added `_get_parallel_sizes` method."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -104,16 +104,7 @@ def _init_executor(self) -> None:\n         self.shutdown_event = threading.Event()\n         self.failure_callback: FailureCallback | None = None\n \n-        self.world_size = self.parallel_config.world_size\n-        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n-            f\"global world_size ({self.parallel_config.world_size}) must be \"\n-            f\"divisible by nnodes_within_dp \"\n-            f\"({self.parallel_config.nnodes_within_dp}). \"\n-        )\n-        self.local_world_size = self.parallel_config.local_world_size\n-        tp_size = self.parallel_config.tensor_parallel_size\n-        pp_size = self.parallel_config.pipeline_parallel_size\n-        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        tp_size, pp_size, pcp_size = self._get_parallel_sizes()\n         assert self.world_size == tp_size * pp_size * pcp_size, (\n             f\"world_size ({self.world_size}) must be equal to the \"\n             f\"tensor_parallel_size ({tp_size}) x pipeline\"\n@@ -154,6 +145,7 @@ def _init_executor(self) -> None:\n             )\n             for local_rank in range(self.local_world_size):\n                 global_rank = global_start_rank + local_rank\n+                is_driver_worker = self._is_driver_worker(global_rank)\n                 unready_workers.append(\n                     WorkerProc.make_worker_process(\n                         vllm_config=self.vllm_config,\n@@ -162,6 +154,7 @@ def _init_executor(self) -> None:\n                         distributed_init_method=distributed_init_method,\n                         input_shm_handle=scheduler_output_handle,\n                         shared_worker_lock=shared_worker_lock,\n+                        is_driver_worker=is_driver_worker,\n                     )\n                 )\n \n@@ -199,6 +192,11 @@ def _init_executor(self) -> None:\n             # Wait for all remote response mqs to be ready.\n             for response_mq in self.response_mqs:\n                 response_mq.wait_until_ready()\n+\n+            self.futures_queue = deque[tuple[FutureWrapper, Callable]]()\n+\n+            self._post_init_executor()\n+\n             success = True\n         finally:\n             if not success:\n@@ -209,10 +207,27 @@ def _init_executor(self) -> None:\n                         uw.death_writer.close()\n                 self._ensure_worker_termination([uw.proc for uw in unready_workers])\n \n-        self.futures_queue = deque[tuple[FutureWrapper, Callable]]()\n-\n         self.output_rank = self._get_output_rank()\n \n+    def _get_parallel_sizes(self) -> tuple[int, int, int]:\n+        self.world_size = self.parallel_config.world_size\n+        assert self.world_size % self.parallel_config.nnodes_within_dp == 0, (\n+            f\"global world_size ({self.parallel_config.world_size}) must be \"\n+            f\"divisible by nnodes_within_dp \"\n+            f\"({self.parallel_config.nnodes_within_dp}). \"\n+        )\n+        self.local_world_size = self.parallel_config.local_world_size\n+        tp_size = self.parallel_config.tensor_parallel_size\n+        pp_size = self.parallel_config.pipeline_parallel_size\n+        pcp_size = self.parallel_config.prefill_context_parallel_size\n+        return tp_size, pp_size, pcp_size\n+\n+    def _post_init_executor(self) -> None:\n+        pass\n+\n+    def _is_driver_worker(self, rank: int) -> bool:\n+        return rank % self.parallel_config.tensor_parallel_size == 0\n+\n     def start_worker_monitor(self, inline=False) -> None:\n         workers = self.workers\n         self_ref = weakref.ref(self)\n@@ -517,14 +532,14 @@ def __init__(\n         distributed_init_method: str,\n         input_shm_handle: Handle,\n         shared_worker_lock: LockType,\n+        is_driver_worker: bool,\n     ):\n         self.rank = rank\n         wrapper = WorkerWrapperBase(rpc_rank=local_rank, global_rank=rank)\n         # TODO: move `init_worker` to executor level as a collective rpc call\n         all_kwargs: list[dict] = [\n             {} for _ in range(vllm_config.parallel_config.world_size)\n         ]\n-        is_driver_worker = rank % vllm_config.parallel_config.tensor_parallel_size == 0\n         all_kwargs[local_rank] = {\n             \"vllm_config\": vllm_config,\n             \"local_rank\": local_rank,\n@@ -571,6 +586,7 @@ def make_worker_process(\n         distributed_init_method: str,\n         input_shm_handle,  # Receive SchedulerOutput\n         shared_worker_lock: LockType,\n+        is_driver_worker: bool,\n     ) -> UnreadyWorkerProcHandle:\n         context = get_mp_context()\n         # (reader, writer)\n@@ -588,6 +604,7 @@ def make_worker_process(\n             \"ready_pipe\": (reader, writer),\n             \"death_pipe\": death_reader,\n             \"shared_worker_lock\": shared_worker_lock,\n+            \"is_driver_worker\": is_driver_worker,\n         }\n         # Run EngineCore busy loop in background process.\n         proc = context.Process(", "patch_sha256": "48ce475737c8301e"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2577939340", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/parallel.py\nReview Comment:\nNo need to separate HT and LL mode?\n\nOriginal Code Context:\n@@ -128,6 +128,7 @@ class ParallelConfig:\n             \"pplx\",\n             \"deepep_high_throughput\",\n             \"deepep_low_latency\",\n+            \"mori\",\n", "materials": ["No need to separate HT and LL mode?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -43,6 +43,7 @@\n     \"pplx\",\n     \"deepep_high_throughput\",\n     \"deepep_low_latency\",\n+    \"mori\",\n     \"allgather_reducescatter\",\n     \"flashinfer_all2allv\",\n ]\n@@ -158,6 +159,7 @@ class ParallelConfig:\n     - \"pplx\": Use pplx kernels\\n\n     - \"deepep_high_throughput\": Use deepep high-throughput kernels\\n\n     - \"deepep_low_latency\": Use deepep low-latency kernels\\n\n+    - \"mori\": Use mori kernels\\n\n     - \"flashinfer_all2allv\": Use flashinfer alltoallv kernels for mnnvl\"\"\"\n \n     max_parallel_loading_workers: int | None = None\n@@ -443,6 +445,7 @@ def use_sequence_parallel_moe(self) -> bool:\n                 \"naive\",\n                 \"deepep_high_throughput\",\n                 \"deepep_low_latency\",\n+                \"mori\",\n             )\n             and self.enable_expert_parallel\n             and self.tensor_parallel_size > 1", "patch_sha256": "b48cc4877a07bfe9"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2578011883", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/distributed/device_communicators/all2all.py\nReview Comment:\nimport done in `__init__`\r\nPlease remove redundant import in methods.\n\nOriginal Code Context:\n@@ -488,3 +488,82 @@ def cleanup(self):\n                 self.prepare_workspace_tensor = None\n                 self.mapping = None\n                 self.initialized = False\n+\n+\n+class MoriAll2AllManager(All2AllManagerBase):\n+    def __init__(self, cpu_group):\n+        assert has_mori(), (\n+            \"MoRI kernels not found. Please follow https://github.com/ROCm/mori/blob/main/README.md\"\n+            \" to install MoRI kernels.\"\n+        )  # noqa\n+        import mori\n+\n+        super().__init__(cpu_group)\n+        self.handle_cache = Cache()\n+\n+        torch._C._distributed_c10d._register_process_group(\"mori\", cpu_group)\n+        mori.shmem.shmem_torch_process_group_init(\"mori\")\n+\n+    def _make_all2all_kwargs(\n+        self,\n+        rank: int,\n+        num_ep_ranks: int,\n+        input_dtype: torch.dtype,\n+        quant_dtype: torch.dtype,\n+        token_hidden_size: int,\n+        scale_dim: int,\n+        scale_type_size: int,\n+        max_num_tokens_per_dp_rank: int,\n+        num_local_experts: int,\n+        num_experts_per_token: int,\n+        gpu_per_node: int,\n+    ):\n+        import mori  # type: ignore[import-not-found]\n", "materials": ["import done in `__init__`\r\nPlease remove redundant import in methods."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -10,7 +10,7 @@\n from vllm.forward_context import get_forward_context\n from vllm.logger import init_logger\n from vllm.utils.flashinfer import has_flashinfer_all2all\n-from vllm.utils.import_utils import has_deep_ep, has_pplx\n+from vllm.utils.import_utils import has_deep_ep, has_mori, has_pplx\n \n from .base_device_communicator import All2AllManagerBase, Cache\n \n@@ -507,3 +507,96 @@ def cleanup(self):\n                 self.prepare_workspace_tensor = None\n                 self.mapping = None\n                 self.initialized = False\n+\n+\n+class MoriAll2AllManager(All2AllManagerBase):\n+    def __init__(self, cpu_group):\n+        assert has_mori(), (\n+            \"MoRI kernels not found. Please follow https://github.com/ROCm/mori/blob/main/README.md\"\n+            \" to install MoRI kernels.\"\n+        )  # noqa\n+        import mori\n+\n+        super().__init__(cpu_group)\n+        self.handle_cache = Cache()\n+\n+        torch._C._distributed_c10d._register_process_group(\"mori\", cpu_group)\n+        mori.shmem.shmem_torch_process_group_init(\"mori\")\n+\n+    def _make_all2all_kwargs(\n+        self,\n+        rank: int,\n+        num_ep_ranks: int,\n+        input_dtype: torch.dtype,\n+        quant_dtype: torch.dtype,\n+        token_hidden_size: int,\n+        scale_dim: int,\n+        scale_type_size: int,\n+        max_num_tokens_per_dp_rank: int,\n+        num_local_experts: int,\n+        num_experts_per_token: int,\n+    ):\n+        import mori  # type: ignore[import-not-found]\n+\n+        from vllm.platforms.rocm import on_gfx942, on_gfx950\n+\n+        assert on_gfx942() or on_gfx950(), (\n+            \"mori currently only support arch gfx942 and gfx950\"\n+        )\n+\n+        if not self.internode:\n+            # single node\n+            kernel_type = mori.ops.EpDispatchCombineKernelType.IntraNode\n+            rdma_block_num = 0\n+            warp_num_per_block = 16\n+            block_num = 80\n+        else:\n+            # multi node\n+            kernel_type = mori.ops.EpDispatchCombineKernelType.InterNodeV1\n+            if on_gfx942():\n+                warp_num_per_block = 16\n+                block_num = 32\n+                rdma_block_num = 16\n+            elif on_gfx950():\n+                warp_num_per_block = 8\n+                block_num = 64\n+                rdma_block_num = 32\n+            else:\n+                raise NotImplementedError(\n+                    \"mori currently only support arch gfx942 and gfx950\"\n+                )\n+\n+        return dict(\n+            rank=rank,\n+            world_size=num_ep_ranks,\n+            data_type=quant_dtype,\n+            hidden_dim=token_hidden_size,\n+            scale_dim=scale_dim,\n+            scale_type_size=scale_type_size,\n+            max_token_type_size=input_dtype.itemsize,\n+            max_num_inp_token_per_rank=max_num_tokens_per_dp_rank,\n+            num_experts_per_rank=num_local_experts,\n+            num_experts_per_token=num_experts_per_token,\n+            warp_num_per_block=warp_num_per_block,\n+            block_num=block_num,\n+            kernel_type=kernel_type,\n+            rdma_block_num=rdma_block_num,\n+            gpu_per_node=min(8, num_ep_ranks),\n+        )\n+\n+    def _make_handle(self, **kwargs):\n+        import mori  # type: ignore[import-not-found]\n+\n+        mori_config = mori.ops.EpDispatchCombineConfig(**kwargs)\n+        handle = mori.ops.EpDispatchCombineOp(mori_config)\n+        return handle\n+\n+    def get_handle(self, kwargs):\n+        import mori  # type: ignore[import-not-found]\n+\n+        mori_kwargs = self._make_all2all_kwargs(**kwargs)\n+        logger.debug(\"MoRI all2all args %s\", mori_kwargs)\n+        handle: mori.ops.EpDispatchCombineOp = self.handle_cache.get_or_create(\n+            mori_kwargs, self._make_handle\n+        )\n+        return handle", "patch_sha256": "51f0d3664411f652"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2578016044", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/distributed/device_communicators/all2all.py\nReview Comment:\nPlease not to use hard coded numbers, define it when necessary, better be architecture dependent.\n\nOriginal Code Context:\n@@ -488,3 +488,82 @@ def cleanup(self):\n                 self.prepare_workspace_tensor = None\n                 self.mapping = None\n                 self.initialized = False\n+\n+\n+class MoriAll2AllManager(All2AllManagerBase):\n+    def __init__(self, cpu_group):\n+        assert has_mori(), (\n+            \"MoRI kernels not found. Please follow https://github.com/ROCm/mori/blob/main/README.md\"\n+            \" to install MoRI kernels.\"\n+        )  # noqa\n+        import mori\n+\n+        super().__init__(cpu_group)\n+        self.handle_cache = Cache()\n+\n+        torch._C._distributed_c10d._register_process_group(\"mori\", cpu_group)\n+        mori.shmem.shmem_torch_process_group_init(\"mori\")\n+\n+    def _make_all2all_kwargs(\n+        self,\n+        rank: int,\n+        num_ep_ranks: int,\n+        input_dtype: torch.dtype,\n+        quant_dtype: torch.dtype,\n+        token_hidden_size: int,\n+        scale_dim: int,\n+        scale_type_size: int,\n+        max_num_tokens_per_dp_rank: int,\n+        num_local_experts: int,\n+        num_experts_per_token: int,\n+        gpu_per_node: int,\n+    ):\n+        import mori  # type: ignore[import-not-found]\n+\n+        if not self.internode:\n+            # single node\n+            kernel_type = mori.ops.EpDispatchCombineKernelType.IntraNode\n+            warp_num_per_block = 16\n+            block_num = 80\n", "materials": ["Please not to use hard coded numbers, define it when necessary, better be architecture dependent."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -10,7 +10,7 @@\n from vllm.forward_context import get_forward_context\n from vllm.logger import init_logger\n from vllm.utils.flashinfer import has_flashinfer_all2all\n-from vllm.utils.import_utils import has_deep_ep, has_pplx\n+from vllm.utils.import_utils import has_deep_ep, has_mori, has_pplx\n \n from .base_device_communicator import All2AllManagerBase, Cache\n \n@@ -507,3 +507,96 @@ def cleanup(self):\n                 self.prepare_workspace_tensor = None\n                 self.mapping = None\n                 self.initialized = False\n+\n+\n+class MoriAll2AllManager(All2AllManagerBase):\n+    def __init__(self, cpu_group):\n+        assert has_mori(), (\n+            \"MoRI kernels not found. Please follow https://github.com/ROCm/mori/blob/main/README.md\"\n+            \" to install MoRI kernels.\"\n+        )  # noqa\n+        import mori\n+\n+        super().__init__(cpu_group)\n+        self.handle_cache = Cache()\n+\n+        torch._C._distributed_c10d._register_process_group(\"mori\", cpu_group)\n+        mori.shmem.shmem_torch_process_group_init(\"mori\")\n+\n+    def _make_all2all_kwargs(\n+        self,\n+        rank: int,\n+        num_ep_ranks: int,\n+        input_dtype: torch.dtype,\n+        quant_dtype: torch.dtype,\n+        token_hidden_size: int,\n+        scale_dim: int,\n+        scale_type_size: int,\n+        max_num_tokens_per_dp_rank: int,\n+        num_local_experts: int,\n+        num_experts_per_token: int,\n+    ):\n+        import mori  # type: ignore[import-not-found]\n+\n+        from vllm.platforms.rocm import on_gfx942, on_gfx950\n+\n+        assert on_gfx942() or on_gfx950(), (\n+            \"mori currently only support arch gfx942 and gfx950\"\n+        )\n+\n+        if not self.internode:\n+            # single node\n+            kernel_type = mori.ops.EpDispatchCombineKernelType.IntraNode\n+            rdma_block_num = 0\n+            warp_num_per_block = 16\n+            block_num = 80\n+        else:\n+            # multi node\n+            kernel_type = mori.ops.EpDispatchCombineKernelType.InterNodeV1\n+            if on_gfx942():\n+                warp_num_per_block = 16\n+                block_num = 32\n+                rdma_block_num = 16\n+            elif on_gfx950():\n+                warp_num_per_block = 8\n+                block_num = 64\n+                rdma_block_num = 32\n+            else:\n+                raise NotImplementedError(\n+                    \"mori currently only support arch gfx942 and gfx950\"\n+                )\n+\n+        return dict(\n+            rank=rank,\n+            world_size=num_ep_ranks,\n+            data_type=quant_dtype,\n+            hidden_dim=token_hidden_size,\n+            scale_dim=scale_dim,\n+            scale_type_size=scale_type_size,\n+            max_token_type_size=input_dtype.itemsize,\n+            max_num_inp_token_per_rank=max_num_tokens_per_dp_rank,\n+            num_experts_per_rank=num_local_experts,\n+            num_experts_per_token=num_experts_per_token,\n+            warp_num_per_block=warp_num_per_block,\n+            block_num=block_num,\n+            kernel_type=kernel_type,\n+            rdma_block_num=rdma_block_num,\n+            gpu_per_node=min(8, num_ep_ranks),\n+        )\n+\n+    def _make_handle(self, **kwargs):\n+        import mori  # type: ignore[import-not-found]\n+\n+        mori_config = mori.ops.EpDispatchCombineConfig(**kwargs)\n+        handle = mori.ops.EpDispatchCombineOp(mori_config)\n+        return handle\n+\n+    def get_handle(self, kwargs):\n+        import mori  # type: ignore[import-not-found]\n+\n+        mori_kwargs = self._make_all2all_kwargs(**kwargs)\n+        logger.debug(\"MoRI all2all args %s\", mori_kwargs)\n+        handle: mori.ops.EpDispatchCombineOp = self.handle_cache.get_or_create(\n+            mori_kwargs, self._make_handle\n+        )\n+        return handle", "patch_sha256": "51f0d3664411f652"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2578120487", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/utils/import_utils.py\nReview Comment:\nGiven the dependency that `aiter->mori`, we can check both modules to claim `has_mori`\n\nOriginal Code Context:\n@@ -447,3 +447,13 @@ def has_arctic_inference() -> bool:\n     \"\"\"Whether the optional `arctic_inference` package is available.\"\"\"\n \n     return _has_module(\"arctic_inference\")\n+\n+\n+def has_aiter() -> bool:\n+    \"\"\"Whether the optional `aiter` package is available.\"\"\"\n+    return _has_module(\"aiter\")\n+\n+\n+def has_mori() -> bool:\n+    \"\"\"Whether the optional `mori` package is available.\"\"\"\n+    return _has_module(\"mori\")\n", "materials": ["Given the dependency that `aiter->mori`, we can check both modules to claim `has_mori`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -451,3 +451,13 @@ def has_helion() -> bool:\n             # use helion...\n     \"\"\"\n     return _has_module(\"helion\")\n+\n+\n+def has_aiter() -> bool:\n+    \"\"\"Whether the optional `aiter` package is available.\"\"\"\n+    return _has_module(\"aiter\")\n+\n+\n+def has_mori() -> bool:\n+    \"\"\"Whether the optional `mori` package is available.\"\"\"\n+    return _has_module(\"mori\")", "patch_sha256": "2de655bc6e085627"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2579444342", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/distributed/device_communicators/all2all.py\nReview Comment:\n> import done in `__init__` Please remove redundant import in methods.\r\n\r\nused local import in `__init__`. So this import is also necessary.\n\nOriginal Code Context:\n@@ -488,3 +488,82 @@ def cleanup(self):\n                 self.prepare_workspace_tensor = None\n                 self.mapping = None\n                 self.initialized = False\n+\n+\n+class MoriAll2AllManager(All2AllManagerBase):\n+    def __init__(self, cpu_group):\n+        assert has_mori(), (\n+            \"MoRI kernels not found. Please follow https://github.com/ROCm/mori/blob/main/README.md\"\n+            \" to install MoRI kernels.\"\n+        )  # noqa\n+        import mori\n+\n+        super().__init__(cpu_group)\n+        self.handle_cache = Cache()\n+\n+        torch._C._distributed_c10d._register_process_group(\"mori\", cpu_group)\n+        mori.shmem.shmem_torch_process_group_init(\"mori\")\n+\n+    def _make_all2all_kwargs(\n+        self,\n+        rank: int,\n+        num_ep_ranks: int,\n+        input_dtype: torch.dtype,\n+        quant_dtype: torch.dtype,\n+        token_hidden_size: int,\n+        scale_dim: int,\n+        scale_type_size: int,\n+        max_num_tokens_per_dp_rank: int,\n+        num_local_experts: int,\n+        num_experts_per_token: int,\n+        gpu_per_node: int,\n+    ):\n+        import mori  # type: ignore[import-not-found]\n", "materials": ["> import done in `__init__` Please remove redundant import in methods.\r\n\r\nused local import in `__init__`. So this import is also necessary."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -10,7 +10,7 @@\n from vllm.forward_context import get_forward_context\n from vllm.logger import init_logger\n from vllm.utils.flashinfer import has_flashinfer_all2all\n-from vllm.utils.import_utils import has_deep_ep, has_pplx\n+from vllm.utils.import_utils import has_deep_ep, has_mori, has_pplx\n \n from .base_device_communicator import All2AllManagerBase, Cache\n \n@@ -507,3 +507,96 @@ def cleanup(self):\n                 self.prepare_workspace_tensor = None\n                 self.mapping = None\n                 self.initialized = False\n+\n+\n+class MoriAll2AllManager(All2AllManagerBase):\n+    def __init__(self, cpu_group):\n+        assert has_mori(), (\n+            \"MoRI kernels not found. Please follow https://github.com/ROCm/mori/blob/main/README.md\"\n+            \" to install MoRI kernels.\"\n+        )  # noqa\n+        import mori\n+\n+        super().__init__(cpu_group)\n+        self.handle_cache = Cache()\n+\n+        torch._C._distributed_c10d._register_process_group(\"mori\", cpu_group)\n+        mori.shmem.shmem_torch_process_group_init(\"mori\")\n+\n+    def _make_all2all_kwargs(\n+        self,\n+        rank: int,\n+        num_ep_ranks: int,\n+        input_dtype: torch.dtype,\n+        quant_dtype: torch.dtype,\n+        token_hidden_size: int,\n+        scale_dim: int,\n+        scale_type_size: int,\n+        max_num_tokens_per_dp_rank: int,\n+        num_local_experts: int,\n+        num_experts_per_token: int,\n+    ):\n+        import mori  # type: ignore[import-not-found]\n+\n+        from vllm.platforms.rocm import on_gfx942, on_gfx950\n+\n+        assert on_gfx942() or on_gfx950(), (\n+            \"mori currently only support arch gfx942 and gfx950\"\n+        )\n+\n+        if not self.internode:\n+            # single node\n+            kernel_type = mori.ops.EpDispatchCombineKernelType.IntraNode\n+            rdma_block_num = 0\n+            warp_num_per_block = 16\n+            block_num = 80\n+        else:\n+            # multi node\n+            kernel_type = mori.ops.EpDispatchCombineKernelType.InterNodeV1\n+            if on_gfx942():\n+                warp_num_per_block = 16\n+                block_num = 32\n+                rdma_block_num = 16\n+            elif on_gfx950():\n+                warp_num_per_block = 8\n+                block_num = 64\n+                rdma_block_num = 32\n+            else:\n+                raise NotImplementedError(\n+                    \"mori currently only support arch gfx942 and gfx950\"\n+                )\n+\n+        return dict(\n+            rank=rank,\n+            world_size=num_ep_ranks,\n+            data_type=quant_dtype,\n+            hidden_dim=token_hidden_size,\n+            scale_dim=scale_dim,\n+            scale_type_size=scale_type_size,\n+            max_token_type_size=input_dtype.itemsize,\n+            max_num_inp_token_per_rank=max_num_tokens_per_dp_rank,\n+            num_experts_per_rank=num_local_experts,\n+            num_experts_per_token=num_experts_per_token,\n+            warp_num_per_block=warp_num_per_block,\n+            block_num=block_num,\n+            kernel_type=kernel_type,\n+            rdma_block_num=rdma_block_num,\n+            gpu_per_node=min(8, num_ep_ranks),\n+        )\n+\n+    def _make_handle(self, **kwargs):\n+        import mori  # type: ignore[import-not-found]\n+\n+        mori_config = mori.ops.EpDispatchCombineConfig(**kwargs)\n+        handle = mori.ops.EpDispatchCombineOp(mori_config)\n+        return handle\n+\n+    def get_handle(self, kwargs):\n+        import mori  # type: ignore[import-not-found]\n+\n+        mori_kwargs = self._make_all2all_kwargs(**kwargs)\n+        logger.debug(\"MoRI all2all args %s\", mori_kwargs)\n+        handle: mori.ops.EpDispatchCombineOp = self.handle_cache.get_or_create(\n+            mori_kwargs, self._make_handle\n+        )\n+        return handle", "patch_sha256": "51f0d3664411f652"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2580997374", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/utils/import_utils.py\nReview Comment:\nMORI-EP does indeed rely on AITER now, but we will add support for other MOE backends in the future. Also, MORI KVConnector https://github.com/vllm-project/vllm/pull/29304 depends on MORI-IO, but does not depend on AITER.\n\nOriginal Code Context:\n@@ -447,3 +447,13 @@ def has_arctic_inference() -> bool:\n     \"\"\"Whether the optional `arctic_inference` package is available.\"\"\"\n \n     return _has_module(\"arctic_inference\")\n+\n+\n+def has_aiter() -> bool:\n+    \"\"\"Whether the optional `aiter` package is available.\"\"\"\n+    return _has_module(\"aiter\")\n+\n+\n+def has_mori() -> bool:\n+    \"\"\"Whether the optional `mori` package is available.\"\"\"\n+    return _has_module(\"mori\")\n", "materials": ["MORI-EP does indeed rely on AITER now, but we will add support for other MOE backends in the future. Also, MORI KVConnector https://github.com/vllm-project/vllm/pull/29304 depends on MORI-IO, but does not depend on AITER."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -451,3 +451,13 @@ def has_helion() -> bool:\n             # use helion...\n     \"\"\"\n     return _has_module(\"helion\")\n+\n+\n+def has_aiter() -> bool:\n+    \"\"\"Whether the optional `aiter` package is available.\"\"\"\n+    return _has_module(\"aiter\")\n+\n+\n+def has_mori() -> bool:\n+    \"\"\"Whether the optional `mori` package is available.\"\"\"\n+    return _has_module(\"mori\")", "patch_sha256": "2de655bc6e085627"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2581034759", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/parallel.py\nReview Comment:\nIn the short term, MORI has no plans to separate HT and LL modes.\n\nOriginal Code Context:\n@@ -128,6 +128,7 @@ class ParallelConfig:\n             \"pplx\",\n             \"deepep_high_throughput\",\n             \"deepep_low_latency\",\n+            \"mori\",\n", "materials": ["In the short term, MORI has no plans to separate HT and LL modes."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -43,6 +43,7 @@\n     \"pplx\",\n     \"deepep_high_throughput\",\n     \"deepep_low_latency\",\n+    \"mori\",\n     \"allgather_reducescatter\",\n     \"flashinfer_all2allv\",\n ]\n@@ -158,6 +159,7 @@ class ParallelConfig:\n     - \"pplx\": Use pplx kernels\\n\n     - \"deepep_high_throughput\": Use deepep high-throughput kernels\\n\n     - \"deepep_low_latency\": Use deepep low-latency kernels\\n\n+    - \"mori\": Use mori kernels\\n\n     - \"flashinfer_all2allv\": Use flashinfer alltoallv kernels for mnnvl\"\"\"\n \n     max_parallel_loading_workers: int | None = None\n@@ -443,6 +445,7 @@ def use_sequence_parallel_moe(self) -> bool:\n                 \"naive\",\n                 \"deepep_high_throughput\",\n                 \"deepep_low_latency\",\n+                \"mori\",\n             )\n             and self.enable_expert_parallel\n             and self.tensor_parallel_size > 1", "patch_sha256": "b48cc4877a07bfe9"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2581135093", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/distributed/device_communicators/all2all.py\nReview Comment:\nThat’s how it should be. However, it's not only related to the GPU architecture, also requires some testing to determine the optimal configuration. The current value is based on commonly accepted best practices. As the next step, we plan to run the necessary tests to determine a more appropriate and logical configuration, and update it accordingly.\n\nOriginal Code Context:\n@@ -488,3 +488,82 @@ def cleanup(self):\n                 self.prepare_workspace_tensor = None\n                 self.mapping = None\n                 self.initialized = False\n+\n+\n+class MoriAll2AllManager(All2AllManagerBase):\n+    def __init__(self, cpu_group):\n+        assert has_mori(), (\n+            \"MoRI kernels not found. Please follow https://github.com/ROCm/mori/blob/main/README.md\"\n+            \" to install MoRI kernels.\"\n+        )  # noqa\n+        import mori\n+\n+        super().__init__(cpu_group)\n+        self.handle_cache = Cache()\n+\n+        torch._C._distributed_c10d._register_process_group(\"mori\", cpu_group)\n+        mori.shmem.shmem_torch_process_group_init(\"mori\")\n+\n+    def _make_all2all_kwargs(\n+        self,\n+        rank: int,\n+        num_ep_ranks: int,\n+        input_dtype: torch.dtype,\n+        quant_dtype: torch.dtype,\n+        token_hidden_size: int,\n+        scale_dim: int,\n+        scale_type_size: int,\n+        max_num_tokens_per_dp_rank: int,\n+        num_local_experts: int,\n+        num_experts_per_token: int,\n+        gpu_per_node: int,\n+    ):\n+        import mori  # type: ignore[import-not-found]\n+\n+        if not self.internode:\n+            # single node\n+            kernel_type = mori.ops.EpDispatchCombineKernelType.IntraNode\n+            warp_num_per_block = 16\n+            block_num = 80\n", "materials": ["That’s how it should be. However, it's not only related to the GPU architecture, also requires some testing to determine the optimal configuration. The current value is based on commonly accepted best practices. As the next step, we plan to run the necessary tests to determine a more appropriate and logical configuration, and update it accordingly."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -10,7 +10,7 @@\n from vllm.forward_context import get_forward_context\n from vllm.logger import init_logger\n from vllm.utils.flashinfer import has_flashinfer_all2all\n-from vllm.utils.import_utils import has_deep_ep, has_pplx\n+from vllm.utils.import_utils import has_deep_ep, has_mori, has_pplx\n \n from .base_device_communicator import All2AllManagerBase, Cache\n \n@@ -507,3 +507,96 @@ def cleanup(self):\n                 self.prepare_workspace_tensor = None\n                 self.mapping = None\n                 self.initialized = False\n+\n+\n+class MoriAll2AllManager(All2AllManagerBase):\n+    def __init__(self, cpu_group):\n+        assert has_mori(), (\n+            \"MoRI kernels not found. Please follow https://github.com/ROCm/mori/blob/main/README.md\"\n+            \" to install MoRI kernels.\"\n+        )  # noqa\n+        import mori\n+\n+        super().__init__(cpu_group)\n+        self.handle_cache = Cache()\n+\n+        torch._C._distributed_c10d._register_process_group(\"mori\", cpu_group)\n+        mori.shmem.shmem_torch_process_group_init(\"mori\")\n+\n+    def _make_all2all_kwargs(\n+        self,\n+        rank: int,\n+        num_ep_ranks: int,\n+        input_dtype: torch.dtype,\n+        quant_dtype: torch.dtype,\n+        token_hidden_size: int,\n+        scale_dim: int,\n+        scale_type_size: int,\n+        max_num_tokens_per_dp_rank: int,\n+        num_local_experts: int,\n+        num_experts_per_token: int,\n+    ):\n+        import mori  # type: ignore[import-not-found]\n+\n+        from vllm.platforms.rocm import on_gfx942, on_gfx950\n+\n+        assert on_gfx942() or on_gfx950(), (\n+            \"mori currently only support arch gfx942 and gfx950\"\n+        )\n+\n+        if not self.internode:\n+            # single node\n+            kernel_type = mori.ops.EpDispatchCombineKernelType.IntraNode\n+            rdma_block_num = 0\n+            warp_num_per_block = 16\n+            block_num = 80\n+        else:\n+            # multi node\n+            kernel_type = mori.ops.EpDispatchCombineKernelType.InterNodeV1\n+            if on_gfx942():\n+                warp_num_per_block = 16\n+                block_num = 32\n+                rdma_block_num = 16\n+            elif on_gfx950():\n+                warp_num_per_block = 8\n+                block_num = 64\n+                rdma_block_num = 32\n+            else:\n+                raise NotImplementedError(\n+                    \"mori currently only support arch gfx942 and gfx950\"\n+                )\n+\n+        return dict(\n+            rank=rank,\n+            world_size=num_ep_ranks,\n+            data_type=quant_dtype,\n+            hidden_dim=token_hidden_size,\n+            scale_dim=scale_dim,\n+            scale_type_size=scale_type_size,\n+            max_token_type_size=input_dtype.itemsize,\n+            max_num_inp_token_per_rank=max_num_tokens_per_dp_rank,\n+            num_experts_per_rank=num_local_experts,\n+            num_experts_per_token=num_experts_per_token,\n+            warp_num_per_block=warp_num_per_block,\n+            block_num=block_num,\n+            kernel_type=kernel_type,\n+            rdma_block_num=rdma_block_num,\n+            gpu_per_node=min(8, num_ep_ranks),\n+        )\n+\n+    def _make_handle(self, **kwargs):\n+        import mori  # type: ignore[import-not-found]\n+\n+        mori_config = mori.ops.EpDispatchCombineConfig(**kwargs)\n+        handle = mori.ops.EpDispatchCombineOp(mori_config)\n+        return handle\n+\n+    def get_handle(self, kwargs):\n+        import mori  # type: ignore[import-not-found]\n+\n+        mori_kwargs = self._make_all2all_kwargs(**kwargs)\n+        logger.debug(\"MoRI all2all args %s\", mori_kwargs)\n+        handle: mori.ops.EpDispatchCombineOp = self.handle_cache.get_or_create(\n+            mori_kwargs, self._make_handle\n+        )\n+        return handle", "patch_sha256": "51f0d3664411f652"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2707973313", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/_aiter_ops.py\nReview Comment:\n### Fake function ignores `output_dtype` parameter for torch.compile\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `_rocm_aiter_fused_moe_fake` function was updated to accept the new `output_dtype` parameter, but the return statement still uses `torch.empty_like(hidden_states)`, which ignores `output_dtype`. The real implementation passes `dtype=output_dtype` to aiter's fused_moe (line 102). When `output_dtype` differs from `hidden_states.dtype`, torch.compile will infer the wrong output dtype, potentially causing compilation failures or runtime errors when the MoRI backend is used.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/_aiter_ops.py#L119-L123\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..UV4UPznOxauEmQvvl6V1EB0AHFNDSReN4UQk43yzQ6Ghv6Z1tQ42a_25sNjtqt70w7eaGWjXFFlqc76DgMubVuVpsbvZK00NnNaJ-P-_qHIZ-1biacR71vbQSqVDmmfdni6eyj1yKE1uUszbfe6mXAj779tKqAn68fkU9TKXTXSIua1-cQ_MrVlYuIQ3g_P9tOtJWMBrkfcI88dKaSIeXMOShLU7pcOyosJ1vBenEmPZNpaoBH4F5Wt6B_-Zj-IhbbMPpYh8oN7Wu28LnopTERzR-AeVRv3ZTIwNF_UR2MDEiTW09uLkL6aNANMSGVLwW625zlt-mPuzyCTI7zLoNA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..j4DcaUQ2fmqHnOXllckUAcxY0hcxX4UqtZQYKmGaX8CmDSJQl1QNTgapLxZestjz_MF0f1cGIb7207CIDUVViU6PQ4WjGBHPKzHONmygrXi5SJfBExwI3mC9P0r8k3_VxJxqt_xEwTyH1wbqLxoz_6IJHCIa8BV6fjnf32dxIODRn09GmKWbVcpN3otziVncfFmz5TaKaPsCzrK20SHRpX1M8ApSk25wmoyVuCi3xLttDpz5GiKxynX3Td4T5muGGAlwHmR5U5-wzAtGLPFt6PnZ_7o1DNWMOelXDN0_p70LFHQ7kTaJU7lIv1Gy549m-QB3xFIOK_Ud1uShrohfng\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -113,6 +117,8 @@ def _rocm_aiter_fused_moe_fake(\n     w2_scale: torch.Tensor | None = None,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    num_local_tokens: torch.Tensor | None = None,\n+    output_dtype: torch.dtype | None = None,\n", "materials": ["### Fake function ignores `output_dtype` parameter for torch.compile\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe `_rocm_aiter_fused_moe_fake` function was updated to accept the new `output_dtype` parameter, but the return statement still uses `torch.empty_like(hidden_states)`, which ignores `output_dtype`. The real implementation passes `dtype=output_dtype` to aiter's fused_moe (line 102). When `output_dtype` differs from `hidden_states.dtype`, torch.compile will infer the wrong output dtype, potentially causing compilation failures or runtime errors when the MoRI backend is used.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/_aiter_ops.py#L119-L123\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..UV4UPznOxauEmQvvl6V1EB0AHFNDSReN4UQk43yzQ6Ghv6Z1tQ42a_25sNjtqt70w7eaGWjXFFlqc76DgMubVuVpsbvZK00NnNaJ-P-_qHIZ-1biacR71vbQSqVDmmfdni6eyj1yKE1uUszbfe6mXAj779tKqAn68fkU9TKXTXSIua1-cQ_MrVlYuIQ3g_P9tOtJWMBrkfcI88dKaSIeXMOShLU7pcOyosJ1vBenEmPZNpaoBH4F5Wt6B_-Zj-IhbbMPpYh8oN7Wu28LnopTERzR-AeVRv3ZTIwNF_UR2MDEiTW09uLkL6aNANMSGVLwW625zlt-mPuzyCTI7zLoNA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..j4DcaUQ2fmqHnOXllckUAcxY0hcxX4UqtZQYKmGaX8CmDSJQl1QNTgapLxZestjz_MF0f1cGIb7207CIDUVViU6PQ4WjGBHPKzHONmygrXi5SJfBExwI3mC9P0r8k3_VxJxqt_xEwTyH1wbqLxoz_6IJHCIa8BV6fjnf32dxIODRn09GmKWbVcpN3otziVncfFmz5TaKaPsCzrK20SHRpX1M8ApSk25wmoyVuCi3xLttDpz5GiKxynX3Td4T5muGGAlwHmR5U5-wzAtGLPFt6PnZ_7o1DNWMOelXDN0_p70LFHQ7kTaJU7lIv1Gy549m-QB3xFIOK_Ud1uShrohfng\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -79,6 +79,8 @@ def _rocm_aiter_fused_moe_impl(\n     w2_scale: torch.Tensor | None = None,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    num_local_tokens: torch.Tensor | None = None,\n+    output_dtype: torch.dtype | None = None,\n ) -> torch.Tensor:\n     from aiter import ActivationType, QuantType\n     from aiter.fused_moe import fused_moe\n@@ -100,6 +102,8 @@ def _rocm_aiter_fused_moe_impl(\n         w2_scale,\n         a1_scale,\n         a2_scale,\n+        num_local_tokens=num_local_tokens,\n+        dtype=output_dtype,\n     )\n \n \n@@ -117,7 +121,11 @@ def _rocm_aiter_fused_moe_fake(\n     w2_scale: torch.Tensor | None = None,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    num_local_tokens: torch.Tensor | None = None,\n+    output_dtype: torch.dtype | None = None,\n ) -> torch.Tensor:\n+    if output_dtype is not None:\n+        return torch.empty_like(hidden_states, dtype=output_dtype)\n     return torch.empty_like(hidden_states)\n \n \n@@ -1236,6 +1244,8 @@ def fused_moe(\n         w2_scale: torch.Tensor | None = None,\n         a1_scale: torch.Tensor | None = None,\n         a2_scale: torch.Tensor | None = None,\n+        num_local_tokens: torch.Tensor | None = None,\n+        output_dtype: torch.dtype | None = None,\n     ) -> torch.Tensor:\n         return torch.ops.vllm.rocm_aiter_fused_moe(\n             hidden_states,\n@@ -1251,6 +1261,8 @@ def fused_moe(\n             w2_scale,\n             a1_scale,\n             a2_scale,\n+            num_local_tokens,\n+            output_dtype,\n         )\n \n     @staticmethod", "patch_sha256": "18337aae584aa754"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2711258748", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/_aiter_ops.py\nReview Comment:\ngood point! fixed\n\nOriginal Code Context:\n@@ -113,6 +117,8 @@ def _rocm_aiter_fused_moe_fake(\n     w2_scale: torch.Tensor | None = None,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    num_local_tokens: torch.Tensor | None = None,\n+    output_dtype: torch.dtype | None = None,\n", "materials": ["good point! fixed"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -79,6 +79,8 @@ def _rocm_aiter_fused_moe_impl(\n     w2_scale: torch.Tensor | None = None,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    num_local_tokens: torch.Tensor | None = None,\n+    output_dtype: torch.dtype | None = None,\n ) -> torch.Tensor:\n     from aiter import ActivationType, QuantType\n     from aiter.fused_moe import fused_moe\n@@ -100,6 +102,8 @@ def _rocm_aiter_fused_moe_impl(\n         w2_scale,\n         a1_scale,\n         a2_scale,\n+        num_local_tokens=num_local_tokens,\n+        dtype=output_dtype,\n     )\n \n \n@@ -117,7 +121,11 @@ def _rocm_aiter_fused_moe_fake(\n     w2_scale: torch.Tensor | None = None,\n     a1_scale: torch.Tensor | None = None,\n     a2_scale: torch.Tensor | None = None,\n+    num_local_tokens: torch.Tensor | None = None,\n+    output_dtype: torch.dtype | None = None,\n ) -> torch.Tensor:\n+    if output_dtype is not None:\n+        return torch.empty_like(hidden_states, dtype=output_dtype)\n     return torch.empty_like(hidden_states)\n \n \n@@ -1236,6 +1244,8 @@ def fused_moe(\n         w2_scale: torch.Tensor | None = None,\n         a1_scale: torch.Tensor | None = None,\n         a2_scale: torch.Tensor | None = None,\n+        num_local_tokens: torch.Tensor | None = None,\n+        output_dtype: torch.dtype | None = None,\n     ) -> torch.Tensor:\n         return torch.ops.vllm.rocm_aiter_fused_moe(\n             hidden_states,\n@@ -1251,6 +1261,8 @@ def fused_moe(\n             w2_scale,\n             a1_scale,\n             a2_scale,\n+            num_local_tokens,\n+            output_dtype,\n         )\n \n     @staticmethod", "patch_sha256": "18337aae584aa754"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2711262046", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/distributed/device_communicators/all2all.py\nReview Comment:\nNow we have different configs for mi300x and mi350\n\nOriginal Code Context:\n@@ -488,3 +488,82 @@ def cleanup(self):\n                 self.prepare_workspace_tensor = None\n                 self.mapping = None\n                 self.initialized = False\n+\n+\n+class MoriAll2AllManager(All2AllManagerBase):\n+    def __init__(self, cpu_group):\n+        assert has_mori(), (\n+            \"MoRI kernels not found. Please follow https://github.com/ROCm/mori/blob/main/README.md\"\n+            \" to install MoRI kernels.\"\n+        )  # noqa\n+        import mori\n+\n+        super().__init__(cpu_group)\n+        self.handle_cache = Cache()\n+\n+        torch._C._distributed_c10d._register_process_group(\"mori\", cpu_group)\n+        mori.shmem.shmem_torch_process_group_init(\"mori\")\n+\n+    def _make_all2all_kwargs(\n+        self,\n+        rank: int,\n+        num_ep_ranks: int,\n+        input_dtype: torch.dtype,\n+        quant_dtype: torch.dtype,\n+        token_hidden_size: int,\n+        scale_dim: int,\n+        scale_type_size: int,\n+        max_num_tokens_per_dp_rank: int,\n+        num_local_experts: int,\n+        num_experts_per_token: int,\n+        gpu_per_node: int,\n+    ):\n+        import mori  # type: ignore[import-not-found]\n+\n+        if not self.internode:\n+            # single node\n+            kernel_type = mori.ops.EpDispatchCombineKernelType.IntraNode\n+            warp_num_per_block = 16\n+            block_num = 80\n", "materials": ["Now we have different configs for mi300x and mi350"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -10,7 +10,7 @@\n from vllm.forward_context import get_forward_context\n from vllm.logger import init_logger\n from vllm.utils.flashinfer import has_flashinfer_all2all\n-from vllm.utils.import_utils import has_deep_ep, has_pplx\n+from vllm.utils.import_utils import has_deep_ep, has_mori, has_pplx\n \n from .base_device_communicator import All2AllManagerBase, Cache\n \n@@ -507,3 +507,96 @@ def cleanup(self):\n                 self.prepare_workspace_tensor = None\n                 self.mapping = None\n                 self.initialized = False\n+\n+\n+class MoriAll2AllManager(All2AllManagerBase):\n+    def __init__(self, cpu_group):\n+        assert has_mori(), (\n+            \"MoRI kernels not found. Please follow https://github.com/ROCm/mori/blob/main/README.md\"\n+            \" to install MoRI kernels.\"\n+        )  # noqa\n+        import mori\n+\n+        super().__init__(cpu_group)\n+        self.handle_cache = Cache()\n+\n+        torch._C._distributed_c10d._register_process_group(\"mori\", cpu_group)\n+        mori.shmem.shmem_torch_process_group_init(\"mori\")\n+\n+    def _make_all2all_kwargs(\n+        self,\n+        rank: int,\n+        num_ep_ranks: int,\n+        input_dtype: torch.dtype,\n+        quant_dtype: torch.dtype,\n+        token_hidden_size: int,\n+        scale_dim: int,\n+        scale_type_size: int,\n+        max_num_tokens_per_dp_rank: int,\n+        num_local_experts: int,\n+        num_experts_per_token: int,\n+    ):\n+        import mori  # type: ignore[import-not-found]\n+\n+        from vllm.platforms.rocm import on_gfx942, on_gfx950\n+\n+        assert on_gfx942() or on_gfx950(), (\n+            \"mori currently only support arch gfx942 and gfx950\"\n+        )\n+\n+        if not self.internode:\n+            # single node\n+            kernel_type = mori.ops.EpDispatchCombineKernelType.IntraNode\n+            rdma_block_num = 0\n+            warp_num_per_block = 16\n+            block_num = 80\n+        else:\n+            # multi node\n+            kernel_type = mori.ops.EpDispatchCombineKernelType.InterNodeV1\n+            if on_gfx942():\n+                warp_num_per_block = 16\n+                block_num = 32\n+                rdma_block_num = 16\n+            elif on_gfx950():\n+                warp_num_per_block = 8\n+                block_num = 64\n+                rdma_block_num = 32\n+            else:\n+                raise NotImplementedError(\n+                    \"mori currently only support arch gfx942 and gfx950\"\n+                )\n+\n+        return dict(\n+            rank=rank,\n+            world_size=num_ep_ranks,\n+            data_type=quant_dtype,\n+            hidden_dim=token_hidden_size,\n+            scale_dim=scale_dim,\n+            scale_type_size=scale_type_size,\n+            max_token_type_size=input_dtype.itemsize,\n+            max_num_inp_token_per_rank=max_num_tokens_per_dp_rank,\n+            num_experts_per_rank=num_local_experts,\n+            num_experts_per_token=num_experts_per_token,\n+            warp_num_per_block=warp_num_per_block,\n+            block_num=block_num,\n+            kernel_type=kernel_type,\n+            rdma_block_num=rdma_block_num,\n+            gpu_per_node=min(8, num_ep_ranks),\n+        )\n+\n+    def _make_handle(self, **kwargs):\n+        import mori  # type: ignore[import-not-found]\n+\n+        mori_config = mori.ops.EpDispatchCombineConfig(**kwargs)\n+        handle = mori.ops.EpDispatchCombineOp(mori_config)\n+        return handle\n+\n+    def get_handle(self, kwargs):\n+        import mori  # type: ignore[import-not-found]\n+\n+        mori_kwargs = self._make_all2all_kwargs(**kwargs)\n+        logger.debug(\"MoRI all2all args %s\", mori_kwargs)\n+        handle: mori.ops.EpDispatchCombineOp = self.handle_cache.get_or_create(\n+            mori_kwargs, self._make_handle\n+        )\n+        return handle", "patch_sha256": "51f0d3664411f652"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28672/review_2599719051", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28672", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/vllm.py\nReview Comment:\n![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThe logic to calculate `min_token_num` here is duplicated from `vllm/compilation/sequence_parallelism.py` in `SequenceParallelismPass.__init__`. This duplication can lead to maintenance issues, where a change in one location might be missed in the other.\n\nTo improve maintainability, I suggest centralizing this calculation. You could compute the threshold once within `VllmConfig` (e.g., in a new helper method called from `__post_init__`) and store the result in `compilation_config.pass_config`. Both this method (`_set_compile_ranges`) and `SequenceParallelismPass` could then consume the pre-computed value.\n\nThis would remove the redundant code and ensure that the threshold calculation is consistent across the codebase.\n\nOriginal Code Context:\n@@ -1160,6 +1160,37 @@ def _set_compile_ranges(self):\n                         \"allreduce-rms fusion will be enabled for all num_tokens.\"\n                     )\n \n+        # Add the compile ranges for sequence parallelism\n+        if compilation_config.pass_config.enable_sp:\n+            pass_config = compilation_config.pass_config\n+\n+            # Check if user provided explicit token override\n+            # User override works regardless of hidden_size\n+            if pass_config.sequence_parallelism_min_token_num is not None:\n+                min_token_num = pass_config.sequence_parallelism_min_token_num\n+            else:\n+                # Otherwise calculate using helper function with branching logic\n+                from vllm.compilation.sequence_parallelism import (\n+                    get_sequence_parallelism_threshold,\n+                )\n+\n+                tp_size = self.parallel_config.tensor_parallel_size\n+                hidden_size = self.model_config.get_hidden_size()\n+                element_size = self.model_config.dtype.itemsize\n+                min_token_num = get_sequence_parallelism_threshold(\n+                    hidden_size, tp_size, element_size\n+                )\n", "materials": ["![high](https://www.gstatic.com/codereviewagent/high-priority.svg)\n\nThe logic to calculate `min_token_num` here is duplicated from `vllm/compilation/sequence_parallelism.py` in `SequenceParallelismPass.__init__`. This duplication can lead to maintenance issues, where a change in one location might be missed in the other.\n\nTo improve maintainability, I suggest centralizing this calculation. You could compute the threshold once within `VllmConfig` (e.g., in a new helper method called from `__post_init__`) and store the result in `compilation_config.pass_config`. Both this method (`_set_compile_ranges`) and `SequenceParallelismPass` could then consume the pre-computed value.\n\nThis would remove the redundant code and ensure that the threshold calculation is consistent across the codebase."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -853,8 +853,33 @@ def has_blocked_weights():\n                 logger.warning(\"Sequence Parallelism requires TP>1, disabling\")\n                 self.compilation_config.pass_config.enable_sp = False\n                 self.compilation_config.pass_config.fuse_gemm_comms = False\n+            else:\n+                # Compute SP threshold early; disable if None (model too\n+                # small) before +rms_norm gets forced into custom_ops.\n+                pass_config = self.compilation_config.pass_config\n+                if pass_config.sp_min_token_num is None:\n+                    from vllm.compilation.passes.fusion.sequence_parallelism import (\n+                        get_sequence_parallelism_threshold,\n+                    )\n+\n+                    tp_size = self.parallel_config.tensor_parallel_size\n+                    hidden_size = self.model_config.get_hidden_size()\n+                    element_size = self.model_config.dtype.itemsize\n+                    pass_config.sp_min_token_num = get_sequence_parallelism_threshold(\n+                        hidden_size, tp_size, element_size\n+                    )\n \n-            elif \"-rms_norm\" in self.compilation_config.custom_ops:\n+                if pass_config.sp_min_token_num is None:\n+                    logger.warning(\n+                        \"Model hidden_size too small for the SP \"\n+                        \"threshold heuristic, disabling. To force SP, \"\n+                        \"set pass_config.sp_min_token_num manually.\"\n+                    )\n+                    self.compilation_config.pass_config.enable_sp = False\n+                    self.compilation_config.pass_config.fuse_gemm_comms = False\n+\n+        if self.compilation_config.pass_config.enable_sp:\n+            if \"-rms_norm\" in self.compilation_config.custom_ops:\n                 logger.warning(\n                     \"RMS norm force disabled, sequence parallelism might break\"\n                 )\n@@ -1456,6 +1481,36 @@ def _set_compile_ranges(self):\n                         \"allreduce-rms fusion will be enabled for all num_tokens.\"\n                     )\n \n+        # Add the compile ranges for sequence parallelism\n+        if compilation_config.pass_config.enable_sp:\n+            pass_config = compilation_config.pass_config\n+\n+            # Calculate min_token_num if not explicitly provided\n+            # User override works regardless of hidden_size\n+            if pass_config.sp_min_token_num is None:\n+                from vllm.compilation.passes.fusion.sequence_parallelism import (\n+                    get_sequence_parallelism_threshold,\n+                )\n+\n+                tp_size = self.parallel_config.tensor_parallel_size\n+                hidden_size = self.model_config.get_hidden_size()\n+                element_size = self.model_config.dtype.itemsize\n+                pass_config.sp_min_token_num = get_sequence_parallelism_threshold(\n+                    hidden_size, tp_size, element_size\n+                )\n+\n+            min_token_num = pass_config.sp_min_token_num\n+            max_num_batched_tokens = self.scheduler_config.max_num_batched_tokens\n+            if min_token_num is not None and (\n+                max_num_batched_tokens is not None\n+                and min_token_num < max_num_batched_tokens\n+                and min_token_num > 1\n+            ):\n+                # Add split point at min_token_num - 1 to ensure SP applies\n+                # starting from min_token_num\n+                # This creates ranges: [1, min-1] (no SP), [min, max] (SP applies)\n+                computed_compile_ranges_split_points.append(min_token_num - 1)\n+\n         if compilation_config.pass_config.fuse_rope_kvcache:\n             max_token_num = (\n                 compilation_config.pass_config.rope_kvcache_fusion_max_token_num", "patch_sha256": "53f59b2b702d5fdf"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28672/review_2621125413", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28672", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/compilation/passes/fusion/sequence_parallelism.py\nReview Comment:\nLet's add tests for this function\n\nOriginal Code Context:\n@@ -25,6 +25,62 @@\n \n logger = init_logger(__name__)\n \n+# Min hidden size per device capability for sequence parallelism\n+# Only apply sequence parallelism for models with hidden_size >= threshold\n+SP_MIN_HIDDEN_SIZE: dict[int, int] = {\n+    90: 8192,  # H100: only for models with hidden_size >= 8192\n+    100: 8192,  # Blackwell: only for models with hidden_size >= 8192\n+}\n+\n+# Min size per GPU per device capability for sequence parallelism\n+# Total min size = min_per_gpu_size * tp_size\n+# This ensures the threshold scales appropriately with tensor parallelism\n+SP_MIN_PER_GPU_SIZE_MB: dict[int, float] = {\n+    90: 8,  # 8MB per GPU for H100\n+    100: 64,  # 64MB per GPU for Blackwell\n+}\n+\n+\n+def get_sequence_parallelism_threshold(\n", "materials": ["Let's add tests for this function"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -27,6 +27,63 @@\n \n logger = init_logger(__name__)\n \n+# Min hidden size per device capability for sequence parallelism\n+# Only apply sequence parallelism for models with hidden_size >= threshold\n+SP_MIN_HIDDEN_SIZE: dict[int, int] = {\n+    90: 8192,  # H100: only for models with hidden_size >= 8192\n+}\n+\n+# Min size per GPU per device capability for sequence parallelism\n+# Total min size = min_per_gpu_size * tp_size\n+# This ensures the threshold scales appropriately with tensor parallelism\n+SP_MIN_PER_GPU_SIZE_MB: dict[int, float] = {\n+    90: 8,  # 8MB per GPU for H100\n+}\n+\n+\n+def get_sequence_parallelism_threshold(\n+    hidden_size: int,\n+    tp_size: int,\n+    element_size: int,\n+) -> int | None:\n+    \"\"\"\n+    Calculate the minimum token threshold for applying sequence parallelism.\n+\n+    Returns None if sequence parallelism should not be applied based on model size.\n+\n+    Branching logic based on device capability:\n+    - Check if hidden_size >= SP_MIN_HIDDEN_SIZE[device_capability]\n+    - If not, returns None (SP disabled for small models on this device)\n+    - If yes, calculates threshold based on per-GPU size\n+\n+    Formula: min_token_num = (min_per_gpu_size_mb * tp_size * MiB) //\n+             (hidden_size * element_size)\n+    \"\"\"\n+    from vllm.platforms import current_platform\n+\n+    if not current_platform.is_cuda():\n+        return None\n+\n+    capability = current_platform.get_device_capability()\n+    if capability is None:\n+        return None\n+    device_capability = capability.to_int()\n+\n+    # Check if device has configured thresholds\n+    min_hidden_size = SP_MIN_HIDDEN_SIZE.get(device_capability)\n+    min_per_gpu_size_mb = SP_MIN_PER_GPU_SIZE_MB.get(device_capability)\n+\n+    if min_hidden_size is None or min_per_gpu_size_mb is None:\n+        return None\n+\n+    # Only apply sequence parallelism for models meeting the size threshold\n+    if hidden_size < min_hidden_size:\n+        return None\n+\n+    MiB = 1024 * 1024\n+    min_size = min_per_gpu_size_mb * MiB * tp_size\n+    return int(min_size // (hidden_size * element_size))\n+\n \n def get_first_out_wrapper(\n     fn: Callable[..., Sequence[torch.Tensor]],\n@@ -309,6 +366,23 @@ class SequenceParallelismPass(VllmPatternMatcherPass):\n     def __init__(self, config: VllmConfig) -> None:\n         super().__init__(config)\n \n+        # Get min_token_num threshold\n+        # Read min_token_num from config (calculated during config init)\n+        self.min_token_num = None\n+        if config.model_config is not None:\n+            pass_config = config.compilation_config.pass_config\n+            self.min_token_num = pass_config.sp_min_token_num\n+\n+            if self.min_token_num is not None:\n+                # Take the min to avoid exceeding max_num_batched_tokens\n+                max_batched = config.scheduler_config.max_num_batched_tokens\n+                if max_batched is not None:\n+                    self.min_token_num = min(self.min_token_num, max_batched)\n+                logger.debug_once(\n+                    f\"Sequence parallelism min token threshold: {self.min_token_num}\",\n+                    scope=\"global\",\n+                )\n+\n         # Used to clean up redundant views created temporarily\n         # to circumvent residual shape change issues\n         self.noop_cleanup = NoOpEliminationPass(config)\n@@ -339,29 +413,36 @@ def __init__(self, config: VllmConfig) -> None:\n         self.dump_patterns(config, self.patterns)\n \n     def is_applicable_for_range(self, compile_range: Range) -> bool:\n-        # When sequence parallelism is enabled, the residual tensor from RMSNorm\n-        # needs to be split along the sequence dimension. However, this dimension\n-        # is symbolic during piecewise compilation, and splitting symbolic shapes\n-        # is not supported.\n-        #\n-        # This pass is therefore only applied when the sequence dimension is\n-        # concrete:\n-        # 1. In full-graph compilation mode (no Dynamo splitting ops are used).\n-        #   For this case we always pad num_tokens to be a multiple of\n-        #   tensor_parallel_size, so there's no need to check shape % tp_size == 0.\n-        # 2. For specific shape provided during compilation (e.g., from\n-        #    `compile_sizes`), which must be divisible by the tensor-parallel\n-        #    size.\n+        \"\"\"\n+        Determines if sequence parallelism should be applied for the given\n+        compile range.\n+\n+        SP is only beneficial for larger batch sizes where the communication\n+        overhead is amortized. For small batches, the overhead of splitting\n+        and gathering tensors across TP ranks outweighs the benefits.\n+\n+        Returns False (SP disabled) when:\n+        - Using piecewise compilation with non-concrete or TP-indivisible sizes\n+        - min_token_num is None (SP disabled for this device/config)\n+        - The compile range starts below the minimum token threshold\n+        \"\"\"\n+        # For piecewise compilation (not using inductor graph partition),\n+        # we need concrete sizes that are divisible by TP for correct splitting\n         if (\n-            not self.compilation_config.splitting_ops\n-            or self.compilation_config.use_inductor_graph_partition\n+            not self.compilation_config.use_inductor_graph_partition\n+            and self.compilation_config.splitting_ops\n         ):\n-            return True\n-        tp_size = get_tensor_model_parallel_world_size()\n-        result: bool = (compile_range.is_single_size()) and (\n-            compile_range.end % tp_size == 0\n-        )\n-        return result\n+            tp_size = get_tensor_model_parallel_world_size()\n+            if not compile_range.is_single_size() or compile_range.end % tp_size != 0:\n+                return False\n+\n+        # min_token_num is None when SP is disabled for this device/config\n+        # (e.g., non-CUDA platform, unsupported GPU, or small hidden_size)\n+        if self.min_token_num is None:\n+            return False\n+\n+        # Only apply SP when batch size meets the minimum threshold\n+        return compile_range.start >= self.min_token_num\n \n     @VllmInductorPass.time_and_log\n     def __call__(self, graph: fx.Graph) -> None:", "patch_sha256": "a34bba469d383a59"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28672/review_2751297270", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28672", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/vllm.py\nReview Comment:\nSave this number back to pass config? \n\nOriginal Code Context:\n@@ -1265,6 +1265,38 @@ def _set_compile_ranges(self):\n                         \"allreduce-rms fusion will be enabled for all num_tokens.\"\n                     )\n \n+        # Add the compile ranges for sequence parallelism\n+        if compilation_config.pass_config.enable_sp:\n+            pass_config = compilation_config.pass_config\n+\n+            # Check if user provided explicit token override\n+            # User override works regardless of hidden_size\n+            if pass_config.sequence_parallelism_min_token_num is not None:\n+                min_token_num = pass_config.sequence_parallelism_min_token_num\n+            else:\n+                # Otherwise calculate using helper function with branching logic\n+                from vllm.compilation.sequence_parallelism import (\n+                    get_sequence_parallelism_threshold,\n+                )\n+\n+                tp_size = self.parallel_config.tensor_parallel_size\n+                hidden_size = self.model_config.get_hidden_size()\n+                element_size = self.model_config.dtype.itemsize\n+                min_token_num = get_sequence_parallelism_threshold(\n", "materials": ["Save this number back to pass config? "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -853,8 +853,33 @@ def has_blocked_weights():\n                 logger.warning(\"Sequence Parallelism requires TP>1, disabling\")\n                 self.compilation_config.pass_config.enable_sp = False\n                 self.compilation_config.pass_config.fuse_gemm_comms = False\n+            else:\n+                # Compute SP threshold early; disable if None (model too\n+                # small) before +rms_norm gets forced into custom_ops.\n+                pass_config = self.compilation_config.pass_config\n+                if pass_config.sp_min_token_num is None:\n+                    from vllm.compilation.passes.fusion.sequence_parallelism import (\n+                        get_sequence_parallelism_threshold,\n+                    )\n+\n+                    tp_size = self.parallel_config.tensor_parallel_size\n+                    hidden_size = self.model_config.get_hidden_size()\n+                    element_size = self.model_config.dtype.itemsize\n+                    pass_config.sp_min_token_num = get_sequence_parallelism_threshold(\n+                        hidden_size, tp_size, element_size\n+                    )\n \n-            elif \"-rms_norm\" in self.compilation_config.custom_ops:\n+                if pass_config.sp_min_token_num is None:\n+                    logger.warning(\n+                        \"Model hidden_size too small for the SP \"\n+                        \"threshold heuristic, disabling. To force SP, \"\n+                        \"set pass_config.sp_min_token_num manually.\"\n+                    )\n+                    self.compilation_config.pass_config.enable_sp = False\n+                    self.compilation_config.pass_config.fuse_gemm_comms = False\n+\n+        if self.compilation_config.pass_config.enable_sp:\n+            if \"-rms_norm\" in self.compilation_config.custom_ops:\n                 logger.warning(\n                     \"RMS norm force disabled, sequence parallelism might break\"\n                 )\n@@ -1456,6 +1481,36 @@ def _set_compile_ranges(self):\n                         \"allreduce-rms fusion will be enabled for all num_tokens.\"\n                     )\n \n+        # Add the compile ranges for sequence parallelism\n+        if compilation_config.pass_config.enable_sp:\n+            pass_config = compilation_config.pass_config\n+\n+            # Calculate min_token_num if not explicitly provided\n+            # User override works regardless of hidden_size\n+            if pass_config.sp_min_token_num is None:\n+                from vllm.compilation.passes.fusion.sequence_parallelism import (\n+                    get_sequence_parallelism_threshold,\n+                )\n+\n+                tp_size = self.parallel_config.tensor_parallel_size\n+                hidden_size = self.model_config.get_hidden_size()\n+                element_size = self.model_config.dtype.itemsize\n+                pass_config.sp_min_token_num = get_sequence_parallelism_threshold(\n+                    hidden_size, tp_size, element_size\n+                )\n+\n+            min_token_num = pass_config.sp_min_token_num\n+            max_num_batched_tokens = self.scheduler_config.max_num_batched_tokens\n+            if min_token_num is not None and (\n+                max_num_batched_tokens is not None\n+                and min_token_num < max_num_batched_tokens\n+                and min_token_num > 1\n+            ):\n+                # Add split point at min_token_num - 1 to ensure SP applies\n+                # starting from min_token_num\n+                # This creates ranges: [1, min-1] (no SP), [min, max] (SP applies)\n+                computed_compile_ranges_split_points.append(min_token_num - 1)\n+\n         if compilation_config.pass_config.fuse_rope_kvcache:\n             max_token_num = (\n                 compilation_config.pass_config.rope_kvcache_fusion_max_token_num", "patch_sha256": "53f59b2b702d5fdf"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28672/review_2751301575", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28672", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/compilation.py\nReview Comment:\nShoeten to `sp_...`? \n\nOriginal Code Context:\n@@ -144,6 +144,11 @@ class PassConfig:\n                 8: 1,  # 1MB\n             },\n         }, where key is the device capability\"\"\"\n+    sequence_parallelism_min_token_num: int | None = None\n", "materials": ["Shoeten to `sp_...`? "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -118,7 +118,9 @@ class PassConfig:\n     eliminate_noops: bool = Field(default=True)\n     \"\"\"Eliminate no-op ops.\"\"\"\n     enable_sp: bool = Field(default=None)\n-    \"\"\"Enable sequence parallelism.\"\"\"\n+    \"\"\"Enable sequence parallelism. Requires TP>1. Automatically disabled\n+    if the model's hidden_size is too small for SP to be beneficial\n+    (threshold is device-capability dependent).\"\"\"\n     fuse_gemm_comms: bool = Field(default=None)\n     \"\"\"Enable async TP.\"\"\"\n     fuse_allreduce_rms: bool = Field(default=None)\n@@ -155,6 +157,11 @@ class PassConfig:\n                 8: 1,  # 1MB\n             },\n         }, where key is the device capability\"\"\"\n+    sp_min_token_num: int | None = None\n+    \"\"\"The minimum number of tokens above which vllm should use\n+    sequence parallelism. Specified as an integer token count.\n+    Unspecified will fallback to default values which are compute\n+    capability and world size dependent.\"\"\"\n \n     # TODO(luka) better pass enabling system.\n ", "patch_sha256": "add4b4ab342c329b"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28672/review_2751302815", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28672", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/vllm.py\nReview Comment:\nThis will be resolved by saving this value back to config. But we could also extract this logic into a helper function that lives on the passconfig\n\nOriginal Code Context:\n@@ -1160,6 +1160,37 @@ def _set_compile_ranges(self):\n                         \"allreduce-rms fusion will be enabled for all num_tokens.\"\n                     )\n \n+        # Add the compile ranges for sequence parallelism\n+        if compilation_config.pass_config.enable_sp:\n+            pass_config = compilation_config.pass_config\n+\n+            # Check if user provided explicit token override\n+            # User override works regardless of hidden_size\n+            if pass_config.sequence_parallelism_min_token_num is not None:\n+                min_token_num = pass_config.sequence_parallelism_min_token_num\n+            else:\n+                # Otherwise calculate using helper function with branching logic\n+                from vllm.compilation.sequence_parallelism import (\n+                    get_sequence_parallelism_threshold,\n+                )\n+\n+                tp_size = self.parallel_config.tensor_parallel_size\n+                hidden_size = self.model_config.get_hidden_size()\n+                element_size = self.model_config.dtype.itemsize\n+                min_token_num = get_sequence_parallelism_threshold(\n+                    hidden_size, tp_size, element_size\n+                )\n", "materials": ["This will be resolved by saving this value back to config. But we could also extract this logic into a helper function that lives on the passconfig"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -853,8 +853,33 @@ def has_blocked_weights():\n                 logger.warning(\"Sequence Parallelism requires TP>1, disabling\")\n                 self.compilation_config.pass_config.enable_sp = False\n                 self.compilation_config.pass_config.fuse_gemm_comms = False\n+            else:\n+                # Compute SP threshold early; disable if None (model too\n+                # small) before +rms_norm gets forced into custom_ops.\n+                pass_config = self.compilation_config.pass_config\n+                if pass_config.sp_min_token_num is None:\n+                    from vllm.compilation.passes.fusion.sequence_parallelism import (\n+                        get_sequence_parallelism_threshold,\n+                    )\n+\n+                    tp_size = self.parallel_config.tensor_parallel_size\n+                    hidden_size = self.model_config.get_hidden_size()\n+                    element_size = self.model_config.dtype.itemsize\n+                    pass_config.sp_min_token_num = get_sequence_parallelism_threshold(\n+                        hidden_size, tp_size, element_size\n+                    )\n \n-            elif \"-rms_norm\" in self.compilation_config.custom_ops:\n+                if pass_config.sp_min_token_num is None:\n+                    logger.warning(\n+                        \"Model hidden_size too small for the SP \"\n+                        \"threshold heuristic, disabling. To force SP, \"\n+                        \"set pass_config.sp_min_token_num manually.\"\n+                    )\n+                    self.compilation_config.pass_config.enable_sp = False\n+                    self.compilation_config.pass_config.fuse_gemm_comms = False\n+\n+        if self.compilation_config.pass_config.enable_sp:\n+            if \"-rms_norm\" in self.compilation_config.custom_ops:\n                 logger.warning(\n                     \"RMS norm force disabled, sequence parallelism might break\"\n                 )\n@@ -1456,6 +1481,36 @@ def _set_compile_ranges(self):\n                         \"allreduce-rms fusion will be enabled for all num_tokens.\"\n                     )\n \n+        # Add the compile ranges for sequence parallelism\n+        if compilation_config.pass_config.enable_sp:\n+            pass_config = compilation_config.pass_config\n+\n+            # Calculate min_token_num if not explicitly provided\n+            # User override works regardless of hidden_size\n+            if pass_config.sp_min_token_num is None:\n+                from vllm.compilation.passes.fusion.sequence_parallelism import (\n+                    get_sequence_parallelism_threshold,\n+                )\n+\n+                tp_size = self.parallel_config.tensor_parallel_size\n+                hidden_size = self.model_config.get_hidden_size()\n+                element_size = self.model_config.dtype.itemsize\n+                pass_config.sp_min_token_num = get_sequence_parallelism_threshold(\n+                    hidden_size, tp_size, element_size\n+                )\n+\n+            min_token_num = pass_config.sp_min_token_num\n+            max_num_batched_tokens = self.scheduler_config.max_num_batched_tokens\n+            if min_token_num is not None and (\n+                max_num_batched_tokens is not None\n+                and min_token_num < max_num_batched_tokens\n+                and min_token_num > 1\n+            ):\n+                # Add split point at min_token_num - 1 to ensure SP applies\n+                # starting from min_token_num\n+                # This creates ranges: [1, min-1] (no SP), [min, max] (SP applies)\n+                computed_compile_ranges_split_points.append(min_token_num - 1)\n+\n         if compilation_config.pass_config.fuse_rope_kvcache:\n             max_token_num = (\n                 compilation_config.pass_config.rope_kvcache_fusion_max_token_num", "patch_sha256": "53f59b2b702d5fdf"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28672/review_2856450426", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28672", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/vllm.py\nReview Comment:\nWe should unhallucinate this in a follow-up: https://github.com/vllm-project/vllm/pull/26847/changes#r2856447408\n\nOriginal Code Context:\n\n", "materials": ["We should unhallucinate this in a follow-up: https://github.com/vllm-project/vllm/pull/26847/changes#r2856447408"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -853,8 +853,33 @@ def has_blocked_weights():\n                 logger.warning(\"Sequence Parallelism requires TP>1, disabling\")\n                 self.compilation_config.pass_config.enable_sp = False\n                 self.compilation_config.pass_config.fuse_gemm_comms = False\n+            else:\n+                # Compute SP threshold early; disable if None (model too\n+                # small) before +rms_norm gets forced into custom_ops.\n+                pass_config = self.compilation_config.pass_config\n+                if pass_config.sp_min_token_num is None:\n+                    from vllm.compilation.passes.fusion.sequence_parallelism import (\n+                        get_sequence_parallelism_threshold,\n+                    )\n+\n+                    tp_size = self.parallel_config.tensor_parallel_size\n+                    hidden_size = self.model_config.get_hidden_size()\n+                    element_size = self.model_config.dtype.itemsize\n+                    pass_config.sp_min_token_num = get_sequence_parallelism_threshold(\n+                        hidden_size, tp_size, element_size\n+                    )\n \n-            elif \"-rms_norm\" in self.compilation_config.custom_ops:\n+                if pass_config.sp_min_token_num is None:\n+                    logger.warning(\n+                        \"Model hidden_size too small for the SP \"\n+                        \"threshold heuristic, disabling. To force SP, \"\n+                        \"set pass_config.sp_min_token_num manually.\"\n+                    )\n+                    self.compilation_config.pass_config.enable_sp = False\n+                    self.compilation_config.pass_config.fuse_gemm_comms = False\n+\n+        if self.compilation_config.pass_config.enable_sp:\n+            if \"-rms_norm\" in self.compilation_config.custom_ops:\n                 logger.warning(\n                     \"RMS norm force disabled, sequence parallelism might break\"\n                 )\n@@ -1456,6 +1481,36 @@ def _set_compile_ranges(self):\n                         \"allreduce-rms fusion will be enabled for all num_tokens.\"\n                     )\n \n+        # Add the compile ranges for sequence parallelism\n+        if compilation_config.pass_config.enable_sp:\n+            pass_config = compilation_config.pass_config\n+\n+            # Calculate min_token_num if not explicitly provided\n+            # User override works regardless of hidden_size\n+            if pass_config.sp_min_token_num is None:\n+                from vllm.compilation.passes.fusion.sequence_parallelism import (\n+                    get_sequence_parallelism_threshold,\n+                )\n+\n+                tp_size = self.parallel_config.tensor_parallel_size\n+                hidden_size = self.model_config.get_hidden_size()\n+                element_size = self.model_config.dtype.itemsize\n+                pass_config.sp_min_token_num = get_sequence_parallelism_threshold(\n+                    hidden_size, tp_size, element_size\n+                )\n+\n+            min_token_num = pass_config.sp_min_token_num\n+            max_num_batched_tokens = self.scheduler_config.max_num_batched_tokens\n+            if min_token_num is not None and (\n+                max_num_batched_tokens is not None\n+                and min_token_num < max_num_batched_tokens\n+                and min_token_num > 1\n+            ):\n+                # Add split point at min_token_num - 1 to ensure SP applies\n+                # starting from min_token_num\n+                # This creates ranges: [1, min-1] (no SP), [min, max] (SP applies)\n+                computed_compile_ranges_split_points.append(min_token_num - 1)\n+\n         if compilation_config.pass_config.fuse_rope_kvcache:\n             max_token_num = (\n                 compilation_config.pass_config.rope_kvcache_fusion_max_token_num", "patch_sha256": "53f59b2b702d5fdf"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28672/review_2856465178", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28672", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/vllm.py\nReview Comment:\nsure I can add a follow-up PR\n\nOriginal Code Context:\n\n", "materials": ["sure I can add a follow-up PR"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -853,8 +853,33 @@ def has_blocked_weights():\n                 logger.warning(\"Sequence Parallelism requires TP>1, disabling\")\n                 self.compilation_config.pass_config.enable_sp = False\n                 self.compilation_config.pass_config.fuse_gemm_comms = False\n+            else:\n+                # Compute SP threshold early; disable if None (model too\n+                # small) before +rms_norm gets forced into custom_ops.\n+                pass_config = self.compilation_config.pass_config\n+                if pass_config.sp_min_token_num is None:\n+                    from vllm.compilation.passes.fusion.sequence_parallelism import (\n+                        get_sequence_parallelism_threshold,\n+                    )\n+\n+                    tp_size = self.parallel_config.tensor_parallel_size\n+                    hidden_size = self.model_config.get_hidden_size()\n+                    element_size = self.model_config.dtype.itemsize\n+                    pass_config.sp_min_token_num = get_sequence_parallelism_threshold(\n+                        hidden_size, tp_size, element_size\n+                    )\n \n-            elif \"-rms_norm\" in self.compilation_config.custom_ops:\n+                if pass_config.sp_min_token_num is None:\n+                    logger.warning(\n+                        \"Model hidden_size too small for the SP \"\n+                        \"threshold heuristic, disabling. To force SP, \"\n+                        \"set pass_config.sp_min_token_num manually.\"\n+                    )\n+                    self.compilation_config.pass_config.enable_sp = False\n+                    self.compilation_config.pass_config.fuse_gemm_comms = False\n+\n+        if self.compilation_config.pass_config.enable_sp:\n+            if \"-rms_norm\" in self.compilation_config.custom_ops:\n                 logger.warning(\n                     \"RMS norm force disabled, sequence parallelism might break\"\n                 )\n@@ -1456,6 +1481,36 @@ def _set_compile_ranges(self):\n                         \"allreduce-rms fusion will be enabled for all num_tokens.\"\n                     )\n \n+        # Add the compile ranges for sequence parallelism\n+        if compilation_config.pass_config.enable_sp:\n+            pass_config = compilation_config.pass_config\n+\n+            # Calculate min_token_num if not explicitly provided\n+            # User override works regardless of hidden_size\n+            if pass_config.sp_min_token_num is None:\n+                from vllm.compilation.passes.fusion.sequence_parallelism import (\n+                    get_sequence_parallelism_threshold,\n+                )\n+\n+                tp_size = self.parallel_config.tensor_parallel_size\n+                hidden_size = self.model_config.get_hidden_size()\n+                element_size = self.model_config.dtype.itemsize\n+                pass_config.sp_min_token_num = get_sequence_parallelism_threshold(\n+                    hidden_size, tp_size, element_size\n+                )\n+\n+            min_token_num = pass_config.sp_min_token_num\n+            max_num_batched_tokens = self.scheduler_config.max_num_batched_tokens\n+            if min_token_num is not None and (\n+                max_num_batched_tokens is not None\n+                and min_token_num < max_num_batched_tokens\n+                and min_token_num > 1\n+            ):\n+                # Add split point at min_token_num - 1 to ensure SP applies\n+                # starting from min_token_num\n+                # This creates ranges: [1, min-1] (no SP), [min, max] (SP applies)\n+                computed_compile_ranges_split_points.append(min_token_num - 1)\n+\n         if compilation_config.pass_config.fuse_rope_kvcache:\n             max_token_num = (\n                 compilation_config.pass_config.rope_kvcache_fusion_max_token_num", "patch_sha256": "53f59b2b702d5fdf"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28672/review_2856472107", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28672", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/vllm.py\nReview Comment:\nShould actually help perf I think!\n\nOriginal Code Context:\n\n", "materials": ["Should actually help perf I think!"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -853,8 +853,33 @@ def has_blocked_weights():\n                 logger.warning(\"Sequence Parallelism requires TP>1, disabling\")\n                 self.compilation_config.pass_config.enable_sp = False\n                 self.compilation_config.pass_config.fuse_gemm_comms = False\n+            else:\n+                # Compute SP threshold early; disable if None (model too\n+                # small) before +rms_norm gets forced into custom_ops.\n+                pass_config = self.compilation_config.pass_config\n+                if pass_config.sp_min_token_num is None:\n+                    from vllm.compilation.passes.fusion.sequence_parallelism import (\n+                        get_sequence_parallelism_threshold,\n+                    )\n+\n+                    tp_size = self.parallel_config.tensor_parallel_size\n+                    hidden_size = self.model_config.get_hidden_size()\n+                    element_size = self.model_config.dtype.itemsize\n+                    pass_config.sp_min_token_num = get_sequence_parallelism_threshold(\n+                        hidden_size, tp_size, element_size\n+                    )\n \n-            elif \"-rms_norm\" in self.compilation_config.custom_ops:\n+                if pass_config.sp_min_token_num is None:\n+                    logger.warning(\n+                        \"Model hidden_size too small for the SP \"\n+                        \"threshold heuristic, disabling. To force SP, \"\n+                        \"set pass_config.sp_min_token_num manually.\"\n+                    )\n+                    self.compilation_config.pass_config.enable_sp = False\n+                    self.compilation_config.pass_config.fuse_gemm_comms = False\n+\n+        if self.compilation_config.pass_config.enable_sp:\n+            if \"-rms_norm\" in self.compilation_config.custom_ops:\n                 logger.warning(\n                     \"RMS norm force disabled, sequence parallelism might break\"\n                 )\n@@ -1456,6 +1481,36 @@ def _set_compile_ranges(self):\n                         \"allreduce-rms fusion will be enabled for all num_tokens.\"\n                     )\n \n+        # Add the compile ranges for sequence parallelism\n+        if compilation_config.pass_config.enable_sp:\n+            pass_config = compilation_config.pass_config\n+\n+            # Calculate min_token_num if not explicitly provided\n+            # User override works regardless of hidden_size\n+            if pass_config.sp_min_token_num is None:\n+                from vllm.compilation.passes.fusion.sequence_parallelism import (\n+                    get_sequence_parallelism_threshold,\n+                )\n+\n+                tp_size = self.parallel_config.tensor_parallel_size\n+                hidden_size = self.model_config.get_hidden_size()\n+                element_size = self.model_config.dtype.itemsize\n+                pass_config.sp_min_token_num = get_sequence_parallelism_threshold(\n+                    hidden_size, tp_size, element_size\n+                )\n+\n+            min_token_num = pass_config.sp_min_token_num\n+            max_num_batched_tokens = self.scheduler_config.max_num_batched_tokens\n+            if min_token_num is not None and (\n+                max_num_batched_tokens is not None\n+                and min_token_num < max_num_batched_tokens\n+                and min_token_num > 1\n+            ):\n+                # Add split point at min_token_num - 1 to ensure SP applies\n+                # starting from min_token_num\n+                # This creates ranges: [1, min-1] (no SP), [min, max] (SP applies)\n+                computed_compile_ranges_split_points.append(min_token_num - 1)\n+\n         if compilation_config.pass_config.fuse_rope_kvcache:\n             max_token_num = (\n                 compilation_config.pass_config.rope_kvcache_fusion_max_token_num", "patch_sha256": "53f59b2b702d5fdf"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28973/review_2559671921", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28973", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/engine/__init__.py\nReview Comment:\n```suggestion\r\n    close_streaming_session: bool | None = None\r\n```\r\n\r\nThen maybe we could check that this can only be True/False for streaming requests but never for normal requests where it always has to stay None? \n\nOriginal Code Context:\n@@ -71,6 +71,7 @@ class EngineCoreRequest(\n     priority: int = 0\n \n     trace_headers: Mapping[str, str] | None = None\n+    close_session: bool = True\n", "materials": ["```suggestion\r\n    close_streaming_session: bool | None = None\r\n```\r\n\r\nThen maybe we could check that this can only be True/False for streaming requests but never for normal requests where it always has to stay None? "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -75,6 +75,7 @@ class EngineCoreRequest(\n     priority: int = 0\n \n     trace_headers: Mapping[str, str] | None = None\n+    resumable: bool = False\n \n     # The user-provided request ID. This field is set internally,\n     # copied from the provided request_id that's originally assigned", "patch_sha256": "99290cae4ebdd149"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28973/review_2559674901", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28973", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/engine/__init__.py\nReview Comment:\nWhy does the EngineCoreOutput need to have a close_session bool - is it not enough that the input has it? It's always the user that decides whether the session is closed or not? \n\nOriginal Code Context:\n@@ -126,6 +127,7 @@ class EngineCoreOutput(\n     # The number of NaNs in logits.\n     # A value greater than 0 indicates that the output is corrupted.\n     num_nans_in_logits: int = 0\n+    close_session: bool = True\n", "materials": ["Why does the EngineCoreOutput need to have a close_session bool - is it not enough that the input has it? It's always the user that decides whether the session is closed or not? "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -75,6 +75,7 @@ class EngineCoreRequest(\n     priority: int = 0\n \n     trace_headers: Mapping[str, str] | None = None\n+    resumable: bool = False\n \n     # The user-provided request ID. This field is set internally,\n     # copied from the provided request_id that's originally assigned", "patch_sha256": "99290cae4ebdd149"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28973/review_2560966707", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28973", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/engine/__init__.py\nReview Comment:\nit's needed in output_processor.py, to know if we need to close the streaming session for finished request\n\nOriginal Code Context:\n@@ -126,6 +127,7 @@ class EngineCoreOutput(\n     # The number of NaNs in logits.\n     # A value greater than 0 indicates that the output is corrupted.\n     num_nans_in_logits: int = 0\n+    close_session: bool = True\n", "materials": ["it's needed in output_processor.py, to know if we need to close the streaming session for finished request"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -75,6 +75,7 @@ class EngineCoreRequest(\n     priority: int = 0\n \n     trace_headers: Mapping[str, str] | None = None\n+    resumable: bool = False\n \n     # The user-provided request ID. This field is set internally,\n     # copied from the provided request_id that's originally assigned", "patch_sha256": "99290cae4ebdd149"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28973/review_2579468404", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28973", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/request.py\nReview Comment:\nWhy do we count `RequestStatus.WAITING_FOR_STREAMING_REQ` as a finished state?\n\nOriginal Code Context:\n@@ -259,4 +275,5 @@ def get_finished_reason(status: \"RequestStatus\") -> FinishReason | None:\n     RequestStatus.FINISHED_LENGTH_CAPPED: FinishReason.LENGTH,\n     RequestStatus.FINISHED_ABORTED: FinishReason.ABORT,\n     RequestStatus.FINISHED_IGNORED: FinishReason.LENGTH,\n+    RequestStatus.WAITING_FOR_STREAMING_REQ: FinishReason.STOP,\n", "materials": ["Why do we count `RequestStatus.WAITING_FOR_STREAMING_REQ` as a finished state?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -3,7 +3,9 @@\n \n import enum\n import time\n+from collections import deque\n from collections.abc import Callable, Mapping\n+from dataclasses import dataclass\n from functools import partial\n from typing import TYPE_CHECKING, Any, Optional\n \n@@ -27,6 +29,33 @@\n     from vllm.v1.core.kv_cache_utils import BlockHash\n \n \n+@dataclass\n+class StreamingUpdate:\n+    \"\"\"Lightweight data for streaming session continuation.\n+\n+    Contains only the fields needed to update an existing streaming session\n+    with new input data.\n+    \"\"\"\n+\n+    mm_features: list[MultiModalFeatureSpec] | None\n+    prompt_token_ids: list[int] | None\n+    max_tokens: int\n+    arrival_time: float\n+    sampling_params: SamplingParams | None\n+\n+    @classmethod\n+    def from_request(cls, request: \"Request\") -> \"StreamingUpdate | None\":\n+        if not request.resumable:\n+            return None\n+        return cls(\n+            mm_features=request.mm_features,\n+            prompt_token_ids=request.prompt_token_ids,\n+            max_tokens=request.max_tokens,\n+            arrival_time=request.arrival_time,\n+            sampling_params=request.sampling_params,\n+        )\n+\n+\n class Request:\n     def __init__(\n         self,\n@@ -44,6 +73,7 @@ def __init__(\n         priority: int = 0,\n         trace_headers: Mapping[str, str] | None = None,\n         block_hasher: Callable[[\"Request\"], list[\"BlockHash\"]] | None = None,\n+        resumable: bool = False,\n     ) -> None:\n         self.request_id = request_id\n         self.client_index = client_index\n@@ -105,8 +135,6 @@ def __init__(\n \n         # Multi-modal related\n         self.mm_features = mm_features or []\n-        self.num_encoder_inputs = len(self.mm_features)\n-        self.has_encoder_inputs = self.num_encoder_inputs > 0\n \n         # Read-only views\n         # Prevent directly appending to these lists since\n@@ -137,6 +165,11 @@ def __init__(\n \n         self.skip_reading_prefix_cache = self.get_skip_reading_prefix_cache()\n \n+        # Used for streaming\n+        self.resumable = resumable\n+        # None entry in the queue means finished.\n+        self.streaming_queue: deque[StreamingUpdate | None] | None = None\n+\n     @classmethod\n     def from_engine_core_request(\n         cls,\n@@ -158,6 +191,7 @@ def from_engine_core_request(\n             priority=request.priority,\n             trace_headers=request.trace_headers,\n             block_hasher=block_hasher,\n+            resumable=request.resumable,\n         )\n \n     def append_output_token_ids(\n@@ -190,6 +224,14 @@ def num_tokens_with_spec(self) -> int:\n     def num_output_tokens(self) -> int:\n         return len(self._output_token_ids)\n \n+    @property\n+    def num_encoder_inputs(self) -> int:\n+        return len(self.mm_features)\n+\n+    @property\n+    def has_encoder_inputs(self) -> bool:\n+        return self.num_encoder_inputs > 0\n+\n     def get_skip_reading_prefix_cache(self) -> bool:\n         if (\n             self.sampling_params is not None\n@@ -246,6 +288,7 @@ class RequestStatus(enum.IntEnum):\n     WAITING = enum.auto()\n     WAITING_FOR_FSM = enum.auto()\n     WAITING_FOR_REMOTE_KVS = enum.auto()\n+    WAITING_FOR_STREAMING_REQ = enum.auto()\n     RUNNING = enum.auto()\n     PREEMPTED = enum.auto()\n     # Note: anything after PREEMPTED will be considered\n@@ -256,7 +299,7 @@ class RequestStatus(enum.IntEnum):\n     FINISHED_IGNORED = enum.auto()\n     FINISHED_ERROR = enum.auto()\n \n-    def __str__(self):\n+    def __str__(self) -> str:\n         return self.name\n \n     @staticmethod\n@@ -278,4 +321,5 @@ def get_finished_reason(status: \"RequestStatus\") -> FinishReason | None:\n     RequestStatus.FINISHED_ABORTED: FinishReason.ABORT,\n     RequestStatus.FINISHED_IGNORED: FinishReason.LENGTH,\n     RequestStatus.FINISHED_ERROR: FinishReason.ERROR,\n+    RequestStatus.WAITING_FOR_STREAMING_REQ: FinishReason.STOP,\n }", "patch_sha256": "d4b40f1a3e3bbfca"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28973/review_2579525327", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28973", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/engine/__init__.py\nReview Comment:\nNit: We can maybe call this field something like `more_tokens_coming` (probably also not the best name but you got the point)? Then we can default this field to False for all requests but only set this to True for requests that have more streaming inputs coming.\n\nOriginal Code Context:\n@@ -71,6 +71,7 @@ class EngineCoreRequest(\n     priority: int = 0\n \n     trace_headers: Mapping[str, str] | None = None\n+    close_streaming_session: bool | None = None\n", "materials": ["Nit: We can maybe call this field something like `more_tokens_coming` (probably also not the best name but you got the point)? Then we can default this field to False for all requests but only set this to True for requests that have more streaming inputs coming."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -75,6 +75,7 @@ class EngineCoreRequest(\n     priority: int = 0\n \n     trace_headers: Mapping[str, str] | None = None\n+    resumable: bool = False\n \n     # The user-provided request ID. This field is set internally,\n     # copied from the provided request_id that's originally assigned", "patch_sha256": "99290cae4ebdd149"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28973/review_2582433458", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28973", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/request.py\nReview Comment:\nwe need to provide a finish reason when creating EngineCoreOutput, otherwise it would be none. the client will expect a finish reason to know generation stopped (temporarily for streaming), so we can consume the output chunk, and it won't hang.\n\nOriginal Code Context:\n@@ -259,4 +275,5 @@ def get_finished_reason(status: \"RequestStatus\") -> FinishReason | None:\n     RequestStatus.FINISHED_LENGTH_CAPPED: FinishReason.LENGTH,\n     RequestStatus.FINISHED_ABORTED: FinishReason.ABORT,\n     RequestStatus.FINISHED_IGNORED: FinishReason.LENGTH,\n+    RequestStatus.WAITING_FOR_STREAMING_REQ: FinishReason.STOP,\n", "materials": ["we need to provide a finish reason when creating EngineCoreOutput, otherwise it would be none. the client will expect a finish reason to know generation stopped (temporarily for streaming), so we can consume the output chunk, and it won't hang."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -3,7 +3,9 @@\n \n import enum\n import time\n+from collections import deque\n from collections.abc import Callable, Mapping\n+from dataclasses import dataclass\n from functools import partial\n from typing import TYPE_CHECKING, Any, Optional\n \n@@ -27,6 +29,33 @@\n     from vllm.v1.core.kv_cache_utils import BlockHash\n \n \n+@dataclass\n+class StreamingUpdate:\n+    \"\"\"Lightweight data for streaming session continuation.\n+\n+    Contains only the fields needed to update an existing streaming session\n+    with new input data.\n+    \"\"\"\n+\n+    mm_features: list[MultiModalFeatureSpec] | None\n+    prompt_token_ids: list[int] | None\n+    max_tokens: int\n+    arrival_time: float\n+    sampling_params: SamplingParams | None\n+\n+    @classmethod\n+    def from_request(cls, request: \"Request\") -> \"StreamingUpdate | None\":\n+        if not request.resumable:\n+            return None\n+        return cls(\n+            mm_features=request.mm_features,\n+            prompt_token_ids=request.prompt_token_ids,\n+            max_tokens=request.max_tokens,\n+            arrival_time=request.arrival_time,\n+            sampling_params=request.sampling_params,\n+        )\n+\n+\n class Request:\n     def __init__(\n         self,\n@@ -44,6 +73,7 @@ def __init__(\n         priority: int = 0,\n         trace_headers: Mapping[str, str] | None = None,\n         block_hasher: Callable[[\"Request\"], list[\"BlockHash\"]] | None = None,\n+        resumable: bool = False,\n     ) -> None:\n         self.request_id = request_id\n         self.client_index = client_index\n@@ -105,8 +135,6 @@ def __init__(\n \n         # Multi-modal related\n         self.mm_features = mm_features or []\n-        self.num_encoder_inputs = len(self.mm_features)\n-        self.has_encoder_inputs = self.num_encoder_inputs > 0\n \n         # Read-only views\n         # Prevent directly appending to these lists since\n@@ -137,6 +165,11 @@ def __init__(\n \n         self.skip_reading_prefix_cache = self.get_skip_reading_prefix_cache()\n \n+        # Used for streaming\n+        self.resumable = resumable\n+        # None entry in the queue means finished.\n+        self.streaming_queue: deque[StreamingUpdate | None] | None = None\n+\n     @classmethod\n     def from_engine_core_request(\n         cls,\n@@ -158,6 +191,7 @@ def from_engine_core_request(\n             priority=request.priority,\n             trace_headers=request.trace_headers,\n             block_hasher=block_hasher,\n+            resumable=request.resumable,\n         )\n \n     def append_output_token_ids(\n@@ -190,6 +224,14 @@ def num_tokens_with_spec(self) -> int:\n     def num_output_tokens(self) -> int:\n         return len(self._output_token_ids)\n \n+    @property\n+    def num_encoder_inputs(self) -> int:\n+        return len(self.mm_features)\n+\n+    @property\n+    def has_encoder_inputs(self) -> bool:\n+        return self.num_encoder_inputs > 0\n+\n     def get_skip_reading_prefix_cache(self) -> bool:\n         if (\n             self.sampling_params is not None\n@@ -246,6 +288,7 @@ class RequestStatus(enum.IntEnum):\n     WAITING = enum.auto()\n     WAITING_FOR_FSM = enum.auto()\n     WAITING_FOR_REMOTE_KVS = enum.auto()\n+    WAITING_FOR_STREAMING_REQ = enum.auto()\n     RUNNING = enum.auto()\n     PREEMPTED = enum.auto()\n     # Note: anything after PREEMPTED will be considered\n@@ -256,7 +299,7 @@ class RequestStatus(enum.IntEnum):\n     FINISHED_IGNORED = enum.auto()\n     FINISHED_ERROR = enum.auto()\n \n-    def __str__(self):\n+    def __str__(self) -> str:\n         return self.name\n \n     @staticmethod\n@@ -278,4 +321,5 @@ def get_finished_reason(status: \"RequestStatus\") -> FinishReason | None:\n     RequestStatus.FINISHED_ABORTED: FinishReason.ABORT,\n     RequestStatus.FINISHED_IGNORED: FinishReason.LENGTH,\n     RequestStatus.FINISHED_ERROR: FinishReason.ERROR,\n+    RequestStatus.WAITING_FOR_STREAMING_REQ: FinishReason.STOP,\n }", "patch_sha256": "d4b40f1a3e3bbfca"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28973/review_2585696869", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28973", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/engine/__init__.py\nReview Comment:\nShould we maybe re-use `resumable` that was used in another PR: https://github.com/vllm-project/vllm/pull/25463 and which I think is quite intuitive \n\nOriginal Code Context:\n@@ -71,6 +71,7 @@ class EngineCoreRequest(\n     priority: int = 0\n \n     trace_headers: Mapping[str, str] | None = None\n+    close_streaming_session: bool | None = None\n", "materials": ["Should we maybe re-use `resumable` that was used in another PR: https://github.com/vllm-project/vllm/pull/25463 and which I think is quite intuitive "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -75,6 +75,7 @@ class EngineCoreRequest(\n     priority: int = 0\n \n     trace_headers: Mapping[str, str] | None = None\n+    resumable: bool = False\n \n     # The user-provided request ID. This field is set internally,\n     # copied from the provided request_id that's originally assigned", "patch_sha256": "99290cae4ebdd149"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28973/review_2585720802", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28973", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/request.py\nReview Comment:\nlet's maybe not init a streaming queue if it's not a streaming request no? \n\nOriginal Code Context:\n@@ -132,6 +129,10 @@ def __init__(\n \n         self.skip_reading_prefix_cache = self.get_skip_reading_prefix_cache()\n \n+        # Used for streaming\n+        self.close_streaming_session = close_streaming_session\n+        self.streaming_queue: deque[Request] = deque()\n", "materials": ["let's maybe not init a streaming queue if it's not a streaming request no? "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -3,7 +3,9 @@\n \n import enum\n import time\n+from collections import deque\n from collections.abc import Callable, Mapping\n+from dataclasses import dataclass\n from functools import partial\n from typing import TYPE_CHECKING, Any, Optional\n \n@@ -27,6 +29,33 @@\n     from vllm.v1.core.kv_cache_utils import BlockHash\n \n \n+@dataclass\n+class StreamingUpdate:\n+    \"\"\"Lightweight data for streaming session continuation.\n+\n+    Contains only the fields needed to update an existing streaming session\n+    with new input data.\n+    \"\"\"\n+\n+    mm_features: list[MultiModalFeatureSpec] | None\n+    prompt_token_ids: list[int] | None\n+    max_tokens: int\n+    arrival_time: float\n+    sampling_params: SamplingParams | None\n+\n+    @classmethod\n+    def from_request(cls, request: \"Request\") -> \"StreamingUpdate | None\":\n+        if not request.resumable:\n+            return None\n+        return cls(\n+            mm_features=request.mm_features,\n+            prompt_token_ids=request.prompt_token_ids,\n+            max_tokens=request.max_tokens,\n+            arrival_time=request.arrival_time,\n+            sampling_params=request.sampling_params,\n+        )\n+\n+\n class Request:\n     def __init__(\n         self,\n@@ -44,6 +73,7 @@ def __init__(\n         priority: int = 0,\n         trace_headers: Mapping[str, str] | None = None,\n         block_hasher: Callable[[\"Request\"], list[\"BlockHash\"]] | None = None,\n+        resumable: bool = False,\n     ) -> None:\n         self.request_id = request_id\n         self.client_index = client_index\n@@ -105,8 +135,6 @@ def __init__(\n \n         # Multi-modal related\n         self.mm_features = mm_features or []\n-        self.num_encoder_inputs = len(self.mm_features)\n-        self.has_encoder_inputs = self.num_encoder_inputs > 0\n \n         # Read-only views\n         # Prevent directly appending to these lists since\n@@ -137,6 +165,11 @@ def __init__(\n \n         self.skip_reading_prefix_cache = self.get_skip_reading_prefix_cache()\n \n+        # Used for streaming\n+        self.resumable = resumable\n+        # None entry in the queue means finished.\n+        self.streaming_queue: deque[StreamingUpdate | None] | None = None\n+\n     @classmethod\n     def from_engine_core_request(\n         cls,\n@@ -158,6 +191,7 @@ def from_engine_core_request(\n             priority=request.priority,\n             trace_headers=request.trace_headers,\n             block_hasher=block_hasher,\n+            resumable=request.resumable,\n         )\n \n     def append_output_token_ids(\n@@ -190,6 +224,14 @@ def num_tokens_with_spec(self) -> int:\n     def num_output_tokens(self) -> int:\n         return len(self._output_token_ids)\n \n+    @property\n+    def num_encoder_inputs(self) -> int:\n+        return len(self.mm_features)\n+\n+    @property\n+    def has_encoder_inputs(self) -> bool:\n+        return self.num_encoder_inputs > 0\n+\n     def get_skip_reading_prefix_cache(self) -> bool:\n         if (\n             self.sampling_params is not None\n@@ -246,6 +288,7 @@ class RequestStatus(enum.IntEnum):\n     WAITING = enum.auto()\n     WAITING_FOR_FSM = enum.auto()\n     WAITING_FOR_REMOTE_KVS = enum.auto()\n+    WAITING_FOR_STREAMING_REQ = enum.auto()\n     RUNNING = enum.auto()\n     PREEMPTED = enum.auto()\n     # Note: anything after PREEMPTED will be considered\n@@ -256,7 +299,7 @@ class RequestStatus(enum.IntEnum):\n     FINISHED_IGNORED = enum.auto()\n     FINISHED_ERROR = enum.auto()\n \n-    def __str__(self):\n+    def __str__(self) -> str:\n         return self.name\n \n     @staticmethod\n@@ -278,4 +321,5 @@ def get_finished_reason(status: \"RequestStatus\") -> FinishReason | None:\n     RequestStatus.FINISHED_ABORTED: FinishReason.ABORT,\n     RequestStatus.FINISHED_IGNORED: FinishReason.LENGTH,\n     RequestStatus.FINISHED_ERROR: FinishReason.ERROR,\n+    RequestStatus.WAITING_FOR_STREAMING_REQ: FinishReason.STOP,\n }", "patch_sha256": "d4b40f1a3e3bbfca"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28973/review_2611117142", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28973", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/request.py\nReview Comment:\nsimilar comment to the other one\n\nOriginal Code Context:\n@@ -190,6 +191,20 @@ def num_tokens_with_spec(self) -> int:\n     def num_output_tokens(self) -> int:\n         return len(self._output_token_ids)\n \n+    @property\n+    def num_prompt_tokens(self) -> int:\n+        return length_from_prompt_token_ids_or_embeds(\n+            self.prompt_token_ids, self.prompt_embeds\n+        )\n", "materials": ["similar comment to the other one"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -3,7 +3,9 @@\n \n import enum\n import time\n+from collections import deque\n from collections.abc import Callable, Mapping\n+from dataclasses import dataclass\n from functools import partial\n from typing import TYPE_CHECKING, Any, Optional\n \n@@ -27,6 +29,33 @@\n     from vllm.v1.core.kv_cache_utils import BlockHash\n \n \n+@dataclass\n+class StreamingUpdate:\n+    \"\"\"Lightweight data for streaming session continuation.\n+\n+    Contains only the fields needed to update an existing streaming session\n+    with new input data.\n+    \"\"\"\n+\n+    mm_features: list[MultiModalFeatureSpec] | None\n+    prompt_token_ids: list[int] | None\n+    max_tokens: int\n+    arrival_time: float\n+    sampling_params: SamplingParams | None\n+\n+    @classmethod\n+    def from_request(cls, request: \"Request\") -> \"StreamingUpdate | None\":\n+        if not request.resumable:\n+            return None\n+        return cls(\n+            mm_features=request.mm_features,\n+            prompt_token_ids=request.prompt_token_ids,\n+            max_tokens=request.max_tokens,\n+            arrival_time=request.arrival_time,\n+            sampling_params=request.sampling_params,\n+        )\n+\n+\n class Request:\n     def __init__(\n         self,\n@@ -44,6 +73,7 @@ def __init__(\n         priority: int = 0,\n         trace_headers: Mapping[str, str] | None = None,\n         block_hasher: Callable[[\"Request\"], list[\"BlockHash\"]] | None = None,\n+        resumable: bool = False,\n     ) -> None:\n         self.request_id = request_id\n         self.client_index = client_index\n@@ -105,8 +135,6 @@ def __init__(\n \n         # Multi-modal related\n         self.mm_features = mm_features or []\n-        self.num_encoder_inputs = len(self.mm_features)\n-        self.has_encoder_inputs = self.num_encoder_inputs > 0\n \n         # Read-only views\n         # Prevent directly appending to these lists since\n@@ -137,6 +165,11 @@ def __init__(\n \n         self.skip_reading_prefix_cache = self.get_skip_reading_prefix_cache()\n \n+        # Used for streaming\n+        self.resumable = resumable\n+        # None entry in the queue means finished.\n+        self.streaming_queue: deque[StreamingUpdate | None] | None = None\n+\n     @classmethod\n     def from_engine_core_request(\n         cls,\n@@ -158,6 +191,7 @@ def from_engine_core_request(\n             priority=request.priority,\n             trace_headers=request.trace_headers,\n             block_hasher=block_hasher,\n+            resumable=request.resumable,\n         )\n \n     def append_output_token_ids(\n@@ -190,6 +224,14 @@ def num_tokens_with_spec(self) -> int:\n     def num_output_tokens(self) -> int:\n         return len(self._output_token_ids)\n \n+    @property\n+    def num_encoder_inputs(self) -> int:\n+        return len(self.mm_features)\n+\n+    @property\n+    def has_encoder_inputs(self) -> bool:\n+        return self.num_encoder_inputs > 0\n+\n     def get_skip_reading_prefix_cache(self) -> bool:\n         if (\n             self.sampling_params is not None\n@@ -246,6 +288,7 @@ class RequestStatus(enum.IntEnum):\n     WAITING = enum.auto()\n     WAITING_FOR_FSM = enum.auto()\n     WAITING_FOR_REMOTE_KVS = enum.auto()\n+    WAITING_FOR_STREAMING_REQ = enum.auto()\n     RUNNING = enum.auto()\n     PREEMPTED = enum.auto()\n     # Note: anything after PREEMPTED will be considered\n@@ -256,7 +299,7 @@ class RequestStatus(enum.IntEnum):\n     FINISHED_IGNORED = enum.auto()\n     FINISHED_ERROR = enum.auto()\n \n-    def __str__(self):\n+    def __str__(self) -> str:\n         return self.name\n \n     @staticmethod\n@@ -278,4 +321,5 @@ def get_finished_reason(status: \"RequestStatus\") -> FinishReason | None:\n     RequestStatus.FINISHED_ABORTED: FinishReason.ABORT,\n     RequestStatus.FINISHED_IGNORED: FinishReason.LENGTH,\n     RequestStatus.FINISHED_ERROR: FinishReason.ERROR,\n+    RequestStatus.WAITING_FOR_STREAMING_REQ: FinishReason.STOP,\n }", "patch_sha256": "d4b40f1a3e3bbfca"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28973/review_2684094148", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28973", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/request.py\nReview Comment:\n### Missing `resumable` field in `from_engine_core_request`\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe `from_engine_core_request` class method doesn't pass the `resumable` field from `EngineCoreRequest` to the `Request` constructor. This means that when a request with `resumable=True` is processed through the production code path (via `EngineCore.preprocess_add_request`), the resulting `Request` object will have `resumable=False` and `streaming_queue=None`, completely breaking the streaming feature. The tests pass because they use `DummyRequest` which directly sets `resumable=True`, bypassing this broken code path.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/request.py#L172-L194\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..lj-hSobYlQX2Me_cs8_a2e_L3bHa1dYJs_hnOW8JiP9TJOzZKPUBLnm7mzzPOGlhMJHeUohiNfPo3yObmqOfpQXOREAlgdOH-yoQqLqaRMIHP9rCxVjFa9be-dR3-6quQqGIpE-vyXWBw1E-W-vHBJJIuQztVu95hWA3lME-H1si8738gE5IYsnnxknosRGExNQMhDX9kXjz_YzveOfTIQDGdouTKuLVk6YpoM7VAE7LD2_K92MlhzMXAAn6WxdJDIC41KtTSgEoZQdyxrNv2nHeVrg_rZuuxxUQuwoJFG29OKR3KgJRlcfdMOQcfdK5Q7yhxv50GJAkjL0hPTzV6A\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..DzSqEk5HoJ8Us8ocxKfI1FcuMe0wmfWeyC7b6qERxv9hyTUM4setY0K33lvVLEGdf4z6amtyadogkkZ-udYFwMSmUV8OUNraoPBY7vCobrjeop_m9uJfEk-Ul-pWlea0vNxTP-p2aEgo-kQk2YBVuk14uf5Pbbam4J7_y4lQsF_-y6AYw4fjUMtKgOnzJoWNod8v8tKg6TEC-k9Gw7h3wb9IHq0nkg7ZV6KkbdK6jMf4oaIqupQm8dyFVf1IIMFUfGiUinnTqxgvOCO4rVr9WhXBYhfxnJZNrU0ylC77BMsMNDMTVG9FOKQZFaKKtYwg6_WppQTvOryCpNT6Vtg37Q\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -137,6 +164,12 @@ def __init__(\n \n         self.skip_reading_prefix_cache = self.get_skip_reading_prefix_cache()\n \n+        # Used for streaming\n+        self.resumable = resumable\n+        self.streaming_queue: deque[StreamingUpdate] | None = (\n+            deque() if resumable else None\n+        )\n+\n", "materials": ["### Missing `resumable` field in `from_engine_core_request`\n\n**High Severity**\n\n<!-- DESCRIPTION START -->\nThe `from_engine_core_request` class method doesn't pass the `resumable` field from `EngineCoreRequest` to the `Request` constructor. This means that when a request with `resumable=True` is processed through the production code path (via `EngineCore.preprocess_add_request`), the resulting `Request` object will have `resumable=False` and `streaming_queue=None`, completely breaking the streaming feature. The tests pass because they use `DummyRequest` which directly sets `resumable=True`, bypassing this broken code path.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\nvllm/v1/request.py#L172-L194\nLOCATIONS END -->\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..lj-hSobYlQX2Me_cs8_a2e_L3bHa1dYJs_hnOW8JiP9TJOzZKPUBLnm7mzzPOGlhMJHeUohiNfPo3yObmqOfpQXOREAlgdOH-yoQqLqaRMIHP9rCxVjFa9be-dR3-6quQqGIpE-vyXWBw1E-W-vHBJJIuQztVu95hWA3lME-H1si8738gE5IYsnnxknosRGExNQMhDX9kXjz_YzveOfTIQDGdouTKuLVk6YpoM7VAE7LD2_K92MlhzMXAAn6WxdJDIC41KtTSgEoZQdyxrNv2nHeVrg_rZuuxxUQuwoJFG29OKR3KgJRlcfdMOQcfdK5Q7yhxv50GJAkjL0hPTzV6A\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..DzSqEk5HoJ8Us8ocxKfI1FcuMe0wmfWeyC7b6qERxv9hyTUM4setY0K33lvVLEGdf4z6amtyadogkkZ-udYFwMSmUV8OUNraoPBY7vCobrjeop_m9uJfEk-Ul-pWlea0vNxTP-p2aEgo-kQk2YBVuk14uf5Pbbam4J7_y4lQsF_-y6AYw4fjUMtKgOnzJoWNod8v8tKg6TEC-k9Gw7h3wb9IHq0nkg7ZV6KkbdK6jMf4oaIqupQm8dyFVf1IIMFUfGiUinnTqxgvOCO4rVr9WhXBYhfxnJZNrU0ylC77BMsMNDMTVG9FOKQZFaKKtYwg6_WppQTvOryCpNT6Vtg37Q\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -3,7 +3,9 @@\n \n import enum\n import time\n+from collections import deque\n from collections.abc import Callable, Mapping\n+from dataclasses import dataclass\n from functools import partial\n from typing import TYPE_CHECKING, Any, Optional\n \n@@ -27,6 +29,33 @@\n     from vllm.v1.core.kv_cache_utils import BlockHash\n \n \n+@dataclass\n+class StreamingUpdate:\n+    \"\"\"Lightweight data for streaming session continuation.\n+\n+    Contains only the fields needed to update an existing streaming session\n+    with new input data.\n+    \"\"\"\n+\n+    mm_features: list[MultiModalFeatureSpec] | None\n+    prompt_token_ids: list[int] | None\n+    max_tokens: int\n+    arrival_time: float\n+    sampling_params: SamplingParams | None\n+\n+    @classmethod\n+    def from_request(cls, request: \"Request\") -> \"StreamingUpdate | None\":\n+        if not request.resumable:\n+            return None\n+        return cls(\n+            mm_features=request.mm_features,\n+            prompt_token_ids=request.prompt_token_ids,\n+            max_tokens=request.max_tokens,\n+            arrival_time=request.arrival_time,\n+            sampling_params=request.sampling_params,\n+        )\n+\n+\n class Request:\n     def __init__(\n         self,\n@@ -44,6 +73,7 @@ def __init__(\n         priority: int = 0,\n         trace_headers: Mapping[str, str] | None = None,\n         block_hasher: Callable[[\"Request\"], list[\"BlockHash\"]] | None = None,\n+        resumable: bool = False,\n     ) -> None:\n         self.request_id = request_id\n         self.client_index = client_index\n@@ -105,8 +135,6 @@ def __init__(\n \n         # Multi-modal related\n         self.mm_features = mm_features or []\n-        self.num_encoder_inputs = len(self.mm_features)\n-        self.has_encoder_inputs = self.num_encoder_inputs > 0\n \n         # Read-only views\n         # Prevent directly appending to these lists since\n@@ -137,6 +165,11 @@ def __init__(\n \n         self.skip_reading_prefix_cache = self.get_skip_reading_prefix_cache()\n \n+        # Used for streaming\n+        self.resumable = resumable\n+        # None entry in the queue means finished.\n+        self.streaming_queue: deque[StreamingUpdate | None] | None = None\n+\n     @classmethod\n     def from_engine_core_request(\n         cls,\n@@ -158,6 +191,7 @@ def from_engine_core_request(\n             priority=request.priority,\n             trace_headers=request.trace_headers,\n             block_hasher=block_hasher,\n+            resumable=request.resumable,\n         )\n \n     def append_output_token_ids(\n@@ -190,6 +224,14 @@ def num_tokens_with_spec(self) -> int:\n     def num_output_tokens(self) -> int:\n         return len(self._output_token_ids)\n \n+    @property\n+    def num_encoder_inputs(self) -> int:\n+        return len(self.mm_features)\n+\n+    @property\n+    def has_encoder_inputs(self) -> bool:\n+        return self.num_encoder_inputs > 0\n+\n     def get_skip_reading_prefix_cache(self) -> bool:\n         if (\n             self.sampling_params is not None\n@@ -246,6 +288,7 @@ class RequestStatus(enum.IntEnum):\n     WAITING = enum.auto()\n     WAITING_FOR_FSM = enum.auto()\n     WAITING_FOR_REMOTE_KVS = enum.auto()\n+    WAITING_FOR_STREAMING_REQ = enum.auto()\n     RUNNING = enum.auto()\n     PREEMPTED = enum.auto()\n     # Note: anything after PREEMPTED will be considered\n@@ -256,7 +299,7 @@ class RequestStatus(enum.IntEnum):\n     FINISHED_IGNORED = enum.auto()\n     FINISHED_ERROR = enum.auto()\n \n-    def __str__(self):\n+    def __str__(self) -> str:\n         return self.name\n \n     @staticmethod\n@@ -278,4 +321,5 @@ def get_finished_reason(status: \"RequestStatus\") -> FinishReason | None:\n     RequestStatus.FINISHED_ABORTED: FinishReason.ABORT,\n     RequestStatus.FINISHED_IGNORED: FinishReason.LENGTH,\n     RequestStatus.FINISHED_ERROR: FinishReason.ERROR,\n+    RequestStatus.WAITING_FOR_STREAMING_REQ: FinishReason.STOP,\n }", "patch_sha256": "d4b40f1a3e3bbfca"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29087/review_2545737058", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29087", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/distributed/kv_transfer/kv_connector/v1/offloading_connector.py\nReview Comment:\n![critical](https://www.gstatic.com/codereviewagent/critical.svg)\n\nThere's a potential `TypeError` here. If `req_id` is not in `self._reqs_being_loaded`, `self._reqs_being_loaded.pop(req_id, None)` will return `None`. Subsequently, `self._blocks_being_loaded.difference_update(None)` will be called, which raises a `TypeError` because the argument to `difference_update` must be an iterable.\n\nTo fix this, the `difference_update` call should be inside the `if block_hashes:` block to ensure `block_hashes` is not `None`.\n\n```suggestion\n            block_hashes = self._reqs_being_loaded.pop(req_id, None)\n            if block_hashes:\n                if self._blocks_being_loaded is not None:\n                    self._blocks_being_loaded.difference_update(block_hashes)\n                self.manager.complete_load(block_hashes)\n```\n\nOriginal Code Context:\n@@ -353,6 +384,8 @@ def update_connector_output(self, connector_output: KVConnectorOutput):\n \n         for req_id in connector_output.finished_recving or []:\n             block_hashes = self._reqs_being_loaded.pop(req_id, None)\n+            if self._blocks_being_loaded is not None:\n+                self._blocks_being_loaded.difference_update(block_hashes)\n             if block_hashes:\n                 self.manager.complete_load(block_hashes)\n", "materials": ["![critical](https://www.gstatic.com/codereviewagent/critical.svg)\n\nThere's a potential `TypeError` here. If `req_id` is not in `self._reqs_being_loaded`, `self._reqs_being_loaded.pop(req_id, None)` will return `None`. Subsequently, `self._blocks_being_loaded.difference_update(None)` will be called, which raises a `TypeError` because the argument to `difference_update` must be an iterable.\n\nTo fix this, the `difference_update` call should be inside the `if block_hashes:` block to ensure `block_hashes` is not `None`.\n\n```suggestion\n            block_hashes = self._reqs_being_loaded.pop(req_id, None)\n            if block_hashes:\n                if self._blocks_being_loaded is not None:\n                    self._blocks_being_loaded.difference_update(block_hashes)\n                self.manager.complete_load(block_hashes)\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -107,7 +107,7 @@ def get_finished(self, finished_req_ids: set[str]) -> tuple[set[str], set[str]]:\n \n     def get_num_new_matched_tokens(\n         self, request: \"Request\", num_computed_tokens: int\n-    ) -> tuple[int, bool]:\n+    ) -> tuple[int | None, bool]:\n         assert self.connector_scheduler is not None\n         return self.connector_scheduler.get_num_new_matched_tokens(\n             request, num_computed_tokens\n@@ -161,6 +161,11 @@ def __init__(self, spec: OffloadingSpec):\n         # request blocks are stored in order\n         # index of next block (of size offloaded_block_size) to offload\n         self._next_stored_block_idx: dict[ReqId, int] = {}\n+        # if GPU prefix caching is enabled,\n+        # track loaded blocks to avoid redundant loads\n+        self._blocks_being_loaded: set[BlockHash] | None = (\n+            set() if spec.vllm_config.cache_config.enable_prefix_caching else None\n+        )\n \n         # request ID -> set(block hashes being stored/load)\n         self._reqs_being_stored = defaultdict[ReqId, set[BlockHash]](set)\n@@ -181,7 +186,7 @@ def _get_block_hashes(\n \n     def get_num_new_matched_tokens(\n         self, request: Request, num_computed_tokens: int\n-    ) -> tuple[int, bool]:\n+    ) -> tuple[int | None, bool]:\n         \"\"\"\n         Get number of new tokens that can be loaded beyond the\n         num_computed_tokens.\n@@ -195,6 +200,9 @@ def get_num_new_matched_tokens(\n             A tuple with the following elements:\n                 - The number of tokens that can be loaded beyond what is\n                   already computed.\n+                  If None, it means that the connector needs more time to\n+                  determine the number of matched tokens, and the scheduler\n+                  should query for this request again later.\n                 - `True` if tokens will be loaded asynchronously\n                   (between scheduler steps).\n         \"\"\"\n@@ -214,6 +222,9 @@ def get_num_new_matched_tokens(\n         hits = self.manager.lookup(\n             self._get_block_hashes(request, start_idx=start_block_idx)\n         )\n+        if hits is None:\n+            # indicates a lookup that should be tried later\n+            return None, False\n         if hits == 0:\n             return 0, False\n \n@@ -229,6 +240,22 @@ def get_num_new_matched_tokens(\n         if num_hit_tokens < self.offloaded_block_size:\n             return 0, False\n \n+        if self._blocks_being_loaded:\n+            block_hashes = self._get_block_hashes(\n+                request, start_idx=start_block_idx, end_idx=start_block_idx + hits\n+            )\n+\n+            if any(\n+                block_hash in self._blocks_being_loaded for block_hash in block_hashes\n+            ):\n+                # hit blocks are being loaded, delay request\n+                logger.debug(\n+                    \"Delaying request %s since some of its blocks are already\"\n+                    \" being loaded\",\n+                    request.request_id,\n+                )\n+                return None, False\n+\n         return num_hit_tokens, True\n \n     def update_state_after_alloc(\n@@ -270,9 +297,13 @@ def update_state_after_alloc(\n         )\n \n         self._reqs_to_load[request.request_id] = (src_spec, dst_spec)\n-        self._reqs_being_loaded[request.request_id].update(block_hashes)\n+        req_blocks_being_loaded = self._reqs_being_loaded[request.request_id]\n+        req_blocks_being_loaded.update(block_hashes)\n         self._next_stored_block_idx[request.request_id] = num_blocks\n \n+        if self._blocks_being_loaded is not None:\n+            self._blocks_being_loaded.update(req_blocks_being_loaded)\n+\n     def _get_reqs_to_store(self, scheduler_output: SchedulerOutput):\n         reqs_to_store: dict[ReqId, TransferSpec] = {}\n         # iterate over both new and cached requests\n@@ -379,6 +410,8 @@ def update_connector_output(self, connector_output: KVConnectorOutput):\n         for req_id in connector_output.finished_recving or []:\n             block_hashes = self._reqs_being_loaded.pop(req_id, None)\n             if block_hashes:\n+                if self._blocks_being_loaded:\n+                    self._blocks_being_loaded.difference_update(block_hashes)\n                 self.manager.complete_load(block_hashes)\n \n     def request_finished(", "patch_sha256": "f09b72968893b90c"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29087/review_2678867334", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29087", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/distributed/kv_transfer/kv_connector/v1/offloading_connector.py\nReview Comment:\nAdd small comment here?\n\nOriginal Code Context:\n@@ -198,6 +206,8 @@ def get_num_new_matched_tokens(\n         hits = self.manager.lookup(\n             self._get_block_hashes(request, start_idx=start_block_idx)\n         )\n+        if hits is None:\n", "materials": ["Add small comment here?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -107,7 +107,7 @@ def get_finished(self, finished_req_ids: set[str]) -> tuple[set[str], set[str]]:\n \n     def get_num_new_matched_tokens(\n         self, request: \"Request\", num_computed_tokens: int\n-    ) -> tuple[int, bool]:\n+    ) -> tuple[int | None, bool]:\n         assert self.connector_scheduler is not None\n         return self.connector_scheduler.get_num_new_matched_tokens(\n             request, num_computed_tokens\n@@ -161,6 +161,11 @@ def __init__(self, spec: OffloadingSpec):\n         # request blocks are stored in order\n         # index of next block (of size offloaded_block_size) to offload\n         self._next_stored_block_idx: dict[ReqId, int] = {}\n+        # if GPU prefix caching is enabled,\n+        # track loaded blocks to avoid redundant loads\n+        self._blocks_being_loaded: set[BlockHash] | None = (\n+            set() if spec.vllm_config.cache_config.enable_prefix_caching else None\n+        )\n \n         # request ID -> set(block hashes being stored/load)\n         self._reqs_being_stored = defaultdict[ReqId, set[BlockHash]](set)\n@@ -181,7 +186,7 @@ def _get_block_hashes(\n \n     def get_num_new_matched_tokens(\n         self, request: Request, num_computed_tokens: int\n-    ) -> tuple[int, bool]:\n+    ) -> tuple[int | None, bool]:\n         \"\"\"\n         Get number of new tokens that can be loaded beyond the\n         num_computed_tokens.\n@@ -195,6 +200,9 @@ def get_num_new_matched_tokens(\n             A tuple with the following elements:\n                 - The number of tokens that can be loaded beyond what is\n                   already computed.\n+                  If None, it means that the connector needs more time to\n+                  determine the number of matched tokens, and the scheduler\n+                  should query for this request again later.\n                 - `True` if tokens will be loaded asynchronously\n                   (between scheduler steps).\n         \"\"\"\n@@ -214,6 +222,9 @@ def get_num_new_matched_tokens(\n         hits = self.manager.lookup(\n             self._get_block_hashes(request, start_idx=start_block_idx)\n         )\n+        if hits is None:\n+            # indicates a lookup that should be tried later\n+            return None, False\n         if hits == 0:\n             return 0, False\n \n@@ -229,6 +240,22 @@ def get_num_new_matched_tokens(\n         if num_hit_tokens < self.offloaded_block_size:\n             return 0, False\n \n+        if self._blocks_being_loaded:\n+            block_hashes = self._get_block_hashes(\n+                request, start_idx=start_block_idx, end_idx=start_block_idx + hits\n+            )\n+\n+            if any(\n+                block_hash in self._blocks_being_loaded for block_hash in block_hashes\n+            ):\n+                # hit blocks are being loaded, delay request\n+                logger.debug(\n+                    \"Delaying request %s since some of its blocks are already\"\n+                    \" being loaded\",\n+                    request.request_id,\n+                )\n+                return None, False\n+\n         return num_hit_tokens, True\n \n     def update_state_after_alloc(\n@@ -270,9 +297,13 @@ def update_state_after_alloc(\n         )\n \n         self._reqs_to_load[request.request_id] = (src_spec, dst_spec)\n-        self._reqs_being_loaded[request.request_id].update(block_hashes)\n+        req_blocks_being_loaded = self._reqs_being_loaded[request.request_id]\n+        req_blocks_being_loaded.update(block_hashes)\n         self._next_stored_block_idx[request.request_id] = num_blocks\n \n+        if self._blocks_being_loaded is not None:\n+            self._blocks_being_loaded.update(req_blocks_being_loaded)\n+\n     def _get_reqs_to_store(self, scheduler_output: SchedulerOutput):\n         reqs_to_store: dict[ReqId, TransferSpec] = {}\n         # iterate over both new and cached requests\n@@ -379,6 +410,8 @@ def update_connector_output(self, connector_output: KVConnectorOutput):\n         for req_id in connector_output.finished_recving or []:\n             block_hashes = self._reqs_being_loaded.pop(req_id, None)\n             if block_hashes:\n+                if self._blocks_being_loaded:\n+                    self._blocks_being_loaded.difference_update(block_hashes)\n                 self.manager.complete_load(block_hashes)\n \n     def request_finished(", "patch_sha256": "f09b72968893b90c"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29087/review_2678867909", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29087", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/distributed/kv_transfer/kv_connector/v1/offloading_connector.py\nReview Comment:\nnit: could store this in local var above to avoid looking up twice\n\nOriginal Code Context:\n@@ -257,6 +283,11 @@ def update_state_after_alloc(\n         self._reqs_being_loaded[request.request_id].update(block_hashes)\n         self._next_stored_block_idx[request.request_id] = num_blocks\n \n+        if self._blocks_being_loaded is not None:\n+            self._blocks_being_loaded.update(\n+                self._reqs_being_loaded[request.request_id]\n", "materials": ["nit: could store this in local var above to avoid looking up twice"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -107,7 +107,7 @@ def get_finished(self, finished_req_ids: set[str]) -> tuple[set[str], set[str]]:\n \n     def get_num_new_matched_tokens(\n         self, request: \"Request\", num_computed_tokens: int\n-    ) -> tuple[int, bool]:\n+    ) -> tuple[int | None, bool]:\n         assert self.connector_scheduler is not None\n         return self.connector_scheduler.get_num_new_matched_tokens(\n             request, num_computed_tokens\n@@ -161,6 +161,11 @@ def __init__(self, spec: OffloadingSpec):\n         # request blocks are stored in order\n         # index of next block (of size offloaded_block_size) to offload\n         self._next_stored_block_idx: dict[ReqId, int] = {}\n+        # if GPU prefix caching is enabled,\n+        # track loaded blocks to avoid redundant loads\n+        self._blocks_being_loaded: set[BlockHash] | None = (\n+            set() if spec.vllm_config.cache_config.enable_prefix_caching else None\n+        )\n \n         # request ID -> set(block hashes being stored/load)\n         self._reqs_being_stored = defaultdict[ReqId, set[BlockHash]](set)\n@@ -181,7 +186,7 @@ def _get_block_hashes(\n \n     def get_num_new_matched_tokens(\n         self, request: Request, num_computed_tokens: int\n-    ) -> tuple[int, bool]:\n+    ) -> tuple[int | None, bool]:\n         \"\"\"\n         Get number of new tokens that can be loaded beyond the\n         num_computed_tokens.\n@@ -195,6 +200,9 @@ def get_num_new_matched_tokens(\n             A tuple with the following elements:\n                 - The number of tokens that can be loaded beyond what is\n                   already computed.\n+                  If None, it means that the connector needs more time to\n+                  determine the number of matched tokens, and the scheduler\n+                  should query for this request again later.\n                 - `True` if tokens will be loaded asynchronously\n                   (between scheduler steps).\n         \"\"\"\n@@ -214,6 +222,9 @@ def get_num_new_matched_tokens(\n         hits = self.manager.lookup(\n             self._get_block_hashes(request, start_idx=start_block_idx)\n         )\n+        if hits is None:\n+            # indicates a lookup that should be tried later\n+            return None, False\n         if hits == 0:\n             return 0, False\n \n@@ -229,6 +240,22 @@ def get_num_new_matched_tokens(\n         if num_hit_tokens < self.offloaded_block_size:\n             return 0, False\n \n+        if self._blocks_being_loaded:\n+            block_hashes = self._get_block_hashes(\n+                request, start_idx=start_block_idx, end_idx=start_block_idx + hits\n+            )\n+\n+            if any(\n+                block_hash in self._blocks_being_loaded for block_hash in block_hashes\n+            ):\n+                # hit blocks are being loaded, delay request\n+                logger.debug(\n+                    \"Delaying request %s since some of its blocks are already\"\n+                    \" being loaded\",\n+                    request.request_id,\n+                )\n+                return None, False\n+\n         return num_hit_tokens, True\n \n     def update_state_after_alloc(\n@@ -270,9 +297,13 @@ def update_state_after_alloc(\n         )\n \n         self._reqs_to_load[request.request_id] = (src_spec, dst_spec)\n-        self._reqs_being_loaded[request.request_id].update(block_hashes)\n+        req_blocks_being_loaded = self._reqs_being_loaded[request.request_id]\n+        req_blocks_being_loaded.update(block_hashes)\n         self._next_stored_block_idx[request.request_id] = num_blocks\n \n+        if self._blocks_being_loaded is not None:\n+            self._blocks_being_loaded.update(req_blocks_being_loaded)\n+\n     def _get_reqs_to_store(self, scheduler_output: SchedulerOutput):\n         reqs_to_store: dict[ReqId, TransferSpec] = {}\n         # iterate over both new and cached requests\n@@ -379,6 +410,8 @@ def update_connector_output(self, connector_output: KVConnectorOutput):\n         for req_id in connector_output.finished_recving or []:\n             block_hashes = self._reqs_being_loaded.pop(req_id, None)\n             if block_hashes:\n+                if self._blocks_being_loaded:\n+                    self._blocks_being_loaded.difference_update(block_hashes)\n                 self.manager.complete_load(block_hashes)\n \n     def request_finished(", "patch_sha256": "f09b72968893b90c"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29087/review_2683246658", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29087", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/distributed/kv_transfer/kv_connector/v1/offloading_connector.py\nReview Comment:\nAlso skip if it's an empty set\r\n```suggestion\r\n        if self._blocks_being_loaded:\r\n```\n\nOriginal Code Context:\n@@ -229,6 +240,22 @@ def get_num_new_matched_tokens(\n         if num_hit_tokens < self.offloaded_block_size:\n             return 0, False\n \n+        if self._blocks_being_loaded is not None:\n", "materials": ["Also skip if it's an empty set\r\n```suggestion\r\n        if self._blocks_being_loaded:\r\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -107,7 +107,7 @@ def get_finished(self, finished_req_ids: set[str]) -> tuple[set[str], set[str]]:\n \n     def get_num_new_matched_tokens(\n         self, request: \"Request\", num_computed_tokens: int\n-    ) -> tuple[int, bool]:\n+    ) -> tuple[int | None, bool]:\n         assert self.connector_scheduler is not None\n         return self.connector_scheduler.get_num_new_matched_tokens(\n             request, num_computed_tokens\n@@ -161,6 +161,11 @@ def __init__(self, spec: OffloadingSpec):\n         # request blocks are stored in order\n         # index of next block (of size offloaded_block_size) to offload\n         self._next_stored_block_idx: dict[ReqId, int] = {}\n+        # if GPU prefix caching is enabled,\n+        # track loaded blocks to avoid redundant loads\n+        self._blocks_being_loaded: set[BlockHash] | None = (\n+            set() if spec.vllm_config.cache_config.enable_prefix_caching else None\n+        )\n \n         # request ID -> set(block hashes being stored/load)\n         self._reqs_being_stored = defaultdict[ReqId, set[BlockHash]](set)\n@@ -181,7 +186,7 @@ def _get_block_hashes(\n \n     def get_num_new_matched_tokens(\n         self, request: Request, num_computed_tokens: int\n-    ) -> tuple[int, bool]:\n+    ) -> tuple[int | None, bool]:\n         \"\"\"\n         Get number of new tokens that can be loaded beyond the\n         num_computed_tokens.\n@@ -195,6 +200,9 @@ def get_num_new_matched_tokens(\n             A tuple with the following elements:\n                 - The number of tokens that can be loaded beyond what is\n                   already computed.\n+                  If None, it means that the connector needs more time to\n+                  determine the number of matched tokens, and the scheduler\n+                  should query for this request again later.\n                 - `True` if tokens will be loaded asynchronously\n                   (between scheduler steps).\n         \"\"\"\n@@ -214,6 +222,9 @@ def get_num_new_matched_tokens(\n         hits = self.manager.lookup(\n             self._get_block_hashes(request, start_idx=start_block_idx)\n         )\n+        if hits is None:\n+            # indicates a lookup that should be tried later\n+            return None, False\n         if hits == 0:\n             return 0, False\n \n@@ -229,6 +240,22 @@ def get_num_new_matched_tokens(\n         if num_hit_tokens < self.offloaded_block_size:\n             return 0, False\n \n+        if self._blocks_being_loaded:\n+            block_hashes = self._get_block_hashes(\n+                request, start_idx=start_block_idx, end_idx=start_block_idx + hits\n+            )\n+\n+            if any(\n+                block_hash in self._blocks_being_loaded for block_hash in block_hashes\n+            ):\n+                # hit blocks are being loaded, delay request\n+                logger.debug(\n+                    \"Delaying request %s since some of its blocks are already\"\n+                    \" being loaded\",\n+                    request.request_id,\n+                )\n+                return None, False\n+\n         return num_hit_tokens, True\n \n     def update_state_after_alloc(\n@@ -270,9 +297,13 @@ def update_state_after_alloc(\n         )\n \n         self._reqs_to_load[request.request_id] = (src_spec, dst_spec)\n-        self._reqs_being_loaded[request.request_id].update(block_hashes)\n+        req_blocks_being_loaded = self._reqs_being_loaded[request.request_id]\n+        req_blocks_being_loaded.update(block_hashes)\n         self._next_stored_block_idx[request.request_id] = num_blocks\n \n+        if self._blocks_being_loaded is not None:\n+            self._blocks_being_loaded.update(req_blocks_being_loaded)\n+\n     def _get_reqs_to_store(self, scheduler_output: SchedulerOutput):\n         reqs_to_store: dict[ReqId, TransferSpec] = {}\n         # iterate over both new and cached requests\n@@ -379,6 +410,8 @@ def update_connector_output(self, connector_output: KVConnectorOutput):\n         for req_id in connector_output.finished_recving or []:\n             block_hashes = self._reqs_being_loaded.pop(req_id, None)\n             if block_hashes:\n+                if self._blocks_being_loaded:\n+                    self._blocks_being_loaded.difference_update(block_hashes)\n                 self.manager.complete_load(block_hashes)\n \n     def request_finished(", "patch_sha256": "f09b72968893b90c"}}], "heldout_tasks": [{"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2205426153", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/sampling_params.py\nReview Comment:\nCan we rename this to thinking_budget, would help provide consistency in naming since the max thinking here would refer to the thinking budget provided by the user.  \n\nOriginal Code Context:\n@@ -248,6 +249,9 @@ class SamplingParams(\n     bad_words: Optional[list[str]] = None\n     _bad_words_token_ids: Optional[list[list[int]]] = None\n \n+    # Maximum number of tokens allowed for thinking operations.\n+    max_think_tokens: Optional[int] = None\n", "materials": ["Can we rename this to thinking_budget, would help provide consistency in naming since the max thinking here would refer to the thinking budget provided by the user.  "], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2213421123", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/sampling_params.py\nReview Comment:\nYeah, that’s possible. But I’m also thinking that maybe in the future min_think_tokens option will be added, which forces the model to generate at least min_think_tokens number of 'think' tokens.\n\nOriginal Code Context:\n@@ -248,6 +249,9 @@ class SamplingParams(\n     bad_words: Optional[list[str]] = None\n     _bad_words_token_ids: Optional[list[list[int]]] = None\n \n+    # Maximum number of tokens allowed for thinking operations.\n+    max_think_tokens: Optional[int] = None\n", "materials": ["Yeah, that’s possible. But I’m also thinking that maybe in the future min_think_tokens option will be added, which forces the model to generate at least min_think_tokens number of 'think' tokens."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2215952048", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/sampling_params.py\nReview Comment:\nSimilar to your recommendation, I renamed it as `thinking_token_budget`.\n\nOriginal Code Context:\n@@ -248,6 +249,9 @@ class SamplingParams(\n     bad_words: Optional[list[str]] = None\n     _bad_words_token_ids: Optional[list[list[int]]] = None\n \n+    # Maximum number of tokens allowed for thinking operations.\n+    max_think_tokens: Optional[int] = None\n", "materials": ["Similar to your recommendation, I renamed it as `thinking_token_budget`."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2218469411", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/engine/arg_utils.py\nReview Comment:\nNot necessary, if you make `ReasoningConfig` a `dataclass`, as suggested, `argparse` will produce a `ReasoningConfig`, not a `dict`:\r\n\r\n```suggestion\r\n```\n\nOriginal Code Context:\n@@ -1272,6 +1276,11 @@ def create_engine_config(\n             collect_detailed_traces=self.collect_detailed_traces,\n         )\n \n+        reasoning_config = None\n+        if self.reasoning_config is not None:\n+            reasoning_config_dict = json.loads(self.reasoning_config)\n+            reasoning_config = ReasoningConfig(**reasoning_config_dict)\n+\n", "materials": ["Not necessary, if you make `ReasoningConfig` a `dataclass`, as suggested, `argparse` will produce a `ReasoningConfig`, not a `dict`:\r\n\r\n```suggestion\r\n```"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2219445390", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/engine/arg_utils.py\nReview Comment:\nThis might be better to avoid `Optional`:\r\n\r\n```suggestion\r\n    reasoning_config: ReasoningConfig = get_field(VllmConfig, \"reasoning_config\")\r\n```\n\nOriginal Code Context:\n@@ -418,6 +418,8 @@ class EngineArgs:\n     kv_transfer_config: Optional[KVTransferConfig] = None\n     kv_events_config: Optional[KVEventsConfig] = None\n \n+    reasoning_config: Optional[ReasoningConfig] = None\n", "materials": ["This might be better to avoid `Optional`:\r\n\r\n```suggestion\r\n    reasoning_config: ReasoningConfig = get_field(VllmConfig, \"reasoning_config\")\r\n```"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2400788394", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nThis is too verbose, i don't think user need these level of fine-grained controls.\n\nOriginal Code Context:\n@@ -0,0 +1,31 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from typing import Optional\n+\n+from pydantic.dataclasses import dataclass\n+\n+from vllm.config.utils import config\n+\n+\n+@config\n+@dataclass\n+class ReasoningConfig:\n", "materials": ["This is too verbose, i don't think user need these level of fine-grained controls."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2400797352", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nwe should also unify the reasoning config from the reasoning parser if you want to have a reasoning config like this in here.\n\nbut I think it is too verbose. We should get the id from the parser instead of maintaining a config\n\nOriginal Code Context:\n@@ -0,0 +1,31 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from typing import Optional\n+\n+from pydantic.dataclasses import dataclass\n+\n+from vllm.config.utils import config\n+\n+\n+@config\n+@dataclass\n+class ReasoningConfig:\n", "materials": ["we should also unify the reasoning config from the reasoning parser if you want to have a reasoning config like this in here.\n\nbut I think it is too verbose. We should get the id from the parser instead of maintaining a config"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2481391939", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\n```suggestion\r\n    @property\r\n    def is_thinking_enabled(self) -> bool:\r\n```\n\nOriginal Code Context:\n@@ -0,0 +1,48 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from typing import Optional\n+\n+from pydantic.dataclasses import dataclass\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.transformers_utils.tokenizer import init_tokenizer_from_configs\n+\n+\n+@config\n+@dataclass\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\"\"\"\n+\n+    think_start_str: Optional[str] = None\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: Optional[str] = None\n+    \"\"\"String that indicates the end of reasoning.\"\"\"\n+    think_start_token_ids: Optional[list[int]] = None\n+    \"\"\"Token ID that indicates the start of reasoning.\"\"\"\n+    think_end_token_ids: Optional[list[int]] = None\n+    \"\"\"Token ID that indicates the end of reasoning.\"\"\"\n+\n+    def is_thinking_enabled(self) -> bool:\n", "materials": ["```suggestion\r\n    @property\r\n    def is_thinking_enabled(self) -> bool:\r\n```"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2488570226", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nJust a question: how are cases with `think_start_str` containing regular expressions, such as `(?:Here's|Here is) my thought process`, handled?\n\n\nOriginal Code Context:\n@@ -0,0 +1,48 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+from pydantic.dataclasses import dataclass\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.transformers_utils.tokenizer import init_tokenizer_from_configs\n+\n+\n+@config\n+@dataclass\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\"\"\"\n+\n+    think_start_str: str | None = None\n", "materials": ["Just a question: how are cases with `think_start_str` containing regular expressions, such as `(?:Here's|Here is) my thought process`, handled?\n"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2496864142", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nI thought regular expressions or multiple think start strings could be possible scenarios. I believe once this PR is merged, those extensions can be applied later if needed.\n\nOriginal Code Context:\n@@ -0,0 +1,48 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+from pydantic.dataclasses import dataclass\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.transformers_utils.tokenizer import init_tokenizer_from_configs\n+\n+\n+@config\n+@dataclass\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\"\"\"\n+\n+    think_start_str: str | None = None\n", "materials": ["I thought regular expressions or multiple think start strings could be possible scenarios. I believe once this PR is merged, those extensions can be applied later if needed."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2661967606", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\n```suggestion\nfrom vllm.tokenizers import cached_tokenizer_from_config\n```\n\nOriginal Code Context:\n@@ -0,0 +1,48 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+from pydantic.dataclasses import dataclass\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.transformers_utils.tokenizer import init_tokenizer_from_configs\n", "materials": ["```suggestion\nfrom vllm.tokenizers import cached_tokenizer_from_config\n```"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2661970015", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\n```suggestion\n            tokenizer = cached_tokenizer_from_config(model_config=model_config)\n```\n\nOriginal Code Context:\n@@ -0,0 +1,48 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+from pydantic.dataclasses import dataclass\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.transformers_utils.tokenizer import init_tokenizer_from_configs\n+\n+\n+@config\n+@dataclass\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\"\"\"\n+\n+    think_start_str: str | None = None\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: str | None = None\n+    \"\"\"String that indicates the end of reasoning.\"\"\"\n+    think_start_token_ids: list[int] | None = None\n+    \"\"\"Token ID that indicates the start of reasoning.\"\"\"\n+    think_end_token_ids: list[int] | None = None\n+    \"\"\"Token ID that indicates the end of reasoning.\"\"\"\n+\n+    @property\n+    def is_thinking_enabled(self) -> bool:\n+        \"\"\"Check if both start and end thinking token IDs\n+        are set to enable thinking token budget logic.\"\"\"\n+        return (\n+            self.think_start_token_ids is not None\n+            and self.think_end_token_ids is not None\n+            and len(self.think_start_token_ids) > 0\n+            and len(self.think_end_token_ids) > 0\n+        )\n+\n+    def initialize_token_ids(self, model_config: ModelConfig) -> None:\n+        \"\"\"Initialize reasoning token IDs from strings using the tokenizer.\"\"\"\n+        if self.think_start_str is not None and self.think_end_str is not None:\n+            tokenizer = init_tokenizer_from_configs(model_config=model_config)\n", "materials": ["```suggestion\n            tokenizer = cached_tokenizer_from_config(model_config=model_config)\n```"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2662020545", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\n`init_tokenizer_from_configs` has already been removed and replaced by `cached_tokenizer_from_config`.\r\n@llsj14 \n\nOriginal Code Context:\n@@ -0,0 +1,48 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+from pydantic.dataclasses import dataclass\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.transformers_utils.tokenizer import init_tokenizer_from_configs\n", "materials": ["`init_tokenizer_from_configs` has already been removed and replaced by `cached_tokenizer_from_config`.\r\n@llsj14 "], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2788671081", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\n```suggestion\n```\n\nOriginal Code Context:\n@@ -0,0 +1,48 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+from pydantic.dataclasses import dataclass\n+\n", "materials": ["```suggestion\n```"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2788671645", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\n```suggestion\n```\n\nOriginal Code Context:\n@@ -0,0 +1,48 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\n+from pydantic.dataclasses import dataclass\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.tokenizers import cached_tokenizer_from_config\n+\n+\n+@config\n+@dataclass\n", "materials": ["```suggestion\n```"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2862925160", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/engine/arg_utils.py\nReview Comment:\nCould you write a usage guide for this as well?\n\nOriginal Code Context:\n@@ -1259,6 +1261,7 @@ def add_cli_args(parser: FlexibleArgumentParser) -> FlexibleArgumentParser:\n         vllm_group.add_argument(\n             \"--attention-config\", \"-ac\", **vllm_kwargs[\"attention_config\"]\n         )\n+        vllm_group.add_argument(\"--reasoning-config\", **vllm_kwargs[\"reasoning_config\"])\n", "materials": ["Could you write a usage guide for this as well?"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2863014569", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/engine/arg_utils.py\nReview Comment:\nyes I can.\r\nWould it be alright to add a guide to [docs/features/reasoning_outputs.md](https://github.com/vllm-project/vllm/blob/07bdabef03c78c9dc6beb549185930888ec6f2ce/docs/features/reasoning_outputs.md)?\r\n\r\n\n\nOriginal Code Context:\n@@ -1259,6 +1261,7 @@ def add_cli_args(parser: FlexibleArgumentParser) -> FlexibleArgumentParser:\n         vllm_group.add_argument(\n             \"--attention-config\", \"-ac\", **vllm_kwargs[\"attention_config\"]\n         )\n+        vllm_group.add_argument(\"--reasoning-config\", **vllm_kwargs[\"reasoning_config\"])\n", "materials": ["yes I can.\r\nWould it be alright to add a guide to [docs/features/reasoning_outputs.md](https://github.com/vllm-project/vllm/blob/07bdabef03c78c9dc6beb549185930888ec6f2ce/docs/features/reasoning_outputs.md)?\r\n\r\n"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2863512140", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/engine/arg_utils.py\nReview Comment:\nI just added docs for guides\n\nOriginal Code Context:\n@@ -1259,6 +1261,7 @@ def add_cli_args(parser: FlexibleArgumentParser) -> FlexibleArgumentParser:\n         vllm_group.add_argument(\n             \"--attention-config\", \"-ac\", **vllm_kwargs[\"attention_config\"]\n         )\n+        vllm_group.add_argument(\"--reasoning-config\", **vllm_kwargs[\"reasoning_config\"])\n", "materials": ["I just added docs for guides"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2893726765", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nI don't think these need to be checked here\n\nOriginal Code Context:\n@@ -0,0 +1,56 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from dataclasses import field\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.tokenizers import cached_tokenizer_from_config\n+\n+\n+@config\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\n+\n+    Set `think_start_str` and `think_end_str` to the strings that delimit\n+    the reasoning block (e.g. `\"<think>\"` and `\"</think>\"`).  The\n+    corresponding token IDs are derived automatically via\n+    `initialize_token_ids` and are not intended to be set directly.\n+    \"\"\"\n+\n+    think_start_str: str | None = None\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: str | None = None\n+    \"\"\"String that indicates the end of reasoning content.\"\"\"\n+\n+    think_start_token_ids: list[int] | None = field(\n+        default=None, init=False, repr=False\n+    )\n+    \"\"\"Token IDs derived from `think_start_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+    think_end_token_ids: list[int] | None = field(default=None, init=False, repr=False)\n+    \"\"\"Token IDs derived from `think_end_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+\n+    @property\n+    def is_thinking_enabled(self) -> bool:\n+        \"\"\"Check if thinking boundaries are configured.\"\"\"\n+        return (\n+            self.think_start_token_ids is not None\n+            and self.think_end_token_ids is not None\n", "materials": ["I don't think these need to be checked here"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2893744337", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\ncan encode be used for these?\n```suggestion\n            self.think_start_token_ids = tokenizer.encode(self.think_start_str)\n```\n\nOriginal Code Context:\n@@ -0,0 +1,56 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from dataclasses import field\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.tokenizers import cached_tokenizer_from_config\n+\n+\n+@config\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\n+\n+    Set `think_start_str` and `think_end_str` to the strings that delimit\n+    the reasoning block (e.g. `\"<think>\"` and `\"</think>\"`).  The\n+    corresponding token IDs are derived automatically via\n+    `initialize_token_ids` and are not intended to be set directly.\n+    \"\"\"\n+\n+    think_start_str: str | None = None\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: str | None = None\n+    \"\"\"String that indicates the end of reasoning content.\"\"\"\n+\n+    think_start_token_ids: list[int] | None = field(\n+        default=None, init=False, repr=False\n+    )\n+    \"\"\"Token IDs derived from `think_start_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+    think_end_token_ids: list[int] | None = field(default=None, init=False, repr=False)\n+    \"\"\"Token IDs derived from `think_end_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+\n+    @property\n+    def is_thinking_enabled(self) -> bool:\n+        \"\"\"Check if thinking boundaries are configured.\"\"\"\n+        return (\n+            self.think_start_token_ids is not None\n+            and self.think_end_token_ids is not None\n+            and len(self.think_start_token_ids) > 0\n+            and len(self.think_end_token_ids) > 0\n+        )\n+\n+    def initialize_token_ids(self, model_config: ModelConfig) -> None:\n+        \"\"\"Initialize reasoning token IDs from strings using the tokenizer.\"\"\"\n+        if self.think_start_str is not None and self.think_end_str is not None:\n+            tokenizer = cached_tokenizer_from_config(model_config=model_config)\n+\n+            # Convert reasoning strings to token IDs\n+            self.think_start_token_ids = tokenizer.convert_tokens_to_ids(\n+                tokenizer.tokenize(self.think_start_str)\n+            )\n", "materials": ["can encode be used for these?\n```suggestion\n            self.think_start_token_ids = tokenizer.encode(self.think_start_str)\n```"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2893761804", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nMake these required? \n\nIf `ReasoningConfig` is provided then I think these must be non-None right? Then thinking enabled can be equivalent to `vllm_config.reasoning_config is not None`\n\nOriginal Code Context:\n@@ -0,0 +1,56 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from dataclasses import field\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.tokenizers import cached_tokenizer_from_config\n+\n+\n+@config\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\n+\n+    Set `think_start_str` and `think_end_str` to the strings that delimit\n+    the reasoning block (e.g. `\"<think>\"` and `\"</think>\"`).  The\n+    corresponding token IDs are derived automatically via\n+    `initialize_token_ids` and are not intended to be set directly.\n+    \"\"\"\n+\n+    think_start_str: str | None = None\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: str | None = None\n+    \"\"\"String that indicates the end of reasoning content.\"\"\"\n", "materials": ["Make these required? \n\nIf `ReasoningConfig` is provided then I think these must be non-None right? Then thinking enabled can be equivalent to `vllm_config.reasoning_config is not None`"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2893822066", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nFrom UX pov, I feel it's much better/important to have these be determined automatically. We should be able to access the tokenizer config if that's where they are defined.\r\n\r\n>  I believe it needs an common interface and refactors on every reasoning parsers impelemntation\r\n\r\nAre all of these parser implementations within vLLM? If so I think we should do this. Claude/codex should be able to do it if asked in the right way.\r\n\r\n> Users may want to extend it with a custom prefix (e.g., \"I have to give the solution based on the thinking directly now.\"), which can't be statically determined from the model config.\r\n\r\nDo we know for sure that this will be needed? If not IMO it would be better to not expose these config parameters initially, they can always be added later if needed.\n\nOriginal Code Context:\n@@ -0,0 +1,44 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.tokenizers import cached_tokenizer_from_config\n+\n+\n+@config\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\"\"\"\n+\n+    think_start_str: str | None = None\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: str | None = None\n+    \"\"\"String that indicates the end of reasoning.\"\"\"\n", "materials": ["From UX pov, I feel it's much better/important to have these be determined automatically. We should be able to access the tokenizer config if that's where they are defined.\r\n\r\n>  I believe it needs an common interface and refactors on every reasoning parsers impelemntation\r\n\r\nAre all of these parser implementations within vLLM? If so I think we should do this. Claude/codex should be able to do it if asked in the right way.\r\n\r\n> Users may want to extend it with a custom prefix (e.g., \"I have to give the solution based on the thinking directly now.\"), which can't be statically determined from the model config.\r\n\r\nDo we know for sure that this will be needed? If not IMO it would be better to not expose these config parameters initially, they can always be added later if needed."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2901769561", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nThe same result appears with the Qwen models, but not with the Llama 3 models. If I use `tokenizer.encode` without `add_special_tokens=False`, the tokenizer adds a BOS token at the beginning of the token IDs. Could you explain the reason for replacing `convert_tokens_to_ids` with the `encode` method?\n\n```\n(APIServer pid=96689)   think_start_str='<think>'\n(APIServer pid=96689)   tokenize+convert : [14023, 771, 29]\n(APIServer pid=96689)   encode           : [128000, 14023, 771, 29]\n(APIServer pid=96689)   encode(add_special_tokens=False): [14023, 771, 29]\n(APIServer pid=96689)   think_end_str=' Now formulate the final answer.</think>'\n(APIServer pid=96689)   tokenize+convert : [4800, 89959, 279, 1620, 4320, 4005, 27963, 29]\n(APIServer pid=96689)   encode           : [128000, 4800, 89959, 279, 1620, 4320, 4005, 27963, 29]\n(APIServer pid=96689)   encode(add_special_tokens=False): [4800, 89959, 279, 1620, 4320, 4005, 27963, 29]\n```\n\n```\n    {\n      \"id\": 128000,\n      \"content\": \"<|begin_of_text|>\",\n      \"single_word\": false,\n      \"lstrip\": false,\n      \"rstrip\": false,\n      \"normalized\": false,\n      \"special\": true\n    },\n```\n\nOriginal Code Context:\n@@ -0,0 +1,56 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from dataclasses import field\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.tokenizers import cached_tokenizer_from_config\n+\n+\n+@config\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\n+\n+    Set `think_start_str` and `think_end_str` to the strings that delimit\n+    the reasoning block (e.g. `\"<think>\"` and `\"</think>\"`).  The\n+    corresponding token IDs are derived automatically via\n+    `initialize_token_ids` and are not intended to be set directly.\n+    \"\"\"\n+\n+    think_start_str: str | None = None\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: str | None = None\n+    \"\"\"String that indicates the end of reasoning content.\"\"\"\n+\n+    think_start_token_ids: list[int] | None = field(\n+        default=None, init=False, repr=False\n+    )\n+    \"\"\"Token IDs derived from `think_start_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+    think_end_token_ids: list[int] | None = field(default=None, init=False, repr=False)\n+    \"\"\"Token IDs derived from `think_end_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+\n+    @property\n+    def is_thinking_enabled(self) -> bool:\n+        \"\"\"Check if thinking boundaries are configured.\"\"\"\n+        return (\n+            self.think_start_token_ids is not None\n+            and self.think_end_token_ids is not None\n+            and len(self.think_start_token_ids) > 0\n+            and len(self.think_end_token_ids) > 0\n+        )\n+\n+    def initialize_token_ids(self, model_config: ModelConfig) -> None:\n+        \"\"\"Initialize reasoning token IDs from strings using the tokenizer.\"\"\"\n+        if self.think_start_str is not None and self.think_end_str is not None:\n+            tokenizer = cached_tokenizer_from_config(model_config=model_config)\n+\n+            # Convert reasoning strings to token IDs\n+            self.think_start_token_ids = tokenizer.convert_tokens_to_ids(\n+                tokenizer.tokenize(self.think_start_str)\n+            )\n", "materials": ["The same result appears with the Qwen models, but not with the Llama 3 models. If I use `tokenizer.encode` without `add_special_tokens=False`, the tokenizer adds a BOS token at the beginning of the token IDs. Could you explain the reason for replacing `convert_tokens_to_ids` with the `encode` method?\n\n```\n(APIServer pid=96689)   think_start_str='<think>'\n(APIServer pid=96689)   tokenize+convert : [14023, 771, 29]\n(APIServer pid=96689)   encode           : [128000, 14023, 771, 29]\n(APIServer pid=96689)   encode(add_special_tokens=False): [14023, 771, 29]\n(APIServer pid=96689)   think_end_str=' Now formulate the final answer.</think>'\n(APIServer pid=96689)   tokenize+convert : [4800, 89959, 279, 1620, 4320, 4005, 27963, 29]\n(APIServer pid=96689)   encode           : [128000, 4800, 89959, 279, 1620, 4320, 4005, 27963, 29]\n(APIServer pid=96689)   encode(add_special_tokens=False): [4800, 89959, 279, 1620, 4320, 4005, 27963, 29]\n```\n\n```\n    {\n      \"id\": 128000,\n      \"content\": \"<|begin_of_text|>\",\n      \"single_word\": false,\n      \"lstrip\": false,\n      \"rstrip\": false,\n      \"normalized\": false,\n      \"special\": true\n    },\n```"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2901784537", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nyes, I made them required and removed `is_thinking_enabled` because it became equivalent to `vllm_config.reasoning_config is not None`.\n\nOriginal Code Context:\n@@ -0,0 +1,56 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from dataclasses import field\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.tokenizers import cached_tokenizer_from_config\n+\n+\n+@config\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\n+\n+    Set `think_start_str` and `think_end_str` to the strings that delimit\n+    the reasoning block (e.g. `\"<think>\"` and `\"</think>\"`).  The\n+    corresponding token IDs are derived automatically via\n+    `initialize_token_ids` and are not intended to be set directly.\n+    \"\"\"\n+\n+    think_start_str: str | None = None\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: str | None = None\n+    \"\"\"String that indicates the end of reasoning content.\"\"\"\n", "materials": ["yes, I made them required and removed `is_thinking_enabled` because it became equivalent to `vllm_config.reasoning_config is not None`."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2901786203", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nAfter making think_start_str and think_end_str as required,\n`is_thinking_enabled` method is no more required, so I removed.\n\nOriginal Code Context:\n@@ -0,0 +1,56 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from dataclasses import field\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.tokenizers import cached_tokenizer_from_config\n+\n+\n+@config\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\n+\n+    Set `think_start_str` and `think_end_str` to the strings that delimit\n+    the reasoning block (e.g. `\"<think>\"` and `\"</think>\"`).  The\n+    corresponding token IDs are derived automatically via\n+    `initialize_token_ids` and are not intended to be set directly.\n+    \"\"\"\n+\n+    think_start_str: str | None = None\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: str | None = None\n+    \"\"\"String that indicates the end of reasoning content.\"\"\"\n+\n+    think_start_token_ids: list[int] | None = field(\n+        default=None, init=False, repr=False\n+    )\n+    \"\"\"Token IDs derived from `think_start_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+    think_end_token_ids: list[int] | None = field(default=None, init=False, repr=False)\n+    \"\"\"Token IDs derived from `think_end_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+\n+    @property\n+    def is_thinking_enabled(self) -> bool:\n+        \"\"\"Check if thinking boundaries are configured.\"\"\"\n+        return (\n+            self.think_start_token_ids is not None\n+            and self.think_end_token_ids is not None\n", "materials": ["After making think_start_str and think_end_str as required,\n`is_thinking_enabled` method is no more required, so I removed."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2901899380", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nI also think even if there is an automation, it could be configured by users too.\r\n\r\nFor example, if ReasoningParser doesn't have `special think_end_str`, with automation it could fall back to think_end_token, `</think>`.\r\n\r\nBut it is quite aggressive to end reasoning directly. Users may want to add custom sequence to end reasoning.\r\n\r\ne.g.)\r\n`\r\nReasoning: ... Let me convert them to the same decimal places to check. For example, 9.11 can be written as</think>`\r\n`\r\nContent: ...\r\n`\n\nOriginal Code Context:\n@@ -0,0 +1,44 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.tokenizers import cached_tokenizer_from_config\n+\n+\n+@config\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\"\"\"\n+\n+    think_start_str: str | None = None\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: str | None = None\n+    \"\"\"String that indicates the end of reasoning.\"\"\"\n", "materials": ["I also think even if there is an automation, it could be configured by users too.\r\n\r\nFor example, if ReasoningParser doesn't have `special think_end_str`, with automation it could fall back to think_end_token, `</think>`.\r\n\r\nBut it is quite aggressive to end reasoning directly. Users may want to add custom sequence to end reasoning.\r\n\r\ne.g.)\r\n`\r\nReasoning: ... Let me convert them to the same decimal places to check. For example, 9.11 can be written as</think>`\r\n`\r\nContent: ...\r\n`"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2926666553", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\n> Could you explain the reason for replacing `convert_tokens_to_ids` with the `encode` method?\r\n\r\nperformance .. we are making one call into rust without materializing intermediate python strings\r\n\r\nCan we use encode with `add_special_tokens=False`?\n\nOriginal Code Context:\n@@ -0,0 +1,56 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from dataclasses import field\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.tokenizers import cached_tokenizer_from_config\n+\n+\n+@config\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\n+\n+    Set `think_start_str` and `think_end_str` to the strings that delimit\n+    the reasoning block (e.g. `\"<think>\"` and `\"</think>\"`).  The\n+    corresponding token IDs are derived automatically via\n+    `initialize_token_ids` and are not intended to be set directly.\n+    \"\"\"\n+\n+    think_start_str: str | None = None\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: str | None = None\n+    \"\"\"String that indicates the end of reasoning content.\"\"\"\n+\n+    think_start_token_ids: list[int] | None = field(\n+        default=None, init=False, repr=False\n+    )\n+    \"\"\"Token IDs derived from `think_start_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+    think_end_token_ids: list[int] | None = field(default=None, init=False, repr=False)\n+    \"\"\"Token IDs derived from `think_end_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+\n+    @property\n+    def is_thinking_enabled(self) -> bool:\n+        \"\"\"Check if thinking boundaries are configured.\"\"\"\n+        return (\n+            self.think_start_token_ids is not None\n+            and self.think_end_token_ids is not None\n+            and len(self.think_start_token_ids) > 0\n+            and len(self.think_end_token_ids) > 0\n+        )\n+\n+    def initialize_token_ids(self, model_config: ModelConfig) -> None:\n+        \"\"\"Initialize reasoning token IDs from strings using the tokenizer.\"\"\"\n+        if self.think_start_str is not None and self.think_end_str is not None:\n+            tokenizer = cached_tokenizer_from_config(model_config=model_config)\n+\n+            # Convert reasoning strings to token IDs\n+            self.think_start_token_ids = tokenizer.convert_tokens_to_ids(\n+                tokenizer.tokenize(self.think_start_str)\n+            )\n", "materials": ["> Could you explain the reason for replacing `convert_tokens_to_ids` with the `encode` method?\r\n\r\nperformance .. we are making one call into rust without materializing intermediate python strings\r\n\r\nCan we use encode with `add_special_tokens=False`?"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2926766895", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nI think we need a check here that `think_start_token_ids` and `think_end_token_ids` are None.\n\nAnd we should perhaps rename them to start with an underscore and have `@property` accessors, as I think we do with other \"derived\" values in the config classes.\n\nOriginal Code Context:\n@@ -0,0 +1,53 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from dataclasses import field\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.tokenizers import cached_tokenizer_from_config\n+\n+\n+@config\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\n+\n+    Set `think_start_str` and `think_end_str` to the strings that delimit\n+    the reasoning block (e.g. `\"<think>\"` and `\"</think>\"`).  The\n+    corresponding token IDs are derived automatically via\n+    `initialize_token_ids` and are not intended to be set directly.\n+    \"\"\"\n+\n+    think_start_str: str = \"<think>\"\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: str = \"</think>\"\n+    \"\"\"String that indicates the end of reasoning content.\"\"\"\n+\n+    think_start_token_ids: list[int] | None = field(\n+        default=None, init=False, repr=False\n+    )\n+    \"\"\"Token IDs derived from `think_start_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+    think_end_token_ids: list[int] | None = field(default=None, init=False, repr=False)\n+    \"\"\"Token IDs derived from `think_end_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+\n+    def initialize_token_ids(self, model_config: ModelConfig) -> None:\n+        \"\"\"Initialize reasoning token IDs from strings using the tokenizer.\"\"\"\n", "materials": ["I think we need a check here that `think_start_token_ids` and `think_end_token_ids` are None.\n\nAnd we should perhaps rename them to start with an underscore and have `@property` accessors, as I think we do with other \"derived\" values in the config classes."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2926795248", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nMy main issue here is that we're exposing a new arg / config parameter externally that isn't really required, just because we don't want to go to the hassle of wiring up to the reasoning parsers.\r\n\r\nLet's at least add a comment explaining that setting the parameter shouldn't be required and is a temporary state, that the parameter will likely be removed in a subsequent version.\n\nOriginal Code Context:\n@@ -0,0 +1,44 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.tokenizers import cached_tokenizer_from_config\n+\n+\n+@config\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\"\"\"\n+\n+    think_start_str: str | None = None\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: str | None = None\n+    \"\"\"String that indicates the end of reasoning.\"\"\"\n", "materials": ["My main issue here is that we're exposing a new arg / config parameter externally that isn't really required, just because we don't want to go to the hassle of wiring up to the reasoning parsers.\r\n\r\nLet's at least add a comment explaining that setting the parameter shouldn't be required and is a temporary state, that the parameter will likely be removed in a subsequent version."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2926874231", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/sampling_params.py\nReview Comment:\nI think we need to add a check in the appropriate place to fail the request if `thinking_token_budget` is set but reasoning config is None (no logit processor initialized).\n\nOriginal Code Context:\n@@ -281,6 +281,8 @@ class SamplingParams(\n     _bad_words_token_ids: list[list[int]] | None = None\n \n     skip_reading_prefix_cache: bool | None = None\n+    thinking_token_budget: int | None = None\n", "materials": ["I think we need to add a check in the appropriate place to fail the request if `thinking_token_budget` is set but reasoning config is None (no logit processor initialized)."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2931106962", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nI see. I replaced it with encode method.\n\nOriginal Code Context:\n@@ -0,0 +1,56 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from dataclasses import field\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.tokenizers import cached_tokenizer_from_config\n+\n+\n+@config\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\n+\n+    Set `think_start_str` and `think_end_str` to the strings that delimit\n+    the reasoning block (e.g. `\"<think>\"` and `\"</think>\"`).  The\n+    corresponding token IDs are derived automatically via\n+    `initialize_token_ids` and are not intended to be set directly.\n+    \"\"\"\n+\n+    think_start_str: str | None = None\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: str | None = None\n+    \"\"\"String that indicates the end of reasoning content.\"\"\"\n+\n+    think_start_token_ids: list[int] | None = field(\n+        default=None, init=False, repr=False\n+    )\n+    \"\"\"Token IDs derived from `think_start_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+    think_end_token_ids: list[int] | None = field(default=None, init=False, repr=False)\n+    \"\"\"Token IDs derived from `think_end_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+\n+    @property\n+    def is_thinking_enabled(self) -> bool:\n+        \"\"\"Check if thinking boundaries are configured.\"\"\"\n+        return (\n+            self.think_start_token_ids is not None\n+            and self.think_end_token_ids is not None\n+            and len(self.think_start_token_ids) > 0\n+            and len(self.think_end_token_ids) > 0\n+        )\n+\n+    def initialize_token_ids(self, model_config: ModelConfig) -> None:\n+        \"\"\"Initialize reasoning token IDs from strings using the tokenizer.\"\"\"\n+        if self.think_start_str is not None and self.think_end_str is not None:\n+            tokenizer = cached_tokenizer_from_config(model_config=model_config)\n+\n+            # Convert reasoning strings to token IDs\n+            self.think_start_token_ids = tokenizer.convert_tokens_to_ids(\n+                tokenizer.tokenize(self.think_start_str)\n+            )\n", "materials": ["I see. I replaced it with encode method."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2931115551", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/sampling_params.py\nReview Comment:\nAdded a ValueError for that situation.\r\nhttps://github.com/vllm-project/vllm/pull/20859/commits/45bed67db63b95de1d205653ab25a7bf4be9b774\n\nOriginal Code Context:\n@@ -281,6 +281,8 @@ class SamplingParams(\n     _bad_words_token_ids: list[list[int]] | None = None\n \n     skip_reading_prefix_cache: bool | None = None\n+    thinking_token_budget: int | None = None\n", "materials": ["Added a ValueError for that situation.\r\nhttps://github.com/vllm-project/vllm/pull/20859/commits/45bed67db63b95de1d205653ab25a7bf4be9b774"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2931471064", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/config/reasoning.py\nReview Comment:\nIt is applied by this commit, https://github.com/vllm-project/vllm/pull/20859/changes/825217582e4266a2e325520a1cfb1171ba0aabc7\n\nOriginal Code Context:\n@@ -0,0 +1,53 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from dataclasses import field\n+\n+from vllm.config.model import ModelConfig\n+from vllm.config.utils import config\n+from vllm.tokenizers import cached_tokenizer_from_config\n+\n+\n+@config\n+class ReasoningConfig:\n+    \"\"\"Configuration for reasoning models.\n+\n+    Set `think_start_str` and `think_end_str` to the strings that delimit\n+    the reasoning block (e.g. `\"<think>\"` and `\"</think>\"`).  The\n+    corresponding token IDs are derived automatically via\n+    `initialize_token_ids` and are not intended to be set directly.\n+    \"\"\"\n+\n+    think_start_str: str = \"<think>\"\n+    \"\"\"String that indicates the start of reasoning.\"\"\"\n+    think_end_str: str = \"</think>\"\n+    \"\"\"String that indicates the end of reasoning content.\"\"\"\n+\n+    think_start_token_ids: list[int] | None = field(\n+        default=None, init=False, repr=False\n+    )\n+    \"\"\"Token IDs derived from `think_start_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+    think_end_token_ids: list[int] | None = field(default=None, init=False, repr=False)\n+    \"\"\"Token IDs derived from `think_end_str`. Set automatically by\n+    `initialize_token_ids`. Not intended to be configured directly.\"\"\"\n+\n+    def initialize_token_ids(self, model_config: ModelConfig) -> None:\n+        \"\"\"Initialize reasoning token IDs from strings using the tokenizer.\"\"\"\n", "materials": ["It is applied by this commit, https://github.com/vllm-project/vllm/pull/20859/changes/825217582e4266a2e325520a1cfb1171ba0aabc7"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#28631/review_2781700744", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28631", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/entrypoints/pooling/base/serving.py\nReview Comment:\nI want to catch and handle exceptions at the highest level, rather than handling them in every layer by using create_error_response.\r\n\r\nExceptions that many arise from very deep levels,  catch and handle them here. \r\n\r\nWe should have a universal standard—for example, specifying when to use ValueError, when to use TypeError, and when to use NotImplementedError....\r\n\n\nOriginal Code Context:\n@@ -0,0 +1,367 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+from collections.abc import AsyncGenerator, Mapping\n+from http import HTTPStatus\n+from typing import (\n+    ClassVar,\n+    TypeAlias,\n+    assert_never,\n+)\n+from wsgiref.headers import Headers\n+\n+from fastapi import Request\n+from starlette.responses import JSONResponse\n+\n+from vllm import (\n+    PoolingParams,\n+    PoolingRequestOutput,\n+    PromptType,\n+    SamplingParams,\n+    envs,\n+)\n+from vllm.engine.protocol import EngineClient\n+from vllm.entrypoints.logger import RequestLogger\n+from vllm.entrypoints.openai.engine.protocol import ErrorResponse, OpenAIBaseModel\n+from vllm.entrypoints.openai.engine.serving import (\n+    AnyRequest,\n+    AnyResponse,\n+    ServeContext,\n+)\n+from vllm.entrypoints.openai.models.serving import OpenAIServingModels\n+from vllm.entrypoints.openai.utils import create_error_response\n+from vllm.entrypoints.pooling.classify.protocol import ClassificationRequest\n+from vllm.lora.request import LoRARequest\n+from vllm.sampling_params import BeamSearchParams\n+from vllm.tracing import (\n+    contains_trace_headers,\n+    extract_trace_headers,\n+    log_tracing_disabled_warning,\n+)\n+from vllm.utils import random_uuid\n+from vllm.utils.async_utils import merge_async_iterators\n+\n+ClassificationServeContext: TypeAlias = ServeContext[ClassificationRequest]\n+\n+\n+class PoolingServing:\n+    request_id_prefix: ClassVar[str]\n+\n+    def __init__(\n+        self,\n+        engine_client: EngineClient,\n+        models: OpenAIServingModels,\n+        *,\n+        request_logger: RequestLogger | None,\n+        return_tokens_as_token_ids: bool = False,\n+        log_error_stack: bool = False,\n+    ):\n+        super().__init__()\n+\n+        self.engine_client = engine_client\n+\n+        self.models = models\n+\n+        self.request_logger = request_logger\n+        self.return_tokens_as_token_ids = return_tokens_as_token_ids\n+\n+        self.log_error_stack = True\n+\n+        self.input_processor = self.models.input_processor\n+        self.io_processor = self.models.io_processor\n+        self.renderer = self.models.renderer\n+        self.model_config = self.models.model_config\n+        self.max_model_len = self.model_config.max_model_len\n+\n+    async def __call__(\n+        self,\n+        request: OpenAIBaseModel,\n+        raw_request: Request,\n+    ):\n+        try:\n+            model_name = self.models.model_name()\n+            request_id = (\n+                f\"{self.request_id_prefix}-{self._base_request_id(raw_request)}\"\n+            )\n+\n+            await self._check_model(request, raw_request)\n+\n+            ctx = ClassificationServeContext(\n+                request=request,\n+                raw_request=raw_request,\n+                model_name=model_name,\n+                request_id=request_id,\n+            )\n+\n+            self._validate_request(ctx)\n+            ctx.lora_request = self._maybe_get_adapters(ctx.request)\n+\n+            await self._preprocess(ctx)\n+            await self._prepare_generators(ctx)\n+            await self._collect_batch(ctx)\n+            generator = await self._build_response(ctx)\n+        except Exception as e:\n+            generator = self.create_error_response(e)\n+\n+        if isinstance(generator, ErrorResponse):\n+            return JSONResponse(\n+                content=generator.model_dump(), status_code=generator.error.code\n+            )\n+        elif isinstance(generator, OpenAIBaseModel):\n+            return JSONResponse(content=generator.model_dump())\n+\n+        assert_never(generator)\n", "materials": ["I want to catch and handle exceptions at the highest level, rather than handling them in every layer by using create_error_response.\r\n\r\nExceptions that many arise from very deep levels,  catch and handle them here. \r\n\r\nWe should have a universal standard—for example, specifying when to use ValueError, when to use TypeError, and when to use NotImplementedError....\r\n"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#28631/review_2782003534", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28631", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/entrypoints/pooling/base/serving.py\nReview Comment:\nWe currently have a top level exception handler for the app, can use that instead?\n\nOriginal Code Context:\n@@ -0,0 +1,367 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+from collections.abc import AsyncGenerator, Mapping\n+from http import HTTPStatus\n+from typing import (\n+    ClassVar,\n+    TypeAlias,\n+    assert_never,\n+)\n+from wsgiref.headers import Headers\n+\n+from fastapi import Request\n+from starlette.responses import JSONResponse\n+\n+from vllm import (\n+    PoolingParams,\n+    PoolingRequestOutput,\n+    PromptType,\n+    SamplingParams,\n+    envs,\n+)\n+from vllm.engine.protocol import EngineClient\n+from vllm.entrypoints.logger import RequestLogger\n+from vllm.entrypoints.openai.engine.protocol import ErrorResponse, OpenAIBaseModel\n+from vllm.entrypoints.openai.engine.serving import (\n+    AnyRequest,\n+    AnyResponse,\n+    ServeContext,\n+)\n+from vllm.entrypoints.openai.models.serving import OpenAIServingModels\n+from vllm.entrypoints.openai.utils import create_error_response\n+from vllm.entrypoints.pooling.classify.protocol import ClassificationRequest\n+from vllm.lora.request import LoRARequest\n+from vllm.sampling_params import BeamSearchParams\n+from vllm.tracing import (\n+    contains_trace_headers,\n+    extract_trace_headers,\n+    log_tracing_disabled_warning,\n+)\n+from vllm.utils import random_uuid\n+from vllm.utils.async_utils import merge_async_iterators\n+\n+ClassificationServeContext: TypeAlias = ServeContext[ClassificationRequest]\n+\n+\n+class PoolingServing:\n+    request_id_prefix: ClassVar[str]\n+\n+    def __init__(\n+        self,\n+        engine_client: EngineClient,\n+        models: OpenAIServingModels,\n+        *,\n+        request_logger: RequestLogger | None,\n+        return_tokens_as_token_ids: bool = False,\n+        log_error_stack: bool = False,\n+    ):\n+        super().__init__()\n+\n+        self.engine_client = engine_client\n+\n+        self.models = models\n+\n+        self.request_logger = request_logger\n+        self.return_tokens_as_token_ids = return_tokens_as_token_ids\n+\n+        self.log_error_stack = True\n+\n+        self.input_processor = self.models.input_processor\n+        self.io_processor = self.models.io_processor\n+        self.renderer = self.models.renderer\n+        self.model_config = self.models.model_config\n+        self.max_model_len = self.model_config.max_model_len\n+\n+    async def __call__(\n+        self,\n+        request: OpenAIBaseModel,\n+        raw_request: Request,\n+    ):\n+        try:\n+            model_name = self.models.model_name()\n+            request_id = (\n+                f\"{self.request_id_prefix}-{self._base_request_id(raw_request)}\"\n+            )\n+\n+            await self._check_model(request, raw_request)\n+\n+            ctx = ClassificationServeContext(\n+                request=request,\n+                raw_request=raw_request,\n+                model_name=model_name,\n+                request_id=request_id,\n+            )\n+\n+            self._validate_request(ctx)\n+            ctx.lora_request = self._maybe_get_adapters(ctx.request)\n+\n+            await self._preprocess(ctx)\n+            await self._prepare_generators(ctx)\n+            await self._collect_batch(ctx)\n+            generator = await self._build_response(ctx)\n+        except Exception as e:\n+            generator = self.create_error_response(e)\n+\n+        if isinstance(generator, ErrorResponse):\n+            return JSONResponse(\n+                content=generator.model_dump(), status_code=generator.error.code\n+            )\n+        elif isinstance(generator, OpenAIBaseModel):\n+            return JSONResponse(content=generator.model_dump())\n+\n+        assert_never(generator)\n", "materials": ["We currently have a top level exception handler for the app, can use that instead?"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#28631/review_2782006615", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28631", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/entrypoints/pooling/base/serving.py\nReview Comment:\nThere was a PR that tried to do it: https://github.com/vllm-project/vllm/pull/31164\n\nOriginal Code Context:\n@@ -0,0 +1,367 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+from collections.abc import AsyncGenerator, Mapping\n+from http import HTTPStatus\n+from typing import (\n+    ClassVar,\n+    TypeAlias,\n+    assert_never,\n+)\n+from wsgiref.headers import Headers\n+\n+from fastapi import Request\n+from starlette.responses import JSONResponse\n+\n+from vllm import (\n+    PoolingParams,\n+    PoolingRequestOutput,\n+    PromptType,\n+    SamplingParams,\n+    envs,\n+)\n+from vllm.engine.protocol import EngineClient\n+from vllm.entrypoints.logger import RequestLogger\n+from vllm.entrypoints.openai.engine.protocol import ErrorResponse, OpenAIBaseModel\n+from vllm.entrypoints.openai.engine.serving import (\n+    AnyRequest,\n+    AnyResponse,\n+    ServeContext,\n+)\n+from vllm.entrypoints.openai.models.serving import OpenAIServingModels\n+from vllm.entrypoints.openai.utils import create_error_response\n+from vllm.entrypoints.pooling.classify.protocol import ClassificationRequest\n+from vllm.lora.request import LoRARequest\n+from vllm.sampling_params import BeamSearchParams\n+from vllm.tracing import (\n+    contains_trace_headers,\n+    extract_trace_headers,\n+    log_tracing_disabled_warning,\n+)\n+from vllm.utils import random_uuid\n+from vllm.utils.async_utils import merge_async_iterators\n+\n+ClassificationServeContext: TypeAlias = ServeContext[ClassificationRequest]\n+\n+\n+class PoolingServing:\n+    request_id_prefix: ClassVar[str]\n+\n+    def __init__(\n+        self,\n+        engine_client: EngineClient,\n+        models: OpenAIServingModels,\n+        *,\n+        request_logger: RequestLogger | None,\n+        return_tokens_as_token_ids: bool = False,\n+        log_error_stack: bool = False,\n+    ):\n+        super().__init__()\n+\n+        self.engine_client = engine_client\n+\n+        self.models = models\n+\n+        self.request_logger = request_logger\n+        self.return_tokens_as_token_ids = return_tokens_as_token_ids\n+\n+        self.log_error_stack = True\n+\n+        self.input_processor = self.models.input_processor\n+        self.io_processor = self.models.io_processor\n+        self.renderer = self.models.renderer\n+        self.model_config = self.models.model_config\n+        self.max_model_len = self.model_config.max_model_len\n+\n+    async def __call__(\n+        self,\n+        request: OpenAIBaseModel,\n+        raw_request: Request,\n+    ):\n+        try:\n+            model_name = self.models.model_name()\n+            request_id = (\n+                f\"{self.request_id_prefix}-{self._base_request_id(raw_request)}\"\n+            )\n+\n+            await self._check_model(request, raw_request)\n+\n+            ctx = ClassificationServeContext(\n+                request=request,\n+                raw_request=raw_request,\n+                model_name=model_name,\n+                request_id=request_id,\n+            )\n+\n+            self._validate_request(ctx)\n+            ctx.lora_request = self._maybe_get_adapters(ctx.request)\n+\n+            await self._preprocess(ctx)\n+            await self._prepare_generators(ctx)\n+            await self._collect_batch(ctx)\n+            generator = await self._build_response(ctx)\n+        except Exception as e:\n+            generator = self.create_error_response(e)\n+\n+        if isinstance(generator, ErrorResponse):\n+            return JSONResponse(\n+                content=generator.model_dump(), status_code=generator.error.code\n+            )\n+        elif isinstance(generator, OpenAIBaseModel):\n+            return JSONResponse(content=generator.model_dump())\n+\n+        assert_never(generator)\n", "materials": ["There was a PR that tried to do it: https://github.com/vllm-project/vllm/pull/31164"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#28631/review_3009068987", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28631", "domain": {"primary": "code", "secondary": "general", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/entrypoints/pooling/scoring/typing.py\nReview Comment:\nAs discussed before, prefer `ScoreInput | list[ScoreInput]` over` ScoreInputs`\n\nOriginal Code Context:\n@@ -0,0 +1,47 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+from dataclasses import dataclass\n+from typing import TypeAlias\n+\n+from typing_extensions import Required, TypedDict\n+\n+from vllm.entrypoints.chat_utils import (\n+    ChatCompletionContentPartImageEmbedsParam,\n+    ChatCompletionContentPartImageParam,\n+    ChatCompletionContentPartTextParam,\n+    ChatCompletionContentPartVideoParam,\n+)\n+\n+ScoreContentPartParam: TypeAlias = (\n+    ChatCompletionContentPartImageParam\n+    | ChatCompletionContentPartImageEmbedsParam\n+    | ChatCompletionContentPartTextParam\n+    | ChatCompletionContentPartVideoParam\n+)\n+\n+\n+class ScoreMultiModalParam(TypedDict, total=False):\n+    \"\"\"\n+    A specialized parameter type for scoring multimodal content\n+\n+    The reasons why don't reuse `CustomChatCompletionMessageParam` directly:\n+    1. Score tasks don't need the 'role' field (user/assistant/system) that's required in chat completions\n+    2. Including chat-specific fields would confuse users about their purpose in scoring\n+    3. This is a more focused interface that only exposes what's needed for scoring\n+    \"\"\"  # noqa: E501\n+\n+    content: Required[list[ScoreContentPartParam]]\n+    \"\"\"The multimodal contents\"\"\"\n+\n+\n+# Raw input data with content key in ScoreMultiModalParam.\n+ScoreInput = str | ScoreMultiModalParam\n+ScoreInputs = ScoreInput | list[ScoreInput]\n", "materials": ["As discussed before, prefer `ScoreInput | list[ScoreInput]` over` ScoreInputs`"], "expected_artifacts": ["code_patch"], "desired_output": null}], "example_pack_status": {"desired_outputs_applied": 168, "desired_outputs_missing": 0, "desired_outputs_rejected": 0, "rejections": []}}
{"schema_version": "example-pack/v1", "builder_version": "code-pack-builder/0.1", "pack_id": "codebench_vllm-project_vllm_docs", "split_id": "codebench::vllm::docs", "source": "CodeBench", "learning_problem": "few_shot_skill_induction", "domain": {"primary": "code", "secondary": "docs", "language": "python"}, "input_boundary": {"auto_skill_module_can_use": ["train_examples.task_input", "train_examples.materials", "train_examples.desired_output.text"], "must_not_use_for_induction": ["private rubrics", "private judge prompts", "heldout tasks", "heldout scores", "CI logs", "maintainer-only notes"]}, "train_examples": [{"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#20320/review_2688756448", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20320", "domain": {"primary": "code", "secondary": "docs", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: docs/features/lora.md\nReview Comment:\nYup! I also added warnings to the plugin itself:\r\n\r\nIf you set `VLLM_LORA_RESOLVER_HF_REPO_LIST` and don't have the plugin explicitly enabled:\r\n```\r\nWARNING 01-14 02:45:07 [hf_hub_resolver.py:130] It appears that VLLM_LORA_RESOLVER_HF_REPO_LIST is set, but lora_hf_hub_resolver is not enabled in VLLM_PLUGINS; you must enable this resolver directly in VLLM_PLUGINS to use it because it allows remote downloads.\r\n```\r\n\r\nAlso, if it is enabled, just to be safe:\r\n```\r\nWARNING 01-14 02:53:15 [hf_hub_resolver.py:19] LoRA is allowing resolution from the following repositories on HF Hub: ['ibm-granite/granite-3.3-8b-rag-agent-lib'] please note that allowing remote downloads is not secure, and that this plugin is not intended for use in production environments.\r\n```\n\nOriginal Code Context:\n@@ -162,10 +162,12 @@ Alternatively, you can use the LoRAResolver plugin to dynamically load LoRA adap\n \n You can set up multiple LoRAResolver plugins if you want to load LoRA adapters from different sources. For example, you might have one resolver for local files and another for S3 storage. vLLM will load the first LoRA adapter that it finds.\n \n-You can either install existing plugins or implement your own. By default, vLLM comes with a [resolver plugin to load LoRA adapters from a local directory.](https://github.com/vllm-project/vllm/tree/main/vllm/plugins/lora_resolvers)\n-To enable this resolver, set `VLLM_ALLOW_RUNTIME_LORA_UPDATING` to True, set `VLLM_PLUGINS` to include `lora_filesystem_resolver`, and then set `VLLM_LORA_RESOLVER_CACHE_DIR` to a local directory. When vLLM receives a request using a LoRA adapter `foobar`,\n-it will first look in the local directory for a directory `foobar`, and attempt to load the contents of that directory as a LoRA adapter. If successful, the request will complete as normal and\n-that adapter will then be available for normal use on the server.\n+You can either install existing plugins or implement your own. By default, vLLM comes with a [resolver plugin to load LoRA adapters from a local directory, as well as a resolver plugin to load LoRA adapters from repositories on Hugging Face Hub](https://github.com/vllm-project/vllm/tree/main/vllm/plugins/lora_resolvers)\n", "materials": ["Yup! I also added warnings to the plugin itself:\r\n\r\nIf you set `VLLM_LORA_RESOLVER_HF_REPO_LIST` and don't have the plugin explicitly enabled:\r\n```\r\nWARNING 01-14 02:45:07 [hf_hub_resolver.py:130] It appears that VLLM_LORA_RESOLVER_HF_REPO_LIST is set, but lora_hf_hub_resolver is not enabled in VLLM_PLUGINS; you must enable this resolver directly in VLLM_PLUGINS to use it because it allows remote downloads.\r\n```\r\n\r\nAlso, if it is enabled, just to be safe:\r\n```\r\nWARNING 01-14 02:53:15 [hf_hub_resolver.py:19] LoRA is allowing resolution from the following repositories on HF Hub: ['ibm-granite/granite-3.3-8b-rag-agent-lib'] please note that allowing remote downloads is not secure, and that this plugin is not intended for use in production environments.\r\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -162,10 +162,12 @@ Alternatively, you can use the LoRAResolver plugin to dynamically load LoRA adap\n \n You can set up multiple LoRAResolver plugins if you want to load LoRA adapters from different sources. For example, you might have one resolver for local files and another for S3 storage. vLLM will load the first LoRA adapter that it finds.\n \n-You can either install existing plugins or implement your own. By default, vLLM comes with a [resolver plugin to load LoRA adapters from a local directory.](https://github.com/vllm-project/vllm/tree/main/vllm/plugins/lora_resolvers)\n-To enable this resolver, set `VLLM_ALLOW_RUNTIME_LORA_UPDATING` to True, set `VLLM_PLUGINS` to include `lora_filesystem_resolver`, and then set `VLLM_LORA_RESOLVER_CACHE_DIR` to a local directory. When vLLM receives a request using a LoRA adapter `foobar`,\n-it will first look in the local directory for a directory `foobar`, and attempt to load the contents of that directory as a LoRA adapter. If successful, the request will complete as normal and\n-that adapter will then be available for normal use on the server.\n+You can either install existing plugins or implement your own. By default, vLLM comes with a [resolver plugin to load LoRA adapters from a local directory, as well as a resolver plugin to load LoRA adapters from repositories on Hugging Face Hub](https://github.com/vllm-project/vllm/tree/main/vllm/plugins/lora_resolvers)\n+To enable either of these resolvers, you must `set VLLM_ALLOW_RUNTIME_LORA_UPDATING` to True.\n+\n+- To leverage a local directory, set `VLLM_PLUGINS` to include `lora_filesystem_resolver` and set `VLLM_LORA_RESOLVER_CACHE_DIR` to a local directory. When vLLM receives a request using a LoRA adapter `foobar`,\n+it will first look in the local directory for a directory `foobar`, and attempt to load the contents of that directory as a LoRA adapter. If successful, the request will complete as normal and that adapter will then be available for normal use on the server.\n+- To leverage repositories on Hugging Face Hub, set `VLLM_PLUGINS` to include `lora_hf_hub_resolver` and set `VLLM_LORA_RESOLVER_HF_REPO_LIST` to a comma separated list of repository IDs on Hugging Face Hub. When vLLM receives a request for the LoRA adapter `my/repo/subpath`, it will download the adapter at the `subpath` of `my/repo` if it exists and contains an `adapter_config.json`, then build a request to the cached dir for the adapter, similar to the `lora_filesystem_resolver`. Please note that enabling remote downloads is insecure and not intended for use in production environments.\n \n Alternatively, follow these example steps to implement your own plugin:\n ", "patch_sha256": "1ee76e94c82800b0"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24498/review_2672528665", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24498", "domain": {"primary": "code", "secondary": "docs", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: docs/features/disagg_prefill.md\nReview Comment:\nnit: we should probably follow up with a more human readable way of expressing the value\n\nOriginal Code Context:\n@@ -37,10 +37,10 @@ For NixlConnector, you may also specify one or multiple NIXL_Backend. Such as:\n   --kv-transfer-config '{\"kv_connector\":\"NixlConnector\",\"kv_role\":\"kv_both\", \"kv_buffer_device\":\"cuda\", \"kv_connector_extra_config\":{\"backends\":[\"UCX\", \"GDS\"]}}'\n   ```\n \n-- **OffloadingConnector**: enable offloading of KV data to CPU memory, customizing the CPU block size (in tokens) and number of blocks to allocate (per worker):\n+- **OffloadingConnector**: enable offloading of KV data to CPU memory, customizing the CPU block size (in tokens) and total CPU memory bytes to allocate:\n \n   ```bash\n-  --kv-transfer-config '{\"kv_connector\":\"OffloadingConnector\",\"kv_role\":\"kv_both\",\"kv_connector_extra_config\":{\"block_size\": 64, \"num_cpu_blocks\": 1000}}'\n+  --kv-transfer-config '{\"kv_connector\":\"OffloadingConnector\",\"kv_role\":\"kv_both\",\"kv_connector_extra_config\":{\"block_size\": 64, \"cpu_bytes_to_use\": 1000000000}}'\n", "materials": ["nit: we should probably follow up with a more human readable way of expressing the value"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -37,10 +37,10 @@ For NixlConnector, you may also specify one or multiple NIXL_Backend. Such as:\n   --kv-transfer-config '{\"kv_connector\":\"NixlConnector\",\"kv_role\":\"kv_both\", \"kv_buffer_device\":\"cuda\", \"kv_connector_extra_config\":{\"backends\":[\"UCX\", \"GDS\"]}}'\n   ```\n \n-- **OffloadingConnector**: enable offloading of KV data to CPU memory, customizing the CPU block size (in tokens) and number of blocks to allocate (per worker):\n+- **OffloadingConnector**: enable offloading of KV data to CPU memory, customizing the CPU block size (in tokens) and total CPU memory bytes to allocate:\n \n   ```bash\n-  --kv-transfer-config '{\"kv_connector\":\"OffloadingConnector\",\"kv_role\":\"kv_both\",\"kv_connector_extra_config\":{\"block_size\": 64, \"num_cpu_blocks\": 1000}}'\n+  --kv-transfer-config '{\"kv_connector\":\"OffloadingConnector\",\"kv_role\":\"kv_both\",\"kv_connector_extra_config\":{\"block_size\": 64, \"cpu_bytes_to_use\": 1000000000}}'\n   ```\n \n ## Benchmarks", "patch_sha256": "617aa1b8450e204a"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24498/review_2678855443", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24498", "domain": {"primary": "code", "secondary": "docs", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: docs/features/disagg_prefill.md\nReview Comment:\nI'm open for suggestions :)\n\nOriginal Code Context:\n@@ -37,10 +37,10 @@ For NixlConnector, you may also specify one or multiple NIXL_Backend. Such as:\n   --kv-transfer-config '{\"kv_connector\":\"NixlConnector\",\"kv_role\":\"kv_both\", \"kv_buffer_device\":\"cuda\", \"kv_connector_extra_config\":{\"backends\":[\"UCX\", \"GDS\"]}}'\n   ```\n \n-- **OffloadingConnector**: enable offloading of KV data to CPU memory, customizing the CPU block size (in tokens) and number of blocks to allocate (per worker):\n+- **OffloadingConnector**: enable offloading of KV data to CPU memory, customizing the CPU block size (in tokens) and total CPU memory bytes to allocate:\n \n   ```bash\n-  --kv-transfer-config '{\"kv_connector\":\"OffloadingConnector\",\"kv_role\":\"kv_both\",\"kv_connector_extra_config\":{\"block_size\": 64, \"num_cpu_blocks\": 1000}}'\n+  --kv-transfer-config '{\"kv_connector\":\"OffloadingConnector\",\"kv_role\":\"kv_both\",\"kv_connector_extra_config\":{\"block_size\": 64, \"cpu_bytes_to_use\": 1000000000}}'\n", "materials": ["I'm open for suggestions :)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -37,10 +37,10 @@ For NixlConnector, you may also specify one or multiple NIXL_Backend. Such as:\n   --kv-transfer-config '{\"kv_connector\":\"NixlConnector\",\"kv_role\":\"kv_both\", \"kv_buffer_device\":\"cuda\", \"kv_connector_extra_config\":{\"backends\":[\"UCX\", \"GDS\"]}}'\n   ```\n \n-- **OffloadingConnector**: enable offloading of KV data to CPU memory, customizing the CPU block size (in tokens) and number of blocks to allocate (per worker):\n+- **OffloadingConnector**: enable offloading of KV data to CPU memory, customizing the CPU block size (in tokens) and total CPU memory bytes to allocate:\n \n   ```bash\n-  --kv-transfer-config '{\"kv_connector\":\"OffloadingConnector\",\"kv_role\":\"kv_both\",\"kv_connector_extra_config\":{\"block_size\": 64, \"num_cpu_blocks\": 1000}}'\n+  --kv-transfer-config '{\"kv_connector\":\"OffloadingConnector\",\"kv_role\":\"kv_both\",\"kv_connector_extra_config\":{\"block_size\": 64, \"cpu_bytes_to_use\": 1000000000}}'\n   ```\n \n ## Benchmarks", "patch_sha256": "617aa1b8450e204a"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24498/review_2682286091", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24498", "domain": {"primary": "code", "secondary": "docs", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: docs/features/disagg_prefill.md\nReview Comment:\nthinking about unifying with max-model-len format\r\n\n\nOriginal Code Context:\n@@ -37,10 +37,10 @@ For NixlConnector, you may also specify one or multiple NIXL_Backend. Such as:\n   --kv-transfer-config '{\"kv_connector\":\"NixlConnector\",\"kv_role\":\"kv_both\", \"kv_buffer_device\":\"cuda\", \"kv_connector_extra_config\":{\"backends\":[\"UCX\", \"GDS\"]}}'\n   ```\n \n-- **OffloadingConnector**: enable offloading of KV data to CPU memory, customizing the CPU block size (in tokens) and number of blocks to allocate (per worker):\n+- **OffloadingConnector**: enable offloading of KV data to CPU memory, customizing the CPU block size (in tokens) and total CPU memory bytes to allocate:\n \n   ```bash\n-  --kv-transfer-config '{\"kv_connector\":\"OffloadingConnector\",\"kv_role\":\"kv_both\",\"kv_connector_extra_config\":{\"block_size\": 64, \"num_cpu_blocks\": 1000}}'\n+  --kv-transfer-config '{\"kv_connector\":\"OffloadingConnector\",\"kv_role\":\"kv_both\",\"kv_connector_extra_config\":{\"block_size\": 64, \"cpu_bytes_to_use\": 1000000000}}'\n", "materials": ["thinking about unifying with max-model-len format\r\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -37,10 +37,10 @@ For NixlConnector, you may also specify one or multiple NIXL_Backend. Such as:\n   --kv-transfer-config '{\"kv_connector\":\"NixlConnector\",\"kv_role\":\"kv_both\", \"kv_buffer_device\":\"cuda\", \"kv_connector_extra_config\":{\"backends\":[\"UCX\", \"GDS\"]}}'\n   ```\n \n-- **OffloadingConnector**: enable offloading of KV data to CPU memory, customizing the CPU block size (in tokens) and number of blocks to allocate (per worker):\n+- **OffloadingConnector**: enable offloading of KV data to CPU memory, customizing the CPU block size (in tokens) and total CPU memory bytes to allocate:\n \n   ```bash\n-  --kv-transfer-config '{\"kv_connector\":\"OffloadingConnector\",\"kv_role\":\"kv_both\",\"kv_connector_extra_config\":{\"block_size\": 64, \"num_cpu_blocks\": 1000}}'\n+  --kv-transfer-config '{\"kv_connector\":\"OffloadingConnector\",\"kv_role\":\"kv_both\",\"kv_connector_extra_config\":{\"block_size\": 64, \"cpu_bytes_to_use\": 1000000000}}'\n   ```\n \n ## Benchmarks", "patch_sha256": "617aa1b8450e204a"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28784/review_2535238260", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28784", "domain": {"primary": "code", "secondary": "docs", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: docs/design/prefix_caching.md\nReview Comment:\nNice catch of the problem. But the version refactored by gemini is more clear to me.\n\nOriginal Code Context:\n@@ -22,8 +22,9 @@ In the example above, the KV cache in the first block can be uniquely identified\n     We only cache full blocks.\n \n !!! note \"Note 2\"\n-    The above hash key structure is not 100% collision free. Theoretically it’s still possible for the different prefix tokens to have the same hash value. To avoid any hash collisions **in a multi-tenant setup, we advise to use SHA256** as hash function instead of the default builtin hash.\n-    SHA256 is supported since vLLM v0.8.3 and must be enabled with a command line argument. It comes with a performance impact of about 100-200ns per token (~6ms for 50k tokens of context).\n+    In previous versions, the above hash key structure was not 100% collision free. As of v0.11, the default hash is \"sha256\", which uses Pickle for object serialization before hashing.\n+    For `vllm serve`, the relevant option to change this hashing function is `--prefix-caching-hash-algo {sha256,sha256_cbor}`. \"sha256_cbor\" provides a reproducible, cross-language compatible hash.\n", "materials": ["Nice catch of the problem. But the version refactored by gemini is more clear to me."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -22,8 +22,13 @@ In the example above, the KV cache in the first block can be uniquely identified\n     We only cache full blocks.\n \n !!! note \"Note 2\"\n-    The above hash key structure is not 100% collision free. Theoretically it’s still possible for the different prefix tokens to have the same hash value. To avoid any hash collisions **in a multi-tenant setup, we use SHA256** as hash function instead of the builtin hash.\n-    SHA256 is supported since vLLM v0.8.3 and the default since v0.10.2. It comes with a negligible performance impact of about 75ns per token (<4ms for 50k tokens of context).\n+    In previous versions, the hash key was not guaranteed to be collision-free. As of v0.11, the default hashing algorithm is `sha256`, which addresses collision risks.\n+\n+    For `vllm serve`, you can control the hashing algorithm via `--prefix-caching-hash-algo`:\n+    - `sha256` (default): Uses Python's `pickle` for serialization. Hashes may not be reproducible across different Python or vLLM versions.\n+    - `sha256_cbor`: Uses `cbor2` for serialization, providing a reproducible, cross-language compatible hash. This is recommended for deterministic caching across environments.\n+    - `xxhash`: `Uses Pickle serialization with xxHash (128-bit) for faster, non-cryptographic hashing. Requires the optional `xxhash` package. IMPORTANT: Use of a hashing algorithm that is not considered cryptographically secure theoretically increases the risk of hash collisions, which can cause undefined behavior or even leak private information in multi-tenant environments. Even if collisions are still very unlikely, it is important to consider your security risk tolerance against the performance benefits before turning this on.\n+    - `xxhash_cbor` combines canonical CBOR serialization with xxHash for reproducible hashing. Requires the optional `xxhash` package.    \n \n **A hashing example with multi-modality inputs**  \n In this example, we illustrate how prefix caching works with multi-modality inputs (e.g., images). Assuming we have a request with the following messages:", "patch_sha256": "1411b3509192f892"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29008/review_2639420817", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29008", "domain": {"primary": "code", "secondary": "docs", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/model_executor/layers/quantization/utils/ocp_mx_utils.py\nReview Comment:\nUpdate the PR description part, `W-MXFP4-A-MXFP8` will not be included in the PR scope.\n\nOriginal Code Context:\n@@ -20,26 +20,38 @@\n \n \n class OCP_MX_Scheme(str, Enum):\n+    w_mxfp4 = \"w_mxfp4\"\n     w_mxfp4_a_mxfp4 = \"w_mxfp4_a_mxfp4\"\n     w_mxfp4_a_mxfp6_e3m2 = \"w_mxfp4_a_mxfp6_e3m2\"\n     w_mxfp4_a_mxfp6_e2m3 = \"w_mxfp4_a_mxfp6_e2m3\"\n+    w_mxfp4_a_fp8 = \"w_mxfp4_a_fp8\"\n", "materials": ["Update the PR description part, `W-MXFP4-A-MXFP8` will not be included in the PR scope."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -20,26 +20,44 @@\n \n \n class OCP_MX_Scheme(str, Enum):\n+    w_mxfp4 = \"w_mxfp4\"\n     w_mxfp4_a_mxfp4 = \"w_mxfp4_a_mxfp4\"\n     w_mxfp4_a_mxfp6_e3m2 = \"w_mxfp4_a_mxfp6_e3m2\"\n     w_mxfp4_a_mxfp6_e2m3 = \"w_mxfp4_a_mxfp6_e2m3\"\n+    w_mxfp4_a_fp8 = \"w_mxfp4_a_fp8\"\n+    w_mxfp6_e3m2 = \"w_mxfp6_e3m2\"\n     w_mxfp6_e3m2_a_mxfp6_e3m2 = \"w_mxfp6_e3m2_a_mxfp6_e3m2\"\n+    w_mxfp6_e3m2_a_fp8 = \"w_mxfp6_e3m2_a_fp8\"\n+    w_mxfp6_e2m3 = \"w_mxfp6_e2m3\"\n     w_mxfp6_e2m3_a_mxfp6_e2m3 = \"w_mxfp6_e2m3_a_mxfp6_e2m3\"\n+    w_mxfp6_e2m3_a_fp8 = \"w_mxfp6_e2m3_a_fp8\"\n \n     @classmethod\n     def from_quant_dtype(cls, input_dtype: str | None, weight_dtype: str | None):\n-        if input_dtype not in OCP_MX_DTYPES or weight_dtype not in OCP_MX_DTYPES:\n+        if input_dtype not in OCP_MX_DTYPES and weight_dtype not in OCP_MX_DTYPES:\n             return None\n+        elif input_dtype is None and weight_dtype == \"mxfp4\":\n+            return cls.w_mxfp4\n+        elif input_dtype is None and weight_dtype == \"mxfp6_e3m2\":\n+            return cls.w_mxfp6_e3m2\n+        elif input_dtype is None and weight_dtype == \"mxfp6_e2m3\":\n+            return cls.w_mxfp6_e2m3\n         elif input_dtype == \"mxfp4\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp4\n         elif input_dtype == \"mxfp6_e3m2\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp6_e3m2\n         elif input_dtype == \"mxfp6_e2m3\" and weight_dtype == \"mxfp4\":\n             return cls.w_mxfp4_a_mxfp6_e2m3\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp4\":\n+            return cls.w_mxfp4_a_fp8\n         elif input_dtype == \"mxfp6_e3m2\" and weight_dtype == \"mxfp6_e3m2\":\n             return cls.w_mxfp6_e3m2_a_mxfp6_e3m2\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp6_e3m2\":\n+            return cls.w_mxfp6_e3m2_a_fp8\n         elif input_dtype == \"mxfp6_e2m3\" and weight_dtype == \"mxfp6_e2m3\":\n             return cls.w_mxfp6_e2m3_a_mxfp6_e2m3\n+        elif input_dtype == \"fp8\" and weight_dtype == \"mxfp6_e2m3\":\n+            return cls.w_mxfp6_e2m3_a_fp8\n         else:\n             logger.warning(\n                 \"input_dtype='%s' and\"", "patch_sha256": "43f3fe8f1fd07fb8"}}], "heldout_tasks": [], "example_pack_status": {"desired_outputs_applied": 6, "desired_outputs_missing": 0, "desired_outputs_rejected": 0, "rejections": []}}
{"schema_version": "example-pack/v1", "builder_version": "code-pack-builder/0.1", "pack_id": "codebench_vllm-project_vllm_generation", "split_id": "codebench::vllm::generation", "source": "CodeBench", "learning_problem": "few_shot_skill_induction", "domain": {"primary": "code", "secondary": "generation", "language": "python"}, "input_boundary": {"auto_skill_module_can_use": ["train_examples.task_input", "train_examples.materials", "train_examples.desired_output.text"], "must_not_use_for_induction": ["private rubrics", "private judge prompts", "heldout tasks", "heldout scores", "CI logs", "maintainer-only notes"]}, "train_examples": [{"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2433158693", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "generation", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/v1/worker/gpu_model_runner.py\nReview Comment:\n@smsegal I tested putting the draft model into a single rank, but it was complicated. This would be a very valuable follow-up optimization\n\nOriginal Code Context:\n@@ -281,6 +282,12 @@ def __init__(\n         if self.speculative_config and get_pp_group().is_last_rank:\n             if self.speculative_config.method == \"ngram\":\n                 self.drafter = NgramProposer(self.vllm_config)\n+            elif self.speculative_config.uses_draft_model():\n+                self.drafter = DraftModelProposer(\n+                    vllm_config=self.vllm_config,\n+                    device=self.device,\n+                    runner=self,\n+                )  # type: ignore\n", "materials": ["@smsegal I tested putting the draft model into a single rank, but it was complicated. This would be a very valuable follow-up optimization"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -145,6 +145,7 @@\n from vllm.v1.sample.metadata import SamplingMetadata\n from vllm.v1.sample.rejection_sampler import RejectionSampler\n from vllm.v1.sample.sampler import Sampler\n+from vllm.v1.spec_decode.draft_model import DraftModelProposer\n from vllm.v1.spec_decode.eagle import EagleProposer\n from vllm.v1.spec_decode.medusa import MedusaProposer\n from vllm.v1.spec_decode.metadata import SpecDecodeMetadata\n@@ -432,10 +433,20 @@ def __init__(\n         # layers in the draft model.\n         if self.speculative_config and get_pp_group().is_last_rank:\n             self.drafter: (\n-                NgramProposer | SuffixDecodingProposer | EagleProposer | MedusaProposer\n+                NgramProposer\n+                | SuffixDecodingProposer\n+                | EagleProposer\n+                | DraftModelProposer\n+                | MedusaProposer\n             )\n             if self.speculative_config.method == \"ngram\":\n                 self.drafter = NgramProposer(self.vllm_config)\n+            elif self.speculative_config.uses_draft_model():\n+                self.drafter = DraftModelProposer(\n+                    vllm_config=self.vllm_config,\n+                    device=self.device,\n+                    runner=self,\n+                )\n             elif self.speculative_config.method == \"suffix\":\n                 self.drafter = SuffixDecodingProposer(self.vllm_config)\n             elif self.speculative_config.use_eagle():\n@@ -3443,10 +3454,13 @@ def propose_draft_token_ids(sampled_token_ids):\n                 spec_decode_common_attn_metadata.max_seq_len + self.num_spec_tokens\n                 <= self.effective_drafter_max_model_len\n             )\n-            if spec_config.use_eagle() and not spec_config.disable_padded_drafter_batch:\n-                # EAGLE speculative decoding can use the GPU sampled tokens\n+            use_gpu_toks = (\n+                spec_config.use_eagle() or spec_config.uses_draft_model()\n+            ) and not spec_config.disable_padded_drafter_batch\n+            if use_gpu_toks:\n+                # EAGLE/DraftModel speculative decoding can use the GPU sampled tokens\n                 # as inputs, and does not need to wait for bookkeeping to finish.\n-                assert isinstance(self.drafter, EagleProposer)\n+                assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n                 sampled_token_ids = sampler_output.sampled_token_ids\n                 if input_fits_in_drafter:\n                     propose_draft_token_ids(sampled_token_ids)\n@@ -3679,8 +3693,8 @@ def propose_draft_token_ids(\n                 target_hidden_states=hidden_states,\n                 sampling_metadata=sampling_metadata,\n             )\n-        elif spec_config.use_eagle():\n-            assert isinstance(self.drafter, EagleProposer)\n+        elif spec_config.use_eagle() or spec_config.uses_draft_model():\n+            assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n \n             if spec_config.disable_padded_drafter_batch:\n                 # When padded-batch is disabled, the sampled_token_ids should be\n@@ -4475,8 +4489,12 @@ def _dummy_run(\n             else:\n                 hidden_states = outputs\n \n-            if self.speculative_config and self.speculative_config.use_eagle():\n-                assert isinstance(self.drafter, EagleProposer)\n+            if self.speculative_config and (\n+                self.speculative_config.use_eagle()\n+                or self.speculative_config.uses_draft_model()\n+            ):\n+                assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n+                assert self.speculative_config is not None\n                 # Eagle currently only supports PIECEWISE cudagraphs.\n                 # Therefore only use cudagraphs if the main model uses PIECEWISE\n                 # NOTE(lucas): this is a hack, need to clean up.\n@@ -5652,8 +5670,11 @@ def initialize_kv_cache(self, kv_cache_config: KVCacheConfig) -> None:\n             kv_cache_config, kernel_block_sizes\n         )\n \n-        if self.speculative_config and self.speculative_config.use_eagle():\n-            assert isinstance(self.drafter, EagleProposer)\n+        if self.speculative_config and (\n+            self.speculative_config.use_eagle()\n+            or self.speculative_config.uses_draft_model()\n+        ):\n+            assert isinstance(self.drafter, EagleProposer | DraftModelProposer)\n             # validate all draft model layers belong to the same kv cache\n             # group\n             self.drafter.validate_same_kv_cache_group(kv_cache_config)", "patch_sha256": "edac1e210171a53d"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#26291/review_2670063193", "source": "CodeBench", "source_task_id": "vllm-project/vllm#26291", "domain": {"primary": "code", "secondary": "generation", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/entrypoints/openai/chat_completion/serving.py\nReview Comment:\nI believe these changes are just to format the logged deltas in a more readable format as well as add reasoning to the logged deltas. They're not strictly required for this fix, but make debugging streaming deltas easier by improved logging output?\n\nOriginal Code Context:\n@@ -1127,17 +1135,23 @@ async def chat_completion_stream_generator(\n \n                     # Log streaming delta if output logging is enabled\n                     if self.enable_log_outputs and self.request_logger:\n-                        delta_content = \"\"\n+                        delta_content_parts = []\n                         if delta_message.content:\n-                            delta_content = delta_message.content\n-                        elif delta_message.tool_calls:\n-                            delta_content = \"\".join(\n+                            delta_content_parts.append(delta_message.content)\n+                        if delta_message.reasoning_content:\n+                            reasoning = delta_message.reasoning_content\n+                            delta_content_parts.append(f\"[reasoning: {reasoning}]\")\n+                        if delta_message.tool_calls:\n+                            tool_args = \"\".join(\n                                 tc.function.arguments\n                                 for tc in delta_message.tool_calls\n                                 if tc.function and tc.function.arguments\n                             )\n+                            if tool_args:\n+                                delta_content_parts.append(f\"[tool_calls: {tool_args}]\")\n \n-                        if delta_content and not self.exclude_log_deltas:\n+                        if delta_content_parts and not self.exclude_log_deltas:\n+                            delta_content = \" \".join(delta_content_parts)\n", "materials": ["I believe these changes are just to format the logged deltas in a more readable format as well as add reasoning to the logged deltas. They're not strictly required for this fix, but make debugging streaming deltas easier by improved logging output?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -36,6 +36,7 @@\n     ChatMessage,\n )\n from vllm.entrypoints.openai.chat_completion.stream_harmony import (\n+    TokenState,\n     extract_harmony_streaming_delta,\n )\n from vllm.entrypoints.openai.engine.protocol import (\n@@ -826,12 +827,22 @@ async def chat_completion_stream_generator(\n                     if self.use_harmony:\n                         harmony_parser = harmony_parsers[i]\n                         prev_recipient = harmony_parser.current_recipient\n-                        delta_text = \"\"\n+\n+                        # Track accumulated content per token with their state\n+                        token_states: list[TokenState] = []\n                         for token_id in output.token_ids:\n                             harmony_parser.process(token_id)\n-                            delta_text += harmony_parser.last_content_delta or \"\"\n+                            token_delta = harmony_parser.last_content_delta or \"\"\n+                            token_states.append(\n+                                TokenState(\n+                                    harmony_parser.current_channel,\n+                                    harmony_parser.current_recipient,\n+                                    token_delta,\n+                                )\n+                            )\n+                        delta_text = \"\".join(delta for _, _, delta in token_states)\n                         cur_channel = harmony_parser.current_channel\n-                        cur_recipient = harmony_parser.current_recipient\n+\n                         # handle the case where several tokens where generated at once\n                         # including the final token, leading to a delta in the text\n                         # but the current channel to be empty (start state)\n@@ -869,10 +880,8 @@ async def chat_completion_stream_generator(\n                         delta_message, tools_streamed_flag = (\n                             extract_harmony_streaming_delta(\n                                 harmony_parser=harmony_parser,\n-                                cur_channel=cur_channel,\n-                                cur_recipient=cur_recipient,\n+                                token_states=token_states,\n                                 prev_recipient=prev_recipient,\n-                                delta_text=delta_text,\n                                 include_reasoning=request.include_reasoning,\n                             )\n                         )\n@@ -1139,17 +1148,23 @@ async def chat_completion_stream_generator(\n \n                     # Log streaming delta if output logging is enabled\n                     if self.enable_log_outputs and self.request_logger:\n-                        delta_content = \"\"\n+                        delta_content_parts = []\n                         if delta_message.content:\n-                            delta_content = delta_message.content\n-                        elif delta_message.tool_calls:\n-                            delta_content = \"\".join(\n+                            delta_content_parts.append(delta_message.content)\n+                        if delta_message.reasoning_content:\n+                            reasoning = delta_message.reasoning_content\n+                            delta_content_parts.append(f\"[reasoning: {reasoning}]\")\n+                        if delta_message.tool_calls:\n+                            tool_args = \"\".join(\n                                 tc.function.arguments\n                                 for tc in delta_message.tool_calls\n                                 if tc.function and tc.function.arguments\n                             )\n+                            if tool_args:\n+                                delta_content_parts.append(f\"[tool_calls: {tool_args}]\")\n \n-                        if delta_content and self.enable_log_deltas:\n+                        if delta_content_parts and self.enable_log_deltas:\n+                            delta_content = \" \".join(delta_content_parts)\n                             self.request_logger.log_outputs(\n                                 request_id=request_id,\n                                 outputs=delta_content,", "patch_sha256": "25ad65935b5ed33e"}}], "heldout_tasks": [{"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2218469843", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "generation", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: vllm/engine/arg_utils.py\nReview Comment:\n```suggestion\r\n            reasoning_config=self.reasoning_config,\r\n```\n\nOriginal Code Context:\n@@ -1287,6 +1296,7 @@ def create_engine_config(\n             compilation_config=self.compilation_config,\n             kv_transfer_config=self.kv_transfer_config,\n             kv_events_config=self.kv_events_config,\n+            reasoning_config=reasoning_config,\n", "materials": ["```suggestion\r\n            reasoning_config=self.reasoning_config,\r\n```"], "expected_artifacts": ["code_patch"], "desired_output": null}], "example_pack_status": {"desired_outputs_applied": 2, "desired_outputs_missing": 0, "desired_outputs_rejected": 0, "rejections": []}}
{"schema_version": "example-pack/v1", "builder_version": "code-pack-builder/0.1", "pack_id": "codebench_vllm-project_vllm_tests", "split_id": "codebench::vllm::tests", "source": "CodeBench", "learning_problem": "few_shot_skill_induction", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "input_boundary": {"auto_skill_module_can_use": ["train_examples.task_input", "train_examples.materials", "train_examples.desired_output.text"], "must_not_use_for_induction": ["private rubrics", "private judge prompts", "heldout tasks", "heldout scores", "CI logs", "maintainer-only notes"]}, "train_examples": [{"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2688870852", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/v1/worker/test_utils.py\nReview Comment:\nWhat's the idea here?\n\nOriginal Code Context:\n@@ -55,3 +55,38 @@ def test_bind_kv_cache_non_attention(default_vllm_config):\n \n     assert runner_kv_caches[0] is kv_cache[\"model.layers.20.attn\"]\n     assert runner_kv_caches[1] is kv_cache[\"model.layers.28.attn\"]\n+\n+\n+def test_bind_kv_cache_draft_model():\n+    from vllm.attention import Attention\n+\n+    ctx = {\n+        \"model.layers.0.attn\": Attention(32, 128, 0.1),\n+        \"model.layers.1.attn\": Attention(32, 128, 0.1),\n+        \"draft_model.layers.0.attn\": Attention(32, 128, 0.1),\n+        \"draft_model.layers.1.attn\": Attention(32, 128, 0.1),\n+    }\n+    kv_cache = {\n+        \"model.layers.0.attn\": torch.zeros((1,)),\n+        \"model.layers.1.attn\": torch.zeros((1,)),\n+        \"draft_model.layers.0.attn\": torch.zeros((1,)),\n+        \"draft_model.layers.1.attn\": torch.zeros((1,)),\n+    }\n+    runner_kv_caches: list[torch.Tensor] = []\n+    bind_kv_cache(kv_cache, ctx, runner_kv_caches)\n+    assert ctx[\"model.layers.0.attn\"].kv_cache[0] is kv_cache[\"model.layers.0.attn\"]\n+    assert ctx[\"model.layers.1.attn\"].kv_cache[0] is kv_cache[\"model.layers.1.attn\"]\n+    assert (\n+        ctx[\"draft_model.layers.0.attn\"].kv_cache[0]\n+        is kv_cache[\"draft_model.layers.0.attn\"]\n+    )\n+    assert (\n+        ctx[\"draft_model.layers.1.attn\"].kv_cache[0]\n+        is kv_cache[\"draft_model.layers.1.attn\"]\n+    )\n+\n+    # caches are ordered by layer_index, interleaving target and draft model\n", "materials": ["What's the idea here?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -55,3 +55,38 @@ def test_bind_kv_cache_non_attention(default_vllm_config):\n \n     assert runner_kv_caches[0] is kv_cache[\"model.layers.20.attn\"]\n     assert runner_kv_caches[1] is kv_cache[\"model.layers.28.attn\"]\n+\n+\n+def test_bind_kv_cache_draft_model(default_vllm_config):\n+    from vllm.attention.layer import Attention\n+\n+    layer_names = [\n+        \"model.layers.0.attn\",\n+        \"model.layers.1.attn\",\n+        \"draft_model.layers.0.attn\",\n+        \"draft_model.layers.1.attn\",\n+    ]\n+    ctx = {\n+        layer_name: Attention(32, 128, 0.1, prefix=layer_name)\n+        for layer_name in layer_names\n+    }\n+    kv_cache = {layer_name: torch.zeros((1,)) for layer_name in layer_names}\n+    runner_kv_caches: list[torch.Tensor] = []\n+    bind_kv_cache(kv_cache, ctx, runner_kv_caches)\n+\n+    assert ctx[\"model.layers.0.attn\"].kv_cache[0] is kv_cache[\"model.layers.0.attn\"]\n+    assert ctx[\"model.layers.1.attn\"].kv_cache[0] is kv_cache[\"model.layers.1.attn\"]\n+    assert (\n+        ctx[\"draft_model.layers.0.attn\"].kv_cache[0]\n+        is kv_cache[\"draft_model.layers.0.attn\"]\n+    )\n+    assert (\n+        ctx[\"draft_model.layers.1.attn\"].kv_cache[0]\n+        is kv_cache[\"draft_model.layers.1.attn\"]\n+    )\n+\n+    # caches are ordered by layer_index, interleaving target and draft model\n+    assert runner_kv_caches[0] is kv_cache[\"model.layers.0.attn\"]\n+    assert runner_kv_caches[1] is kv_cache[\"draft_model.layers.0.attn\"]\n+    assert runner_kv_caches[2] is kv_cache[\"model.layers.1.attn\"]\n+    assert runner_kv_caches[3] is kv_cache[\"draft_model.layers.1.attn\"]", "patch_sha256": "869c059ee163870f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#24322/review_2690521350", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24322", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/v1/worker/test_utils.py\nReview Comment:\nThe ordering is arbitrary, there is nothing behind it. Let me know if I should include this in the comment.\n\nOriginal Code Context:\n@@ -55,3 +55,38 @@ def test_bind_kv_cache_non_attention(default_vllm_config):\n \n     assert runner_kv_caches[0] is kv_cache[\"model.layers.20.attn\"]\n     assert runner_kv_caches[1] is kv_cache[\"model.layers.28.attn\"]\n+\n+\n+def test_bind_kv_cache_draft_model():\n+    from vllm.attention import Attention\n+\n+    ctx = {\n+        \"model.layers.0.attn\": Attention(32, 128, 0.1),\n+        \"model.layers.1.attn\": Attention(32, 128, 0.1),\n+        \"draft_model.layers.0.attn\": Attention(32, 128, 0.1),\n+        \"draft_model.layers.1.attn\": Attention(32, 128, 0.1),\n+    }\n+    kv_cache = {\n+        \"model.layers.0.attn\": torch.zeros((1,)),\n+        \"model.layers.1.attn\": torch.zeros((1,)),\n+        \"draft_model.layers.0.attn\": torch.zeros((1,)),\n+        \"draft_model.layers.1.attn\": torch.zeros((1,)),\n+    }\n+    runner_kv_caches: list[torch.Tensor] = []\n+    bind_kv_cache(kv_cache, ctx, runner_kv_caches)\n+    assert ctx[\"model.layers.0.attn\"].kv_cache[0] is kv_cache[\"model.layers.0.attn\"]\n+    assert ctx[\"model.layers.1.attn\"].kv_cache[0] is kv_cache[\"model.layers.1.attn\"]\n+    assert (\n+        ctx[\"draft_model.layers.0.attn\"].kv_cache[0]\n+        is kv_cache[\"draft_model.layers.0.attn\"]\n+    )\n+    assert (\n+        ctx[\"draft_model.layers.1.attn\"].kv_cache[0]\n+        is kv_cache[\"draft_model.layers.1.attn\"]\n+    )\n+\n+    # caches are ordered by layer_index, interleaving target and draft model\n", "materials": ["The ordering is arbitrary, there is nothing behind it. Let me know if I should include this in the comment."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -55,3 +55,38 @@ def test_bind_kv_cache_non_attention(default_vllm_config):\n \n     assert runner_kv_caches[0] is kv_cache[\"model.layers.20.attn\"]\n     assert runner_kv_caches[1] is kv_cache[\"model.layers.28.attn\"]\n+\n+\n+def test_bind_kv_cache_draft_model(default_vllm_config):\n+    from vllm.attention.layer import Attention\n+\n+    layer_names = [\n+        \"model.layers.0.attn\",\n+        \"model.layers.1.attn\",\n+        \"draft_model.layers.0.attn\",\n+        \"draft_model.layers.1.attn\",\n+    ]\n+    ctx = {\n+        layer_name: Attention(32, 128, 0.1, prefix=layer_name)\n+        for layer_name in layer_names\n+    }\n+    kv_cache = {layer_name: torch.zeros((1,)) for layer_name in layer_names}\n+    runner_kv_caches: list[torch.Tensor] = []\n+    bind_kv_cache(kv_cache, ctx, runner_kv_caches)\n+\n+    assert ctx[\"model.layers.0.attn\"].kv_cache[0] is kv_cache[\"model.layers.0.attn\"]\n+    assert ctx[\"model.layers.1.attn\"].kv_cache[0] is kv_cache[\"model.layers.1.attn\"]\n+    assert (\n+        ctx[\"draft_model.layers.0.attn\"].kv_cache[0]\n+        is kv_cache[\"draft_model.layers.0.attn\"]\n+    )\n+    assert (\n+        ctx[\"draft_model.layers.1.attn\"].kv_cache[0]\n+        is kv_cache[\"draft_model.layers.1.attn\"]\n+    )\n+\n+    # caches are ordered by layer_index, interleaving target and draft model\n+    assert runner_kv_caches[0] is kv_cache[\"model.layers.0.attn\"]\n+    assert runner_kv_caches[1] is kv_cache[\"draft_model.layers.0.attn\"]\n+    assert runner_kv_caches[2] is kv_cache[\"model.layers.1.attn\"]\n+    assert runner_kv_caches[3] is kv_cache[\"draft_model.layers.1.attn\"]", "patch_sha256": "869c059ee163870f"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2549041787", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/v1/attention/utils.py\nReview Comment:\nqq: which unit tests can we run to ensure result is the same with and without `forward_includes_kv_cache` being set? \n\nOriginal Code Context:\n@@ -130,6 +130,18 @@ def try_get_attention_backend(\n         raise AssertionError(\"unreachable\") from None\n", "materials": ["qq: which unit tests can we run to ensure result is the same with and without `forward_includes_kv_cache` being set? "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -130,6 +130,18 @@ def try_get_attention_backend(\n         raise AssertionError(\"unreachable\") from None\n \n \n+def try_backend_includes_kv_cache_update(\n+    backend: AttentionBackendEnum,\n+) -> bool:\n+    \"\"\"Try to get the attention backend class, skipping test if not found.\"\"\"\n+    try:\n+        backend_class = backend.get_class()\n+        return backend_class.forward_includes_kv_cache_update\n+    except ImportError as e:\n+        pytest.skip(f\"{backend.name} not available: {e}\")\n+        raise AssertionError(\"unreachable\") from None\n+\n+\n def create_standard_kv_cache_spec(vllm_config: VllmConfig) -> FullAttentionSpec:\n     \"\"\"Create a FullAttentionSpec from ModelParams only.\"\"\"\n     return FullAttentionSpec(", "patch_sha256": "90c90ac60f7ca947"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2607416906", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/v1/attention/utils.py\nReview Comment:\nI still need to add a test that checks correctness, but since `forward_includes_kv_cache` will become specific to each backend, we would probably have to compare results between backends that do/don't do the KV-cache update outside their `forward` functions.\n\nOriginal Code Context:\n@@ -130,6 +130,18 @@ def try_get_attention_backend(\n         raise AssertionError(\"unreachable\") from None\n", "materials": ["I still need to add a test that checks correctness, but since `forward_includes_kv_cache` will become specific to each backend, we would probably have to compare results between backends that do/don't do the KV-cache update outside their `forward` functions."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -130,6 +130,18 @@ def try_get_attention_backend(\n         raise AssertionError(\"unreachable\") from None\n \n \n+def try_backend_includes_kv_cache_update(\n+    backend: AttentionBackendEnum,\n+) -> bool:\n+    \"\"\"Try to get the attention backend class, skipping test if not found.\"\"\"\n+    try:\n+        backend_class = backend.get_class()\n+        return backend_class.forward_includes_kv_cache_update\n+    except ImportError as e:\n+        pytest.skip(f\"{backend.name} not available: {e}\")\n+        raise AssertionError(\"unreachable\") from None\n+\n+\n def create_standard_kv_cache_spec(vllm_config: VllmConfig) -> FullAttentionSpec:\n     \"\"\"Create a FullAttentionSpec from ModelParams only.\"\"\"\n     return FullAttentionSpec(", "patch_sha256": "90c90ac60f7ca947"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2718873449", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/v1/attention/utils.py\nReview Comment:\nnit: `try_backend_includes_kv_cache` -> `try_backend_includes_kv_cache_update`\n\nOriginal Code Context:\n@@ -130,6 +130,18 @@ def try_get_attention_backend(\n         raise AssertionError(\"unreachable\") from None\n \n \n+def try_backend_includes_kv_cache(\n", "materials": ["nit: `try_backend_includes_kv_cache` -> `try_backend_includes_kv_cache_update`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -130,6 +130,18 @@ def try_get_attention_backend(\n         raise AssertionError(\"unreachable\") from None\n \n \n+def try_backend_includes_kv_cache_update(\n+    backend: AttentionBackendEnum,\n+) -> bool:\n+    \"\"\"Try to get the attention backend class, skipping test if not found.\"\"\"\n+    try:\n+        backend_class = backend.get_class()\n+        return backend_class.forward_includes_kv_cache_update\n+    except ImportError as e:\n+        pytest.skip(f\"{backend.name} not available: {e}\")\n+        raise AssertionError(\"unreachable\") from None\n+\n+\n def create_standard_kv_cache_spec(vllm_config: VllmConfig) -> FullAttentionSpec:\n     \"\"\"Create a FullAttentionSpec from ModelParams only.\"\"\"\n     return FullAttentionSpec(", "patch_sha256": "90c90ac60f7ca947"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#25954/review_2718979484", "source": "CodeBench", "source_task_id": "vllm-project/vllm#25954", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/v1/attention/utils.py\nReview Comment:\nDone in [238ae7e](https://github.com/vllm-project/vllm/pull/25954/commits/238ae7e0fcc3d9e28e4a685c2e18da35698777d0)\n\nOriginal Code Context:\n@@ -130,6 +130,18 @@ def try_get_attention_backend(\n         raise AssertionError(\"unreachable\") from None\n \n \n+def try_backend_includes_kv_cache(\n", "materials": ["Done in [238ae7e](https://github.com/vllm-project/vllm/pull/25954/commits/238ae7e0fcc3d9e28e4a685c2e18da35698777d0)"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -130,6 +130,18 @@ def try_get_attention_backend(\n         raise AssertionError(\"unreachable\") from None\n \n \n+def try_backend_includes_kv_cache_update(\n+    backend: AttentionBackendEnum,\n+) -> bool:\n+    \"\"\"Try to get the attention backend class, skipping test if not found.\"\"\"\n+    try:\n+        backend_class = backend.get_class()\n+        return backend_class.forward_includes_kv_cache_update\n+    except ImportError as e:\n+        pytest.skip(f\"{backend.name} not available: {e}\")\n+        raise AssertionError(\"unreachable\") from None\n+\n+\n def create_standard_kv_cache_spec(vllm_config: VllmConfig) -> FullAttentionSpec:\n     \"\"\"Create a FullAttentionSpec from ModelParams only.\"\"\"\n     return FullAttentionSpec(", "patch_sha256": "90c90ac60f7ca947"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2487969666", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/compile/test_fusion_attn.py\nReview Comment:\nWith the utility, this should look like:\n```suggestion\n        return self.fp8_linear(attn_output)\n```\n\nOriginal Code Context:\n@@ -190,12 +204,8 @@ def __init__(self, *args, **kwargs):\n     def forward(self, q: torch.Tensor, k: torch.Tensor, v: torch.Tensor):\n         \"\"\"Forward pass that creates the pattern to be fused.\"\"\"\n         attn_output = self.attn(q, k, v)\n-        return self.fp8_linear.apply(\n-            input=attn_output,\n-            weight=self.w[\"weight\"],\n-            weight_scale=self.w[\"wscale\"],\n-            input_scale=self.w[\"scale\"],\n-        )\n+        layer = TestFP8Layer(self.w[\"weight\"], self.w[\"wscale\"], self.w[\"scale\"])\n+        return self.fp8_linear.apply_weights(layer, attn_output)\n", "materials": ["With the utility, this should look like:\n```suggestion\n        return self.fp8_linear(attn_output)\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -45,14 +45,15 @@\n     kFp8StaticTensorSym,\n     kNvfp4Quant,\n )\n-from vllm.model_executor.layers.quantization.utils.w8a8_utils import Fp8LinearOp\n from vllm.platforms import current_platform\n from vllm.utils.flashinfer import has_flashinfer\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n from vllm.v1.attention.backend import AttentionMetadata\n from vllm.v1.attention.backends.registry import AttentionBackendEnum\n from vllm.v1.kv_cache_interface import AttentionSpec\n \n+from ..utils import TestFP8Layer\n+\n FP8_DTYPE = current_platform.fp8_dtype()\n FP4_DTYPE = torch.uint8\n \n@@ -185,32 +186,30 @@ class TestAttentionFp8StaticQuantPatternModel(AttentionQuantPatternModel):\n     def __init__(self, *args, **kwargs):\n         super().__init__(*args, **kwargs)\n \n-        self.fp8_linear = Fp8LinearOp(\n-            act_quant_static=self.quant_key.scale.static,\n-            act_quant_group_shape=self.quant_key.scale.group_shape,\n-        )\n-\n         hidden_size = self.num_qo_heads * self.head_size\n-        self.w = kwargs.get(\n-            \"w\",\n-            {\n-                \"weight\": torch.randn(hidden_size, hidden_size)\n-                .to(dtype=FP8_DTYPE, device=self.device)\n-                .t(),\n-                \"wscale\": torch.tensor([1.0], dtype=torch.float32, device=self.device),\n-                \"scale\": torch.tensor([1.0], dtype=torch.float32, device=self.device),\n-            },\n+        self.fp8_linear = TestFP8Layer(\n+            weight_shape=(hidden_size, hidden_size),\n+            activation_quant_key=self.quant_key,\n+            weight_quant_key=self.quant_key,\n+            device=self.device,\n         )\n \n+        w = kwargs.get(\"w\")\n+        if w is not None:\n+            self.fp8_linear.weight = w[\"weight\"]\n+            self.fp8_linear.weight_scale = w[\"wscale\"]\n+            self.fp8_linear.input_scale = w[\"scale\"]\n+\n+        self.w = {\n+            \"weight\": self.fp8_linear.weight,\n+            \"wscale\": self.fp8_linear.weight_scale,\n+            \"scale\": self.fp8_linear.input_scale,\n+        }\n+\n     def forward(self, q: torch.Tensor, k: torch.Tensor, v: torch.Tensor):\n         \"\"\"Forward pass that creates the pattern to be fused.\"\"\"\n         attn_output = self.attn(q, k, v)\n-        return self.fp8_linear.apply(\n-            input=attn_output,\n-            weight=self.w[\"weight\"],\n-            weight_scale=self.w[\"wscale\"],\n-            input_scale=self.w[\"scale\"],\n-        )\n+        return self.fp8_linear(attn_output)\n \n \n class TestAttentionNvfp4QuantPatternModel(AttentionQuantPatternModel):", "patch_sha256": "55010739ad1dc048"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2487970831", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/compile/test_fusion_attn.py\nReview Comment:\nCan you add a utility that can be reused across fusion tests that constructs the layer, passes it to the kernel, etc.\n\nOriginal Code Context:\n@@ -170,11 +177,18 @@ class TestAttentionFp8StaticQuantPatternModel(AttentionQuantPatternModel):\n     def __init__(self, *args, **kwargs):\n         super().__init__(*args, **kwargs)\n \n-        self.fp8_linear = Fp8LinearOp(\n-            act_quant_static=self.quant_key.scale.static,\n-            act_quant_group_shape=self.quant_key.scale.group_shape,\n+        if self.quant_key.scale.group_shape.is_per_tensor():\n+            weight_quant_strategy = ScaledMMLinearQuantStrategy.TENSOR\n+        else:\n+            weight_quant_strategy = ScaledMMLinearQuantStrategy.CHANNEL\n+\n+        self.fp8_linear = init_fp8_linear_kernel(\n+            act_q_static=self.quant_key.scale.static,\n+            act_q_group_shape=self.quant_key.scale.group_shape,\n+            weight_quant_strategy=weight_quant_strategy,\n+            out_dtype=torch.get_default_dtype(),\n+            module_name=self.__class__.__name__,\n", "materials": ["Can you add a utility that can be reused across fusion tests that constructs the layer, passes it to the kernel, etc."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -45,14 +45,15 @@\n     kFp8StaticTensorSym,\n     kNvfp4Quant,\n )\n-from vllm.model_executor.layers.quantization.utils.w8a8_utils import Fp8LinearOp\n from vllm.platforms import current_platform\n from vllm.utils.flashinfer import has_flashinfer\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n from vllm.v1.attention.backend import AttentionMetadata\n from vllm.v1.attention.backends.registry import AttentionBackendEnum\n from vllm.v1.kv_cache_interface import AttentionSpec\n \n+from ..utils import TestFP8Layer\n+\n FP8_DTYPE = current_platform.fp8_dtype()\n FP4_DTYPE = torch.uint8\n \n@@ -185,32 +186,30 @@ class TestAttentionFp8StaticQuantPatternModel(AttentionQuantPatternModel):\n     def __init__(self, *args, **kwargs):\n         super().__init__(*args, **kwargs)\n \n-        self.fp8_linear = Fp8LinearOp(\n-            act_quant_static=self.quant_key.scale.static,\n-            act_quant_group_shape=self.quant_key.scale.group_shape,\n-        )\n-\n         hidden_size = self.num_qo_heads * self.head_size\n-        self.w = kwargs.get(\n-            \"w\",\n-            {\n-                \"weight\": torch.randn(hidden_size, hidden_size)\n-                .to(dtype=FP8_DTYPE, device=self.device)\n-                .t(),\n-                \"wscale\": torch.tensor([1.0], dtype=torch.float32, device=self.device),\n-                \"scale\": torch.tensor([1.0], dtype=torch.float32, device=self.device),\n-            },\n+        self.fp8_linear = TestFP8Layer(\n+            weight_shape=(hidden_size, hidden_size),\n+            activation_quant_key=self.quant_key,\n+            weight_quant_key=self.quant_key,\n+            device=self.device,\n         )\n \n+        w = kwargs.get(\"w\")\n+        if w is not None:\n+            self.fp8_linear.weight = w[\"weight\"]\n+            self.fp8_linear.weight_scale = w[\"wscale\"]\n+            self.fp8_linear.input_scale = w[\"scale\"]\n+\n+        self.w = {\n+            \"weight\": self.fp8_linear.weight,\n+            \"wscale\": self.fp8_linear.weight_scale,\n+            \"scale\": self.fp8_linear.input_scale,\n+        }\n+\n     def forward(self, q: torch.Tensor, k: torch.Tensor, v: torch.Tensor):\n         \"\"\"Forward pass that creates the pattern to be fused.\"\"\"\n         attn_output = self.attn(q, k, v)\n-        return self.fp8_linear.apply(\n-            input=attn_output,\n-            weight=self.w[\"weight\"],\n-            weight_scale=self.w[\"wscale\"],\n-            input_scale=self.w[\"scale\"],\n-        )\n+        return self.fp8_linear(attn_output)\n \n \n class TestAttentionNvfp4QuantPatternModel(AttentionQuantPatternModel):", "patch_sha256": "55010739ad1dc048"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2487972381", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/compile/test_fusion_attn.py\nReview Comment:\nAlso it should be easy to select each (supported) implementation so that tests can test with all of them,\n\nOriginal Code Context:\n@@ -170,11 +177,18 @@ class TestAttentionFp8StaticQuantPatternModel(AttentionQuantPatternModel):\n     def __init__(self, *args, **kwargs):\n         super().__init__(*args, **kwargs)\n \n-        self.fp8_linear = Fp8LinearOp(\n-            act_quant_static=self.quant_key.scale.static,\n-            act_quant_group_shape=self.quant_key.scale.group_shape,\n+        if self.quant_key.scale.group_shape.is_per_tensor():\n+            weight_quant_strategy = ScaledMMLinearQuantStrategy.TENSOR\n+        else:\n+            weight_quant_strategy = ScaledMMLinearQuantStrategy.CHANNEL\n+\n+        self.fp8_linear = init_fp8_linear_kernel(\n+            act_q_static=self.quant_key.scale.static,\n+            act_q_group_shape=self.quant_key.scale.group_shape,\n+            weight_quant_strategy=weight_quant_strategy,\n+            out_dtype=torch.get_default_dtype(),\n+            module_name=self.__class__.__name__,\n", "materials": ["Also it should be easy to select each (supported) implementation so that tests can test with all of them,"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -45,14 +45,15 @@\n     kFp8StaticTensorSym,\n     kNvfp4Quant,\n )\n-from vllm.model_executor.layers.quantization.utils.w8a8_utils import Fp8LinearOp\n from vllm.platforms import current_platform\n from vllm.utils.flashinfer import has_flashinfer\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n from vllm.v1.attention.backend import AttentionMetadata\n from vllm.v1.attention.backends.registry import AttentionBackendEnum\n from vllm.v1.kv_cache_interface import AttentionSpec\n \n+from ..utils import TestFP8Layer\n+\n FP8_DTYPE = current_platform.fp8_dtype()\n FP4_DTYPE = torch.uint8\n \n@@ -185,32 +186,30 @@ class TestAttentionFp8StaticQuantPatternModel(AttentionQuantPatternModel):\n     def __init__(self, *args, **kwargs):\n         super().__init__(*args, **kwargs)\n \n-        self.fp8_linear = Fp8LinearOp(\n-            act_quant_static=self.quant_key.scale.static,\n-            act_quant_group_shape=self.quant_key.scale.group_shape,\n-        )\n-\n         hidden_size = self.num_qo_heads * self.head_size\n-        self.w = kwargs.get(\n-            \"w\",\n-            {\n-                \"weight\": torch.randn(hidden_size, hidden_size)\n-                .to(dtype=FP8_DTYPE, device=self.device)\n-                .t(),\n-                \"wscale\": torch.tensor([1.0], dtype=torch.float32, device=self.device),\n-                \"scale\": torch.tensor([1.0], dtype=torch.float32, device=self.device),\n-            },\n+        self.fp8_linear = TestFP8Layer(\n+            weight_shape=(hidden_size, hidden_size),\n+            activation_quant_key=self.quant_key,\n+            weight_quant_key=self.quant_key,\n+            device=self.device,\n         )\n \n+        w = kwargs.get(\"w\")\n+        if w is not None:\n+            self.fp8_linear.weight = w[\"weight\"]\n+            self.fp8_linear.weight_scale = w[\"wscale\"]\n+            self.fp8_linear.input_scale = w[\"scale\"]\n+\n+        self.w = {\n+            \"weight\": self.fp8_linear.weight,\n+            \"wscale\": self.fp8_linear.weight_scale,\n+            \"scale\": self.fp8_linear.input_scale,\n+        }\n+\n     def forward(self, q: torch.Tensor, k: torch.Tensor, v: torch.Tensor):\n         \"\"\"Forward pass that creates the pattern to be fused.\"\"\"\n         attn_output = self.attn(q, k, v)\n-        return self.fp8_linear.apply(\n-            input=attn_output,\n-            weight=self.w[\"weight\"],\n-            weight_scale=self.w[\"wscale\"],\n-            input_scale=self.w[\"scale\"],\n-        )\n+        return self.fp8_linear(attn_output)\n \n \n class TestAttentionNvfp4QuantPatternModel(AttentionQuantPatternModel):", "patch_sha256": "55010739ad1dc048"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2490743372", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/compile/test_fusion_attn.py\nReview Comment:\nGreat idea, I have implemented it \n\nOriginal Code Context:\n@@ -170,11 +177,18 @@ class TestAttentionFp8StaticQuantPatternModel(AttentionQuantPatternModel):\n     def __init__(self, *args, **kwargs):\n         super().__init__(*args, **kwargs)\n \n-        self.fp8_linear = Fp8LinearOp(\n-            act_quant_static=self.quant_key.scale.static,\n-            act_quant_group_shape=self.quant_key.scale.group_shape,\n+        if self.quant_key.scale.group_shape.is_per_tensor():\n+            weight_quant_strategy = ScaledMMLinearQuantStrategy.TENSOR\n+        else:\n+            weight_quant_strategy = ScaledMMLinearQuantStrategy.CHANNEL\n+\n+        self.fp8_linear = init_fp8_linear_kernel(\n+            act_q_static=self.quant_key.scale.static,\n+            act_q_group_shape=self.quant_key.scale.group_shape,\n+            weight_quant_strategy=weight_quant_strategy,\n+            out_dtype=torch.get_default_dtype(),\n+            module_name=self.__class__.__name__,\n", "materials": ["Great idea, I have implemented it "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -45,14 +45,15 @@\n     kFp8StaticTensorSym,\n     kNvfp4Quant,\n )\n-from vllm.model_executor.layers.quantization.utils.w8a8_utils import Fp8LinearOp\n from vllm.platforms import current_platform\n from vllm.utils.flashinfer import has_flashinfer\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n from vllm.v1.attention.backend import AttentionMetadata\n from vllm.v1.attention.backends.registry import AttentionBackendEnum\n from vllm.v1.kv_cache_interface import AttentionSpec\n \n+from ..utils import TestFP8Layer\n+\n FP8_DTYPE = current_platform.fp8_dtype()\n FP4_DTYPE = torch.uint8\n \n@@ -185,32 +186,30 @@ class TestAttentionFp8StaticQuantPatternModel(AttentionQuantPatternModel):\n     def __init__(self, *args, **kwargs):\n         super().__init__(*args, **kwargs)\n \n-        self.fp8_linear = Fp8LinearOp(\n-            act_quant_static=self.quant_key.scale.static,\n-            act_quant_group_shape=self.quant_key.scale.group_shape,\n-        )\n-\n         hidden_size = self.num_qo_heads * self.head_size\n-        self.w = kwargs.get(\n-            \"w\",\n-            {\n-                \"weight\": torch.randn(hidden_size, hidden_size)\n-                .to(dtype=FP8_DTYPE, device=self.device)\n-                .t(),\n-                \"wscale\": torch.tensor([1.0], dtype=torch.float32, device=self.device),\n-                \"scale\": torch.tensor([1.0], dtype=torch.float32, device=self.device),\n-            },\n+        self.fp8_linear = TestFP8Layer(\n+            weight_shape=(hidden_size, hidden_size),\n+            activation_quant_key=self.quant_key,\n+            weight_quant_key=self.quant_key,\n+            device=self.device,\n         )\n \n+        w = kwargs.get(\"w\")\n+        if w is not None:\n+            self.fp8_linear.weight = w[\"weight\"]\n+            self.fp8_linear.weight_scale = w[\"wscale\"]\n+            self.fp8_linear.input_scale = w[\"scale\"]\n+\n+        self.w = {\n+            \"weight\": self.fp8_linear.weight,\n+            \"wscale\": self.fp8_linear.weight_scale,\n+            \"scale\": self.fp8_linear.input_scale,\n+        }\n+\n     def forward(self, q: torch.Tensor, k: torch.Tensor, v: torch.Tensor):\n         \"\"\"Forward pass that creates the pattern to be fused.\"\"\"\n         attn_output = self.attn(q, k, v)\n-        return self.fp8_linear.apply(\n-            input=attn_output,\n-            weight=self.w[\"weight\"],\n-            weight_scale=self.w[\"wscale\"],\n-            input_scale=self.w[\"scale\"],\n-        )\n+        return self.fp8_linear(attn_output)\n \n \n class TestAttentionNvfp4QuantPatternModel(AttentionQuantPatternModel):", "patch_sha256": "55010739ad1dc048"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2501081419", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/utils.py\nReview Comment:\ninput_scale should be optional in case quant is dynamic \n\nOriginal Code Context:\n@@ -1414,11 +1418,49 @@ def flat_product(*iterables: Iterable[Any]):\n \n \n class TestFP8Layer(torch.nn.Module):\n-    \"\"\"Helper class for ScaledMMLinearKernels.\"\"\"\n+    \"\"\"\n+    Test helper class for evaluating FP8 linear operations with quantization.\n+\n+    It supports configurable activation and weight quantization parameters,\n+    and provides a forward method that applies the FP8 linear transformation\n+    with optional bias.\n+\n+    Args:\n+        activation_quant_key (QuantKey): Key for activation quantization configuration.\n+        weight_quant_key (QuantKey): Key for weight quantization configuration.\n+        weight (torch.Tensor): Weight tensor for linear transformation.\n+        weight_scale (torch.Tensor): Per-tensor or per-group scale for weights.\n+        input_scale (torch.Tensor): Scale tensor for input quantization.\n", "materials": ["input_scale should be optional in case quant is dynamic "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -42,6 +42,17 @@\n )\n from vllm.engine.arg_utils import AsyncEngineArgs\n from vllm.entrypoints.cli.serve import ServeSubcommand\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm.ScaledMMLinearKernel import (  # noqa: E501\n+    FP8ScaledMMLinearKernel,\n+)\n+from vllm.model_executor.layers.quantization.utils.fp8_utils import W8A8BlockFp8LinearOp\n+from vllm.model_executor.layers.quantization.utils.quant_utils import (\n+    GroupShape,\n+    QuantKey,\n+)\n from vllm.model_executor.model_loader import get_model_loader\n from vllm.platforms import current_platform\n from vllm.tokenizers import get_tokenizer\n@@ -50,6 +61,8 @@\n from vllm.utils.network_utils import get_open_port\n from vllm.utils.torch_utils import cuda_device_count_stateless\n \n+FP8_DTYPE = current_platform.fp8_dtype()\n+\n if current_platform.is_rocm():\n     from amdsmi import (\n         amdsmi_get_gpu_vram_usage,\n@@ -1332,3 +1345,117 @@ def flat_product(*iterables: Iterable[Any]):\n     for element in itertools.product(*iterables):\n         normalized = (e if isinstance(e, tuple) else (e,) for e in element)\n         yield tuple(itertools.chain(*normalized))\n+\n+\n+class TestFP8Layer(torch.nn.Module):\n+    \"\"\"\n+    Test helper for FP8 linear operations. Creates random weights and scales\n+    based on quantization configuration.\n+\n+    Args:\n+        weight_shape: Shape of the weight tensor (out_features, in_features).\n+        activation_quant_key: Activation quantization configuration.\n+        weight_quant_key: Weight quantization configuration.\n+        out_dtype: Output dtype. Defaults to current default dtype.\n+        force_kernel: Optional kernel to force use of specific implementation.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        weight_shape: tuple[int, int],\n+        activation_quant_key: QuantKey,\n+        weight_quant_key: QuantKey,\n+        out_dtype: torch.dtype | None = None,\n+        device: torch.device | None = None,\n+        force_kernel: FP8ScaledMMLinearKernel | None = None,\n+    ):\n+        super().__init__()\n+        per_tensor_weights = weight_quant_key.scale.group_shape.is_per_tensor()\n+        is_static_activation_scale = activation_quant_key.scale.static\n+        weight_scale_shape = (1,) if per_tensor_weights else (weight_shape[0], 1)\n+\n+        self.weight_scale = torch.rand(\n+            weight_scale_shape, dtype=torch.float32, device=device\n+        )\n+        self.input_scale = (\n+            torch.rand(1, dtype=torch.float32, device=device)\n+            if is_static_activation_scale\n+            else None\n+        )\n+        self.weight = torch.rand(weight_shape, device=device).to(dtype=FP8_DTYPE).t()\n+        self.input_scale_ub = None\n+\n+        out_dtype = torch.get_default_dtype() if out_dtype is None else out_dtype\n+\n+        self.kernel = init_fp8_linear_kernel(\n+            activation_quant_key=activation_quant_key,\n+            weight_quant_key=weight_quant_key,\n+            out_dtype=out_dtype,\n+            force_kernel=force_kernel,\n+        )\n+\n+    def is_quant_fp8_enabled(self) -> bool:\n+        return self.kernel.quant_fp8.enabled()\n+\n+    def forward(\n+        self, y: torch.Tensor, bias: torch.Tensor | None = None\n+    ) -> torch.Tensor:\n+        return self.kernel.apply_weights(self, y, bias)\n+\n+\n+# TODO: Drop TestBlockFP8Layer in favour of a unified TestFP8Layer\n+# after refactoring W8A8BlockFp8LinearOp.\n+# https://github.com/vllm-project/vllm/issues/31818\n+class TestBlockFP8Layer:\n+    \"\"\"\n+    Test helper for blockwise FP8 linear operations. Creates random weights\n+    and scales for W8A8BlockFp8LinearOp.\n+\n+    This is a workaround until W8A8BlockFp8LinearOp implements the kernel\n+    abstraction (ScaledMMLinearKernel) for blockwise quantization.\n+\n+    Args:\n+        weight_shape: Shape of the weight tensor (out_features, in_features).\n+        group_shape: Blockwise quantization group shape.\n+        cutlass_block_fp8_supported: Whether CUTLASS blockwise FP8 is available.\n+        use_aiter_and_is_supported: Whether to use aiter quantization ops.\n+        transpose_weights: Whether to transpose weights after creation.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        weight_shape: tuple[int, int],\n+        group_shape: GroupShape,\n+        cutlass_block_fp8_supported: bool = False,\n+        use_aiter_and_is_supported: bool = False,\n+        transpose_weights: bool = False,\n+    ):\n+        weight_scale_shape = weight_shape[0] // group_shape[1]\n+        self.weight_scale = torch.rand(\n+            (weight_scale_shape, weight_scale_shape), dtype=torch.float32\n+        )\n+        self.weight = torch.rand(weight_shape).to(dtype=FP8_DTYPE)\n+        self.input_scale = None\n+        if transpose_weights:\n+            self.weight = self.weight.t()\n+\n+        self.linear_op = W8A8BlockFp8LinearOp(\n+            weight_group_shape=GroupShape(group_shape[1], group_shape[1]),\n+            act_quant_group_shape=group_shape,\n+            cutlass_block_fp8_supported=cutlass_block_fp8_supported,\n+            use_aiter_and_is_supported=use_aiter_and_is_supported,\n+        )\n+\n+    def __call__(\n+        self, y: torch.Tensor, bias: torch.Tensor | None = None\n+    ) -> torch.Tensor:\n+        return self.linear_op.apply(\n+            input=y,\n+            weight=self.weight,\n+            weight_scale=self.weight_scale,\n+            input_scale=self.input_scale,\n+            bias=bias,\n+        )\n+\n+    def is_quant_fp8_enabled(self) -> bool:\n+        return self.linear_op.input_quant_op.enabled()", "patch_sha256": "ed7b4088150ad6b5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2501103240", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/utils.py\nReview Comment:\nGreat utility! Something that's not easy to but should be is selecting all possible backends for the platform so that we can test with each of them\n\nOriginal Code Context:\n@@ -1414,11 +1418,49 @@ def flat_product(*iterables: Iterable[Any]):\n \n \n class TestFP8Layer(torch.nn.Module):\n-    \"\"\"Helper class for ScaledMMLinearKernels.\"\"\"\n+    \"\"\"\n+    Test helper class for evaluating FP8 linear operations with quantization.\n+\n+    It supports configurable activation and weight quantization parameters,\n+    and provides a forward method that applies the FP8 linear transformation\n+    with optional bias.\n+\n+    Args:\n+        activation_quant_key (QuantKey): Key for activation quantization configuration.\n+        weight_quant_key (QuantKey): Key for weight quantization configuration.\n+        weight (torch.Tensor): Weight tensor for linear transformation.\n+        weight_scale (torch.Tensor): Per-tensor or per-group scale for weights.\n+        input_scale (torch.Tensor): Scale tensor for input quantization.\n+        out_dtype (torch.dtype, optional): Output tensor data type.\n+            Defaults to torch.get_default_dtype().\n+    \"\"\"\n \n-    def __init__(self, weight, weight_scale, input_scale):\n+    def __init__(\n+        self,\n+        activation_quant_key: QuantKey,\n+        weight_quant_key: QuantKey,\n+        weight: torch.Tensor,\n+        weight_scale: torch.Tensor,\n+        input_scale: torch.Tensor,\n+        out_dtype: torch.dtype | None = None,\n+    ):\n         super().__init__()\n         self.weight_scale = weight_scale\n         self.weight = weight\n         self.input_scale = input_scale\n         self.input_scale_ub = None\n+        out_dtype = torch.get_default_dtype() if out_dtype is None else out_dtype\n+        self.kernel = init_fp8_linear_kernel(\n+            activation_quant_key=activation_quant_key,\n+            weight_quant_key=weight_quant_key,\n+            out_dtype=out_dtype,\n+            module_name=self.__class__.__name__,\n+        )\n", "materials": ["Great utility! Something that's not easy to but should be is selecting all possible backends for the platform so that we can test with each of them"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -42,6 +42,17 @@\n )\n from vllm.engine.arg_utils import AsyncEngineArgs\n from vllm.entrypoints.cli.serve import ServeSubcommand\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm.ScaledMMLinearKernel import (  # noqa: E501\n+    FP8ScaledMMLinearKernel,\n+)\n+from vllm.model_executor.layers.quantization.utils.fp8_utils import W8A8BlockFp8LinearOp\n+from vllm.model_executor.layers.quantization.utils.quant_utils import (\n+    GroupShape,\n+    QuantKey,\n+)\n from vllm.model_executor.model_loader import get_model_loader\n from vllm.platforms import current_platform\n from vllm.tokenizers import get_tokenizer\n@@ -50,6 +61,8 @@\n from vllm.utils.network_utils import get_open_port\n from vllm.utils.torch_utils import cuda_device_count_stateless\n \n+FP8_DTYPE = current_platform.fp8_dtype()\n+\n if current_platform.is_rocm():\n     from amdsmi import (\n         amdsmi_get_gpu_vram_usage,\n@@ -1332,3 +1345,117 @@ def flat_product(*iterables: Iterable[Any]):\n     for element in itertools.product(*iterables):\n         normalized = (e if isinstance(e, tuple) else (e,) for e in element)\n         yield tuple(itertools.chain(*normalized))\n+\n+\n+class TestFP8Layer(torch.nn.Module):\n+    \"\"\"\n+    Test helper for FP8 linear operations. Creates random weights and scales\n+    based on quantization configuration.\n+\n+    Args:\n+        weight_shape: Shape of the weight tensor (out_features, in_features).\n+        activation_quant_key: Activation quantization configuration.\n+        weight_quant_key: Weight quantization configuration.\n+        out_dtype: Output dtype. Defaults to current default dtype.\n+        force_kernel: Optional kernel to force use of specific implementation.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        weight_shape: tuple[int, int],\n+        activation_quant_key: QuantKey,\n+        weight_quant_key: QuantKey,\n+        out_dtype: torch.dtype | None = None,\n+        device: torch.device | None = None,\n+        force_kernel: FP8ScaledMMLinearKernel | None = None,\n+    ):\n+        super().__init__()\n+        per_tensor_weights = weight_quant_key.scale.group_shape.is_per_tensor()\n+        is_static_activation_scale = activation_quant_key.scale.static\n+        weight_scale_shape = (1,) if per_tensor_weights else (weight_shape[0], 1)\n+\n+        self.weight_scale = torch.rand(\n+            weight_scale_shape, dtype=torch.float32, device=device\n+        )\n+        self.input_scale = (\n+            torch.rand(1, dtype=torch.float32, device=device)\n+            if is_static_activation_scale\n+            else None\n+        )\n+        self.weight = torch.rand(weight_shape, device=device).to(dtype=FP8_DTYPE).t()\n+        self.input_scale_ub = None\n+\n+        out_dtype = torch.get_default_dtype() if out_dtype is None else out_dtype\n+\n+        self.kernel = init_fp8_linear_kernel(\n+            activation_quant_key=activation_quant_key,\n+            weight_quant_key=weight_quant_key,\n+            out_dtype=out_dtype,\n+            force_kernel=force_kernel,\n+        )\n+\n+    def is_quant_fp8_enabled(self) -> bool:\n+        return self.kernel.quant_fp8.enabled()\n+\n+    def forward(\n+        self, y: torch.Tensor, bias: torch.Tensor | None = None\n+    ) -> torch.Tensor:\n+        return self.kernel.apply_weights(self, y, bias)\n+\n+\n+# TODO: Drop TestBlockFP8Layer in favour of a unified TestFP8Layer\n+# after refactoring W8A8BlockFp8LinearOp.\n+# https://github.com/vllm-project/vllm/issues/31818\n+class TestBlockFP8Layer:\n+    \"\"\"\n+    Test helper for blockwise FP8 linear operations. Creates random weights\n+    and scales for W8A8BlockFp8LinearOp.\n+\n+    This is a workaround until W8A8BlockFp8LinearOp implements the kernel\n+    abstraction (ScaledMMLinearKernel) for blockwise quantization.\n+\n+    Args:\n+        weight_shape: Shape of the weight tensor (out_features, in_features).\n+        group_shape: Blockwise quantization group shape.\n+        cutlass_block_fp8_supported: Whether CUTLASS blockwise FP8 is available.\n+        use_aiter_and_is_supported: Whether to use aiter quantization ops.\n+        transpose_weights: Whether to transpose weights after creation.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        weight_shape: tuple[int, int],\n+        group_shape: GroupShape,\n+        cutlass_block_fp8_supported: bool = False,\n+        use_aiter_and_is_supported: bool = False,\n+        transpose_weights: bool = False,\n+    ):\n+        weight_scale_shape = weight_shape[0] // group_shape[1]\n+        self.weight_scale = torch.rand(\n+            (weight_scale_shape, weight_scale_shape), dtype=torch.float32\n+        )\n+        self.weight = torch.rand(weight_shape).to(dtype=FP8_DTYPE)\n+        self.input_scale = None\n+        if transpose_weights:\n+            self.weight = self.weight.t()\n+\n+        self.linear_op = W8A8BlockFp8LinearOp(\n+            weight_group_shape=GroupShape(group_shape[1], group_shape[1]),\n+            act_quant_group_shape=group_shape,\n+            cutlass_block_fp8_supported=cutlass_block_fp8_supported,\n+            use_aiter_and_is_supported=use_aiter_and_is_supported,\n+        )\n+\n+    def __call__(\n+        self, y: torch.Tensor, bias: torch.Tensor | None = None\n+    ) -> torch.Tensor:\n+        return self.linear_op.apply(\n+            input=y,\n+            weight=self.weight,\n+            weight_scale=self.weight_scale,\n+            input_scale=self.input_scale,\n+            bias=bias,\n+        )\n+\n+    def is_quant_fp8_enabled(self) -> bool:\n+        return self.linear_op.input_quant_op.enabled()", "patch_sha256": "ed7b4088150ad6b5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2503210032", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/utils.py\nReview Comment:\nadded the ability to force a kernel\n\nOriginal Code Context:\n@@ -1414,11 +1418,49 @@ def flat_product(*iterables: Iterable[Any]):\n \n \n class TestFP8Layer(torch.nn.Module):\n-    \"\"\"Helper class for ScaledMMLinearKernels.\"\"\"\n+    \"\"\"\n+    Test helper class for evaluating FP8 linear operations with quantization.\n+\n+    It supports configurable activation and weight quantization parameters,\n+    and provides a forward method that applies the FP8 linear transformation\n+    with optional bias.\n+\n+    Args:\n+        activation_quant_key (QuantKey): Key for activation quantization configuration.\n+        weight_quant_key (QuantKey): Key for weight quantization configuration.\n+        weight (torch.Tensor): Weight tensor for linear transformation.\n+        weight_scale (torch.Tensor): Per-tensor or per-group scale for weights.\n+        input_scale (torch.Tensor): Scale tensor for input quantization.\n+        out_dtype (torch.dtype, optional): Output tensor data type.\n+            Defaults to torch.get_default_dtype().\n+    \"\"\"\n \n-    def __init__(self, weight, weight_scale, input_scale):\n+    def __init__(\n+        self,\n+        activation_quant_key: QuantKey,\n+        weight_quant_key: QuantKey,\n+        weight: torch.Tensor,\n+        weight_scale: torch.Tensor,\n+        input_scale: torch.Tensor,\n+        out_dtype: torch.dtype | None = None,\n+    ):\n         super().__init__()\n         self.weight_scale = weight_scale\n         self.weight = weight\n         self.input_scale = input_scale\n         self.input_scale_ub = None\n+        out_dtype = torch.get_default_dtype() if out_dtype is None else out_dtype\n+        self.kernel = init_fp8_linear_kernel(\n+            activation_quant_key=activation_quant_key,\n+            weight_quant_key=weight_quant_key,\n+            out_dtype=out_dtype,\n+            module_name=self.__class__.__name__,\n+        )\n", "materials": ["added the ability to force a kernel"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -42,6 +42,17 @@\n )\n from vllm.engine.arg_utils import AsyncEngineArgs\n from vllm.entrypoints.cli.serve import ServeSubcommand\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm.ScaledMMLinearKernel import (  # noqa: E501\n+    FP8ScaledMMLinearKernel,\n+)\n+from vllm.model_executor.layers.quantization.utils.fp8_utils import W8A8BlockFp8LinearOp\n+from vllm.model_executor.layers.quantization.utils.quant_utils import (\n+    GroupShape,\n+    QuantKey,\n+)\n from vllm.model_executor.model_loader import get_model_loader\n from vllm.platforms import current_platform\n from vllm.tokenizers import get_tokenizer\n@@ -50,6 +61,8 @@\n from vllm.utils.network_utils import get_open_port\n from vllm.utils.torch_utils import cuda_device_count_stateless\n \n+FP8_DTYPE = current_platform.fp8_dtype()\n+\n if current_platform.is_rocm():\n     from amdsmi import (\n         amdsmi_get_gpu_vram_usage,\n@@ -1332,3 +1345,117 @@ def flat_product(*iterables: Iterable[Any]):\n     for element in itertools.product(*iterables):\n         normalized = (e if isinstance(e, tuple) else (e,) for e in element)\n         yield tuple(itertools.chain(*normalized))\n+\n+\n+class TestFP8Layer(torch.nn.Module):\n+    \"\"\"\n+    Test helper for FP8 linear operations. Creates random weights and scales\n+    based on quantization configuration.\n+\n+    Args:\n+        weight_shape: Shape of the weight tensor (out_features, in_features).\n+        activation_quant_key: Activation quantization configuration.\n+        weight_quant_key: Weight quantization configuration.\n+        out_dtype: Output dtype. Defaults to current default dtype.\n+        force_kernel: Optional kernel to force use of specific implementation.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        weight_shape: tuple[int, int],\n+        activation_quant_key: QuantKey,\n+        weight_quant_key: QuantKey,\n+        out_dtype: torch.dtype | None = None,\n+        device: torch.device | None = None,\n+        force_kernel: FP8ScaledMMLinearKernel | None = None,\n+    ):\n+        super().__init__()\n+        per_tensor_weights = weight_quant_key.scale.group_shape.is_per_tensor()\n+        is_static_activation_scale = activation_quant_key.scale.static\n+        weight_scale_shape = (1,) if per_tensor_weights else (weight_shape[0], 1)\n+\n+        self.weight_scale = torch.rand(\n+            weight_scale_shape, dtype=torch.float32, device=device\n+        )\n+        self.input_scale = (\n+            torch.rand(1, dtype=torch.float32, device=device)\n+            if is_static_activation_scale\n+            else None\n+        )\n+        self.weight = torch.rand(weight_shape, device=device).to(dtype=FP8_DTYPE).t()\n+        self.input_scale_ub = None\n+\n+        out_dtype = torch.get_default_dtype() if out_dtype is None else out_dtype\n+\n+        self.kernel = init_fp8_linear_kernel(\n+            activation_quant_key=activation_quant_key,\n+            weight_quant_key=weight_quant_key,\n+            out_dtype=out_dtype,\n+            force_kernel=force_kernel,\n+        )\n+\n+    def is_quant_fp8_enabled(self) -> bool:\n+        return self.kernel.quant_fp8.enabled()\n+\n+    def forward(\n+        self, y: torch.Tensor, bias: torch.Tensor | None = None\n+    ) -> torch.Tensor:\n+        return self.kernel.apply_weights(self, y, bias)\n+\n+\n+# TODO: Drop TestBlockFP8Layer in favour of a unified TestFP8Layer\n+# after refactoring W8A8BlockFp8LinearOp.\n+# https://github.com/vllm-project/vllm/issues/31818\n+class TestBlockFP8Layer:\n+    \"\"\"\n+    Test helper for blockwise FP8 linear operations. Creates random weights\n+    and scales for W8A8BlockFp8LinearOp.\n+\n+    This is a workaround until W8A8BlockFp8LinearOp implements the kernel\n+    abstraction (ScaledMMLinearKernel) for blockwise quantization.\n+\n+    Args:\n+        weight_shape: Shape of the weight tensor (out_features, in_features).\n+        group_shape: Blockwise quantization group shape.\n+        cutlass_block_fp8_supported: Whether CUTLASS blockwise FP8 is available.\n+        use_aiter_and_is_supported: Whether to use aiter quantization ops.\n+        transpose_weights: Whether to transpose weights after creation.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        weight_shape: tuple[int, int],\n+        group_shape: GroupShape,\n+        cutlass_block_fp8_supported: bool = False,\n+        use_aiter_and_is_supported: bool = False,\n+        transpose_weights: bool = False,\n+    ):\n+        weight_scale_shape = weight_shape[0] // group_shape[1]\n+        self.weight_scale = torch.rand(\n+            (weight_scale_shape, weight_scale_shape), dtype=torch.float32\n+        )\n+        self.weight = torch.rand(weight_shape).to(dtype=FP8_DTYPE)\n+        self.input_scale = None\n+        if transpose_weights:\n+            self.weight = self.weight.t()\n+\n+        self.linear_op = W8A8BlockFp8LinearOp(\n+            weight_group_shape=GroupShape(group_shape[1], group_shape[1]),\n+            act_quant_group_shape=group_shape,\n+            cutlass_block_fp8_supported=cutlass_block_fp8_supported,\n+            use_aiter_and_is_supported=use_aiter_and_is_supported,\n+        )\n+\n+    def __call__(\n+        self, y: torch.Tensor, bias: torch.Tensor | None = None\n+    ) -> torch.Tensor:\n+        return self.linear_op.apply(\n+            input=y,\n+            weight=self.weight,\n+            weight_scale=self.weight_scale,\n+            input_scale=self.input_scale,\n+            bias=bias,\n+        )\n+\n+    def is_quant_fp8_enabled(self) -> bool:\n+        return self.linear_op.input_quant_op.enabled()", "patch_sha256": "ed7b4088150ad6b5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2516521078", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/utils.py\nReview Comment:\nNot reflected in the type annotation though?\n\nOriginal Code Context:\n@@ -1414,11 +1418,49 @@ def flat_product(*iterables: Iterable[Any]):\n \n \n class TestFP8Layer(torch.nn.Module):\n-    \"\"\"Helper class for ScaledMMLinearKernels.\"\"\"\n+    \"\"\"\n+    Test helper class for evaluating FP8 linear operations with quantization.\n+\n+    It supports configurable activation and weight quantization parameters,\n+    and provides a forward method that applies the FP8 linear transformation\n+    with optional bias.\n+\n+    Args:\n+        activation_quant_key (QuantKey): Key for activation quantization configuration.\n+        weight_quant_key (QuantKey): Key for weight quantization configuration.\n+        weight (torch.Tensor): Weight tensor for linear transformation.\n+        weight_scale (torch.Tensor): Per-tensor or per-group scale for weights.\n+        input_scale (torch.Tensor): Scale tensor for input quantization.\n", "materials": ["Not reflected in the type annotation though?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -42,6 +42,17 @@\n )\n from vllm.engine.arg_utils import AsyncEngineArgs\n from vllm.entrypoints.cli.serve import ServeSubcommand\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm.ScaledMMLinearKernel import (  # noqa: E501\n+    FP8ScaledMMLinearKernel,\n+)\n+from vllm.model_executor.layers.quantization.utils.fp8_utils import W8A8BlockFp8LinearOp\n+from vllm.model_executor.layers.quantization.utils.quant_utils import (\n+    GroupShape,\n+    QuantKey,\n+)\n from vllm.model_executor.model_loader import get_model_loader\n from vllm.platforms import current_platform\n from vllm.tokenizers import get_tokenizer\n@@ -50,6 +61,8 @@\n from vllm.utils.network_utils import get_open_port\n from vllm.utils.torch_utils import cuda_device_count_stateless\n \n+FP8_DTYPE = current_platform.fp8_dtype()\n+\n if current_platform.is_rocm():\n     from amdsmi import (\n         amdsmi_get_gpu_vram_usage,\n@@ -1332,3 +1345,117 @@ def flat_product(*iterables: Iterable[Any]):\n     for element in itertools.product(*iterables):\n         normalized = (e if isinstance(e, tuple) else (e,) for e in element)\n         yield tuple(itertools.chain(*normalized))\n+\n+\n+class TestFP8Layer(torch.nn.Module):\n+    \"\"\"\n+    Test helper for FP8 linear operations. Creates random weights and scales\n+    based on quantization configuration.\n+\n+    Args:\n+        weight_shape: Shape of the weight tensor (out_features, in_features).\n+        activation_quant_key: Activation quantization configuration.\n+        weight_quant_key: Weight quantization configuration.\n+        out_dtype: Output dtype. Defaults to current default dtype.\n+        force_kernel: Optional kernel to force use of specific implementation.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        weight_shape: tuple[int, int],\n+        activation_quant_key: QuantKey,\n+        weight_quant_key: QuantKey,\n+        out_dtype: torch.dtype | None = None,\n+        device: torch.device | None = None,\n+        force_kernel: FP8ScaledMMLinearKernel | None = None,\n+    ):\n+        super().__init__()\n+        per_tensor_weights = weight_quant_key.scale.group_shape.is_per_tensor()\n+        is_static_activation_scale = activation_quant_key.scale.static\n+        weight_scale_shape = (1,) if per_tensor_weights else (weight_shape[0], 1)\n+\n+        self.weight_scale = torch.rand(\n+            weight_scale_shape, dtype=torch.float32, device=device\n+        )\n+        self.input_scale = (\n+            torch.rand(1, dtype=torch.float32, device=device)\n+            if is_static_activation_scale\n+            else None\n+        )\n+        self.weight = torch.rand(weight_shape, device=device).to(dtype=FP8_DTYPE).t()\n+        self.input_scale_ub = None\n+\n+        out_dtype = torch.get_default_dtype() if out_dtype is None else out_dtype\n+\n+        self.kernel = init_fp8_linear_kernel(\n+            activation_quant_key=activation_quant_key,\n+            weight_quant_key=weight_quant_key,\n+            out_dtype=out_dtype,\n+            force_kernel=force_kernel,\n+        )\n+\n+    def is_quant_fp8_enabled(self) -> bool:\n+        return self.kernel.quant_fp8.enabled()\n+\n+    def forward(\n+        self, y: torch.Tensor, bias: torch.Tensor | None = None\n+    ) -> torch.Tensor:\n+        return self.kernel.apply_weights(self, y, bias)\n+\n+\n+# TODO: Drop TestBlockFP8Layer in favour of a unified TestFP8Layer\n+# after refactoring W8A8BlockFp8LinearOp.\n+# https://github.com/vllm-project/vllm/issues/31818\n+class TestBlockFP8Layer:\n+    \"\"\"\n+    Test helper for blockwise FP8 linear operations. Creates random weights\n+    and scales for W8A8BlockFp8LinearOp.\n+\n+    This is a workaround until W8A8BlockFp8LinearOp implements the kernel\n+    abstraction (ScaledMMLinearKernel) for blockwise quantization.\n+\n+    Args:\n+        weight_shape: Shape of the weight tensor (out_features, in_features).\n+        group_shape: Blockwise quantization group shape.\n+        cutlass_block_fp8_supported: Whether CUTLASS blockwise FP8 is available.\n+        use_aiter_and_is_supported: Whether to use aiter quantization ops.\n+        transpose_weights: Whether to transpose weights after creation.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        weight_shape: tuple[int, int],\n+        group_shape: GroupShape,\n+        cutlass_block_fp8_supported: bool = False,\n+        use_aiter_and_is_supported: bool = False,\n+        transpose_weights: bool = False,\n+    ):\n+        weight_scale_shape = weight_shape[0] // group_shape[1]\n+        self.weight_scale = torch.rand(\n+            (weight_scale_shape, weight_scale_shape), dtype=torch.float32\n+        )\n+        self.weight = torch.rand(weight_shape).to(dtype=FP8_DTYPE)\n+        self.input_scale = None\n+        if transpose_weights:\n+            self.weight = self.weight.t()\n+\n+        self.linear_op = W8A8BlockFp8LinearOp(\n+            weight_group_shape=GroupShape(group_shape[1], group_shape[1]),\n+            act_quant_group_shape=group_shape,\n+            cutlass_block_fp8_supported=cutlass_block_fp8_supported,\n+            use_aiter_and_is_supported=use_aiter_and_is_supported,\n+        )\n+\n+    def __call__(\n+        self, y: torch.Tensor, bias: torch.Tensor | None = None\n+    ) -> torch.Tensor:\n+        return self.linear_op.apply(\n+            input=y,\n+            weight=self.weight,\n+            weight_scale=self.weight_scale,\n+            input_scale=self.input_scale,\n+            bias=bias,\n+        )\n+\n+    def is_quant_fp8_enabled(self) -> bool:\n+        return self.linear_op.input_quant_op.enabled()", "patch_sha256": "ed7b4088150ad6b5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2592950681", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/utils.py\nReview Comment:\nThis is really nice!\n\nOriginal Code Context:\n@@ -1310,3 +1317,54 @@ def flat_product(*iterables: Iterable[Any]):\n     for element in itertools.product(*iterables):\n         normalized = (e if isinstance(e, tuple) else (e,) for e in element)\n         yield tuple(itertools.chain(*normalized))\n+\n+\n+class TestFP8Layer(torch.nn.Module):\n", "materials": ["This is really nice!"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -42,6 +42,17 @@\n )\n from vllm.engine.arg_utils import AsyncEngineArgs\n from vllm.entrypoints.cli.serve import ServeSubcommand\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm.ScaledMMLinearKernel import (  # noqa: E501\n+    FP8ScaledMMLinearKernel,\n+)\n+from vllm.model_executor.layers.quantization.utils.fp8_utils import W8A8BlockFp8LinearOp\n+from vllm.model_executor.layers.quantization.utils.quant_utils import (\n+    GroupShape,\n+    QuantKey,\n+)\n from vllm.model_executor.model_loader import get_model_loader\n from vllm.platforms import current_platform\n from vllm.tokenizers import get_tokenizer\n@@ -50,6 +61,8 @@\n from vllm.utils.network_utils import get_open_port\n from vllm.utils.torch_utils import cuda_device_count_stateless\n \n+FP8_DTYPE = current_platform.fp8_dtype()\n+\n if current_platform.is_rocm():\n     from amdsmi import (\n         amdsmi_get_gpu_vram_usage,\n@@ -1332,3 +1345,117 @@ def flat_product(*iterables: Iterable[Any]):\n     for element in itertools.product(*iterables):\n         normalized = (e if isinstance(e, tuple) else (e,) for e in element)\n         yield tuple(itertools.chain(*normalized))\n+\n+\n+class TestFP8Layer(torch.nn.Module):\n+    \"\"\"\n+    Test helper for FP8 linear operations. Creates random weights and scales\n+    based on quantization configuration.\n+\n+    Args:\n+        weight_shape: Shape of the weight tensor (out_features, in_features).\n+        activation_quant_key: Activation quantization configuration.\n+        weight_quant_key: Weight quantization configuration.\n+        out_dtype: Output dtype. Defaults to current default dtype.\n+        force_kernel: Optional kernel to force use of specific implementation.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        weight_shape: tuple[int, int],\n+        activation_quant_key: QuantKey,\n+        weight_quant_key: QuantKey,\n+        out_dtype: torch.dtype | None = None,\n+        device: torch.device | None = None,\n+        force_kernel: FP8ScaledMMLinearKernel | None = None,\n+    ):\n+        super().__init__()\n+        per_tensor_weights = weight_quant_key.scale.group_shape.is_per_tensor()\n+        is_static_activation_scale = activation_quant_key.scale.static\n+        weight_scale_shape = (1,) if per_tensor_weights else (weight_shape[0], 1)\n+\n+        self.weight_scale = torch.rand(\n+            weight_scale_shape, dtype=torch.float32, device=device\n+        )\n+        self.input_scale = (\n+            torch.rand(1, dtype=torch.float32, device=device)\n+            if is_static_activation_scale\n+            else None\n+        )\n+        self.weight = torch.rand(weight_shape, device=device).to(dtype=FP8_DTYPE).t()\n+        self.input_scale_ub = None\n+\n+        out_dtype = torch.get_default_dtype() if out_dtype is None else out_dtype\n+\n+        self.kernel = init_fp8_linear_kernel(\n+            activation_quant_key=activation_quant_key,\n+            weight_quant_key=weight_quant_key,\n+            out_dtype=out_dtype,\n+            force_kernel=force_kernel,\n+        )\n+\n+    def is_quant_fp8_enabled(self) -> bool:\n+        return self.kernel.quant_fp8.enabled()\n+\n+    def forward(\n+        self, y: torch.Tensor, bias: torch.Tensor | None = None\n+    ) -> torch.Tensor:\n+        return self.kernel.apply_weights(self, y, bias)\n+\n+\n+# TODO: Drop TestBlockFP8Layer in favour of a unified TestFP8Layer\n+# after refactoring W8A8BlockFp8LinearOp.\n+# https://github.com/vllm-project/vllm/issues/31818\n+class TestBlockFP8Layer:\n+    \"\"\"\n+    Test helper for blockwise FP8 linear operations. Creates random weights\n+    and scales for W8A8BlockFp8LinearOp.\n+\n+    This is a workaround until W8A8BlockFp8LinearOp implements the kernel\n+    abstraction (ScaledMMLinearKernel) for blockwise quantization.\n+\n+    Args:\n+        weight_shape: Shape of the weight tensor (out_features, in_features).\n+        group_shape: Blockwise quantization group shape.\n+        cutlass_block_fp8_supported: Whether CUTLASS blockwise FP8 is available.\n+        use_aiter_and_is_supported: Whether to use aiter quantization ops.\n+        transpose_weights: Whether to transpose weights after creation.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        weight_shape: tuple[int, int],\n+        group_shape: GroupShape,\n+        cutlass_block_fp8_supported: bool = False,\n+        use_aiter_and_is_supported: bool = False,\n+        transpose_weights: bool = False,\n+    ):\n+        weight_scale_shape = weight_shape[0] // group_shape[1]\n+        self.weight_scale = torch.rand(\n+            (weight_scale_shape, weight_scale_shape), dtype=torch.float32\n+        )\n+        self.weight = torch.rand(weight_shape).to(dtype=FP8_DTYPE)\n+        self.input_scale = None\n+        if transpose_weights:\n+            self.weight = self.weight.t()\n+\n+        self.linear_op = W8A8BlockFp8LinearOp(\n+            weight_group_shape=GroupShape(group_shape[1], group_shape[1]),\n+            act_quant_group_shape=group_shape,\n+            cutlass_block_fp8_supported=cutlass_block_fp8_supported,\n+            use_aiter_and_is_supported=use_aiter_and_is_supported,\n+        )\n+\n+    def __call__(\n+        self, y: torch.Tensor, bias: torch.Tensor | None = None\n+    ) -> torch.Tensor:\n+        return self.linear_op.apply(\n+            input=y,\n+            weight=self.weight,\n+            weight_scale=self.weight_scale,\n+            input_scale=self.input_scale,\n+            bias=bias,\n+        )\n+\n+    def is_quant_fp8_enabled(self) -> bool:\n+        return self.linear_op.input_quant_op.enabled()", "patch_sha256": "ed7b4088150ad6b5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2662381538", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/compile/distributed/test_fusion_all_reduce.py\nReview Comment:\nLet's move this init into `TestFP8Layer`, and add optional params if anyone wants to override any of the weights/scales. That way we're not repeating ourselves in every test\n\nOriginal Code Context:\n@@ -76,49 +75,51 @@ def ops_in_model_after(self):\n \n \n class TestAllReduceRMSNormStaticQuantFP8Model(torch.nn.Module):\n+    quant_key = kFp8StaticTensorSym\n+\n     def __init__(self, hidden_size=16, token_num=16, eps=1e-6):\n         super().__init__()\n         self.hidden_size = hidden_size\n         self.eps = eps\n         self.norm = [RMSNorm(hidden_size, eps) for i in range(4)]\n         self.wscale = [torch.rand(1, dtype=torch.float32) for _ in range(3)]\n-        self.w = [\n+        self.input_scale = [torch.rand(1, dtype=torch.float32) for _ in range(3)]\n+        self.weight = [\n             torch.rand(hidden_size, hidden_size)\n             .to(dtype=current_platform.fp8_dtype())\n             .t()\n             for _ in range(3)\n         ]\n", "materials": ["Let's move this init into `TestFP8Layer`, and add optional params if anyone wants to override any of the weights/scales. That way we're not repeating ourselves in every test"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -26,15 +26,14 @@\n     initialize_model_parallel,\n )\n from vllm.model_executor.layers.layernorm import RMSNorm\n-from vllm.model_executor.layers.quantization.utils.w8a8_utils import (\n-    Fp8LinearOp,\n-    GroupShape,\n+from vllm.model_executor.layers.quantization.utils.quant_utils import (\n+    kFp8StaticTensorSym,\n )\n from vllm.platforms import current_platform\n from vllm.utils.system_utils import update_environment_variables\n from vllm.utils.torch_utils import set_random_seed\n \n-from ...utils import has_module_attribute, multi_gpu_test\n+from ...utils import TestFP8Layer, has_module_attribute, multi_gpu_test\n from ..backend import TestBackend\n \n \n@@ -76,49 +75,40 @@ def ops_in_model_after(self):\n \n \n class TestAllReduceRMSNormStaticQuantFP8Model(torch.nn.Module):\n+    quant_key = kFp8StaticTensorSym\n+\n     def __init__(self, hidden_size=16, token_num=16, eps=1e-6):\n         super().__init__()\n         self.hidden_size = hidden_size\n         self.eps = eps\n         self.norm = [RMSNorm(hidden_size, eps) for i in range(4)]\n-        self.wscale = [torch.rand(1, dtype=torch.float32) for _ in range(3)]\n-        self.w = [\n-            torch.rand(hidden_size, hidden_size)\n-            .to(dtype=current_platform.fp8_dtype())\n-            .t()\n-            for _ in range(3)\n+        self.fp8_linear_layers = [\n+            TestFP8Layer(\n+                weight_shape=(hidden_size, hidden_size),\n+                activation_quant_key=self.quant_key,\n+                weight_quant_key=self.quant_key,\n+            )\n+            for i in range(3)\n         ]\n \n-        self.fp8_linear = Fp8LinearOp(\n-            act_quant_static=True,\n-            act_quant_group_shape=GroupShape.PER_TENSOR,\n-        )\n-\n-        self.scale = [torch.rand(1, dtype=torch.float32) for _ in range(3)]\n-\n     def forward(self, hidden_states):\n         # avoid having graph input be an arg to a pattern directly\n         z = torch.relu(hidden_states)\n         x = resid = tensor_model_parallel_all_reduce(z)\n         y = self.norm[0](x)\n \n-        z2 = self.fp8_linear.apply(\n-            y, self.w[0], self.wscale[0], input_scale=self.scale[0]\n-        )\n+        z2 = self.fp8_linear_layers[0](y)\n \n         x2 = tensor_model_parallel_all_reduce(z2)\n         y2, resid = self.norm[1](x2, resid)\n \n-        z3 = self.fp8_linear.apply(\n-            y2, self.w[1], self.wscale[1], input_scale=self.scale[1]\n-        )\n+        z3 = self.fp8_linear_layers[1](y2)\n \n         x3 = tensor_model_parallel_all_reduce(z3)\n         y3, resid = self.norm[2](x3, resid)  # use resid here\n \n-        z4 = self.fp8_linear.apply(\n-            y3, self.w[2], self.wscale[2], input_scale=self.scale[2]\n-        )\n+        z4 = self.fp8_linear_layers[2](y3)\n+\n         x4 = tensor_model_parallel_all_reduce(z4)\n         y4, resid = self.norm[3](x4, resid)  # use resid here\n         return y4\n@@ -130,7 +120,7 @@ def ops_in_model_before(self):\n         return [\n             torch.ops.vllm.all_reduce.default,\n             torch.ops._C.static_scaled_fp8_quant.default\n-            if self.fp8_linear.quant_fp8.enabled()\n+            if self.fp8_linear_layers[0].is_quant_fp8_enabled()\n             else torch.ops.aten.reciprocal.default,\n         ]\n ", "patch_sha256": "34a08e913d0d138a"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2684394977", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/compile/test_fusion_attn.py\nReview Comment:\nThis code is no longer used?\n\nOriginal Code Context:\n@@ -187,16 +183,16 @@ def __init__(self, *args, **kwargs):\n                 \"scale\": torch.tensor([1.0], dtype=torch.float32, device=self.device),\n             },\n         )\n", "materials": ["This code is no longer used?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -45,14 +45,15 @@\n     kFp8StaticTensorSym,\n     kNvfp4Quant,\n )\n-from vllm.model_executor.layers.quantization.utils.w8a8_utils import Fp8LinearOp\n from vllm.platforms import current_platform\n from vllm.utils.flashinfer import has_flashinfer\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n from vllm.v1.attention.backend import AttentionMetadata\n from vllm.v1.attention.backends.registry import AttentionBackendEnum\n from vllm.v1.kv_cache_interface import AttentionSpec\n \n+from ..utils import TestFP8Layer\n+\n FP8_DTYPE = current_platform.fp8_dtype()\n FP4_DTYPE = torch.uint8\n \n@@ -185,32 +186,30 @@ class TestAttentionFp8StaticQuantPatternModel(AttentionQuantPatternModel):\n     def __init__(self, *args, **kwargs):\n         super().__init__(*args, **kwargs)\n \n-        self.fp8_linear = Fp8LinearOp(\n-            act_quant_static=self.quant_key.scale.static,\n-            act_quant_group_shape=self.quant_key.scale.group_shape,\n-        )\n-\n         hidden_size = self.num_qo_heads * self.head_size\n-        self.w = kwargs.get(\n-            \"w\",\n-            {\n-                \"weight\": torch.randn(hidden_size, hidden_size)\n-                .to(dtype=FP8_DTYPE, device=self.device)\n-                .t(),\n-                \"wscale\": torch.tensor([1.0], dtype=torch.float32, device=self.device),\n-                \"scale\": torch.tensor([1.0], dtype=torch.float32, device=self.device),\n-            },\n+        self.fp8_linear = TestFP8Layer(\n+            weight_shape=(hidden_size, hidden_size),\n+            activation_quant_key=self.quant_key,\n+            weight_quant_key=self.quant_key,\n+            device=self.device,\n         )\n \n+        w = kwargs.get(\"w\")\n+        if w is not None:\n+            self.fp8_linear.weight = w[\"weight\"]\n+            self.fp8_linear.weight_scale = w[\"wscale\"]\n+            self.fp8_linear.input_scale = w[\"scale\"]\n+\n+        self.w = {\n+            \"weight\": self.fp8_linear.weight,\n+            \"wscale\": self.fp8_linear.weight_scale,\n+            \"scale\": self.fp8_linear.input_scale,\n+        }\n+\n     def forward(self, q: torch.Tensor, k: torch.Tensor, v: torch.Tensor):\n         \"\"\"Forward pass that creates the pattern to be fused.\"\"\"\n         attn_output = self.attn(q, k, v)\n-        return self.fp8_linear.apply(\n-            input=attn_output,\n-            weight=self.w[\"weight\"],\n-            weight_scale=self.w[\"wscale\"],\n-            input_scale=self.w[\"scale\"],\n-        )\n+        return self.fp8_linear(attn_output)\n \n \n class TestAttentionNvfp4QuantPatternModel(AttentionQuantPatternModel):", "patch_sha256": "55010739ad1dc048"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2684397534", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/utils.py\nReview Comment:\nAdd TODO with link to #31818 to fold this into `TestFP8Layer`\n\nOriginal Code Context:\n@@ -1332,3 +1345,109 @@ def flat_product(*iterables: Iterable[Any]):\n     for element in itertools.product(*iterables):\n         normalized = (e if isinstance(e, tuple) else (e,) for e in element)\n         yield tuple(itertools.chain(*normalized))\n+\n+\n+class TestFP8Layer(torch.nn.Module):\n+    \"\"\"\n+    Test helper for FP8 linear operations. Creates random weights and scales\n+    based on quantization configuration.\n+\n+    Args:\n+        weight_shape: Shape of the weight tensor (out_features, in_features).\n+        activation_quant_key: Activation quantization configuration.\n+        weight_quant_key: Weight quantization configuration.\n+        out_dtype: Output dtype. Defaults to current default dtype.\n+        force_kernel: Optional kernel to force use of specific implementation.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        weight_shape: tuple[int, int],\n+        activation_quant_key: QuantKey,\n+        weight_quant_key: QuantKey,\n+        out_dtype: torch.dtype | None = None,\n+        force_kernel: FP8ScaledMMLinearKernel | None = None,\n+    ):\n+        super().__init__()\n+        per_tensor_weights = weight_quant_key.scale.group_shape.is_per_tensor()\n+        is_static_activation_scale = activation_quant_key.scale.static\n+        weight_scale_shape = (1,) if per_tensor_weights else (weight_shape[0], 1)\n+\n+        self.weight_scale = torch.rand(weight_scale_shape, dtype=torch.float32)\n+        self.input_scale = (\n+            torch.rand(1, dtype=torch.float32) if is_static_activation_scale else None\n+        )\n+        self.weight = torch.rand(weight_shape).to(dtype=FP8_DTYPE).t()\n+        self.input_scale_ub = None\n+\n+        out_dtype = torch.get_default_dtype() if out_dtype is None else out_dtype\n+\n+        self.kernel = init_fp8_linear_kernel(\n+            activation_quant_key=activation_quant_key,\n+            weight_quant_key=weight_quant_key,\n+            out_dtype=out_dtype,\n+            force_kernel=force_kernel,\n+        )\n+\n+    def is_quant_fp8_enabled(self) -> bool:\n+        return self.kernel.quant_fp8.enabled()\n+\n+    def forward(\n+        self, y: torch.Tensor, bias: torch.Tensor | None = None\n+    ) -> torch.Tensor:\n+        return self.kernel.apply_weights(self, y, bias)\n+\n+\n+class TestBlockFP8Layer:\n", "materials": ["Add TODO with link to #31818 to fold this into `TestFP8Layer`"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -42,6 +42,17 @@\n )\n from vllm.engine.arg_utils import AsyncEngineArgs\n from vllm.entrypoints.cli.serve import ServeSubcommand\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm import (\n+    init_fp8_linear_kernel,\n+)\n+from vllm.model_executor.layers.quantization.kernels.scaled_mm.ScaledMMLinearKernel import (  # noqa: E501\n+    FP8ScaledMMLinearKernel,\n+)\n+from vllm.model_executor.layers.quantization.utils.fp8_utils import W8A8BlockFp8LinearOp\n+from vllm.model_executor.layers.quantization.utils.quant_utils import (\n+    GroupShape,\n+    QuantKey,\n+)\n from vllm.model_executor.model_loader import get_model_loader\n from vllm.platforms import current_platform\n from vllm.tokenizers import get_tokenizer\n@@ -50,6 +61,8 @@\n from vllm.utils.network_utils import get_open_port\n from vllm.utils.torch_utils import cuda_device_count_stateless\n \n+FP8_DTYPE = current_platform.fp8_dtype()\n+\n if current_platform.is_rocm():\n     from amdsmi import (\n         amdsmi_get_gpu_vram_usage,\n@@ -1332,3 +1345,117 @@ def flat_product(*iterables: Iterable[Any]):\n     for element in itertools.product(*iterables):\n         normalized = (e if isinstance(e, tuple) else (e,) for e in element)\n         yield tuple(itertools.chain(*normalized))\n+\n+\n+class TestFP8Layer(torch.nn.Module):\n+    \"\"\"\n+    Test helper for FP8 linear operations. Creates random weights and scales\n+    based on quantization configuration.\n+\n+    Args:\n+        weight_shape: Shape of the weight tensor (out_features, in_features).\n+        activation_quant_key: Activation quantization configuration.\n+        weight_quant_key: Weight quantization configuration.\n+        out_dtype: Output dtype. Defaults to current default dtype.\n+        force_kernel: Optional kernel to force use of specific implementation.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        weight_shape: tuple[int, int],\n+        activation_quant_key: QuantKey,\n+        weight_quant_key: QuantKey,\n+        out_dtype: torch.dtype | None = None,\n+        device: torch.device | None = None,\n+        force_kernel: FP8ScaledMMLinearKernel | None = None,\n+    ):\n+        super().__init__()\n+        per_tensor_weights = weight_quant_key.scale.group_shape.is_per_tensor()\n+        is_static_activation_scale = activation_quant_key.scale.static\n+        weight_scale_shape = (1,) if per_tensor_weights else (weight_shape[0], 1)\n+\n+        self.weight_scale = torch.rand(\n+            weight_scale_shape, dtype=torch.float32, device=device\n+        )\n+        self.input_scale = (\n+            torch.rand(1, dtype=torch.float32, device=device)\n+            if is_static_activation_scale\n+            else None\n+        )\n+        self.weight = torch.rand(weight_shape, device=device).to(dtype=FP8_DTYPE).t()\n+        self.input_scale_ub = None\n+\n+        out_dtype = torch.get_default_dtype() if out_dtype is None else out_dtype\n+\n+        self.kernel = init_fp8_linear_kernel(\n+            activation_quant_key=activation_quant_key,\n+            weight_quant_key=weight_quant_key,\n+            out_dtype=out_dtype,\n+            force_kernel=force_kernel,\n+        )\n+\n+    def is_quant_fp8_enabled(self) -> bool:\n+        return self.kernel.quant_fp8.enabled()\n+\n+    def forward(\n+        self, y: torch.Tensor, bias: torch.Tensor | None = None\n+    ) -> torch.Tensor:\n+        return self.kernel.apply_weights(self, y, bias)\n+\n+\n+# TODO: Drop TestBlockFP8Layer in favour of a unified TestFP8Layer\n+# after refactoring W8A8BlockFp8LinearOp.\n+# https://github.com/vllm-project/vllm/issues/31818\n+class TestBlockFP8Layer:\n+    \"\"\"\n+    Test helper for blockwise FP8 linear operations. Creates random weights\n+    and scales for W8A8BlockFp8LinearOp.\n+\n+    This is a workaround until W8A8BlockFp8LinearOp implements the kernel\n+    abstraction (ScaledMMLinearKernel) for blockwise quantization.\n+\n+    Args:\n+        weight_shape: Shape of the weight tensor (out_features, in_features).\n+        group_shape: Blockwise quantization group shape.\n+        cutlass_block_fp8_supported: Whether CUTLASS blockwise FP8 is available.\n+        use_aiter_and_is_supported: Whether to use aiter quantization ops.\n+        transpose_weights: Whether to transpose weights after creation.\n+    \"\"\"\n+\n+    def __init__(\n+        self,\n+        weight_shape: tuple[int, int],\n+        group_shape: GroupShape,\n+        cutlass_block_fp8_supported: bool = False,\n+        use_aiter_and_is_supported: bool = False,\n+        transpose_weights: bool = False,\n+    ):\n+        weight_scale_shape = weight_shape[0] // group_shape[1]\n+        self.weight_scale = torch.rand(\n+            (weight_scale_shape, weight_scale_shape), dtype=torch.float32\n+        )\n+        self.weight = torch.rand(weight_shape).to(dtype=FP8_DTYPE)\n+        self.input_scale = None\n+        if transpose_weights:\n+            self.weight = self.weight.t()\n+\n+        self.linear_op = W8A8BlockFp8LinearOp(\n+            weight_group_shape=GroupShape(group_shape[1], group_shape[1]),\n+            act_quant_group_shape=group_shape,\n+            cutlass_block_fp8_supported=cutlass_block_fp8_supported,\n+            use_aiter_and_is_supported=use_aiter_and_is_supported,\n+        )\n+\n+    def __call__(\n+        self, y: torch.Tensor, bias: torch.Tensor | None = None\n+    ) -> torch.Tensor:\n+        return self.linear_op.apply(\n+            input=y,\n+            weight=self.weight,\n+            weight_scale=self.weight_scale,\n+            input_scale=self.input_scale,\n+            bias=bias,\n+        )\n+\n+    def is_quant_fp8_enabled(self) -> bool:\n+        return self.linear_op.input_quant_op.enabled()", "patch_sha256": "ed7b4088150ad6b5"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2699487141", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/compile/test_fusion_attn.py\nReview Comment:\nWhy was this needed? Isn't this the default also?\n\nOriginal Code Context:\n@@ -178,6 +181,9 @@ def __init__(self, *args, **kwargs):\n             activation_quant_key=self.quant_key,\n             weight_quant_key=self.quant_key,\n             device=self.device,\n+            force_kernel=CutlassFP8ScaledMMLinearKernel\n", "materials": ["Why was this needed? Isn't this the default also?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -45,14 +45,15 @@\n     kFp8StaticTensorSym,\n     kNvfp4Quant,\n )\n-from vllm.model_executor.layers.quantization.utils.w8a8_utils import Fp8LinearOp\n from vllm.platforms import current_platform\n from vllm.utils.flashinfer import has_flashinfer\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n from vllm.v1.attention.backend import AttentionMetadata\n from vllm.v1.attention.backends.registry import AttentionBackendEnum\n from vllm.v1.kv_cache_interface import AttentionSpec\n \n+from ..utils import TestFP8Layer\n+\n FP8_DTYPE = current_platform.fp8_dtype()\n FP4_DTYPE = torch.uint8\n \n@@ -185,32 +186,30 @@ class TestAttentionFp8StaticQuantPatternModel(AttentionQuantPatternModel):\n     def __init__(self, *args, **kwargs):\n         super().__init__(*args, **kwargs)\n \n-        self.fp8_linear = Fp8LinearOp(\n-            act_quant_static=self.quant_key.scale.static,\n-            act_quant_group_shape=self.quant_key.scale.group_shape,\n-        )\n-\n         hidden_size = self.num_qo_heads * self.head_size\n-        self.w = kwargs.get(\n-            \"w\",\n-            {\n-                \"weight\": torch.randn(hidden_size, hidden_size)\n-                .to(dtype=FP8_DTYPE, device=self.device)\n-                .t(),\n-                \"wscale\": torch.tensor([1.0], dtype=torch.float32, device=self.device),\n-                \"scale\": torch.tensor([1.0], dtype=torch.float32, device=self.device),\n-            },\n+        self.fp8_linear = TestFP8Layer(\n+            weight_shape=(hidden_size, hidden_size),\n+            activation_quant_key=self.quant_key,\n+            weight_quant_key=self.quant_key,\n+            device=self.device,\n         )\n \n+        w = kwargs.get(\"w\")\n+        if w is not None:\n+            self.fp8_linear.weight = w[\"weight\"]\n+            self.fp8_linear.weight_scale = w[\"wscale\"]\n+            self.fp8_linear.input_scale = w[\"scale\"]\n+\n+        self.w = {\n+            \"weight\": self.fp8_linear.weight,\n+            \"wscale\": self.fp8_linear.weight_scale,\n+            \"scale\": self.fp8_linear.input_scale,\n+        }\n+\n     def forward(self, q: torch.Tensor, k: torch.Tensor, v: torch.Tensor):\n         \"\"\"Forward pass that creates the pattern to be fused.\"\"\"\n         attn_output = self.attn(q, k, v)\n-        return self.fp8_linear.apply(\n-            input=attn_output,\n-            weight=self.w[\"weight\"],\n-            weight_scale=self.w[\"wscale\"],\n-            input_scale=self.w[\"scale\"],\n-        )\n+        return self.fp8_linear(attn_output)\n \n \n class TestAttentionNvfp4QuantPatternModel(AttentionQuantPatternModel):", "patch_sha256": "55010739ad1dc048"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27814/review_2700750214", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27814", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/compile/test_fusion_attn.py\nReview Comment:\nThe priority list places FlashInfer first for CUDA platforms, this was added to debug a CI test failure and was promptly removed afterwards.\n\nOriginal Code Context:\n@@ -178,6 +181,9 @@ def __init__(self, *args, **kwargs):\n             activation_quant_key=self.quant_key,\n             weight_quant_key=self.quant_key,\n             device=self.device,\n+            force_kernel=CutlassFP8ScaledMMLinearKernel\n", "materials": ["The priority list places FlashInfer first for CUDA platforms, this was added to debug a CI test failure and was promptly removed afterwards."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -45,14 +45,15 @@\n     kFp8StaticTensorSym,\n     kNvfp4Quant,\n )\n-from vllm.model_executor.layers.quantization.utils.w8a8_utils import Fp8LinearOp\n from vllm.platforms import current_platform\n from vllm.utils.flashinfer import has_flashinfer\n from vllm.utils.torch_utils import is_torch_equal_or_newer\n from vllm.v1.attention.backend import AttentionMetadata\n from vllm.v1.attention.backends.registry import AttentionBackendEnum\n from vllm.v1.kv_cache_interface import AttentionSpec\n \n+from ..utils import TestFP8Layer\n+\n FP8_DTYPE = current_platform.fp8_dtype()\n FP4_DTYPE = torch.uint8\n \n@@ -185,32 +186,30 @@ class TestAttentionFp8StaticQuantPatternModel(AttentionQuantPatternModel):\n     def __init__(self, *args, **kwargs):\n         super().__init__(*args, **kwargs)\n \n-        self.fp8_linear = Fp8LinearOp(\n-            act_quant_static=self.quant_key.scale.static,\n-            act_quant_group_shape=self.quant_key.scale.group_shape,\n-        )\n-\n         hidden_size = self.num_qo_heads * self.head_size\n-        self.w = kwargs.get(\n-            \"w\",\n-            {\n-                \"weight\": torch.randn(hidden_size, hidden_size)\n-                .to(dtype=FP8_DTYPE, device=self.device)\n-                .t(),\n-                \"wscale\": torch.tensor([1.0], dtype=torch.float32, device=self.device),\n-                \"scale\": torch.tensor([1.0], dtype=torch.float32, device=self.device),\n-            },\n+        self.fp8_linear = TestFP8Layer(\n+            weight_shape=(hidden_size, hidden_size),\n+            activation_quant_key=self.quant_key,\n+            weight_quant_key=self.quant_key,\n+            device=self.device,\n         )\n \n+        w = kwargs.get(\"w\")\n+        if w is not None:\n+            self.fp8_linear.weight = w[\"weight\"]\n+            self.fp8_linear.weight_scale = w[\"wscale\"]\n+            self.fp8_linear.input_scale = w[\"scale\"]\n+\n+        self.w = {\n+            \"weight\": self.fp8_linear.weight,\n+            \"wscale\": self.fp8_linear.weight_scale,\n+            \"scale\": self.fp8_linear.input_scale,\n+        }\n+\n     def forward(self, q: torch.Tensor, k: torch.Tensor, v: torch.Tensor):\n         \"\"\"Forward pass that creates the pattern to be fused.\"\"\"\n         attn_output = self.attn(q, k, v)\n-        return self.fp8_linear.apply(\n-            input=attn_output,\n-            weight=self.w[\"weight\"],\n-            weight_scale=self.w[\"wscale\"],\n-            input_scale=self.w[\"scale\"],\n-        )\n+        return self.fp8_linear(attn_output)\n \n \n class TestAttentionNvfp4QuantPatternModel(AttentionQuantPatternModel):", "patch_sha256": "55010739ad1dc048"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2717129387", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/v1/kv_connector/unit/test_offloading_connector.py\nReview Comment:\nnit: I would remove the comments\n\nOriginal Code Context:\n@@ -86,7 +87,14 @@ def complete_jobs(self, job_ids: set[int]) -> None:\n             if job_id in self.waiting_jobs:\n                 self.waiting_jobs.remove(job_id)\n                 self.completed_jobs.append(job_id)\n-                self.completed_transfers.append((job_id, True))\n+                result = TransferResult(\n+                    job_id=job_id,\n+                    success=True,\n+                    transfer_size=None,  # For now - just to get the test working\n+                    transfer_time=None,  # For now - just to get the test working\n+                    transfer_type=None,  # For now - just to get the test working\n", "materials": ["nit: I would remove the comments"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -16,6 +16,7 @@\n from vllm.distributed.kv_transfer.kv_connector.v1.offloading_connector import (\n     OffloadingConnector,\n     OffloadingConnectorMetadata,\n+    OffloadingConnectorStats,\n )\n from vllm.forward_context import ForwardContext\n from vllm.utils.hashing import sha256\n@@ -86,7 +87,14 @@ def complete_jobs(self, job_ids: set[int]) -> None:\n             if job_id in self.waiting_jobs:\n                 self.waiting_jobs.remove(job_id)\n                 self.completed_jobs.append(job_id)\n-                self.completed_transfers.append((job_id, True))\n+                result = TransferResult(\n+                    job_id=job_id,\n+                    success=True,\n+                    transfer_size=None,\n+                    transfer_time=None,\n+                    transfer_type=None,\n+                )\n+                self.completed_transfers.append(result)\n \n     def wait(self, job_ids: set[int]) -> None:\n         self.flushed_jobs |= job_ids\n@@ -720,3 +728,144 @@ def test_concurrent_lookups_of_the_same_prefix(request_runner):\n \n     # second request will use the GPU prefix cache\n     assert transfer_jobs == list(runner.offloading_spec.handler.transfer_specs)\n+\n+\n+class TestOffloadingConnectorStats:\n+    \"\"\"Tests for OffloadingConnector stats reconstruction and operations.\"\"\"\n+\n+    def test_build_kv_connector_stats_with_none(self):\n+        \"\"\"Test that build_kv_connector_stats returns empty stats when given None.\"\"\"\n+        stats = OffloadingConnector.build_kv_connector_stats(data=None)\n+\n+        assert stats is not None\n+        assert isinstance(stats, OffloadingConnectorStats)\n+        assert len(stats.data) == 0\n+        assert stats.is_empty()\n+\n+    def test_build_kv_connector_stats_with_empty_dict(self):\n+        \"\"\"Test that build_kv_connector_stats returns empty stats with empty dict.\"\"\"\n+        stats = OffloadingConnector.build_kv_connector_stats(data={})\n+\n+        assert stats is not None\n+        assert isinstance(stats, OffloadingConnectorStats)\n+        assert len(stats.data) == 0\n+        assert stats.is_empty()\n+\n+    def test_build_kv_connector_stats_reconstructs_offload_stats(self):\n+        \"\"\"Test that OffloadingConnector stats are properly reconstructed with\n+        correct data.\"\"\"\n+        serialized_data = {\n+            \"CPU_to_GPU\": [\n+                {\"op_size\": 16, \"op_time\": 1.0},\n+                {\"op_size\": 8, \"op_time\": 0.5},\n+            ],\n+            \"GPU_to_CPU\": [\n+                {\"op_size\": 1, \"op_time\": 0.1},\n+                {\"op_size\": 2, \"op_time\": 0.2},\n+            ],\n+        }\n+\n+        stats = OffloadingConnector.build_kv_connector_stats(data=serialized_data)\n+\n+        offload_connector_stats = stats\n+        assert isinstance(offload_connector_stats, OffloadingConnectorStats)\n+        assert offload_connector_stats.data[\"CPU_to_GPU\"] == [\n+            {\"op_size\": 16, \"op_time\": 1.0},\n+            {\"op_size\": 8, \"op_time\": 0.5},\n+        ]\n+        assert offload_connector_stats.data[\"GPU_to_CPU\"] == [\n+            {\"op_size\": 1, \"op_time\": 0.1},\n+            {\"op_size\": 2, \"op_time\": 0.2},\n+        ]\n+\n+    def test_aggregate_same_connector(self):\n+        \"\"\"Test aggregating stats from the same connector type.\"\"\"\n+        stats1 = OffloadingConnectorStats(\n+            data={\n+                \"CPU_to_GPU\": [\n+                    {\"op_size\": 16, \"op_time\": 1.0},\n+                    {\"op_size\": 8, \"op_time\": 0.5},\n+                ],\n+                \"GPU_to_CPU\": [\n+                    {\"op_size\": 1, \"op_time\": 0.1},\n+                    {\"op_size\": 2, \"op_time\": 0.2},\n+                ],\n+            }\n+        )\n+\n+        stats2 = OffloadingConnectorStats(\n+            data={\n+                \"CPU_to_GPU\": [\n+                    {\"op_size\": 3, \"op_time\": 0.2},\n+                    {\"op_size\": 7, \"op_time\": 0.9},\n+                ],\n+                \"GPU_to_CPU\": [{\"op_size\": 16, \"op_time\": 2}],\n+            }\n+        )\n+\n+        result = stats1.aggregate(stats2)\n+\n+        assert result is stats1  # Should return self\n+        offload_connector_stats = result\n+        assert offload_connector_stats.data[\"CPU_to_GPU\"] == [\n+            {\"op_size\": 16, \"op_time\": 1.0},\n+            {\"op_size\": 8, \"op_time\": 0.5},\n+            {\"op_size\": 3, \"op_time\": 0.2},\n+            {\"op_size\": 7, \"op_time\": 0.9},\n+        ]\n+        assert offload_connector_stats.data[\"GPU_to_CPU\"] == [\n+            {\"op_size\": 1, \"op_time\": 0.1},\n+            {\"op_size\": 2, \"op_time\": 0.2},\n+            {\"op_size\": 16, \"op_time\": 2},\n+        ]\n+\n+    def test_reduce(self):\n+        \"\"\"Test that reduce() correctly reduces all nested connector stats.\"\"\"\n+        stats = OffloadingConnectorStats(\n+            data={\n+                \"CPU_to_GPU\": [\n+                    {\"op_size\": 16, \"op_time\": 1.0},\n+                    {\"op_size\": 8, \"op_time\": 0.5},\n+                    {\"op_size\": 3, \"op_time\": 0.2},\n+                    {\"op_size\": 7, \"op_time\": 0.9},\n+                ],\n+                \"GPU_to_CPU\": [\n+                    {\"op_size\": 1, \"op_time\": 0.1},\n+                    {\"op_size\": 2, \"op_time\": 0.2},\n+                    {\"op_size\": 16, \"op_time\": 2},\n+                ],\n+            }\n+        )\n+\n+        reduced = stats.reduce()\n+\n+        assert isinstance(reduced, dict)\n+        # Check that the stats were reduced (should have aggregated values)\n+        assert \"CPU_to_GPU_total_bytes\" in reduced\n+        assert \"CPU_to_GPU_total_time\" in reduced\n+        assert \"GPU_to_CPU_total_bytes\" in reduced\n+        assert \"GPU_to_CPU_total_time\" in reduced\n+        assert reduced[\"CPU_to_GPU_total_bytes\"] == 34\n+        assert reduced[\"CPU_to_GPU_total_time\"] == 2.6\n+        assert reduced[\"GPU_to_CPU_total_time\"] == 2.3\n+        assert reduced[\"GPU_to_CPU_total_bytes\"] == 19\n+\n+    def test_reset(self):\n+        \"\"\"Test that reset() resets all nested connector stats.\"\"\"\n+        offload_connector_stats = OffloadingConnectorStats(\n+            data={\n+                \"CPU_to_GPU\": [\n+                    {\"op_size\": 3, \"op_time\": 0.2},\n+                    {\"op_size\": 7, \"op_time\": 0.9},\n+                ],\n+                \"GPU_to_CPU\": [{\"op_size\": 16, \"op_time\": 2}],\n+            }\n+        )\n+\n+        assert not offload_connector_stats.is_empty()\n+\n+        offload_connector_stats.reset()\n+\n+        # After reset, stats should be empty\n+        assert offload_connector_stats.is_empty()\n+        assert len(offload_connector_stats.data) == 0", "patch_sha256": "3b4a7692b70cb1ad"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2717131093", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/v1/kv_connector/unit/test_offloading_connector.py\nReview Comment:\n```suggestion\r\n    \"\"\"Tests for OffloadingConnector stats reconstruction and operations.\"\"\"\r\n```\n\nOriginal Code Context:\n@@ -717,3 +725,144 @@ def test_concurrent_lookups_of_the_same_prefix(request_runner):\n \n     # second request will use the GPU prefix cache\n     assert transfer_jobs == list(runner.offloading_spec.handler.transfer_specs)\n+\n+\n+class TestOffloadingConnectorStats:\n+    \"\"\"Tests for MultiConnector stats reconstruction and operations.\"\"\"\n", "materials": ["```suggestion\r\n    \"\"\"Tests for OffloadingConnector stats reconstruction and operations.\"\"\"\r\n```"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -16,6 +16,7 @@\n from vllm.distributed.kv_transfer.kv_connector.v1.offloading_connector import (\n     OffloadingConnector,\n     OffloadingConnectorMetadata,\n+    OffloadingConnectorStats,\n )\n from vllm.forward_context import ForwardContext\n from vllm.utils.hashing import sha256\n@@ -86,7 +87,14 @@ def complete_jobs(self, job_ids: set[int]) -> None:\n             if job_id in self.waiting_jobs:\n                 self.waiting_jobs.remove(job_id)\n                 self.completed_jobs.append(job_id)\n-                self.completed_transfers.append((job_id, True))\n+                result = TransferResult(\n+                    job_id=job_id,\n+                    success=True,\n+                    transfer_size=None,\n+                    transfer_time=None,\n+                    transfer_type=None,\n+                )\n+                self.completed_transfers.append(result)\n \n     def wait(self, job_ids: set[int]) -> None:\n         self.flushed_jobs |= job_ids\n@@ -720,3 +728,144 @@ def test_concurrent_lookups_of_the_same_prefix(request_runner):\n \n     # second request will use the GPU prefix cache\n     assert transfer_jobs == list(runner.offloading_spec.handler.transfer_specs)\n+\n+\n+class TestOffloadingConnectorStats:\n+    \"\"\"Tests for OffloadingConnector stats reconstruction and operations.\"\"\"\n+\n+    def test_build_kv_connector_stats_with_none(self):\n+        \"\"\"Test that build_kv_connector_stats returns empty stats when given None.\"\"\"\n+        stats = OffloadingConnector.build_kv_connector_stats(data=None)\n+\n+        assert stats is not None\n+        assert isinstance(stats, OffloadingConnectorStats)\n+        assert len(stats.data) == 0\n+        assert stats.is_empty()\n+\n+    def test_build_kv_connector_stats_with_empty_dict(self):\n+        \"\"\"Test that build_kv_connector_stats returns empty stats with empty dict.\"\"\"\n+        stats = OffloadingConnector.build_kv_connector_stats(data={})\n+\n+        assert stats is not None\n+        assert isinstance(stats, OffloadingConnectorStats)\n+        assert len(stats.data) == 0\n+        assert stats.is_empty()\n+\n+    def test_build_kv_connector_stats_reconstructs_offload_stats(self):\n+        \"\"\"Test that OffloadingConnector stats are properly reconstructed with\n+        correct data.\"\"\"\n+        serialized_data = {\n+            \"CPU_to_GPU\": [\n+                {\"op_size\": 16, \"op_time\": 1.0},\n+                {\"op_size\": 8, \"op_time\": 0.5},\n+            ],\n+            \"GPU_to_CPU\": [\n+                {\"op_size\": 1, \"op_time\": 0.1},\n+                {\"op_size\": 2, \"op_time\": 0.2},\n+            ],\n+        }\n+\n+        stats = OffloadingConnector.build_kv_connector_stats(data=serialized_data)\n+\n+        offload_connector_stats = stats\n+        assert isinstance(offload_connector_stats, OffloadingConnectorStats)\n+        assert offload_connector_stats.data[\"CPU_to_GPU\"] == [\n+            {\"op_size\": 16, \"op_time\": 1.0},\n+            {\"op_size\": 8, \"op_time\": 0.5},\n+        ]\n+        assert offload_connector_stats.data[\"GPU_to_CPU\"] == [\n+            {\"op_size\": 1, \"op_time\": 0.1},\n+            {\"op_size\": 2, \"op_time\": 0.2},\n+        ]\n+\n+    def test_aggregate_same_connector(self):\n+        \"\"\"Test aggregating stats from the same connector type.\"\"\"\n+        stats1 = OffloadingConnectorStats(\n+            data={\n+                \"CPU_to_GPU\": [\n+                    {\"op_size\": 16, \"op_time\": 1.0},\n+                    {\"op_size\": 8, \"op_time\": 0.5},\n+                ],\n+                \"GPU_to_CPU\": [\n+                    {\"op_size\": 1, \"op_time\": 0.1},\n+                    {\"op_size\": 2, \"op_time\": 0.2},\n+                ],\n+            }\n+        )\n+\n+        stats2 = OffloadingConnectorStats(\n+            data={\n+                \"CPU_to_GPU\": [\n+                    {\"op_size\": 3, \"op_time\": 0.2},\n+                    {\"op_size\": 7, \"op_time\": 0.9},\n+                ],\n+                \"GPU_to_CPU\": [{\"op_size\": 16, \"op_time\": 2}],\n+            }\n+        )\n+\n+        result = stats1.aggregate(stats2)\n+\n+        assert result is stats1  # Should return self\n+        offload_connector_stats = result\n+        assert offload_connector_stats.data[\"CPU_to_GPU\"] == [\n+            {\"op_size\": 16, \"op_time\": 1.0},\n+            {\"op_size\": 8, \"op_time\": 0.5},\n+            {\"op_size\": 3, \"op_time\": 0.2},\n+            {\"op_size\": 7, \"op_time\": 0.9},\n+        ]\n+        assert offload_connector_stats.data[\"GPU_to_CPU\"] == [\n+            {\"op_size\": 1, \"op_time\": 0.1},\n+            {\"op_size\": 2, \"op_time\": 0.2},\n+            {\"op_size\": 16, \"op_time\": 2},\n+        ]\n+\n+    def test_reduce(self):\n+        \"\"\"Test that reduce() correctly reduces all nested connector stats.\"\"\"\n+        stats = OffloadingConnectorStats(\n+            data={\n+                \"CPU_to_GPU\": [\n+                    {\"op_size\": 16, \"op_time\": 1.0},\n+                    {\"op_size\": 8, \"op_time\": 0.5},\n+                    {\"op_size\": 3, \"op_time\": 0.2},\n+                    {\"op_size\": 7, \"op_time\": 0.9},\n+                ],\n+                \"GPU_to_CPU\": [\n+                    {\"op_size\": 1, \"op_time\": 0.1},\n+                    {\"op_size\": 2, \"op_time\": 0.2},\n+                    {\"op_size\": 16, \"op_time\": 2},\n+                ],\n+            }\n+        )\n+\n+        reduced = stats.reduce()\n+\n+        assert isinstance(reduced, dict)\n+        # Check that the stats were reduced (should have aggregated values)\n+        assert \"CPU_to_GPU_total_bytes\" in reduced\n+        assert \"CPU_to_GPU_total_time\" in reduced\n+        assert \"GPU_to_CPU_total_bytes\" in reduced\n+        assert \"GPU_to_CPU_total_time\" in reduced\n+        assert reduced[\"CPU_to_GPU_total_bytes\"] == 34\n+        assert reduced[\"CPU_to_GPU_total_time\"] == 2.6\n+        assert reduced[\"GPU_to_CPU_total_time\"] == 2.3\n+        assert reduced[\"GPU_to_CPU_total_bytes\"] == 19\n+\n+    def test_reset(self):\n+        \"\"\"Test that reset() resets all nested connector stats.\"\"\"\n+        offload_connector_stats = OffloadingConnectorStats(\n+            data={\n+                \"CPU_to_GPU\": [\n+                    {\"op_size\": 3, \"op_time\": 0.2},\n+                    {\"op_size\": 7, \"op_time\": 0.9},\n+                ],\n+                \"GPU_to_CPU\": [{\"op_size\": 16, \"op_time\": 2}],\n+            }\n+        )\n+\n+        assert not offload_connector_stats.is_empty()\n+\n+        offload_connector_stats.reset()\n+\n+        # After reset, stats should be empty\n+        assert offload_connector_stats.is_empty()\n+        assert len(offload_connector_stats.data) == 0", "patch_sha256": "3b4a7692b70cb1ad"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#27942/review_2717169992", "source": "CodeBench", "source_task_id": "vllm-project/vllm#27942", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/v1/kv_offload/test_cpu_gpu.py\nReview Comment:\ncan you also assert `0 < finished[0].time < (time.time() - start_time)`?\n\nOriginal Code Context:\n@@ -169,14 +169,26 @@ def test_transfer(\n \n     # call transfer function\n     assert handler.transfer_async(1, (src_spec, dst_spec))\n-    assert set({x[0] for x in handler._transfers}) == {1}\n+    assert set({x.job_id for x in handler._transfers}) == {1}\n \n     # wait for transfer to complete\n     end_time = time.time() + 10\n     while time.time() < end_time:\n         finished = handler.get_finished()\n         if finished:\n-            assert finished == [(1, True)]\n+            assert finished[0].job_id == 1\n+            assert finished[0].success\n+            assert (\n+                finished[0].transfer_type == (\"GPU\", \"CPU\")\n+                if gpu_to_cpu\n+                else (\"CPU\", \"GPU\")\n+            )\n+            assert (\n+                finished[0].transfer_size\n+                == handler.block_size_in_bytes\n+                * handler.dst_block_size_factor\n+                * len(dst_blocks)\n+            )\n", "materials": ["can you also assert `0 < finished[0].time < (time.time() - start_time)`?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -168,15 +168,30 @@ def test_transfer(\n     orig_dst_caches = [x.clone() for x in handler.dst_tensors]\n \n     # call transfer function\n+    start_time = time.time()\n     assert handler.transfer_async(1, (src_spec, dst_spec))\n-    assert set({x[0] for x in handler._transfers}) == {1}\n+    assert set({x.job_id for x in handler._transfers}) == {1}\n \n     # wait for transfer to complete\n     end_time = time.time() + 10\n     while time.time() < end_time:\n         finished = handler.get_finished()\n         if finished:\n-            assert finished == [(1, True)]\n+            assert finished[0].job_id == 1\n+            assert finished[0].success\n+            assert (\n+                finished[0].transfer_type == (\"GPU\", \"CPU\")\n+                if gpu_to_cpu\n+                else (\"CPU\", \"GPU\")\n+            )\n+            assert (\n+                finished[0].transfer_size\n+                == handler.total_block_size_in_bytes\n+                * handler.dst_block_size_factor\n+                * len(dst_blocks)\n+            )\n+            assert finished[0].transfer_time > 0\n+            assert finished[0].transfer_time < (time.time() - start_time)\n             break\n         time.sleep(0.1)\n ", "patch_sha256": "e0d2ed8f8f98ffb3"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28664/review_2546647378", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28664", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/kernels/moe/modular_kernel_tools/mk_objects.py\nReview Comment:\nnit: I think it would be better to make the support checks asserts under each branch rather than making the construction conditional on them.\n\nOriginal Code Context:\n@@ -449,34 +482,34 @@ def make_fused_experts(\n \n     torch.set_printoptions(threshold=0, edgeitems=0, linewidth=10000)\n \n-    if fused_experts_type == BatchedDeepGemmExperts:\n+    if fused_experts_type.__name__ == \"BatchedDeepGemmExperts\":\n         kwargs = batch_kwargs | quant_kwargs\n         print(f\"Making BatchedDeepGemmExperts {kwargs} ...\")\n         experts = BatchedDeepGemmExperts(**kwargs)\n-    elif fused_experts_type == BatchedTritonExperts:\n+    elif fused_experts_type.__name__ == \"BatchedTritonExperts\":\n         kwargs = batch_kwargs | quant_kwargs\n         print(f\"Making BatchedTritonExperts {kwargs} ...\")\n         experts = BatchedTritonExperts(**kwargs)\n-    elif fused_experts_type == BatchedTritonOrDeepGemmExperts:\n+    elif fused_experts_type.__name__ == \"BatchedTritonOrDeepGemmExperts\":\n         kwargs = batch_kwargs | quant_kwargs | deepgemm_kwargs\n         print(f\"Making BatchedTritonOrDeepGemmExperts {kwargs} ...\")\n         experts = BatchedTritonOrDeepGemmExperts(**kwargs)\n-    elif fused_experts_type == DeepGemmExperts:\n+    elif fused_experts_type.__name__ == \"DeepGemmExperts\":\n         print(f\"Making DeepGemmExperts {quant_config} ...\")\n         experts = DeepGemmExperts(quant_config)\n-    elif fused_experts_type == TritonExperts:\n+    elif fused_experts_type.__name__ == \"TritonExperts\":\n         kwargs = quant_kwargs\n         print(f\"Making TritonExperts {kwargs} ...\")\n         experts = TritonExperts(**kwargs)\n-    elif fused_experts_type == TritonOrDeepGemmExperts:\n+    elif fused_experts_type.__name__ == \"TritonOrDeepGemmExperts\":\n         kwargs = quant_kwargs | deepgemm_kwargs\n         print(f\"Making TritonOrDeepGemmExperts {kwargs} ...\")\n         experts = TritonOrDeepGemmExperts(**kwargs)\n-    elif fused_experts_type == NaiveBatchedExperts:\n+    elif fused_experts_type.__name__ == \"NaiveBatchedExperts\":\n         kwargs = batch_kwargs | quant_kwargs\n         print(f\"Making NaiveBatchedExperts {kwargs} ...\")\n         experts = NaiveBatchedExperts(**kwargs)\n-    elif fused_experts_type == CutlassExpertsFp8:\n+    elif cutlass_fp8_supported() and fused_experts_type.__name__ == \"CutlassExpertsFp8\":\n", "materials": ["nit: I think it would be better to make the support checks asserts under each branch rather than making the construction conditional on them."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -37,7 +37,13 @@\n from vllm.platforms import current_platform\n from vllm.utils.deep_gemm import is_deep_gemm_supported\n from vllm.utils.flashinfer import has_flashinfer_cutlass_fused_moe\n-from vllm.utils.import_utils import has_deep_ep, has_deep_gemm, has_pplx\n+from vllm.utils.import_utils import (\n+    has_aiter,\n+    has_deep_ep,\n+    has_deep_gemm,\n+    has_mori,\n+    has_pplx,\n+)\n \n \n @dataclass\n@@ -66,6 +72,7 @@ class ExpertInfo:\n     supports_expert_map: bool\n     needs_matching_quant: bool = False\n     needs_deep_gemm: bool = False\n+    needs_aiter: bool = False\n \n \n PREPARE_FINALIZE_INFO: dict[mk.FusedMoEPrepareAndFinalize, PrepareFinalizeInfo] = {}\n@@ -126,6 +133,7 @@ def register_experts(\n     supports_expert_map: bool,\n     needs_matching_quant: bool = False,\n     needs_deep_gemm: bool = False,\n+    needs_aiter: bool = False,\n ):\n     global EXPERT_INFO\n     global MK_FUSED_EXPERT_TYPES\n@@ -139,6 +147,7 @@ def register_experts(\n         supports_expert_map,\n         needs_matching_quant,\n         needs_deep_gemm,\n+        needs_aiter,\n     )\n \n     MK_FUSED_EXPERT_TYPES.append(kind)\n@@ -218,6 +227,20 @@ def expert_info(kind) -> ExpertInfo:\n         backend=\"deepep_low_latency\",\n     )\n \n+if has_mori():\n+    from vllm.model_executor.layers.fused_moe.mori_prepare_finalize import (\n+        MoriPrepareAndFinalize,\n+    )\n+\n+    register_prepare_and_finalize(\n+        MoriPrepareAndFinalize,\n+        standard_format,\n+        fp8_types,\n+        blocked_quantization_support=True,\n+        backend=\"mori\",\n+        supports_apply_weight_on_input=False,\n+    )\n+\n if has_pplx():\n     from vllm.model_executor.layers.fused_moe.pplx_prepare_finalize import (\n         PplxPrepareAndFinalize,\n@@ -261,6 +284,25 @@ def expert_info(kind) -> ExpertInfo:\n     )\n else:\n     FlashInferCutlassMoEPrepareAndFinalize = None\n+    FlashInferExperts = None\n+\n+\n+if has_aiter():\n+    from vllm.model_executor.layers.fused_moe.rocm_aiter_fused_moe import (\n+        AiterExperts,\n+    )\n+\n+    register_experts(\n+        AiterExperts,\n+        standard_format,\n+        fp8_types,\n+        blocked_quantization_support=True,\n+        supports_chunking=True,\n+        supports_expert_map=True,\n+        needs_aiter=True,\n+    )\n+else:\n+    AiterExperts = None\n \n if has_deep_gemm() and is_deep_gemm_supported():\n     register_experts(\n@@ -316,6 +358,9 @@ def expert_info(kind) -> ExpertInfo:\n         supports_chunking=False,\n         supports_expert_map=False,\n     )\n+else:\n+    CutlassBatchedExpertsFp8 = None\n+    CutlassExpertsFp8 = None\n \n if cutlass_fp4_supported():\n     from vllm.model_executor.layers.fused_moe.cutlass_moe import CutlassExpertsFp4\n@@ -328,6 +373,8 @@ def expert_info(kind) -> ExpertInfo:\n         supports_chunking=True,\n         supports_expert_map=False,\n     )\n+else:\n+    CutlassExpertsFp4 = None\n \n MK_QUANT_CONFIGS: list[TestMoEQuantConfig | None] = [\n     None,", "patch_sha256": "8db90861a475088d"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28672/review_2751299579", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28672", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/compile/test_sequence_parallelism_threshold.py\nReview Comment:\nCan we make this a global pytest fixture? I feel like checking type and compute capability of platform is very common. \n\nOriginal Code Context:\n@@ -0,0 +1,137 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+from unittest.mock import MagicMock, patch\n+\n+import pytest\n+\n+from vllm.compilation.sequence_parallelism import (\n+    SP_MIN_HIDDEN_SIZE,\n+    SP_MIN_PER_GPU_SIZE_MB,\n+    get_sequence_parallelism_threshold,\n+)\n+from vllm.platforms.interface import DeviceCapability\n+\n+\n+class TestGetSequenceParallelismThreshold:\n+    \"\"\"Tests for get_sequence_parallelism_threshold function.\"\"\"\n+\n+    def test_non_cuda_returns_none(self):\n+        \"\"\"Non-CUDA platforms should return None.\"\"\"\n+        mock_platform = MagicMock()\n+        mock_platform.is_cuda.return_value = False\n+\n+        # Patch at vllm.platforms since the function does a local import\n+        with patch(\"vllm.platforms.current_platform\", mock_platform):\n+            result = get_sequence_parallelism_threshold(\n+                hidden_size=8192, tp_size=2, element_size=2\n+            )\n+        assert result is None\n+\n+    def test_unsupported_device_capability_returns_none(self):\n+        \"\"\"Unsupported device capabilities (e.g., sm80) should return None.\"\"\"\n+        mock_platform = MagicMock()\n+        mock_platform.is_cuda.return_value = True\n+        mock_platform.get_device_capability.return_value = DeviceCapability(8, 0)\n+\n+        with patch(\"vllm.platforms.current_platform\", mock_platform):\n+            result = get_sequence_parallelism_threshold(\n+                hidden_size=8192, tp_size=2, element_size=2\n+            )\n+        assert result is None\n+\n+    def test_small_hidden_size_returns_none(self):\n+        \"\"\"H100 with hidden_size below threshold should return None.\"\"\"\n+        mock_platform = MagicMock()\n", "materials": ["Can we make this a global pytest fixture? I feel like checking type and compute capability of platform is very common. "], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -0,0 +1,110 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+import pytest\n+\n+from vllm.compilation.passes.fusion.sequence_parallelism import (\n+    SP_MIN_HIDDEN_SIZE,\n+    SP_MIN_PER_GPU_SIZE_MB,\n+    get_sequence_parallelism_threshold,\n+)\n+\n+\n+class TestGetSequenceParallelismThreshold:\n+    \"\"\"Tests for get_sequence_parallelism_threshold function.\"\"\"\n+\n+    def test_non_cuda_returns_none(self, mock_cuda_platform):\n+        \"\"\"Non-CUDA platforms should return None.\"\"\"\n+        with mock_cuda_platform(is_cuda=False):\n+            result = get_sequence_parallelism_threshold(\n+                hidden_size=8192, tp_size=2, element_size=2\n+            )\n+        assert result is None\n+\n+    def test_unsupported_device_capability_returns_none(self, mock_cuda_platform):\n+        \"\"\"Unsupported device capabilities (e.g., sm80) should return None.\"\"\"\n+        with mock_cuda_platform(capability=(8, 0)):\n+            result = get_sequence_parallelism_threshold(\n+                hidden_size=8192, tp_size=2, element_size=2\n+            )\n+        assert result is None\n+\n+    def test_small_hidden_size_returns_none(self, mock_cuda_platform):\n+        \"\"\"H100 with hidden_size below threshold should return None.\"\"\"\n+        with mock_cuda_platform(capability=(9, 0)):\n+            result = get_sequence_parallelism_threshold(\n+                hidden_size=4096,\n+                tp_size=2,\n+                element_size=2,  # 4096 < 8192\n+            )\n+        assert result is None\n+\n+    def test_h100_large_model_returns_threshold(self, mock_cuda_platform):\n+        \"\"\"H100 with large enough hidden_size should return calculated threshold.\"\"\"\n+        with mock_cuda_platform(capability=(9, 0)):\n+            hidden_size = 8192\n+            tp_size = 2\n+            element_size = 2  # float16/bfloat16\n+\n+            result = get_sequence_parallelism_threshold(\n+                hidden_size=hidden_size,\n+                tp_size=tp_size,\n+                element_size=element_size,\n+            )\n+\n+            # Verify calculation: (8 * 2 * 1024 * 1024) // (8192 * 2) = 1024\n+            MiB = 1024 * 1024\n+            expected = int(\n+                (SP_MIN_PER_GPU_SIZE_MB[90] * tp_size * MiB)\n+                // (hidden_size * element_size)\n+            )\n+            assert result == expected\n+            assert result == 1024\n+\n+    @pytest.mark.parametrize(\n+        \"hidden_size,tp_size,element_size,expected\",\n+        [\n+            # Boundary: exactly at min hidden size threshold, tp_size=1\n+            # (8 * 1 * 1024 * 1024) // (8192 * 2) = 512\n+            (8192, 1, 2, 512),\n+            # Larger hidden size reduces token threshold\n+            # (8 * 1 * 1024 * 1024) // (16384 * 2) = 256\n+            (16384, 1, 2, 256),\n+            # Larger tp_size increases token threshold\n+            # (8 * 4 * 1024 * 1024) // (8192 * 2) = 2048\n+            (8192, 4, 2, 2048),\n+            # Larger element_size (fp32) reduces token threshold\n+            # (8 * 2 * 1024 * 1024) // (8192 * 4) = 512\n+            (8192, 2, 4, 512),\n+        ],\n+    )\n+    def test_threshold_calculation_variations(\n+        self, mock_cuda_platform, hidden_size, tp_size, element_size, expected\n+    ):\n+        \"\"\"Test threshold calculation with various parameter combinations.\"\"\"\n+        with mock_cuda_platform(capability=(9, 0)):\n+            result = get_sequence_parallelism_threshold(\n+                hidden_size=hidden_size,\n+                tp_size=tp_size,\n+                element_size=element_size,\n+            )\n+            assert result == expected\n+\n+    def test_hidden_size_boundary(self, mock_cuda_platform):\n+        \"\"\"Test behavior at the exact hidden_size boundary.\"\"\"\n+        with mock_cuda_platform(capability=(9, 0)):\n+            # Just below threshold\n+            result = get_sequence_parallelism_threshold(\n+                hidden_size=SP_MIN_HIDDEN_SIZE[90] - 1,\n+                tp_size=2,\n+                element_size=2,\n+            )\n+            assert result is None\n+\n+            # Exactly at threshold\n+            result = get_sequence_parallelism_threshold(\n+                hidden_size=SP_MIN_HIDDEN_SIZE[90],\n+                tp_size=2,\n+                element_size=2,\n+            )\n+            assert result is not None", "patch_sha256": "9321454c45ade7c6"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28672/review_2775418832", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28672", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/compile/fusions_e2e/conftest.py\nReview Comment:\nWe don't have any cases where we get 3 ranges?\n\nOriginal Code Context:\n@@ -107,8 +107,14 @@ def run(\n \n         # Now check the matches\n         for match_name in matches_check:\n+            # AR+RMS fusion applies to small batch ranges only, SP applies to\n+            # large batch ranges only. With 2 compile ranges, each activates\n+            # on exactly 1 range.\n             num_ranges_activated = (\n-                1 if match_name == \"ar_rms_fusion\" else num_compile_ranges\n+                1\n+                if match_name in (\"ar_rms_fusion\", \"sequence_parallel\")\n+                and num_compile_ranges == 2\n", "materials": ["We don't have any cases where we get 3 ranges?"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -94,7 +94,7 @@ def run(\n             run_model(full_compilation_config, model_name, **model_kwargs)\n \n         num_compile_ranges = len(full_compilation_config.get_compile_ranges())\n-        assert num_compile_ranges in [1, 2]\n+        assert num_compile_ranges in [1, 2, 3]\n \n         print(f\"Compile ranges: {full_compilation_config.get_compile_ranges()}\")\n         print(\"Fusion results:\")\n@@ -107,12 +107,33 @@ def run(\n \n         # Now check the matches\n         for match_name in matches_check:\n-            num_ranges_activated = (\n-                1 if match_name == \"ar_rms_fusion\" else num_compile_ranges\n-            )\n-            n_expected = tp_size * num_ranges_activated\n-\n             log_matches = list(int(ms) for ms in log_matches_dict[match_name])\n+\n+            # AR+RMS skips the largest range; SP skips the smallest.\n+            # When both are enabled, AR+RMS activation count is\n+            # model-dependent (hidden_size affects threshold), so derive\n+            # from log data.\n+            if (\n+                match_name == \"ar_rms_fusion\"\n+                and \"sequence_parallel\" in matches_check\n+                and num_compile_ranges >= 2\n+            ):\n+                assert (\n+                    len(log_matches) >= tp_size and len(log_matches) % tp_size == 0\n+                ), (\n+                    f\"Expected multiple of {tp_size} ar_rms log entries, \"\n+                    f\"found {len(log_matches)}\"\n+                )\n+                num_ranges_activated = len(log_matches) // tp_size\n+            elif (\n+                match_name in (\"ar_rms_fusion\", \"sequence_parallel\")\n+                and num_compile_ranges >= 2\n+            ):\n+                num_ranges_activated = num_compile_ranges - 1\n+            else:\n+                num_ranges_activated = num_compile_ranges\n+\n+            n_expected = tp_size * num_ranges_activated\n             assert len(log_matches) == n_expected, (\n                 f\"Could not find {n_expected} {match_name} \"\n                 f\"(found {len(log_matches)}) in:\\n {log_holder.text}\"\n@@ -122,8 +143,8 @@ def run(\n \n             if match_name == \"rms_quant_fusion\" and \"ar_rms_fusion\" in matches_check:\n                 # AR+rms+quant takes precedence over rms+quant if activated.\n-                # That means we get full matching where ar+rms+quant was not activated,\n-                # and less where it was\n+                # That means we get full matching where ar+rms+quant was not\n+                # activated, and less where it was (only the smallest range).\n                 assert sum(m == expected_matches for m in log_matches) == tp_size * (\n                     num_ranges_activated - 1\n                 ), \"Expecting full rms+quant fusion where ar+rms+quant not activated\"\n@@ -135,14 +156,51 @@ def run(\n                     f\"Expecting at least {expected_matches - matches.ar_rms_fusion} \"\n                     f\"where ar+rms+quant was activated\"\n                 )\n+            elif (\n+                match_name == \"async_tp\"\n+                and \"sequence_parallel\" in matches_check\n+                and num_compile_ranges >= 2\n+            ):\n+                # AsyncTP only finds patterns on ranges where SP ran.\n+                n_sp_ranges = num_compile_ranges - 1\n+                assert (\n+                    sum(m == expected_matches for m in log_matches)\n+                    == tp_size * n_sp_ranges\n+                ), (\n+                    f\"Expecting {expected_matches} async_tp on \"\n+                    f\"{tp_size * n_sp_ranges} SP-range entries, \"\n+                    f\"found: {log_matches}\"\n+                )\n+                assert sum(m == 0 for m in log_matches) == tp_size, (\n+                    f\"Expecting 0 async_tp on {tp_size} small-range entries \"\n+                    f\"(no SP), found: {log_matches}\"\n+                )\n+            elif (\n+                match_name == \"ar_rms_fusion\"\n+                and \"sequence_parallel\" in matches_check\n+                and num_compile_ranges >= 2\n+            ):\n+                # SP consumes allreduce patterns first, so AR+RMS finds\n+                # full matches only on the smallest range (no SP).\n+                assert sum(m == expected_matches for m in log_matches) == tp_size, (\n+                    f\"Expecting {expected_matches} ar_rms on \"\n+                    f\"{tp_size} small-range entries, found: {log_matches}\"\n+                )\n+                assert sum(m == 0 for m in log_matches) == tp_size * (\n+                    num_ranges_activated - 1\n+                ), (\n+                    f\"Expecting 0 ar_rms on \"\n+                    f\"{tp_size * (num_ranges_activated - 1)} large-range \"\n+                    f\"entries (SP took precedence), found: {log_matches}\"\n+                )\n             else:\n                 expected_matches_list = [expected_matches] * n_expected\n                 assert sorted(log_matches) == expected_matches_list, (\n                     f\"{match_name} expected: {expected_matches_list}, \"\n                     f\"found: {sorted(log_matches)}\"\n                 )\n \n-            if match_name == \"ar_rms_fusion\":\n+            if match_name == \"ar_rms_fusion\" and num_compile_ranges >= 2:\n                 log_matches = re.findall(\n                     r\"pass_manager.py:\\d+] Skipping \"\n                     r\".*AllReduceFusionPass.* with compile range\",\n@@ -155,4 +213,17 @@ def run(\n                     f\"(found {len(log_matches)}) in:\\n {log_holder.text}\"\n                 )\n \n+            if match_name == \"sequence_parallel\" and num_compile_ranges >= 2:\n+                log_matches = re.findall(\n+                    r\"pass_manager.py:\\d+] Skipping \"\n+                    r\".*SequenceParallelismPass.* with compile range\",\n+                    log_holder.text,\n+                )\n+\n+                n_expected = tp_size * (num_compile_ranges - num_ranges_activated)\n+                assert len(log_matches) == n_expected, (\n+                    f'Could not find {n_expected} \"Skipping SequenceParallelismPass\" '\n+                    f\"(found {len(log_matches)}) in:\\n {log_holder.text}\"\n+                )\n+\n     return run", "patch_sha256": "14967ef7b49a9693"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#28672/review_2775429401", "source": "CodeBench", "source_task_id": "vllm-project/vllm#28672", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/compile/fusions_e2e/conftest.py\nReview Comment:\nOh right because we never do SP + AsyncTP + AR+RMS yet. We should add those tests as well.\n\nOriginal Code Context:\n@@ -107,8 +107,14 @@ def run(\n \n         # Now check the matches\n         for match_name in matches_check:\n+            # AR+RMS fusion applies to small batch ranges only, SP applies to\n+            # large batch ranges only. With 2 compile ranges, each activates\n+            # on exactly 1 range.\n             num_ranges_activated = (\n-                1 if match_name == \"ar_rms_fusion\" else num_compile_ranges\n+                1\n+                if match_name in (\"ar_rms_fusion\", \"sequence_parallel\")\n+                and num_compile_ranges == 2\n", "materials": ["Oh right because we never do SP + AsyncTP + AR+RMS yet. We should add those tests as well."], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -94,7 +94,7 @@ def run(\n             run_model(full_compilation_config, model_name, **model_kwargs)\n \n         num_compile_ranges = len(full_compilation_config.get_compile_ranges())\n-        assert num_compile_ranges in [1, 2]\n+        assert num_compile_ranges in [1, 2, 3]\n \n         print(f\"Compile ranges: {full_compilation_config.get_compile_ranges()}\")\n         print(\"Fusion results:\")\n@@ -107,12 +107,33 @@ def run(\n \n         # Now check the matches\n         for match_name in matches_check:\n-            num_ranges_activated = (\n-                1 if match_name == \"ar_rms_fusion\" else num_compile_ranges\n-            )\n-            n_expected = tp_size * num_ranges_activated\n-\n             log_matches = list(int(ms) for ms in log_matches_dict[match_name])\n+\n+            # AR+RMS skips the largest range; SP skips the smallest.\n+            # When both are enabled, AR+RMS activation count is\n+            # model-dependent (hidden_size affects threshold), so derive\n+            # from log data.\n+            if (\n+                match_name == \"ar_rms_fusion\"\n+                and \"sequence_parallel\" in matches_check\n+                and num_compile_ranges >= 2\n+            ):\n+                assert (\n+                    len(log_matches) >= tp_size and len(log_matches) % tp_size == 0\n+                ), (\n+                    f\"Expected multiple of {tp_size} ar_rms log entries, \"\n+                    f\"found {len(log_matches)}\"\n+                )\n+                num_ranges_activated = len(log_matches) // tp_size\n+            elif (\n+                match_name in (\"ar_rms_fusion\", \"sequence_parallel\")\n+                and num_compile_ranges >= 2\n+            ):\n+                num_ranges_activated = num_compile_ranges - 1\n+            else:\n+                num_ranges_activated = num_compile_ranges\n+\n+            n_expected = tp_size * num_ranges_activated\n             assert len(log_matches) == n_expected, (\n                 f\"Could not find {n_expected} {match_name} \"\n                 f\"(found {len(log_matches)}) in:\\n {log_holder.text}\"\n@@ -122,8 +143,8 @@ def run(\n \n             if match_name == \"rms_quant_fusion\" and \"ar_rms_fusion\" in matches_check:\n                 # AR+rms+quant takes precedence over rms+quant if activated.\n-                # That means we get full matching where ar+rms+quant was not activated,\n-                # and less where it was\n+                # That means we get full matching where ar+rms+quant was not\n+                # activated, and less where it was (only the smallest range).\n                 assert sum(m == expected_matches for m in log_matches) == tp_size * (\n                     num_ranges_activated - 1\n                 ), \"Expecting full rms+quant fusion where ar+rms+quant not activated\"\n@@ -135,14 +156,51 @@ def run(\n                     f\"Expecting at least {expected_matches - matches.ar_rms_fusion} \"\n                     f\"where ar+rms+quant was activated\"\n                 )\n+            elif (\n+                match_name == \"async_tp\"\n+                and \"sequence_parallel\" in matches_check\n+                and num_compile_ranges >= 2\n+            ):\n+                # AsyncTP only finds patterns on ranges where SP ran.\n+                n_sp_ranges = num_compile_ranges - 1\n+                assert (\n+                    sum(m == expected_matches for m in log_matches)\n+                    == tp_size * n_sp_ranges\n+                ), (\n+                    f\"Expecting {expected_matches} async_tp on \"\n+                    f\"{tp_size * n_sp_ranges} SP-range entries, \"\n+                    f\"found: {log_matches}\"\n+                )\n+                assert sum(m == 0 for m in log_matches) == tp_size, (\n+                    f\"Expecting 0 async_tp on {tp_size} small-range entries \"\n+                    f\"(no SP), found: {log_matches}\"\n+                )\n+            elif (\n+                match_name == \"ar_rms_fusion\"\n+                and \"sequence_parallel\" in matches_check\n+                and num_compile_ranges >= 2\n+            ):\n+                # SP consumes allreduce patterns first, so AR+RMS finds\n+                # full matches only on the smallest range (no SP).\n+                assert sum(m == expected_matches for m in log_matches) == tp_size, (\n+                    f\"Expecting {expected_matches} ar_rms on \"\n+                    f\"{tp_size} small-range entries, found: {log_matches}\"\n+                )\n+                assert sum(m == 0 for m in log_matches) == tp_size * (\n+                    num_ranges_activated - 1\n+                ), (\n+                    f\"Expecting 0 ar_rms on \"\n+                    f\"{tp_size * (num_ranges_activated - 1)} large-range \"\n+                    f\"entries (SP took precedence), found: {log_matches}\"\n+                )\n             else:\n                 expected_matches_list = [expected_matches] * n_expected\n                 assert sorted(log_matches) == expected_matches_list, (\n                     f\"{match_name} expected: {expected_matches_list}, \"\n                     f\"found: {sorted(log_matches)}\"\n                 )\n \n-            if match_name == \"ar_rms_fusion\":\n+            if match_name == \"ar_rms_fusion\" and num_compile_ranges >= 2:\n                 log_matches = re.findall(\n                     r\"pass_manager.py:\\d+] Skipping \"\n                     r\".*AllReduceFusionPass.* with compile range\",\n@@ -155,4 +213,17 @@ def run(\n                     f\"(found {len(log_matches)}) in:\\n {log_holder.text}\"\n                 )\n \n+            if match_name == \"sequence_parallel\" and num_compile_ranges >= 2:\n+                log_matches = re.findall(\n+                    r\"pass_manager.py:\\d+] Skipping \"\n+                    r\".*SequenceParallelismPass.* with compile range\",\n+                    log_holder.text,\n+                )\n+\n+                n_expected = tp_size * (num_compile_ranges - num_ranges_activated)\n+                assert len(log_matches) == n_expected, (\n+                    f'Could not find {n_expected} \"Skipping SequenceParallelismPass\" '\n+                    f\"(found {len(log_matches)}) in:\\n {log_holder.text}\"\n+                )\n+\n     return run", "patch_sha256": "14967ef7b49a9693"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29087/review_2682074023", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29087", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/v1/kv_connector/unit/test_offloading_connector.py\nReview Comment:\n### Test assertion compares dict reference to itself\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe test stores a reference to `handler.transfer_specs` at line 689, then later asserts `transfer_specs == runner.offloading_spec.handler.transfer_specs` at lines 701 and 713. Since both variables point to the same dict object (not a copy), this comparison always returns `True` regardless of whether new entries were added. The test cannot actually verify that redundant loads were prevented - it would pass even if the second request triggered an unwanted load. The assignment should create a copy like `dict(runner.offloading_spec.handler.transfer_specs)` or compare the length instead.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\ntests/v1/kv_connector/unit/test_offloading_connector.py#L688-L701\ntests/v1/kv_connector/unit/test_offloading_connector.py#L712-L713\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`tests/v1/kv_connector/unit/test_offloading_connector.py#L712-L713`](https://github.com/vllm-project/vllm/blob/75825f82eff921c6c40a44247f3939d99f4e4f72/tests/v1/kv_connector/unit/test_offloading_connector.py#L712-L713)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..lcy2iQ6Z92E2IaDwmXQkg64_Vu6fdaNMdtQb61oKyTIcXz1mrr_Pm0bIILNZQmU_axs1b68KJeRMKC3o6lpA4U2fCfb6eUGNxwBVcbz9YHaSmASETUdtcCBtK6HMLN5ANq2up3RB8Os-5j7BSWr47v2QoN9Ym8fh_WQW7fjq9edJybm1KZf94QDXm3gXdFfdqOsL04PQ_StCbr2omMmVaSjS-iferYtqOSXbLRhE7UDZAjwLoi0qn9ootXhdSZYpGLClYCyEYR4_rpYcT1Q6EmVtsRMKftVCzOfGRXaDIDpcFoeU7yLEi8apTCxm-IbTsCZbXv29P3xcSBShY4gbXg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..pXWBede3zAik6jliKZjdN8wPMqyyhiR73v-4TU17e1ihtyYVQBtM9emgVG6pKpgSeibhTk5lF56WxdjnCKrd31qeIcbFBO9kq0csrGZv7Ztc0tS9jfx7aU9LHRYu3dLJd-bOimejnT-XGPbeOkWPySxPndErZDHkCfH6QDZqVgVcLczIm8wxxxh2-xZ2YNZ3gQTJUS1Dpj75caBfnAj0eJ0Fy_69beGQQvYnUOmvrBnIerF0lzVNFh7oSflgjGXrahfuALM8N7EEf1A8Snc6oEx4KBaRMt32-H11ZUhb1qXPAGS0CjdjD2iI2tGjy2WwvYH700fCzdicE-btpxj5kA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -650,3 +653,61 @@ def test_request_preemption(request_runner):\n         decoded_tokens=[EOS_TOKEN_ID],\n         expected_stored_gpu_block_indexes=(9, 10, 11),\n     )\n+\n+\n+def test_concurrent_lookups_of_the_same_prefix(request_runner):\n+    offloaded_block_size = 12\n+    gpu_block_size = 4\n+    num_gpu_blocks = 100\n+\n+    runner = request_runner(\n+        offloaded_block_size=offloaded_block_size,\n+        gpu_block_size=gpu_block_size,\n+        num_gpu_blocks=num_gpu_blocks,\n+    )\n+\n+    # store 1 blocks\n+    runner.new_request(token_ids=[0] * offloaded_block_size)\n+    runner.manager.prepare_store.side_effect = (\n+        lambda block_hashes: generate_store_output(block_hashes)\n+    )\n+    runner.run(\n+        decoded_tokens=[EOS_TOKEN_ID],\n+        expected_stored_gpu_block_indexes=(0, 1, 2),\n+    )\n+\n+    # start a request to load the first block, but don't complete\n+    runner.scheduler.reset_prefix_cache()\n+    runner.new_request(token_ids=[0] * offloaded_block_size)\n+    runner.manager.lookup.return_value = 1\n+    runner.run(\n+        decoded_tokens=[],\n+        complete_transfers=False,\n+    )\n+\n+    # request triggered a load\n+    transfer_specs = runner.offloading_spec.handler.transfer_specs\n+    assert transfer_specs\n+\n+    # start a new request to load the same first block\n+    runner.new_request(token_ids=[0] * offloaded_block_size)\n+    runner.manager.lookup.return_value = 1\n+    runner.run(\n+        decoded_tokens=[],\n+        complete_transfers=False,\n+    )\n+\n+    # request did not trigger a load\n+    assert transfer_specs == runner.offloading_spec.handler.transfer_specs\n", "materials": ["### Test assertion compares dict reference to itself\n\n**Medium Severity**\n\n<!-- DESCRIPTION START -->\nThe test stores a reference to `handler.transfer_specs` at line 689, then later asserts `transfer_specs == runner.offloading_spec.handler.transfer_specs` at lines 701 and 713. Since both variables point to the same dict object (not a copy), this comparison always returns `True` regardless of whether new entries were added. The test cannot actually verify that redundant loads were prevented - it would pass even if the second request triggered an unwanted load. The assignment should create a copy like `dict(runner.offloading_spec.handler.transfer_specs)` or compare the length instead.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\ntests/v1/kv_connector/unit/test_offloading_connector.py#L688-L701\ntests/v1/kv_connector/unit/test_offloading_connector.py#L712-L713\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`tests/v1/kv_connector/unit/test_offloading_connector.py#L712-L713`](https://github.com/vllm-project/vllm/blob/75825f82eff921c6c40a44247f3939d99f4e4f72/tests/v1/kv_connector/unit/test_offloading_connector.py#L712-L713)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..lcy2iQ6Z92E2IaDwmXQkg64_Vu6fdaNMdtQb61oKyTIcXz1mrr_Pm0bIILNZQmU_axs1b68KJeRMKC3o6lpA4U2fCfb6eUGNxwBVcbz9YHaSmASETUdtcCBtK6HMLN5ANq2up3RB8Os-5j7BSWr47v2QoN9Ym8fh_WQW7fjq9edJybm1KZf94QDXm3gXdFfdqOsL04PQ_StCbr2omMmVaSjS-iferYtqOSXbLRhE7UDZAjwLoi0qn9ootXhdSZYpGLClYCyEYR4_rpYcT1Q6EmVtsRMKftVCzOfGRXaDIDpcFoeU7yLEi8apTCxm-IbTsCZbXv29P3xcSBShY4gbXg\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..pXWBede3zAik6jliKZjdN8wPMqyyhiR73v-4TU17e1ihtyYVQBtM9emgVG6pKpgSeibhTk5lF56WxdjnCKrd31qeIcbFBO9kq0csrGZv7Ztc0tS9jfx7aU9LHRYu3dLJd-bOimejnT-XGPbeOkWPySxPndErZDHkCfH6QDZqVgVcLczIm8wxxxh2-xZ2YNZ3gQTJUS1Dpj75caBfnAj0eJ0Fy_69beGQQvYnUOmvrBnIerF0lzVNFh7oSflgjGXrahfuALM8N7EEf1A8Snc6oEx4KBaRMt32-H11ZUhb1qXPAGS0CjdjD2iI2tGjy2WwvYH700fCzdicE-btpxj5kA\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -213,7 +213,6 @@ def __init__(\n         )\n \n     def new_request(self, token_ids: list[int]):\n-        assert not self.scheduler.requests\n         self.req_id += 1\n \n         req = Request(\n@@ -338,11 +337,20 @@ def _run(self, decoded_tokens: list[int], complete_transfers: bool):\n                 token_id=token_id or 0,\n             )\n \n+            prev_token_id = token_id\n             if self.scheduler.running:\n                 token_id = next(tokens_iter, None)\n \n             self.scheduler.update_from_output(scheduler_output, model_runner_output)\n \n+            if (\n+                prev_token_id is EOS_TOKEN_ID\n+                and prev_token_id != token_id\n+                and self.scheduler.requests\n+            ):\n+                # continue for one more step to allow offloading to kick off\n+                continue\n+\n             if token_id is None:\n                 break\n \n@@ -651,3 +659,61 @@ def test_request_preemption(request_runner):\n         decoded_tokens=[EOS_TOKEN_ID],\n         expected_stored_gpu_block_indexes=(9, 10, 11),\n     )\n+\n+\n+def test_concurrent_lookups_of_the_same_prefix(request_runner):\n+    offloaded_block_size = 12\n+    gpu_block_size = 4\n+    num_gpu_blocks = 100\n+\n+    runner = request_runner(\n+        offloaded_block_size=offloaded_block_size,\n+        gpu_block_size=gpu_block_size,\n+        num_gpu_blocks=num_gpu_blocks,\n+    )\n+\n+    # store 1 blocks\n+    runner.new_request(token_ids=[0] * offloaded_block_size)\n+    runner.manager.prepare_store.side_effect = (\n+        lambda block_hashes: generate_store_output(block_hashes)\n+    )\n+    runner.run(\n+        decoded_tokens=[EOS_TOKEN_ID],\n+        expected_stored_gpu_block_indexes=(0, 1, 2),\n+    )\n+\n+    # start a request to load the first block, but don't complete\n+    runner.scheduler.reset_prefix_cache()\n+    runner.new_request(token_ids=[0] * offloaded_block_size)\n+    runner.manager.lookup.return_value = 1\n+    runner.run(\n+        decoded_tokens=[],\n+        complete_transfers=False,\n+    )\n+\n+    # request triggered a load\n+    transfer_jobs = list(runner.offloading_spec.handler.transfer_specs)\n+    assert transfer_jobs\n+\n+    # start a new request to load the same first block\n+    runner.new_request(token_ids=[0] * offloaded_block_size)\n+    runner.manager.lookup.return_value = 1\n+    runner.run(\n+        decoded_tokens=[],\n+        complete_transfers=False,\n+    )\n+\n+    # request did not trigger a load\n+    assert transfer_jobs == list(runner.offloading_spec.handler.transfer_specs)\n+\n+    # complete transfers\n+    runner.manager.prepare_store.side_effect = (\n+        lambda block_hashes: generate_store_output([])\n+    )\n+    runner.run(\n+        decoded_tokens=[EOS_TOKEN_ID],\n+        expected_loaded_gpu_block_indexes=(0, 1, 2),\n+    )\n+\n+    # second request will use the GPU prefix cache\n+    assert transfer_jobs == list(runner.offloading_spec.handler.transfer_specs)", "patch_sha256": "11b00187a0c16beb"}}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::train::vllm-project/vllm#29087/review_2683557316", "source": "CodeBench", "source_task_id": "vllm-project/vllm#29087", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/v1/kv_connector/unit/test_offloading_connector.py\nReview Comment:\n### Test loop may hang when no requests are running\n\n**Low Severity**\n\n<!-- DESCRIPTION START -->\nThe new `continue` statement at line 346-347 can cause an infinite loop in edge cases. The `token_id` is only advanced if `self.scheduler.running` is non-empty (line 341-342). If all requests are waiting (none running) but `scheduler.requests` is non-empty, and `token_id` is `EOS_TOKEN_ID`, then `token_id` never changes and the `continue` triggers indefinitely. The `if token_id is None: break` at line 349-350 is never reached because `token_id` stays as `EOS_TOKEN_ID`. While the current test cases don't exercise this path, the loop structure is fragile.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\ntests/v1/kv_connector/unit/test_offloading_connector.py#L345-L347\ntests/v1/kv_connector/unit/test_offloading_connector.py#L340-L342\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`tests/v1/kv_connector/unit/test_offloading_connector.py#L340-L342`](https://github.com/vllm-project/vllm/blob/f3450fdb8ccfb7e7ebf8ce468f7f15ef76959ed4/tests/v1/kv_connector/unit/test_offloading_connector.py#L340-L342)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..hBtG-9VV8wPVwmO021Gel6Nai3ukvlBdNI1c9NbykdnNPmqwWAauoVITkMkzrznG_5ZxM7oHtJ6UmhzgmcsP_cCbBXoC0VzrU0YT-_YxHPXJCdz1h78rZY-PixbVM_BbCEAYZTATDrJHNFKZjF9szxXXwVzizeabyMbY2NoznwdG53p0dyQq08j-DAT5flTfvQDt9n1xOvd0tGHEVv-0aJNNx2FjBOr-T7RLdKjTwX-g54EqHHHjJ5EXvl7HaEXrI7UgxNmYXk3guoGEPqwEm6ySfHYyr54c9GlieB_NrIFAFZKq8IrS8MTwSqq3ROPIy_8oLQKCxjf1T0PEwHsyZQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..lPQ_1aQkb_NU5QWVUUpcXh8aE6Ox9L0sl5M9W2K6Aqd7BDqGX3RPp3P3zWWp-e2tW9SZYetuWK0onsHA3BmVZIxipg51pQtu1iaZpx-NAbvHQTmwlkEVRgsIySEfWcoWXY6-aQvOuFQO4twFfgybmFTHWK5nX5NQooKKB_qkZEz3xxfe9F5hdGbuBtWxY4cjyt28DA55RKY9vnJcMH0pAdSNB3oGMX3CE3Ijki29RCqZk8L7rSd-uKKfasFvMNgfj8h1GZ-Y_PK6COScJVoiRw3zDJSPpnWY3dPQibvW6uTmv8pgDeNr3AVu9AoRFxBU--gxTm6gW2ze1qz8zI2_tw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n\n\nOriginal Code Context:\n@@ -338,11 +337,15 @@ def _run(self, decoded_tokens: list[int], complete_transfers: bool):\n                 token_id=token_id or 0,\n             )\n \n+            prev_token_id = token_id\n             if self.scheduler.running:\n                 token_id = next(tokens_iter, None)\n \n             self.scheduler.update_from_output(scheduler_output, model_runner_output)\n \n+            if prev_token_id is EOS_TOKEN_ID and self.scheduler.requests:\n+                continue\n", "materials": ["### Test loop may hang when no requests are running\n\n**Low Severity**\n\n<!-- DESCRIPTION START -->\nThe new `continue` statement at line 346-347 can cause an infinite loop in edge cases. The `token_id` is only advanced if `self.scheduler.running` is non-empty (line 341-342). If all requests are waiting (none running) but `scheduler.requests` is non-empty, and `token_id` is `EOS_TOKEN_ID`, then `token_id` never changes and the `continue` triggers indefinitely. The `if token_id is None: break` at line 349-350 is never reached because `token_id` stays as `EOS_TOKEN_ID`. While the current test cases don't exercise this path, the loop structure is fragile.\n<!-- DESCRIPTION END -->\n\n<!-- LOCATIONS START\ntests/v1/kv_connector/unit/test_offloading_connector.py#L345-L347\ntests/v1/kv_connector/unit/test_offloading_connector.py#L340-L342\nLOCATIONS END -->\n<details>\n<summary>Additional Locations (1)</summary>\n\n- [`tests/v1/kv_connector/unit/test_offloading_connector.py#L340-L342`](https://github.com/vllm-project/vllm/blob/f3450fdb8ccfb7e7ebf8ce468f7f15ef76959ed4/tests/v1/kv_connector/unit/test_offloading_connector.py#L340-L342)\n\n</details>\n\n<a href=\"https://cursor.com/open?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..hBtG-9VV8wPVwmO021Gel6Nai3ukvlBdNI1c9NbykdnNPmqwWAauoVITkMkzrznG_5ZxM7oHtJ6UmhzgmcsP_cCbBXoC0VzrU0YT-_YxHPXJCdz1h78rZY-PixbVM_BbCEAYZTATDrJHNFKZjF9szxXXwVzizeabyMbY2NoznwdG53p0dyQq08j-DAT5flTfvQDt9n1xOvd0tGHEVv-0aJNNx2FjBOr-T7RLdKjTwX-g54EqHHHjJ5EXvl7HaEXrI7UgxNmYXk3guoGEPqwEm6ySfHYyr54c9GlieB_NrIFAFZKq8IrS8MTwSqq3ROPIy_8oLQKCxjf1T0PEwHsyZQ\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-cursor-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-cursor-light.svg\"><img alt=\"Fix in Cursor\" src=\"https://cursor.com/fix-in-cursor.svg\"></picture></a>&nbsp;<a href=\"https://cursor.com/agents?data=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImJ1Z2JvdC12MiJ9..lPQ_1aQkb_NU5QWVUUpcXh8aE6Ox9L0sl5M9W2K6Aqd7BDqGX3RPp3P3zWWp-e2tW9SZYetuWK0onsHA3BmVZIxipg51pQtu1iaZpx-NAbvHQTmwlkEVRgsIySEfWcoWXY6-aQvOuFQO4twFfgybmFTHWK5nX5NQooKKB_qkZEz3xxfe9F5hdGbuBtWxY4cjyt28DA55RKY9vnJcMH0pAdSNB3oGMX3CE3Ijki29RCqZk8L7rSd-uKKfasFvMNgfj8h1GZ-Y_PK6COScJVoiRw3zDJSPpnWY3dPQibvW6uTmv8pgDeNr3AVu9AoRFxBU--gxTm6gW2ze1qz8zI2_tw\" target=\"_blank\" rel=\"noopener noreferrer\"><picture><source media=\"(prefers-color-scheme: dark)\" srcset=\"https://cursor.com/fix-in-web-dark.svg\"><source media=\"(prefers-color-scheme: light)\" srcset=\"https://cursor.com/fix-in-web-light.svg\"><img alt=\"Fix in Web\" src=\"https://cursor.com/fix-in-web.svg\"></picture></a>\n\n"], "expected_artifacts": ["code_patch"], "desired_output": {"schema_version": "extracted-patch/v1", "status": "extracted", "text": "@@ -213,7 +213,6 @@ def __init__(\n         )\n \n     def new_request(self, token_ids: list[int]):\n-        assert not self.scheduler.requests\n         self.req_id += 1\n \n         req = Request(\n@@ -338,11 +337,20 @@ def _run(self, decoded_tokens: list[int], complete_transfers: bool):\n                 token_id=token_id or 0,\n             )\n \n+            prev_token_id = token_id\n             if self.scheduler.running:\n                 token_id = next(tokens_iter, None)\n \n             self.scheduler.update_from_output(scheduler_output, model_runner_output)\n \n+            if (\n+                prev_token_id is EOS_TOKEN_ID\n+                and prev_token_id != token_id\n+                and self.scheduler.requests\n+            ):\n+                # continue for one more step to allow offloading to kick off\n+                continue\n+\n             if token_id is None:\n                 break\n \n@@ -651,3 +659,61 @@ def test_request_preemption(request_runner):\n         decoded_tokens=[EOS_TOKEN_ID],\n         expected_stored_gpu_block_indexes=(9, 10, 11),\n     )\n+\n+\n+def test_concurrent_lookups_of_the_same_prefix(request_runner):\n+    offloaded_block_size = 12\n+    gpu_block_size = 4\n+    num_gpu_blocks = 100\n+\n+    runner = request_runner(\n+        offloaded_block_size=offloaded_block_size,\n+        gpu_block_size=gpu_block_size,\n+        num_gpu_blocks=num_gpu_blocks,\n+    )\n+\n+    # store 1 blocks\n+    runner.new_request(token_ids=[0] * offloaded_block_size)\n+    runner.manager.prepare_store.side_effect = (\n+        lambda block_hashes: generate_store_output(block_hashes)\n+    )\n+    runner.run(\n+        decoded_tokens=[EOS_TOKEN_ID],\n+        expected_stored_gpu_block_indexes=(0, 1, 2),\n+    )\n+\n+    # start a request to load the first block, but don't complete\n+    runner.scheduler.reset_prefix_cache()\n+    runner.new_request(token_ids=[0] * offloaded_block_size)\n+    runner.manager.lookup.return_value = 1\n+    runner.run(\n+        decoded_tokens=[],\n+        complete_transfers=False,\n+    )\n+\n+    # request triggered a load\n+    transfer_jobs = list(runner.offloading_spec.handler.transfer_specs)\n+    assert transfer_jobs\n+\n+    # start a new request to load the same first block\n+    runner.new_request(token_ids=[0] * offloaded_block_size)\n+    runner.manager.lookup.return_value = 1\n+    runner.run(\n+        decoded_tokens=[],\n+        complete_transfers=False,\n+    )\n+\n+    # request did not trigger a load\n+    assert transfer_jobs == list(runner.offloading_spec.handler.transfer_specs)\n+\n+    # complete transfers\n+    runner.manager.prepare_store.side_effect = (\n+        lambda block_hashes: generate_store_output([])\n+    )\n+    runner.run(\n+        decoded_tokens=[EOS_TOKEN_ID],\n+        expected_loaded_gpu_block_indexes=(0, 1, 2),\n+    )\n+\n+    # second request will use the GPU prefix cache\n+    assert transfer_jobs == list(runner.offloading_spec.handler.transfer_specs)", "patch_sha256": "11b00187a0c16beb"}}], "heldout_tasks": [{"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2902017642", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/v1/entrypoints/openai/test_thinking_token_budget.py\nReview Comment:\n I added an e2e test. (To run, `python -m pytest tests/v1/entrypoints/openai/test_thinking_token_budget.py`)\n\nLimiting the thinking token budget works with async scheduling, but achieving exact budget enforcement is difficult, because with async scheduling, output token IDs are not updated in sync with each token generation step. I think this issue could also be addressed by the @rishitdholakia13 's following PR (https://github.com/vllm-project/vllm/pull/34668), which aims to enable this feature with speculative decoding. It is also a case where more than one token can be generated per step.\n\nOriginal Code Context:\n@@ -0,0 +1,87 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\"\"\"E2E tests for thinking_token_budget with reasoning models.\"\"\"\n+\n+import openai\n+import pytest\n+import pytest_asyncio\n+\n+from tests.utils import RemoteOpenAIServer\n+\n+MODEL_NAME = \"Qwen/Qwen3-0.6B\"\n+MESSAGES = [{\"role\": \"user\", \"content\": \"What is 1+1? Be concise.\"}]\n+THINK_BUDGET = 5\n+\n+\n+@pytest.fixture(scope=\"module\")\n+def server():\n+    args = [\n+        \"--reasoning-parser\",\n+        \"qwen3\",\n+        \"--reasoning-config\",\n+        '{\"think_start_str\": \"<think>\", \"think_end_str\": \"</think>\"}',\n+        \"--max-model-len\",\n+        \"2048\",\n+        \"--enforce-eager\",\n+        \"--no-async-scheduling\",\n", "materials": [" I added an e2e test. (To run, `python -m pytest tests/v1/entrypoints/openai/test_thinking_token_budget.py`)\n\nLimiting the thinking token budget works with async scheduling, but achieving exact budget enforcement is difficult, because with async scheduling, output token IDs are not updated in sync with each token generation step. I think this issue could also be addressed by the @rishitdholakia13 's following PR (https://github.com/vllm-project/vllm/pull/34668), which aims to enable this feature with speculative decoding. It is also a case where more than one token can be generated per step."], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#20859/review_2902020466", "source": "CodeBench", "source_task_id": "vllm-project/vllm#20859", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: tests/v1/entrypoints/openai/test_thinking_token_budget.py\nReview Comment:\nYes, i have addressed the issue in the spec + thinking budget PR and added e2e tests as well that, ensure the exact thinking budget limit is enforced with spec and non spec mode while hsing both sync and async\n\nOriginal Code Context:\n@@ -0,0 +1,87 @@\n+# SPDX-License-Identifier: Apache-2.0\n+# SPDX-FileCopyrightText: Copyright contributors to the vLLM project\n+\n+\"\"\"E2E tests for thinking_token_budget with reasoning models.\"\"\"\n+\n+import openai\n+import pytest\n+import pytest_asyncio\n+\n+from tests.utils import RemoteOpenAIServer\n+\n+MODEL_NAME = \"Qwen/Qwen3-0.6B\"\n+MESSAGES = [{\"role\": \"user\", \"content\": \"What is 1+1? Be concise.\"}]\n+THINK_BUDGET = 5\n+\n+\n+@pytest.fixture(scope=\"module\")\n+def server():\n+    args = [\n+        \"--reasoning-parser\",\n+        \"qwen3\",\n+        \"--reasoning-config\",\n+        '{\"think_start_str\": \"<think>\", \"think_end_str\": \"</think>\"}',\n+        \"--max-model-len\",\n+        \"2048\",\n+        \"--enforce-eager\",\n+        \"--no-async-scheduling\",\n", "materials": ["Yes, i have addressed the issue in the spec + thinking budget PR and added e2e tests as well that, ensure the exact thinking budget limit is enforced with spec and non spec mode while hsing both sync and async"], "expected_artifacts": ["code_patch"], "desired_output": null}, {"schema_version": "example-pack/v1", "example_id": "vllm-project_vllm::heldout::vllm-project/vllm#24675/review_3032502075", "source": "CodeBench", "source_task_id": "vllm-project/vllm#24675", "domain": {"primary": "code", "secondary": "tests", "language": "python"}, "task_input": "Repository: vllm-project/vllm\nFile: .buildkite/test_areas/kernels.yaml\nReview Comment:\nthis should have optional: true\n\nOriginal Code Context:\n@@ -178,3 +178,20 @@ steps:\n     - pytest -v -s kernels/moe/test_flashinfer_moe.py\n     - pytest -v -s kernels/moe/test_nvfp4_moe.py\n     - pytest -v -s kernels/moe/test_ocp_mx_moe.py\n+\n+\n+- label: Kernels FusedMoE Layer Test (2 H100s)\n+  timeout_in_minutes: 90\n+  device: h100\n+  num_devices: 2\n", "materials": ["this should have optional: true"], "expected_artifacts": ["code_patch"], "desired_output": null}], "example_pack_status": {"desired_outputs_applied": 29, "desired_outputs_missing": 0, "desired_outputs_rejected": 0, "rejections": []}}
